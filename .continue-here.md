# Continue here — updated 2026-07-31 (overnight run). Good morning, Wyatt.

Everything below is on disk and pushed. Nothing depends on conversation memory.

## Read this first, it's the only thing that needs you

**`.planning/COPY-AND-TASTE-REVIEW.md`** — every taste call I made and the one player-facing string
I invented, itemised with rationale, for you to approve or edit. You said to make the calls and flag
them; that file is the flagging. **It is all already live on the branch** — nothing is waiting on
you to proceed, only to correct.

The rows most worth your eye:

- **B5** — UI-07 hides the narration box at end of voyage. This sits in tension with **F6, your own
  "the blue box should never be empty" rule.** I read F6 as governing play, not the end of the
  voyage. If you meant it to hold at EOV too, that's the row to reject.
- **C5** — a winning side bet may pay **one stake more** than "double or nothing" implies. Long
  standing, playtested, and I did **not** touch it. Your call.
- **A1** — the only invented string: the 🍪 on the Ko-Fi footer button. Your "Buy me a cookie" text
  is verbatim; the emoji is mine.

## State

- **Branch:** `july30-economy-crate-and-bet-fixes`, worktree `.claude/worktrees/new-gsd-project-e18e9f`
- **Base:** `main` @ `57ac568` — **PR #8 is MERGED and live on playpastrypirates.com**
- **`npm test`:** green, **18 gate scripts**, 23/23 assertion groups, 31/31 determinism seeds
- **`src/engine/index.js` still byte-identical to `9ddd214`.** `git diff 9ddd214..HEAD -- src/engine/`
  is empty. No re-record needed. Keep it that way.

## What landed overnight

### CR-02 / CR-03 — the two economy bugs (`9427de4`)

- **CR-03** — the flee "refund" is **deleted, not made real.** The todo proposed debiting the stake
  at collection; I checked and that was wrong. `settleSideBets` computes `won ? 1+2*amt : -amt`, so
  the losing arm *is* the stake — the maths is already consistent with no debit at collection.
  Debiting there would rewrite economics you've playtested. On a flee nothing was ever taken, so
  nothing should be given back.
- **CR-02** — fixed at the **root**, your way: the shot clock no longer takes a crate *or* coins.
  Both 30-second penalties are gone; the 20-second one (1🌕 to each other captain) still runs, as you
  chose. With no confiscation there's no vanishing crate.
- Kept anyway, because a timed-out partner must not auto-accept: `turnExpired` guards in
  `humanTrade`, and a shared `moveCrate()` that makes `splice(indexOf(x),1)` unwritable. **The sweep
  found a fifth site the code review never reported**, in the bot-hail settlement.
- Your copy, chosen from three drafts: **"Dozed at the helm!"**

### Phase 16 — UI/UX, Ko-Fi, host shortcut (`02f0262`, `8cd0b28`)

All of UI-01…07, META-01/02, KOFI-01. **META-01/02 turned out to be already done and live.** Each
change was measured in a real browser, not just written — numbers are in `17-VERIFICATION.md`.

The one to know about: **KOFI-01 uses your snippet's three values but calls `getHTML()`, not
`draw()`.** `draw()` is `document.writeln`, which after page load opens a *new* document — the first
click on Credits would have replaced the whole game with a lone Ko-Fi button. Verified: widget
mounts, page survives.

## What still needs you

1. **Read `COPY-AND-TASTE-REVIEW.md`** (above).
2. **Phase 17 — the Safari two-window playtest.** `17-VERIFICATION.md` is deliberately
   `human_needed`; its three success criteria are all "Safari, two windows, a human watching" and I
   closed none of them. What I *did* close (all automated gates, and 9 Chrome checks including a real
   Firebase room) is listed there so you don't redo it.
3. **UI-07 was gated, never watched.** Assertion 9 pins it, red-proofed against the real pre-change
   file, but I never saw the collapse on screen — two autoplay attempts failed to reach an
   end-of-voyage. Worth one look next time you finish a game, especially given the F6 tension.
4. **Open a PR when you're happy.** I did **not** open one and did **not** merge — you were asleep,
   and the last merge went straight to the public site.

## Found while verifying — recorded, not fixed

**`.planning/todos/pending/host-enabled-without-firebase-after-solo-resume.md`**

`boot()` resumes an in-progress solo game and returns **before** `fbInit()`, so `appState.db` is null
while Host/Join stay enabled and `#fbnote` never shows. Clicking Host then fires *"the server's got
too many pirates baking right now"* — untrue, and the same sentence a real capacity failure uses, so
the two are indistinguishable. **Pre-existing**; UI-05 shortens the path from two clicks to one. Left
for you because the honest fix has a copy half. (It also blocks the renderer, which is why it first
looked like a frozen tab.)

## Still open from before — do not rediscover as bugs

- **No shipped-vs-approved copy comparison.** 19 of 144 approval fields conclusively settled.
  `.planning/todos/pending/copy-shipped-vs-approved-gate.md` — still the most valuable thing left.
- **Two D-41 greyed states never eyeballed** — `— coins only —` and the hail Counter-offer.
- **G29** — the storm leg summary is unguarded in `src/ui/flow.js:549`/`:599`.
- **Four weak gates** named in `15-REVIEW.md`, incl. `ui_contract_check` assertion 6a inspecting zero
  chains, and that file still `process.exit()`s on import (WR-04) — red-proof it by file swap.
- **`captions()` has zero callers** — ~20 builders' `caps:[…]` is dead copy. Needs your ruling.

## Environment

Servers from tonight: **8370** served this worktree and is still running; 8350/8351 and everything
below are older. **Always use a fresh, never-loaded port** — Chrome caches ES modules per URL, and
that produced two phantom bugs in Phase 15. Two tabs share `localStorage`, so a guest seat needs a
separate profile or incognito, or both tabs collide on one `pp_id` — that's exactly what sent me
chasing a phantom host bug tonight.
