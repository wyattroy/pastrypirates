# Continue here — updated 2026-07-30, Phase 15 SHIPPED as PR #8

Everything below is on disk and pushed. Nothing depends on conversation memory.

## State

- **Branch:** `claude/confident-bassi-7263ea`, worktree `.claude/worktrees/new-gsd-project-e18e9f`
- **Remote:** `origin/july29-narration-playtest-and-audit-tool` — fully pushed
- **`npm test`:** green, 18 gate scripts, 31/31 determinism seeds
- **`src/engine/index.js`: byte-identical since before Phase 15 started.** `git diff 9ddd214..HEAD -- src/engine/` is empty. This is why none of this work needed a determinism re-record. Keep it that way.
- **Wyatt's ruling on history:** KEEP the `.planning/` commits in the merge. Do not use `/gsd-pr-branch` to strip them.

## Phase 15 — DONE, pending only the ship step

- **Re-verified: 19/19 must-haves.** `15-VERIFICATION.md` was rewritten; the old `gaps_found (14/19)` verdict is superseded and all five gaps are closed in code.
- **Code-reviewed** for the first time: `15-REVIEW.md`, 3 blockers / 14 warnings / 4 info.
- **All checkpoints closed**, including the six retired audit cards (Wyatt approved them 2026-07-30).
- **All three human-eye checks cleared:** guest highlight parity, the storm rain in Safari, the fade after his retune.
- Two recorded two-tab playtests; every fix confirmed in a live game.

### SHIPPED — PR #8

**https://github.com/wyattroy/pastrypirates/pull/8** — Phases 13, 14 and 15 against `main`, 403
commits, `.planning/` history kept per Wyatt's ruling. Open, awaiting his review and merge. No
external review command is configured and there are no `ship:post` hooks, so nothing else runs
automatically.

At the ship gate the verification status was `human_needed`, not `passed`. Wyatt was asked and chose
**"ship now, carry both."** The two open items are recorded in `15-VERIFICATION.md` as
`accepted-as-residual` — **carried, not cleared, and not claimed as verified**:

- the shipped-vs-approved copy comparison (19 of 144 approval fields conclusively settled) →
  `.planning/todos/pending/copy-shipped-vs-approved-gate.md`
- two D-41 greyed states never eyeballed →
  `.planning/todos/pending/d41-two-greyed-states-never-eyeballed.md`

The third human item (G28 pacing) is genuinely cleared — he watched it, and CR-01 later fixed the
250ms belt that had been defeating the fade he approved.

`/gsd-extract-learnings` finished; its nine learnings are committed at
`.planning/phases/15-narration-audit-fixes/15-LEARNINGS.md`. Worth reading before Phase 16 — #3
(partial fixes are the dominant failure mode) and #2 (a gate must be watched to fail) are the two
that keep paying.

## NEXT WORK, in Wyatt's stated order

1. **`.planning/todos/pending/economy-trade-and-flee-corruption.md`** — CR-02 (a trade can delete the
   wrong crate and mint one that never existed; `humanTrade` has no `turnExpired` guard) and CR-03
   (fleeing a battle credits side bets that were never debited, minting coins). **Both PRE-EXISTING
   since Phase 11**, which is why they did not block the merge. Same family as the coin audit; do them
   with its residue.
2. **Phase 16** — UI/UX polish, Open Graph preview, favicon, Ko-Fi button (UI-01…07, META-01/02,
   KOFI-01). Note UI-06 and D-55 already landed early, so it is smaller than the roadmap suggests.
   Mostly taste work — good candidate for sketching options rather than describing them.
3. **`docs/DETERMINISM-RERECORD-NEXT.md`** — the queued gated pass: engine purity (`spoil`/`gave`
   become data, drop `ilabelImg` from the engine, delete the dead raider branch), bot intelligence
   (`.planning/quick/20260730-bot-intelligence/PLAN.md`), and STORM-02 guest storm parity. One pass,
   not three.

## KNOWN-OPEN, all deliberate — do not rediscover as bugs

- **G29** — `src/ui/flow.js:549` and `:599` emit the storm leg summary unguarded, so a ship that never
  moved still announces "A gale blows X off the dock!". `src/engine/index.js:722` has the movement
  guard; the two UI copies do not. Wyatt: *"note this down, we'll solve it later it is not a blocker"*.
- **G30 — WITHDRAWN.** I claimed `home` shelter should stop saying "docked". That contradicted Wyatt's
  own G2 model (*"we're just treating tortuga like any other dock"*) and D-28 (justDocked/home/dock are
  one string with three doors, correctly tagged `keep`). What he saw is explained by G29 alone.
- **The copy comparison still does not exist** (audit-tool plan Tasks 5/6/7). Honest numbers: 19 of 144
  approval fields conclusively settled, 84 with fragments present but order unverified, 41 unjudgeable
  mechanically. Nothing is known wrong; it is unmeasured. **This is the gap that let four approved
  rewrites ship missing in the first place** and is the most valuable thing left to build.
- **Four weak gates** (`15-REVIEW.md`): `ui_contract_check` assertion 6a inspects zero chains today and
  its regex misses `if(cond){sub=...}`; 6b requires `disabled:!flag` so `disabled:broke` is skipped;
  `narration_audit_check` appends four assertions CONDITIONALLY, so archiving the dispositions file
  makes them vanish rather than fail while the summary still reads "22/22 PASS".
- **`captions()` has zero callers** — every `caps:[…]` entry across ~20 builders is dead player-facing
  copy. Needs Wyatt's ruling: wire it up or delete it.
- `flash()`/`showNarration()` remain two schedulers (D-57 residue), recorded and unenforced.
- Two of six D-41 greyed states never eyeballed (coins-only, hail Counter) — bots closed the window both
  sessions.

## Standing rules that earned their place this phase

- **Verify in a browser before declaring done.** Twice an agent reported a page fine when it was not
  (claimed 210 cards; the browser showed 61 with 128 texts unrendered).
- **A gate must be watched to fail before it is trusted.** FIVE vacuous checks were caught — including
  one that passed because it inspected nothing, and two that faked their own red-proof by exiting on
  import.
- **Partial fixes are this codebase's dominant failure mode**, not missed ones. G18, G15, G29 and CR-01
  each had a correct implementation sitting nearby that was never carried across.
- **Never invent player-facing copy.** Wyatt authors it; stop and ask.
- **Never widen a pattern or loosen an equality to make a gate pass.** Re-pin fixtures in the same
  commit with the reason in their `_provenance`.
- **Never edit `15-DISPOSITIONS-FINAL.json`** — his authored record.
- Check the decision record before calling behaviour a bug. G30 was diagnosed from code outward against
  a model set four hours earlier in the same session.

## Environment

Local servers, all serving this worktree: `8350` (Wyatt's game) and `8351` (Claude's guest seat) are the
current pair; `8117`/`8118`/`8231`/`8232`/`8341` are older and may hold stale cached modules. **Always
use a fresh, never-loaded port for a browser check** — Chrome caches ES modules per URL and this
produced two false "bugs" already.
