# Continue here — 2026-07-31, mid-verification of Phase 13's last two checks

**v1.2 is SHIPPED, LIVE and ARCHIVED.** Tagged `v1.2`, merged to `main`, serving on
playpastrypirates.com. Nothing below blocks anything — it is closing verification debt the milestone
audit surfaced.

## RIGHT NOW: a live game is in progress

- **Room `SGZZ`** on `http://localhost:8460/index.html`
- **Wyatt = host**, Safari, seat 0 ("Davy Scones")
- **Claude = guest**, Chrome, seat 1 ("Claude"), identity `claude-guest-430272`, tab titled
  "🤖 CLAUDE IS USING THIS — guest seat"
- Both recipes drafted; sailing order just drawn (Davy Scones first)

**Rejoin the guest seat by reading the Chrome tab** — it is still in the room. Do not re-join; that
would take a second seat.

## The two checks still open (Phase 13 `human_verification`)

Both are *guest-side* actions, which is exactly why they were never closed — every previous session
drove pause from the HOST.

### Check 2 — guest-initiated pause
From the GUEST tab: `document.getElementById('scPause').click()`
- **Expect:** Wyatt's Safari view pauses too; guest `shotClockPaused` goes true.
- Assert on both sides. Ask Wyatt to confirm his view paused — that half cannot be read from here.

### Check 3 — click-to-resume (CLOCK-03's actual affordance)
While paused, from the GUEST tab: `document.getElementById('shotClockNum').click()`
- **Expect:** the clock RE-ARMS and play continues — not stuck. A stuck clock here is BUG-02's
  failure mode in miniature.
- Assert `shotClockPaused` false and `turnExpired` NOT stuck true afterwards.

**NOTE:** `timerOff` is currently **true** (Wyatt turned the timer off earlier). The shot clock may
need turning back on before pause/resume is meaningful — check `timerOff` first and ask him to
re-enable if needed.

## Already closed today — do not redo

- **Check 1, the localStorage version guard** — all four cases run in Safari. Recorded in
  `13-VERIFICATION.md`. The proof is **C vs D** (identical `pp_solo` blob differing only by `v:1`:
  cleared+home vs survived+resumed). `pp_id` and `pp_timerOff` survived all four.
- **Check B re-run against real room `SGZZ`** — host reconnected into the running game. The original
  B used a fake room and **could not fail**; that is recorded as inconclusive, not as a pass.

## Housekeeping when done

1. Record checks 2 and 3 in BOTH `.planning/phases/13-*/13-VERIFICATION.md` and
   `.planning/milestones/v1.2-phases/13-*/13-VERIFICATION.md` (the archive copy must match).
2. Update `.planning/v1.2-MILESTONE-AUDIT.md` — its tech_debt list names these three checks as open.
3. **Release the Chrome tab**: `clearInterval(window.__claimTitle)`, restore
   `document.title = "Pastry Pirates on the Sugar Seas — Online"` and the favicon to `favicon.png`.
   Standing rule — see the memory `feedback-label-claude-browser-tabs`.
4. Delete the test room if it is still around: `appState.db.ref('rooms/SGZZ').remove()`.
5. Servers running: **8460** (this game). Older ports were killed. Always use a fresh, never-loaded
   port for any NEW browser check — Chrome and Safari both cache ES modules per URL.

## State of the repo

- Branch `july30-economy-crate-and-bet-fixes`, clean, pushed, **merged to main**.
- `npm test`: 19 gates, 23/23 assertion groups, 31/31 determinism seeds.
- `src/engine/index.js` byte-identical to `9ddd214`.
- Read `docs/DRIVING-THE-GAME.md` before any browser automation — especially §4a, the flippenator
  coin IS the flip button, which stalled three earlier attempts.
