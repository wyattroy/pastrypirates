# Continue here — paused 2026-07-30, mid-flight

Wyatt stepped away from his desk. Work was NOT at a natural stopping point; a build is in flight.

## Where things stand

- **Branch:** `claude/confident-bassi-7263ea`, worktree `.claude/worktrees/new-gsd-project-e18e9f`
- **Remote:** `origin/july29-narration-playtest-and-audit-tool` — last pushed at `9dd36c0`. Local has run ahead; **push again once the in-flight build is green AND browser-verified.**
- **`npm test`:** green (23/23 assertion groups) at last check
- **`src/engine/index.js`: byte-identical for three days.** This is the single most important invariant in the repo — 31 determinism fixtures verify against it. Every fix in this run was deliberately scoped UI-tier to preserve it. Do not break it casually; a change there forces a gated re-record (`docs/DETERMINISM-RERECORD.md`).

## IN FLIGHT — finish this first

`gsd-planner` is writing `.planning/quick/20260730-playtest-session2-fixes/PLAN.md` — **17 items** from the recorded two-tab playtest (room NAMF). When it completes, **spawn a `gsd-executor` against that plan.** It has not been executed yet.

The 17 are enumerated in the plan; the source of truth for WHY each exists is
`.planning/phases/15-narration-audit-fixes/15-PLAYTEST-NOTES.md` — read **SESSION 2** at the top.

Two items were rescoped mid-planning and the corrections were sent as follow-up messages, so make sure
the written plan reflects them:
- **Item 7 (trade-wind animation)** — the guest half is **FREE, not a re-record**. The guest already has
  `rimCellInfo` (the ordered rim ring, `src/engine/index.js:92`) and `rimHead`, so the sweep path is
  derivable geometry, not simulation. Must be **ONE shared stepper called by host and guest**, not two.
- **Items 16/17** — pull D-55 forward (guest highlight rect gets `class:"sailCell"`, drop inline
  fill/opacity) and build the host/guest parity gate D-56 recommended and nobody wrote.

## THEN — the agreed pipeline, in this order

Wyatt approved all of it: *"queue all of that behind the current build"*.

1. **Re-verify Phase 15.** `.planning/phases/15-narration-audit-fixes/15-VERIFICATION.md` currently reads
   **gaps_found (14/19)**. That was true when written; all five gaps have since been closed. The verdict is
   stale and would be a bad artifact to merge. ROADMAP still shows Phase 15 unchecked and STATE.md still
   says `status: verifying`.
2. **`/gsd-code-review` on the phase diff.** Never run. Everything caught so far came from playtesting or
   verification; nothing has read this as code. Two days of changes across narration, prompts, storm
   handling, coin paths and the audit tool.
   **When it reports: bring findings to Wyatt, do not auto-fix.** Some will be real; some will be style
   opinions about deliberate conventions (dense one-liners, single-letter variables). He decides which.
3. **Close the two open checkpoints** (see "Needs Wyatt" below).
4. **`/gsd-extract-learnings`** — this run produced unusually good material; cheap while fresh.
5. **`/gsd-ship`** — PR, review, merge prep. Wyatt is keen to merge.

Open question he has NOT answered: whether main's history should keep the `.planning/` commits or use
`/gsd-pr-branch` to filter them out. Ask before shipping.

## NEEDS WYATT — one thing only

**Confirm the six retired audit cards.** The audit-tool task (`20260729-narration-audit-tool-hardening`)
retired 6 of his 209 reviewed dispositions. All six carry `tag:"merge"` in his own export and the tool now
refuses to retire anything he did not tag merge — but nobody has confirmed the actual list with him.

**Present them as plain sentences — what each line said and where it went — NOT as card ids.** Six lines
leaving a review set he spent multiple sessions on deserves an explicit yes.

Everything else he has already ruled on. Recent rulings, for the record:
- recipe.js prose and the credits paragraph: **leave** as plain English
- privacy notice: **revert to "you"** (out-of-character chrome stays plain)
- flip outcomes: ALL CAPS **in play only**; prose and stats stay lowercase
- ship stacking after a rim sweep: **accepted, do not fix** (the renderer nudges them visibly apart)
- flee not offered to a broke defender: **not a bug, leave it**
- inline icon spacing: **explicitly declined**
- boxed-in bot: **should** escape via the rim (UI-tier fix, `botTurn` must match `takeTurn`)

## Host/guest drift audit — the honest state

Wyatt asked whether the four drifts are structurally fixed or merely patched. Audited:

| Drift | State |
|---|---|
| **F7** prompt leak | ✅ gated — `ui_contract_check.js` assertion 7 flags viewer-branching broadcasts |
| **D-35** sail wording | ⚠️ structurally safe (guest renders host's `msg`), nothing asserts it |
| **D-57** guest fade | ⚠️ patched, NOT unified — `flash()` and `showNarration()` remain two hold+fade schedulers on one element |
| **D-55** highlight parity | ❌ never fixed — item 16 fixes it, item 17 gates it |

D-56's recommended class-parity test was **never built**; that is item 17. **Do not attempt the
`flash()`/`showNarration()` unification in this pass** — item 4 is already changing that code.

## Standing rules that have earned their place

- **Verify in a browser before declaring done.** Twice an agent reported a page was fine when it was not
  (claimed 210 cards; the browser showed 61 with 128 texts unrendered). Tests passing is not the same as
  working.
- **A gate must fail on a broken tree before it is trusted.** Red-proof every new guard. Three separate
  vacuous checks were caught this way — one passed when the assertion had never been written.
- **Never invent player-facing copy.** Wyatt authors it. If a fix seems to need a new string, stop and ask.
- **Never widen a pattern or loosen an equality to make a gate pass.** Re-pin fixtures in the same commit
  with the reason appended to their `_provenance`.
- **Never edit `15-DISPOSITIONS-FINAL.json`** — it is his authored record.

## Environment

Local servers running from this worktree (leave them up; Wyatt's convention is to stop only at commit time):
`8117`, `8118`, `8231` (Wyatt's game), `8232` (Claude's guest seat), plus older `8911`/`8912`/`8913`.
Use a fresh, never-loaded port for any new browser check — Chrome caches ES modules per URL and this has
produced two false "bugs" already.

## Queued for the next gated re-record

`docs/DETERMINISM-RERECORD-NEXT.md` — engine-purity (`spoil`/`gave` become data, drop `ilabelImg` from the
engine, delete the dead raider branch), plus bot intelligence
(`.planning/quick/20260730-bot-intelligence/PLAN.md`) and STORM-02 guest storm parity. One gated pass, not
three. **The trade-wind animation is NO LONGER in this batch** — it turned out to be free.
