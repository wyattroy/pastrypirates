# HANDOFF — the laser-cut Pastry Pirates (branch `physical-board`)

Written 2026-08-22 at the end of a two-day session, so the next session can pick up without the
context. Read this whole file, then `README.md`, then open `index.html`. The git log on this branch
is long on purpose — every commit message records a ruling of Wyatt's and why.

## 1. Where things are, and the one rule about git

- **Everything lives on branch `physical-board`. Nothing of this may land on `main`** (public; the
  live game is served from it). Wyatt, 2026-08-22: *"i don't want any of this work to land on main in
  the public repo."* The folder was briefly on main (f8f7e24) and reverted (735b520); history keeps
  the files by his choice.
- **The main checkout stays on `main`.** Another session was live in it with uncommitted changes
  under `4/` (`git status` shows them), so this session never switched branches. Instead:
  - `physical-board/` is listed in `.git/info/exclude`, so on `main` it is invisible to git.
  - Commits go to the branch through a temporary index, never touching the working tree:
    ```bash
    export GIT_INDEX_FILE=$(mktemp) && git read-tree physical-board && git add -f -A physical-board \
      && T=$(git write-tree) && C=$(git commit-tree $T -p physical-board -F msg.txt) && unset GIT_INDEX_FILE \
      && git update-ref refs/heads/physical-board $C && git push -q origin physical-board
    ```
  - Last commit at handoff: `298fffd` (branch == origin). Local `main` and `origin/main` have
    diverged by a few planning commits each way that are NOT this session's business.
- zsh gotcha: `$C:refs/heads/x` — zsh treats `:r` as a modifier. Write `${C}:refs/heads/x`.

## 2. What the set is (the rulings, all Wyatt's)

V3 "the Round Table". 25 mm squares. Board and board-pieces 6 mm; spinner, crates, chests, cards
3 mm. Kerf 0.18 (compensated on cutting sheets only). 600 × 400 bed. Everything generated by
`generate.mjs` from the game's own numbers; the art traced from the game's PNGs by `art/trace.py`;
lettering (Georgia, Avenir Next) as outlines from `fonts/extract.py`.

| Group | Status on the page | What it is now |
|---|---|---|
| Board | not yet approved | Four jigsaw quadrants on grid-line seams; NW carries the centre square; a **dotted outline** where Tortuga sits; water = short brushy wave strokes (not rings); rim = the app's chevron. |
| Tortuga | (part of board) | A **+‑shaped piece that sits ON the board**, same style as the islands, says "Tortuga", **no anchor**, berth piers on the arms **reaching the middle square**, no palm. |
| Islands (9) | **round five built to his second drawing — not yet seen by him, see §8** | Per `notes/islands.jpeg` and his answers: straight-edged cut, 5 mm corners; shore line + grass line (0.6 mm, each waving its own way) with bare wood between as the beach; **notch floor bites 0.3 mm into the shore line**; one mark per square — wind-blown palm (H), three stones (C), the game's tuft (C), all his picks from rendered line-ups. Shapes 8/9 = mirrored L and S. |
| Docks (7) | "these look good" then widened | Pier whose **9 mm deck becomes the tab** (9 × 2.5, 0.15 play), planks to the tab's end, **bollards touching the deck**, no anchor. |
| Ingredient tokens (28) | **C picked (2026-08-25), two defects fixed, not re-approved yet** | Padding C on the sheets. His two findings fixed in `trace.py`: the dilation clipped flat at the PNG canvas (a 24 px margin is now added first), and a 1 px speck under the sugar grew into a notch (specks are dropped from the raw silhouette before dilation). |
| Whirlpools (4) | **approved** | 3 mm tile, the swirl art traced. Do not touch. |
| Spinner | not yet approved | Backing disc + compass dial (glued) + turning ring with a slot + flat balanced needle + standing **WIND NOW vane**. Storm wedges on the diagonals, centre-to-rim, **the 🌩️ emoji silhouette knocked out** (`art/storm-emoji.png`, rendered by Chrome on black) with STORM. |
| Ships (4) | **skull added 2026-08-25 — not yet seen** | 6 mm hull + two 3 mm sails; the traced ☠️ emoji (art/skull-emoji.png, the 🌩️ pipeline) big on BOTH sail faces — he wrote "masts", the question UI settled sails; overrules "plain sails". |
| Cargo crates (4) | **approved** | Slatted, box-jointed, no captain marks. |
| Treasure chests (4) | not yet approved | Box-jointed body 20 + lid 12; hinge = chest back wall tongues + a 2.4 mm **hinge strip** under the lid top + 3 mm dowel; 0.15 mm side play on tongues; card rails 5 mm; **labels are blue GUIDE layer, page-only, never in cut files**. |
| Extras | rules card only | Storm token and first-player wheel DELETED (2026-08-25, his call: “the game doesn't need them”) — the needle parked in a storm wedge is the forecast. Rules card unchanged. |
| Recipe cards (21) | liked, egg fixed | The game's 21 recipe titles, ink icons in the app's rounded-square chip. |
| Mockups | — | Isometric renders on the page: board on the table, ship, chest open + closed, spinner, crate. Built from the real cut geometry (`isoScene`). |

## 3. The island saga — read before touching islands

Four rounds. (1) Generated wavy coast + black sand band + palm/tufts/rock: he liked the look but
said notches cut through the engraving and docks should nest. (2) Wider dovetail notches + band
routed around them: *"much worse"*. (3) The island PNGs traced as the cut outline: *"nothing like
I asked for... You have completely failed."* (4) He drew it (`notes/docknotch.jpeg`) and said **"go
back to the first version, then make that version look like the diagram"** — which is what is on
the branch now. His answers to the questions: clean rounded polyomino is the cut; square-corner
notches; a thin line inside; the art as engraving was his earlier wish but the drawing won.
**Do not re-interpret the islands again without asking him with the question UI first.**

## 4. How to work it

```bash
node physical-board/generate.mjs                 # ~1 min; writes v3-round/*.svg/.dxf + site-data.js
python3 physical-board/art/trace.py physical-board/art/ingredients.json    # re-trace art (slow-ish)
python3 physical-board/fonts/extract.py physical-board/fonts/glyphs.json   # fonts (macOS system fonts)
```
Run `generate.mjs` under `perl -e 'alarm 300; exec @ARGV' node …` — a float-step loop once hung it
(fixed, but keep the cap). macOS has no `timeout`.

**QA = render and LOOK** (rule 19). The pattern used all session: inline each group's SVG from
`site-data.js` into a tiny HTML wrapper, screenshot with headless Chrome
(`--headless=new --screenshot=… --window-size=W,H`, one-shot, exits itself), montage with PIL, Read
the PNG. Scratch files lived in the session scratchpad; nothing of that is in the repo. Kill any
Chrome you start; there is usually ANOTHER session's headless Chrome running (port 9762) — leave it.

**The page** (`index.html`): groups in the nav; approve/notes per group; "clear this group" and
"Clear all feedback"; Copy all feedback → markdown he pastes back. Blue = GUIDE layer (page only).

## 5. Geometry lessons paid for (don't re-learn)

- `offsetPoly` is a mitre offset; near-parallel tiny segments shoot the join to infinity (mitre
  limit added); offsetting INWARD past a sliver folds the polygon — a notch plus a band around it
  must fit in the straight run between two rounded corners.
- `flatten()` must honour a circle's direction or a flattened ring fills solid when clipped.
- Raster shapes never overlap; holes are reversed sub-paths under nonzero (LightBurn fill modes
  cancel overlaps). Wedge polygons must be traced clockwise or their cut-outs vanish.
- Kerf: outward on outlines, inward on holes, decided by containment within the same `piece`.
- `notchPolyline` rotates the polyline to start far from the notch (a window straddling index 0
  otherwise deletes everything).
- Tracer: per-asset ink thresholds; "not red" keeps the cocoa wrapper open; morphological close
  heals the egg's nick; padded variants open the silhouette first or sparkle pixels grow blobs;
  `inkInset` drops ink near a coast; `blackbg` for the Chrome-rendered emoji.
- Chest hinge: both halves' holes 3.3 mm from their edge, tongue ends r 3.1 about the axle, strip
  body 2.4 → lid wall 9 mm = 3.3 + 2.4 + 3.3. Side play 0.15 per tongue.

## 6. Open items, in the order he is likely to raise them

1. His verdict on the islands as they now are (§3) and the three token paddings (A/B/C).
2. Board group: he wanted Tortuga's docks touching the island — done; re-check with him.
3. Spinner: storm emoji + balanced needle — done this round; not yet seen by him.
4. Chests: thin piece fixed (hinge strip) — not yet seen by him.
5. A test cut is still the real gate: one dock tab into an island notch, one chest hinge. Real
   kerf → `--kerf`; real ply thickness → `--material` / `MAT3`.
6. Sheets: 6 mm ×2 and 3 mm ×2 at 600 × 400; token option B on them until he picks.
7. Not built on purpose: flip coin, gold coins (he has them), sea-creature cards, captain's screen.

## 7. How to talk to him (the short version of the project's CLAUDE.md)

Plain English, state the size of what you did, ask 2–5 questions with the question UI before
building anything non-trivial, read every screenshot he sends pixel by pixel, QA your own renders
before he sees them, restate every mid-flight instruction, and never claim a defect or a fix you
have not looked at.

## 8. Round five — the islands from his SECOND drawing (2026-08-22, evening)

He drew `notes/islands.jpeg` (an L on a pencil grid: red = cut, black = engraving, in the project's own
colours) and asked for 10–20 questions before anything was built. Fifteen were asked with the question
UI, in four batches; then he asked to SEE grass tufts, stones and palms before choosing, so two
line-up sheets were rendered (four of each, magnified and at true size on a 25 mm square). The first
palm sheet he rejected outright ("draw them better"); the second he chose from. **All of the below are
his rulings, not inferences:**

- **Cut:** ruler-straight between the corners (the wave moved off the cut), corners **stay 5 mm**
  (his drawing's ≈7.5 was the pen), clearance stays 0.4, notch **stays 9 mm** to match the dock.
- **Notch floor goes a little INTO the shore line** — "we want it to look like the dock is literally
  touching the sand." With the dock unchanged (2.5 mm tab) that fixes where the shore line can be:
  `SHORE_LINE = 2.6` (spans 2.3–2.9, floor at 2.6), `GRASS_LINE = 5.6`. The drawing had them at ≈3.5
  and ≈6.5; the 2.4 mm beach between is the drawing's. **This was a judgment call** — three rulings
  (dock unchanged, notch bites line, drawing's spacing) could not all hold — and it was told to him.
- **Two lines:** outer = shore, inner = grass, the wood between is the beach; **0.6 mm** (he kept the
  build's weight over the pen's 0.7); **each wavy its own way** (`waveCoast` now takes `phase` and
  `ripple`, and fits whole waves round the loop so the line closes without a step).
- **Tokens cover what they cover** — he knows a 20 mm token hides the grass line on a stocked square.
- **Marks, one per square, centred:** palm on an end square, the stones on the junction square with a
  tuft beside them, a tuft on every other square. Sizes: palm box stays 12.5 mm, stones 6, tufts 4.
- **The art, by his pick:** tuft = "C · the game's tuft" (four fat blades, one closed fan), stones =
  "C · a cluster of three", palm = **"H · wind-blown"** (he first answered E, the traced 🌴 emoji, then
  interrupted: "Actually I prefer H!"). Drawing code lives in `islandArt` beside `ICONS`.
- **Tortuga** gets the same coast (piers and name kept, no marks). Dock untouched.

How the line-ups were made, for next time: author each candidate in the icons' 100-box, render ONE
`<path fill-rule="evenodd">` per candidate so any overlap between shapes shows as a white hole (the
laser's fill mode does the same), magnified beside a true-size 25 mm square with the two lines. Three
rounds of that caught: blades drawn as petals floating off their base (edge control points on the
wrong side), crowded hubs, and a "droop toward the ground" that rotated the long way round for leaves
pointing left — every one visible in the render, none in the code.
