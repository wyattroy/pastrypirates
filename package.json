{
  "name": "pastry-pirates",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node scripts/determinism_baseline.js --verify && node scripts/engine_contract_check.js && node scripts/dlog_replay_test.js",
    "test:determinism": "node scripts/determinism_baseline.js --verify"
  }
}
