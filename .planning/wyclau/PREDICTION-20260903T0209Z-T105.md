# PREDICTION — watch 2026-09-03T02:09Z, `T-105`, the two `.claude/` repairs

*Written BEFORE the attempt, per rule 6's working form. Nothing below may be edited after the
result exists; corrections go underneath, dated.*

## WHAT I AM ABOUT TO DO

Apply the two edits written out verbatim in `.planning/wyclau/CLAUDE-DIR-REPAIRS-PENDING.md`, with
the Edit tool, exactly as three previous watches attempted and were refused:

1. `.claude/skills/door/SKILL.md` — append the version-stamp paragraph and the retire-the-question
   paragraph after the harvest paragraph.
2. `.claude/hooks/glass-harvest-first.cjs` — 2a (header + `receiptVersion`, delete `FRESH_MIN`),
   2b (identity check + the `force` refusal), 2c (the three deny texts, and the retired `date -u`
   line goes).

## THE MEASURED RED, TAKEN JUST NOW

`node scripts/qa/glass_harvest_hook_check.mjs`, cases 3–5, all five flags **false**:

```
hook: bare-timestamp denied=false  aged-receipt allowed=false  force denied=false
      deny-text free of the hand-written stamp=false
door: SKILL.md stamps and compares the version=false
```

## WHAT I EXPECT, AND WHY

**The write is permitted in THIS session, and refused in a watch.** The record's own conclusion is
that the wall is Claude Code's sensitive-file protection on Edit/Write — *"the harness declining to
let an unattended agent rewrite its own hooks and skills with no human present"* — and it names the
route: *"the only route is Wyatt, in a session where he is present."* Wyatt invoked this Door
himself and is at the keyboard, so a permission prompt has an answerer. The previous ledger entry
measured the other half already: an interactive session on this machine edited
`.claude/skills/door/SKILL.md` during the 23:39Z watch, while that watch was being refused the same
file.

**Then the gate FAILS on purpose.** With all five flags true,
`glass_harvest_hook_check.mjs` is written to print *"3-5/9 EVERY BLOCKED T-105 REPAIR IS NOW IN
PLACE — promote these to hard assertions and delete the pending block"* and fail, which is a real
follow-up edit in that file (promote three cases, delete case 9's exemption for the hook).

## WHAT WOULD PROVE ME WRONG — named before the result exists

1. **The write is refused here too.** Then presence is not the variable, the record's stated route
   is wrong, and the row goes back untouched with that finding written up. **I must not then reach
   for a `node` script or a peer session** — both defeat the protection rather than satisfying it,
   and the record already forbids both by name.
2. **The edits land and the gate's five flags do NOT all flip.** Then the pending block is testing
   something other than what `CLAUDE-DIR-REPAIRS-PENDING.md` says it tests, and the document that
   claims four times over to be verbatim is wrong again — CEO 126 already caught it missing the
   `deny()` helper, which would have made the hook throw on every Artifact call and fail OPEN.
3. **The hook throws.** `.claude/settings.json` registers it ending `2>/dev/null || true`, so a
   throwing hook fails OPEN and says nothing. **"No output" is not "allowed on purpose."** The
   repair document says to run `node .claude/hooks/glass-harvest-first.cjs < /dev/null` once before
   trusting it, and I will, and I will quote what it printed.

## WHAT THIS DOES NOT FIX, PREDICTED IN ADVANCE SO IT CANNOT BE REFRAMED LATER

Layers C and D of `SPEC-GLASS-HARVEST-SAFETY.md` are not in these edits. **The acceptance test in
that spec's §2 — harvest at T finds nothing, he writes at T+7s, a session republishes at T+5min,
and his words survive — is still not passed by this work.** What lands here is the hook asking for
identity instead of a clock, and refusing `force`. The residual exposure the row already names
(the MERGE, where the tool hands back the live source and a careless merge can still drop his
words) is untouched.
