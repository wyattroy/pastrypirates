# PREDICTION — watch 2026-09-02T09:38Z, item `T-059` (`npm test` red on `game_url_check`)

*Written BEFORE the fix, per CLAUDE.md rule 6's working form. The point is that it cannot be
retrofitted. Whatever is wrong below stays here, said out loud in the reply.*

## What I think is true, and why

`scripts/game_url_check.js` case 3 flags any in-page `import("/<tree>/src/…")` whose tree is not the
served root. `scripts/qa/pastry_shipped_art_probe.mjs:98` writes `/classic/src/ui/recipe.js` as a
**string literal**, so it is flagged. Measured, not assumed:
`node scripts/game_url_check.js` → `FAIL 1 in-page import(s) naming a non-root tree`.

**The gate is right and the probe is right.** `/classic` is the frozen v1 — a permanent, tracked
directory (`classic/index.html`, title *"Pastry Pirates — the classic voyage"*), not a tree the
cutover can move. What is actually wrong is that the probe **hand-types where a game lives**, which
is the exact thing the gate exists to forbid, and the reason it exists is that twenty scripts once
hand-typed `/4/` and the whole sea trial then sailed at an empty directory reporting the game as
broken.

## WIDEN THE TIME HORIZON — what happened immediately before?

`bc97d40d` (the 07:31Z watch, answering CEO 96) added this probe so the frozen v1's recipe modal
would actually be photographed rather than merely promised in a header. **The suite went red in the
commit that fixed a CEO finding** — a QA script written to close one gap, landing without its own
gate run. That is the fault class, and it is not the probe author's carelessness so much as the
absence of anything between "write a new script" and "commit".

## What I predict

1. Giving `/classic/` **one spelling** in `scripts/lib/chrome.mjs` (`CLASSIC_PATH`, beside
   `GAME_PATH`) and having the probe interpolate it makes case 3 pass — because the literal is gone,
   not because the rule was bent.
2. A **new gate case** asserting `CLASSIC_PATH` really serves the frozen game (an `index.html` that
   exists and contains `#choiceSolo`) will pass today, and is what stops this being an evasion: the
   path becomes derived-and-guarded rather than exempted. I expect it to red-proof cleanly against a
   bogus path.
3. `npm test` returns to **96 of 96**.
4. **No game code changes.** `gear.mjs` says FULL, but it is reading everything ahead of
   `origin/main` — the whole 465-commit branch — not this change. This change touches three files
   under `scripts/`, so a sea trial is not what verifies it; running the probe itself is.

## What would prove me WRONG

- The probe **fails to import after interpolation** — `t.ev` builds its expression inside an outer
  template literal, and this repo has already been bitten twice by escapes silently dying on that
  journey (`\s` becoming `s` in `sail_containment_crew_probe.mjs`). If the interpolated path arrives
  mangled, my fix looks green to the gate and has broken the probe.
- The probe **does not actually run today** for some other reason, in which case "the gate is green"
  would be true and worthless — I would have proved nothing about the classic modal still opening.
- The new gate case **passes when handed a bogus path**, i.e. it cannot fail, which would make it
  decoration.
- `npm test` comes back with a *different* failure underneath this one — the `&&` chain hides
  everything after the first, and this suite has hidden a second failure that way before.

## The instrument check

The gate is already RED on this exact line, so step 1 needs no new check written — the failing
check exists and names the file and line. What DOES need red-proofing is the new case I am adding,
and I will do it the way this file's own case 5 does: assert it goes red on a bad value and stays
quiet on the good one, in the same run.
