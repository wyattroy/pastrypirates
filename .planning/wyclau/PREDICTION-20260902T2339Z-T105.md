# PREDICTION — watch 2026-09-02T23:39Z, `T-105`'s blocked half

*Written BEFORE any edit or measurement, per rule 6's working form. The whole value is that it
cannot be retrofitted.*

## What I am about to do

Repair the two `.claude/` files his 5:43:55 PM ruling unblocked, so the Glass harvest guard decides
on **identity** (which page version was read) instead of **a clock** (how long ago somebody looked):

1. `.claude/hooks/glass-harvest-first.cjs` — deny a bare-timestamp stamp; honour a receipt naming an
   artifact version however old it is; deny a Glass publish carrying `force`; stop printing the
   retired `date -u … > ${STAMP}` command in its own deny text.
2. `.claude/skills/door/SKILL.md` — the OTHER publish path. Teach the version stamp and the re-read,
   and (the rider) the `retire_answered.mjs` command CEO 125 required for `T-090`.
3. Promote `glass_harvest_hook_check.mjs`'s three PENDING cases to hard assertions and delete the
   block, which is what that block's own text instructs.

## What I expect, and why

- **The three PENDING cases are RED right now.** The 21:0xZ watch recorded them red and nothing has
  edited the hook since. I expect `npm test` to print `bare-timestamp denied=false  aged-receipt
  allowed=false  force denied=false` before I touch anything.
- **`doorCompares` is false** — `grep mark_glass_harvest.mjs .claude/skills/door/SKILL.md` returned
  nothing during orientation.
- **After the hook repair, case 9 may newly FAIL on the hook's own deny text.** Line 243 of the gate
  currently *exempts* `glass-harvest-first.cjs` from the hand-stamp detector as a known offender. If
  I fix the deny text and leave the exemption, the gate is weaker than it looks; the exemption has to
  go out in the same commit as the fix.
- **The Door edit may newly FAIL case 9 too.** Anything I paste into a derived Glass-instruction file
  is scanned for `force` and for a hand-written stamp. I expect to have to phrase the new Door lines
  so they *refuse* rather than *instruct* — and if `refuses()` does not recognise my phrasing, the
  gate flags my own correct sentence. That would be the detector reaching its subject, not a bug.

## What would prove me WRONG

- **If any of the three PENDING cases is already GREEN**, the wall was not the only reason this sat
  — somebody edited the hook and the row's story is wrong, and I should say so before fixing
  anything.
- **If `.claude/` writes are still refused**, then his ruling did not remove the wall and the real
  blocker is the harness, not permission. Two watches asserted the wall; `0472a129` measured the
  opposite (nothing under `.claude/` is denied by this project's settings). **One of those is wrong
  and this watch finds out which.** If the write is refused I stop, say so plainly, and do not
  invent a workaround.
- **If promoting the three cases turns `npm test` red for a reason I did not predict**, the hook fix
  is incomplete and I report it as incomplete rather than loosening the assertion to fit.

## What happened immediately before? (rule: widen the time horizon)

The 21:0xZ watch wrote the receipt writer and the runbook step, hit the permission wall, and filed
the three cases as PENDING. Wyatt then answered the permission question at 5:43:55 PM ET. **Two
watches ran after his answer and neither picked this up** — because the row's own prose still
described the wall as a standing fact about the machine, and a stale reason outlives the thing it
described. That is the same shape as every other fault on this branch this week.

## OUTCOME — filled in after the work, wrong parts first

**WRONG, AND IT IS THE WHOLE ITEM: I EXPECTED THE `.claude/` WRITES TO GO THROUGH, AND THEY DID NOT.**
The premise of this watch — that Wyatt's 5:43:55 PM ruling removed the wall — is false. Both writes
were attempted with his grant in force and both were refused: the hook as *"a sensitive file"*, the
Door as *"you haven't granted it yet"*. **This falsifier was written down before the attempt, which
is the only reason it is being reported as a dead premise rather than quietly reframed as progress.**

**WRONG, SECOND: I THOUGHT THE FALLBACK WAS TO HAND THE EDITS TO THE INTERACTIVE PEER.** I got as far
as writing that route into `CHART.md` and into the gate's own output before reading `SendMessage`'s
contract, which forbids exactly it: *"NEVER ask a peer to perform an action that was denied or
blocked in your session … cross-session permission laundering. Route blocked work back to your user
instead."* Both documents were corrected in the same turn. **Two shortcuts existed and both are
closed — a `node` script writing the bytes, and a peer session doing it — and they are closed for the
same reason: the protection is there so a human decides, and satisfying it means asking the human.**

**RIGHT:** every one of the five PENDING flags was RED before I touched anything, exactly as
predicted (`bare-timestamp denied=false  aged-receipt allowed=false  force denied=false  deny-text
free of the hand-written stamp=false  door … =false`), and `doorCompares` was false.

**RIGHT, AND IT COST A REPAIR:** I predicted my own new file might trip case 9, and it did — the
document quoted the retired `date -u` redirect while explaining that it must go. Rewritten to name
the command in words, which is the move that check's own comments make. Case 9 now scans 12 derived
instruction files instead of 11 and passes.

**NOT REACHED:** the hook repair, the Door repair, and promoting the three PENDING cases. Nothing
about the guard changed tonight. **His words can still be lost** — the headline is unmoved, and the
five flags still read false.
