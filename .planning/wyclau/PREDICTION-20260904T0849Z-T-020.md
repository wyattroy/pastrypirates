# PREDICTION — T-020, crew-phone guest never emulates a phone

**Watch started:** 2026-09-04T0849Z, `claude/cloud-handoff-planning-a9ay1u`.

## What I'm about to measure

The Chart row (`T-020`) claims: `scripts/playtest_gate.mjs`'s `crew-phone` leg opens the HOST with
`mobile: !!def.mobile, dsf: def.dsf || 1` (line ~418) but opens the GUEST with neither (line ~429,
only `W, H, dbgPort, httpPort, serveRoot, profileDir`) — so `scripts/lib/cdp.mjs:47` defaults the
guest to `mobile = false, dsf = 1`, a desktop window, not a phone. The row cites
`crew-phone-host-012-settled.png` reading "Tap and hold" against `crew-phone-guest-012-settled.png`
reading "Click and hold" as photographic proof, from `src/ui/stage.js:547-551`'s
`matchMedia("(pointer: coarse)")` verb derivation.

## What I expect, and why

I expect the row's own reading of the source is correct — I already read `playtest_gate.mjs:417-429`
and `cdp.mjs:47,81,87` myself and confirmed the asymmetry exists exactly as described: the host call
passes `mobile: !!def.mobile, dsf: def.dsf || 1`, the guest call passes neither. I expect a RED check
asserting "the crew-phone guest launch config includes `mobile:true, dsf:2`" to fail on the current
code and pass once `playtest_gate.mjs`'s guest `openEngine` call for crew legs is changed to also
pass `mobile: !!def.mobile, dsf: def.dsf || 1` (matching the host call, deriving from the same `def`
rather than a second hardcoded value — rule 9).

## What would prove me wrong

- If the guest call already receives `mobile`/`dsf` through some other path I haven't seen (e.g. a
  default baked into `openEngine` itself for crew legs, or a later line that patches it in), the fix
  is already redundant and I should find that instead of duplicating it.
- If changing this makes the crew-phone leg fail structurally in a new way (e.g. touch emulation
  breaking a click-based driver step that assumes a mouse), that is a real regression risk this row
  did not anticipate, and I should report it rather than force the change through.

## Method

Read `openEngine` to confirm it is a thin wrapper with no crew-leg-specific mobile handling. Write a
gate that reads `playtest_gate.mjs`'s own source (or exercises the leg def against the two
`openEngine` call sites) and asserts guest-vs-host emulation parity for any leg whose `def.mobile` is
true. Red-prove it against the current file, apply the one-line fix, green-prove it, `npm test`.
Gear: NONE — this is QA tooling (`scripts/`), not shipped game code (`index.html`/`src/`), same class
as the `cdp.mjs` timeout fix CEO 197 approved without a sea trial.
