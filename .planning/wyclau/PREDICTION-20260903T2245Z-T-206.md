# PREDICTION — watch 2026-09-03T2245Z, `T-206` (Google Analytics)

Written BEFORE any measurement of `src/analytics.js`, before the CEO is spawned, and before
`gear.mjs` is run. I have read: the Chart row, the commit MESSAGE of `09f8658c`, the ledger tail
(CEO 188's two debts), `index.html:2767`, and the fact that `BLOCKED ON WYATT` is an empty table.
I have **not** read `src/analytics.js` or `scripts/qa/analytics_consent_check.mjs`.

## What I expect, and why

**1. The install is real and matches his two rulings.** The commit message names both verbatim
("The public pages only — the game, About and Rules", "Cookieless, no banner"), and the three
pages each carry one `<script type="module" src="src/analytics.js">` line — I grepped that.
*Why:* the file list shows one module and three one-line references, which is rule 23's shape.

**2. No CEO verdict exists for `09f8658c`.** `CEO-REVIEWS.md`'s last four are 183, 184, 187, 188 —
and 187/188 are both `T-247`, the staging publish. *Why:* the analytics work was committed by the
**Blade Advisor session**, which is record-only by his 2026-09-02T0400Z ruling and does not run the
watch loop. So rule 25's per-item review was never run on the one change that puts a third-party
tracker on a page children play on.

**3. `index.html:2767` becomes FALSE on production the moment the tag fires.** It reads
*"Anonymised move data is recorded to help improve the game — nothing beyond the name ye confirm
after picking how to play is collected."* Google Analytics — even cookieless — sends the referrer,
coarse geography and per-page hits to a third party. That is more than "the name ye confirm".
*Why:* CEO 188 named exactly this as a production blocker, and nothing has changed since.

**4. Nothing is asked of him.** `BLOCKED ON WYATT` has a header row and no rows under it. The
ledger says the Blade session "is putting the copy question to Wyatt directly"; Blade has been
`waiting` (idle) since. **So the question exists in a ledger paragraph and on nobody's page.**

**5. `gear.mjs` will say FULL** for any edit to `index.html`, even a one-line copy change — the
Chart row already records that `looksCosmetic` accepts only blanks, comments and CSS.

## What would prove me WRONG — named in advance

- **On 2:** if `CEO-REVIEWS.md` contains a review of `09f8658c` under a number I have not looked at
  (185 or 186 by another name), then the review is NOT owed and my whole framing of this watch is
  wrong. I will grep the file for the sha, not just the headings.
- **On 3:** if `src/analytics.js` sends **nothing** identifying at all — no referrer, no geography,
  only a count indistinguishable from `usage.js` — then the front-card line is still TRUE and there
  is no blocker, only a wording preference. **I must read the file before asserting the line is
  false.** If Consent Mode with all four storage types denied also strips referrer and geography,
  my claim 3 collapses.
- **On 4:** if a question row for the privacy copy exists anywhere the Glass renders — in
  `## RULED`, or as a `qid:` I did not grep — then it IS asked and I must not ask it twice.
- **On 5:** if `gear.mjs` returns COSMETIC for a `<div class="muted">` text change, the sizing in
  the Chart row is wrong and should be corrected there.

## What I will NOT do this watch, decided in advance

- **No edit to `index.html`, `src/` or anything a browser loads.** A sea trial is at sea on its
  10th of 10 legs (`LONG-RUN`, 4 debug-port browsers up, three `ppjudge` sessions spawned in the
  last minute). Its report is the merge evidence CEO 188 called a hard precondition. Editing the
  tree underneath it would make that report describe a build that no longer exists.
- **No second sea trial.** One is at sea; two fight for browsers and for the report.
- **I therefore expect to PARK `T-206`, not close it.** The FULL trial owed on `09f8658c` cannot
  be sailed by this watch, and `close_item.mjs` should not tick a row whose evidence is owed.
