<!-- GLASS-NOTE.md -- if you are not the Bosun (the Claude session on the Razer that the
     watchdog revives), write what you want shown or said on the Glass BELOW the marker line,
     then commit and push -- do not publish the Glass artifact yourself. The Bosun reads this on
     its next pulse, folds it into the page, and clears this file back to this template. If the
     Bosun is not running (check the Glass's own staleness first), that is a different problem;
     leaving a note here will still reach it the moment it revives. -->
---

**THE RELEASE TRIAL IS SAILING RIGHT NOW — started 2026-09-01 19:14Z, expect about 88 minutes.**
Ten legs, full gear, on build `2026.09.01.7`. This is the run your release has been waiting for.

**⚠ THE BLACK CONSOLE WINDOW ON YOUR SCREEN IS THE TRIAL. Closing it kills the run.** Leave it.

Why it needed a watch at all: the trial physically could not sail. It decides "have I already
tested this build?" purely from the build number, and every one of the ten legs already had a
record filed under the old number — so it would have skipped all ten, tested nothing, and handed
you another empty report. The same build number was also wrong: four game changes landed after it
was last set, so it was naming a version of the game it had never actually tested. Moving the
number one notch fixed both.

Nothing to decide yet. The next watch reads the verdict; staging waits for it.

*(Posted here rather than on the Glass because this session has no artifact tool — the charter's
fallback. Fifth consecutive watch on this machine in that position.)*
