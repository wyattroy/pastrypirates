<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

**Watch 13:10Z — your "yes" from 12:39 was sitting unread, and it cost us the job you'd just unblocked.**

At 12:39 you answered a question on this page: *may an unattended watch read the claude-kit folder?*
You said **yes**. Thirty-one minutes later a watch — me — wrote *"this half cannot be built here"*
into the code, because three earlier watches had tried to read that folder, been refused, and
written it down as a fact. **It was never a fact. It was a setting, and you had already changed it.**
A fresh reviewer caught it in one pass.

**The lesson, and it is going in the rulebook: "I tried and it was refused" tells you about the
fence, not about what's behind it.** Before anyone writes "we can't", they read your answers first.

**What actually changed today.** You'd ruled that this project owns its own copy of the shared
toolkit — that a local improvement should be *news*, not a build failure. Half of that shipped this
morning. The half that didn't: the check was passing on things it had never even looked at,
including a whole area it couldn't examine at all. It said PASSED about work it hadn't done. That's
fixed, with a new 16-check gate, six of which failed on the old code before they passed on the new.

**And the gate on the door is off.** Every watch from now on can read the toolkit folder — your
ruling, executed. That was one missing line. Five queued jobs were waiting behind it.

**Two things I could NOT finish, said plainly:**
1. **The other half you asked for — "tell me when the toolkit has fallen behind" — still isn't
   built.** My own session started before the gate came off, so I still can't see that folder. The
   next watch can. It's at the top of the list as `T-084`.
2. **Your 12:39 ruling still isn't filed in the decisions record.** That file is locked against an
   unattended watch. The text is written and waiting; it needs a session with permission (`T-085`).

**One more, because you should know it happened:** another session sharing this checkout wiped my
work-in-progress mid-write, silently. Nothing was lost — I still had it — but it's the seventh time
today, by a new route: restoring a file with a bare path throws away *everyone's* uncommitted work,
not just your own. Recorded, with the one-word fix.

*Verdict on file: PARTIAL, in the reviewer's own words — "you asked for two things and got one."*
