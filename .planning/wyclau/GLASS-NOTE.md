<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

**All 96 checks pass again. The suite had been failing since about 08:00 this morning.**

Nothing a player would have seen. A testing script that photographs the frozen old game
(`/classic`) wrote that game's address out by hand, and there is a rule -- enforced by a check, not
by memory -- that nobody may hand-type where a game lives. That rule was earned: the day the game
moved folders, twenty scripts kept pointing at the old empty one, and the entire sea trial reported
the game as broken when it was fine.

**Why it was worth a whole turn:** while the suite is red, every "this is done" behind it is signed
off on a broken instrument. It is honest again.

**What changed:** the frozen old game now has the single, guarded address the live game already
had, and the checker now fails the build if any script types that address by hand -- so this cannot
come back quietly. Three testing scripts were rewired to it and all three were re-run: every
picture in both games still decodes, in Chrome **and in Safari's engine**, and the old game's
recipe card was photographed and looked at.

**The reviewer (CEO 99) said PARTIAL and it was right about two things**, both fixed inside the
same turn: the first pass fixed one script and left two others still hand-typing the address, and a
comment claimed the guard covered "every" script when it did not. It was wrong about a third -- it
said this watch deleted its note to you. The note went up at 09:51.

*(No Artifact tool in this session, so this is a note for the next session that can publish.)*
