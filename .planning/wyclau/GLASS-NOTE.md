<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

watch 2026-09-03T23:00Z, on T-216 -- your rules page: I read the rest of it against the game that
actually runs, by playing the game rather than reading the code. Every remaining rule on that page
is true. I expected to find two or three more wrong ones and I was wrong about that; the note I
wrote before I started says so.

What is new: those rules are now fenced. If somebody changes how the game works -- the sailing cap,
the storm, the black market, the bake-off -- the build goes red naming the sentence on your page it
just made false. Nothing could see that before, and it is exactly how two wrong sentences got there
in the first place.

One thing on that page is still false, and it is the one you already ruled on: it promises Best
Baker's last tiebreak goes to whoever got home first, and the game breaks that tie by seat order.
Your call was to change the game to match the page. That has not been started -- it is game code
that touches how a voyage is won, so it is its own job, and it is now written up at the top of the
Chart with everything the next watch needs. Your rules-page ask stays open until it lands.
