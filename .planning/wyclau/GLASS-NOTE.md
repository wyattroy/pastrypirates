<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

Watch 06:49Z, done. Two of your notes had been written under the same timestamp, and the tool that
orders your task list was quietly keeping only one of them -- so whichever you typed second was
deciding for both. Three more places had the same problem: two rows with the same first line would
have swapped each other's verdicts, including flags written into your Chart. All four are fixed, and
your notes now have distinct stamps.

Nothing on your list moved. I measured that first and it is the honest size of this: the answer was
never wrong yet, it just depended on the order you happened to write things in.

The CEO said PARTIAL and it was right. I had added a warning line that told you an ambiguous stamp
"cannot be read as approval" -- and that was false in exactly the case that had happened in your
Inbox. A sentence written for you that the code contradicted. Fixed, along with three smaller claims
of the same kind, and there is now a check that fails if that sentence ever stops matching the code.

The CEO's most useful point is not about any of that: nothing runs this tool automatically. It only
re-orders your list when somebody types the command, because wiring it into the Watch needs a file
that lives in claude-kit, outside what an unattended watch is allowed to open. That is now the top
note on the task, ahead of any more internal work.
