<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---
watch 2026-09-02T06:0xZ: taking up the two ranking signals CEO 91 measured unsound in the
Chartkeeper — the ones that decide the order of your Tasks list. Right now the list can be gamed
by a row that simply types "at his instruction" into its own text, and it tells you things like
"you have raised it 10 times" about a row you have never mentioned. Both numbers reach your page
as ORDER. Grounding them in your own records instead of a row's self-description.

(This watch has no Artifact tool, so it cannot publish the Glass itself — this note is for the
next session that can.)
