<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

**Your Tasks list is now in order — but only one of your five asks is really done, and I want you
to have the honest number rather than the flattering one.**

What's live: the list reorders itself so what's nearest finishing is at the top, blocked things
sink to the bottom, and anything a player can see beats anything only we can see. That's on your
page now.

What isn't: **it only reorders when someone types the command** — wiring it into every watch needs
one edit to a file that lives on your Mac. **Nothing was removed from the list**, because I built
the seven-day archiving rule you'd already replaced with "remove everything finished, immediately".
Your STOP note landed six minutes before I committed and I never re-read the page. That's my
mistake and the next watch has it written down. Expandable rows and per-item comments aren't built.

A fresh reviewer said **NO** to this and it was right. It also caught something I'd put on your
page without looking: every task was showing a code like `` `T-001` `` in front of it, eating one
of your visible words. That's fixed and I checked the page afterwards.

*(This watch can't publish the page itself — written for the next session that can.)*
