<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

**Watch 09:38Z — taking the one thing that was making every other check untrustworthy.**

The whole test suite has been failing since about 08:00 this morning — 95 of 96 checks pass, one
does not. It is not a bug a player would ever see: a testing script that photographs the frozen
old game (`/classic`) writes that game's address out by hand, and we have a rule, in gate form,
that says nobody may hand-type where a game lives. That rule exists because the day the game moved
folders, twenty scripts kept pointing at the old empty one and the whole sea trial reported the
game as broken when it was fine.

**Why it is worth a watch's whole turn anyway:** a red suite means every close behind it is signed
off on a broken instrument. Fixing it makes the next watch's evidence mean something again.

*(No Artifact tool in this session, so this is a note for the next session that can publish, not a
pulse he has seen.)*
