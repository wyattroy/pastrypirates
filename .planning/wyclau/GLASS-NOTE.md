<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

**From watch f1, 2026-09-03 ~10:55Z. This session had no Artifact tool, so it could neither read
nor publish the Glass. Please fold this into the pulse line and publish.**

Pulse line: *"watch f1: there is probably already a Google Analytics account for your game, and
nothing has ever used it -- one question for you on Your Call."*

**ONE NEW QUESTION** in BLOCKED ON WYATT, `qid:t206-ga-turn-on` -- it should render on his Your
Call card as soon as the page is republished. The short version if a summary is wanted anywhere:
**the game's Firebase settings carry a Google Analytics ID (`G-2KK6EZDZSP`) and no page in the
repo -- 38 pages and 71 script files checked -- has ever loaded it, so it has collected nothing.
Switching it on is one line, not a setup job.** The question is only which pages and whether he
wants a cookie notice.

⚠ **THIS NOTE WAS WRITTEN ONCE ALREADY AND WAS RESET AWAY.** A peer session's Glass tick cleared
this file back to its template while this watch was still working, around 10:3xZ. Nothing of his
was lost -- it was this watch's own note, and it is re-written above. **But the same reset would
have destroyed a note carrying HIS words**, which is the hazard `INBOX-20260902T0350Z` already
names. Worth a row: the reset should refuse when the file has content the resetting session did
not write.

⚠ **THE GLASS WAS NOT HARVESTED BY THIS WATCH.** Anything he typed on the page since the last
harvest is still only on the page. The next session with an Artifact tool must read the live page
and run `node scripts/wyclau/harvest_glass.mjs --html=<saved file>` **before** republishing, or his
words are deleted.
