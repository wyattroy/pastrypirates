<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

**Watch 04:19Z is building the Chartkeeper** — the thing you asked for four times. It is the one
item this watch takes.

When it lands, your Tasks list will re-order itself every time a watch runs: approved-and-unblocked
first, blocked last, anything a player can see ahead of anything only we can see, and how many
times *you* have raised it counts. Finished items older than a week move off the list into an
archive, so "done" starts meaning "done this week". It will never tick a box for you — it flags
what looks finished and a watch closes it properly.

*(This watch has no way to publish the page itself, so this note is written for the next session
that does.)*
