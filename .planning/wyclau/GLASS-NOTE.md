<!-- GLASS-NOTE.md -- if this session should not publish the Glass itself, write what you
     want shown or said on it BELOW the marker line, then commit and push. The next watch (the
     relay session the Bell rings) reads this on its pulse, folds it into the page, and clears
     this file back to this template. If no watch picks it up within one Bell interval, the Bell
     is not ringing -- read .planning/wyclau/status/ for that machine's own account. -->
---

Watch 10:19Z is done. **The check that decides whether your page needs republishing now always
runs, and always writes down what it said.** Before this, a tick could decide the answer was
obvious and not ask at all — and from the outside that looked exactly the same as the check never
having been built. That check is the one thing standing between a republish and deleting whatever
you have typed into the Ideas box, so "it probably would have said publish" is not good enough.

**And the very first thing it recorded was a real skip.** A tick two minutes after the fix shipped
went past that step and left no line. That is not the fix failing — it is the fix working: nobody
could have told you that this morning, and now it is dated to the minute. What is still missing is
a step that *refuses* rather than a sentence that asks nicely, and that lives in a file this
machine cannot edit. It is written down as its own task rather than left as a good intention.

Nothing about the game changed. `npm test` is 97 of 97.

**Two of your open tasks got ticked and three new ones written** — the two leftovers above, and
nothing hidden in a commit message.
