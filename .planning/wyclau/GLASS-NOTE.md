# FOR THE NEXT SESSION THAT CAN PUBLISH — written by the Bell watch, 2026-09-03T17:2xZ

*A Bell-launched watch has no Artifact tool on this machine, so it cannot publish and must not
stamp `mark_glass_published.mjs`. This file is what it wanted shown.*

## 1. ⛔ PUBLISH `.planning/ANALYTICS-PLAN.html` AND PUT THE URL IN FRONT OF HIM

**This is rule 27 and it is currently UNDISCHARGED.** CEO 176's one sentence for Wyatt:

> *"The plan is real, its numbers are right, and I checked every one of them — but it has been
> handed to you as a file path in a folder, which on your phone opens the stylesheet, so ask for
> the link before you read a word of it."*

- The file is already in **publishable shape** — begins `<title>` then `<style>`, no
  `<!doctype>`/`<html>`/`<head>`/`<body>` of its own, no `localStorage` access anywhere, so there
  is nothing to fix before publishing it.
- **Publish it, then replace the path with the `https://` URL in two places:**
  - `.planning/CHART.md` — the `- [ ] Your ruling: ⟨T-206⟩ …` checklist row (this is what his
    Tasks card renders, so it is the one he actually reads);
  - `.planning/CHART-LOG.md` — the settled `qid:t206-ga-turn-on` row's verdict cell.
- **Then tell him in a message, not only on the page.** Rule 27's own note: the hook can see the
  file's shape and cannot see whether a link reached him — *"you are the reminder."*

**Why it is worth the step:** it ends with **two decisions only he can make**, both numbered with
a recommendation marked, in the format he asked for on 2026-09-03 (*"label your suggestions in the
same way as the claude question UI does — with numbers, and a (recommended)"*):

1. **Which pages get the tag** — public pages only *(recommended)* · everything including
   `/classic` · the game page alone.
2. **Cookie notice, or not** — cookieless with no banner *(recommended)* · standard GA4 plus a
   banner · standard GA4 with no banner.

And **step 1 is ten seconds of his:** open `analytics.google.com` and confirm `G-2KK6EZDZSP` is
there. Nothing was installed; nothing will be until he answers.

## 2. The pulse this watch could not send

> watch 2026-09-03T16:48Z → 17:2xZ: took `T-206`, his ruling on Google Analytics. **The plan he
> asked for is written and photographed; nothing was installed and consent is still his.** His
> third sentence — a sea trial whose depth a person can choose — is filed as its own row ⟨`T-220`⟩
> rather than folded in. CEO 176: **PARTIAL** — three findings acted on in the same turn, one
> (the link) needs a session that can publish.

## 3. What a later watch should NOT re-do

- **Do not re-ask him the analytics question.** He answered it (`qid:t206-ga-turn-on`), the answer
  is acted on, and the ruling is settled in `CHART-LOG.md`.
- **Do not install the tag before he answers the two decisions above.** Consent is explicitly his
  and has been since the row was written.
- **⟨`T-220`⟩ is not "add a bypass flag."** The row says why: an unconditional `--gear=cosmetic`
  re-creates the exact failure the depth rule was written against. Recommend the shape to him
  first — a depth a person can lower *on the record*, with the reason and the picker's own verdict
  both printed in the trial report.
