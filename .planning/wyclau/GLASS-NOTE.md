# For the next session that CAN publish the Glass

**Written by watch c2, Wy-Blade, 2026-09-03T08:2xZ. This watch has NO Artifact tool** — recorded
as a fact, not a guess about why. I cannot read the live page, so **the harvest is owed by
whoever publishes next**: read `glassState.ideas`, `glassState.rulings` AND `glassState.comments`
into the Chart before republishing, or his writing is deleted.

## What to put in front of him

**`T-114` is closed. The About page has stopped teaching four things the game does not do — and a
fifth that nobody had counted, because it was inside a picture.**

`about.html`'s "How it plays" section is deleted, which is exactly what he ruled last night
(*"Agree with your rec -- delete "how it plays"*, confirmed in the question UI: *"That's the whole
instruction"*). The two cards he said to keep — "What the captains are saying" and "Credits" —
are byte-unchanged.

What that section had been telling strangers arriving from Google:

- there is a **fish** action (fishing was deleted from the game outright)
- a dock flip wins you a **crate** (it pays coins; buying is a separate step)
- **first baker home wins**, bake-off as a tiebreak (baking is how everyone wins; two home the
  same day bake together)
- the wind sets a **sailing budget**, "cheap with it, dear against it" (sailing is free — the wind
  caps how far, it never charges)
- **and the Flippenator picture still showed the shot clock** — *"PLAY IN 13 seconds or pay 1
  coin"* — a mechanic removed on 2026-08-28. Found by looking at the before screenshot; no text
  check could ever have caught it.

His search-result snippet also still promised "how a round plays" on all three meta tags. Fixed.

## The thing he should probably hear about, plainly

**His answers from last night never reached the task list.** He answered the two rules-page
questions at 6:50 PM ET. They were filed correctly in the inbox — and no row on the Chart was
updated to say so. So `T-114`, `T-100` (build /rules.html) and `T-101` (the credits page) all kept
reading "blocked on Wyatt", and **three watches in a row skipped them for a blocker he had already
lifted.** That is now recorded on `T-099`, and **`/rules.html` is unblocked and is the top real
row.**

Two of his four rules-page questions are still his: `rules-page-3-of-4` and `rules-page-4-of-4`.

## Housekeeping for whoever publishes

- `.planning/posed/about-about-{before,after}-430w.png` are untracked leftovers from CEO 154's
  own verification run; this session's sandbox refused to delete them. **Please remove them.**
- `T-139` and `T-138` are Glass machinery sitting on the GAME chart (`.planning/CHART.md`), where
  they rank 1 and 2. His ruling `INBOX-20260902T2225Z` puts machinery on `GLASS-CHART.md` and the
  game on `CHART.md`. **One line each to move back** — a watch skipping them every ring is the
  cost until someone does.
