# PREDICTION — T-216, the rest of the rules-page audit

Written 2026-09-03T18:42Z by watch `pastrypirates-95 [3fed7c]`, **before measuring anything.**
Item: `INBOX-20260902T225008Z` — *"Do a new /rules.html that explains the rules — using the latest
version of the game"*. The previous watch checked 5 of ~20 claims (4 right, 1 wrong — the sanctuary
sentence) and deliberately left this open.

## WHAT I EXPECT

**Roughly 2–4 of the remaining claims are wrong or materially misleading.** That is the previous
watch's own arithmetic (1 in 5 wrong × ~15 remaining ≈ 3) and I have no better prior than theirs.

**Where I expect the wrongness to sit, and why.** Not evenly spread. A sentence drifts from the
engine when the engine's version of it is (a) an ordered list, (b) an edge case nobody plays, or
(c) something Wyatt re-ruled later. So my named suspects, in order:

1. **The Best Baker tiebreak** — *"most crates in the hold, then most coin, then whoever got home
   first."* An ordered three-key comparator is the single most drift-prone shape on the page: any
   later ruling that reorders the keys leaves the prose reading fine.
2. **"Nobody's paid on a battle that ends with no winner"** — a bounty edge case. Edge cases in
   prose outlive the branches that implemented them.
3. **The storm's "Land and other ships stop ye short — that's the whole of it."** *"That's the
   whole of it"* is an absolute claim, and absolutes are what break when a third stopper (the rim,
   a dock, a trade-wind square) exists in the code.
4. **The black market's "any two crates … even two of the same."** A barter rule with an explicit
   permission in it; the permission is the part that gets tightened.

**Where I expect to find them RIGHT:** the number-bearing sentences — sail range, the upwind cap,
crate prices, the flip payouts, the muse coin, the refire cost. Every one of those is an empty
`data-rule` span filled at runtime from `rulesFacts(cfg)` (`index.html:2862`), so they are already
mechanically joined to the engine's own config and **cannot** drift the way the sanctuary sentence
did. That is a structural argument, not a hopeful one — and it is testable.

## WHAT WOULD PROVE ME WRONG

- **If I audit every remaining claim and find ZERO wrong**, my estimate and the previous watch's
  arithmetic are both wrong, and the honest report is that the sanctuary sentence was the outlier,
  not the first of a set. **I must say that plainly rather than hunting for a fourth suspect to
  justify the prediction.**
- **If a `data-rule` span turns out to be wrong**, my structural argument above is false — the
  spans would then not be the safe class I claim, and the whole "numbers are safe, prose is risky"
  split I am about to audit by would need throwing out.
- **If the wrong ones are none of my four suspects**, my model of *how* prose drifts is wrong, even
  if the count lands. Naming the count right by luck is not the same as understanding the failure.
- **If a claim has no single code site to check it against** (it is emergent from several systems),
  then "audit each claim against the code that implements it" is not a method that reaches it, and
  I must say so instead of quietly scoring it as right.

## THE TRAP — what I want to be true

**I want the count to be low, because a low count lets me close `INBOX-20260902T225008Z` this
watch** and the row has now been left open twice. That is a live motive to read an ambiguous
sentence generously. The previous watch had the opposite discipline forced on it by CEO 181 — its
own falsifier fired and it still intended to tick. **So the standing rule for me: an ambiguous
sentence counts as WRONG until the code says otherwise, and if I cannot reach a claim I record it
as UNCHECKED, never as right.** The tick is not the goal; the audit is.
