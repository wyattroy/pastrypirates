# PREDICTION — watch 2026-09-03T1513Z (11:13 AM ET) · item INBOX-20260903T142249Z

**His words, verbatim:** *"Change the buttons that say Do It and Don't to Approve and Deny— and
always when giving me options to choose number or letter them"*

**He pressed DO NOW on it.** No `solution:` beyond the words themselves — but the words ARE the
solution for half of it, so his version goes in first with no investigation ahead of it.

## What I expect, and why

1. **The buttons live in exactly ONE place: `scripts/wyclau/glass.mjs`, the "Your call" card's
   `.ruleRow`.** Reason: a grep for `data-choice` across `scripts/`, `.claude/` and `.planning/`
   returns three lines, all in that file (1183–1185), plus two handlers reading the attribute.
   **Prediction: relabelling is a two-string edit and nothing else in the repo shows him those
   words.**

2. **The stored VALUE must not change.** `data-choice` is `yes` / `no` / `talk`, and
   `glass.mjs:1535` matches a saved ruling against that attribute to redraw the pressed state.
   Renaming the value would orphan every ruling already saved in `glassState.rulings` on his live
   page. **Prediction: label changes, value does not.**

3. **There IS a second surface showing him those words and it is not a button.**
   `harvest_glass.mjs:178` writes his ruling into `DECISIONS.md` as **Wyatt ruled "yes"** — the raw
   value. **Prediction: after the relabel, his page will say Approve and his own decision record
   will say "yes", which is rule 8 (same thing, same words, everywhere) breaking on the day of the
   change.** I expect to map the value to his new words at that one read site.

4. **The second half of his sentence — "always when giving me options to choose number or letter
   them" — is a WRITING rule, not a button.** It governs the question UI, the Chart's BLOCKED ON
   WYATT questions, and the Glass's Your-call prose. Precedent on the record:
   `INBOX-20260902T1904Z` ("always write to him in his local time") closed as *"no build, it is a
   writing rule"* and went to `DECISIONS.md`. **Prediction: the durable home is `DECISIONS.md` plus
   the rule-1 section of `.claude/CLAUDE.md` that every session loads, and no gate can honestly
   check prose for it.**

## What would prove me WRONG

- **On 1:** if the rendered page shows "Do it"/"Don't" anywhere OTHER than that `.ruleRow` — a
  second card, a demo ask, a `README`, the vendored claude-kit copy that `vendor_check.mjs` guards
  — then this is not a two-string edit and my grep was too narrow. **I will check the RENDERED
  HTML, not the source, before saying it is done.**
- **On 2:** if a saved ruling stops redrawing as pressed after the change, the value was load-bearing
  in a way I did not find and my "labels only" claim is wrong.
- **On 3:** if `harvest_glass.mjs` turns out to already map the value to words, my claim that the
  record will disagree with his page is false.
- **On 4:** if a mechanical surface exists that GENERATES options for him (rather than a human
  writing them), then "no gate is honest here" is wrong and it should be gated. I will look at how
  the Your-call questions are produced before concluding.
- **On the whole item:** if `glass.mjs` is vendored and this project may not edit it, the fix cannot
  land here at all. `vendor_check.mjs` exists precisely to catch drift, so I check what it covers
  BEFORE editing — his 2026-09-02 ruling says the project copy is the truth, but a ruling is not a
  measurement of what the gate does.

## WHAT HAPPENED — filled in after measuring

**1 — RIGHT.** Three lines, one file. `glass.mjs:1183-1185`. Nothing else in the repo showed him
those words; the rendered page confirms it (posed pair below), so the falsifier was tested rather
than assumed.

**2 — RIGHT, and it is now the thing a future session cannot get wrong.** The values are load-
bearing (`glass.mjs:1542` `paintAsk`), and case 4 pins them. Red-proofed: renaming `yes` to
`approve` fails at two named assertions.

**3 — RIGHT, and this was the part worth predicting.** `harvest_glass.mjs:178` wrote the raw value.
Case 5 was RED on the pre-fix tree with the exact sentence I predicted — *his decision record still
says he ruled "yes"*. Fixed with a value→word map that falls back to printing an unrecognised value
VERBATIM, because `note` is a real fourth value (a note typed with no button pressed still saves as
a ruling).

**4 — HALF WRONG, and the wrong half is the useful one.** The writing rule is a writing rule, as
predicted. But **I predicted its home would be `.claude/CLAUDE.md` and `.claude/memory/DECISIONS.md`
and I could write neither** — both edits were refused as protected files. That is not a guess about
permissions, it is two refusals, and it **answers the open question under `INBOX-20260902T2143Z`**
("his ruling says a watch MAY edit hooks and skills; whether the ALLOWLIST permits it is a separate,
mechanical question") with a measured **no** for `.claude/` generally. The rule went to
`.planning/wyclau/CHARTER.md` instead — the most-read file this watch could write — and case 6 pins
it there. **A watch cannot put a standing rule where sessions read standing rules. That is worth
more than this item.**

**5 — the falsifier I did not name, and it fired.** The explanatory comment I wrote beside the
buttons used backticks around `data-choice`. That comment lives **inside a JS template literal**, so
the backtick ended the string and the whole Glass generator stopped parsing. Caught one minute later
by the gate I had just written — before a commit, before his page. **The gate paid for itself before
it was committed.** I had no prediction covering "the comment breaks the file", which is exactly the
class of thing a prediction cannot cover and a check can.

**On the vendor question:** `vendor_check.mjs` reports `glass.mjs` as already AHEAD of claude-kit,
i.e. project-owned. So the edit was legal here. Measured before editing, not assumed from his
ruling.

## THE EVIDENCE

- RED first: cases 1, 2 and 5 failed on the pre-fix tree; cases 3, 4 and 6 passed from the first
  run and were therefore mutated deliberately (`scripts/qa/_redproof_ruling_words.mjs`) — three
  mutants, three deaths, each at its own named assertion.
- GREEN after: 6 of 6.
- POSED PAIR, same page, same real Chart, same 390×844 DPR 2 phone:
  `.planning/posed/donow-buttons-before.png` (**Do it · Don't · Let's talk**) and
  `donow-buttons-after.png` (**Approve · Deny · Let's talk**). Read side by side: the two labels
  changed, the third did not, and nothing else on the page moved.
- `npm test`: **121 gates, 119 green, 2 red — both pre-existing and both unreachable from this
  diff.** `rulings_triage_check.mjs` (reads `.planning/CHART.md`) and `sitemap_lastmod_check.mjs`
  (reads `sitemap.xml` and `index.html`). `git status` shows all three of those files unmodified by
  this change. Filed, not fixed — one item per watch.
