# PREDICTION — `T-111`, written 2026-09-03T00:40Z, BEFORE any measurement

*Rule 6's working form: write down what you expect and why, name what would prove you wrong, then
measure and say plainly which parts were wrong.*

## WHAT I EXPECT

1. **Neither writer checks the KIND of the value it is handed.** `mark_glass_harvest.mjs:96-117`
   and `mark_glass_published.mjs:51-69` both test only `if (!version)`. So
   `--version=2026-09-02T21:55:24.391Z` is accepted by both and lands in the receipt.
2. **No gate in the suite reads the CONTENTS of `artifactVersion`.** `glass_harvest_hook_check.mjs`
   asserts the writer stores *something* under that name (case 10/9, a source-text regex
   `/artifactVersion:\s*version/`). A source regex cannot see what runs through the variable.
3. **The two on-disk receipts on this machine now hold the CORRECT `<epoch>-<hash>` form** — I read
   them during orientation: `1788394958-ad3f` and `1788395643-2eb7`. So the symptom the Chart row
   photographed at 5:56 and 6:06 PM is NOT currently on disk. **That does not mean it is fixed** —
   nothing stops the next session writing a clock again, which is the whole item.
4. **`Date.parse` separates the two shapes cleanly.** `Date.parse("1788394958-ad3f")` is `NaN`;
   `Date.parse("2026-09-02T21:55:24.391Z")` is a number. So a clock-shaped value can be refused
   without hard-coding the platform's id format.

## WHAT WOULD PROVE ME WRONG

- If **either writer already refuses a date-shaped value**, expectation 1 is wrong and most of this
  item is already built — the fix collapses to the gate alone.
- If **any existing gate already fails on a clock in the receipt**, expectation 2 is wrong and the
  row's headline ("the gate cannot see it because it only checks the name") is false.
- If `Date.parse("1788394958-ad3f")` returns a NUMBER on this Node, expectation 4 is wrong and the
  predicate has to be a positive shape match instead — and a positive-only match risks refusing a
  real id if the platform ever changes format, which is a worse failure than the one being fixed.
- If the on-disk receipts still hold clocks, expectation 3 is wrong and the red-proof is the live
  file rather than the two values quoted in the Chart row.

## WHAT HAPPENED IMMEDIATELY BEFORE THE BUG (rule: widen the time horizon)

The Chart row already names it and I am not re-deriving it: the Glass-update session dispatches each
tick to a fresh subagent and sees only its prose report, never its tool calls. So a subagent that
substituted a timestamp for the version id was invisible to the session that reported the tick as
clean. **The wrong value spread from `LAST-HARVEST` to `LAST-PUBLISH` in about eleven minutes**, which
is what a mistake with no gate under it looks like.

## THE ONE DESIGN CALL I EXPECT TO HAVE TO MAKE, AND MY REASONING BEFORE I SEE THE RESULT

**Refuse a clock HARD; do not refuse an unrecognised-but-plausible id.** A hard refusal of anything
not matching `<digits>-<hex>` would mean that if the platform ever changes its id format, the harvest
stamp starts failing — and the harvest hook then denies every Glass publish, wedging the one surface
Wyatt steers from. The runbook's own rule for that hook is *"it must never wedge anything."* So the
predicate refuses what is provably the wrong kind and warns loudly on what it merely does not
recognise. **If I find myself writing a strict allow-list instead, this paragraph is the thing I
talked myself out of, and it should be said out loud rather than quietly reversed.**
