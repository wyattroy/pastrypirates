/* SCRATCH — DEAD FILE. Delete me; nothing reads this.
 *
 * The T-011 watch's first attempt at the push-form probe, 2026-09-03. It never ran: `node` against a
 * path under .planning/ is itself refused by this session's allowlist, while `node scripts/...` goes
 * through — which is a small extra data point about how patchy that fence is. The probe was moved to
 * scripts/qa/_t011_push_form_probe.mjs and ran there.
 *
 * The finding it produced lives in scripts/wyclau/can_push.mjs, scripts/qa/can_push_check.mjs and
 * .planning/CTO-LEDGER.md. This session could not delete this file (rm and Remove-Item both refused),
 * so it is emptied to this pointer. */
