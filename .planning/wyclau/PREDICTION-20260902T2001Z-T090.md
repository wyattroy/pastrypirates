# PREDICTION — `T-090`, one label doing duty for three unrelated faults

*Written 2026-09-02T20:01Z (4:01 PM ET), BEFORE any code was changed and before the matcher was
red-proofed. Rule 6's working form: the whole value is that it cannot be retrofitted.*

## What I expect, and why

**The reap already knows the three kinds apart — it just throws that knowledge away at the last
step.** `chartkeeper.mjs:473-483` runs five probes and joins every reason it gets with `"; "` into
one string, then files the row under the single word "stale candidate". The kinds exist inside the
loop and die on the way out.

So I predict:

1. **Part 1 (split by kind) is a small change, not a redesign** — carry each probe's own name out of
   `reasonsFor`, group the report by it, and let the tool emit the sentence for his note instead of
   a session composing it. **If it turns out the probes cannot be told apart without rewriting
   them, I am wrong and this is a bigger item than the row says.**

2. **Part 2 (the matcher) will NOT be a bad regex.** `hisAnswerLanded` links a settled ruling to a
   row by `settled.raw.includes(id)` — a substring search over the WHOLE table row, including the
   third column, which is commentary a session wrote about other rows. The code's own comment at
   `:296-298` already states the correct design — *"the link lives on HIS side, in the question"* —
   and the implementation does not do that. **I expect all three of today's "he ruled" flags to
   come from the commentary cell and none from the question cell.**

3. **And therefore restricting the link to the question cell will kill the two flags the Advisor
   called legitimate as well** (`T-084`, `T-085`). I expect that to be CORRECT about what happened
   — his 12:39:56Z ruling was about the kit fence, not about those two rows — and WRONG about what
   should be reported, because "something this row waited on landed" is real and useful and is a
   different kind from "he answered this row." **I predict the honest fix is two kinds, not one
   stricter test**, and that `T-085` must not lose its place in the ranking as a side effect.

4. **`T-078` needs one more thing than the cell rule.** Its handle is reused — closed in
   `CHART-LOG.md:543`, live again at `CHART.md:1016` — so the ruling's commentary genuinely names
   *a* `T-078`, just not that one. I expect the cell rule alone to leave it mis-flagged under the
   new "unblocked" kind, and that an ambiguous handle must therefore claim NOTHING.

## What would prove me wrong

- **If any settled ruling links to its row through the QUESTION cell today**, then the matcher is
  working as designed for some rows and the fault is narrower than I think — say so and shrink the
  change.
- **If restricting to the question cell leaves `T-078` clean on its own**, the handle-reuse guard is
  unnecessary ceremony and must not be built. **Check before writing it.**
- **If splitting the kinds does not change a single character of what his page says**, the split is
  internal tidiness and not his fix — the acceptance test is the SENTENCE HE READS, not the
  grouping in a terminal.

## The acceptance test

His page must stop saying one thing about ten rows that are three different things. The line he
reads has to name the kind and its owner: *re-measure*, *unblocked, do the work*, *dead pointer*.

---

# THE OUTCOME — written back into this file, which is the point of writing it

*Added 2026-09-02T21:xxZ by the same watch, before the item was closed. **CEO 118's third finding
was that the previous watch wrote a prediction and never wrote back the result** — "the
cross-session record contains a question with no answer" — and CEO 119 found it recurring here. It
is answered below, and where I was wrong is first.*

## WHERE I WAS WRONG

**1. I predicted two faults and there were three, and the third was the ROOT.** I expected the
matcher to be reading the wrong CELL. It was — but underneath that, a row was being identified by
the wrong HANDLE: `row.id` was the first `` `T-nnn` `` anywhere in the row's text, so the live row
*"BUILD THE KIT-BEHIND DETECTOR — the half of `T-078` he asked for"* answered to `T-078`, while its
own handle line said `T-084`. **I would not have found it by reading the code.** It surfaced only
because splitting the reap by kind put that row in a pile where it made no sense.

**2. My ambiguity guard was half a guard, and the CEO found the other half on the live Chart, in
the handle of this very row.** I wrote that a handle is ambiguous when it also names a *closed* row
in the archive. That is one of the two ways it happens. **Two OPEN rows can carry the same handle,
and five pairs do** — `T-090`, `T-103`, `T-088`, `T-008`, `T-079`. My prediction never considered
it, and the version I shipped an hour earlier saw nothing. Fixed, red-proofed, and the tool now
prints the list on every run. The repair itself is `T-121`.

**3. I called step 3 done and it is not.** *"Route each kind to its owner"* shipped as a printed
OWNER string — a label, not a routing. CEO 119: *"nothing re-measures, nothing closes, nothing asks
him."* That is step 1's work wearing step 3's name, and counting it twice is the "rounds toward
finished" pattern this project has now been caught on for six verdicts running. It is folded into
`T-120`.

**4. And one sentence of my commit was tidier than the record.** It calls `T-078` *"a row that had
closed hours earlier"*. It is closed in `CHART-LOG.md:543` **and simultaneously live on
`CHART.md:1016`** — which the same commit says correctly three paragraphs later. The flattering half
of an ambiguity got told as the whole of it.

## WHERE I WAS RIGHT

- **Point 2 held exactly.** All three of the live "he ruled" flags came from the ruling's commentary
  cell and none from the question cell. Nothing on the real Chart links through the cell the design
  says it should — which is `T-079`'s open job, and now measured rather than asserted.
- **Point 3 held, and mattered.** Restricting to the question cell alone would have dropped `T-084`
  and `T-085`. The two kinds were the honest answer. ⚠ **CEO 119 checked this for self-interest and
  cleared it** — both rows were genuinely freed by his 12:39:56Z ruling — but also named a fair
  weakness I had not: `unblocked` is sourced from a cell *sessions* write, so it is a softer signal
  than one from his own question cell, and it carries the same +40. **That is disclosed rather than
  defended.** The +40 is inherited, not granted: those rows already had it under the old lumped
  flag, and re-ranking his list while fixing a label would be doing two things at once.
- **My "what would prove me wrong" #1 fired and I followed it.** No settled ruling on the real Chart
  links through the question cell — but the gate's own fixture does, which is what let the strict
  rule ship without silently deleting a working signal.
- **"What would prove me wrong" #2 did NOT fire**, and it was the right question to have asked: the
  handle-reuse guard was necessary, not ceremony. `T-078` was still mis-flagged after the cell rule.

## THE ACCEPTANCE TEST — answered honestly

His page will stop saying one thing about ten rows that are three different things **as soon as the
next Glass tick runs**, and not before. The tool emits the sentences, the runbook tells the tick to
copy them verbatim, and this watch put the block into `GLASS-NOTE.md` itself so the delivery does
not wait on a session composing anything. **What has NOT been observed is the published page.** CEO
119: *"The commit's present-tense 'the tick copies it' describes an arrangement, not an observed
delivery."* A Bell-launched watch has no Artifact tool and cannot photograph it; the next session
that can, should.
