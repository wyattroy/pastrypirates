# HANDOFF — 2026-09-06, ~10:15 AM ET (Wyatt's local time; use it, it is his standing rule)

*Written by the Advisor session `pastrypirates-14` at Wyatt's ask: "this branch is now closed and
merged. write a handoff file and i'm giving it to a new session." Everything below was measured
this morning, not remembered. Where something is unverified it says so.*

---

## ⛔ READ THIS FIRST — TWO REPOS. THE MERGE HAPPENED, IN THE OTHER ONE.

**THIS WORK SHIPS TO `pastrypirates-staging`, NOT TO THIS REPO'S `main`.** Wyatt, 2026-09-06:
*"all of our work here is done for the repo pastrypirates-staging."* He merged this branch there
around 9:00 AM ET, and it worked.

**Measured at ~10:30 AM ET — everything below is a live fetch, not a memory:**

| | build stamp | meaning |
|---|---|---|
| `staging.playpastrypirates.com` | `2026.09.04.2-staging@0fdbe853` | **current — this branch, at this morning's commit** |
| this working tree | `2026.09.04.2` | matches |
| `playpastrypirates.com` (production) | `2026-08-26k-CUTOVER` | **older ON PURPOSE — not released yet** |

`staging.playpastrypirates.com/stats.html` → **200, rendering, `<title>Pastry Pirates — usage</title>`.**
`playpastrypirates.com/stats.html` → **404**, correctly.

> ### ⚠ THE TRAP THAT COST THIS SESSION THREE ROUNDS, AND IT IS RULE 6 WEARING GIT'S CLOTHES
>
> This checkout's `origin` is `github.com/wyattroy/pastrypirates` — **the PRODUCTION repo.** So:
>
> ```
> git merge-base --is-ancestor HEAD origin/main   → NO
> git rev-list --count origin/main..HEAD          → 1355
> ```
>
> Both numbers are **true and completely beside the point.** They measure whether the work reached
> *production*, which nobody claimed and which is gated on his approval. This session read them as
> *"the merge failed"*, told him so twice, and raised an alarm that **players are on an eleven-day-old
> build** — which is accurate, intentional, and not a fault.
>
> **An instrument that answers honestly about the wrong subject is the most convincing kind of
> wrong.** `docs/HARD-WON-LESSONS.md` §10 and CLAUDE.md rule 6 both say it: ask what the check
> actually looked at before repeating it to him. **To ask "did our work ship?", read the STAMP off
> the staging URL — never this checkout's ancestry.**

**Merging to this repo's `main` is a production release: served to real players immediately
(CLAUDE.md §6), and always his call, never a session's.** Do not do it because a handoff mentioned
it.

---

## THE SITUATION IN FIVE LINES

1. **The top of the Chart is `T-261`** — pinned DO NOW, and it is NOT delivered. See below.
2. **`T-073` (Add New SFX) is GATED** behind it and must not be started.
3. **A FULL sea trial is sailing right now** — pid `47468`, run `2026-09-06T1328Z-Wy-Blade`, build
   `2026.09.04.2`, started 9:28 AM ET, **5 of 10 legs** as of 10:12 AM. Report:
   `.planning/SEA-TRIAL-2026-09-06T1328Z-Wy-Blade.md`. **Do not start a second. Do not change game
   code before its verdict lands** — expect it around 11:00 AM ET.
4. **He is at the keyboard** and answering in the question UI. Two of his instructions today were
   corrections to how sessions talk to him.
5. **The Bell is live** (`schtasks` task `wyclau-bell`, every 10 min) and rings a fresh watch
   whenever none is on deck. **Watches cycled roughly every five minutes this morning** — four
   started and ended during one conversation.

---

## HIS INSTRUCTIONS TODAY, VERBATIM — all four are in `.planning/wyclau/INBOX.md`

| when (ET) | his words | where it landed |
|---|---|---|
| ~9:17 AM | *"Always give me times in my local time."* | `INBOX-20260906T1317Z` — a **re-statement** of his 2026-09-02 ruling (`04d73d21`), broken by a session that morning |
| ~9:29 AM | *"don't start the sfx work yourself -- i want the Watch to do all gamefacing changes."* | `INBOX-20260906T1329Z` — **his THIRD time ruling this** (see `INBOX-20260902T0058Z`, `INBOX-20260902T0400Z`) |
| ~9:33 AM | *"i do want you to add a new task to the watch at the beginning of the SFX changes, which is to scope the full SFX changes into a prd artifact and show it to me BEFORE you implement the sfx, because i have ideas that i want to express before that work is started"* | `INBOX-20260906T1333Z` → Chart row `T-261` |
| ~9:55 AM | *"i want to correct many assumptions made in the artifact -- make comment boxes in the artifact that i can write notes in for you, and you can read them"* | `INBOX-20260906T1355Z` → **reopens `T-261`** |

**A rule he has to repeat is a rule with no mechanism behind it.** Two of the four above are repeats.

---

## `T-261` — WHAT IS ACTUALLY LEFT, AND IT IS THE NEXT ITEM

**The PRD is written AND published.** He has the link:
**https://claude.ai/code/artifact/ed82256e-9196-4ada-bbef-60c4adc7df8d**
(`.planning/wyclau/T-261-SFX-PRD.html` is the source; published by session Blade 9.6 at ~9:45 AM ET
after he was asked directly through the question UI — never on a peer's say-so.)

**He read it and said it contains many wrong assumptions.** Asked to confirm five inferred sound
mappings, he refused the *shape* of the question, not the question: a multiple-choice card cannot
carry the corrections he has.

### THE JOB: give that page real comment boxes.

- ⛔ **Build them on `glassState.comments`** — shaped `{"T-nnn":[{"text":"…","at":"…"}]}` — the
  mechanism his Glass already uses and `harvest_glass.mjs` already reads back. **Never a fresh
  textarea** (rule 23, ONE DISPLAY PATH). Read `scripts/wyclau/glass.mjs`'s comment rendering and
  `harvest_glass.mjs` before writing a line.
- **A box he types into that nobody reads back is WORSE than no box** — it silently eats his
  corrections and he has no way to know.
- Guard every `localStorage` touch: in a private tab the accessor *throws* and hands him a blank
  screen.
- **This row is not delivered until the boxes work AND he can open the page.** Published is not
  delivered — that mistake already cost twenty minutes this morning (below).

### His four rulings on the PRD — full text in `.claude/memory/DECISIONS.md`, `RULING-20260906T1355Z-t261-sfx-prd`

1. **`PP_SFX_Alarm.mp3` = "Time up — the buzzer."** His sheet wants both timer sounds mechanical,
   not digital — worth a listen before wiring.
2. **Scope: ONE PASS**, Luis's files plus the already-chosen library sounds together.
3. **"Your turn": WAIT for a dedicated file from Luis.** Not `Navigation.mp3`, not ElevenLabs.
4. **The five inferred mappings are NOT confirmed** — Cannons, ClockTick, Ocean_Loop, the 5
   Seagulls, the 6 BoatCreaks. **Do not wire them as read.**

**STILL UNANSWERED — the second question card never reached him:** the PRD's **Q5** (fight-resolves:
a fresh unclipped export from Luis, or a second sound layered with the cannon), **Q6** (the 3 music
tracks stay out of this pass), **Q7** (level everything once at the end, or as it lands).
**Do not re-ask Q1–Q4.**

---

## `T-073` — THE SFX WORK ITSELF. GATED. DO NOT START.

**The Google Drive blocker is GONE.** He was present to grant the permission, both links opened, and
everything is captured in **`.planning/wyclau/T-073-SFX-BRIEF.md`** (commit `0fdbe853`): all 30
Drive files with ids and byte sizes, plus his full 28-sound plan sheet with his own wording for
where each fires and its ideal duration. **No session needs Drive access.**

**The headline finding, verified twice (by this session and independently by CEO 218):** all six
sound files already in the game are Luis's, matching **exactly in size, six for six.**

| shipped as | bytes | is actually |
|---|---|---|
| `battle-swords.mp3` | 35631 | `PP_SFX_Battle.mp3` |
| `coin-flip.mp3` | 15504 | `PP_SFX_CoinFlip.mp3` |
| `storm.mp3` | 177567 | `PP_SFX_Storm.mp3` |
| `store-ingredient.mp3` | 5343 | `PP_SFX_StoreGoods.mp3` |
| **`fishing.mp3`** | 54615 | **`PP_SFX_Anchor.mp3`** |
| **`ship-move.mp3`** | 17568 | **`PP_SFX_Navigation.mp3`** |

⚠ **Say "identical in size", never "byte-for-byte"** — no file was downloaded and hashed. CEO 218
finding 8 caught that chimera phrase in two files. **Hashing them is minutes of work and would make
the claim exact; nobody has done it.**

**THE REFRAME THIS UNLOCKS, and it is worth a line in `docs/AUDIO.md`:** `fishing.mp3` IS the anchor
sound. So DEFECT-1 (*"a dead file that can never play"*) and DEFECT-2 (*"anchoring dumps 8 seconds
of storm at full volume"*) both trace to the code mapping `anchorHold → fishing` — which everyone
read as a mistake and **which was correct.** Two documented defects born from a rename.

**Read `docs/AUDIO.md` before any of this.** `SFX_VOLUME` exists as the one tuning point and every
value is still `1`, across a measured **15.6 dB spread**; `battle-swords` clips at **+0.2 dBFS**,
which independently confirms his own sheet's note that the sword swish *"is clipped a bit"*.

---

## THE TRAPS FOUND TODAY — each cost real time, none is written anywhere else yet

1. **PUBLISHED IS NOT DELIVERED.** The DO NOW pin was handed from `T-261` back to `T-073` the moment
   the PRD was *published* — and for twenty minutes the Chart ranked **the SFX implementation he
   forbade at #1**, with the gate at #3. The hand-back instruction was right; its trigger was wrong.
   Caught by CEO 218 finding 3. **`--do-now` is ONE SLOT, not a queue** — pinning anything releases
   the previous pin, silently.
2. **THE SEA TRIAL REPLAYS CACHED LEGS AND SAILS NOTHING.** His 9:13 AM trial finished having sailed
   zero legs because `sea-trial-shots/legs/` held ten entries under the current stamp
   `2026.09.04.2`. That is Chart row `T-219` biting exactly as it predicts. **Before starting any
   trial, check `sea-trial-shots/legs/` for the current `PP4_STAMP`** (`src/ui/stage.js:43`) and
   clear that stamp's legs.
3. **THE `LONG-RUN` MARKER LOSES ITS OWN PID.** `start_trial_detached.mjs` writes
   `{label, startedAt, pid, reportPath}`; the trial's own progress update then rewrites the file as
   `{what, startedAt, updatedAt, progress, staleAfterMinutes}` — **pid and reportPath gone.** So the
   launcher's own documented liveness check is impossible minutes after any trial starts, and its
   guard against stacking a second trial reads `prev.pid` as `undefined`. **Observed, NOT fully
   measured** — nobody has confirmed the stacking guard actually fails. Worth a row and a
   prediction, not a repeated claim.
4. **A STASH/PULL/POP DESTROYED ONE OF HIS INSTRUCTIONS.** The `INBOX-20260906T1355Z` entry was
   written, lost, and survived only in an uncommitted `DECISIONS.md`. CEO 218 finding 4 predicted
   that fragility while it was already happening. **His words go in and get COMMITTED in the same
   act.**
5. **NOT EVERY SESSION CAN PUBLISH, AND THE ONES THAT CAN CHANGE BY THE MINUTE.** This session had
   no Artifact tool; three consecutive watches had none; `Blade 9.6` did. `3c0789ba` built a durable
   publish queue for exactly this. **Check, don't assume — in either direction.** This session told
   him twice that publishing was impossible while it had already happened.

---

## WHAT IS OPEN AND WAITING ON HIM (not a session's to decide)

- **`T-138`** — the player-count console. ⚠ **ITS OWN ROW ON THE CHART IS NOW STALE AND SAYS THE
  OPPOSITE: it reads *"both are 404 today"* and *"this row closes when you can load that URL."***
  Measured ~10:30 AM ET: **`staging.playpastrypirates.com/stats.html` returns 200 and renders.** He
  can load it. **So the condition that row set for itself is MET** — what remains is his look at it
  and, separately, the production release, which is a different decision.
- **PRD questions Q5, Q6, Q7** — see `T-261` above.
- **The production release** — promoting any of this from staging to `playpastrypirates.com`. His
  call alone, and not urgent: it is deliberately unreleased.

## WHAT IS OPEN FOR A WATCH

`T-261` (the comment boxes — next item) · `T-237` (trade-offer circle covers its own ask pill —
**re-measured 2026-09-06, real, phone-only, 10 of 42 poses, CEO 217 YES, left unfixed on purpose:
~900 lines of fragile placement code**) · `T-219` (the trial's resume cache) · `T-013` / `T-214`
(call-circle placement) · `T-239` (his rules living in files sessions don't read) · and the rest of
`.planning/CHART.md`.

---

## HOW TO WORK HERE

**Enter through the Door** (`.claude/skills/door/SKILL.md`). A session Wyatt opens is the
**ADVISOR** — record, advise, answer; **it does not touch game code** (his ruling, three times). A
Bell-launched session is a **WATCH** — one item, full Proof, then END.

**The last CEO verdict is 218**, in `.planning/CEO-REVIEWS.md`. Hand it to the next CEO so it can
spot a recurrence. **Its four remedies were all applied by the session it judged** — so a reader
seeing PARTIAL should not re-fix them; check the Chart first.
