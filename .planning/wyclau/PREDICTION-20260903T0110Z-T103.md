# PREDICTION — `T-103`, drag to reprioritise the Chart in the Glass

*Watch 2026-09-03T01:10Z, Wy-Blade. **Written BEFORE any measurement or code**, per the rulebook's
"write the prediction down before you measure". Each expectation carries the thing that would prove
it wrong.*

**His words:** *"DO NOW: build a way for me to drag to reprioritize the chart, in The Glass."*

---

## WHAT HAPPENED IMMEDIATELY BEFORE

The `T-104` watch built the **DO NOW button** — the other half of his 3:09 PM note — and split this
row off. It wrote `⟨… · now: yes⟩` onto a head line and taught `score()` to add 10000 for it. So the
Chart already has a place where **his own say-so** is written onto a row and read by RANK. **The
drag is the same mechanism with an ORDER instead of a single slot**, and the honest first question
is whether anything already carries an order. I expect not.

## THREE EXPECTATIONS, AND WHAT WOULD KILL EACH

**1. No task on his page carries its handle today, so there is nothing for a drag to grab.**
`glass.mjs:552` builds `tasks` as an array of plain strings and `:1020` renders `tasks.map(esc)`.
**Wrong if** the generated `glass.html` already contains a per-task identifier — a `data-handle`, a
`T-nnn`, anything a drop could name. Then the work is smaller than this row says.

**2. Nothing in the Chart's grammar can express "his order", and `now: yes` cannot be stretched to
carry it.** `headIsPinned` matches one boolean field, and the pin is deliberately ONE SLOT — his own
constraint, quoted at `chartkeeper.mjs:107-112`: *"an interrupt with a queue is just another
backlog"*. **Wrong if** a head field for sequence already exists (`rank:`, `order:`, `pos:`) or if
`score()` already reads a numeric ordering. Then I should extend that, not invent one.

**3. HTML5 drag-and-drop will not work for him, because he reads this page on a phone.**
`dragstart`/`drop` are mouse-only on iOS Safari. **Wrong if** the page already carries a
touch-capable drag anywhere (a slider, a reorder, a swipe) that I can copy. I expect it does not, and
that the answer is pointer events (`pointerdown`/`pointermove`/`setPointerCapture`), which are one
code path for mouse AND touch — which is also rule 23's answer: one gesture, not two kept in step.

## WHAT I EXPECT TO BUILD, IN THE ORDER THE FOUR STEPS DEMAND

RED first: a gate asserting the three joints, failing on every one of them today.
Then: (a) each Tasks row carries its handle; (b) the page saves the sequence he drags into as
`glassState.order`; (c) `chartkeeper.mjs --order=` writes it onto the rows and RANK obeys it.

**THE ONE THING I EXPECT TO GET WRONG:** the ranking margin. His drag order must outrank every
DERIVED signal and I currently think it must sit BELOW `now: yes` — a pin is a sharper, later act
than an ordering. **That is a judgement, not a measurement, and it is his to overrule.** If a watch
later finds he re-drags to undo a pin, the margin is backwards.

**THE FAILING CASE FOR THE WHOLE ITEM:** if, after this ships, an order he drags on the page does not
survive to the next `--rank` run — because the harvest never carries it, or a republish drops
`glassState.order` the way it drops `rulings` — **then nothing was fixed and the drag is decoration.**
The round-trip is the acceptance test, not the gesture.
