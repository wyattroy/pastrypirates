# PREDICTION — T-247, "we need to push all these changes to staging!!"

Written 2026-09-03T21:40Z, Wy-Blade (Windows), **before any measurement**, per the Door step 3.

His words, `INBOX-20260903T213129Z`, 21:31:29Z — eight minutes ago as I write:
> we need to push all these changes to staging!!

No `solution:` stated. So there is no solution-first obligation beyond the obvious reading: run
this branch's tree through the release loop's staging half and give him a URL he can open.

## What I expect, and why

1. **The publish command is `npm run deploy:staging -- "<what changed>"`**, which runs
   `scripts/deploy-staging.sh` — a bash script. `CLAUDE.md` §6 and `docs/GIT-AND-DEPLOY.md` §5 both
   print exactly that line, and the script is the only file in `scripts/` matching `deploy`.

2. **⚠ MY LEADING WORRY: this script has never been run on Windows, and I expect it to fail on a
   missing tool.** The evidence is in `docs/GIT-AND-DEPLOY.md`'s cloud table: the first cloud run of
   this same script died three times in a row on `gh`, then `rsync`, then BSD `sed -i ''`. Every one
   of those is a "it exists on Wyatt's Mac" assumption. Git Bash on this laptop ships **no `rsync`**
   in my experience of it, and I have already proved this session that Git Bash here has **no
   `pkill`/`pgrep`** (CLAUDE.md rule 17's own warning box, earned on this machine 2026-09-02). So I
   predict `rsync: command not found`, and possibly `gh` too.
   **If that happens the correct move is NOT to hand-roll the copy.** `docs/GIT-AND-DEPLOY.md` §7 is
   explicit: rsync's `--exclude` list is what keeps `CNAME`/`robots.txt`/`sitemap.xml` out of the
   staging repo, and hand-rolling that sync is the exact move that twice came within one command of
   taking the live game down. I would install rsync, or report the block — never substitute `cp`.

3. **I expect `npm test` to pass**, because the last four watches each closed an item through
   `close_item.mjs` and the branch has been green all evening.

4. **I expect the gear to be COSMETIC or NONE**, because the commits since the last staging publish
   are overwhelmingly `.planning/`, `scripts/wyclau/` and gates — not `index.html` or `src/`.
   **If the gear comes back FULL, this prediction is wrong and the item is much bigger than his
   sentence sounds** — a full sea trial would have to sail before anything is published, and that is
   a detached job (Door step 4), not something this watch finishes.

5. **I expect the stamp check to be the thing that proves it landed** — staging must serve
   `<stamp>-staging@<sha>`, per §5 step 6. A bare stamp means he is looking at production.

## What would prove me wrong

- **rsync and gh both present and the script runs clean on the first try** → my whole "Mac
  assumption" thesis is wrong, and the interesting part of this item never existed.
- **`gear.mjs` reports FULL** → point 4 is wrong; this cannot be finished in one watch and must be
  reported to him as such rather than published on a shallow trial.
- **`npm test` red** → point 3 is wrong, and nothing publishes until it is green.
- **The staging site already serves this branch's sha** → the item is already done and the right
  answer is to tell him the URL, not to publish again.

## The measurement, in order

`npm test` → `node scripts/qa/gear.mjs` → read `deploy-staging.sh` → run it → `curl` the staging
stamp. Result written back into this file.

---

## RESULT — measured 2026-09-03T21:41Z–22:05Z

**I WAS WRONG ON THE TWO PREDICTIONS THAT MATTERED, AND WRONG IN THE SAME DIRECTION BOTH TIMES:
I assumed the job was undone and hard. It was already done.**

| # | I predicted | measured | verdict |
|---|---|---|---|
| 1 | the command is `npm run deploy:staging` | it is — but it never needed running | right, irrelevant |
| 2 | **`deploy-staging.sh` fails here on a missing `rsync`/`gh`** | **BOTH ARE INSTALLED on this machine** — `/c/ProgramData/chocolatey/bin/rsync`, `/c/Program Files/GitHub CLI/gh` | **WRONG** |
| 3 | `npm test` passes | passes, 0 failures | right |
| 4 | **the gear is COSMETIC or NONE** | **`gear.mjs` says FULL** — it reads everything ahead of `origin/main`, which on a 465-commit branch is the whole game | **WRONG** |
| 5 | the stamp proves the publish landed | **the stamp CANNOT prove it** — see below | right in spirit, wrong in method |

**MY OWN FALSIFIER FIRED, which is the only reason this watch did not re-publish for nothing.**
I wrote: *"the staging site already serves this branch's sha → the item is already done and the
right answer is to tell him the URL, not to publish again."* It does. `curl` returned
`PP4_STAMP = "2026.09.03.4-staging@a72f2f12"` — **and `a72f2f12` is the very commit that RECORDS
his instruction**, so somebody published within a minute of him asking. HEAD is one commit further
on (`a78021ae`) and that commit touches `.planning/CHART.md` only, which is never published.

**AND THE PREDICTION I ALMOST DID NOT WRITE IS THE ONE THAT SAVED THE ITEM FROM BEING A LIE.**
Point 5 said the stamp is the proof. **It is not, and `docs/GIT-AND-DEPLOY.md` §5 says why in its
own words:** `deploy-staging.sh` *rewrites* `PP4_STAMP` on the published copy, so the stamp is a
claim the publisher wrote about itself — and on 2026-08-27 staging served DIFFERENT CODE under a
stamp identical to production, *"worse than no stamp, because the one tell Wyatt uses to know which
build he is looking at was actively lying."* A watch that read the stamp and told him "it's live"
would have been repeating the publisher's own claim back to him as verification.

**So the check compares BYTES, and derives its file list from git rather than from my typing:**
`scripts/qa/_t247_staging_parity.mjs`. 1,080 files change on this branch; 845 are never published
(site-identity files and the tracked dirs the deploy excludes); of the 235 that must be live, 65
are deletions and **all 170 remaining are byte-identical to this tree** — every asset, every module,
`classic/`, and all four pages. The only two differences anywhere are the two the deploy is
*designed* to write: the `[STAGING]` `<title>` prefix and the stamp line.

**RED-PROOFED, because a check that cannot fail is not a check.** Same gate, same tree, pointed at
**production**: **153 of 235 files DIFFER or 404**. Both verdicts are reachable, so PASS means
something.

**AND BYTES ARE NOT A GAME (rule 19).** `scripts/qa/_t247_staging_shots.mjs` photographed the real
staging origin at 1280×900 and 390×844: the lobby draws, all four mode buttons and their art are
there, `window.firebase` is **LOADED** (so Host/Join can work at all), the title carries
`[STAGING]`, and `rules.html` renders. Pictures in `.planning/posed/t247-staging-*.png`.

**WHAT I DID NOT DO, deliberately:** start a sea trial. One is already at sea on this exact build
(`2026.09.03.4`, 7/10 legs at 21:44Z), begun by an earlier watch. Starting a second is the `T-026`
fault. **His staging publish is verified; the build's VERDICT is that trial's to give.**
