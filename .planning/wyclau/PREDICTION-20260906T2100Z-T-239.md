# Prediction — T-239, moving the "number or letter the options" rule to its canonical home

**Item:** `.planning/CHART.md` row `T-239` — his DO NOW rule (`INBOX-20260903T142249Z`, *"always
when giving me options to choose number or letter them"*) is written only in
`.planning/wyclau/CHARTER.md` §"Putting a choice in front of him". Two prior watches (2026-09-03,
CEO 172) found their Edit tool refused on `.claude/CLAUDE.md` and `.claude/memory/DECISIONS.md` and
parked the rule in the CHARTER instead — one hop off the path every session actually reads first.

**What I expect:** this session's Edit tool will NOT be refused on either file. Two sessions on
2026-09-02 (`eee58a5d`, `04d73d21`) successfully edited both files, including the exact precedent
this rule follows (`04d73d21`, "always write to me in my local time"). The refusal in T-239's own
text is described as a property of "one agent's tool", not the files themselves — so it should not
recur here, though it could (sandbox/session config can differ).

**Why I believe it:** `.claude/CLAUDE.md` is a project file, not `.gitignore`d, not flagged
elsewhere in this repo's docs as edit-protected for any session. The two 2026-09-02 commits are
direct proof the file has been edited by an agent before.

**What would prove me wrong:** the Edit tool returns a refusal/error when I attempt to write to
`.claude/CLAUDE.md` or `.claude/memory/DECISIONS.md`. If so, I fall back exactly as the CHARTER
text and T-239 anticipate: say so plainly in the ledger, leave the CHARTER copy as the working
home, and do not silently write the rule somewhere else.

**The plan if the prediction holds:**
1. Point `scripts/qa/glass_ruling_button_words_check.mjs` case 6 at `.claude/CLAUDE.md` (the
   canonical home) instead of the CHARTER — confirm it now FAILS (red), proving the gap is real
   before touching anything else.
2. Add the rule into `.claude/CLAUDE.md` §1's "Ask with the question UI" block.
3. Add an entry to `.claude/memory/DECISIONS.md`.
4. Update the CHARTER's own "WRONG HOME" note to say it has moved, rather than deleting the
   history.
5. Re-run the gate — confirm GREEN.
6. `npm test` full run, gear NONE (no game code touched).
