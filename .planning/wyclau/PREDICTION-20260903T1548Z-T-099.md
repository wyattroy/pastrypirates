# PREDICTION — watch h1, 2026-09-03T15:48Z, `T-099` (the rules-page content split)

Written BEFORE any measurement, per the Door step 3 and CLAUDE.md rule 6's working form.

## What the row says its own close condition is

`CHART.md:1104-1168`, in its own words: *"THIS ROW STAYS OPEN ON PURPOSE: the questions are asked
and the recommendation is written — the DECISION is his… This row closes when he answers, not
before."*

## What I expect to find, and why

1. **All four of his questions are answered**, and the row already says so in two later notes
   (Q1 → `INBOX-20260902T225008Z`, Q2 → `INBOX-20260902T225032Z`, Q3 → `T-117`, Q4 → `T-118`).
   So the close condition is met and the row is stale on his page.
   *Why I expect this:* the row's own text, corrected by watch e3 on CEO 171's finding, states it.
   I am NOT taking that on trust — see the falsifiers.
2. **The work those answers authorised has landed**: `/rules.html` exists (`T-100`, CEO 171,
   commit `4dbc7842`), About's "How it plays" is deleted (`T-114`, CEO 154, commit `c5ca91b8`),
   the modal keeps its full text, and the page speaks pirate.
3. **Therefore this item's remaining work is zero game code** — it is a decision row whose
   decision he made, and closing it removes a row from his page that still claims to be waiting
   on him. That is his own standing complaint of 2026-09-02 6:57 PM ET
   (*"the page continues to re-show me the questions AFTER they're harvested"*) wearing a
   different hat: not a question re-shown, but a row that says it is waiting for an answer he
   already gave.

## WHAT WOULD PROVE ME WRONG — the falsifiers, named before I look

- **F1.** If any of the four INBOX entries above does not exist, or its text does not answer the
  question the row asked, then "he answered" is my inference and not a fact, and this row is not
  closeable.
- **F2.** If `rules.html` does not exist at the repo root, or `about.html` still carries a "How it
  plays" section, then the work his answers authorised has NOT landed, and closing this row would
  tick a decision whose consequences are still open.
- **F3.** If a `BLOCKED ON WYATT` question in `CHART.md` still names `T-099` or the rules-page
  split, then the row is genuinely still waiting on him and I must not close it — I would be
  answering his question for him.
- **F4.** If the ranker put this row first for a reason other than its own merit — e.g. it is
  scoring +100 three times over from citations rather than from being next-to-be-completed — then
  "take row one" gave me a bookkeeping item while a player-visible bug sits below it. I will say so
  plainly rather than dress the close up as progress.

## WHAT HAPPENED — written after the measurement, falsifier by falsifier

**F1 — did NOT fire.** All four answers exist, verbatim, with timestamps: Q1
`INBOX-20260902T225008Z` / ⟨`T-115`⟩, Q2 `INBOX-20260902T225032Z` / ⟨`T-116`⟩, Q3 ⟨`T-117`⟩
22:51:10.628Z, Q4 ⟨`T-118`⟩ 22:51:18.219Z. He answered.

**F2 — did NOT fire, and the check nearly went the other way.** `grep -c "How it plays" about.html`
returned **3**, which reads as a falsifier hit. Opening the three lines showed one CSS comment and
two HTML gravestones; the section itself is gone. *A count is not a reading.*

**F3 — did NOT fire.** No `BLOCKED ON WYATT` question names `T-099` or the rules-page split.

**F4 — FIRED, and this section exists because I did not answer it the first time.** I wrote *"if the
ranker put this row first for a reason other than its own merit… I will say so plainly rather than
dress the close up as progress"*, then wrote the close and left this file ending at *"What I will
NOT do"*. **CEO 173 found the blank and answered the question for me: NO, this was not the right
item.** Its words: *"A watch was spent on bookkeeping: zero game code, no screenshot, and the
deliverable was one row leaving a list."* It named the row it would have taken instead —
`CHART.md:389`, the CAPTAINS panel showing through every modal on tablet, which the Chart's own text
calls *"the one unambiguously broken"* thing and which two posed screenshots would settle.

**Why the ranker put a bookkeeping row first, since that is the reusable part.** `T-099` scored 154
from three signals that all measure ATTENTION, not remaining work: it is his own writing (+100), he
raised it in three notes (+24), and it is tagged player-visible because it is *about* a page a player
sees. **Nothing in `score()` asks whether the work is already done** — the row had been finished for
twelve hours and still outranked measured, unclaimed, player-visible bugs. That is a scoring gap
worth filing, and it is not the same as ⟨`T-216`⟩.

**The honest summary: the close is correct and the item was small.** Both halves belong in the
report he reads.

## What I will NOT do

- I will not touch `/rules.html`, `about.html`, or the modal. A detached sea trial is at sea
  (`.planning/wyclau/LONG-RUN`, 4/10 legs, started 15:03:21Z) reading these very files.
- I will not hand-tick the Chart or the INBOX. `close_item.mjs` writes the tick, the fate and the
  ledger entry together.
