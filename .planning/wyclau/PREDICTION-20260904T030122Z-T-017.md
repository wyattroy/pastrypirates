# Prediction — T-017 (trade-offer circle name sizing), watch 2026-09-04T0301Z

**What I expect, and why.** Commit `4c6f1624` (2026-09-03T16:38:34-04:00) implemented his ruling
("Do bigger circles, not smaller text") for all three T-017 rows in `.planning/CHART.md`, and left
them open pending one thing: `.planning/SEA-TRIAL-2026-09-03T2031Z-Wy-Blade.md`, a FULL trial on
build `2026.09.03.4` (the build the fix shipped in), pid 53536. The commit message says the close
is blocked ONLY on that trial, and instructs the next watch to run
`close_item.mjs --ceo=184` on all three T-017 rows "if it sails clean."

I expect the trial report, now finished (pid 53536 no longer running — Chart line 374 already
flags the pid as dead), to contain **no failure naming the trade-offer circle, a captain's name
being clipped/severed by its disc, or the sibling defect class** across all 10 legs — because the
gate that catches this class (`trade_circle_type_size_check.mjs`) is in `npm test`, which the
commit says was 128/128 green including this gate, and the fix is unrelated to anything else the
trial exercises.

I do NOT expect the trial's overall verdict to read PASS — sea trials in this project routinely
fail on settle-timing geometry churn and vision-judge findings unrelated to the item being closed
(rule: "nine of the ten fail only on known instrument noise"). A trial-level FAIL is not evidence
against T-017 specifically; only a T-017-shaped finding inside it would be.

**What would prove me wrong:** any leg's vision-judge or structural findings naming a captain's
name overflowing, clipping, or being severed by an offer/trade circle (the exact defect T-017
fixed) — solo-tablet's "captain who calls the winner" wording would also count if it turns out to
be the T-017 defect recurring under different framing. If that appears anywhere in the 10-leg
report, T-017 is NOT sailing clean and must stay open.

**What I will do:** read the full report (already open above), grep it for circle/clip/sever/name
language, and separately re-run `node scripts/qa/trade_circle_type_size_check.mjs` on the current
tree (not just trust the commit's claim) before closing.
