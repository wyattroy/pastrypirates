# PREDICTION — watch 2026-09-03T18:09Z, item `T-216`

*Written BEFORE any measurement, per the Door's step 3. What I expect, why, and what would prove me
wrong.*

## The item

Chart row `T-216`: **`INBOX-20260902T225008Z` still reads `status: OPEN`, and the job it asks for was
finished yesterday.** His ruling was *"Do a new /rules.html that explains the rules -- using the
latest version of the game."* The row says the page was built and closed this morning under `T-100`,
CEO 171, commit `4dbc7842`, and that the fix is a close through `close_item.mjs`.

## WHAT I EXPECT

1. **The page really exists and really was built** — I have already seen `rules.html` on disk
   (11,092 bytes, written 2026-09-03 11:00). So the *work* half is not in doubt.
2. **THE ROW'S CITED COMMIT IS WRONG.** `git log --oneline -1 4dbc7842` reads
   *"INBOX-20260903T142249Z CLOSED (CEO 172, PARTIAL) -- his buttons say Approve and Deny"* — a
   different item entirely. The rules page landed under `INBOX-20260902T190730Z`, whose own fate line
   reads `DONE 2026-09-03 — CEO 171, commit 067760a`. **So I expect the correct evidence pair is
   `--commit=067760a --solution-commit=1efe53a`, not `4dbc7842`.**
   *Why I expect this rather than a typo in the fate line:* the fate lines are written by the gate,
   which assembles them from what it was passed; the Chart row was hand-written prose by the watch
   that filed it. **The gate-written record is the more trustworthy of the two**, so where they
   disagree I expect the hand-written one to be the error.
3. **His second instruction — "using the LATEST version of the game" — is the half most likely to
   have been skipped**, because the INBOX entry itself warns in those words that it would be. I
   expect to find that CEO 171 or the closing commit says something about checking the rules against
   the shipped game rather than copying the modal's 765 words forward. If it does not, this item is
   NOT finished and must not be closed.
4. **The two siblings the row asks me to check are already closed.** `INBOX-20260902T190730Z` reads
   DONE (CEO 171) and `INBOX-20260902T225032Z` reads DONE (CEO 154). I expect no further stale
   siblings in that cluster.

## WHAT WOULD PROVE ME WRONG

- **If `rules.html`'s text is a copy of the in-game "How to play" modal's 765 words**, then his
  second instruction was skipped, the row's premise ("the job it asks for was finished") is false,
  and the right act is to leave the item OPEN with that stated — not to close it. *This is the
  falsifier that matters and it is the one I must actually run: read both texts and compare, not
  reason about whether someone would have.*
- **If `067760a` does not touch `rules.html`**, my reconstruction of which commit carries the work is
  wrong and I must find the real one before citing anything.
- **If CEO 171's verdict is on file and says the "latest version" half was not verified**, I should
  report that rather than close on a verdict that does not cover the ask.
- **If a third sibling in that Glass burst is also stale-OPEN**, expectation 4 is wrong and the close
  is incomplete.

## WHAT HAPPENED — written after measuring, saying plainly which parts were wrong

**Expectation 1 (the page exists and was built): RIGHT.** `rules.html`, 11,092 bytes, commit
`1efe53ab`, generated from the modal — genuinely one-source, not a copy.

**Expectation 2 (the row's cited commit is wrong): RIGHT.** `4dbc7842` is
*"INBOX-20260903T142249Z CLOSED (CEO 172) -- his buttons say Approve and Deny"*, a different item.
The rules-page work is `1efe53ab` (build) and `067760a` (CEO 171's link fix). The reasoning behind
the prediction also held: the gate-written fate line was the accurate record and the hand-written
Chart prose was the error.

**Expectation 3 (the "latest version of the game" half was skipped): RIGHT, AND IT WAS NOT
COSMETIC.** The falsifier I named — *"if `rules.html`'s text is a copy of the in-game modal's 765
words, his second instruction was skipped"* — **fired.** Commit `1efe53ab` says so in its own words:
*"Not one sentence was rewritten; the extraction is verbatim."* That is correct engineering for
rule 23 and it carried the modal's stale prose onto a public page.
**And the stale prose was WRONG about a rule Wyatt himself made.** The page told a reader
*"a berth protects nobody, not even a captain who's already fired up the ovens"*; `Game.canAttack`
has refused exactly that attack since his 2026-08-06 SANCTUARY ruling. **Measured by running the
engine, not by reading it:** defender not baking → `canAttack = true`; defender baking →
`canAttack = false`, at the shipped default `cfg.bakeoff = true`.

**Expectation 4 (no other stale siblings): RIGHT.** `190730Z` is DONE (CEO 171) and `225032Z` is
DONE (CEO 154). `225008Z` was the only one left open.

### WHAT I GOT WRONG, in the open

**I nearly trusted a comment, and it said the opposite of the code beneath it.** The block above
`canAttack` still reads *"there is deliberately no `def.done` check: v2 rule 13c is 'nobody is
safe'"* — which is the OLD rule, and which agrees with the wrong sentence on the page. Had I
stopped at the comment I would have confirmed the error and closed the item. The line four rows
below it (`if(this.cfg.bakeoff&&def.baking)return false;`) is the truth, and only running it settled
which of the two to believe. **That is rule 6's "a comment is not a measurement" landing on me
directly, and it is why the new gate is behavioural rather than a grep.**

**And the honest LIMIT of what I checked, stated because "using the latest version of the game" is
his sentence and I have not fully discharged it:** I checked **five** substantive prose claims
against the code — sanctuary, Best Baker's tiebreak order, the call bounty and its null-battle rule,
the black market's any-two-crates barter, and the empty-hold rule. **Four were right; one was
wrong.** The page carries roughly twenty such claims. **The rest are unaudited**, and that is filed
as its own Chart row rather than waved through here.

## THE TIME-HORIZON QUESTION — what happened immediately BEFORE?

The rules-page cluster is four Glass rulings answered in one burst at 2026-09-02T22:50Z
(`rules-page-1-of-4` … `4-of-4`). Three of the four have fates; one does not. **The likely cause is
not a bug but an ordering artefact**: `225008Z` is the ruling that *unblocked* the build item, so the
watch that did the build closed the item it was building (`190730Z`) and never went back to close the
ruling that authorised it. If that is right, I should also look for `rules-page-3-of-4` and
`4-of-4` — the same omission would hit them the same way.
