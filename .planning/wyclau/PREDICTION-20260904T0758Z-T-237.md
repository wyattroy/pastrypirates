# PREDICTION — T-237, the trade-offer circle vs. the ask pill (CEO 198's next step)

**Watch started:** 2026-09-04T0758Z, `claude/cloud-handoff-planning-a9ay1u`.

## What I'm about to measure

CEO 198 (recorded in `.planning/CEO-REVIEWS.md` and `.planning/CHART.md` under `T-237`) found a
real screenshot on the shipped build (`2026.09.03.4`, after the `T-235` bigger-circle fix landed)
where a trade-offer circle — "Flaky Jack" — is drawn over its own prompt's ask-pill text
(`crew-desktop-guest-021-settled.png`, `no-cover-ask` structural hit from the 2031Z sea trial).
CEO 184's own risk note (finding 5) predicted this before it shipped: "bigger circles cover more
of the ask pill … NOT yet measured either way." Nobody has measured it since.

The recommended next step (CEO 198, and rule 26) is a POSED pair — same seed, before/after — not
another full sea trial, because this is a yes/no geometry question a stochastic voyage already
failed to answer efficiently.

## What I expect, and why

**I expect the coverage is real and reproducible on a posed board**, not a one-off animation
artifact. Reasoning: the screenshot in question is a `-settled` frame (the trial only captures
those after its own settle-detection passes), and the sea trial's `no-cover-ask` check uses the
same `shapeOverlap` geometry as `w54_call_clear_of_ask.mjs`'s `MEASURE` function — the same
instrument CEO 198 independently verified as a real structural hit on the prompt's own `.apMsg`.
I do not expect this to be a false positive from an unsettled frame, because CEO 198 already ruled
that out (`scripts/lib/checks.mjs:135-142`, verified against the real `.apMsg`).

I expect the trade-offer prompt (`humanTrade` in `src/ui/flow.js`, the `ask(...)` call at
`flow.js:2208-2210`) to reproduce the cover when the answer text is long (many captains, or a
counter with both a crate and coins) and the fight is near the top of the board, where the pill
has less room to grow downward before the circles' row.

## What would prove me wrong

- If a posed sweep across the same board positions `w54` already walks (rows 0, 1, 2, mid, N-2,
  and the two top-corner adjacents) with realistic trade-answer text never produces an overlap,
  my prediction is wrong and the screenshot was a fluke of that one voyage's specific board state
  (e.g., an unusually long answer line, or a coincidence that will not recur under normal play).
- If the trade prompt's ask pill turns out to already clamp/lift correctly (unlike the call-winner
  prompt, which needed `liftAskClearOfFan()`), then there may be no shared mechanism to fix and
  this becomes a one-off worth flagging rather than patching.

## Method

Pose `humanTrade`'s "table answers" prompt directly via `localAsk` (matching w54's approach for
the call-winner prompt) with 1-2 trade responses shaped like the real code builds them (`short:`
carrying a captain name plus an ingredient icon and/or coins), across the same board positions
`w54` already covers, and measure circle-vs-pill overlap with the same `shapeOverlap`-shaped
geometry. Bounded (finite pose list), read-only against the shipped code first.
