# PREDICTION — `T-104`, his "DO NOW" button. Written 2026-09-02T21:22Z, BEFORE any edit.

**His words, verbatim (Glass, 2026-09-02 3:09 PM ET):** *"Do Now: in the Glass, Add a \"DO now\"
button next to \"Send to the Chart\" button that tells RANK to put this task at the top"*

And the design it belongs to, `T-083`, also his: *"i need a way to say DO THIS NOW such that RANK
puts it at the top -- eg a checkbox underneath the ideas list that says 'Add to top of list'"*

---

## WHAT I EXPECT TO FIND, AND WHY

1. **There is no pin signal anywhere today.** `score()` in `chartkeeper.mjs:703-823` has seven
   signals — gated/needs-you (−1000), his-own-ruling (+100), settle (+50/60), reap (+40),
   player-facing (+30), raised-count (+8 each), size (±3). **Nothing he can set himself.** The
   highest score achievable is around 108, which is exactly what his top two rows score right now.
   *Falsified if:* a `now`/`pin`/`top` field already exists.

2. **`headField(row, name)` already reads `field: value` out of the `⟨…⟩` head line**, splitting on
   `·` — so `⟨\`T-104\` · now: yes⟩` needs no new parser. `needs:` and `size:` are the worked
   examples.

3. **The Glass page's Ideas box has one button** (`#ideaSend`, `glass.mjs:952`) and its click
   handler pushes `{id, text, at}` onto `state.ideas`, then `cap.publish(buildDoc(state))`. A second
   button carrying one more field on that object is a small change to a well-worn path.
   *Falsified if:* the state shape is validated somewhere that would reject an extra key.

4. **The pin must survive the harvest, and the harvest is a HUMAN STEP.** A session reads
   `glassState.ideas` and writes them into `CHART.md`. So the durable form has to be something a
   session writes into the Chart — a head field — and the instruction to carry the flag has to be
   somewhere that session actually reads.
   ⚠ **This is the weakest joint in the whole chain and I expect it to be the thing a CEO attacks.**

5. **The one-slot rule cannot be enforced by the page alone.** He can pin from the page; a session
   can typo a second `now:` into the Chart. So a gate has to count them.

## WHAT I EXPECT THE MEASUREMENT TO SHOW

- **RED:** a Chart row carrying `now: yes` ranks wherever its other signals put it — NOT first.
- **GREEN:** the same row ranks 1, with a why-now phrase in his own words, and every other row's
  relative order is unchanged (the pin must not reshuffle the rest).
- **RED:** two rows carrying `now:` pass the build.
- **GREEN:** two rows carrying `now:` fail it, naming both.

## WHAT WOULD PROVE ME WRONG

- If RANK already has any way for him to interrupt it, this row is smaller than I think and the
  honest report is "half of it already existed".
- If the pin, once wired, changes the order of rows OTHER than the pinned one, the implementation
  is wrong — his ask is "put this task at the top", not "re-rank everything".
- **If I cannot demonstrate the button in a rendered page**, I have shipped a mechanism and called
  it a feature. A watch has no Artifact tool and cannot publish, but it CAN render `glass.mjs` to a
  file and photograph it in a browser. **If I do not do that, this watch has not seen its own work**
  (rule 19) and the close must say so.

## WHAT HAPPENED IMMEDIATELY BEFORE (rule: widen the time horizon)

The reason this row is rank 1 is that it was rescued by hand out of THE IDEA INBOX, where the
harvest had correctly parked it below every open checklist row. **He had to type "DO NOW" in prose
because the button does not exist — and the request for the button then sank.** The feature is its
own acceptance test: had `T-104` shipped, `T-104` would not have needed rescuing.

## SCOPE — SAID BEFORE, NOT AFTER

`T-104` only. `T-103` (drag to reprioritise) is split into its own row. `T-083` steps 1–3 (converge
the two orderings, a source signal, the Door dropping oldest-first) are NOT in this watch.
