# Prediction — closing T-206 ("THE OTHER HALF OF HIS ANALYTICS ASK") and T-240 (duplicate)

**What I expect, before measuring further:** both Chart checklist rows (line ~131 handle `T-206`,
line ~233 handle `T-240`) describe the analytics install as having outstanding sub-parts — the
which-pages decision, the cookie-choice decision, and (via a cross-reference) the shallow-trial
depth question `T-220`. My reading of the record (git log, THE IDEA INBOX section, and the
06:0xZ watch's own ledger note flagging `T-240` as stale) is that **every one of those sub-parts is
already implemented and separately closed**:

- Analytics installed cookieless on exactly 3 pages (game/about/rules), never firing on staging/
  localhost — commit `09f8658c`, gated green by `scripts/qa/analytics_consent_check.mjs`.
- `t206-which-pages` ("public pages only") and `t206-cookie-choice` ("cookieless, no banner") —
  his answers, both implemented in the same commit above and PASSing the same gate today.
- `t206-privacy-line` (the front-card wording ask) — closed via CEO 195/200, commits `aa4c0c71`,
  `1b98ca8a`, `5c110f76`; `privacy.html` exists and is linked from `index.html`.
- `t220-shallow-green` (bypass/depth question referenced inside the T-206 row) — closed this
  watch-cycle via CEO 201, commit `0fb2654c`.

**Why I have not just closed them yet:** the previous watch (05:0x–05:3xZ) explicitly flagged
`T-240` as "left for the next watch" rather than closing it itself, and did not touch `T-206` at
all. I want to confirm with a fresh, independent read (not trust my own skim) before closing,
because closing a Chart row is exactly the kind of act rule 6 warns against doing on an unmeasured
claim.

**What would prove me wrong:** if `analytics_consent_check.mjs` is not actually run against the
live tree (e.g. it's a stale/vendored gate not wired to real files), or if any of the four cited
commits do not actually exist on this branch, or if a fresh read of the T-206/T-240 row text
reveals a sub-part I have not accounted for that is genuinely still open. I will re-run the gate,
re-check `git log`, and re-read both rows in full before closing.
