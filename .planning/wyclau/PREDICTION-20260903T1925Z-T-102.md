# PREDICTION — 2026-09-03T1925Z — `T-102`

*Written BEFORE any measurement, per the Door's step 3 and CLAUDE.md's prediction rule.*

## The item

His ruling **"yes"** on: *"Google can index your working files right now, and your note assumed it
could not. Thirteen pages are live on the domain with nothing stopping a crawler: five
`art-review/` galleries, seven `notes/sketches/` mockups, and `battle_sim.html` (plus nineteen files
under `.planning/`)."*

The row is UNTRIAGED — my job is to decide whether it still owes work, do that work, and retire the
ruling.

## What I expect, and why

1. **`robots.txt` at the repo root exists and contains no `Disallow` line covering those paths.**
   Why: the row says only four pages in the repo say anything about crawling at all, and CLAUDE.md
   rule 14 describes `robots.txt` as a site-identity file that is deliberately permissive on
   production (`Allow: /`). Nobody has had a reason to fence a directory before.
2. **The thirteen named pages are real files in the repo root tree**, therefore served by GitHub
   Pages at `playpastrypirates.com/<path>`, therefore crawlable. Why: Pages serves the whole branch;
   there is no build step and no server-side exclusion mechanism.
3. **The fix is `robots.txt` plus per-page `<meta name="robots" content="noindex">`**, not one or
   the other. Why: `robots.txt` stops a polite crawler FETCHING a page but does not stop it INDEXING
   a URL it learned about elsewhere (Google's own documented behaviour — a disallowed URL can still
   appear in results as a bare link). A `noindex` meta tag is what actually removes a page, and it
   only works if the crawler is *allowed* to fetch the page and see it. **So the two mechanisms
   partly conflict**, and I expect the honest answer to be: `noindex` on the HTML pages, and
   `robots.txt` for the non-HTML directories where no meta tag can live (`.planning/`, `scripts/`).
4. **This is a `robots.txt` change, so CLAUDE.md rule 14 is live.** `robots.txt` is a site-identity
   file. I expect the risk here is not deploying it wrong but COPYING it somewhere — and I am not
   copying anything, so the rule should be satisfiable by touching only this repo.

## What would prove me WRONG

- **`robots.txt` already disallows those paths.** Then the row is stale and owes nothing but a
  retire — and my whole model of "nobody has fenced anything" is wrong.
- **Those paths 404 on the live domain.** Then they are not live, the row's central factual claim is
  false, and the honest close is a correction, not a fix.
- **`noindex` turns out to be unnecessary because the pages already carry it.** Then only the
  non-HTML directories need work.
- **The count is wrong** — not five galleries, seven mockups and one sim. If the count is wrong the
  row was written from a stale listing and I should say so plainly rather than fix the number
  silently.

## The trap I am naming for myself

**I want this to be a small, clean, one-file change, because that closes a watch quickly and this
row is `rank 1` on his Chart.** That pull will push me toward "just add four `Disallow` lines and
call it done" — which is exactly the answer that leaves already-indexed URLs in Google forever and
looks finished. If the measurement says `noindex` is needed on thirteen HTML pages, I do thirteen
pages.

**And a second trap, from the ledger of the watch before me:** it got a claim wrong by checking the
comparator instead of the write-sites. The analogue here is checking `robots.txt`'s CONTENTS and
concluding the site is fenced, without checking whether the file is even SERVED at the paths in
question or whether anything links to those pages. **Verify against the live domain, not against the
repo file.**

## Gear

No game code (`src/`, `index.html`) is expected to change — this is `robots.txt`, standalone
review/mockup pages, and possibly a gate. I will run `gear.mjs` and obey what it says rather than
this sentence.
