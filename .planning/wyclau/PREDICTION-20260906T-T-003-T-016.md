# Prediction — closing T-003 and T-016 (stale-evidence trial rows)

**Written before measuring further, per the Proof's rule 6 working form.**

## What I expect

Both `T-003` ("judge the 267 screenshots the release trial queued") and `T-016` ("the release
trial's evidence was retired by the fix, staging needs another ~90-minute trial") describe the same
underlying gap: no judged trial exists for the CURRENT build. I expect that gap is now closed,
because:

1. `SEA-TRIAL-2026-09-06T1328Z-Wy-Blade.md` sailed **10/10 legs, FULL gear**, on build
   `2026.09.04.2` — which matches the tree's live `PP4_STAMP` (`src/ui/stage.js`) right now.
2. Sea-trial-v2 judges **as it sails** — there is no queue mechanism any more (that architecture
   was replaced after `T-003`/`T-016` were written); "the eyes see EVERY distinct screen."
3. That same trial's findings were independently triaged (`T-262`) and CEO-verified twice (CEO 220
   YES, plus a second independent fresh-context review this session's predecessor commissioned),
   both concluding zero NEW player-facing defects beyond rows already open and tracked elsewhere.

So: a judged trial of the current build exists, and its findings have already been vetted. That
should be enough to close both rows as superseded-and-satisfied, not as "we skipped the ask."

## What would prove me wrong

- If the trial has a real NOT-RUN or meaningfully-unjudged column beyond the single spot-checked
  `1 screen(s) NOT looked at` line in one leg (already confirmed small and isolated) — i.e., if a
  substantial fraction of screens were never actually looked at, the "judged" claim is false and
  these rows should stay open.
- If `T-262`'s CEO verification only checked a SUBSET of the trial's findings (not the whole
  report) — then "zero new defects" would be an overclaim inherited from a narrower review than
  this closure needs.
- If either row's actual intent is narrower than "a judged current-build trial exists" — e.g. if
  `T-003` specifically requires the original 267-screenshot queue to be worked off item-by-item
  regardless of newer mechanisms — then closing it as superseded would be answering a different
  question than the one he asked. I will check the row's own text for this before closing.

## Falsifier check, before closing

I will re-read the T-262 closure and CEO 220's own text in full before writing SETTLED verdicts,
not just take the ledger's summary of it on faith.
