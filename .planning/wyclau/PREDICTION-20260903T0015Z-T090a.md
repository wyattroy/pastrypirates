# PREDICTION — watch 2026-09-03T00:15Z, `T-090` gap (a)

*Written BEFORE any measurement, per `.claude/CLAUDE.md`'s "write the prediction down before you
measure". The whole value is that it cannot be retrofitted.*

## The item

His standing priority, verbatim: *"the page continues to re-show me thw e questions AFTER they're
harvested. this is NOT fixed and it is a PRIORITY more than any of the SEO work"*.

`T-090`'s own row and CEO 125 agree on what is left:

> (a) **NOTHING CALLS THE SCRIPT.** The spec asks for retirement *"run by the harvest… not a session
> following a runbook step"*, and what shipped is a command a session types from a runbook step.

## WHAT HAPPENED IMMEDIATELY BEFORE THE BUG — asked of the SEQUENCE, not the screen

Every one of the six instances has the same three beats: **(1)** he answers on his page; **(2)** a
session reads the page, writes his ruling into the record, and runs
`mark_glass_harvest.mjs --version=… --rulings=<his keys>` to stamp the receipt; **(3)** nothing
deletes the question, so the next regeneration renders it again.

**Beat 2 already knows the answer.** The stamp is handed the exact ids he ruled on, and it is the
one command a harvest cannot skip — the runbook, the hook and a gate all require it. So the caller
`retire_answered.mjs` has been waiting for is a command that already runs, already holds the keys,
and today does nothing with them.

## WHAT I EXPECT TO FIND (the RED)

1. `mark_glass_harvest.mjs --version=X --rulings=<the qid of a LIVE question>` **exits 0 and writes
   the receipt**, leaving the question in `## BLOCKED ON WYATT`. That is the bug, reproducible in
   one command, with no page and no Artifact tool needed.
2. `--retire=<qid>::<verdict>` is **not a flag it knows**, and `argOf` ignores unknown arguments, so
   passing it changes nothing and still exits 0.
3. `retire_answered.mjs` builds the `## RULED` row itself, so there is **one copy** of that act
   today and adding a second caller would make two (rule 23) unless it is extracted first.

## WHAT WOULD PROVE ME WRONG

- If `mark_glass_harvest.mjs` **already** refuses, or already retires, then gap (a) is not what the
  row says and this item is smaller or already closed — say so and stop.
- If the ids in `--rulings=` turn out **not** to be the same ids `questionId()` derives for a
  `BLOCKED ON WYATT` row, the join does not exist and this whole approach is wrong. (`T-090`'s row
  claims they match character for character; I am treating that as a claim to verify, not a fact.)
- If refusing at the stamp can **wedge** a harvest — some legitimate case where a ruling key matches
  a live row that must stay — then a refusal is the wrong shape and it should warn instead.

## WHAT I INTEND TO SHIP

Retirement moves inside the stamp. `--retire=<qid>::<verdict>` retires each question and writes the
receipt in **one act**; a `--rulings=` key that still has a live question and no verdict is
**refused**, naming the exact command. One definition of the retirement, imported by both scripts.

**What it does NOT do, said now so it cannot be blurred later:** the acceptance test in
`SPEC-ANSWERED-QUESTIONS-RETIRE.md` §4 is the SYMPTOM on his live page. A Bell-launched watch has no
Artifact tool and cannot read or publish that page, so that half stays NOT RUN.
