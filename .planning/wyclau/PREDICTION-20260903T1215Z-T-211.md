# PREDICTION — `T-211`, written 2026-09-03T12:15Z, BEFORE any measurement

*Watch d4. Rule 6's working form, and the Door's step 3. Nothing below has been measured yet;
the results are appended underneath as a separate act, and every wrong part is named out loud.*

**The row:** on 42 posed fights, `t013_call_circle_beside_check.mjs` reports **34 UNANSWERABLE** —
the captain a call button names is not fully on screen, so there is no boat for the circle to be
beside. The row itself marks the cause **UNPROVEN** and says a watch taking it must settle whether
that is the live fault or an artifact of the probe.

---

## WHAT I EXPECT

**The 34 is dominated by the probe's own re-posing, not by a fault a player meets.**

## WHY — read out of the code, not run

1. `src/ui/stage.js:2932` — `const key = S.turnSerial + "|" + (ap.querySelector(".apMsg")||{}).textContent;`
   and the camera is only asked to frame the fight when `S.frameKey !== key` (`:2933-2938`).
2. `src/ui/stage.js:4030` — `turnSerial` bumps **only when the active seat changes**
   (`actor: seat => { if (S.activeSeat !== seat) S.turnSerial++ }`). The probe never changes the
   actor, so `turnSerial` is constant for a whole leg.
3. `scripts/qa/t013_call_circle_beside_check.mjs:68-78` — every one of the 7 poses in a leg calls
   `localAsk` with **the same sentence**: the attacker is always `seats[0]`, so the name in the text
   never varies. Only the ships' board coordinates change.
4. Therefore `key` is **byte-identical for all 7 poses of a leg**. `camFitSeats` runs on pose 1 and
   **never again** — while poses 2–7 teleport the hulls to new squares under a camera that has been
   told the shot is already correct.

**So the arithmetic I expect:** 3 legs × 7 poses × 2 buttons = 42 rows; roughly the first pose of
each leg answerable (3 × 2 = 6) plus whichever later poses happen to land inside the frame anyway.
**8 judged is what the row reports, and 6 is what this mechanism predicts** — close enough that I am
treating the gap as the thing to explain, not as agreement.

## WHAT WOULD PROVE ME WRONG

- **Falsifier A (the cheap one, run first).** Print the pose index beside every judged row. If the
  judged rows are **spread across poses 2–7** rather than concentrated in pose 1, the frameKey
  staleness is not what is emptying the screen and this whole theory is dead.
- **Falsifier B.** Give each pose a distinct sentence so `key` genuinely changes, and re-run. If
  UNANSWERABLE **stays above 20 of 42**, the cause is somewhere other than the frame key.
- **Falsifier C — the one that matters to Wyatt.** Even if A and B both confirm the mechanism, that
  proves the *probe* is shaped wrong, **not** that the game is fine. The separate question is
  whether a player can ever meet two prompts inside ONE turn that share a sentence and name
  DIFFERENT captains. If they cannot, then `T-211`'s population is an artifact and his sentence
  (`INBOX-20260901T1332Z`) needs a different instrument — and I must say so plainly rather than
  bank the count as a fix.

## THE TRAP I AM WALKING INTO, NAMED WHILE I CAN STILL TEST IT

**I want this to be an instrument fault**, because that is cheap, it lets me hand back a smaller
item, and it makes the previous watch's split look tidy. That is exactly the shape of the wish that
produced the last three wrong calls on this bug — three geometry theories died here already
(`CLAUDE.md`, "WIDEN THE TIME HORIZON"). **If falsifier A fires, I stop and say so before touching a
line of `stage.js`.**

## WHAT I WILL NOT DO

Build a second instrument for the same bug. If the measurement is ambiguous, the next step is a
posed pair of screenshots at a real battle (rule 26, `docs/DRIVING-THE-GAME.md` §5e), not a fourth
probe.

---

## ACT TWO — WHAT ACTUALLY HAPPENED

*(appended after the measurement, below this line — nothing above was edited)*
