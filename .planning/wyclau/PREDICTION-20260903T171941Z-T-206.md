# PREDICTION — 2026-09-03T17:19:41Z — `T-206`, watch on the Blade

**Written BEFORE any measurement.** Door step 3; CLAUDE.md rule 6's working form.

## What I am about to do

`T-206` ranks 1 on the Chart. Its remaining half — actually installing the Google Analytics tag —
is stated in the row as blocked on **two calls of his**: which pages get the tag, and cookie or
cookieless. The previous watch wrote both onto `.planning/ANALYTICS-PLAN.html` with numbered
options and a marked recommendation, and CEO 177's top finding is that **that page is not a URL he
can tap** — so the questions reach him nowhere.

I intend to put those same two calls into `CHART.md`'s **BLOCKED ON WYATT** table as `qid:` rows,
which is the one surface that renders onto his Glass Your Call card without an Artifact tool.

## What I expect, and why

1. **Neither question exists as a `qid:` today.** The only `qid:` strings in `CHART.md` are
   `t206-ga-turn-on` (already answered), `t012-battle-card-clip`, `t102-search-console`,
   `t102-sitemap-coverage`, `t102-working-files-indexable`, `t017-fan-mixed-sizes`,
   `t017-name-type-too-small`, `t121-drag-scope` — seven live rows, none about pages-to-tag or
   cookies. *Why:* I read the grep of `qid:` above; the two new ones were written to the HTML
   artifact instead, which is exactly the fault CEO 177 named.
2. **The BLOCKED ON WYATT table is the Glass's only question source.** I expect `glass.mjs` to
   parse the `<!--qid:…-->` comment out of that table and nothing else. *Why:* `CHART.md`'s own
   lifecycle note at :883 says the question lives "as an HTML comment holding `qid:` and a short
   slug", and `rulings_triage_check.mjs` polices that table specifically.
3. **Adding a row will make `rulings_triage_check.mjs` demand a matching `- [ ] Your ruling: …`
   checklist row** — no, wait: re-reading the lifecycle note, that row is written at move 2, when
   he ANSWERS. I expect a fresh unanswered question needs only the table row. *This is the part I
   am least sure of.*
4. **I have no Artifact tool**, so I cannot publish the Glass myself; step 6b (message the Glass
   session) is the only route his page has.

## WHAT WOULD PROVE ME WRONG

- **(1) is wrong** if a grep across `.planning/` and `scripts/wyclau/` finds either question
  already carrying a `qid:` — on `GLASS-CHART.md`, in a questions JSON, or anywhere `glass.mjs`
  reads. Then the work is already done and this row is blocked on him, not on me, and I must say
  so instead of adding a duplicate.
- **(2) is wrong** if `glass.mjs` builds the Your Call card from something other than `CHART.md`'s
  table — an artifact state blob, `INBOX.md`, or a separate file. Then adding a table row puts the
  question on no surface he can see, and my whole plan is the same rule-27 failure in a new file.
- **(3) is wrong** if `rulings_triage_check.mjs` goes red on an unanswered question with no
  checklist row. Then I owe the checklist row too, and I will say the prediction was wrong rather
  than quietly adding it.
- **The whole item is wrong** if `T-206`'s two calls are not in fact what blocks the install — if
  the row's *"WHAT IS STILL OPEN"* paragraph is stale and something else is the blocker.

## RESULTS — measured 2026-09-03T17:20–17:35Z

**(1) RIGHT.** `grep -rln cookieless .planning/` returns eight files and not one of them is a
question surface: the two calls live in `ANALYTICS-PLAN.html`'s prose, in the `T-206` Chart row's
prose, in `CHART-LOG.md`, and in my own working files. No `qid:` for either. His Your Call card
showed **zero** questions.

**(2) RIGHT ABOUT THE SOURCE, WRONG ABOUT WHICH TABLE I WAS LOOKING AT — and the red-first check
is the only reason I know.** `glass.mjs:435-468` does read `## BLOCKED ON WYATT`'s table rows and
nothing else, exactly as predicted. But the table I had read at `CHART.md:961-969` and assumed was
that section is under **`## RULED — his answers, waiting to be triaged`** (:918). I wrote both rows
into it. **`glass_calm_check`, `rulings_triage_check` and `numbered_options_check` were all green
on that state** — the questions were in a table, well formed, and on no surface he can answer from.
The check written to fail first is what caught it, in one run, before anything was committed. The
real `## BLOCKED ON WYATT` table (:910) was **empty** — header row only.

**(3) RIGHT.** `rulings_triage_check.mjs` stays green with an unanswered question and no
`- [ ] Your ruling:` row; that row is written at move 2, when he answers.

**(4) RIGHT.** No Artifact tool in this session. `GLASS-NOTE.md` and a `SendMessage` to the Glass
session are the only routes his page has.

## WHAT NEITHER THE PREDICTION NOR ITS FALSIFIERS COVERED, and it was the bigger find

`numbered_options_check.mjs` declared `judged5` **twice** — once at `:45` and again at `:168`,
shadowing it — so `judged5++` fed a variable that died at the inner block's brace and the report
line read the outer one, permanently `0`. **The line CEO 176 added so nobody would read the gate's
silence as protection could only ever print *"BLOCKED ON WYATT is empty"*** — and it printed it in
the same run in which the gate had just correctly failed both of my undated rows. Rule 6: a
measurement that cannot fail is not a measurement. Found by disbelieving the gate's own summary and
parsing the section independently (`_t206_dbg.mjs`), which is the move rule 6 asks for. Fixed; it
now reads **2**. **I had no falsifier for "the instrument's report is lying about itself"**, and
that is the gap worth carrying forward.
