# PREDICTION — 2026-09-03T10:45Z — `T-138`, his player-count console

*Written BEFORE the work. Bell watch, Wy-Blade. Chart rank 2 (rank 1, `T-099`, is open only on
two taste questions that are his — no work exists for a watch there).*

## The item, as I understand it

His ask, `INBOX-20260902T214507Z`: *"create a firebase admin console so I can see how many people
are playing"*. His ruling on where: *"put it at /stats.html behind a simple curtain and block it
from robots.txt."* The INBOX sizes the acceptance test in his own sentence: **a number on a screen
he can open.** *"A watch that installs the tag and reports success without him being able to see a
player count has not done this item."*

## What I already measured, before writing this (stated so it is not passed off as prediction)

- `stats.html` exists at the repo root, **committed** (`b13a68c0`) — so `T-138`'s own body text
  (*"`stats.html` and `lab.html` do not exist at the repo root"*) is **false today**.
- `curl` → production **404**, staging **404**. He cannot open it.
- CEO 159's FINDING 1 (the curtain word `sugarfish` was the helper's default and the page pointed
  at the helper) is **already fixed** by a later watch: the hash at `stats.html:117` is
  `58aa0afd…`, which is not `sugarfish`'s, the helper was renamed to
  `scripts/qa/curtain_hash.mjs` with no default, and `58aa0afd` appears in no other file.

## What I expect

1. **`npm run deploy:staging` will publish this branch to `staging.playpastrypirates.com`, and
   `/stats.html` will then return 200** — because `scripts/deploy-staging.sh` rsyncs the whole tree
   with a named `EXCLUDES` list of site-identity files only, and `stats.html` is an ordinary root
   page that is in no exclude list.
2. **After that he can open it** — the page's own `reveal()` is the only caller of `load()`, so a
   200 plus the word gets him the numbers.

## WHAT WOULD PROVE ME WRONG — the falsifiers, named while they can still fire

- **F1 — `stats.html` is excluded.** If `EXCLUDES` in `deploy-staging.sh` names it (or a pattern
  catching it), publishing cannot deliver it and my whole route is wrong.
- **F2 — the deploy fails on this machine.** `INBOX-20260902T05xxZ` says in its own status line
  *"THE PERMISSION IS FIXED AND THE DEPLOY STILL FAILS, for a completely different and now
  MEASURED reason."* If that reason is still live, this item does not close by publishing and I
  must say so rather than route around it.
- **F3 — 200 but not a page.** If staging serves it and the curtain form does not render (or the
  page throws before drawing), a 200 is not the deliverable and I must screenshot rather than
  `curl`.
- **F4 — HE DOES NOT KNOW THE WORD.** `stats.html:98` says *"The word itself lives with Wyatt and
  appears nowhere in this repository."* **I have not verified that anybody ever told him.** If no
  record shows the word reaching him, then a published page behind a word he does not have is
  **not** "a number on a screen he can open" — the item would be delivered on paper and closed on
  nothing, which is precisely the failure his own sentence anticipates. If F4 fires, the honest
  close is: publish, and put the word-delivery to him as a question, saying plainly that this half
  is not done.

## THE TRAP, named because I want one answer more than the other

**I want the deploy to work**, because it is the only route by which this item reaches him inside
one watch. That makes me likely to read a 200 as success and to skip F4 entirely — F4 is the one
that costs me the close. It is written first in my order of checking for that reason.
