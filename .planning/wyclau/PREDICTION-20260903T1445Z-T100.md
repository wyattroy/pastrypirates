# PREDICTION — `T-100`, build `/rules.html`

*Watch e3, Wy-Blade, 2026-09-03T14:45Z. Written BEFORE any gate was run or any page built.*

**His ask, verbatim (`CHART.md:1020`, harvested off the Glass 2026-09-02 3:07 PM ET):** build the
rules page, and *"The in-game 'How to play' modal … and the new rules page must NOT be two copies of
the same 765 words … Before writing anything, answer this out loud: what makes these two agree? If
the honest answer is 'we keep them in sync,' that's the defect."* Plus: wire `.footerHow` /
`.footerCredits`, house-pattern `<title>` / description / `og:`, screenshot both, run the gear +
trial, bump `PP4_STAMP`.

**No stated `solution:` beyond that constraint** — so there is no "his version first" to implement
ahead of mine. The constraint IS the solution he stated, and it is what I am building to.

---

## WHAT I EXPECT, AND WHY

**1. The one-source answer is a GENERATOR plus the existing gate, not a runtime fetch.** I expect the
right shape to be `scripts/build_rules_page.mjs`: read `index.html`, cut out the `#howToPlayModal`
body, fill every `<b data-rule="k"></b>` from `rulesFacts(roundCfg(4 seats))`, wrap in a page shell,
write `rules.html` — and then extend `scripts/qa/rules_page_check.mjs` to **regenerate in memory and
fail when the file on disk differs.** Nobody keeps them in step; the gate refuses a page that has
drifted.

*Why not the obvious alternative.* A `rules.html` that `fetch()`es `index.html` at load and injects
the modal is even purer one-source — zero copies on disk — and I expect to reject it **on his own
stated purpose, not on taste**: he asked for a *findable* page, and a page whose entire body arrives
by client-side JavaScript is the weakest thing you can hand a crawler. The generated file is static
HTML and indexes like any other page.

*Why the numbers must be baked in rather than filled at runtime like the modal's:* same reason. The
modal fills its spans live because a 2-player table has different crate prices from a 4-player one
(`SPEC-RULES-PAGE-SPLIT.md` Q3). A public page has no table, so it can only ever show the four-seat
default — which means the honest thing is to generate it from `rulesFacts` at publish time and let
the gate re-derive it, not to ship empty spans that render blank to a crawler.

**2. Register: pirate speak, and NOT because I chose it.** Q4 is unanswered on his Your Call card.
I expect the generator to make the question moot — the page IS the modal's sentences, so it speaks
whatever the modal speaks. If he later rules "plain English", the modal changes and the page follows
in one command. **I am predicting I will not have to make a taste call here. If I find myself
rewriting a sentence, my design is wrong.**

**3. `.footerHow` on `index.html` should KEEP opening the modal.** I expect the right wiring is a
link to `/rules.html` from `about.html` (and from the new page back), never a change that navigates
a player off the board mid-turn onto a static page with generic numbers. `.footerCredits` I expect to
leave alone entirely — the credits page is `T-101` and does not exist yet, so wiring it now would
point at a 404.
**⚠ THIS IS THE PART MOST LIKELY TO BE WRONG, and it is the only place his words and my reading can
genuinely differ.** He wrote *"Wire the footer links (index.html has .footerHow / .footerCredits)"*.
That sentence can mean "make them navigate to the new pages". I am reading it as "make sure the
footer reaches the new page", and I am choosing the reading that does not take a live player off the
board. **If he meant the other one, this is the item to correct, and I will say so in the report
rather than hide it inside a diff.**

**4. `sitemap.xml` gains a `rules.html` entry with a derived `lastmod`.** Site-identity file
(`GIT-AND-DEPLOY.md` §1) — it never leaves this repo, and the date comes from
`git log -1 --format=%cs -- rules.html`, never hand-typed (`T-098`'s standing instruction).

**5. Gear: I expect `gear.mjs` to say something OTHER than what this change actually needs, and to
have to say so out loud.** `CHART.md:542` already records it: *"THE GEAR PICKER IS BLIND TO A FILE
THAT DOES NOT EXIST YET, SO A BRAND-NEW PAGE SERVED…"*. A new top-level `.html` is game-adjacent but
draws no board. I predict **gear says FULL or NONE, and neither is the truth**, and that the honest
answer is `npm test` green + posed screenshots of the new page and the untouched modal (rule 19),
with the discrepancy reported rather than smoothed over.

---

## WHAT WOULD PROVE ME WRONG — the falsifiers, named before the result exists

- **On (1):** if the modal's markup cannot be extracted cleanly by the same regex
  `rules_page_check.mjs:52` already uses, the generator has no reliable source and the whole design
  collapses to a hand-copy. *Then my answer to "what makes these two agree?" is "we keep them in
  sync" — the defect he named — and I should stop and say so, not ship it.*
- **On (1), the sharper one:** if I extend the gate and it **passes on a `rules.html` I have
  deliberately corrupted**, the gate is decorative and everything above is worthless. I will red-proof
  it by hand-editing one number in the generated page and requiring a FAIL.
- **On (2):** if the extracted text needs ANY sentence rewritten to read as a standalone page — even
  one — then it is not the modal's text and the two WILL drift. That is a real failure of the
  design, not a copy-editing task.
- **On (3):** if `.footerHow`'s handler turns out not to open the modal at all, my whole reading of
  the footer is built on a wrong premise.
- **On (5):** if `gear.mjs` names this correctly and cheaply, `CHART.md:542` is stale and I should
  say the row is already fixed rather than repeat its claim.

## WHAT HAPPENED — filled in AFTER measuring

**(1) THE DESIGN — RIGHT, and the falsifier fired clean.** The modal extracted verbatim through the
same anchor the gate already used; **not one sentence needed rewriting**, which was the named
failure condition. The gate went RED first (`rules.html does not exist`, exit 1), green after, and
`scripts/qa/rules_page_redproof.mjs` proves it fails on **five** corruptions, not the three I
planned — a number edited on the page, a word edited on the page, the modal moving while the page
went stale, the link being removed, and the omit marker leaking. **CEO 171 broke it independently
with a wind-rule change and got the same red.**

**(2) REGISTER — RIGHT, and better than I knew.** I predicted the question would be moot by
construction and that rewriting a sentence would mean my design was wrong. Neither happened.
**What I got wrong was the premise: I said Q3 and Q4 were unanswered, and he had answered BOTH the
night before** — `CHART.md:868-876`, 2026-09-02T22:51:10Z: *"Do your recommendation -- full text,
modal stays the source"*, and 22:51:18Z: *"Pirate speak!"*. The design matches his rulings exactly,
which is luck dressed as judgement. **A ten-second grep of the Chart for `rules-page-3-of-4` would
have turned a guess into a citation, and I did not run it.**

**(3) THE FOOTER — I FLAGGED THIS AS THE LIKELIEST WRONG CALL AND IT WAS HALF WRONG, in the half I
did not flag.** Keeping `.footerHow` on the modal is right, and it is right for a better reason
than the one I gave: it is **his ruling**, not my taste. Leaving `.footerCredits` alone is right.
**What I missed entirely: `.footerHow` is a `<button>`, so index.html linked to the new page
NOWHERE — no crawler following links from the homepage could reach it, on a page whose whole
purpose is being findable.** Found by CEO 171, not by me, and not covered by any falsifier I wrote.
Fixed: a real `<a href="rules.html">` at the foot of the modal, marked `data-page-omit` so it is
not copied onto the page it points at, with both halves red-proofed.

**(4) THE SITEMAP — RIGHT.** `sitemap_write.mjs` derived every date from `git log -1 --format=%cs`
and **refused** the page until it was committed, which is why the page went in first.

**(5) THE GEAR — RIGHT, and sharper than predicted.** With only `sitemap.xml` uncommitted,
`gear.mjs` answered **FULL, naming `sitemap.xml` as a file where "behaviour can change"** — a
sitemap cannot change what a captain sees. And it never saw `rules.html` at all, because gear reads
the *uncommitted* diff and the page was already committed. `CHART.md:542`'s row is not stale; this
is a second, different way the picker misreads a new page.

**THE PATTERN THIS FILE WAS WRITTEN TO EXPOSE, and it held exactly:** every claim I attached a
falsifier to survived. **The two things that were wrong — his rulings already existing, and the
game page linking nowhere — are the two I never wrote a falsifier for.**
