# Handoff — 2026-08-26, overnight

**Live build: `2026-08-26d`.** `main` level with `origin/main`. Nothing half-finished in the tree.
**Start with §1. It is the answer to "how did this get past your QA", and it is not a good one.**

---

## 1. HOW YOUR 35 BUGS GOT PAST THE GATES

**The gate built for exactly your host/guest bugs is GREEN — because the hole you fell into was
formally declared acceptable.** `scripts/host_guest_parity_check.js --tree=4` passes all six
assertions and ends with its own disclaimer:

> `THE DECLARATION STILL NAMES 1 GAP(S): localAsk.`
> `Green here means "no worse than declared", NOT "converted".`

Measured in that run: **`localAsk` — the prompt renderer — has 6 call sites in the host's game loop
and 0 reachable from the guest's path.** That 0/6 is your *"entire parallel track of code for
guests"*, located. It was scheduled to close at phase 02.15 Stage 4, which somebody marked
**abandonable**.

**A second gate was red about your worst bug the whole time and nobody read it.**
`prompt_field_parity_check` assertion 2 has been failing with this sentence: *"pickCell() SENDS
'pos' and watchPrompt's pick branch never reads p.pos."* That one missing field IS why a guest
cannot stay put (§2, T-02). The gate named the field, the tier, and the consequence.

**And the bare gate reads the wrong game.** `node scripts/host_guest_parity_check.js` with no flag
prints *"tree: root (the OLD game — not the tree under development)"* and passes 5/5 on a game
nobody is developing.

**Two corrections to your own diagnosis, because a wrong mechanism wastes your time:**

1. **The bake-off swap is NOT rewritten for baker vs watcher.** You suspected *"terrible sloppy
   embarassing architecture"*. Measured: `playBakeoffLive` is ONE function, shared, reachable from
   both tiers. The jitter you saw is real; a duplicate is not its cause, so it needs a different fix.
2. **The prompt split IS real** — you identified it from the outside, without reading a line.

---

## 2. FIXED, WITH EVIDENCE

| | What you said | What was wrong | Proof |
|---|---|---|---|
| **T-02** | "The guest cannot stay put" | The guest's prompt was built as `{cells,msg,hint}` — no `pos`. `renderPickPrompt` draws the stay square only `if(spec.pos)`. **No guest has ever had one.** Not a parallel renderer: the one renderer, starved of an argument | parity assertion 2 red→green |
| **T-04** | Battle card stays up "indefinitely" | Nothing told a watcher's *screen* the fight ended — the listener set a flag and drew nothing. Measured: card left the host at 14.4s and the guest at **27.8s**, 13.4s of dead card | now one function, `applyBattleSnap`, both tiers |
| **T-07** | Battle box "chases my own boat" | It does: the card is hung off `boatUXY(mySeat)` while the camera holds the fighters. Guest's card travelled 285px in 600ms (the camera's 650ms tween); host's moved 2px | centred, like an over-tall card already is |
| **T-16** | No glow on Start | The class was right; **specificity** was wrong. `.ahoyGlow` is (0,1,0) and the grow rules that also set `animation` carry an id + six pseudo-classes | glow restated at a weight that wins |
| **T-25** | Bake-off title | Now `"{Captain}, Yer Bake-Off"` / `"{Captain}'s Bake-Off"` — the name rides the shared spec so both screens read one source | screenshot |
| **T-30** | Watch again glows | `animation:none` — covers glow AND grow | screenshot |
| **T-33** | Blue "?" art | **Your bench data was clean** — every rewatch a perfect permutation. The duplicate was PIXELS. Measured churn: **1743 image-URL writes, 1739 of them rewriting the value already there**; 600 hold-chips rebuilt; hole-art never preloaded, so always cold. Two real load failures caught | 4 fixes; board art verified intact on screen |
| **T-19/20/21/22** | slider copy · dock coin · blue button · focus | all as you asked | npm test |
| **T-12** | "the homepage looks crazy" | **Hiding `#game` is not hiding the game.** The ribbon, pill, captains box, chat sheet, fx layer and prompt are all appended to `document.body`, OUTSIDE `#game`. There was no stage teardown in the codebase at all | played a voyage, went back to port: `["pp4Ribbon","pp4Pill","pp4Cap","pp4Fx"]` → `[]`. **Screenshot: clean welcome card** |
| **T-15** | Boxes "completely blank white… looks laggy" | You under-estimated it. The reveal waited for the OLD line's entire 800ms fade before typing the first character of the new one | **median 917ms → 334ms**, worst 1001 → 360 |
| **T-24** | Tap the card to finish the text | now does | mid-type tap 44→76 chars; a tap on a finished message changes nothing |
| **T-31** | pink isn't "bad"; blue too close to green | Worse than you said: *selected* had no colour of its own — it inherited the same teal as the recipe step numbers | wrong → red, selected → yellow |
| **T-13** | Stale captains in a new room | **Partial.** A new room now starts empty. The *persistence* you describe is NOT reproduced, and I ruled out two causes rather than guessing | see §3 |

**Two comments were lying, and both were load-bearing:**
- `shared/index.js` claimed *"iconAt() removes the `<image>` on load failure, leaving the emoji
  visible"*. **It had no error handler at all** — which is why a failed image showed Safari's blue
  `?` instead of degrading. The fallback now exists.
- `renderPickPrompt` claimed `pos` is *"absent only across a version skew"*. It was absent on
  **every guest in every game**.

---

## 3. NOT FIXED — and honestly why

- **T-02 and T-04 are proven AT THE SEAM, not on screen.** Four attempts at a two-window
  verification, each getting further (lobby → voyage → Ahoy → host driving); the last hung with the
  guest never asked to sail. Its Chromes were killed rather than left running. **Do not call these
  done until two windows say so.**
- **T-18 (parentheses)** — parked at budget. `nobrk` holds in solo at 390px and in a static box, so
  the break needs the ceremony at a wider width.
- **T-01 (solo Enter)** — does NOT reproduce in Chrome; both a real Enter key and the button start
  the game. Your screenshot is Safari. It moves to the WebKit mount.
- **T-03** — I claimed a screenshot confirmed it. **It did not.** The `WIND NOW: ?` I saw was at the
  Ahoy barrier on day 1, and `stage.js:924` documents that placeholder as deliberate. Your item is
  about the weather *announcement*. Retracted before it became a false finding.
- **T-09 (previous captain stays lit)** — LOCATED, not fixed, and the probe says so: it still reads
  "Flaky Jack" lit while "Davy Probe" bakes. There are **two independent answers to "whose turn is
  it"** — `appState.curSeat`, which every prompt sets, and an event walk that the ring and captains
  box actually read. They disagree for the length of a bake. Converging them means deciding which
  wins during REPLAY, where there is no live actor. **That is a design call and it is yours.**
- **T-13's persistence** — ruled out: the registry refusing a duplicate attach (the key carries the
  room code), and listeners leaking from the old room (`netLeaveRoom` → `detachRoom` does detach).
  Both tested, both wrong. Recorded so nobody re-runs them.
- **Everything else on the list is untouched.** See `TRIAGE.md` for all 35, numbered and themed.

---

## 4. THE QA PROCESS, BUILT AND RUNNING

`4/scripts/qa/` — built ON `mp_rig.mjs`, never beside it.

| | |
|---|---|
| `lib/advance.mjs` | walks a client through the intro. Carries the traps that were **already written down and that I re-learned anyway**: Start isn't the button that starts, recipes take two taps, the coin is its own button, and a blank narration box is mid-reveal rather than a screen to click through |
| `t01_solo_enter.mjs` | drives Enter **and** the button, so agreement reports NOT REPRODUCED instead of passing |
| `narration_break_gate.mjs` | every parenthetical, red-proofed with a control that MUST break |
| `bakeoff_surface.mjs` | the drawn card: title, glow, buttons |
| `crew_stayput_check.mjs` | two windows; currently INCONCLUSIVE, which is the honest verdict |

**The rule that earned itself tonight: red-proof every check.** An early narration probe appended
its box to a `display:none` panel, measured nothing, and reported PASS — caught only because it also
measured a control that had to fail.

**And the gates caught me twice mid-fix**, which is the strongest argument for them:
- Adding `baker` to the bake spec turned parity assertion 4 red — a **fresh** host/guest divergence,
  created while fixing your complaint about host/guest divergence.
- `ING_HOLE_IMG` was not imported into `util.js`; shipped, it would have thrown at boot and broken
  the game. `no_undef_check` passed, because it only inspects call-position identifiers.

---

## 5. WHERE TO PICK UP

1. **Ask him what changed overnight on his own screen first.** Build `2026-08-26a`.
2. **Finish the two-window verification** (§3) before believing T-02/T-04.
3. **Close the `localAsk` gap.** It is the root of a third of his list and it is the thing he is
   angriest about. It was declared abandonable; it should not have been.
4. **Point the bare parity gate at `4/`** so a hand-run cannot bless the wrong game.
