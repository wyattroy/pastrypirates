---
phase: 14-engine-adjacent-gameplay-fixes-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/determinism_diff.js
  - src/engine/index.js
  - docs/DETERMINISM-RERECORD.md
autonomous: true
requirements: [VERIFY-02, STORM-01]

must_haves:
  truths:
    - "`node scripts/determinism_diff.js --assert-clean` exits 0 against an unchanged engine and exits 1 once any seed diverges — the tool is proven fail-first before it is trusted."
    - "`node scripts/determinism_diff.js` walks ALL 30 seeds to the end of each event stream and reports EVERY divergent line, never stopping at the first (the gap `verify()` leaves at scripts/determinism_baseline.js:194-220)."
    - "Every reported divergence is tagged with the event's `t` value, its `round`, its `storm` flag, and the exact set of JSON keys whose values differ."
    - "`--ignore-keys=<list>` re-runs the comparison ignoring named keys, so a purely additive serialization delta (a newly-present key) is separable from a genuine behavioural divergence."
    - "For every seed the tool reports the index of its first `storm:true` event and whether any STRUCTURAL divergence precedes it (D-26's replacement criterion), PASS or FAIL, per seed."
    - "`leeward()` in src/engine/index.js returns true when the square directly upwind is the home square, exactly as it already does for an island square (D-18)."
    - "The determinism comparison is byte-exact `JSON.stringify` line equality, identical to `serializeSeed()` (scripts/determinism_baseline.js:88-92) — never a structural/deep-equality comparison that could mask a key-order or type change."
    - statement: "A push whose very first candidate square is off-board or off-grid (`blocked(nx)` true) produces no event and no position change — the zero-distance/first-square-blocked edge is a silent no-op in both the engine and the diff report."
      verification: backstop
  artifacts:
    - path: "scripts/determinism_diff.js"
      provides: "Full per-seed determinism divergence enumeration, key-delta attribution, and the D-26 pre-storm-divergence assertion"
      exports: ["diffAllSeeds"]
      min_lines: 90
    - path: "docs/DETERMINISM-RERECORD.md"
      provides: "The D-16 'document what changed and why' record for this phase's one-time fixture re-record"
      min_lines: 30
  key_links:
    - from: "scripts/determinism_diff.js"
      to: "scripts/determinism_baseline.js"
      via: "imports MANIFEST_PATH/playSeed/serializeSeed/hashBytes rather than reimplementing them"
      pattern: "from \"./determinism_baseline.js\""
    - from: "src/engine/index.js leeward()"
      to: "src/engine/index.js isHome()"
      via: "the upwind square is now checked against home as well as the island map"
      pattern: "isHome\\("
  prohibitions:
    - statement: "MUST NOT run `--capture` to turn a red determinism gate green — the re-record happens exactly once, in 14-04, only after every divergence has been enumerated and explained."
      requirement_id: VERIFY-02
      category: transparency
    - statement: "MUST NOT 'fix' the expected red determinism gate by reverting the engine change — a red first gate after this plan lands is the correct state, not a regression to undo."
      requirement_id: VERIFY-02
      category: transparency
---

<objective>
Prove the irreversible path end-to-end before anything else is built on it: stand up the full
per-seed divergence tooling D-26 requires, land the single largest fixture-perturbing engine change
(D-18, Tortuga's wind shadow), and produce a real, complete, attributed divergence report.

Purpose: the fixture re-record is a one-way door (D-15, widened by D-18). `verify()` reports only the
FIRST divergent seed and the first divergent event within it (`scripts/determinism_baseline.js:194-220`),
which cannot satisfy "confirm the change is explainable before re-recording". Building that tooling and
exercising it against a real behaviour change — on this phase's best early-context tokens — is what
turns the door from a leap into a measured step.

Output: `scripts/determinism_diff.js`, the D-18 `leeward()` fix, and `docs/DETERMINISM-RERECORD.md`
seeded with the tracer's findings.
</objective>

<tracer_justification>
TRACER_MODE is on. A fix-list phase has no single user-facing stack to slice, so the tracer is the
thinnest slice through the phase's *riskiest machinery*: tooling tier (`scripts/`) → engine tier
(`src/engine/`) → evidence artifact (`docs/`). Every other plan in this phase either feeds the
re-record or waits behind it, so proving this path first is what catches an architectural surprise
after one commit instead of after five. It is production-quality, not a probe: the diff tool is wired,
committed, and becomes a standing capability; the `leeward()` change is the real, final fix.
</tracer_justification>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-CONTEXT.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-RESEARCH.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-PATTERNS.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md

<interfaces>
<!-- Already exported by scripts/determinism_baseline.js — import these, do not reimplement. -->
export const MANIFEST_PATH;                       // scripts/fixtures/determinism/manifest.json
export function playSeed(Game, roundCfg, i, seed);// i is the SEED INDEX (0..29), seed = 12345 + i
export function serializeSeed(g);                 // events JSON-lines + one "__final__" line + "\n"
export function hashBytes(bytes);                 // sha256 hex

<!-- scripts/lib/load_engine.js -->
export async function loadEngine();               // -> { Game, roundCfg, sourceHash }

<!-- manifest.json shape (already committed) -->
{ formatVersion, capturedAt, seedBase: 12345, seedCount: 30, botStrategies, seatRotation,
  engineSourceHash, requiredEventTypes, coverage, perSeed: [{ seed, file, events, sha256 }] }

<!-- src/engine/index.js — the two methods this plan reads/edits -->
isHome(c){return c[0]===this.home[0]&&c[1]===this.home[1];}          // :227
isIsland(c){return this.islands[c]!==undefined;}                     // :232
leeward(p){ const d=DIRS[OPPOSITE[this.windNow]];
  return this.isIsland([p.pos[0]+d[0],p.pos[1]+d[1]]); }             // :255-257
sailBudget(p){return this.leeward(p)?SAIL_BUDGET_LEEWARD:SAIL_BUDGET;} // :259  (9 -> 7)

<!-- The blocking-movement parity idiom to mirror (stepToward's pass(), :295) -->
if(this.blocked(o)||this.isIsland(o)||this.isHome(o))return false;
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan's contribution to the phase's new-symbol inventory:

| Symbol | Kind | Location |
|---|---|---|
| `scripts/determinism_diff.js` | new script | `scripts/` |
| `diffAllSeeds(opts)` | new export | `scripts/determinism_diff.js` |
| `--assert-clean` | new CLI flag | `scripts/determinism_diff.js` |
| `--ignore-keys=<csv>` | new CLI flag | `scripts/determinism_diff.js` |
| `--json` | new CLI flag | `scripts/determinism_diff.js` |
| `docs/DETERMINISM-RERECORD.md` | new doc | `docs/` |

Produced by other plans in this phase (do not create them here): `botWindLeg`, `botMsgHoldMs`,
`BOT_MSG_HOLD_MULTIPLIER`, `STORM_STEP_MS`, `BOT_STORM_STEP_MS`, `rankHailTargets`,
`priceHailOffer`, `Game.mooredReason`, the `moored` event `reason` values
(`justDocked` / `dock` / `home`), the `parley` event `kind:"hail"` tag, and the new test scripts
`scripts/storm_moored_reason_test.js`, `scripts/hail_ranking_test.js`,
`scripts/bot_storm_narration_test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Full per-seed divergence tooling, proved fail-first, then Tortuga's wind shadow (D-26 + D-18)</name>
  <reversibility rating="costly">CONTEXT.md rates D-18 one-way, and it is — but the irreversibility is
  consumed by 14-04's `--capture`, which carries the `one-way` rating and its blocking checkpoint.
  Writing this code only makes the old oracle disagree; until the fixtures are rewritten it is a plain
  `git revert` of one line.</reversibility>
  <files>scripts/determinism_diff.js, src/engine/index.js</files>
  <read_first>
    - `scripts/determinism_baseline.js` (all 254 lines — especially `serializeSeed` :88-92, `verify()` :150-241, and the entry-point guard :248-254)
    - `scripts/rebase_source_hash.js` (the house pattern for a companion tool built on `determinism_baseline.js`'s exports)
    - `scripts/lib/load_engine.js` (the single engine-loading seam every script uses)
    - `src/engine/index.js` lines 225-260 (`isHome`, `isIsland`, `moored`, `leeward`, `sailBudget`) and 291-300 (`stepToward`'s `pass()` — the `isIsland(o)||isHome(o)` idiom to mirror)
    - `.planning/phases/14-.../14-CONTEXT.md` — D-18, D-26, D-16
    - `.planning/phases/14-.../14-RESEARCH.md` §"Determinism harness mechanics"
    - `.planning/phases/14-.../14-PATTERNS.md` §"scripts/determinism_diff.js (NEW, D-26 Wave 0 gap)"
  </read_first>
  <action>
Create `scripts/determinism_diff.js`, then apply the D-18 `leeward()` fix, in that order — the tool
must be proved against a clean corpus before it is trusted to describe a dirty one.

**Part A — the tool.** Import `MANIFEST_PATH`, `playSeed`, `serializeSeed`, `hashBytes` from
`./determinism_baseline.js` and `loadEngine` from `./lib/load_engine.js`. Do not reimplement any of
them (the exports exist precisely so companion tools reuse `verify()`'s comparison logic — see that
file's header at :27-31). Export `diffAllSeeds(opts)` and guard the CLI entry point with the exact
`import.meta.url === \`file://${process.argv[1]}\`` idiom at `determinism_baseline.js:248`.

For each of the 30 manifest entries (`manifest.perSeed`, seed index `i` = array index, seed =
`12345 + i`): read the committed `scripts/fixtures/determinism/seed-<seed>.jsonl`, split on `"\n"`
and drop empty lines exactly as `verify()` does at :197-198; produce fresh lines from
`serializeSeed(playSeed(Game, roundCfg, i, seed))` split the same way; then walk `li` from 0 to
`max(storedLines.length, freshLines.length)` **to the end** — never `break` at the first mismatch.
That non-stopping walk is the whole point of this tool.

For every `li` where the two lines differ, `JSON.parse` both sides (a missing side is `null`) and
record: `seed`, `li`, `storedT`/`freshT` (the `t` field), `round`, `storm`, and `keys` — the sorted
set of top-level keys whose `JSON.stringify` values differ, each with truncated stored/fresh values
(reuse the 160-char truncation shape at `determinism_baseline.js:98-101`).

Distinguish two divergence classes, because this phase adds keys as well as changing behaviour:
a divergence is **additive-only** when every differing key is in the `--ignore-keys` set, and
**structural** otherwise. Per seed report `firstStructuralIndex` (or `-1`), `firstStormEventIndex`
(the index of the first line whose parsed `storm` is `true`, or `-1`), and
`preStormStructuralDivergence` (`firstStructuralIndex >= 0 && (firstStormEventIndex < 0 ||
firstStructuralIndex < firstStormEventIndex)`) — this triple is D-26's replacement criterion made
runnable. Roll up phase-wide: a divergent-seed count, a histogram of divergent-line counts by event
`t`, and a histogram by differing key.

Flags: `--assert-clean` exits 1 if ANY seed diverges and 0 otherwise (this is the tool's own
fail-first proof and its regression use); `--ignore-keys=k1,k2` seeds the additive-only set;
`--json` emits the whole report as one JSON object on stdout. Default mode prints the
human-readable report and exits 0. Follow the file's flat guard-clause house style —
`console.error(...)` + `process.exit(1)` on a genuine tool error (missing manifest, missing fixture,
unparseable line), no try/catch, no assertion library.

**Part B — prove it fail-first.** Run `node scripts/determinism_diff.js --assert-clean` against the
still-unchanged engine. It MUST exit 0 and report zero divergent seeds. A tool that cannot report
"nothing changed" cannot be trusted to report what did.

**Part C — D-18.** In `src/engine/index.js`, change `leeward()` (`:255-257`) so the upwind square is
tested against the home square as well as the island map, mirroring the exact
`isIsland(o)||isHome(o)` idiom already used by `stepToward`'s `pass()` at `:295`. Keep the existing
one-line compact form and add a short `// D-18 ...` comment in the file's own comment idiom
explaining that Tortuga is land for wind purposes, like every other island. Change nothing else in
the engine in this task — `moored()` and its home-proximity clause stay exactly as they are (D-19
keeps them; 14-03 owns the `reason` tagging).

**Part D — run it for real.** Re-run `node scripts/determinism_diff.js` and capture the full report.
Expect widespread divergence: `leeward()` feeds `sailBudget()` (9 → 7), which changes bot routing.
Record what the report actually says, including the `preStormStructuralDivergence` verdict per seed.

**Sequencing warning — read this before you react to a red gate.** Once Part C lands, `npm test`'s
FIRST gate (`node scripts/determinism_baseline.js --verify`) WILL FAIL, and it will keep failing
until 14-04 re-records the fixtures. **That failure is the expected and correct state.** Do NOT run
`--capture`. Do NOT revert the `leeward()` change to make the gate green. Verify this plan with the
other eight gates instead (they are listed in the verify block below).

**Planner note you are expected to confirm, not assume.** Every player spawns on a Tortuga berth
(`src/engine/index.js:209`), so from round 1 at least one player sits directly downwind of home —
measured this session across all 30 seeds. D-18 therefore bites on NON-storm rounds too, which means
D-26's literal "no seed diverges before its first storm round" assertion is expected to FAIL. That is
a finding to report, not a reason to weaken the assertion: implement it exactly as D-26 words it,
run it, and record the honest result. `--ignore-keys` exists so the 14-04 checkpoint can still
attribute each divergence to a named decision.
  </action>
  <verify>
    <automated>node scripts/determinism_diff.js --json | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const r=JSON.parse(s);const ok=Array.isArray(r.seeds)&&r.seeds.length===30&&r.seeds.every(x=>typeof x.firstStormEventIndex==="number"&&typeof x.preStormStructuralDivergence==="boolean")&&r.summary&&r.summary.byEventType&&r.summary.byKey&&r.summary.divergentSeeds>0;if(!ok){console.error("diff report shape or divergence count is wrong");process.exit(1)}console.log("diff report OK: divergentSeeds="+r.summary.divergentSeeds)})'</automated>
    <automated>node scripts/determinism_diff.js --assert-clean; test $? -eq 1 &amp;&amp; echo "fail-first proven: --assert-clean is RED against the changed engine"</automated>
    <automated>node scripts/engine_contract_check.js &amp;&amp; node scripts/module_graph_check.js &amp;&amp; node scripts/ui_contract_check.js &amp;&amp; node scripts/no_undef_check.js &amp;&amp; node scripts/net_contract_check.js &amp;&amp; node scripts/state_contract_check.js</automated>
    <automated>grep -Eq 'isHome\(' src/engine/index.js &amp;&amp; node -e 'const s=require("fs").readFileSync("src/engine/index.js","utf8");const m=s.match(/leeward\(p\)\{[\s\S]{0,400}?\n  \}/);if(!m||!/isHome/.test(m[0])){console.error("leeward() does not test the upwind square against home");process.exit(1)}console.log("D-18 leeward wind shadow present")'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/determinism_diff.js` exists and imports `MANIFEST_PATH`, `playSeed`, `serializeSeed` and `hashBytes` from `./determinism_baseline.js` — `grep -c 'from "./determinism_baseline.js"' scripts/determinism_diff.js` is 1 and no local re-definition of `serializeSeed` appears in the file.
    - `node scripts/determinism_diff.js --json` emits an object with `seeds` (length 30) and `summary`, where every seed entry carries `seed`, `divergentLines`, `firstStructuralIndex`, `firstStormEventIndex`, `preStormStructuralDivergence`, `keyDelta` and `typeDelta`.
    - The report enumerates MORE than one divergent line for at least one seed (proving it does not stop at the first), and `summary.divergentSeeds` is greater than 0.
    - `node scripts/determinism_diff.js --assert-clean` exits 1 now that the engine changed, and the SUMMARY records that the same command exited 0 against the unchanged engine before Part C landed.
    - `node scripts/determinism_diff.js --ignore-keys=wind2` runs without error and reports a smaller or equal structural-divergence count than the default run.
    - `leeward()` in `src/engine/index.js` tests the upwind square against the home square as well as the island map, in the same one-line compact form as before, with a `D-18` comment.
    - `moored()` at `src/engine/index.js:252-254` is byte-unchanged by this task — `grep -c 'man(p.pos,this.home)<=1' src/engine/index.js` is at least 1.
    - The eight non-determinism `npm test` gates all pass; the determinism gate is red and the SUMMARY says so explicitly, with the reason.
    - `scripts/engine_contract_check.js` passes, confirming no new `ORDER IS LOAD-BEARING` annotation was added (the hardcoded count of 7 at `scripts/engine_contract_check.js:121` is unchanged).
  </acceptance_criteria>
  <done>The diff tool enumerates every divergence across all 30 seeds with per-key attribution and a per-seed pre-storm verdict; it is proven to go green on a clean corpus and red on a dirty one; D-18's wind shadow is committed; the determinism gate is deliberately red.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the re-record record with the tracer's real findings (D-16)</name>
  <files>docs/DETERMINISM-RERECORD.md</files>
  <read_first>
    - The full report produced by Task 1's `node scripts/determinism_diff.js` run
    - `docs/MODULES.md` and `docs/VERIFICATION-CHECKLIST.md` (the house shape for a repo doc)
    - `.planning/phases/14-.../14-CONTEXT.md` — D-15, D-16, D-18, D-26
  </read_first>
  <action>
Create `docs/DETERMINISM-RERECORD.md`. D-16's surviving requirement is "document what changed and why
alongside the new fixtures"; this file is that record, started here while the evidence is fresh and
completed in 14-04 immediately before `--capture`.

Sections to write now:

1. **Why the corpus is being re-recorded** — name the three fixture-perturbing decisions by ID and
   one line each: D-15 (the simulator applies both storm gusts and rolls `windNow2`, so every event
   gains a `wind2` field), D-18 (Tortuga casts a wind shadow, so sail budgets and therefore bot
   routing change), D-21 (the `moored` event gains a `reason` field). State plainly that all three
   land BEFORE the single `--capture` run, and that the corpus is re-recorded exactly once.
2. **What the old oracle proved and what is lost** — after `--capture` the previous byte-for-byte
   baseline no longer exists, so "nothing else changed" can never again be proven across this
   boundary. Name the recovery path: revert the engine changes AND restore the fixtures from git
   history.
3. **Tracer findings (D-18 alone)** — paste the real numbers from Task 1: divergent seed count,
   the divergent-line histogram by event type, the histogram by differing key, and the per-seed
   `preStormStructuralDivergence` verdict.
4. **The D-26 criterion, honestly stated** — record that D-26's literal "no seed diverges before its
   first storm round" assertion is implemented and what it actually returned. If it fails, state the
   measured mechanism: every player spawns on a Tortuga berth (`src/engine/index.js:209`), so at
   least one player is downwind of home from round 1, and `leeward()` is a wind effect that applies
   on every round rather than a storm effect. Record the replacement evidence the phase will rely on
   instead — the per-key attribution that separates an additive serialization delta from a real
   behavioural change. Mark this as a finding awaiting Wyatt's confirmation at 14-04's checkpoint;
   do not present it as settled.
5. **Verdict** — left explicitly `PENDING — completed in 14-04 before --capture`.

Write it in the project's plain, direct documentation voice. No hedging, no filler. Do not run
`--capture` in this task.
  </action>
  <verify>
    <automated>test -f docs/DETERMINISM-RERECORD.md &amp;&amp; node -e 'const s=require("fs").readFileSync("docs/DETERMINISM-RERECORD.md","utf8");const need=["D-15","D-18","D-21","D-26","preStormStructuralDivergence","PENDING"];const miss=need.filter(k=>!s.includes(k));if(miss.length){console.error("missing sections/markers: "+miss.join(", "));process.exit(1)}if(s.split("\n").length<30){console.error("record is too thin to be a real record");process.exit(1)}console.log("re-record document OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/DETERMINISM-RERECORD.md` exists, is at least 30 lines, and names D-15, D-18 and D-21 as the three fixture-perturbing decisions.
    - It records the real divergent-seed count and both histograms from Task 1's run — numbers copied from the actual command output, not estimated.
    - It states the per-seed `preStormStructuralDivergence` verdict and, if any seed fails, names the measured mechanism (berth spawn at `src/engine/index.js:209` plus `leeward()` being wind-driven rather than storm-driven).
    - The verdict line reads `PENDING — completed in 14-04 before --capture`.
    - No `--capture` command was run in this task; `scripts/fixtures/determinism/manifest.json` is unmodified (`git diff --name-only` does not list it).
  </acceptance_criteria>
  <done>The re-record record exists with the tracer's real measurements and an explicitly pending verdict, so 14-04's checkpoint has evidence rather than assertions to present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → `scripts/` tooling | Offline Node scripts read committed fixtures and the local engine source; no network, no untrusted input |
| committed fixtures → the oracle | The fixture corpus is the behavioural ground truth; anything that rewrites it silently destroys the ability to detect regressions |

No new attack surface is introduced by this plan: it changes local browser-game simulation logic and
adds an offline developer script. No auth, no session, no network calls, no persisted user data, no
new dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `scripts/fixtures/determinism/*` via `--capture` | medium | mitigate | This plan never runs `--capture`; the re-record is gated in 14-04 behind full divergence enumeration and a blocking `checkpoint:decision`. Both prohibitions in `must_haves` restate the rule. |
| T-14-02 | Repudiation | the re-record having no audit trail | low | mitigate | `docs/DETERMINISM-RERECORD.md` records what changed, why, and with what measured evidence — D-16's surviving requirement. |
| T-14-03 | Denial of Service | `leeward()` change altering pathfinding cost | low | accept | `sailBudget` moves 9 → 7 for ships downwind of home; `stepToward`'s Dijkstra already handles budget 7 for every other island. No unbounded search is introduced. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. RESEARCH.md `## Package Legitimacy Audit` records "N/A — this phase introduces no new external packages"; the supply-chain gate does not fire. |
</threat_model>

<verification>
- `node scripts/determinism_diff.js --json` returns a well-formed 30-seed report with per-key and per-event-type attribution.
- `node scripts/determinism_diff.js --assert-clean` is RED after D-18 and was GREEN before it.
- The eight non-determinism `npm test` gates pass; the determinism gate is deliberately RED.
- `docs/DETERMINISM-RERECORD.md` carries the tracer's real numbers and a `PENDING` verdict.
</verification>

<success_criteria>
- The D-26 tooling gap is closed with a committed, fail-first-proven script.
- Tortuga casts a wind shadow (D-18), and its full blast radius is measured rather than assumed.
- The one-way door is documented and still shut — no fixture byte has been rewritten.
</success_criteria>

<output>
Create `.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-01-SUMMARY.md` when done.
State explicitly in it that `npm test`'s determinism gate is red by design and stays red until 14-04.
</output>
