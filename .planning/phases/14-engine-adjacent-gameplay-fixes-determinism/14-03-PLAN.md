---
phase: 14-engine-adjacent-gameplay-fixes-determinism
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/engine/index.js
  - scripts/storm_moored_reason_test.js
autonomous: true
requirements: [STORM-01, VERIFY-02]

must_haves:
  truths:
    - "The all-bot simulator applies BOTH storm gusts — up to 4 squares — sharing one `dodgedOnce` object across them, exactly as the live bot turn already does (D-15)."
    - "`play()` rolls `windNow2` from `PERP` immediately after the storm roll, consuming exactly one `this.r()` call at the same point in the per-round sequence the orchestrator uses (`src/orchestrator.js:683`), so live play and the simulator draw RNG identically from that round forward."
    - "`windNow2` is a direction letter on a stormy round and `null` otherwise — never left undefined — matching the orchestrator's own assignment."
    - "`Game.mooredReason(p)` returns exactly one of `justDocked`, `dock`, `home`, or `null`, in that precedence order, and `moored(p)` is `mooredReason(p) !== null` — the boolean's behaviour is bit-for-bit what it was (D-19, D-21)."
    - "The `man(p.pos,this.home)<=1` berth clause in `moored()` survives untouched — a ship at a Tortuga berth can never be wrecked by a storm (D-19)."
    - "The separate `isHome(nx)` safe-harbor early return in `windPush` is folded into the ordinary land branch, and the aground path stays unreachable for the home square because every cell that can push you onto Tortuga is a berth (D-19)."
    - "Every `moored` event emitted by `windPush` carries a `reason` field; none is emitted without one (D-21)."
    - "The `moored` event's `reason` is a plain string compared by exact `JSON.stringify` line equality in `serializeSeed()` — the added field changes every fixture SHA the moment a `moored` event fires, which is why it lands before the single re-record."
    - "A storm push whose `dist` runs out mid-leg, or whose very first candidate square is off-grid, emits no event and leaves `p.pos` unchanged."
  artifacts:
    - path: "src/engine/index.js"
      provides: "Two-gust simulator storm, the windNow2 roll, and cause-tagged moored events"
      contains: "mooredReason"
    - path: "scripts/storm_moored_reason_test.js"
      provides: "DOM-free coverage for the three moored causes, the unchanged boolean, and the D-19 berth-protection regression guard"
      min_lines: 60
  key_links:
    - from: "src/engine/index.js play()"
      to: "src/shared/index.js PERP"
      via: "the second gust's direction is drawn from PERP with one this.r() call"
      pattern: "PERP\\[wind\\]"
    - from: "src/engine/index.js takeTurn()"
      to: "src/engine/index.js windPush()"
      via: "two pushes sharing one dodgedOnce, mirroring src/ui/flow.js:556-567"
      pattern: "this\\.windNow2"
    - from: "src/engine/index.js windPush()"
      to: "src/engine/index.js mooredReason()"
      via: "the moored event's reason field is the cause the predicate actually matched"
      pattern: "reason:"
  prohibitions:
    - statement: "MUST NOT run `--capture` from this plan to clear the red determinism gate — the re-record is 14-04's single, gated step."
      requirement_id: VERIFY-02
      category: transparency
    - statement: "MUST NOT weaken or remove the berth clause in `moored()` while folding away the home special case — a ship parked at Tortuga must stay un-wreckable, which is an explicit user decision (D-19), not an implementation detail."
      requirement_id: STORM-01
      category: safety
---

<objective>
Land the remaining two fixture-perturbing engine changes so all three arrive before the single
re-record: the simulator finally models the storm the game actually ships (both gusts, four squares),
and the `moored` event stops telling one story for three different reasons.

Purpose: the determinism corpus is driven by `Game.play()`, which live play never calls — a UI-only
storm fix would report a hollow 30/30 for code it never exercised (D-15). And one narration line
covering three causes is what made the "the dock held fast" message read as a lie (D-21).

Output: a simulator that pushes ships the same distance the real game does, and `moored` events that
say which rule actually fired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-CONTEXT.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-RESEARCH.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-PATTERNS.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-01-SUMMARY.md

<interfaces>
<!-- src/engine/index.js:8 — the existing import line PERP must join -->
import { mulberry32, ING_ALL, TET, DIRS, OPPOSITE, SAIL_BUDGET, SAIL_BUDGET_LEEWARD, windStepCost, man, ilabelImg } from "../shared/index.js";

<!-- src/shared/index.js:147 — already exported and already ORDER IS LOAD-BEARING annotated at its
     declaration site. Importing it adds NO new annotation; engine_contract_check.js:121 hardcodes 7. -->
const PERP={N:["E","W"],S:["E","W"],E:["N","S"],W:["N","S"]};

<!-- src/orchestrator.js:681-683 — the live draw order this plan must reproduce exactly -->
appState.game.windNow="NSEW"[Math.floor(appState.game.r()*4)];
appState.game.stormNow=rollStorm(appState.game);
appState.game.windNow2=appState.game.stormNow?PERP[appState.game.windNow][Math.floor(appState.game.r()*2)]:null;

<!-- src/ui/flow.js:556-567 — the live bot storm block whose shape takeTurn must mirror -->
const before=[...p.pos];
const wasDocked=g.adjPort(p)!==null;
const dodgedOnce={v:false};
g.windPush(p,DIRS[g.windNow],2,dodgedOnce);
g.windPush(p,DIRS[g.windNow2],2,dodgedOnce);
p.justDocked=false;
if(p.pos[0]!==before[0]||p.pos[1]!==before[1])g.ev({t:wasDocked?"blownOut":"windmove",p:p.idx});

<!-- src/engine/index.js:233 — ev() already reads windNow2 onto every event as `wind2` -->
ev(o){if(!this.record)return;o.round=this.round;o.wind=this.windNow;o.storm=this.stormNow;o.wind2=this.windNow2; ... }

<!-- scripts/lib/load_engine.js -->
export async function loadEngine(); // -> { Game, roundCfg, sourceHash }
</interfaces>
</context>

<artifacts_this_phase_produces>
| Symbol | Kind | Location |
|---|---|---|
| `Game.mooredReason(p)` | new method | `src/engine/index.js` |
| `reason` on the `moored` event, values `justDocked` / `dock` / `home` | new event field | `src/engine/index.js` (mirrored in `src/ui/flow.js` by 14-05) |
| `wind2` present on every event (letter on stormy rounds, `null` otherwise) | changed serialization | `src/engine/index.js` `ev()` via `play()` |
| `scripts/storm_moored_reason_test.js` | new test script | `scripts/` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The simulator rides the whole storm — both gusts, four squares (D-15)</name>
  <reversibility rating="costly">CONTEXT.md rates D-15 one-way, and it is — but the irreversibility is
  consumed by 14-04's `--capture`, which carries the `one-way` rating and its blocking checkpoint.
  Until the fixtures are rewritten this is a plain `git revert` of two edits.</reversibility>
  <files>src/engine/index.js</files>
  <precondition>`scripts/determinism_diff.js` exists and `node scripts/determinism_diff.js --json` returns a 30-seed report (created by 14-01) — the divergence this task adds is only safe to create because it can be enumerated.</precondition>
  <read_first>
    - `src/engine/index.js` lines 1-20 (the import line and `rollStorm`), 225-235 (`r()`, `ev()`), 260-290 (`windPush`), 693-735 (`takeTurn`), 744-770 (`play`)
    - `src/orchestrator.js` lines 675-712 (the live per-round wind/storm/`windNow2` draw order — the thing being mirrored)
    - `src/ui/flow.js` lines 549-568 (`botTurn`'s storm block — the exact structural analog)
    - `src/shared/index.js` lines 143-152 (`DIRS`, `PERP` and its annotation)
    - `scripts/engine_contract_check.js` lines 105-130 (the hardcoded annotation count of 7)
    - `.planning/phases/14-.../14-CONTEXT.md` — D-15, D-16, D-23
    - `.planning/phases/14-.../14-RESEARCH.md` §"Determinism harness mechanics" and §"Exact fix location for D-15"
  </read_first>
  <action>
Add `PERP` to the existing `import { ... } from "../shared/index.js"` list at `src/engine/index.js:8`.
Do NOT add an `ORDER IS LOAD-BEARING` comment at the import site — the annotation lives at PERP's
declaration in `src/shared/index.js:147`, and `scripts/engine_contract_check.js:121` hardcodes a total
of exactly 7 across `src/engine` + `src/shared`. Adding one breaks that gate.

In `play()` (`src/engine/index.js:744-770`), insert the second-gust draw immediately after
`this.windNow=wind;this.stormNow=storm;`:
`this.windNow2=storm?PERP[wind][Math.floor(this.r()*2)]:null;`

The draw ORDER is load-bearing and is the whole point of the change: wind letter, then `rollStorm`,
then `windNow2` — byte-for-byte the sequence `src/orchestrator.js:681-683` uses. One `this.r()` call
per round, at a fixed point, exactly like `rollStorm`'s own header comment demands
(`src/engine/index.js:10-11`). Use `null` on a non-storm round, not `undefined`: the orchestrator
uses `null`, and `ev()` already writes `this.windNow2` onto every event as `wind2`.

In `takeTurn`'s storm block (`src/engine/index.js:697-704`), add `const dodgedOnce={v:false};` and a
second push, so the block reads: capture `before`, capture `wasDocked`, create `dodgedOnce`, push
`DIRS[windDir]` for 2 sharing `dodgedOnce`, push `DIRS[this.windNow2]` for 2 sharing the same
`dodgedOnce`, clear `justDocked`, emit the `blownOut`/`windmove` summary on a position change, and
return early on `shipwrecked`. That is `src/ui/flow.js:556-567` with `this.` substituted for `g.` —
copy its shape verbatim rather than inventing a variant. The shared `dodgedOnce` is what makes an
already-paid anchor hold across the second leg (`windPush`'s `anchorHold` branch at
`src/engine/index.js:270`).

**Explicitly out of scope — do not do these while you are in the file.** (1) Do NOT align `play()`'s
`newround` event payload to the orchestrator's (`dir2`/`streak` vs the simulator's `windStreak`);
D-15 is about storm movement, and the simulator's events are never narrated. (2) Do NOT add the
orchestrator's final-round wind re-spin (`src/orchestrator.js:704-707`) to `play()`'s last-lap path.
(3) Do NOT touch `chooseAction` or `takeTurn`'s action dispatch — D-23 established the engine already
enforces one action per turn, so there is nothing to mirror there.

**Sequencing warning.** `npm test`'s first gate is already red from 14-01 and this task widens the
divergence. That is the expected state. Do NOT run `node scripts/determinism_baseline.js --capture`.
Do NOT revert the engine change to make the gate green. Note the specific new effect: because `ev()`
writes `wind2` onto every event and `JSON.stringify` keeps `null` but drops `undefined`, every event
line in every seed now gains a `wind2` field — so all 30 seeds diverge at line 0. That is an additive
serialization delta, which is exactly what 14-01's `--ignore-keys=wind2` flag exists to separate from
a real behavioural change.
  </action>
  <verify>
    <automated>node -e 'const s=require("fs").readFileSync("src/engine/index.js","utf8");if(!/import \{[^}]*\bPERP\b[^}]*\} from "\.\.\/shared\/index\.js"/.test(s)){console.error("PERP not added to the shared import");process.exit(1)}const m=s.match(/if\(storm\)\{[\s\S]*?\n    \}/);if(!m){console.error("takeTurn storm block not found");process.exit(1)}const b=m[0];if((b.match(/this\.windPush\(/g)||[]).length!==2){console.error("takeTurn does not apply exactly two gusts");process.exit(1)}if(!/dodgedOnce/.test(b)||(b.match(/dodgedOnce/g)||[]).length<3){console.error("dodgedOnce is not shared across both gusts");process.exit(1)}if(!/this\.windNow2/.test(b)){console.error("second gust does not use windNow2");process.exit(1)}if(!/this\.windNow2=storm\?PERP\[wind\]\[Math\.floor\(this\.r\(\)\*2\)\]:null;/.test(s)){console.error("play() does not roll windNow2 in the orchestrator-matching form");process.exit(1)}console.log("D-15 two-gust alignment OK")'</automated>
    <automated>node -e 'const s=require("fs").readFileSync("src/engine/index.js","utf8");const i=s.indexOf("const storm=rollStorm(this);");const j=s.indexOf("this.windNow2=storm?");if(i<0||j<0||j<i){console.error("windNow2 is not rolled after the storm roll");process.exit(1)}if(s.slice(i,j).includes("this.r()")){console.error("an extra r() call was introduced between rollStorm and the windNow2 draw");process.exit(1)}console.log("RNG draw order matches src/orchestrator.js:681-683")'</automated>
    <automated>node scripts/engine_contract_check.js &amp;&amp; node scripts/module_graph_check.js &amp;&amp; node scripts/ui_contract_check.js &amp;&amp; node scripts/no_undef_check.js &amp;&amp; node scripts/dlog_replay_test.js</automated>
    <automated>node scripts/determinism_diff.js --ignore-keys=wind2 --json | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const r=JSON.parse(s);if(!r.summary||!r.summary.byKey||!("wind2" in r.summary.byKey)){console.error("wind2 does not appear in the key-delta histogram — the additive field is not being attributed");process.exit(1)}console.log("wind2 attributed in the divergence report: "+r.summary.byKey.wind2+" lines")})'</automated>
  </verify>
  <acceptance_criteria>
    - `PERP` appears in `src/engine/index.js`'s `../shared/index.js` import list, and `scripts/engine_contract_check.js` still passes (the annotation count is still exactly 7).
    - `play()` contains `this.windNow2=storm?PERP[wind][Math.floor(this.r()*2)]:null;` positioned after `const storm=rollStorm(this);`, with no additional `this.r()` call inserted between them.
    - `takeTurn`'s storm block calls `this.windPush` exactly twice, passes the same `dodgedOnce` object to both, and uses `this.windNow2` for the second direction.
    - `takeTurn`'s action-dispatch tail (`chooseAction` and the four branches) is byte-unchanged.
    - `play()`'s `newround` event payload is byte-unchanged, and no wind re-spin was added to the last-lap path.
    - `node scripts/determinism_diff.js --json` reports `wind2` in `summary.byKey` — the additive field is attributed rather than being mistaken for behaviour.
    - The eight non-determinism `npm test` gates pass, including `dlog_replay_test.js`.
    - `scripts/fixtures/determinism/manifest.json` is unmodified (`git diff --name-only` does not list it).
  </acceptance_criteria>
  <done>The headless simulator pushes ships the same four squares the shipping game does, drawing RNG in the same order the live loop does, and the extra divergence it creates is measurable and attributed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `moored` says which rule fired, and Tortuga's berths stay safe (D-19, D-21)</name>
  <reversibility rating="costly">The `reason` field changes the serialized event JSON and therefore
  every fixture SHA, so it must land inside the same re-record pass as D-15 and D-18. The
  irreversibility is consumed by 14-04's `--capture`, which carries the `one-way` rating and its
  blocking checkpoint; until then this is a plain revert.</reversibility>
  <files>src/engine/index.js, scripts/storm_moored_reason_test.js</files>
  <read_first>
    - `src/engine/index.js` lines 250-290 (`moored`, `leeward`, `sailBudget`, `windPush` — the whole push loop)
    - `src/engine/index.js` lines 205-215 (player spawn: `p.pos=[this.home[0]+d[0],this.home[1]+d[1]]`) and 736-742 (`checkFinish`'s own `man(p.pos,this.home)<=1`)
    - `src/engine/index.js` lines 291-300 (`stepToward`'s `pass()` — proof that no ship can ever stand on the home square)
    - `scripts/lib/load_engine.js` and `scripts/dlog_replay_test.js` lines 36-45 (the `check()` convention)
    - `.planning/phases/14-.../14-CONTEXT.md` — D-12 (superseded), D-19, D-20, D-21, D-22
    - `.planning/phases/14-.../14-RESEARCH.md` §"D-12 root cause" and its Assumption A3 (resolved by D-19: the berth clause is intentional and stays)
  </read_first>
  <behavior>
    - A player with `justDocked` true, pushed toward an island, produces a `moored` event with `reason: "justDocked"`.
    - A player standing on a dock cell under `cfg.singleDock`, not `justDocked` and not within 1 of home, pushed toward an island, produces `reason: "dock"`.
    - A player on a Tortuga berth, not `justDocked` and not on a dock cell, pushed toward an adjacent island, produces `reason: "home"`.
    - A player on a Tortuga berth pushed TOWARD the home square produces a `moored` event with `reason: "home"` — never an `aground` or `shipwrecked` event. The berth protection survives (D-19).
    - `mooredReason(p)` returns `null` for a player who is none of the three, and `moored(p)` returns exactly `mooredReason(p) !== null` for every state exercised.
    - Every `moored` event emitted by `windPush` across the whole battery carries a `reason` in the set `{justDocked, dock, home}` — none is emitted bare.
    - A push whose first candidate square is off-grid (`blocked(nx)` true) emits no event at all and leaves `p.pos` unchanged.
    - `windPush(p, d, 0)` emits no event and leaves `p.pos` unchanged.
  </behavior>
  <action>
Write `scripts/storm_moored_reason_test.js` FIRST, covering every case in `<behavior>` against a real
`Game` from `loadEngine()` + `roundCfg(...)` with directly-poked player fields, in the same
constructed-instance style RESEARCH.md's D-12 repro used. Use the repo's hand-rolled `check()` +
`process.exit(1)` convention; no assertion library. Run it and watch it fail.

Then in `src/engine/index.js`:

Add `mooredReason(p)` directly above `moored(p)` (`:252`). It returns the FIRST matching cause in the
same precedence `moored()`'s `||` chain already uses, and `null` when none match:
`p.justDocked` → `"justDocked"`; `this.cfg.singleDock && this.adjPort(p) !== null` → `"dock"`;
`man(p.pos, this.home) <= 1` → `"home"`. Keep the file's compact one-line-conditional density.

Rewrite `moored(p)` as `return this.mooredReason(p)!==null;`. The boolean result must be identical
for every input — this is a refactor with a new accessor, not a rule change. **Keep the
`man(p.pos,this.home)<=1` clause.** D-19 settled that it is not an overreaching patch: Manhattan
distance ≤ 1 from home is exactly the four berths, the same squares `checkFinish` uses at `:737`, and
it is the code expressing "a ship parked at Tortuga can never be wrecked by a storm." Removing or
narrowing it is a regression against an explicit user decision.

In `windPush` (`:262-289`), fold the separate home early-return at `:265` into the ordinary land
branch. The land test becomes `this.isIsland(nx)||this.isHome(nx)` — mirroring the exact
`isIsland(o)||isHome(o)` idiom `stepToward`'s `pass()` already uses at `:295` — and the first thing
inside it captures the cause: take `mooredReason(p)`, and when it is non-null emit
`{t:"moored", p:p.idx, reason:<cause>}` and return. Everything below (the `anchorHold` free pass, the
pay-if-`coins>=3` dodge, the flip, the lose-half / lose-a-crate / shipwreck ladder) stays byte-identical.

Record D-19's geometric proof in a comment where the fold happens: Tortuga is a single square, so the
only cells you can be pushed onto it FROM are its four orthogonal neighbours — which are exactly the
berths — and a berth always satisfies the `home` cause. The aground ladder is therefore unreachable
for the home square, which is why the old special case was redundant rather than load-bearing.

Preserve the relative order of the checks in the loop: blocked → land (island or home) → blocker →
move. Folding home into the land branch moves the home test from before the blocker check to the same
side of it, which is behaviour-neutral because no ship can ever occupy an island or the home square
(`stepToward`'s `pass()` excludes both, and players spawn on berths at `:209`).

Do NOT touch the `dodge`/`anchor`/`aground`/`shipwrecked`/`anchorHold` branches, and do NOT change
what `moored()` returns for any input. D-20 settled that the passive dock save stays — a ship blown
ONTO a dock square is sheltered by that dock on the next square of the same gust, because
`adjPort(p)` evaluates against the already-updated position. That mechanic is unchanged here; only
its narration changes, in 14-05.

**Sequencing warning.** The determinism gate is red and this task widens the divergence again. Do NOT
run `--capture`. Do NOT revert to make the gate green.
  </action>
  <verify>
    <automated>node scripts/storm_moored_reason_test.js</automated>
    <automated>node -e 'const s=require("fs").readFileSync("src/engine/index.js","utf8");if(!/mooredReason\(p\)\{/.test(s)){console.error("mooredReason not defined");process.exit(1)}if(!/moored\(p\)\{[\s\S]{0,120}?mooredReason\(p\)!==null/.test(s)){console.error("moored() is not defined in terms of mooredReason()");process.exit(1)}if(!/man\(p\.pos,this\.home\)<=1/.test(s)){console.error("the D-19 berth clause was removed");process.exit(1)}const w=s.match(/windPush\(p,d,dist,dodgedOnce\)\{[\s\S]*?\n  \}/);if(!w){console.error("windPush not found");process.exit(1)}if(/t:"moored",p:p\.idx\}/.test(w[0])){console.error("a bare moored event with no reason still exists in windPush");process.exit(1)}if((w[0].match(/t:"moored"/g)||[]).length!==1){console.error("windPush should emit moored from exactly one folded site");process.exit(1)}if(!/isIsland\(nx\)\|\|this\.isHome\(nx\)/.test(w[0])){console.error("the home special case was not folded into the land branch");process.exit(1)}console.log("D-19/D-21 fold + reason tagging OK")'</automated>
    <automated>node scripts/engine_contract_check.js &amp;&amp; node scripts/module_graph_check.js &amp;&amp; node scripts/ui_contract_check.js &amp;&amp; node scripts/no_undef_check.js &amp;&amp; node scripts/dlog_replay_test.js</automated>
    <automated>node scripts/determinism_diff.js --json | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const r=JSON.parse(s);const k=r.summary&&r.summary.byKey||{};if(!("reason" in k)&&!(r.summary.byEventType&&r.summary.byEventType.moored)){console.error("neither a reason key-delta nor a moored event-type delta appears — the reason field is not reaching the corpus");process.exit(1)}console.log("moored reason attributed in the divergence report")})'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/storm_moored_reason_test.js` exits 0 with one PASS line per `<behavior>` case, including the D-19 berth-protection case.
    - `Game.mooredReason` exists and `moored(p)` is defined as `this.mooredReason(p)!==null`.
    - `man(p.pos,this.home)<=1` still appears in `src/engine/index.js` — the berth clause survives.
    - `windPush` emits `t:"moored"` from exactly one place, always with a `reason`, and contains no bare `{t:"moored",p:p.idx}` form.
    - The land test in `windPush` reads `this.isIsland(nx)||this.isHome(nx)`; the standalone home early-return is gone.
    - The `anchorHold`, `dodge`, `anchor`, `aground` and `shipwrecked` branches are byte-unchanged.
    - The eight non-determinism `npm test` gates pass.
    - `scripts/fixtures/determinism/manifest.json` is unmodified.
  </acceptance_criteria>
  <done>Three distinct causes now travel with the event that reports them, the berth protection is proven by test rather than assumed, and all three fixture-perturbing changes for this phase have landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine tier → everything | `src/engine/` is the deterministic core; a change to its RNG draw sequence desynchronises live play from host-refresh replay if it is not mirrored exactly |
| committed fixtures → the oracle | The corpus is the behavioural ground truth for every later phase |

No new attack surface: this plan changes local simulation logic in a client-only browser game. No
auth, no session, no network, no persisted user data, no new dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08 | Tampering | RNG draw sequence in `play()` | medium | mitigate | The `windNow2` draw is placed at the exact point `src/orchestrator.js:683` uses and consumes exactly one `this.r()`; a verify probe asserts no extra `r()` call was inserted between the storm roll and the draw, so live play and host-refresh replay stay in lockstep. |
| T-14-09 | Tampering | fixture corpus rewritten to clear a red gate | medium | mitigate | Restated as a prohibition; `--capture` is not run here, and `manifest.json` being unmodified is an acceptance criterion. |
| T-14-10 | Denial of Service | `DIRS[this.windNow2]` when `windNow2` is null | low | accept | The second push only runs when `storm` is true, and `play()` assigns `windNow2` a letter on exactly those rounds. `takeTurn` has one caller (`play()`), so the invariant is local and total. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; RESEARCH.md `## Package Legitimacy Audit` records "N/A — this phase introduces no new external packages". |
</threat_model>

<verification>
- `node scripts/storm_moored_reason_test.js` passes, including the berth-protection regression guard.
- Structural probes confirm the two-gust shape, the RNG draw order, the `mooredReason` accessor, the surviving berth clause, and the folded home branch.
- `node scripts/determinism_diff.js --json` attributes both the additive `wind2` key and the `moored` `reason` change.
- The eight non-determinism `npm test` gates pass; the determinism gate is deliberately RED and stays red until 14-04.
</verification>

<success_criteria>
- All three fixture-perturbing decisions of this phase (D-15, D-18, D-21) have now landed, before any re-record.
- The berth protection Wyatt chose is enforced by a test, not by a comment.
- Nothing outside the storm path changed.
</success_criteria>

<output>
Create `.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-03-SUMMARY.md` when done.
State that the determinism gate is red by design and that D-15, D-18 and D-21 are all now in the tree
awaiting 14-04's single re-record.
</output>
