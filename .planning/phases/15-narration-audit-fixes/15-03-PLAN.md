---
phase: 15-narration-audit-fixes
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/ui/flow.js
  - scripts/narration_flow_test.js
  - package.json
autonomous: true
requirements: [NARR-02, NARR-03, NARR-05]

must_haves:
  truths:
    - "When a storm pushes a captain into a second island in the same turn, the already-paid anchor holding fast is narrated on that captain's own turn, not only on bots' turns (D-13)."
    - "A captain with no coins who cannot sail is told so — for a human captain AND for a bot captain, so a broke bot no longer appears to forget its turn (D-11 case 1)."
    - "A captain with no coins facing a storm-driven island is told plainly that the anchor is out of reach, rather than the Pay-to-anchor option silently vanishing (D-11 case 2)."
    - "The per-turn storm intro names only the leg that is happening now, in second person, instead of pre-announcing both legs (NARR-03)."
    - "Round headers and the round-level storm announcement stay addressed to the whole table and are NOT rewritten in second person (D-09)."
    - "Every ad-hoc narration line in src/ui/flow.js that names a captain broadcasts a viewer-neutral html plus a per-seat variant, so a guest never reads the second person about the host's action (D-07/D-08/D-10)."
  artifacts:
    - path: "scripts/narration_flow_test.js"
      provides: "DOM-free assertions over src/ui/flow.js's pure narration-line builders and source-level structure"
      contains: "process.exit(failures ? 1 : 0)"
    - path: "src/ui/flow.js"
      provides: "brokeSailLine and brokeAnchorLine pure builders, the anchorHold narrate call, and the variants-form ad-hoc flash sites"
      exports: ["brokeSailLine", "brokeAnchorLine", "stormIntroClause"]
  key_links:
    - from: "src/ui/flow.js"
      to: "src/ui/panel.js"
      via: "every captain-naming ad-hoc flash site passes its second-person string as a variants entry rather than selecting inline"
      pattern: "flash\\([^;]*\\[\\{\\s*seat"
    - from: "src/ui/flow.js"
      to: "src/ui/util.js"
      via: "bot storm narration renders the viewer-neutral text via describeFor and the per-seat text via narrationVariants"
      pattern: "narrationVariants\\("
  prohibitions:
    - requirement_id: NARR-02
      category: values
      status: resolved
      verification: judgment
      statement: "A broke captain's narration MUST NOT mock or shame the player for having no coins — it states the constraint and what they can still do, in the game's voice, without a jab at the player."
---

<objective>
Close the three turn-flow narration gaps that live in `src/ui/flow.js`, and put every captain-naming ad-hoc line in that file onto the viewer-aware broadcast the tracer proved.

Implements **D-13** (the `anchorHold` line never plays on your own turn because the narrate call is missing — a bug, not a copy problem), **D-11** (a broke line for BOTH moments: can't-afford-to-sail for humans AND bots, and can't-afford-to-anchor), **NARR-03**'s storm-intro rewrite, and **D-07/D-08/D-10** applied to this file's ad-hoc lines while **D-09** keeps the table-addressed lines third person. Per **D-05** all of this is structural, so it proceeds ahead of NARR-01's approval gate; the new wording is explicit DRAFT copy that goes onto the audit page for Wyatt's single review pass (**D-04**).

Purpose: three of the reported symptoms — the silent broke bot, the vanishing anchor option, and the "already anchored safely" line only appearing for other players — all live in this one file and none of them is a wording decision.
Output: two new pure line builders and one storm-intro clause builder exported from `src/ui/flow.js`; one missing `narrateLastEvent()` call; every captain-naming `flash()` site in the file converted to the neutral-plus-variants form; a second DOM-free harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-narration-audit-fixes/15-CONTEXT.md
@.planning/phases/15-narration-audit-fixes/15-RESEARCH.md
@.planning/phases/15-narration-audit-fixes/15-PATTERNS.md
@.planning/phases/15-narration-audit-fixes/15-01-SUMMARY.md
</context>

<hard_constraint>
`src/engine/index.js` is READ-ONLY. Every fix below is a UI-tier narration change riding existing event types. Do NOT add an event, a field, or a `reason` value — `anchorHold` already exists and already carries everything needed; the broke moments emit no event at all and must not start emitting one.

`src/ui/flow.js` may not import from `src/net/`. Network side effects go through `netHandlers()` / the existing `flash()` seam only.
</hard_constraint>

<disambiguation_D09_vs_NARR03>
D-09 and NARR-03 look like they contradict each other. They do not, and getting this wrong is the most likely mistake in this plan.

- **D-09 protects the ROUND-level announcement** — `EVENT_NARRATION.newround` (`src/ui/util.js:262-278`, owned by plan 15-04) and the round-level storm banner flashed from `src/orchestrator.js:687`/`:708`. Those speak to everyone at once. They stay third person. This plan does not touch them.
- **NARR-03 targets the PER-TURN storm intro** — the clause inside the turn banner at `src/ui/flow.js:570`, which is already addressed to one captain ("Ahoy, {name} — your turn!") and then wrongly pre-announces both storm legs. That clause is this plan's target.

Second person belongs on the per-turn banner because it is already a one-captain line. It does not belong on the round header because that one is not.
</disambiguation_D09_vs_NARR03>

<flagged_planner_assumptions>
The deterministic edge probe returned `unclassified` for NARR-02 and NARR-03 — no edge category applied, and per protocol these stay **unresolved** rather than being auto-backstopped. They are surfaced here as flagged planner assumptions rather than silently dropped:

1. **NARR-02 (unclassified).** Assumption: "the missing broke line" means exactly the two moments D-11 enumerates — cannot afford to sail (human and bot) and cannot afford to anchor — and no third broke moment exists that the punch list meant instead. Basis: D-11 records Wyatt selecting "Both" from an enumerated choice. If a third broke moment surfaces during the D-04 review pass, it is new scope for plan 15-06 or a follow-up, not a silent addition here.
2. **NARR-03 (unclassified).** Assumption: the requirement's quoted target text, "First, the storm pushes you {dir1}", is indicative phrasing rather than the literal final string — Wyatt authors copy (PROJECT.md Key Decisions), so what ships is whatever he approves at the D-04 gate. This plan drafts to that shape and marks it DRAFT.
</flagged_planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Harness for flow-level narration, plus the anchorHold narrate call (D-13)</name>
  <read_first>
    - `scripts/hail_ranking_test.js` lines 1-30 — the exact precedent for a DOM-free test that imports pure exported helpers directly from `src/ui/flow.js` (`rankHailTargets`, `priceHailOffer`, `hailWorthIt`). This proves the shape: import the pure builders only, never the DOM-touching turn functions.
    - `scripts/bot_storm_narration_test.js` lines 1-40 — the house skeleton: shebang, invariant-stating header, local `check`/`checkTrue`, no assertion library, `process.exit(failures ? 1 : 0)`.
    - `src/ui/flow.js` lines 204-259 — `windLeg` in full. Line 221's `moored` branch is the exact pattern the fix copies: `ev()` then `await narrateLastEvent()` then `liveRender()`. Line 224's `anchorHold` branch is missing the middle step.
    - `src/ui/util.js` line 318 — `EVENT_NARRATION.anchorHold`, the copy that already exists and needs no rewriting.
    - `src/ui/flow.js` lines 291-328 — `botWindLeg`, which already narrates every storm event it hits; this is why the line appears for bots and not for you.
    - `package.json` — the gate chain, now including `scripts/narration_test.js` from plan 15-01.
  </read_first>
  <files>scripts/narration_flow_test.js, package.json, src/ui/flow.js</files>
  <action>
    Create `scripts/narration_flow_test.js` following `scripts/hail_ranking_test.js`'s import shape and `scripts/bot_storm_narration_test.js`'s assertion skeleton. Header must state that it proves the turn-flow narration invariants for NARR-02/NARR-03/NARR-05 and D-11/D-13, that it imports only the pure exported builders from `src/ui/flow.js` plus the source text of that file, and that it never touches the DOM or calls a turn function. Local `check`/`checkTrue`, no assertion library, `process.exit(failures ? 1 : 0)`.

    Seed it with the D-13 assertion, which is structural rather than behavioural because `windLeg` needs the DOM: read `src/ui/flow.js` as text, locate the `windLeg` function body, and assert that the branch recording the `anchorHold` event awaits `narrateLastEvent()` before `liveRender()` — the same three-step order the `moored` branch immediately above already uses. Assert both branches match that order, so the test states the rule rather than one instance of it. Follow `scripts/ui_contract_check.js` / `scripts/no_undef_check.js` for the house convention on source-text assertions with named `file:line` failures.

    Register `node scripts/narration_flow_test.js` in `package.json`'s `"test"` chain, appended after `scripts/narration_test.js`, joined the same way. Do not reorder or remove any existing gate.

    Then apply the D-13 fix in `src/ui/flow.js`: the branch that records the `anchorHold` event must await `narrateLastEvent()` between recording the event and rendering, exactly as the `moored` branch three lines above does. No copy change — the line already exists in the table. No new event, no new field.
  </action>
  <verify>
    <automated>node scripts/narration_flow_test.js; test $? -eq 0</automated>
    <automated>npm test</automated>
    <automated>awk '/export async function windLeg/,/^export async function botWindLeg/' src/ui/flow.js | grep -c 'anchorHold'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/narration_flow_test.js` exits 0 and prints a PASSED summary.
    - `npm test` exits 0 with 14 gates (`grep -o 'node scripts/' package.json | wc -l` returns 14).
    - Inside `windLeg`'s body, the `anchorHold` branch contains an awaited `narrateLastEvent()` call positioned after the event is recorded and before the render call; the harness asserts this and fails with a `file:line` message when it is absent.
    - The `moored` branch's existing three-step order is unchanged (same harness assertion covers it).
    - `git diff --stat src/engine/index.js` reports no change.
    - `git diff src/ui/util.js` is empty — `EVENT_NARRATION.anchorHold`'s copy is untouched by this task.
    - `node scripts/bot_storm_narration_test.js` and `node scripts/narration_test.js` both still exit 0.
  </acceptance_criteria>
  <done>The already-anchored-safely line now plays on your own turn, proven by a structural assertion that will catch the call going missing again, and a second DOM-free harness is wired into `npm test`.</done>
</task>

<task type="auto">
  <name>Task 2: NARR-02 — a broke line for both moments, humans and bots (D-11)</name>
  <read_first>
    - `src/ui/flow.js` lines 215-240 — the storm-anchor block. Line 226 gates the Pay-to-anchor option on having a coin; when broke the option silently vanishes. Lines 231-238 build `flipLabel` and `promptMsg` from `broke`/`trueShipwreck`, which already hint at the consequence without ever stating the anchor is out of reach.
    - `src/ui/flow.js` lines 592-604 — the human sail gate. Line 595 gates the whole sail block on having a coin; the comment on line 603 explicitly declines to narrate it because the action prompt right after explains it. That reasoning holds for a human and is why the human line is a nudge, not a duplicate.
    - `src/ui/flow.js` lines 519-523 — the human action prompt (`humanAct`), which already reframes itself when the purse is empty. The new human sail line must not simply restate it.
    - `src/ui/flow.js` lines 666-680 — the bot sail gate. Line 675's condition gates the bot's sail on having a coin and silently skips it. There is NO equivalent prompt for a bot, which is the gap D-11 exists to close and the most likely origin of the reported symptom.
    - `src/ui/flow.js` lines 617-646 — `rankHailTargets`/`priceHailOffer`/`hailWorthIt`, the in-file precedent for a pure, exported, unit-tested helper living in this file.
    - `src/ui/flow.js` lines 528-534 and 595-603 — the existing `seatLocal()` ternary shape the new builders generalise.
    - `src/ui/util.js` — `pn`, `poss`, `isLocalTo`, `NEUTRAL_VIEWER` (the last two added by plan 15-01).
  </read_first>
  <files>src/ui/flow.js, scripts/narration_flow_test.js</files>
  <action>
    Add two pure, exported line builders to `src/ui/flow.js`, sited beside the existing exported pure helpers and following their documentation style (a header comment naming the decision each implements and why it exists):

    - `brokeSailLine(seat, viewerSeat)` — the can't-afford-to-sail line. Returns the addressed form when `isLocalTo(seat, viewerSeat)` and the third-person form otherwise, the third-person form naming the captain through `pn(seat)`. The addressed form follows D-07's name-prefix-then-second-person shape.
    - `brokeAnchorLine(seat, viewerSeat)` — the can't-afford-to-anchor line, same signature, same two forms.

    Both take a `viewerSeat` so the same builder produces the host's own render, the viewer-neutral broadcast text (pass `NEUTRAL_VIEWER`), and each guest's variant — no second string-building path. Mark both strings DRAFT pending the D-04 review, in the same comment style `EVENT_NARRATION.moored` uses for its own D-21 drafts. Keep the voice consistent with the surrounding copy: it states the constraint and what the captain can still do; it does not mock the player for being broke.

    Wire the three call sites, each passing the viewer-neutral text as `flash`'s first argument and the addressed text as a single `variants` entry keyed to that captain's seat, per plan 15-01's mechanism:
    1. **Broke can't anchor** — inside the storm-anchor block, when the captain cannot afford the anchor, flash `brokeAnchorLine` before the `ask()` so the missing option is explained rather than merely absent. Do not change the option list, the flip labels, or the existing `promptMsg` branching; the flip is still offered and its odds are unchanged.
    2. **Broke can't sail, human** — replace the standing comment that declines to narrate with the actual line, flashed when the sail gate's coin condition fails. Keep it a short nudge that does not duplicate the action prompt that follows immediately after.
    3. **Broke can't sail, bot** — add the equivalent branch to the bot sail gate, using the bot's pace (`botMsgHoldMs`) exactly as the neighbouring bot storm narration does, so a broke bot states why it is not moving instead of appearing to forget its turn.

    Extend `scripts/narration_flow_test.js` with a headed block asserting the builders directly, plus source-level assertions that all three call sites exist inside the right functions.
  </action>
  <verify>
    <automated>node scripts/narration_flow_test.js; test $? -eq 0</automated>
    <automated>npm test</automated>
    <automated>node -e 'import("./src/ui/flow.js").then(f=>{const a=f.brokeSailLine(0,0),b=f.brokeSailLine(0,-1);if(!a||!b||a===b){console.error("sail line not viewer-aware",a,b);process.exit(1)}const c=f.brokeAnchorLine(1,1),d=f.brokeAnchorLine(1,-1);if(!c||!d||c===d){console.error("anchor line not viewer-aware",c,d);process.exit(1)}console.log("ok")})'</automated>
  </verify>
  <acceptance_criteria>
    - `brokeSailLine` and `brokeAnchorLine` are exported from `src/ui/flow.js` and importable in Node with no DOM.
    - For each builder, the output with `viewerSeat` equal to the subject seat differs from the output with `viewerSeat` set to `NEUTRAL_VIEWER`, and neither output is empty or contains the JS undefined token.
    - With `appState.mySeat` left `null` and `viewerSeat` omitted, both builders return their third-person form (the `isLocalTo` fallback), and that form contains the `pn()`-rendered seat name.
    - Neither builder's output contains a raw roster name: `grep -cE '\$\{[A-Za-z_.\[\]() ]*\.name\}' src/ui/flow.js` returns 0.
    - `awk '/export async function botTurn/,/^export async function/' src/ui/flow.js | grep -c 'brokeSailLine'` returns 1 — the bot path narrates its own broke turn.
    - `awk '/export async function humanTurn/,/^export async function/' src/ui/flow.js | grep -c 'brokeSailLine'` returns 1 — the human path does too.
    - `awk '/export async function windLeg/,/^export async function botWindLeg/' src/ui/flow.js | grep -c 'brokeAnchorLine'` returns 1.
    - The storm-anchor block still pushes exactly one Pay-to-anchor option when the captain can afford it, and still offers the flip in every case: `awk '/export async function windLeg/,/^export async function botWindLeg/' src/ui/flow.js | grep -c 'value:"flip"'` returns 1.
    - `npm test` exits 0; `git diff --stat src/engine/index.js` reports no change.
  </acceptance_criteria>
  <done>Both broke moments narrate — for humans and for bots — through viewer-aware builders that unit-test DOM-free, with no engine change and no new event.</done>
</task>

<task type="auto">
  <name>Task 3: NARR-03 storm intro, and every captain-naming ad-hoc line onto the variants form (D-07/D-08/D-09/D-10)</name>
  <read_first>
    - `src/ui/flow.js` lines 565-572 — the turn banner, whose storm clause is NARR-03's target, and whose `seatLocal()?…:…` ternary is the shape being converted.
    - `src/ui/flow.js` lines 329-339 — `humanWind`, whose second-leg line at 336 already reads in second person and already announces the second leg at the moment it happens. This is why the turn banner pre-announcing both legs is the redundancy NARR-03 removes.
    - `src/ui/flow.js` lines 647-666 — `botTurn`'s equivalent second-leg line, third person at bot pace.
    - `src/ui/flow.js` lines 291-328 — `botWindLeg`, whose two `describe()`-then-`flash()` sites must render viewer-neutral text and carry variants.
    - `src/ui/flow.js` lines 105-112 (`humanFlip`), 470-486 (`humanTrade`'s refusal lines), 526-534 and 539-546 (`humanAct`'s trade-wind and powder lines), 595-604 (leeward and trade-wind lines), 850-882 (side-bet call and settlement lines) — the full ad-hoc inventory in this file.
    - `src/ui/util.js` — `describeFor`, `narrationVariants`, `NEUTRAL_VIEWER`, `pn`, `poss` (added or confirmed by plan 15-01).
    - `src/ui/panel.js` `flash()` — the 4th `variants` parameter added by plan 15-01, and its header comment explaining the additive-parameter convention.
    - `15-RESEARCH.md` §"Pattern 2" — the exhaustive 18-site line list for this file.
  </read_first>
  <files>src/ui/flow.js, scripts/narration_flow_test.js</files>
  <action>
    **NARR-03 — the per-turn storm intro.** Extract the turn banner's storm clause into an exported pure builder `stormIntroClause(dir1)` in `src/ui/flow.js`, and rewrite it so it names only the leg happening now, in second person, in the shape the requirement quotes — the second leg is already announced at the moment it happens by `humanWind` and `botTurn`, so pre-announcing it here is the redundancy being removed. Mark it DRAFT pending D-04. Keep the rest of the banner (the greeting, the wind direction for the round) intact. See `<disambiguation_D09_vs_NARR03>`: this is the per-turn banner, not the round header.

    **D-09 — leave the table-addressed lines alone.** Do not convert `EVENT_NARRATION.newround` (a different file, and plan 15-04's territory), and do not convert the round-level storm announcement flashed from `src/orchestrator.js`. Nothing in this task may make a line that speaks to everyone read as if it speaks to one captain.

    **D-07/D-08/D-10 — the ad-hoc conversion.** For every `flash()` call site in `src/ui/flow.js` that names a captain, replace the inline `seatLocal()` selection with the neutral-plus-variants form: pass the third-person string as `flash`'s message, and pass the addressed string as a single-entry `variants` array keyed to that captain's seat. This is a correctness fix, not only new copy — today the six existing sites compute the addressed string on the HOST and broadcast it verbatim, so every guest currently reads the second person about the host's action, which is exactly the NARR-05 symptom.

    Cover, at minimum: the coin-flip announcement in `humanFlip`; the trade-wind sweep lines (three sites); the leeward warning; the too-poor-for-powder line; the turn banner; the trade-refusal lines; the two side-bet call lines. Where no addressed form exists yet, draft one following D-07's name-prefix shape and mark it DRAFT. Where one already exists, reuse its exact current wording as the variant so this task changes delivery, not copy, for those six.

    Apply **D-08** where an event names two captains — a refusal or a side-bet call names both the caller and the called — by emitting a variants entry for each named seat, not only the actor.

    For `botWindLeg`'s two `describe()`-then-`flash()` sites, take the message text from `describeFor(event, NEUTRAL_VIEWER)` and the variants from `narrationVariants(event)`, keeping the existing bot-pace hold argument. Leave the per-square render/sleep ordering exactly as it is — that ordering is Phase 14's D-22 fix and is not in scope.

    Leave the lines that name no captain untouched: the nobody-has-cargo notice, the firing-up-the-ovens line, and the side-bet settlement summary.

    Extend `scripts/narration_flow_test.js` with a headed block asserting `stormIntroClause`'s output for each direction, and source-level assertions that no `flash(` call in this file still selects its message with an inline `seatLocal(` ternary.
  </action>
  <verify>
    <automated>node scripts/narration_flow_test.js; test $? -eq 0</automated>
    <automated>node scripts/bot_storm_narration_test.js; test $? -eq 0</automated>
    <automated>npm test</automated>
    <automated>node -e 'import("./src/ui/flow.js").then(f=>{const s=f.stormIntroClause("N");if(!s||/undefined/.test(s)){console.error("bad clause",s);process.exit(1)}console.log("ok",s)})'</automated>
    <human-check>Solo game plus a two-tab Chrome multiplayer session. Solo: start a storm turn and confirm the banner names one leg, then the second leg is announced when it actually happens. Two-tab: trigger a trade-wind sweep and a too-poor-for-powder moment on a remote seat; that seat's tab must read the addressed form and the other tab the third-person form for the same moment.</human-check>
  </verify>
  <acceptance_criteria>
    - `stormIntroClause` is exported from `src/ui/flow.js`, returns a non-empty string containing no JS undefined token for each of the four wind directions, and its output for two different directions differs.
    - The turn banner no longer pre-announces the second storm leg: `awk '/export async function humanTurn/,/^export async function/' src/ui/flow.js | grep -c 'windNow2'` returns 0.
    - `humanWind` still announces the second leg at the moment it happens: `awk '/export async function humanWind/,/^export async function/' src/ui/flow.js | grep -c 'windNow2'` returns at least 1.
    - No `flash(` call in `src/ui/flow.js` selects its message with an inline viewer test any more: `grep -cE 'flash\([^;]*seatLocal\(' src/ui/flow.js` returns 0.
    - The variants form is in use at multiple sites: `grep -cE 'flash\([^;]*\[\{\s*seat' src/ui/flow.js` returns at least 8.
    - `botWindLeg` renders neutral text with variants: `awk '/export async function botWindLeg/,/^\/\/ only ever called during a storm/' src/ui/flow.js | grep -c 'narrationVariants('` returns 2.
    - `grep -cE '\$\{[A-Za-z_.\[\]() ]*\.name\}' src/ui/flow.js` returns 0 — names still flow through `pn()`/`poss()` only (T-15-02).
    - Every commit this plan makes touches only files listed in its `files_modified` frontmatter — `git show --stat` for each of this plan's commits lists no path outside `src/ui/flow.js`, `scripts/narration_flow_test.js`, `package.json`. `EVENT_NARRATION.newround` and the round-level storm announcement in `src/orchestrator.js` are therefore untouched (D-09).
    - `npm test` exits 0 including the 31-seed determinism verify; `git diff --stat src/engine/index.js` reports no change.
    - The two-tab human check above is recorded in the SUMMARY naming which moments were exercised and what each tab read.
  </acceptance_criteria>
  <done>The per-turn storm intro names one leg in second person, and every captain-naming ad-hoc line in this file is delivered per viewer instead of being broadcast in the host's own voice — with the round-level lines deliberately left addressed to the table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player-typed display name → narration HTML | Names reach the new builders and the converted `flash()` sites |
| host browser → `rooms/{code}/narr` → guest browsers | The converted sites now emit per-seat variants across this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Tampering (stored XSS via display name) | `brokeSailLine` / `brokeAnchorLine` / `stormIntroClause` / every converted `flash()` site in `src/ui/flow.js` | high | mitigate | Every new string interpolates captain names through `pn()`/`poss()` only, which route through `pname()`'s existing single-source `escHtml`. Gate: `grep -cE '\$\{[A-Za-z_.\[\]() ]*\.name\}' src/ui/flow.js` returns 0. |
| T-15-01 | Information Disclosure | variants entries emitted from `src/ui/flow.js` | medium | mitigate | Each variant is produced by the SAME builder as the neutral line with only the viewer argument changed, so it can differ only in how it addresses the subject. No converted site reads another captain's hold, recipe, or coin count to build a variant. |
| T-15-07 | Repudiation / desync | the `anchorHold` narrate call added in Task 1 | low | accept | `narrateLastEvent()` renders an already-recorded event; it emits nothing and mutates no state, so adding it cannot change the event stream a guest replays. Proven by the 31-seed determinism verify staying green. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero packages installed by this plan or this phase — `15-RESEARCH.md` §Package Legitimacy Audit records "Not applicable". No install task exists. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and paths **created by this plan** (new — exclude from source-drift verification):

| Kind | Name | Location |
|------|------|----------|
| new file | `scripts/narration_flow_test.js` | repo root `scripts/` |
| new exported function | `brokeSailLine(seat, viewerSeat)` | `src/ui/flow.js` |
| new exported function | `brokeAnchorLine(seat, viewerSeat)` | `src/ui/flow.js` |
| new exported function | `stormIntroClause(dir1)` | `src/ui/flow.js` |

Consumed but NOT created here (added by plan 15-01): `isLocalTo`, `describeFor`, `narrationVariants`, `NEUTRAL_VIEWER`, and `flash`'s 4th `variants` parameter.
</artifacts_this_phase_produces>

<verification>
- `npm test` green at 14 gates including the 31-seed determinism verify.
- `git diff --stat src/engine/index.js` empty.
- Every commit's `git show --stat` confined to this plan's `files_modified` (D-09 boundary held).
- One solo storm turn and one two-tab multiplayer session confirming per-seat addressing and the one-leg storm intro.
</verification>

<success_criteria>
1. The already-anchored-safely line plays on your own turn.
2. A broke human and a broke bot each get a line for the sail they cannot afford; a broke captain facing a storm-driven island is told the anchor is out of reach.
3. The per-turn storm intro names only the leg happening now, in second person.
4. The round header and the round-level storm announcement remain third person and untouched.
5. No `flash()` site in `src/ui/flow.js` selects its message by the host's own seat any more.
6. All 14 `npm test` gates green, with the engine and the event stream unchanged.
</success_criteria>

<output>
Create `.planning/phases/15-narration-audit-fixes/15-03-SUMMARY.md` when done.
</output>
