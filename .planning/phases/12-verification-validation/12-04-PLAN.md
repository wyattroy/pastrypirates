---
phase: 12-verification-validation
plan: 04
type: execute
wave: 4
depends_on: [12-03]
files_modified:
  - docs/VERIFICATION-CHECKLIST.md
  - .planning/phases/12-verification-validation/12-VALIDATION.md
autonomous: false
requirements: [VERIFY-04]
user_setup: []

must_haves:
  truths:
    - "Wyatt completes one full DESKTOP Safari solo playthrough with no perf/compat regression (VERIFY-04, D-03)"
    - "12-VALIDATION.md maps VERIFY-01..04 to their recorded checklist evidence and marks them satisfied, mirroring 11-VALIDATION.md"
  artifacts:
    - ".planning/phases/12-verification-validation/12-VALIDATION.md"
    - "docs/VERIFICATION-CHECKLIST.md (Criterion-4 section recorded)"
  key_links:
    - "Safari caches ES modules by URL — serve on a fresh port (new origin) or hard-reload; a ?cb= query does NOT bust imported module files (D-01 / CONTEXT gotcha)"
    - "12-VALIDATION.md mirrors the Requirements->Test Map format of 11-VALIDATION.md"
---

<objective>
Capture Wyatt's manual sign-off — one full DESKTOP Safari solo playthrough (Criterion 4 / VERIFY-04, D-03) — and close the phase by writing the 12-VALIDATION.md Requirements→Test map that marks VERIFY-01..04 satisfied, mirroring 11-VALIDATION.md.

Per D-03, Wyatt's personal bar is exactly one desktop-Safari solo game start-to-finish (sail, dock, trade, battle, fish, end-of-voyage) on his Mac. The desktop-Safari storm render was already re-verified by Wyatt in Phase 11 (D-12), so this playthrough covers the rest of the Safari surface. Explicitly out of scope for Wyatt's pass: mobile Safari (iPhone), iPad, and Safari multiplayer — the refactor branch is not deployed to a phone-reachable origin and the public site is still v1.0. Claude covers all of Chrome (solo + MP + recovery, plans 12-01..12-03); Wyatt owns only what no tool can drive.

This plan is autonomous:false — Task 1 is a blocking human-verification checkpoint the orchestrator pauses on for Wyatt, then resumes.

Purpose: Human sign-off on the Safari surface plus the formal phase closeout.
Output: docs/VERIFICATION-CHECKLIST.md Criterion-4 recorded; .planning/phases/12-verification-validation/12-VALIDATION.md created with VERIFY-01..04 marked satisfied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-verification-validation/12-CONTEXT.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VALIDATION.md
@.planning/REQUIREMENTS.md
@docs/VERIFICATION-CHECKLIST.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `.planning/phases/12-verification-validation/12-VALIDATION.md` — the Requirements→Test map closeout, mirroring 11-VALIDATION.md, mapping VERIFY-01..04 to their recorded evidence and marking each satisfied.

This plan appends to:
- `docs/VERIFICATION-CHECKLIST.md` (Criterion-4 section — Wyatt's Safari sign-off).

No new source symbols, endpoints, or runtime code are introduced.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wyatt — one full DESKTOP Safari solo playthrough (VERIFY-04, D-03)</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - docs/VERIFICATION-CHECKLIST.md (the Criterion-4 skeleton and the local-serve preamble)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-03 desktop-Safari-only scope; the Safari ES-module-cache / fresh-port gotcha)
  </read_first>
  <what-built>
    Chrome-side verification (solo loop, two-tab multiplayer, and the pause/refresh recovery matrix) is complete and recorded in Criterions 1-3 of docs/VERIFICATION-CHECKLIST.md. The one surface no tool can drive is desktop Safari. Wyatt plays one full solo game there to confirm no perf/compat regression.
  </what-built>
  <how-to-verify>
    1. Serve the refactor branch locally on a FRESH port (Safari caches ES modules by URL — a ?cb= query does NOT bust imported module files; use a new origin/port or a hard reload). Confirm the server cwd is this worktree — NOT playpastrypirates.com (still v1.0), per D-01.
    2. Open the local URL in DESKTOP Safari on the Mac.
    3. Play one full solo game start-to-finish: sail, dock, trade, battle, fish, and reach end-of-voyage. (Storm rendering was already Safari-verified in Phase 11 / D-12, so it is not required again here, though it may appear naturally.)
    4. Watch for any freeze, jank, visual breakage, or broken interaction versus the pre-refactor game.
    Out of scope (do NOT test): mobile Safari, iPad, Safari multiplayer (D-03).
  </how-to-verify>
  <action>
    Present Wyatt the fresh-port local URL and the exact playthrough steps above (in plain language — Wyatt is a non-coder). On his sign-off, record the desktop-Safari solo PASS (with any notes) into the Criterion-4 section of docs/VERIFICATION-CHECKLIST.md and mark VERIFY-04's manual half satisfied (D-03). Do not embed code fences.
  </action>
  <acceptance_criteria>
    - Wyatt confirms a full desktop-Safari solo playthrough (sail, dock, trade, battle, fish, end-of-voyage) with no perf/compat regression
    - The Safari sign-off is recorded in the Criterion-4 section with the date and any notes
  </acceptance_criteria>
  <resume-signal>Wyatt types "approved" or describes the issues he saw.</resume-signal>
  <done>VERIFY-04's human Safari sign-off is captured in the checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Write 12-VALIDATION.md Requirements→Test map; mark VERIFY-01..04 satisfied</name>
  <files>.planning/phases/12-verification-validation/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VALIDATION.md (the exact structure to mirror: Test Infrastructure, Requirements→Test Map, Manual-Only Verifications, Sign-Off)
    - docs/VERIFICATION-CHECKLIST.md (the recorded evidence for all four criteria)
    - .planning/REQUIREMENTS.md (VERIFY-01..04 wording and traceability)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Create .planning/phases/12-verification-validation/12-VALIDATION.md mirroring 11-VALIDATION.md's sections. In the Requirements→Test Map, add one row per requirement pointing at its recorded evidence in docs/VERIFICATION-CHECKLIST.md: VERIFY-01 → `npm test` / determinism 30/30 (Criterion 1, D-04); VERIFY-02 → Chrome-MCP solo loop incl. storm + end-of-voyage (Criterion 2); VERIFY-03 → Chrome-MCP two-tab MP + pause/refresh recovery matrix (Criterion 3, D-02); VERIFY-04 → Wyatt's desktop-Safari solo playthrough (Criterion 4, D-03). Mark each satisfied. In Manual-Only Verifications, list the desktop-Safari playthrough (D-03) and note the deferred surfaces (mobile Safari, iPad, Safari-MP, real two-device play, expanded determinism fixtures) as explicitly out of v1.1 scope. Note that the Test Infrastructure is the existing zero-dependency `npm test` chain plus the committed docs/VERIFICATION-CHECKLIST.md re-runnable procedure — no browser-test framework (D-01). Do not modify .planning/REQUIREMENTS.md's traceability table here (the phase-gate/verify-work step owns that). Do not embed code fences.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-verification-validation/12-VALIDATION.md && [ "$(grep -oE 'VERIFY-0[1-4]' .planning/phases/12-verification-validation/12-VALIDATION.md | sort -u | wc -l | tr -d ' ')" = "4" ] && npm test</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/12-verification-validation/12-VALIDATION.md exists and mirrors 11-VALIDATION.md's structure
    - All four IDs VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04 appear, each mapped to its recorded checklist evidence and marked satisfied
    - Manual-Only Verifications lists the desktop-Safari playthrough (D-03) and the deferred out-of-scope surfaces
    - `npm test` still exits 0
  </acceptance_criteria>
  <done>The Phase 12 closeout artifact exists; VERIFY-01..04 are documented satisfied with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → local HTTP test server | Loopback-only serving of the worktree; Safari opened against a fresh-port local origin |

Phase 12 introduces no new runtime code or endpoint; this plan produces only documentation (a checklist entry and a validation map).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information disclosure | local HTTP test server | low | mitigate | Bind to 127.0.0.1; fresh ephemeral port for Safari; stopped after testing |
| T-12-02 | Spoofing (verification integrity) | browser origin under test | medium | mitigate | Serve THIS worktree on a fresh port; never test against playpastrypirates.com (still v1.0), per D-01 |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase; browser-test-framework out of scope (D-01) |
</threat_model>

<verification>
- Wyatt's desktop-Safari solo playthrough is signed off and recorded in Criterion-4.
- 12-VALIDATION.md exists, mirrors 11-VALIDATION.md, and marks VERIFY-01..04 satisfied with evidence pointers.
- `npm test` still exits 0.
</verification>

<success_criteria>
Criterion 4 (VERIFY-04) is verified via Wyatt's manual desktop-Safari sign-off, and the phase closes with a 12-VALIDATION.md Requirements→Test map marking VERIFY-01..04 satisfied — the v1.1 monolith refactor is proven correct end-to-end.
</success_criteria>

<output>
Create `.planning/phases/12-verification-validation/12-04-SUMMARY.md` when done.
</output>
