---
phase: 12-verification-validation
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - docs/VERIFICATION-CHECKLIST.md
autonomous: false
requirements: [VERIFY-02]
user_setup: []

must_haves:
  truths:
    - "The full solo loop — sail, dock, trade, fish, battle, coin-flip — completes in Chrome with zero console errors (VERIFY-02)"
    - "A storm round renders without freeze or crash (the v1.0 BUG-01 surface) and end-of-voyage (win box / redesigned badges / confetti) renders"
    - "Solo state persists to localStorage['pp_solo'] (the 11-02 fix)"
  artifacts:
    - "docs/VERIFICATION-CHECKLIST.md (Criterion-2 section recorded)"
  key_links:
    - "Any temporary cfg.storm force in src/engine/index.js is reverted — determinism SOURCE stays unchanged (D-04)"
    - "Chrome-MCP asserts on window.__pp_app_state_debug() and read_console_messages (onlyErrors) throughout the loop"
---

<objective>
Verify the full solo gameplay loop end-to-end in Chrome via the browser-MCP (Criterion 2 / VERIFY-02): sail, dock, trade, fish, battle, coin-flip, storm, and end-of-voyage. Phase 11 already exercised sail/dock/battle/coin-flip live; this plan formalizes that into the committed checklist AND closes the gaps that were never formally verified — trade, fish, and end-of-voyage — plus a storm round in Chrome.

Per D-01 this is orchestrator-driven Chrome-MCP verification recorded into the committed checklist (no browser-test framework). Because it requires a live browser drive plus judgment, this plan is autonomous:false.

Purpose: Prove the solo experience survived the monolith refactor with no regressions.
Output: docs/VERIFICATION-CHECKLIST.md Criterion-2 section filled with PASS evidence for every solo sub-step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-verification-validation/12-CONTEXT.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VERIFICATION.md
@docs/VERIFICATION-CHECKLIST.md
@src/main.js
@src/orchestrator.js
@src/ui/board.js
</context>

<artifacts_this_phase_produces>
This plan appends to `docs/VERIFICATION-CHECKLIST.md` (Criterion-2 section). No new files or source symbols are created.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Chrome-MCP solo loop — sail, dock, trade, fish, battle, coin-flip (VERIFY-02)</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - docs/VERIFICATION-CHECKLIST.md (the Criterion-2 section skeleton and the "How to re-run" local-serve preamble from 12-01)
    - src/main.js (debug hooks: window.__pp_app_state_debug(), window.__pp_module_ok)
    - src/orchestrator.js (humanTurn / botTurn / localAsk turn-flow surface)
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VERIFICATION.md (what Phase 11 already proved — avoid re-proving; formalize instead)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-01 Chrome-MCP checklist approach; the debug-hook and local-serve gotchas)
  </read_first>
  <action>
    Serve the branch locally (127.0.0.1, confirm the server cwd is this worktree). Drive Chrome via the browser-MCP through a full solo game against AI captains, exercising every solo mechanic: start a solo game, sail across islands, dock and coin-flip for ingredients, execute at least one trade (swap or buy), perform at least one fish action, and resolve at least one battle against an AI captain. Prioritize formally recording trade and fish (the gaps Phase 11 never verified) alongside the already-exercised sail/dock/battle/coin-flip. Throughout the loop, poll `read_console_messages` with onlyErrors — it MUST stay empty — and sanity-check game state via `window.__pp_app_state_debug()` (confirm it returns a fresh snapshot, e.g. players/recipe/positions look coherent). Record a per-mechanic PASS line (sail, dock, trade, fish, battle, coin-flip) with the observed evidence into the Criterion-2 section. Do not embed code fences.
  </action>
  <verify>
    <automated>grep -qi 'trade' docs/VERIFICATION-CHECKLIST.md && grep -qi 'fish' docs/VERIFICATION-CHECKLIST.md && npm test</automated>
    <human-check>Chrome-MCP solo drive completes sail, dock, trade, fish, battle, and coin-flip with read_console_messages (onlyErrors) empty throughout and window.__pp_app_state_debug() returning coherent state; each mechanic recorded PASS in Criterion-2.</human-check>
  </verify>
  <acceptance_criteria>
    - Solo sail, dock, trade, fish, battle, and coin-flip each complete in Chrome
    - Zero console errors across the entire solo loop (read_console_messages onlyErrors stays empty)
    - Each mechanic is recorded with a PASS line in the Criterion-2 section, with trade and fish explicitly captured (the Phase 11 gaps)
  </acceptance_criteria>
  <done>The interactive solo loop (excluding storm and end-of-voyage) is verified in Chrome and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Storm round + end-of-voyage in Chrome; pp_solo persistence (VERIFY-02)</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - docs/VERIFICATION-CHECKLIST.md (Criterion-2 section)
    - src/ui/board.js (buildStormLayers — the pre-baked PNG tile / snap-not-animate v1.0 BUG-01 fix)
    - src/engine/index.js (cfg.storm and rollStorm — the storm trigger; changing it mutates the determinism source hash)
    - src/orchestrator.js (saveSoloState / the pp_solo localStorage write at ~line 718 and read at ~line 1090)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-04 keep-baseline; the storm-force-and-revert gotcha)
  </read_first>
  <action>
    Continue the solo game until a storm round occurs. Storms are 12.5% per round, so either play through until one triggers naturally, or temporarily set `cfg.storm=1` in src/engine/index.js to force one — and if forced, REVERT the edit immediately afterward, because it changes the determinism source hash (D-04). Confirm in Chrome that the storm renders without freeze, crash, or beachball (the v1.0 BUG-01 surface guarded by buildStormLayers) and the game continues normally. Then play through to end-of-voyage and confirm the win box, redesigned badges, and confetti render and the end screen appears. Confirm the solo game persisted to `localStorage['pp_solo']` (the saveSoloState fix from 11-02) by reading the key in the console. Record storm-render PASS, end-of-voyage PASS, and pp_solo-persistence PASS into Criterion-2, and mark VERIFY-02 satisfied. Do not embed code fences.
  </action>
  <reversibility rating="costly">Any temporary cfg.storm=1 force in src/engine/index.js mutates the determinism source hash; it MUST be reverted and determinism re-verified before the task is done.</reversibility>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && git diff --quiet HEAD -- src/engine/index.js && npm test</automated>
    <human-check>In Chrome, the storm round renders without freeze/crash, end-of-voyage (win box / badges / confetti) renders, and localStorage['pp_solo'] holds the persisted solo game; all recorded in Criterion-2.</human-check>
  </verify>
  <acceptance_criteria>
    - A storm round renders in Chrome without freeze, crash, or beachball; the game continues
    - End-of-voyage screen (win box, redesigned badges, confetti) renders
    - localStorage['pp_solo'] contains the persisted solo game (11-02 fix confirmed)
    - Any temporary cfg.storm force is reverted: `determinism_baseline.js --verify` reports 30/30 with SOURCE: unchanged and `git diff HEAD -- src/engine/index.js` is empty
    - VERIFY-02 recorded satisfied in Criterion-2
  </acceptance_criteria>
  <done>The full solo loop including storm and end-of-voyage is verified in Chrome with the engine left byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → local HTTP test server | Loopback-only serving of the worktree for the Chrome-MCP drive |
| temporary engine edit → determinism corpus | A forced-storm edit to src/engine/index.js would silently change the seeded output hash if not reverted |

Phase 12 introduces no new runtime code or endpoint; the only integrity risk here is a stray unreverted test edit.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information disclosure | local HTTP test server | low | mitigate | Bind to 127.0.0.1; ephemeral; stopped after testing |
| T-12-03 | Tampering (determinism integrity) | src/engine/index.js forced-storm edit | medium | mitigate | Revert any cfg.storm force; the automated verify re-runs `determinism_baseline.js --verify` (SOURCE: unchanged) and asserts `git diff HEAD -- src/engine/index.js` is empty |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase; browser-test-framework out of scope (D-01) |
</threat_model>

<verification>
- Chrome-MCP solo drive covers sail, dock, trade, fish, battle, coin-flip, storm, end-of-voyage with zero console errors.
- `node scripts/determinism_baseline.js --verify` reports 30/30, SOURCE: unchanged (any storm-force reverted).
- `git diff --quiet HEAD -- src/engine/index.js` (engine untouched at plan end).
- Criterion-2 section fully recorded with per-mechanic PASS lines.
</verification>

<success_criteria>
Criterion 2 (VERIFY-02) is verified: the full solo gameplay loop runs cleanly in Chrome, the storm and end-of-voyage surfaces render without regression, solo state persists, and the engine is left byte-identical.
</success_criteria>

<output>
Create `.planning/phases/12-verification-validation/12-02-SUMMARY.md` when done.
</output>
