---
phase: 12-verification-validation
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - docs/VERIFICATION-CHECKLIST.md
autonomous: false
requirements: [VERIFY-03]
user_setup: []

must_haves:
  truths:
    - "A two-tab host+guest game syncs deterministically in Chrome — matching captain-state, narration broadcast, populated watcher registry (VERIFY-03)"
    - "Pausing the multiplayer shot-clock leaves game state intact — no reset (the v1.0 core guarantee, D-02a)"
    - "Refreshing the guest tab mid-game restores the voyage to the same turn/board (D-02b)"
    - "Refreshing the host tab mid-game restores AND deterministic lockstep/sync survives the cycle (D-02c)"
  artifacts:
    - "docs/VERIFICATION-CHECKLIST.md (Criterion-3 section recorded)"
  key_links:
    - "Unique pp_id per tab, set sequentially with set-then-reload — the shared-localStorage gotcha"
    - "Recovery seams: setRecoveryState / netWatchRecovery / wireRestoreFail / endReplay in src/orchestrator.js + netWatchRecovery in src/net/watchers.js; room recovery via localStorage"
---

<objective>
Verify a full two-tab multiplayer game end-to-end in Chrome via the browser-MCP (Criterion 3 / VERIFY-03), and drive the complete pause/refresh recovery matrix (D-02). Phase 11 already exercised host/join, seat+status propagation, and narration broadcast live; this plan formalizes that AND closes the recovery gap that targets the v1.0 core guarantee "pausing the multiplayer timer must never destroy game state," plus refresh-restores-the-voyage for both guest and host.

Per D-01/D-02 this is orchestrator-driven Chrome-MCP verification against two tabs on this Mac, recorded into the committed checklist (no browser-test framework; two-tab same-machine is the accepted surface — a real two-device game is deferred). Because it requires a live browser drive plus judgment, this plan is autonomous:false.

Purpose: Prove deterministic multiplayer sync and the pause/refresh recovery guarantee survived the refactor.
Output: docs/VERIFICATION-CHECKLIST.md Criterion-3 section filled with PASS evidence for sync and all three recovery sub-steps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-verification-validation/12-CONTEXT.md
@docs/VERIFICATION-CHECKLIST.md
@src/main.js
@src/net/index.js
@src/net/watchers.js
@src/orchestrator.js
</context>

<artifacts_this_phase_produces>
This plan appends to `docs/VERIFICATION-CHECKLIST.md` (Criterion-3 section). No new files or source symbols are created.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Two-tab host+guest game with deterministic sync (VERIFY-03)</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - docs/VERIFICATION-CHECKLIST.md (the Criterion-3 skeleton and local-serve preamble)
    - src/main.js (window.__pp_net_debug = { size, list, detachRoom, detachAll })
    - src/net/index.js (the exported net surface and databaseURL)
    - src/net/watchers.js (the 18 registry-mediated watchers)
    - src/orchestrator.js (watchRoom — idempotency guard _watchRoomAttachedFor; createRoom/joinRoom/beginGame)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (the two-tab pp_id gotcha; v1.1 lobby card buttons; __pp_net_debug usage)
  </read_first>
  <action>
    Serve the branch locally (127.0.0.1, confirm cwd). Open two Chrome tabs. Before joining, set a UNIQUE `pp_id` in each tab sequentially using set-then-reload — because both tabs share localStorage, set tab A's pp_id and reload, then set tab B's pp_id and reload, so they do not collide (the shared-localStorage gotcha). In tab A, use the v1.1 lobby card button "Host a Crew" to host; in tab B use "Join a Crew" to join with the room code. Begin the game and play several synced turns. Assert deterministic lockstep: seat and status propagate across both tabs, narration broadcasts appear in both, and the captains' state matches at the same moment across tabs (use `window.__pp_app_state_debug()` in each tab to compare). Confirm the watcher registry is populated via `window.__pp_net_debug.size()` (> 0) and `window.__pp_net_debug.list()`. Keep `read_console_messages` (onlyErrors) empty in both tabs. Record the sync evidence into Criterion-3. Do not embed code fences.
  </action>
  <verify>
    <automated>grep -qi 'sync' docs/VERIFICATION-CHECKLIST.md && npm test</automated>
    <human-check>Two Chrome tabs (unique pp_id each) host+join, play >=3 synced turns with matching captain-state, narration broadcasts in both, and window.__pp_net_debug.size() > 0; zero console errors in both tabs; recorded in Criterion-3.</human-check>
  </verify>
  <acceptance_criteria>
    - Host (tab A) and guest (tab B) both join the same room with distinct pp_id values
    - At least 3 synced turns play with matching captain-state across tabs (deterministic lockstep)
    - Seat/status propagation and narration broadcast are observed in both tabs
    - window.__pp_net_debug.size() > 0 (watcher registry populated)
    - Zero console errors in both tabs; sync evidence recorded in Criterion-3
  </acceptance_criteria>
  <done>Two-tab deterministic multiplayer sync is verified in Chrome and recorded (VERIFY-03 sync half).</done>
</task>

<task type="auto">
  <name>Task 2: Pause/refresh recovery matrix — shot-clock pause, guest refresh, host refresh (VERIFY-03, D-02)</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - docs/VERIFICATION-CHECKLIST.md (Criterion-3 section)
    - src/orchestrator.js (setRecoveryState ~line 724; netWatchRecovery wiring ~line 732; wireRestoreFail ~line 1006; endReplay; broadcastClock ~140; expireShotClock ~177)
    - src/net/watchers.js (netWatchRecovery ~line 84)
    - src/ui/panel.js (setClockUI — the shot-clock UI surface)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-02 recovery matrix; the recovery seams and localStorage room recovery)
  </read_first>
  <action>
    With the two-tab game from Task 1 live, drive the three recovery sub-steps of D-02 and record each: (a) PAUSE the multiplayer shot-clock (the setClockUI / broadcastClock / expireShotClock timer surface) and confirm the game state stays fully intact with no reset — this is the v1.0 core guarantee "pausing the multiplayer timer must never destroy game state" (D-02a); (b) REFRESH the GUEST tab mid-game and confirm the voyage restores to the same turn and board via the recovery watchers (netWatchRecovery / setRecoveryState) and localStorage room recovery (D-02b); (c) REFRESH the HOST tab mid-game and confirm it restores AND deterministic lockstep survives the cycle — after the host reloads and replay settles (endReplay / wireRestoreFail), the host's captain-state still matches the guest's (D-02c). Keep console errors empty across the cycle. Record pause-holds PASS, guest-refresh-restores PASS, and host-refresh-restores-and-syncs PASS into Criterion-3, and mark VERIFY-03 satisfied. Do not embed code fences.
  </action>
  <verify>
    <automated>grep -qi 'pause' docs/VERIFICATION-CHECKLIST.md && grep -qi 'refresh' docs/VERIFICATION-CHECKLIST.md && npm test</automated>
    <human-check>Pausing the shot-clock leaves state intact (no reset); refreshing the guest tab restores the same turn/board; refreshing the host tab restores and the post-refresh captain-state still matches the guest; all three recorded in Criterion-3.</human-check>
  </verify>
  <acceptance_criteria>
    - Pausing the shot-clock leaves game state intact — no reset (D-02a)
    - Refreshing the guest tab restores the voyage to the same turn and board (D-02b)
    - Refreshing the host tab restores the voyage AND deterministic sync survives — post-refresh host captain-state matches the guest (D-02c)
    - Zero console errors across the recovery cycle; all three sub-steps recorded and VERIFY-03 marked satisfied
  </acceptance_criteria>
  <done>The D-02 pause/refresh recovery matrix holds end-to-end and is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → local HTTP test server | Loopback-only serving of the worktree for the two-tab Chrome-MCP drive |
| browser tabs → Firebase Realtime DB | Existing multiplayer sync path (unchanged this phase) — exercised, not modified |

Phase 12 adds no new networking code, watcher, or endpoint — the recovery seams under test already exist in src/orchestrator.js and src/net/.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information disclosure | local HTTP test server | low | mitigate | Bind to 127.0.0.1; ephemeral; stopped after testing |
| T-12-04 | Tampering (test-data integrity) | shared-localStorage pp_id collision | low | mitigate | Set a unique pp_id per tab sequentially (set-then-reload) so the two tabs are distinct players; verification integrity depends on it |
| T-12-05 | Denial of service | Firebase RTDB test room | low | accept | Uses the existing public Firebase config (intended public exposure per project docs); a throwaway room, torn down after testing; no new surface introduced |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase; browser-test-framework out of scope (D-01) |
</threat_model>

<verification>
- Chrome-MCP two-tab drive shows deterministic host+guest sync with matching captain-state and a populated watcher registry.
- Pause holds state; guest refresh restores; host refresh restores and stays in sync — all three recorded.
- `npm test` remains green (determinism/regression unaffected).
- Criterion-3 section fully recorded with sync + three recovery PASS lines.
</verification>

<success_criteria>
Criterion 3 (VERIFY-03) is verified: two-tab multiplayer syncs deterministically in Chrome and the full D-02 pause/refresh recovery matrix holds — the v1.0 timer-pause guarantee and refresh-restores-the-voyage both survive the refactor.
</success_criteria>

<output>
Create `.planning/phases/12-verification-validation/12-03-SUMMARY.md` when done.
</output>
