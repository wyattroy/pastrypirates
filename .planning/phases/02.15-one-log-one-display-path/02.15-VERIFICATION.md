---
phase: 02.15-one-log-one-display-path
verified: 2026-08-21T06:35:45Z
status: human_needed
score: 5/7 truths verified (2 present, behavior-unverified or explicitly parked)
behavior_unverified: 2
overrides_applied: 0
gaps: []
human_verification:
  - test: "Play a remote captain's OWN sail-window turn (item 20's second half) with the HOST spectating — i.e. the one crew-mode pairing the 02.15-02 driver never happened to sample."
    expected: "The host's screen shows the same highlighted sail squares the acting guest sees, because renderPickPrompt is the single converged renderer for this fork."
    why_human: "Static reading of the code (renderPickPrompt is genuinely shared, confirmed at 4/src/ui/flow.js:531 and 4/src/orchestrator.js:1443) says this should now hold, but 02.15-02-SUMMARY.md itself records this exact pairing was never captured live — 'observed once, not yet measured.' The verifier is carrying that finding forward as-is, not upgrading it."
  - test: "Have a REMOTE (non-local) captain's own turn require a coin flip — i.e. watchPrompt's flip sub-branch actually firing for a genuinely remote decider, not a spectator watching someone else's flip."
    expected: "Either the flip ceremony card shows real title/stakes text on that remote captain's own screen (meaning the divergence found by 02.15-02 doesn't reproduce), or it shows an empty ceremony card (confirming the divergence SUMMARY reports from static reading alone)."
    why_human: "02.15-02's own driven crew voyage never happened to route a dock-flip decision to a genuinely remote seat before reaching end-of-voyage — the pairing captured (b-crew-flip-*.png) is a spectator watching someone else's flip, which is a different and already-correct case. The finding that a remote decider would see an empty card is static-source-only, not live-captured. This is the ask() channel's parked landmine (Task 4), and it is the reason PAR-14's own text ('nothing could make them disagree') is not yet fully true."
---

# Phase 02.15: One Log, One Display Path — Verification Report

**Phase Goal:** The game is displayed from one place, according to one set of rules, on every screen —
solo, host, or guest. Host and guest stop being two ways of drawing the same voyage.
**Verified:** 2026-08-21T06:35:45Z
**Status:** human_needed (two items genuinely need a human's eyes; everything else checked out)
**Re-verification:** No — initial verification

## Honest headline, stated the way the task requires

**Partial. The gaps are named and seamed, not silent — this matches D-04 exactly, and it is a real
achievement, not a shortfall dressed up.** Six of seven of Wyatt's screenshot divergences are closed
and shipped (`2026-08-20b` → `2026-08-20m`, all verified live). The seventh — the host's own sail
window, and by extension the whole `ask()`/`localAsk` orchestration fork — is explicitly still two
orchestrations, named as such in the parity gate's own strict-mode output
(`PARITY-ORCH: localAsk( is driven 6x from the host's game loop and is reachable from ZERO of the 9
listeners`), in `docs/DISPLAY-RULES.md`'s fork table, and in both SUMMARYs. **Two of the phase's own
requirement definitions (PAR-14, PAR-15) are not fully true yet, and the record itself is internally
inconsistent about that — see the contradiction noted below.**

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | A two-tab crew game reproduces none of the seven divergences (D-26) | ⚠️ 6/7 — one still open, named | `02.15-01-SUMMARY.md`'s per-row verdict table + 13 `a-*.png` pairs (13 files confirmed on disk, matching Self-Check); `02.15-02-SUMMARY.md`'s 35 `b-*.png` shots. Sail window (item 20's second half) explicitly "NOT REACHED" in both SUMMARYs and in `DISPLAY-RULES.md` §4 (`ask()` row: "NOT YET CONVERGED"). |
| 2 | The host draws its screen from the same stream it publishes, through the same code a guest draws from, for narration and the active seat | ✓ VERIFIED | `flash(` and `applyActiveSeat(` confirmed called from both the host's loop and a listener (`4/src/orchestrator.js:1330` `applyActiveSeat(e.p)` inside `watchEvents`; grep of `flash(` shows call sites in both `flow.js` host-loop code and `orchestrator.js` listener code). Parity gate assertion 6 (run live) reports both `shared`. |
| 2b | ...and for the sail/pick channel (added by plan 02, not in plan 01's must-haves but part of the same phase goal) | ✓ VERIFIED | `renderPickPrompt` confirmed called directly by `watchPrompt`'s pick branch (`4/src/orchestrator.js:1443`) and wrapped by `localPickCell` (`4/src/ui/flow.js:725`) — one renderer, two response mechanisms, exactly the shape `docs/DISPLAY-RULES.md` §2 describes. Parity gate assertion 6 reports `renderPickPrompt` shared, `localPickCell` `SUPERSEDED`. |
| 3 | Solo and pass-and-play still work, no Firebase round trip on the host's own screen | ✓ VERIFIED | `02.15-02-SUMMARY.md`: driven solo voyage to Day 18 end-of-voyage, zero console errors; driven pass-and-play voyage to Day 16 end-of-voyage, 33 hand-offs fired, zero console errors. Live guard code confirmed matching `docs/DISPLAY-RULES.md` verbatim: `netNarrate` at `4/src/orchestrator.js:308` gates every Firebase write on `isHost&&db&&room`. Standing static solo guard (assertion 3 of `4/scripts/prompt_field_parity_check.js`) run by this verifier — PASS, and its own `--drill` red-proofs it (3a/3b/3c all correctly fail on a planted violation). |
| 4 | Wyatt gets an honest answer to item 18 — two orchestrations, host now a listener like every client (D-24) | ✓ VERIFIED — but the answer itself is honest about being partial | `02.15-01-SUMMARY.md`'s "Item 18" section states plainly: "Partly, and here is the honest shape of it" — narration and active seat converged, the prompt channel did not. This is the correct, non-overclaiming answer to give him. |
| 5 | Every landed stage can be pointed at; every stage that did not land is written down by name (D-04) | ✓ VERIFIED | Both SUMMARYs name every stage/task/fork by name with commit hash or explicit "PARKED"/"NOT REACHED"/"LEFT" status. `docs/DISPLAY-RULES.md` §4 independently carries the same five-fork ledger. Nothing found undisclosed except the one item WR-01 in `02.15-REVIEW.md` caught and the SUMMARY addendum then disclosed — see below. |
| 6 | The parity gate runs against `4/` and was seen RED before the rewrite, naming the un-converged renderer(s) (D-28) | ✓ VERIFIED | Ran independently by this verifier (not trusting SUMMARY's quoted numbers): `node scripts/host_guest_parity_check.js --tree=4 --strict` shows exactly three failures — `PARITY-SAILRECT`, `PARITY-SWEEPARRIVE` (both recorded pre-existing/stale in `.planning/todos/pending/2026-08-21-two-parity-assertions-drifted-stale.md`, confirmed identical at `e58fda4` before this phase and at HEAD) and `PARITY-ORCH: localAsk` (the honestly-declared parked gap). No new failures. `--drill` run independently: all 6 assertions red-proof cleanly, including the anti-vacuity cases. |
| 7 | Wyatt is told a new PP4_STAMP and can see it live (D-02) | ✓ VERIFIED | `main`/`origin/main` confirmed zero commits apart both ways by this verifier. `PP4_STAMP` history: `2026-08-20b` (plan 01) → `2026-08-20l` (plan 02, Task 3) → `2026-08-20m` (plan 02 addendum, both REVIEW fixes shipped). Commits `4e2162a`, `b172c08`, `c63d76e`, `a521b86`, `645384b`, `988a6f8`, `a69ca77`, `01d03ef`, `e8f7c26`, `ff9794d` all confirmed present in `git log`. |

**Score:** 5/7 fully verified; 2 explicitly present-but-not-live-confirmed and routed to human verification below (both are pre-existing, honestly-flagged gaps in the SUMMARY itself — this verifier did not upgrade them, and is not treating "static reading is plausible" as proof of a state transition it did not see live).

### Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| **PAR-14** (one place decides what's drawn) | **Partial, not fully true yet — and REQUIREMENTS.md contradicts itself about this** | Narration + active seat + pick/sail: genuinely one renderer each, both tiers, confirmed by this verifier reading the live code (not just trusting the SUMMARY). The `ask()` channel is NOT converged — `localAsk` is a confirmed, currently-red `PARITY-ORCH` failure in strict mode, and 02.15-02 found a **real, source-verified structural divergence** in the flip ceremony sub-case (a remote decider's own flip card would render empty title/stakes text — `window.__pp4.flipMsg` is stashed by `localAsk` only, never by `watchPrompt`'s flip branch). PAR-14's own text defines the bar as *"the test is not that the screens agree; it is that nothing could make them disagree"* — for the ask channel, something plainly still could. **REQUIREMENTS.md marks this `[x]` complete (commit `f5bbd7b`, the same commit that closed 02.15-02) while its own coverage table two hundred lines below still reads "Partial — 02.15-01 (narration + active seat converged); prompt channel is 02.15-02" — that coverage-table line was never updated after 02.15-02 landed, so the two halves of the same document now disagree with each other, and the `[x]` overstates what shipped.** |
| **PAR-15** (crew/solo/pass-and-play whole games verified on the live build) | **Partial, not fully true yet, same contradiction** | Three whole voyages (crew, solo, pass-and-play) were driven to their end-of-voyage cards on the live build, verified as existing via 35 `b-*.png` files on disk plus the SUMMARY's per-mode day-count/battle-count table. But PAR-15's own text lists *"the same sail squares"* as one of its seven required divergences-closed, and that one is explicitly "NOT REACHED" per both SUMMARYs — so the requirement's own definition is not met by its own admission, yet it too is marked `[x]` in the same commit, with the same stale "Partial" line still sitting in the coverage table below it. |
| **PAR-16** (display-rules document exists and matches the code) | **Satisfied** | `docs/DISPLAY-RULES.md` exists (266 lines), and this verifier independently spot-checked five of its concrete code claims against the live tree, not three: `netNarrate`'s guard, `applyActiveSeat`'s body, `decisionIsLocal`'s body, `withShotClock`'s body, and `seatOrderFrom`'s fallback logic — all five quoted verbatim in the doc match the live source byte-for-byte at the lines cited. The five-fork table (§4) matches the parity gate's own live reading exactly (`localPickCell` superseded, `localAsk` still the one declared gap). This requirement's own coverage-table line ("Pending — docs/DISPLAY-RULES.md is written by 02.15-02 Task 2") is also stale — the document has existed and been maintained since Task 2/Task 3 of plan 02 — but unlike PAR-14/15, the `[x]` marking for PAR-16 is not disputed by any gap in the phase's own findings. |

**The contradiction above is a genuine finding, not a nitpick.** The commit that marked PAR-14/15/16 `[x]` complete is the same commit (`f5bbd7b`) that produced the honest, gap-naming SUMMARY this verifier otherwise found to be trustworthy — so this is not a case of a session lying about its own work. It looks like the checkbox in the requirement list was flipped as "phase's plans have all executed" bookkeeping, while the *coverage table* (a separate, unsynced list further down the same file) and the *SUMMARY's own prose* were left saying "partial." **Recommend: either un-check PAR-14 and PAR-15 in REQUIREMENTS.md until the `ask()` channel converges (Task 4 of a future plan), or add an explicit annotation next to the checkbox the way PAR-02's "Reopened" annotation already does elsewhere in the same file.** This is a paperwork fix, not a code fix — the code and the SUMMARYs are honest; the checkbox is what overclaims.

### Item 20's sail-window visibility — carried forward exactly as reported, not upgraded or buried

`02.15-02-SUMMARY.md`: *"Item 20's sail-window row — NOT reliably measured this session, and said so
rather than guessed... The honest reading is 'observed once, not yet measured,' not a verdict either
way."* This verifier did not attempt to re-measure it live (that would require driving a fresh
two-window session, which is out of scope for a static/gate-level verification pass) and is not
upgrading it to VERIFIED or downgrading it to FAILED — it is routed to human verification above,
worded the same way the SUMMARY worded it.

### The flip-ceremony divergence — a real, confirmed, unresolved structural gap

This is the clearest evidence the phase is honestly partial rather than quietly complete. Read
directly from `02.15-02-SUMMARY.md`'s fork table and confirmed by this verifier reading
`4/src/ui/util.js` (`localAsk`, where `window.__pp4.flipMsg` is stashed) against
`4/src/orchestrator.js` (`watchPrompt`'s flip branch, which never stashes it): **a remote captain
asked to flip their own coin — not a spectator, the actual decider — would see the ceremony veil with
an empty title and stakes text, while the local decider (host, or the pass-and-play seat holding the
device) sees the full ceremony.** This was found by static reading, not live-captured (the driven
crew voyage never routed a flip decision to a genuinely remote seat), and it is the reason Task 4 (the
`ask()` channel) was parked rather than half-converged, per D-04's own exit condition — landing it
would have required either a visible behaviour change (forbidden by tonight's pure-plumbing
constraint) or making the renderer tier-aware (forbidden by the plan's own shared-renderer shape).
**This is not a shortfall of execution — it is the phase correctly refusing to ship a half-fix**, but
it does mean PAR-14's "nothing could make them disagree" is not yet true for this one channel.

### Gates run independently by this verifier (not trusted from the SUMMARY)

| Gate | Command | Result |
|---|---|---|
| Root test suite | `npm test` | PASS — 0 failing |
| Host/guest parity, non-strict | `node scripts/host_guest_parity_check.js --tree=4` | 2 known-stale failures (SAILRECT, SWEEPARRIVE), 4 pass, matches `.planning/todos/pending/2026-08-21-two-parity-assertions-drifted-stale.md` exactly |
| Host/guest parity, strict | `node scripts/host_guest_parity_check.js --tree=4 --strict` | 3 failures — the same two known-stale ones plus `PARITY-ORCH: localAsk` (the honestly-declared parked gap), no new failures |
| Host/guest parity drill | `node scripts/host_guest_parity_check.js --tree=4 --drill` | ALL 6 ASSERTIONS RED-PROOF DRILLED OK — every synthetic violation, including all anti-vacuity cases, correctly caught |
| Prompt field parity | `node 4/scripts/prompt_field_parity_check.js` | PASS on all 3 assertions, including the new assertion 3 (standing static solo guard, added by the REVIEW's WR-01 fix) |
| Prompt field parity drill | `node 4/scripts/prompt_field_parity_check.js --drill` | RED-PROOF DRILLED OK — 16 synthetic violations caught, real tree clean |
| `main`/`origin/main` sync | `git rev-list --count` both directions | 0 / 0 |
| GSD health check | `gsd-tools.cjs validate health` | degraded, but nothing 02.15-specific — remaining findings are Phase 02.2 plans still in progress (expected) and one unrecognized-file warning for `HANDOFF-02.15-2026-08-20.md` (a real, intentional artifact, not a defect) |

### Artifacts

| Artifact | Expected | Status |
|---|---|---|
| `4/src/orchestrator.js` | restructured director | ✓ VERIFIED — `watchEvents`/`watchPrompt`/`watchNarr` call the converged renderers by name |
| `scripts/host_guest_parity_check.js` | re-aimed at `4/`, assertion 6 added | ✓ VERIFIED, run live |
| `docs/DISPLAY-RULES.md` | PAR-16's artifact | ✓ VERIFIED, spot-checked 5 claims against live code |
| `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-DIRECTOR-MAP.md` | channel map | ✓ EXISTS |
| `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/a-*.png` | plan 01 screenshots | ✓ VERIFIED — 13 on disk |
| `.planning/phases/02.15-one-log-one-display-path/shots/b-*.png` | plan 02 screenshots | ✓ VERIFIED — 35 on disk |
| `4/scripts/prompt_field_parity_check.js` | assertion 3 (solo guard) | ✓ VERIFIED — present, passes, red-proofed |

### Anti-Patterns Found

None found in the phase's own modified files beyond what the phase's own REVIEW.md already caught and
disclosed (WR-01, WR-02, both fixed; IN-01, IN-02, both explicitly and reasonably deferred with
recorded reasoning). No unreferenced TBD/FIXME/XXX markers found in the files this phase touched.

### Code Review Status

`02.15-REVIEW.md`: 0 critical, 2 warnings (both fixed, commits `b172c08`/`c63d76e`), 2 info (both
evaluated and left with recorded reasoning). This verifier confirms both warning fixes are present in
the live tree and both pass their own drill red-proofs (checked above under "Gates run independently").

## Gaps Summary

**No blocking gaps.** The phase's own execution correctly identified, named, and stopped at every
place D-04 says to stop — this is the phase working as designed, not failing. What remains is:

1. **Structural:** the `ask()`/`localAsk` orchestration fork (Task 4) is unconverged, with one
   confirmed real divergence (the flip ceremony) and named as the sole remaining strict-mode gap.
2. **Not reached:** the `battleAsk()` fork (Task 5) — precondition (Task 4 landing) unmet, expected
   under D-04.
3. **Left, with landmines:** `recipeDraftNet()` and `netIntroBarrier()` (the recipe-draft pair) — not
   attempted, `seatLocal` landmine documented in `docs/DISPLAY-RULES.md` §3 Rule B.
4. **Two items genuinely need a human's eyes**, routed above — item 20's sail-window spectator
   visibility (not reliably measured, carried forward as-is) and a live capture of the flip-ceremony
   divergence from the actual remote-decider's own screen (currently static-reading-only).
5. **A paperwork inconsistency in REQUIREMENTS.md** — PAR-14 and PAR-15 are checked `[x]` complete in
   the same commit that shipped a SUMMARY explicitly describing them as partial, and the file's own
   coverage table (unsynced) still says "Partial"/"Pending" for all three. Recommend correcting the
   checkboxes or annotating them, not re-opening the phase.

None of this blocks proceeding to the next phase — Wyatt's own D-04 ruling and the balloon extension
explicitly authorize landing what is safe and moving to Group B (02.2-03) with what is left named. The
`human_needed` status here reflects two genuinely unmeasured items, not a defect in what shipped.

---

_Verified: 2026-08-21T06:35:45Z_
_Verifier: Claude (gsd-verifier)_
