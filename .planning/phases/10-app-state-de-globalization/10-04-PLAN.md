---
phase: 10-app-state-de-globalization
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - index.html
autonomous: true
requirements: [GLOBAL-01, GLOBAL-02]
user_setup: []

must_haves:
  truths:
    - "The 13 shot-clock/timer-control names (shotClockSeat, shotClockDeadline, shotClockTimer, shotClockForce, shotClockStash, shotClockPaused, shotClockPauseElapsed, timerOff, shotClockFired, turnExpired, clockState, activePickCleanup, curSeat) are migrated to `state.NAME` at every read AND write site."
    - "The 13 live/prompt/turn bookkeeping names (live, liveDone, liveGen, inBattlePrompt, spectatingBattle, activeTurnSeat, recipeRevealed, gameStarted, evPushed, appliedMeta, promptCounter, syncBoardRAF, lastChatSendAt) are migrated to `state.NAME` at every read AND write site."
    - "`revealMyRecipe` stays a top-level `function` declaration (its body's `recipeRevealed=true` becomes a `state.`-qualified write); it is NOT converted to a const/arrow, so it remains an own property of the global object reachable by the inline `onclick` attribute (D-01/D-02/GLOBAL-02)."
    - "The determinism corpus stays 30/30 on `--verify` after both tasks (D-08)."
  prohibitions:
    - "REVEALMYRECIPE-BREAK: do NOT convert the `function revealMyRecipe(){...}` declaration to a const arrow - a const does NOT become a global-object property and the inline handler would break (RESEARCH Q1d)."
    - "MIGRATION-CORRUPTION: no string-literal or comment body changes (byte-safety diff empty)."
    - "MISSED-WRITE-SITE: zero bare (non-`state.`) occurrences of these 26 names remain in the region; a missed shotClockDeadline/shotClockTimer write freezes or double-fires the shot clock."
    - "NEVER-CAPTURE: no `--capture`."
  artifacts: []
  key_links:
    - "The inline `onclick` attribute (index.html template ~:1731) -> the `function revealMyRecipe` declaration -> global-object property; the function keyword is load-bearing for reachability."
    - "shotClockTimer is a setInterval handle: `clearInterval(state.shotClockTimer)` must be correct at every teardown site or a stale interval keeps ticking."
---

<objective>
Migrate the widest but individually smallest name-set: the shot-clock/timer-control cluster and the live/prompt/turn bookkeeping cluster. Split into two tasks so each diff stays reviewable. This plan also migrates the ONE body reference inside `revealMyRecipe` (`recipeRevealed`), and confirms the function-declaration form is preserved so GLOBAL-02's single inline attribute keeps working.

Purpose: These 26 names are mostly single-digit-occurrence bookkeeping fields, but two carry specific hazards: `shotClockTimer` is a live `setInterval` handle whose `clearInterval` sites must migrate correctly, and `recipeRevealed` is the body of the one globally-resolved inline handler. Both are verified explicitly here.

Output: all 26 names `state.`-qualified, `revealMyRecipe` still a reachable `function`, corpus green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-app-state-de-globalization/10-RESEARCH.md
@.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the shot-clock / timer-control cluster</name>
  <files>index.html</files>
  <read_first>
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` inventory rows 23, 26-37 (shot-clock cluster). Re-grep write sites; the shot-clock teardown sites cluster around index.html 2128/2138/2157/2177/2247 (`clearInterval(shotClockTimer)`; confirm by grep).
    - `.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md` - migration-tool + byte-safety procedure.
  </read_first>
  <action>
    Run `node scripts/migrate_app_state.js --migrate shotClockSeat,shotClockDeadline,shotClockTimer,shotClockForce,shotClockStash,shotClockPaused,shotClockPauseElapsed,timerOff,shotClockFired,turnExpired,clockState,activePickCleanup,curSeat`.
    Review the diff. Pay specific attention to the `shotClockTimer` interval handle: confirm every `clearInterval(...)` and `setInterval(...)` site that referenced the bare handle now references the `state.`-qualified handle, so a paused/reset clock genuinely clears the right interval and no stale interval survives. Confirm `setActor(s){curSeat=s;}` migrated its assignment.
    Do not touch the other names or the 7 render handles. NEVER run `--capture`.
  </action>
  <verify>
    <automated>node scripts/migrate_app_state.js --check-names shotClockSeat,shotClockDeadline,shotClockTimer,shotClockForce,shotClockStash,shotClockPaused,shotClockPauseElapsed,timerOff,shotClockFired,turnExpired,clockState,activePickCleanup,curSeat && diff <(node scripts/migrate_app_state.js --extract-strings HEAD:index.html) <(node scripts/migrate_app_state.js --extract-strings index.html) && node scripts/determinism_baseline.js --verify</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/migrate_app_state.js --check-names shotClockSeat,shotClockDeadline,shotClockTimer,shotClockForce,shotClockStash,shotClockPaused,shotClockPauseElapsed,timerOff,shotClockFired,turnExpired,clockState,activePickCleanup,curSeat` exits 0.
    - The `--extract-strings` diff (HEAD vs working tree) is EMPTY.
    - `node scripts/determinism_baseline.js --verify` exits 0 (30/30).
    - `grep -cE "clearInterval\(shotClockTimer\)|setInterval\(shotClockTick" index.html` returns 0 (no bare shotClockTimer handle remains at a timer site).
    - `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1.
  </acceptance_criteria>
  <done>The shot-clock/timer-control cluster is fully `state.`-qualified with interval-handle teardown correct; corpus green; strings/comments byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the live / prompt / turn bookkeeping cluster + preserve revealMyRecipe reachability</name>
  <files>index.html</files>
  <read_first>
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` "The revealMyRecipe minimal-preservation rule (Q1d)" (the function declaration at ~:4309: `function revealMyRecipe(){recipeRevealed=true;liveRender();}`; only the body reference migrates, the function keyword stays), inventory rows 13-22, 24-25, 45-46.
    - `index.html` around the `revealMyRecipe` declaration and the inline `onclick` template (~:1731) - confirm both by grep before and after.
  </read_first>
  <action>
    Run `node scripts/migrate_app_state.js --migrate live,liveDone,liveGen,inBattlePrompt,spectatingBattle,activeTurnSeat,recipeRevealed,gameStarted,evPushed,appliedMeta,promptCounter,syncBoardRAF,lastChatSendAt`.
    Review the diff. Confirm the `revealMyRecipe` body's assignment migrated to a `state.`-qualified write while the enclosing declaration is still the `function` form (not rewritten to a const arrow). Confirm the inline `onclick` template string that calls `revealMyRecipe()` is unchanged (it is UI copy inside a template literal - the tokenizer must leave it byte-identical).
    Do not touch the other names or the 7 render handles. NEVER run `--capture`.
  </action>
  <verify>
    <automated>node scripts/migrate_app_state.js --check-names live,liveDone,liveGen,inBattlePrompt,spectatingBattle,activeTurnSeat,recipeRevealed,gameStarted,evPushed,appliedMeta,promptCounter,syncBoardRAF,lastChatSendAt && diff <(node scripts/migrate_app_state.js --extract-strings HEAD:index.html) <(node scripts/migrate_app_state.js --extract-strings index.html) && node scripts/determinism_baseline.js --verify && grep -cE "^\s*function revealMyRecipe\(" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/migrate_app_state.js --check-names live,liveDone,liveGen,inBattlePrompt,spectatingBattle,activeTurnSeat,recipeRevealed,gameStarted,evPushed,appliedMeta,promptCounter,syncBoardRAF,lastChatSendAt` exits 0.
    - `grep -cE "^\s*function revealMyRecipe\(" index.html` returns 1 (still a function declaration - NOT a const arrow).
    - `grep -c "onclick=\"revealMyRecipe()\"" index.html` returns 1 (the inline attribute template string is byte-identical).
    - The `--extract-strings` diff (HEAD vs working tree) is EMPTY.
    - `node scripts/determinism_baseline.js --verify` exits 0 (30/30).
    - `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1.
  </acceptance_criteria>
  <done>The live/prompt/turn bookkeeping cluster is fully `state.`-qualified; `revealMyRecipe` remains a reachable `function` declaration with its inline attribute intact; corpus green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokenizer -> index.html | Rewriting shot-clock control and the one globally-resolved inline handler's body. |
| inline onclick attr -> global scope | `revealMyRecipe` must stay a global-object property for the inline attribute to resolve it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | revealMyRecipe converted to a const arrow, dropping its global-object reachability | high | mitigate | Grep asserts the `function` form survives (count 1) and the inline attribute string is byte-identical (count 1). |
| T-10-07 | Tampering | a missed shotClockTimer/shotClockDeadline write freezing or double-firing the shot clock | medium | mitigate | `--check-names` exit 0; grep asserts no bare shotClockTimer at a timer site; diff review. |
| T-10-01 | Tampering | inline handler template string or a comment corrupted | high | mitigate | Tokenizer skips strings/comments; `--extract-strings` diff empty. |
</threat_model>

<verification>
- `--check-names` for both clusters exits 0.
- `--extract-strings` diff empty (byte-safety) after each task.
- `revealMyRecipe` is still a `function` declaration (count 1); inline attribute intact (count 1).
- `node scripts/determinism_baseline.js --verify` -> 30/30 after both tasks.
- Corpus still 1 commit deep.
</verification>

<success_criteria>
The shot-clock and live/prompt/turn bookkeeping state is de-globalized, the one inline handler stays reachable, corpus green, and no string/comment content changed.
</success_criteria>

<output>
Create `.planning/phases/10-app-state-de-globalization/10-04-SUMMARY.md` when done.
</output>
