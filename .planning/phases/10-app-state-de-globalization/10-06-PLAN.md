---
phase: 10-app-state-de-globalization
plan: 06
type: execute
wave: 6
depends_on: ["10-05"]
files_modified:
  - src/main.js
  - scripts/state_contract_check.js
  - package.json
  - docs/MODULES.md
autonomous: true
requirements: [GLOBAL-01, GLOBAL-03]
user_setup: []

must_haves:
  truths:
    - "`window.__pp_app_state_debug` exists in src/main.js as a READ-ONLY shallow snapshot (`{...state}` returned by an accessor/getter helper), NOT the live `state` reference - so a console/MCP session cannot mutate authoritative game state through it (GLOBAL-03 + security)."
    - "Exactly four named `window.__pp_*` debug hooks exist and are allowlisted by the contract check: __pp_module_ok, __pp_boot_count, __pp_net_debug, __pp_app_state_debug (D-09/GLOBAL-03)."
    - "`scripts/state_contract_check.js` is wired into the `package.json` test chain after net_contract_check.js and passes 5/5; the full `npm test` is green (GLOBAL-01/D-11)."
    - "docs/MODULES.md documents the `src/state/` module and a `Standing browser debug hooks` table listing all four hooks together (GLOBAL-03 documentation requirement)."
  prohibitions:
    - "LIVE-REFERENCE-LEAK: `window.__pp_app_state_debug` must not expose the live `state` object reference (a defense-in-depth read-only snapshot is the required shape)."
    - "HOOK-RENAME: do NOT rename or remove Phase 9's already-verified `window.__pp_net_debug` (regression risk for no gain - RESEARCH Don't Hand-Roll)."
    - "NO-ADHOC-HOOK: no new `window.__pp_*` name outside the four-name allowlist."
  artifacts:
    - "window.__pp_app_state_debug in src/main.js (read-only snapshot)"
    - "scripts/state_contract_check.js wired into package.json test chain"
    - "docs/MODULES.md: src/state/ section + Standing browser debug hooks table"
  key_links:
    - "src/main.js sets the four `window.__pp_*` hooks inside the `typeof window` guard; the contract check's assertion 3 allowlist is the standing enforcement."
---

<objective>
Land GLOBAL-03: add the single new app-state debug hook under the existing named-hook convention, finalize and wire the standing contract check into `npm test`, and document the `src/state/` module and all four debug hooks together in docs/MODULES.md. This closes the phase's tooling and documentation surface now that all 46 names are migrated.

Purpose: GLOBAL-03 asks for one documented mechanism for test/debug state access. D-09 chose to extend the existing `window.__pp_*` convention with one new named entry rather than break Phase 9's verified `__pp_net_debug`. The new hook is a read-only snapshot so console access cannot mutate authoritative state (there is no new network surface this phase; this is the one intentional read surface).

Output: `window.__pp_app_state_debug`, a green `npm test` including `state_contract_check.js`, and the docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-app-state-de-globalization/10-RESEARCH.md
@src/main.js
@docs/MODULES.md
@scripts/net_contract_check.js
@.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the read-only app-state debug hook, finalize + wire the contract check</name>
  <files>src/main.js, scripts/state_contract_check.js, package.json</files>
  <read_first>
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` Q5 / "Consolidating debug hooks" / Security Domain (the read-only-snapshot recommendation: `window.__pp_app_state_debug` returns `{...state}`, not the live reference), and Wave 0 Gaps assertion 3 (the four-name allowlist and the `window[MODULE_OK_FLAG]` indirection).
    - `src/main.js` - the existing `window.__pp_net_debug` block and the `window[MODULE_OK_FLAG]` / `window.__pp_boot_count` assignments (the pattern to mirror; do NOT rename these).
    - `package.json` `test` script (chained `&&`) - append the new check after net_contract_check.js.
  </read_first>
  <action>
    In src/main.js, inside the existing `typeof window !== "undefined"` guard and alongside `window.__pp_net_debug`, add `window.__pp_app_state_debug` exposing a read-only shallow snapshot of `state`: a small getter/helper that returns a fresh `{...stateNs.state}` on each access so a devtools/MCP session inspects a copy and cannot write back into authoritative state. Do NOT assign the live object reference. Do NOT tag it `PP-BRIDGE` (it outlives the Phase 11 bridge removal, like `__pp_net_debug`).
    In scripts/state_contract_check.js, finalize assertion 3 so it (a) scans src/main.js for every debug-hook assignment - both the direct `window.__pp_*=` form AND the indirect `window[MODULE_OK_FLAG]=` form where MODULE_OK_FLAG imports to the string "__pp_module_ok" - and (b) requires each to be one of exactly {__pp_module_ok, __pp_boot_count, __pp_net_debug, __pp_app_state_debug}, failing on any other name AND failing if any of the four is absent.
    Wire `node scripts/state_contract_check.js` into the `package.json` test chain immediately after `node scripts/net_contract_check.js`.
    Run the full `npm test`. All 46 names are migrated (10-05), so contract-check assertions 1 and 2 now pass; assertions 3/4/5 pass with the hook added and the state module pure.
  </action>
  <verify>
    <automated>node scripts/state_contract_check.js && npm test && grep -c "state_contract_check.js" package.json && grep -c "__pp_app_state_debug" src/main.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/state_contract_check.js` exits 0 and prints 5 PASS lines.
    - `npm test` exits 0 (determinism 30/30, engine_contract, dlog_replay, net_registry, net_contract, state_contract all green).
    - `grep -c "state_contract_check.js" package.json` returns 1 (wired into the test chain).
    - `grep -c "__pp_app_state_debug" src/main.js` returns 1.
    - `grep -Eo "window\.__pp_[A-Za-z_]+" src/main.js | sort -u` lists only `window.__pp_app_state_debug`, `window.__pp_boot_count`, `window.__pp_net_debug` (with `__pp_module_ok` set via the indirect `window[MODULE_OK_FLAG]` form) - exactly the four-name allowlist, no ad-hoc extras.
    - The snapshot is not the live reference: `grep -c "__pp_app_state_debug = state\b" src/main.js` returns 0 (it returns a `{...state}` copy, not the live object).
  </acceptance_criteria>
  <done>The read-only app-state debug hook exists, the contract check passes 5/5 and is wired into a green `npm test`, and exactly four named debug hooks are enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Document src/state/ and the four debug hooks in docs/MODULES.md</name>
  <files>docs/MODULES.md</files>
  <read_first>
    - `docs/MODULES.md` - the existing `src/net/` section (structure to mirror), the `window.__pp_net_debug` section, the `Standing browser tripwires` section, and the "What deliberately did not move into src/net/" note about the `db` handle staying classic (now de-globalized into `state.db`).
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` Wave 0 Gaps (the docs requirement).
  </read_first>
  <action>
    Add a `src/state/` section to docs/MODULES.md mirroring the `src/net/` section's structure: the single exported `state` object (never reassigned), why it is published by reference through the bridge, the purity bar (no DOM/Firebase inside the module), the tokenizer-based migration and the string-collision hazard it guards, and the Phase-11-greppable seam. Add a `Standing browser debug hooks` table listing all four hooks together (`__pp_module_ok`, `__pp_boot_count`, `__pp_net_debug`, `__pp_app_state_debug`) with a one-line purpose each, noting `__pp_app_state_debug` is a read-only snapshot. Update the "What deliberately did not move into src/net/" note where it says the `db` handle stays a classic-script global, to record that Phase 10 de-globalized it into `state.db`.
  </action>
  <verify>
    <automated>grep -c "src/state/" docs/MODULES.md && grep -Ec "__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug" docs/MODULES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/state/" docs/MODULES.md` returns at least 1 (the new section exists).
    - docs/MODULES.md contains a table naming all four `window.__pp_*` hooks: `grep -c "__pp_app_state_debug" docs/MODULES.md` >= 1 and all four names appear.
    - The docs note that `__pp_app_state_debug` is a read-only snapshot (not the live reference).
    - The "db handle" note reflects that `db` is now `state.db`.
  </acceptance_criteria>
  <done>docs/MODULES.md documents the src/state/ module and the four debug hooks together, satisfying GLOBAL-03's documentation requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser console / MCP -> app state | The debug hook is the one intentional read surface into authoritative game state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Information Disclosure | an overly permissive debug hook exposing the live mutable `state` reference | low | mitigate | `window.__pp_app_state_debug` returns a shallow copy `{...state}`, not the live object; a grep asserts it is not assigned the live reference. This is a client-side game with already-public Firebase config, so the disclosure risk is low - the copy is defense-in-depth against accidental mutation, not a confidentiality control. |
| T-10-09 | Tampering | renaming/removing Phase 9's verified __pp_net_debug and regressing NET-03's observation point | medium | mitigate | Contract-check allowlist requires all four hooks present including __pp_net_debug; the plan prohibits renaming it. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase - package.json gains only a test-chain command referencing an in-repo script; no dependency added. |
</threat_model>

<verification>
- `node scripts/state_contract_check.js` -> 5/5 PASS.
- `npm test` -> exit 0 (full chain including state_contract_check.js).
- Exactly four `window.__pp_*` hooks; `__pp_app_state_debug` is a read-only snapshot.
- docs/MODULES.md has the src/state/ section + four-hook table.
</verification>

<success_criteria>
GLOBAL-03 is satisfied: one documented debug mechanism (four named hooks, the new one read-only), the standing contract check is green and wired into `npm test`, and the module + hooks are documented.
</success_criteria>

<output>
Create `.planning/phases/10-app-state-de-globalization/10-06-SUMMARY.md` when done.
</output>
