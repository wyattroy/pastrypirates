---
phase: 10-app-state-de-globalization
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - index.html
autonomous: true
requirements: [GLOBAL-01]
user_setup: []

must_haves:
  truths:
    - "`game` (418 occurrences), `timer`, and `logLines` are migrated to `state.NAME` at every read AND write site in index.html:859-4667; after this task all 46 app-state names are migrated (GLOBAL-01 complete for the migration surface)."
    - "The six `$(\"game\")` DOM-id lookups are byte-identical - the string literal `game` inside them is NOT qualified (Pitfall 1, the confirmed collision)."
    - "The badge-byline UI-copy string containing the word game is byte-identical (Pitfall 1)."
    - "The determinism corpus stays 30/30 on `--verify` (D-08)."
  prohibitions:
    - "MIGRATION-CORRUPTION: the tokenizer must NOT qualify the string-literal `game` inside the DOM-id lookups nor the badge-byline prose - both must survive byte-for-byte (this is the top prohibition of the phase; the corpus cannot catch a UI-string corruption)."
    - "MISSED-WRITE-SITE: zero bare (non-`state.`) occurrences of game/timer/logLines remain in the region (game has the reassignment sites at the new-Game construction and reset paths)."
    - "TIMER-HANDLE: if Open Question 2 classified `timer` as an interval/timeout handle, every clearInterval/clearTimeout site for it must be `state.`-qualified."
    - "NEVER-CAPTURE: no `--capture`."
  artifacts: []
  key_links:
    - "The six `$(\"game\")` calls resolve the `#game` DOM element; a corrupted id string produces a runtime null the headless corpus cannot see - only the browser check (10-07) would catch it, so byte-safety here is the guard."
---

<objective>
Migrate the last and heaviest names: `game` (the single biggest, 418 occurrences), `timer`, and `logLines`. This plan carries the phase's top hazard - the confirmed string-literal collision between the identifier `game` and the six `$("game")` DOM-id lookups plus the badge-byline UI copy. After this plan, all 46 app-state names are migrated.

Purpose: `game` is read pervasively (render, engine calls, net callbacks) and reassigned at the new-Game construction/reset paths. The migration mechanics are identical to prior batches, but the verification is the strictest: prove the six DOM-id string lookups and the badge byline are untouched, because a corrupted id string throws only in a live browser, never in the corpus.

Output: game/timer/logLines migrated; the six DOM-id lookups and the byline byte-identical; corpus green; all 46 names now `state.`-qualified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-app-state-de-globalization/10-RESEARCH.md
@.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate game, timer, logLines - with the DOM-id string collision proven safe</name>
  <files>index.html</files>
  <read_first>
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` "Confirmed false-positive collision (the concrete evidence for Pitfall 1)" (the six `$("game")` lines and the badge-byline line), Common Pitfalls 1, inventory rows 1, 3, 4 (game write sites at the new-Game construction/reset paths; logLines push/reset sites; timer per Open Question 2).
    - `scripts/migrate_app_state.js` Task-1-of-10-01 comment block - the recorded `timer` classification (interval handle: yes/no) that drives the clearInterval/clearTimeout check here.
    - `index.html` - grep the six DOM-id lookups and the badge byline BEFORE migrating, record their exact byte spans, and re-grep AFTER to prove they are unchanged.
  </read_first>
  <action>
    Run `node scripts/migrate_app_state.js --migrate game,timer,logLines`. The tokenizer rewrites only identifier-position occurrences of these three names inside the classic region.
    The critical review: the six DOM-id lookups pass the literal string `game` as an argument (a string, not an identifier) and must remain byte-identical; the badge-byline is UI prose inside a string literal and must remain byte-identical. Both are string content the tokenizer skips by construction - verify that it did, via the byte-safety diff plus the explicit counts in the acceptance criteria.
    If the recorded `timer` classification says it is an interval/timeout handle, confirm each clearInterval/clearTimeout site for it is now `state.`-qualified.
    Do not touch the 7 render handles. NEVER run `--capture`.
  </action>
  <verify>
    <automated>node scripts/migrate_app_state.js --check-names game,timer,logLines && diff <(node scripts/migrate_app_state.js --extract-strings HEAD:index.html) <(node scripts/migrate_app_state.js --extract-strings index.html) && node scripts/determinism_baseline.js --verify</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/migrate_app_state.js --check-names game,timer,logLines` exits 0.
    - `grep -c "\$(\"game\")" index.html` returns 6 (the DOM-id lookups are byte-identical; the string literal was NOT qualified).
    - `grep -c "love of the game" index.html` returns 1 (the badge byline is byte-identical).
    - The `--extract-strings` diff (HEAD vs working tree) is EMPTY.
    - `node scripts/determinism_baseline.js --verify` exits 0 (30/30).
    - After this task, `node scripts/migrate_app_state.js --check-names` run over the full 46-name list reports zero remaining bare usages (all 46 migrated).
    - `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1.
  </acceptance_criteria>
  <done>game/timer/logLines are `state.`-qualified; the six DOM-id lookups and the badge byline are byte-identical; all 46 names are now migrated; corpus green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokenizer -> index.html string literals | The identifier `game` collides with the `$("game")` DOM-id string and the badge-byline prose; a rewrite inside either ships a broken game silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | the tokenizer qualifying the string-literal `game` inside `$("game")` or the badge byline | high | mitigate | Tokenizer skips string/comment regions; explicit grep counts (6 DOM-id lookups, 1 byline) plus an empty `--extract-strings` diff gate the commit. |
| T-10-02 | Tampering | a missed `game` reassignment (new-Game/reset path) leaving an orphaned bare binding | high | mitigate | `--check-names game,timer,logLines` exit 0, then a full-46-name `--check-names` sweep confirms zero bare usages remain anywhere. |
| T-10-08 | Tampering | a stale `timer` interval handle after migration | medium | mitigate | The recorded Open-Question-2 classification drives an explicit clearInterval/clearTimeout `state.`-qualification review. |
</threat_model>

<verification>
- `--check-names game,timer,logLines` exits 0; full-46 `--check-names` sweep exits 0.
- `$("game")` count = 6; `love of the game` count = 1.
- `--extract-strings` diff empty (byte-safety).
- `node scripts/determinism_baseline.js --verify` -> 30/30.
- Corpus still 1 commit deep.
</verification>

<success_criteria>
The heaviest names are de-globalized with the confirmed string collision proven safe, all 46 names migrated, corpus green, and no string/comment content changed.
</success_criteria>

<output>
Create `.planning/phases/10-app-state-de-globalization/10-05-SUMMARY.md` when done.
</output>
