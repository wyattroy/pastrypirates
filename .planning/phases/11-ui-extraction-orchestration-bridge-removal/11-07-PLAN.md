---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 07
type: execute
wave: 7
depends_on: ["11-06"]
files_modified:
  - src/main.js
  - src/state/index.js
  - src/ui/index.js
  - index.html
  - package.json
  - docs/MODULES.md
autonomous: false
requirements: [SPLIT-05, SPLIT-06]
user_setup: []

must_haves:
  truths:
    - "`node scripts/ui_contract_check.js` exits 0 with all 4 assertions PASS (ui-never-imports-net, bridge-gone, no-leftover-bridge-reads, retained-globals-allowlist) and is wired into package.json `test`"
    - "`grep -rc 'PP-BRIDGE' src/` returns 0 — the four bridge-tagged lines and their obsolete explanatory comments are deleted"
    - "The spread of the PP object onto the global object is gone from src/, and the two comment references to it in src/main.js and src/state/index.js are scrubbed — `grep -rc 'Object.assign(globalThis' src/` returns 0"
    - "`grep -c '^<script>$' index.html` returns 0 — the attribute-less classic `<script>` at index.html:859 is removed; only the Firebase compat classic scripts, the JSON-LD block, and the single `<script type=\"module\" src=\"src/main.js\">` entry remain"
    - "`window.revealMyRecipe` is the ONLY new non-debug window.* assignment (the single deliberate retained global, D-05); the 4 debug hooks (__pp_module_ok, __pp_boot_count, __pp_net_debug, __pp_app_state_debug) survive untouched"
    - "`node scripts/module_graph_check.js` exits 0 (acyclic; ui↛net); `npm test` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1"
    - statement: "Full solo game (sail/dock/trade/battle/fish/storm/end-of-voyage) plays in Chrome with a clean console after bridge deletion — the inline revealMyRecipe onclick still resolves"
      verification: backstop
    - statement: "Two-tab multiplayer game syncs deterministically in Chrome (host + guest) with a clean console after bridge deletion"
      verification: backstop
  artifacts:
    - "docs/MODULES.md (bridge section removed; revealMyRecipe retained-global documented)"
  key_links:
    - "ui_contract_check.js wired into package.json `test` after state_contract_check.js — the standing bridge-removal gate for all future phases"
    - "index.html reduced to markup + Firebase compat classics + one module entry — SPLIT-05 completion"
---

<objective>
Delete the strangler-fig bridge in a single, mechanically-gated, one-way commit (D-03 — bridge removal is coupled to UI extraction and finishes only after the last classic consumer is inside a module): remove the `PP` assembly + the spread onto the global object, reduce `index.html` to markup + Firebase compat classics + the one module entry (D-08), add the single deliberate retained global `window.revealMyRecipe` (D-05), scrub the obsolete bridge comments, assert zero leftover bare-global reads mechanically not by eye (D-04), wire `ui_contract_check.js` into `npm test`, and update `docs/MODULES.md`. Then prove the whole game still runs in Chrome (solo + two-tab).

This is the highest-risk single operation in the milestone: a missed bare-global read fails SILENTLY as a runtime `ReferenceError` only when that unexercised code path runs — the UI-blind corpus cannot catch it. The gate is therefore the mechanical bridge-removal assertions PLUS a full solo AND two-tab Chrome click-through — never `--verify` alone.

Purpose: complete SPLIT-05 (index.html = markup + one module entry) and SPLIT-06 (acyclic graph, standing-gated) and criterion 3 (bridge gone, grep-confirmed).
Output: bridge deleted, index.html reduced, ui_contract_check wired green, docs updated, Chrome-verified running game.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: PP-BRIDGE -->
<!-- planner-discipline-allow: Object.assign(globalThis -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decision gate — approve the one-way bridge deletion</name>
  <decision>Proceed with the one-way deletion of the strangler-fig global bridge?</decision>
  <context>
    After 11-06 the classic `<script>` region has zero function declarations (confirmed by `node scripts/analyze_classic.mjs`), `npm test` is green, and the seam is formalized with real imports. Deleting the bridge is effectively irreversible within the phase: unlike a verbatim code move (revertable), a missed bare-global read surfaces only as a runtime `ReferenceError` on a code path the 30-seed corpus never exercises — silent until a real user hits it. This gate exists to confirm the pre-conditions are truly met before the irreversible step.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with bridge deletion now</name>
      <pros>All 183 functions moved; classic region empty; npm test green; ui_contract_check red-proof drilled and ready to wire; the browser click-through gate below will catch any silent miss before the phase closes</pros>
      <cons>One-way within the phase; relies on the Chrome solo + two-tab click-through to surface a silently-missed bare read</cons>
    </option>
    <option id="reaudit">
      <name>Pause to re-audit reachability first</name>
      <pros>Extra confidence via a manual sweep for any remaining bare reference of a former bridge symbol before deleting</pros>
      <cons>Delays completion; the mechanical no-leftover-reads assertion + Chrome gate already provide this coverage</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or reaudit</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete the bridge, reduce index.html to markup, add the retained global, wire the gate, update docs</name>
  <files>src/main.js, src/state/index.js, index.html, package.json, docs/MODULES.md</files>
  <reversibility rating="one-way">Bridge deletion is irreversible within the phase — a missed bare-global read fails silently as a runtime ReferenceError the corpus never exercises; gated by the decision checkpoint above and the Chrome click-through below.</reversibility>
  <read_first>
    - src/main.js (the four bridge-tagged lines at 62-64 + the tag-in-prose at line 37, the large explanatory bridge comment block lines 36-64, the `Object.assign`-referencing comment at line 47, the `window.applyEngineBootstrapEffects()`/`window.attachPastryArt()` boot calls, and the 4 debug hooks that MUST survive)
    - src/state/index.js (line 5 comment references the PP spread onto the global object — reword/scrub so the bridge-gone grep is clean)
    - src/ui/index.js (revealMyRecipe export from flow.js — main binds the retained global from here)
    - scripts/ui_contract_check.js (the 4-assertion gate from 11-01, red-proof drilled — now wire it into package.json `test`)
    - index.html lines 859 and 4668 (the classic `<script>` open at :859 — now empty — and the module entry at :4668; remove the empty classic tag pair)
    - docs/MODULES.md (the bridge / PP-BRIDGE / startup-order sections to update; the "Standing browser debug hooks" section where revealMyRecipe is documented alongside the 4 hooks)
    - RESEARCH.md § Q2 (what must survive: window.revealMyRecipe + the 4 debug hooks; boot already called directly)
  </read_first>
  <action>
    In `src/main.js`: delete the three bridge-tagged executable lines (the `PP` object assembly, the `window.PP` publish, and the spread of `PP` onto the global object) AND the now-obsolete explanatory bridge comment block (including the prose line that names the removal token and the comment that references the global-object spread), so no bridge tag or spread reference remains anywhere in src/. Keep the 4 debug-hook assignments exactly as-is. Rewire the two boot calls `window.applyEngineBootstrapEffects()` / `window.attachPastryArt()` to direct module calls (`ui.applyEngineBootstrapEffects()` / `ui.attachPastryArt()`). Add the single deliberate retained global `window.revealMyRecipe = ui.revealMyRecipe;` with a comment mirroring the debug-hook style (documented, named, intentional — D-05).

    In `src/state/index.js`: reword/scrub the line-5 comment that names the global-object spread so the bridge-gone grep across src/ is clean (describe the mechanism historically without the exact spread call).

    In `index.html`: remove the now-empty attribute-less classic `<script></script>` tag pair at :859 so the file is markup + the two Firebase compat classic scripts + the JSON-LD block + the single `<script type="module" src="src/main.js">` entry — SPLIT-05.

    In `package.json`: append `node scripts/ui_contract_check.js` to the `test` chain after `state_contract_check.js`. It must now PASS all 4 assertions (bridge is genuinely gone).

    In `docs/MODULES.md`: delete/revise the bridge and PP-BRIDGE sections (the bridge no longer exists), and document `window.revealMyRecipe` as the single deliberate retained global in the "Standing browser debug hooks (GLOBAL-03)" area alongside the 4 read-only debug hooks.
  </action>
  <verify>
    <automated>npm test && node scripts/module_graph_check.js && node scripts/ui_contract_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'PP-BRIDGE' src/` returns 0
    - `grep -rc 'Object.assign(globalThis' src/` returns 0
    - `grep -c '^<script>$' index.html` returns 0
    - `node scripts/ui_contract_check.js` exits 0 with 4/4 PASS and appears in `package.json` `test`
    - `src/main.js` contains exactly one new non-debug `window.` assignment: `window.revealMyRecipe` (the 4 `window.__pp_*` debug hooks unchanged); `grep -c 'window.applyEngineBootstrapEffects()\|window.attachPastryArt()\|window.boot()' src/main.js` returns 0 (all rewired to direct module calls)
    - `docs/MODULES.md` no longer documents an active bridge and documents `window.revealMyRecipe`
    - `npm test` exits 0; `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>Bridge deleted and grep-confirmed gone; index.html is markup + one module entry; the single retained global is in place and documented; ui_contract_check wired green; full suite + determinism pass. Browser proof follows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Chrome click-through gate — solo + two-tab after bridge deletion</name>
  <what-built>The bridge-deleted build: index.html reduced to markup + one `<script type="module" src="src/main.js">`, all 183 functions in modules, `window.revealMyRecipe` the only retained non-debug global.</what-built>
  <how-to-verify>
    Serve over the local HTTP test server (never file://; per MEMORY project_mp_test_harness, mind the pp_id shared-localStorage gotcha and the stale-server port trap). (1) SOLO: load index.html in Chrome, play a FULL solo game — sail, dock, trade, battle, fish, trigger a storm, reach end-of-voyage — with DevTools open the whole time; the console must stay clean (no ReferenceError / no-undef), and the inline recipe reveal (`revealMyRecipe`) must work. (2) TWO-TAB MULTIPLAYER: open two Chrome tabs, create a room in one and join from the other, play several turns; confirm host↔guest turn propagation (same captain state at the same moment) and a clean console in BOTH tabs. This is the real gate for a silently-missed bare-global read — the corpus is UI-blind and cannot see it.
  </how-to-verify>
  <resume-signal>Type "approved" or paste the console error / desync observed (with the tab and action that triggered it)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console/global scope → app internals | Deleting the bridge REMOVES engine/net/state internals from bare-identifier console reach — a net reduction in global attack surface |
| inline onclick → global scope | The single `onclick="revealMyRecipe()"` is the only markup→global dependency; served by the one deliberate retained global |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Tampering | silently-missed bare-global read after bridge deletion | high | mitigate | ui_contract_check no-leftover-reads assertion (mechanical) + full solo AND two-tab Chrome click-through with clean console (the gate; `--verify` alone is insufficient — corpus is UI-blind) |
| T-11-11 | Elevation of privilege | global attack surface | low | accept | bridge deletion REDUCES surface — engine/net internals no longer reachable via bare console identifiers; a security improvement |
| T-11-02 | Information disclosure | window.revealMyRecipe + 4 debug hooks | low | accept | game state only, no secrets; hooks read-only (__pp_app_state_debug returns a copy); Firebase config already public by design; allowlisted + documented in docs/MODULES.md |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained; module_graph_check is a custom script, not madge |
</threat_model>

<verification>
- `grep -rc 'PP-BRIDGE' src/` == 0; `grep -rc 'Object.assign(globalThis' src/` == 0; `grep -c '^<script>$' index.html` == 0
- `npm test` green WITH ui_contract_check.js now in the chain (4/4 PASS)
- `node scripts/module_graph_check.js` exits 0 (acyclic, ui↛net)
- `node scripts/determinism_baseline.js --verify` 30/30; corpus fixture commit count stays 1
- Blocking Chrome checkpoint: full solo game + two-tab multiplayer, clean console in all tabs, bridge provably gone
</verification>

<success_criteria>
Bridge deleted and grep-confirmed; index.html reduced to markup + one module entry (SPLIT-05); graph acyclic and standing-gated (SPLIT-06); ui_contract_check green in npm test; single retained global documented; Chrome solo + two-tab click-through approved with clean consoles.
</success_criteria>

## Artifacts this phase produces

- `window.revealMyRecipe` (this plan — the single deliberate retained global); `docs/MODULES.md` updated; `ui_contract_check.js` wired into `npm test`. Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/orchestrator.js`, `src/main.js` (expanded orchestrator, bridge deleted), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug` (NOT part of the deleted bridge).

## Flagged Assumptions

- **SPLIT-05/06 edge-probe unresolved:** represented here by the concrete checks — the bridge-gone/`<script>` greps, `ui_contract_check.js` 4/4, `module_graph_check.js` acyclic, and the browser click-through. Not silently dropped.
- **Silent-miss risk is real:** the corpus is UI-blind, so `--verify` green is necessary-not-sufficient; the browser click-through is the load-bearing gate here (RESEARCH Q5b).

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-07-SUMMARY.md` when done
</output>
