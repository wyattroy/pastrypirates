---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - src/ui/flow.js
  - src/ui/handlers.js
  - src/ui/index.js
  - src/main.js
  - index.html
autonomous: true
requirements: [SPLIT-03]
user_setup: []

must_haves:
  truths:
    - "The turn-flow + interaction cluster (humanTurn, botTurn, humanAct, humanTrade, humanWind, humanDock, windLeg, fishCast, humanFlip, reachable, pickCell, localPickCell, localAsk) lives verbatim in src/ui/flow.js"
    - "The battle-UI + side-bets + intro cluster (battleSnapshot, renderBattleFromSnap, battleFooter, coinHTML, pipsHTML, collectSideBets, settleSideBets, asyncBakeoff, netIntroBarrier, showAhoyIntro, showTurnOrderIntro, startSinglePlayer, startPassAndPlay, revealMyRecipe) lives verbatim in src/ui/flow.js"
    - "The recovery/replay seam trio (endReplay, showRestoreFail, wireRestoreFail) and remotePickHighlights reach net only through injected handlers — src/ui contains no `from \"../net` import (`node scripts/module_graph_check.js` exits 0, ui↛net)"
    - "All 5 handler-injection UI edges (flash→broadcast, liveRender→onEvents, remotePickHighlights→respond, endReplay→recovery, wireRestoreFail→recovery+leave) route through src/ui/handlers.js; the 6th edge (battleAsk) is deferred to orchestration in 11-06"
    - "byte-diff of moved bodies shows no reformatting; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1"
  artifacts:
    - src/ui/flow.js
  key_links:
    - "src/ui/flow.js → ./panel.js/./board.js/./util.js (already-moved siblings) — the deepest ui-internal import layer module_graph_check.js verifies acyclic"
    - "src/ui/handlers.js now carries all 5 UI-side seam edges; main wires respond/recovery/leave alongside the 11-04 broadcast/onEvents"
---

<objective>
Extract the turn-flow, interaction, battle-UI, and recovery clusters — the deepest layer of `src/ui/`, which calls the panel/board/util modules already moved — into `src/ui/flow.js`, and resolve the remaining 3 of the 5 UI-side seam edges (remotePickHighlights→sendResponse, endReplay→setRecoveryState, wireRestoreFail→setRecoveryState+leaveGame) through the injected-handler seam (D-07 — src/ui never imports src/net).

Purpose: leave the classic region holding only the 44 orchestration (net-caller) functions plus top-level wiring, so 11-06 can move orchestration into main and 11-07 can delete the bridge.
Output: `src/ui/flow.js`, all 5 UI seam edges resolved, `revealMyRecipe` in a module, classic region reduced to orchestration + top-level code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the turn-flow + interaction cluster into src/ui/flow.js</name>
  <files>src/ui/flow.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (`helper/logic` + `ui (DOM)` net-free: humanTurn 2692-2748, botTurn 2749-2823, humanAct 2632-2691, humanTrade 2505-2631, humanWind 2113-2122, humanDock 2123-2152, windLeg 2054-2111, fishCast 1952-1973, humanFlip 1934-1949, reachable 1976-2004, pickCell 2005-2017, localPickCell 2018-2037, localAsk 1843-1872 — verify each callee is already in src/ui/{panel,board,util,recipe}.js or shared/engine/state)
    - index.html lines listed above (verbatim bodies)
    - src/ui/panel.js, src/ui/board.js, src/ui/util.js (siblings these flow functions call — import from them)
    - src/engine/index.js (Game methods the turn flow invokes on appState.game)
  </read_first>
  <action>
    Create `src/ui/flow.js` (src/ui header + ui↛net constraint). Move the turn-flow and interaction functions listed above verbatim. Rewire bare reads into imports from ./panel.js, ./board.js, ./util.js, ../engine, ../shared, ../state. These are net-free (per analysis.json net:[]) so no seam handling is needed for this task. Export all; barrel re-exports flow.js. Delete moved bodies from the classic region. The `...ui` bridge keeps any still-classic orchestration caller (e.g. runLiveNet calling humanTurn) resolving.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/flow.js` contains `export ... function humanTrade(` and `export ... function botTurn(` and `export ... function humanTurn(`
    - `src/ui/flow.js` contains NO `from "../net` import (ui↛net)
    - `grep -c 'function humanTrade\|function botTurn\|function humanTurn' index.html` returns 0
    - byte-diff of humanTrade/botTurn/windLeg (the large bodies) shows no reformatting
    - `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>Turn-flow + interaction cluster in src/ui/flow.js with import-based deps; graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract battle-UI + side-bets + intro + game-start helpers (incl. revealMyRecipe)</name>
  <files>src/ui/flow.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (net-free: battleSnapshot 2197-2203, renderBattleFromSnap 2204-2207, battleFooter 2216-2225, coinHTML 2153-2160, pipsHTML 2161-2166, collectSideBets 2275-2314, settleSideBets 2315-2331, asyncBakeoff 2826-2870, netIntroBarrier 1899-1916, showAhoyIntro 1919-1922, showTurnOrderIntro 1927-1933, startSinglePlayer 3391-3401, startPassAndPlay 3404-3412, revealMyRecipe 3450)
    - index.html lines listed above; note revealMyRecipe (3450) is `function revealMyRecipe(){appState.recipeRevealed=true;liveRender();}` — reads appState, calls liveRender (now in src/ui/panel.js)
    - RESEARCH.md § Q2b (revealMyRecipe's inline onclick at index.html:1731 resolves via the bridge this wave; the explicit `window.revealMyRecipe =` retained global is added in 11-07, not here)
    - src/ui/flow.js + src/ui/panel.js + src/ui/util.js (siblings; battle UI calls coinHTML/pipsHTML/liveRender)
  </read_first>
  <action>
    Move the battle-UI render helpers, side-bet logic, intro barriers, game-start helpers, and revealMyRecipe verbatim into `src/ui/flow.js` (extending task 1). Rewire bare reads into imports (liveRender from ./panel.js, format helpers from ./util.js, engine/shared/state as needed). Note `battleAsk` (2228-2271) is NOT moved here — it is orchestration (calls netBroadcast/renderBattle/remotePrompt/logDecision) and is homed in 11-06. Keep revealMyRecipe a plain module `export function` — its inline `onclick="revealMyRecipe()"` still resolves through the `...ui` bridge this wave; the explicit retained-global assignment lands in 11-07. Delete moved bodies from the classic region; keep the barrel current.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/flow.js` contains `export function collectSideBets(` and `export function revealMyRecipe(` and `export ... function asyncBakeoff(`
    - `battleAsk` is STILL in the classic region (deferred to 11-06) — `grep -c 'function battleAsk' index.html` returns 1
    - `grep -c 'function collectSideBets\|function revealMyRecipe\|function asyncBakeoff' index.html` returns 0
    - flow.js still contains NO `from "../net` import
    - byte-diff shows no reformatting; `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30
  </acceptance_criteria>
  <done>Battle-UI, side-bets, intros, game-start, and revealMyRecipe moved into flow.js; battleAsk correctly deferred; graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 3: Extract the recovery/replay seam trio + remotePickHighlights via injected handlers</name>
  <files>src/ui/flow.js, src/ui/handlers.js, src/ui/index.js, src/main.js, index.html</files>
  <read_first>
    - RESEARCH.md § Q1b (the 6-edge seam: remotePickHighlights→sendResponse, endReplay→setRecoveryState, wireRestoreFail→setRecoveryState+leaveGame — the 3 remaining UI-side edges)
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (endReplay 3154-3173, showRestoreFail 3180-3186, wireRestoreFail 3187-3200, remotePickHighlights 2039-2051 — their net-adjacent calls)
    - src/ui/handlers.js (the seam module stood up in 11-04 — extend its handler set with respond/recovery/leave)
    - src/main.js (where 11-04 wired onBroadcast/onEvents through the bridge — extend the same setNetHandlers call with respond/recovery/leave, still through bridge globals sendResponse/setRecoveryState/leaveGame this wave)
    - index.html lines listed above (verbatim bodies)
  </read_first>
  <action>
    Move endReplay, showRestoreFail, wireRestoreFail, and remotePickHighlights verbatim into `src/ui/flow.js` (or ./panel.js — keep consistent). Replace their direct net-adjacent calls with injected-handler calls from src/ui/handlers.js: remotePickHighlights→`_h.onRespond`, endReplay→`_h.onRecovery`, wireRestoreFail→`_h.onRecovery` + `_h.onLeave`. Do NOT import src/net. Extend `src/main.js`'s existing `ui.setNetHandlers({...})` call to also wire `onRespond: (...a)=>globalThis.sendResponse(...a)`, `onRecovery: (...a)=>globalThis.setRecoveryState(...a)`, `onLeave: (...a)=>globalThis.leaveGame(...a)` — the temporary bridge wire, formalized to real net imports in 11-06 when sendResponse/setRecoveryState/leaveGame move into modules. Delete moved bodies from the classic region. After this task the classic region holds ONLY the 44 orchestration net-callers (incl. battleAsk) plus top-level wiring/constants.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/handlers.js` seam now covers respond/recovery/leave in addition to broadcast/onEvents
    - endReplay/wireRestoreFail/remotePickHighlights bodies reference injected handlers, not bare `sendResponse`/`setRecoveryState`/`leaveGame`
    - NO file under `src/ui/` contains a `from "../net` import — `node scripts/module_graph_check.js` prints its "ui does NOT import net" PASS line and exits 0
    - `grep -c 'function endReplay\|function wireRestoreFail\|function remotePickHighlights' index.html` returns 0
    - `npm test` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>All 5 UI-side seam edges resolved through handler injection; classic region reduced to orchestration + top-level code; full npm test + determinism green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → orchestration (the 6-edge seam) | The remaining 3 UI-side edges must reach net only through injected handlers; criterion 1 enforced by module_graph_check |
| replay/recovery state → net | endReplay/wireRestoreFail write recovery state; behavior unchanged (Phase 9/10 already modularized net + de-globalized state), pure code motion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Tampering | criterion-1 UI↛net boundary (5 seam edges) | high | mitigate | all 5 edges routed through handlers.js; `module_graph_check.js` ui↛net assertion is a standing gate; `npm test` at task 3 confirms the whole suite green |
| T-11-07 | Tampering | turn-flow / battle-flow order (humanTurn/botTurn/asyncBakeoff feed this.r()) | high | mitigate | verbatim byte-diff (no reordering of RNG-feeding object literals) + `--verify` 30/30 — RNG desync is the milestone's top risk |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained |
</threat_model>

<verification>
- `npm test` green at task 3 (determinism + all contract checks + module-graph)
- `node scripts/determinism_baseline.js --verify` 30/30 after each task
- byte-diff pure-motion audit on the large flow bodies (humanTrade, botTurn, asyncBakeoff, windLeg)
- all 5 UI-side seam edges proven to reach net only via handlers.js
</verification>

<success_criteria>
Turn-flow, battle-UI, recovery clusters extracted verbatim; all 5 UI-side seam edges resolved without a net import; the classic region now holds only the 44 orchestration net-callers + top-level wiring; graph acyclic; determinism 30/30.
</success_criteria>

## Artifacts this phase produces

- `src/ui/flow.js` (this plan); extends `src/ui/handlers.js`. Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/main.js` (expanded orchestrator), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, `window.revealMyRecipe` (retained global, 11-07), surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug`.

## Flagged Assumptions

- **battleAsk classification (RESEARCH Q1a #1):** deferred to 11-06 as orchestration (it calls 4 net-adjacent fns) rather than a 6th injected-handler UI function — this plan resolves the other 5 edges and leaves battleAsk for the orchestration wave. Low risk; the planner-confirmed home is main/flow.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-05-SUMMARY.md` when done
</output>
