---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 06
type: execute
wave: 6
depends_on: ["11-05"]
files_modified:
  - src/orchestrator.js
  - src/main.js
  - src/ui/index.js
  - index.html
autonomous: true
requirements: [SPLIT-05]
user_setup: []

must_haves:
  truths:
    - "All 44 orchestration (net-caller) functions live in src/orchestrator.js (or src/flow/ modules), importing src/net directly and src/ui for render — the classic <script> region holds no top-level function declarations after this wave"
    - "main.js is a real orchestrator: it imports engine + ui + net + orchestrator, wires ui.setNetHandlers with REAL net/orchestrator functions (the temporary globalThis bridge wires from 11-04/11-05 are gone), and calls boot directly (window.boot indirection removed)"
    - "battleAsk is homed in orchestrator (calls netBroadcast/renderBattle/remotePrompt/logDecision), consuming injected UI render helpers"
    - "watchRoom is idempotent — a normal guest join no longer logs the Phase-9 registry 'duplicate attach refused' ERROR (D-13)"
    - "src/ui/ still contains no `from \"../net` import (`node scripts/module_graph_check.js` exits 0, ui↛net); no import cycle introduced by orchestrator → {ui, net}"
    - "`npm test` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1"
  artifacts:
    - src/orchestrator.js
  key_links:
    - "src/main.js → src/orchestrator.js → {src/net, src/ui, src/engine, src/state}; ui never imports orchestrator or net — the acyclic composition module_graph_check.js verifies"
    - "ui.setNetHandlers now bound to real net/orchestrator functions — the 6-edge seam is fully formalized"
---

<objective>
Move the 44 orchestration (net-caller) functions out of the classic region into `src/orchestrator.js` (imported by main), turn `src/main.js` into the real composition root, and formalize the handler-injection seam with real net imports — satisfying SPLIT-05's "a `main` entry module orchestrates engine + UI + networking." After this wave the classic `<script>` region has no function declarations left; only the bridge deletion + markup reduction remain (11-07).

Purpose: complete the code motion so the bridge can be deleted next as a single gated commit against an empty classic region.
Output: `src/orchestrator.js`, a real `src/main.js` orchestrator, formalized seam, idempotent watchRoom, empty classic region.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move sync/broadcast/battle orchestration into src/orchestrator.js</name>
  <files>src/orchestrator.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (`orchestration` tier with net[] targets: broadcastFlip 1252-1255, watchFlip 1256-1258, broadcastClock 1329-1332, toggleTimer 1336-1341, watchTimer 1342-1360, expireShotClock 1382-1417, watchClock 1418-1420, netNarrate 1658, netBroadcast 1661, sendChat 1669-1677, watchChat 1685-1691, renderBattle 2170-2196, watchBattle 2208-2214, asyncBattle 2332-2504, battleAsk 2228-2271, watchPresence 3065-3079, fbInit 3080-3085, writeMeta 3008-3014, writeGameLog 3019-3032, applyEndMeta 3356-3365)
    - src/net/index.js (the net primitives orchestrator imports: netSetFlip, netWatchFlip, netSetClock, netSetBattle, netWatchBattle, etc.)
    - src/ui/panel.js + src/ui/board.js + src/ui/flow.js (render/battle-UI helpers orchestrator imports — battleAsk builds DOM via these)
    - RESEARCH.md § Q1b (battleAsk homed here as orchestration, not pure UI)
    - index.html lines listed above (verbatim bodies)
  </read_first>
  <action>
    Create `src/orchestrator.js` with a header stating it is the app-level orchestration layer (imports src/net for sync, src/ui for render, src/engine + src/state; imported BY main). Move the flip/clock/timer sync, narration/chat broadcast, battle sync (incl. battleAsk and asyncBattle), presence, meta/gamelog orchestration functions verbatim. Rewire their bare reads: net primitives from `../net/index.js` → `./net/index.js` (relative to src/), render helpers from `./ui/index.js`, engine/state as needed. Export all. Delete moved bodies from the classic region. The bridge (`...ui` etc.) still exists — do not touch it here.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/orchestrator.js` contains `export ... function asyncBattle(` and `export function battleAsk(` and imports from both `./net/index.js` and `./ui/index.js`
    - `grep -c 'function asyncBattle\|function battleAsk\|function renderBattle' index.html` returns 0
    - `node scripts/module_graph_check.js` exits 0 (orchestrator→{net,ui} allowed; no ui→net, no cycle)
    - byte-diff of asyncBattle (173L) shows no reformatting
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>Sync/broadcast/battle orchestration in src/orchestrator.js with real net + ui imports; graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 2: Move room-lifecycle + prompt/recovery/turn-flow orchestration + make watchRoom idempotent (D-13)</name>
  <files>src/orchestrator.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (`orchestration` tier: createRoom 3477-3492, joinRoom 3493-3518, watchRoom 3519-3537, startGame 3559-3572, beginGame 3573-3597, leaveGame 3620, wireLobby 3623-3663, watchTurnOrder 3600-3605, watchRecipes 3611-3619, recipeDraftNet 2875-2920, runLiveNet 2921-2981, liveResolveEndNet 2982-3007, resumeHostGame 3671-3693, setRecoveryState 3204-3208, watchRecoveryState 3211-3217, pushEvents 3219-3225, remotePrompt 3227-3237, sendResponse 3239-3244, remoteDraftPrompt 3248-3259, watchDraftPrompt 3260-3275, watchEvents 3282-3293, watchPrompt 3294-3348, watchNarr 3349-3355, logDecision 3102-3114, boot 3763-3807)
    - src/net/registry.js (the Phase 9 registry that logs 'duplicate attach refused' — watchRoom's double-attach is the D-13 target; make watchRoom guard against re-attaching when already attached)
    - src/ui/flow.js + src/ui/panel.js + src/ui/lobby.js (runLiveNet calls humanTurn/liveRender; watchPrompt calls localAsk; boot calls wireWelcome/wireLobby/showHome — import these)
    - index.html lines listed above (verbatim bodies) — note boot (3763-3807) is the startup seam main will call directly
  </read_first>
  <action>
    Move the room-lifecycle, prompt/response/draft, events/narr watchers, recovery, turn-flow orchestration, and boot functions verbatim into `src/orchestrator.js` (extending task 1). Rewire bare reads into imports from ./net/index.js, ./ui/index.js, ./engine, ./state. Apply D-13: make `watchRoom()` idempotent — guard so a repeated call (normal guest join invokes it more than once per lifecycle) does not re-attach and trigger the registry's ERROR-level 'duplicate attach refused'; a second call should be a no-op or cleanly re-point without the error. Export all (including boot). Delete moved bodies from the classic region. After this task the classic `<script>` region contains NO top-level `function` declarations — verify with the analyzer.
  </action>
  <verify>
    <automated>node scripts/analyze_classic.mjs && node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/orchestrator.js` contains `export ... function watchRoom(` and `export ... function boot(` and `export ... function runLiveNet(`
    - `node scripts/analyze_classic.mjs` reports 0 top-level function declarations remaining in the classic region (down from 183)
    - watchRoom contains an idempotency guard (a re-attach on a second call is prevented) — D-13
    - `node scripts/module_graph_check.js` exits 0; byte-diff of runLiveNet/watchPrompt shows no reformatting
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>All orchestration moved; classic region has zero function declarations; watchRoom idempotent; graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 3: Make src/main.js the real orchestrator and formalize the seam</name>
  <files>src/main.js, src/ui/handlers.js</files>
  <read_first>
    - src/main.js (current file: imports shared/engine/net/state, the PP bridge, the two temporary seam wires from 11-04/11-05 through globalThis, `window.boot()` at the end, and the `window.applyEngineBootstrapEffects()`/`window.attachPastryArt()` boot calls)
    - src/orchestrator.js (the functions main now imports: boot, netNarrate, pushEvents, sendResponse, setRecoveryState, leaveGame — the real targets that replace the globalThis wires)
    - src/ui/handlers.js (the setNetHandlers seam to bind with real functions)
    - RESEARCH.md § Q2a (window.boot: only 2 refs — the definition, now moved, and this call site; replace with a direct import call)
  </read_first>
  <action>
    Expand `src/main.js` into the real composition root: add `import * as orchestrator from "./orchestrator.js"` and `import { boot } from "./orchestrator.js"`. Replace the temporary `ui.setNetHandlers({...globalThis...})` wire with real bindings — `onBroadcast: orchestrator.netNarrate, onEvents: orchestrator.pushEvents, onRespond: orchestrator.sendResponse, onRecovery: orchestrator.setRecoveryState, onLeave: orchestrator.leaveGame` — so no seam handler references a bridge global anymore. Replace `window.boot()` with a direct `boot()` call. Keep the `...ui`/`...net`/`...engine`/`...shared`/`appState` bridge and the 4 debug hooks EXACTLY as-is (bridge deletion is 11-07, gated). Leave `window.applyEngineBootstrapEffects()`/`window.attachPastryArt()` for 11-07's rewire (still bridge-resolved). Run the full suite.
  </action>
  <verify>
    <automated>npm test && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.js` imports from `./orchestrator.js` and calls `boot()` directly — `grep -c 'window.boot()' src/main.js` returns 0
    - `ui.setNetHandlers(` in main binds `orchestrator.netNarrate`/`orchestrator.pushEvents`/etc. — no `globalThis.netNarrate`/`globalThis.sendResponse` remains in the seam wire (`grep -c 'globalThis.netNarrate\|globalThis.pushEvents\|globalThis.sendResponse' src/main.js` returns 0)
    - the PP bridge and 4 debug hooks are still present (deletion is 11-07)
    - `npm test` exits 0; `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30
  </acceptance_criteria>
  <done>main.js is the real orchestrator; the 6-edge seam is fully formalized with real imports; boot called directly; full suite green; only the gated bridge deletion remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| net → orchestration → ui | main wires the layers; the only forbidden edge (ui→net) stays absent; orchestrator legitimately imports both net and ui |
| registry double-attach → guest join | watchRoom idempotency (D-13) removes a spurious ERROR-level log on normal guest join — a small reliability improvement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Tampering | orchestration RNG/turn order (runLiveNet/asyncBattle/botTurn feed this.r()) | high | mitigate | verbatim byte-diff + `--verify` 30/30 — no reordering of RNG-feeding structures during the largest single move |
| T-11-06 | Tampering | criterion-1 UI↛net boundary | high | mitigate | seam now bound to real orchestrator/net functions; `module_graph_check.js` confirms ui still imports no net and no cycle formed |
| T-11-09 | Denial of service | watchRoom re-attach | low | mitigate | idempotency guard prevents the registry error/leak class on repeated guest join (D-13) |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained |
</threat_model>

<verification>
- `npm test` green at task 3 (full suite)
- `node scripts/analyze_classic.mjs` reports 0 remaining function declarations in the classic region
- `node scripts/module_graph_check.js` exits 0 (acyclic; ui↛net)
- `node scripts/determinism_baseline.js --verify` 30/30 after each task
- watchRoom idempotency confirmed (no duplicate-attach error on repeated call)
</verification>

<success_criteria>
All 44 orchestration functions in src/orchestrator.js; main.js a real composition root wiring engine + ui + net; the 6-edge seam formalized with real imports; watchRoom idempotent; classic region free of function declarations; full suite + determinism green. Bridge still present for the gated 11-07 deletion.
</success_criteria>

## Artifacts this phase produces

- `src/orchestrator.js` (this plan); `src/main.js` becomes the real orchestrator. Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/orchestrator.js`, `src/main.js` (expanded orchestrator), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, `window.revealMyRecipe` (retained global, 11-07), surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug`.

## Flagged Assumptions

- **battleAsk final home confirmed (RESEARCH Q1a #1):** homed in src/orchestrator.js as orchestration.
- **95-helper / orchestrator internal split (RESEARCH Q1a #2):** orchestration may be one `src/orchestrator.js` or several `src/flow/*.js` modules imported by main — executor discretion (D-06), as long as ui never imports it and the graph stays acyclic.
- **SPLIT-05 edge-probe unresolved:** represented by the concrete checks here — `analyze_classic.mjs` (0 classic functions), `module_graph_check.js` (main orchestrates acyclically), full `npm test`. Not silently dropped.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-06-SUMMARY.md` when done
</output>
