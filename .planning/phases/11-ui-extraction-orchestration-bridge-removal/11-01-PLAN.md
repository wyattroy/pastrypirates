---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/analyze_classic.mjs
  - scripts/module_graph_check.js
  - scripts/ui_contract_check.js
  - src/ui/recipe.js
  - src/ui/index.js
  - src/main.js
  - index.html
  - package.json
autonomous: false
requirements: [SPLIT-03, SPLIT-06]
user_setup: []

must_haves:
  truths:
    - "`node scripts/module_graph_check.js` exits 0 and asserts the expected acyclic shape (shared ← engine, shared ← ui, shared ← net, engine ← ui, {engine,ui,net} ← main; ui must NOT → net)"
    - "`node scripts/ui_contract_check.js` is red-proof drilled: each of its 4 assertions demonstrably fails against a synthetic violation before the real tree is checked (net_contract_check.js precedent)"
    - "`node scripts/determinism_baseline.js --verify` reports 30/30 after the tracer extraction"
    - "`git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1 (never --capture, D-10)"
    - "The recipe/pastry cluster (escHtml, attachPastryArt, recipeInfo, recipeTitle, winRecipeSpan, recipeCardHTML, recipeModalHTML, openRecipeModal, wireRecipeModal) lives verbatim in src/ui/recipe.js, re-exported through src/ui/index.js, and is spread into the PP bridge via `...ui` so still-classic callers resolve"
    - "byte-diff of the moved recipe-cluster function bodies (pre-move index.html slice vs post-move src/ui/recipe.js) shows no reformatting"
    - statement: "The tracer build loads in Chrome with a clean console; the recipe modal opens and one solo turn renders"
      verification: backstop
  artifacts:
    - scripts/analyze_classic.mjs
    - scripts/module_graph_check.js
    - scripts/ui_contract_check.js
    - src/ui/recipe.js
    - src/ui/index.js
  key_links:
    - "src/main.js PP bridge grows to `...ui` — the mechanism that keeps still-classic callers of moved UI functions resolving until the final bridge-deletion wave"
    - "scripts/module_graph_check.js wired into package.json `test` after state_contract_check.js"
---

<objective>
Build the Wave-0 safety net and prove the UI-extraction pattern end-to-end on ONE small cluster (the tracer), so every later extraction wave rides a verified mechanism with standing gates.

Two things happen here: (1) the safety net — commit the static analyzer, build `module_graph_check.js` (cycle detection + expected acyclic shape, wired into `npm test` now) and `ui_contract_check.js` (the 4 bridge-removal assertions, red-proof drilled but NOT wired into `npm test` until the bridge is actually gone in 11-07); (2) the tracer — extract the recipe/pastry leaf cluster into `src/ui/recipe.js` verbatim, rewire its bare reads into imports, grow the `PP` bridge with `...ui` so still-classic callers still resolve, and prove the game still runs (`--verify` 30/30, graph check green, Chrome load).

Purpose: de-risk the largest, highest-risk phase of the milestone by proving the "move verbatim + import rewiring + bridge grows + gates green" pattern once before fanning out to 130+ more functions.
Output: two committed contract scripts, a committed analyzer, `src/ui/recipe.js` + `src/ui/index.js`, an expanded bridge, and a Chrome-verified running game.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VALIDATION.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the static analyzer + build the two contract scripts</name>
  <files>scripts/analyze_classic.mjs, scripts/module_graph_check.js, scripts/ui_contract_check.js, package.json</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis-tool.mjs (the phase-scratchpad analyzer to commit — note the two absolute/scratchpad paths on lines 4 and 16/89 that MUST become relative/repo-local)
    - scripts/lib/js_region_tokenizer.js (the committed dependency the analyzer imports for string/comment-masked parsing)
    - scripts/net_contract_check.js (the structural analog for BOTH new scripts — shebang, header naming gated requirement IDs, one PASS/FAIL line per assertion, all assertions run before exit, `process.exit(1)` only after, self-exclusion of scripts/, the `://` no-comment-strip caveat)
    - src/net/index.js (its export surface — the `net*` function names the analyzer's net-caller detection keys on; derive the list from here, do NOT read it from a scratchpad file)
    - src/main.js (current PP bridge + the four `PP-BRIDGE`-tagged lines module_graph_check.js and ui_contract_check.js will eventually gate)
    - package.json (current `test` script chain to extend)
  </read_first>
  <action>
    Commit the analyzer as `scripts/analyze_classic.mjs`: copy `11-analysis-tool.mjs`, then rewire its `import` of the tokenizer (line 4) to the relative `./lib/js_region_tokenizer.js` and replace the two scratchpad-absolute file paths (the `netfns.txt` read on line 16 and the `analysis.json` write on line 89) — derive the net-function set from `src/net/index.js`'s exports at runtime (or embed it) instead of reading a scratchpad file, and write its JSON output under the phase dir or stdout, never a scratchpad path. It must run standalone (`node scripts/analyze_classic.mjs`) and reproduce the 183-function inventory.

    Build `scripts/module_graph_check.js` (per D-09 — a custom zero-dep cycle-detection scan, chosen over `madge` to honor the milestone's dependency-free stance) mirroring net_contract_check.js's skeleton: parse every `import ... from "./..."` / `"../..."` specifier across `src/**/*.js` (relative only), build the directed graph, DFS with a recursion stack, report any back-edge as a cycle with its path, and assert the expected acyclic SHAPE (shared imports nothing from src/; engine → shared; ui → shared and ui → engine; net → shared; main → engine/ui/net; and specifically NO edge ui → net). It must tolerate `src/ui/` being small/empty early (trivially passes). Wire it into package.json `test` immediately, appended after `state_contract_check.js`.

    Build `scripts/ui_contract_check.js` with the 4 assertions from RESEARCH Q4: (1) no `src/ui/**/*.js` import line resolves into `src/net/` — raw substring match, NO comment-stripping (inherit net_contract_check.js's `://` reasoning); (2) the bridge is gone — the four bridge-tagged lines in src/main.js are absent and the spread onto the global object is absent from src/; (3) no leftover bridge-symbol bare reads — the classic `<script>` region is empty; (4) retained-globals allowlist — the only new non-debug `window.X =` is `window.revealMyRecipe`. Assertions 2 and 3 CANNOT pass while the bridge legitimately still exists, so do NOT wire this script into `npm test` here — 11-07 wires it once the bridge is deleted. Instead red-proof drill all 4 assertions now (run each against a synthetic violation, confirm FAIL), and record the drill in the summary. Keep the self-exclusion of `scripts/`.
  </action>
  <verify>
    <automated>node scripts/analyze_classic.mjs && node scripts/module_graph_check.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/analyze_classic.mjs` exits 0 and reports 183 top-level functions with no absolute/scratchpad paths remaining in the committed file (`grep -c '/private/tmp\|new-session-d6e9d7/scripts' scripts/analyze_classic.mjs` returns 0)
    - `node scripts/module_graph_check.js` exits 0 and prints one PASS line per shape assertion including an explicit "ui does NOT import net" line
    - `npm test` exits 0 with `module_graph_check.js` now in the chain (determinism + engine/net/state contract checks + module-graph check all green)
    - `ui_contract_check.js` red-proof drill: each of the 4 assertions printed FAIL when run against a synthetic violation (transcript captured in the SUMMARY); the script is NOT yet referenced in package.json `test`
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>Analyzer committed and path-clean; module_graph_check wired and green in npm test; ui_contract_check exists and is proven able to fail on all 4 assertions but not yet part of the green gate.</done>
</task>

<task type="tracer">
  <name>Task 2: Tracer — extract the recipe/pastry leaf cluster into src/ui/ end-to-end</name>
  <files>src/ui/recipe.js, src/ui/index.js, src/main.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (the calls/calledBy map for the cluster: escHtml line 7, attachPastryArt 275-277, recipeInfo 280, recipeTitle 281-287, winRecipeSpan 291-295, recipeCardHTML 296-303, recipeModalHTML 308-324, openRecipeModal 326-336, wireRecipeModal 337-361 — confirms these are dependency-closed except for shared/engine/state reads and escHtml's classic callers)
    - index.html lines 7, 275-361 (the exact function bodies being moved — copy verbatim, no reformatting)
    - src/engine/index.js (the Phase 8 code-motion precedent: header/provenance comment shape, `import { ... } from "../shared/index.js"` pattern, verbatim-motion + import-rewiring discipline)
    - src/main.js (the PP bridge to grow with `...ui`, and the debug-hook assignment style to mirror later)
    - src/shared/index.js and src/state/index.js (export surfaces — what recipe.js imports for its bare reads: escHtml uses none but the recipe builders read ingredient/name maps and appState)
  </read_first>
  <action>
    Create `src/ui/recipe.js` with a header comment stating the phase, the directional constraint (never imports `src/net/` — criterion 1), and what purity means here (DOM + game-state read, no net). Move the nine functions (escHtml, attachPastryArt, recipeInfo, recipeTitle, winRecipeSpan, recipeCardHTML, recipeModalHTML, openRecipeModal, wireRecipeModal) byte-identical from index.html into it. Replace their former bare-identifier reads of shared/engine/state symbols with explicit `import { ... } from "../shared/index.js"` / `"../engine/index.js"` / `"../state/index.js"` lines; sibling calls within the cluster (e.g. recipeModalHTML → escHtml, recipeCardHTML) stay direct same-file calls. Export all nine.

    Create `src/ui/index.js` as the UI barrel that `export * from "./recipe.js"`. In `src/main.js`, add `import * as ui from "./ui/index.js"` and add `...ui` to the `PP` object assembly so every moved UI function is republished as a global — this is the strangler-fig mechanism (RESEARCH Q1d) that lets the ~130 still-classic functions keep calling escHtml/openRecipeModal etc. as bare identifiers until the final wave. Delete the nine moved function bodies from the classic `<script>` region. Do NOT delete the bridge, do NOT change engine/net behavior.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/recipe.js` contains `export function recipeModalHTML(` and at least one `import ... from "../shared/index.js"` (or ../engine / ../state) — bare reads replaced by imports
    - `src/ui/recipe.js` contains NO `from "../net` / `from "./net` import (ui↛net)
    - byte-diff of the nine moved bodies (index.html pre-move slice vs recipe.js) shows no reformatting or logic change (Phase-8 pure-motion audit method)
    - `grep -c 'function recipeModalHTML' index.html` returns 0 (moved out of the classic region)
    - `src/main.js` PP assembly now includes `...ui` alongside `...shared, ...engine, ...net`
    - `node scripts/module_graph_check.js` exits 0 (no cycle introduced; ui→shared/engine edges valid)
    - `node scripts/determinism_baseline.js --verify` reports 30/30
  </acceptance_criteria>
  <done>The recipe cluster is in src/ui/recipe.js, the bridge republishes it, the classic region no longer defines those nine functions, and both the graph check and determinism verify are green — the extraction pattern is proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Chrome mechanism gate — tracer build renders</name>
  <what-built>The tracer build: the recipe/pastry cluster extracted into src/ui/recipe.js with the PP bridge grown to `...ui`, game still driven by the same boot path.</what-built>
  <how-to-verify>
    Serve the repo over the local HTTP test server (per MEMORY project_mp_test_harness — never file://). Load index.html in Chrome. Confirm: (1) DevTools console is clean — no ReferenceError, no no-undef; (2) `window.__pp_module_ok === true` and `window.__pp_boot_count === 1`; (3) start a solo game and open the recipe modal (the `revealMyRecipe`/recipe UI) — it renders correctly; (4) play one full turn (sail → dock) and confirm the board and panel render. This is the mechanism gate — it proves a module-scope function extracted out of the classic region still renders through the grown bridge.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the console error / render issue observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console/global scope → app internals | The `PP`/`globalThis` bridge exposes engine/net/state internals as bare identifiers; this phase begins reducing that surface |
| browser DOM → render code | UI functions build DOM from game state; no new untrusted input crosses here (pure internal code motion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | grown PP bridge / moved recipe functions | medium | mitigate | `module_graph_check.js` (no cycle, ui↛net) + `--verify` 30/30 + blocking Chrome load prove the moved cluster resolves and renders |
| T-11-02 | Information disclosure | bridge-published globals | low | accept | game state only, no secrets; Firebase config already public by design; surface shrinks as extraction proceeds |
| T-11-SC | Tampering | package installs | low | accept | no npm/pip/cargo installs — zero-dep maintained; `module_graph_check.js` is a custom script, not `madge` |
</threat_model>

<verification>
- `npm test` green (now includes module_graph_check.js)
- `node scripts/analyze_classic.mjs` reproduces the 183-function inventory, no absolute paths
- `ui_contract_check.js` red-proof drill recorded (4/4 assertions proven able to fail)
- Blocking Chrome checkpoint: clean console, recipe modal renders, one solo turn renders
</verification>

<success_criteria>
Safety net committed and (for module_graph_check) wired green; ui_contract_check red-proof drilled and staged for 11-07; the recipe cluster extracted verbatim with the bridge grown; determinism 30/30; Chrome mechanism gate approved.
</success_criteria>

## Artifacts this phase produces

New files/symbols created across Phase 11 (source-grounding pass: exclude these from drift verification — they are net-new this phase):
- `src/ui/index.js` — UI barrel re-exporting every extracted UI/helper function
- `src/ui/recipe.js` (this plan), `src/ui/util.js` (11-02), `src/ui/board.js` (11-03), `src/ui/panel.js` + `src/ui/lobby.js` (11-04), `src/ui/flow.js` + `src/ui/handlers.js` (11-05)
- `src/main.js` — expanded orchestrator (imports engine+ui+net, wires the 6 injected handlers, calls boot directly; bridge deleted in 11-07)
- `scripts/module_graph_check.js`, `scripts/ui_contract_check.js`, `scripts/analyze_classic.mjs`
- `window.revealMyRecipe` — the single deliberate retained global (11-07), documented in docs/MODULES.md
- Surviving debug hooks (NOT new, NOT part of the deleted bridge): `window.__pp_module_ok`, `window.__pp_boot_count`, `window.__pp_net_debug`, `window.__pp_app_state_debug`

## Flagged Assumptions

- **SPLIT-03/05/06 edge-probe unresolved:** the deterministic edge probe returned all three as `unclassified`/`unresolved` because they are structural-refactor requirements, not input-boundary edges — it correctly abstained rather than fabricating. They are represented here by the concrete RESEARCH Validation-Architecture checks lifted into `must_haves.truths` (module_graph_check, ui_contract_check red-proof, `--verify` 30/30). Not silently dropped.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-01-SUMMARY.md` when done
</output>
