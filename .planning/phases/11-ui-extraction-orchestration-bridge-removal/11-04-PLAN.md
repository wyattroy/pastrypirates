---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - src/ui/panel.js
  - src/ui/lobby.js
  - src/ui/handlers.js
  - src/ui/index.js
  - src/main.js
  - index.html
autonomous: true
requirements: [SPLIT-03]
user_setup: []

must_haves:
  truths:
    - "The panel/clock/narration/chat/modal render cluster (D-02 major entry points setClockUI, panel, showNarration, flash — plus resizePanel, typewriterReveal, narrateLastEvent, appendChatLine, showChatBubble, setNeedsAction, liveRender) lives verbatim in src/ui/panel.js"
    - "The lobby/room view cluster (showRoom, showHome, showGameView, renderSeatList, buildPlayerRows, wireWelcome, requireName, showStep, passGate, hideBootLoader, applyEngineBootstrapEffects) lives verbatim in src/ui/lobby.js"
    - "src/ui/handlers.js exposes a `setNetHandlers({...})` seam; flash and liveRender call injected handlers (onBroadcast / onEvents) instead of importing src/net — src/ui contains no `from \"../net` import (`node scripts/module_graph_check.js` exits 0, ui↛net)"
    - "main wires the injected handlers; because netNarrate/pushEvents are still classic globals this wave, main references them through the bridge temporarily — this wire is formalized to real net imports in 11-06"
    - "byte-diff of the moved bodies shows no reformatting; `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1"
  artifacts:
    - src/ui/panel.js
    - src/ui/lobby.js
    - src/ui/handlers.js
  key_links:
    - "src/ui/handlers.js is the criterion-1 seam: 2 of the 6 UI→orchestration edges (flash→netNarrate, liveRender→pushEvents) are resolved here by handler injection, not by importing net"
    - "main.js wires ui.setNetHandlers(...) — temporarily via bridge globals, formalized to net imports in 11-06"
---

<objective>
Extract the panel/clock/narration/chat/modal render cluster and the lobby/room views into `src/ui/panel.js` and `src/ui/lobby.js`, and establish `src/ui/handlers.js` — the injected-handler seam that resolves the first 2 of the 6 UI→orchestration edges (flash→netNarrate, liveRender→pushEvents) WITHOUT `src/ui/` ever importing `src/net/` (D-07 — the directional criterion-1 boundary).

Purpose: move the DOM cluster that most classic functions call, and stand up the handler-injection mechanism (the Phase 9 net→UI pattern, applied in reverse) that keeps criterion 1 mechanically true as the seam functions leave the classic region.
Output: `src/ui/panel.js`, `src/ui/lobby.js`, `src/ui/handlers.js`, main wiring the seam, shrunk classic region, game runnable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract panel/clock/narration/chat/modal cluster + stand up the handler-injection seam</name>
  <files>src/ui/panel.js, src/ui/handlers.js, src/ui/index.js, src/main.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (`ui (DOM)` tier: setClockUI 1433-1518, panel 1534-1546, resizePanel 1552-1563, typewriterReveal 1590-1632, narrateLastEvent 1755-1769, appendChatLine 1678-1684, showChatBubble 1722-1747, setNeedsAction 1654, flash 1883-1895, liveRender 1521-1531; plus showNarration 1657 from helper/logic — note flash calls netNarrate and liveRender calls pushEvents per RESEARCH Q1b)
    - RESEARCH.md § Q1b (the 6-edge seam table — flash→netNarrate and liveRender→pushEvents are the two edges resolved here)
    - src/net/watchers.js + src/net/index.js (the Phase 9 handler-injection precedent: net publishes, consumer subscribes via injected callbacks — mirror this direction)
    - index.html lines listed above (verbatim bodies)
    - src/ui/board.js + src/ui/util.js (siblings providing render/drawBoard/format helpers these functions call — import them)
    - src/main.js (where ui.setNetHandlers is wired; netNarrate/pushEvents are still classic globals reachable via the bridge this wave)
  </read_first>
  <action>
    Create `src/ui/handlers.js`: a small module holding `let _h = {}` and `export function setNetHandlers(h){ Object.assign(_h, h); }` plus named getters or a shared accessor the UI functions use to reach their injected net operations (e.g. `_h.onBroadcast`, `_h.onEvents`). This is the reverse of the Phase 9 net→UI seam — UI publishes render, main injects the net-adjacent callbacks.

    Create `src/ui/panel.js` (src/ui header + ui↛net constraint). Move setClockUI, panel, resizePanel, typewriterReveal, narrateLastEvent, appendChatLine, showChatBubble, showNarration, setNeedsAction, flash, liveRender verbatim. Rewire bare reads into imports from ./board.js, ./util.js, ../engine, ../shared, ../state. For the two seam functions: replace `flash`'s direct `netNarrate(...)` call with the injected handler (call through `_h.onBroadcast` from handlers.js) and `liveRender`'s direct `pushEvents(...)` call with `_h.onEvents` — do NOT import src/net. Export all; barrel re-exports panel.js + handlers.js.

    In `src/main.js`, call `ui.setNetHandlers({ onBroadcast: (...a) => globalThis.netNarrate(...a), onEvents: (...a) => globalThis.pushEvents(...a) })` — a temporary composition-root wire through the still-present bridge globals, clearly commented as formalized-to-real-net-imports in 11-06. Delete the moved bodies from the classic region; the `...ui` bridge keeps still-classic callers of flash/liveRender/panel resolving.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/panel.js` contains `export function setClockUI(` and `export function flash(` and `export function liveRender(`
    - `src/ui/handlers.js` contains `export function setNetHandlers(`
    - NO file under `src/ui/` contains a `from "../net` / `from "./net` import (ui↛net) — `node scripts/module_graph_check.js` exits 0 and prints its "ui does NOT import net" PASS line
    - flash/liveRender bodies in panel.js reference the injected handler accessor, not a bare `netNarrate`/`pushEvents`
    - `src/main.js` contains a `setNetHandlers(` call wiring onBroadcast/onEvents
    - byte-diff of moved bodies shows no reformatting; `grep -c 'function setClockUI\|function flash\|function liveRender' index.html` returns 0
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>Panel/clock/narration/chat cluster in src/ui/panel.js; the handler-injection seam stood up and wired; 2 of 6 criterion-1 edges resolved without a net import; graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the lobby / room / welcome view cluster into src/ui/lobby.js</name>
  <files>src/ui/lobby.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (`ui (DOM)` tier, net:[] view functions: buildPlayerRows 372-398, showStep 3368-3370, requireName 3371-3376, wireWelcome 3377-3390, renderSeatList 3538-3558, showHome 3432-3436, showRoom 3437-3441, showGameView 3442-3446, passGate 3457-3475, hideBootLoader 3746-3750, applyEngineBootstrapEffects 3751-3761 — all net-free view/DOM functions; NOTE createRoom/joinRoom/watchRoom/startGame/beginGame/wireLobby are net-callers and belong to 11-06, NOT here)
    - index.html lines listed above (verbatim bodies)
    - src/ui/panel.js + src/ui/util.js + src/ui/board.js (siblings these views call — import from them)
    - src/main.js (currently calls `window.applyEngineBootstrapEffects()` and `window.attachPastryArt()` at boot — these keep resolving via the `...ui` bridge after the move; the rewire to a direct import happens in 11-07 at bridge deletion)
  </read_first>
  <action>
    Create `src/ui/lobby.js` (src/ui header + ui↛net constraint). Move the net-free view functions listed above verbatim: buildPlayerRows, showStep, requireName, wireWelcome, renderSeatList, showHome, showRoom, showGameView, passGate, hideBootLoader, applyEngineBootstrapEffects. Rewire bare reads into imports from ./panel.js, ./board.js, ./util.js, ../engine, ../shared, ../state. Do NOT move the room-lifecycle net-callers (createRoom/joinRoom/watchRoom/startGame/beginGame/wireLobby) — those are orchestration (11-06). Export all; barrel re-exports lobby.js. Delete moved bodies from the classic region. Leave main's `window.applyEngineBootstrapEffects()` / `window.attachPastryArt()` boot calls as-is — the bridge still resolves them; they are rewired to imports in 11-07.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/lobby.js` contains `export function showRoom(` and `export function renderSeatList(` and `export function wireWelcome(`
    - `src/ui/lobby.js` contains NO `from "../net` import (ui↛net)
    - `grep -c 'function showRoom\|function renderSeatList\|function wireWelcome' index.html` returns 0
    - the room-lifecycle net-callers (createRoom/joinRoom/watchRoom/startGame/beginGame) are STILL in the classic region (not moved here) — deferred to 11-06
    - byte-diff shows no reformatting; `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30
  </acceptance_criteria>
  <done>Lobby/room/welcome views live in src/ui/lobby.js with import-based deps; net-caller room-lifecycle functions deferred to 11-06; graph + determinism green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → orchestration (the 6-edge seam) | flash/liveRender must NOT reach src/net directly; the handler-injection seam is the enforced boundary (criterion 1) |
| console/global scope → app internals | `...ui` bridge still republishes moved functions; surface unchanged this wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Tampering | criterion-1 UI↛net boundary via handlers.js | high | mitigate | handler injection keeps src/ui net-free; `module_graph_check.js` "ui does NOT import net" assertion is a standing gate on every `npm test`; a regressive `from "../net"` in src/ui fails the build |
| T-11-05 | Tampering | setClockUI / narration render (timer UI) | medium | mitigate | verbatim byte-diff + `--verify` 30/30 (timer control state already de-globalized in Phase 10) |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained |
</threat_model>

<verification>
- `node scripts/module_graph_check.js` exits 0 with the ui↛net PASS line
- `node scripts/determinism_baseline.js --verify` 30/30 after each task
- byte-diff pure-motion audit on every moved function
- flash/liveRender proven to reach net only through injected handlers
</verification>

<success_criteria>
Panel + lobby clusters extracted verbatim; the handler-injection seam resolves 2 of the 6 criterion-1 edges without any net import; graph acyclic; determinism 30/30; room-lifecycle net-callers correctly deferred to 11-06.
</success_criteria>

## Artifacts this phase produces

- `src/ui/panel.js`, `src/ui/lobby.js`, `src/ui/handlers.js` (this plan). Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/main.js` (expanded orchestrator), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, `window.revealMyRecipe` (retained global, 11-07), surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug`.

## Flagged Assumptions

- **battleAsk classification (RESEARCH Q1a #1):** battleAsk (calls 4 net-adjacent fns) is NOT moved here — it is homed in main/flow with injected handlers in 11-05/11-06. Low risk either way.
- The temporary `globalThis.netNarrate`/`globalThis.pushEvents` reference in main's handler wire is an intentional, commented, composition-root-only bridge use — removed when net orchestration modularizes in 11-06.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-04-SUMMARY.md` when done
</output>
