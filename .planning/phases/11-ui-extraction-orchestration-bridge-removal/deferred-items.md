# Deferred Items — Phase 11 (ui-extraction-orchestration-bridge-removal)

## 11-04: pre-existing uncommitted drift in 11-analysis.json

**Found during:** 11-04 execution, initial `git status` check before Task 1's edits.

**Item:** `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json` was
already modified in the working tree (showing a stale 102-function snapshot, most likely
re-generated by `scripts/analyze_classic.mjs` at some point during 11-03's session but never
committed) before this plan's own edits began. This is unrelated to 11-04's `files_modified`
list (`src/ui/panel.js`, `src/ui/lobby.js`, `src/ui/handlers.js`, `src/ui/index.js`,
`src/main.js`, `index.html`).

**Status:** Left untouched and NOT committed by 11-04 (out of scope per the executor's scope
boundary — only auto-fix issues directly caused by the current task's own changes). The
classic-script function count as of 11-04's completion is 80 (was 102 after 11-03); a future
wave that re-runs `analyze_classic.mjs` to refresh this research artifact should capture that
fresh count rather than the stale mid-11-03 one currently sitting uncommitted in the working
tree.

**Action needed:** None blocking. Whoever runs `node scripts/analyze_classic.mjs` next (likely
11-05 or 11-06's own research read) will naturally regenerate this file with current numbers;
at that point the stale diff can simply be committed or discarded.
