---
phase: 01-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - scripts/dlog_replay_test.js
autonomous: true
requirements: [BUG-03, BUG-04]
must_haves:
  truths:
    - "A host whose decision-log replay rebuilt essentially every prior event is treated as a healthy restore and continues silently (no false alarm on a normal refresh)."
    - "A host whose replay rebuilt far fewer events than Firebase had before the reload is detected as incomplete and the failure is visible on screen, not swallowed (D-07 first slice)."
    - "The incomplete/healthy decision is computed by one named function that a Node harness can execute with no browser and no Firebase."
    - "`node scripts/dlog_replay_test.js` exits 0 on a healthy log and proves the detector fires on a truncated one."
  artifacts:
    - "scripts/dlog_replay_test.js — Node vm harness covering the Wave 0 gap from 01-VALIDATION.md"
    - "index.html — `replayShortfall()` pure helper inside a sentinel-delimited, extractable region"
    - "index.html — `endReplay()` calls `replayShortfall()` and branches on the result"
  key_links:
    - "`endReplay()` → `replayShortfall()` → visible failure signal (`#syncnote` banner + console.error)"
    - "`endReplay()` healthy path → `evPushed = resumeEvLen` → `pushEvents()` → guests receive new events"
    - "scripts/dlog_replay_test.js → string-slice extraction of the `replayShortfall` sentinel region out of index.html"
---

<objective>
Tracer slice for the refresh-recovery cluster (BUG-03 / BUG-04): prove end-to-end, headlessly, that an incomplete decision-log replay can be *detected* and *surfaced*, before any modal or Firebase work is built on top of it.

This is a production-quality vertical slice, not a prototype. It touches every layer the recovery fix will touch — the pure detection rule, its call site inside the real `endReplay()` control flow, and a visible user-facing failure signal — using the thinnest possible path (the existing `#syncnote` banner instead of a new modal). Plan 01-02 expands the signal into the full D-07 Resume-anyway / Restart dialog; nothing about that expansion requires re-architecting what this plan lands.

Purpose: today `endReplay()` (around index.html:4646) sets `evPushed=resumeEvLen` unconditionally with zero check that the replay actually got there. That single unconditional line is one of the two independent causes of "guest sees a frozen game" identified in 01-RESEARCH.md. Detection has to exist and be testable before the recovery UX can be trusted.

Output: a new Node harness `scripts/dlog_replay_test.js`, a sentinel-delimited `replayShortfall()` helper in `index.html`, and a wired, calibrated failure branch in `endReplay()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@.planning/phases/01-critical-bug-fixes/01-PATTERNS.md
@.planning/phases/01-critical-bug-fixes/01-RESEARCH.md
@.planning/phases/01-critical-bug-fixes/01-VALIDATION.md
@scripts/real_game_test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end incomplete-replay detection — one path, headlessly verified</name>

  <files>index.html, scripts/dlog_replay_test.js</files>

  <read_first>
  Read these narrow windows only. `index.html` is ~5,200 lines / 328 KB — never open it without an offset+limit.
  - `scripts/real_game_test.js` lines 1-95 (whole file is small) — the vm-extraction idiom, sandbox shape, and `throw new Error(...)` assertion style you are copying.
  - `index.html` around lines 4605-4665 — `encodeDec`/`decodeDec`/`logDecision`/`endReplay`/`pushEvents`. Verify the exact current body of `endReplay()` before editing; 01-PATTERNS.md quotes it as a 5-line function that sets `replaying=false; evPushed=resumeEvLen; liveRender();`.
  - `index.html` around lines 4570-4580 — `netFail()` and the `#syncnote` visible-failure idiom you are reusing for the tracer's signal.
  - `index.html` around lines 5105-5125 — `resumeHostGame()`, to see where `resumeEvLen` is populated. Do NOT change this function in this plan; plan 01-02 owns it.
  </read_first>

  <behavior>
  `replayShortfall(rebuiltEvLen, resumeEvLen, readFailed)` returns an object. Cases the harness must assert:
  - Healthy, exact: `replayShortfall(120, 120, false)` → `incomplete === false`, `shortfall === 0`.
  - Healthy, one in-flight unlogged decision: `replayShortfall(118, 120, false)` → `incomplete === false` (a small shortfall is the DESIGNED behaviour — `ask()`/`pickCell()`/`battleAsk()` all fall through from replay to live the instant `dlogIdx >= dlog.length`, so the currently-in-flight decision and its narration events are legitimately missing).
  - Catastrophic: `replayShortfall(0, 214, false)` → `incomplete === true`, `shortfall === 214` (the empty-dlog failure mode that produces Wyatt's reported "reset to start").
  - Read failure dominates count: `replayShortfall(214, 214, true)` → `incomplete === true` even though the counts match, because a swallowed read error means the counts themselves are not trustworthy.
  - Degenerate: `replayShortfall(0, 0, false)` → `incomplete === false` (a genuinely brand-new game that was never played is not a failure).
  - Never negative: `replayShortfall(300, 120, false)` → `incomplete === false`, `shortfall === 0` (clamped, not `-180`).
  </behavior>

  <action>
  Two edits, landed together as one working slice.

  **(a) Add the pure helper to `index.html`.** Place it immediately above `endReplay()` (around line 4646 — verify before editing). Wrap it in a matched pair of sentinel comments so a string-slice extractor can lift it out deterministically, matching the existing `/* ===== Section ===== */` header convention:

  Open with `/* ===== replayShortfall — extractable region, see scripts/dlog_replay_test.js ===== */` and close with `/* ===== end replayShortfall ===== */`. The harness in part (b) locates the region by these two exact strings, so do not reword them once written.

  Inside the region define exactly two things and nothing else — no DOM access, no Firebase access, no reference to any global. This purity is what makes it testable at all (01-PATTERNS.md §1 flags that the recovery logic otherwise lives entirely outside the vm-extractable region):
  - `const REPLAY_SHORTFALL_TOLERANCE = 4;` — a first-pass value. Task 2 calibrates it empirically; do not treat 4 as final.
  - `function replayShortfall(rebuiltEvLen, resumeEvLen, readFailed)` returning `{shortfall, incomplete, reason}`. `shortfall` is `Math.max(0, resumeEvLen - rebuiltEvLen)`. `incomplete` is `true` when `readFailed` is truthy, or when `shortfall > REPLAY_SHORTFALL_TOLERANCE`. `reason` is a short machine-readable string — use `"read-failed"`, `"short-replay"`, or `"ok"` — so the caller can vary its copy without re-deriving the cause.

  Add a `// notes/edits BUG-03/BUG-04: ...` comment above the region, per the project's punch-list cross-reference convention, explaining why the shortfall is compared against `resumeEvLen` (the Firebase event count captured pre-reload) rather than against `dlog.length`, and why a small shortfall is expected rather than an error.

  **(b) Wire it into `endReplay()`.** Keep the existing `if(!replaying)return; replaying=false;` opening exactly as-is. After `replaying=false`, compute `const sf = replayShortfall(game.events.length, resumeEvLen, false);`. The third argument is hard-coded `false` here on purpose — plan 01-02 owns `resumeHostGame()` and will supply the real read-failure flag through this same parameter. The signature is final; only the argument is a placeholder.

  If `sf.incomplete` is true: do NOT set `evPushed`. Instead call `console.error("replay incomplete", sf)` and reveal the existing `#syncnote` banner the same way `netFail()` does (around line 4574 — read it and mirror the two-line `const note=$("syncnote"); if(note)note.style.display="";` shape rather than inventing a new mechanism), then `return` without calling `liveRender()`'s event flush. Leaving `evPushed` at its pre-replay value is deliberate: it means `pushEvents()` will broadcast from where the rebuilt log actually ended instead of silently suppressing every future event, which is the second independent freeze cause named in 01-RESEARCH.md.

  If `sf.incomplete` is false: preserve today's behaviour byte-for-byte — `evPushed=resumeEvLen; liveRender();`.

  Do not add any `await`, any new Firebase write, or any new render call inside this function. Per 01-RESEARCH.md Pitfall 4, `endReplay()` sits inside the `replaying`-guard convention used at 27+ call sites; new async or broadcast work here double-broadcasts during fast-forward.

  **(c) Create `scripts/dlog_replay_test.js`.** Follow `scripts/real_game_test.js` exactly: `fs`/`path`/`vm`, read `index.html`, `vm.createContext(sandbox)`, `throw new Error(...)` for broken invariants, `console.log` with `String.padEnd()` for the summary. Open with a header comment in the same voice as `real_game_test.js` lines 1-14, explaining that this harness runs the *real* extracted `replayShortfall` rather than a reimplementation.

  It performs two independent extractions:
  1. The `Game` + `roundCfg` region, using the identical boundaries `real_game_test.js` uses (`<script>` start through the index of `function escHtml`), so the harness can build a real `Game`, `play()` it, and read a realistic `g.events.length` instead of inventing magic numbers.
  2. The sentinel region from part (a): slice between the two sentinel comment strings, append `\nthis.replayShortfall=replayShortfall;this.REPLAY_SHORTFALL_TOLERANCE=REPLAY_SHORTFALL_TOLERANCE;`, and run it in its own bare context. `function` and `const` top-level declarations do not attach to the vm context object the way `var` does — export them explicitly, exactly as `real_game_test.js` does for `Game`/`roundCfg`.

  Throw a clear diagnostic if either extraction comes back empty or if the exported values are not a function / not a number — a silently-passing harness whose extraction boundaries drifted is worse than no harness.

  Then assert every case in the `<behavior>` block above, plus one realistic end-to-end case built from the actual played game: take `g.events.length` from a real finished game as `resumeEvLen`, assert `replayShortfall(g.events.length - 1, g.events.length, false).incomplete === false` and `replayShortfall(0, g.events.length, false).incomplete === true`. Print a padded pass/fail summary line per case.
  </action>

  <verify>
    <automated>node scripts/dlog_replay_test.js &amp;&amp; node scripts/real_game_test.js 50</automated>
  </verify>

  <done>
  `node scripts/dlog_replay_test.js` exits 0 and prints a per-case summary covering all six `<behavior>` cases plus the real-game case. `node scripts/real_game_test.js 50` still exits 0, proving the engine region and its extraction boundaries were not disturbed. `endReplay()` sets `evPushed` only on the healthy branch, and an incomplete replay makes the `#syncnote` banner visible.
  </done>

  <reversibility rating="reversible">
  `replayShortfall()` is additive and self-contained; the `endReplay()` change is a guard around one pre-existing line and reverts to today's behaviour by deleting the branch.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Calibrate the shortfall tolerance against real replay shapes</name>

  <files>index.html, scripts/dlog_replay_test.js</files>

  <read_first>
  - `index.html` around lines 3305-3320 — `ask()`'s dlog-consumption branch. This is the pattern (`if(dlogIdx<dlog.length){dlogN++;return ...;} endReplay();`) that defines how many events a single in-flight decision can legitimately leave unreconstructed.
  - `index.html` around lines 3515-3530 and 3728-3740 — `pickCell()` and `battleAsk()` mirror the same branch. Count how many `ev({...})` calls a single decision can produce downstream (a dodge flip emits its own narration event on top of the ask, per 01-RESEARCH.md Open Question 1).
  </read_first>

  <action>
  01-RESEARCH.md Open Question 1 explicitly defers the tolerance value to implementation time: a single in-flight decision can correspond to more than one unreconstructed *event* (a chained storm-dodge turn produces several), so the tolerance is a small number of events, not exactly 1. Too tight and every healthy refresh throws a false alarm; too loose and the empty-dlog failure mode slips through.

  Determine the value empirically rather than guessing. In `scripts/dlog_replay_test.js`, add a calibration section that plays several real games with different seeds and, for each, measures how many events the *last* decision of a turn contributes — i.e. how far `game.events.length` can lag one unconsumed decision. Report the observed maximum. Use `this.r()`-driven game variation only; introduce no new randomness (`Math.random()` is forbidden anywhere near this code per project convention, and this harness must stay reproducible run-to-run — pass fixed seeds).

  Set `REPLAY_SHORTFALL_TOLERANCE` in `index.html` to the observed maximum plus a small safety margin, and replace the placeholder comment with the measured justification: the number observed, the seeds used, and the two failure modes the value separates. The gap between "a few events short" and "hundreds short" is enormous, so an approximately-right value is genuinely sufficient — record that reasoning so a future reader does not mistake the constant for a tuned magic number.

  Add a regression assertion to the harness that pins the boundary: `replayShortfall(N - REPLAY_SHORTFALL_TOLERANCE, N, false).incomplete === false` and `replayShortfall(N - REPLAY_SHORTFALL_TOLERANCE - 1, N, false).incomplete === true`, reading the tolerance from the extracted region rather than hard-coding it, so the assertion tracks the constant if it is ever retuned.
  </action>

  <verify>
    <automated>node scripts/dlog_replay_test.js &amp;&amp; node scripts/real_game_test.js 50</automated>
  </verify>

  <done>
  `REPLAY_SHORTFALL_TOLERANCE` carries a comment stating the measured worst-case single-decision event lag and the seeds it was measured with. The harness asserts both sides of the boundary by reading the constant, and passes. `node scripts/real_game_test.js 50` still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firebase RTDB → host browser | `dlog` and `ev` node contents are read back and used to rebuild authoritative game state; any client in the room can write them |
| host browser → guest browsers | Host decides what gets pushed to `rooms/{room}/ev`; guests render whatever arrives |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | `endReplay()` event broadcast | high | mitigate | The unconditional `evPushed=resumeEvLen` can permanently suppress every future guest broadcast after a short replay — a self-inflicted DoS on the whole room. Task 1 only advances `evPushed` on the healthy branch. |
| T-01-02 | Tampering | `rooms/{room}/ev` child count feeding `resumeEvLen` | medium | accept | Any client can inflate the `ev` node and make a healthy replay look short, producing a false failure banner. Pre-existing (no RTDB rules are visible in this repo per `CONCERNS.md`); this plan adds no new writable node and the worst outcome is a false alarm with a Resume-anyway escape hatch in 01-02. |
| T-01-03 | Repudiation | detection silently passing | medium | mitigate | A harness whose vm-extraction boundaries drift would pass vacuously and hide a broken detector. Task 1 throws an explicit diagnostic when either extraction yields nothing or the wrong type. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | This phase performs zero package-manager installs; 01-RESEARCH.md §Package Legitimacy Audit records no new external packages. No install checkpoint is required. |
</threat_model>

<risks>
**Determinism / lockstep exposure:** MEDIUM-LOW. `endReplay()` sits inside the `replaying`-guard convention documented at 27+ call sites (`CONCERNS.md` §"Replay Mechanism Complexity"). The change is a pure read of `game.events.length` plus a branch — it introduces no `await`, no new Firebase write, and no new render call, so it cannot reorder decision consumption or double-broadcast during fast-forward. `replayShortfall()` touches no globals and consumes no RNG, so it cannot perturb the mulberry32 sequence that replay correctness depends on.

**Residual risk:** the incomplete branch deliberately leaves `evPushed` untouched, which changes what guests receive in the failure case. That is the intended fix, but it means the failure path is now the *only* path where guest event indices differ from today's behaviour — plan 01-02's two-tab reproduction must confirm guests do not receive duplicate events after a Resume-anyway.

**Mitigating factor:** every edit in this plan is behind an automated headless gate, so a regression is caught in ~2 seconds rather than during live multiplayer.
</risks>

<verification>
- `node scripts/dlog_replay_test.js` — exits 0, all cases reported
- `node scripts/real_game_test.js 50` — exits 0 (engine + extraction boundaries intact)
- Open `index.html` in a browser and confirm the page still loads and a solo game starts (a syntax error inside the sentinel region would break the entire single-file script block; the Node harness extracts a slice and would not necessarily catch a stray brace outside it)
</verification>

<success_criteria>
- A named, pure, vm-extractable `replayShortfall()` exists in `index.html` and is the single place the healthy/incomplete decision is made
- `endReplay()` branches on it, and only the healthy branch advances `evPushed`
- An incomplete replay is visible on screen via the existing `#syncnote` banner rather than silently swallowed
- `scripts/dlog_replay_test.js` covers the Wave 0 gap named in `01-VALIDATION.md` and runs in under ~5 seconds
- The tolerance constant is justified by a recorded measurement, not a guess
</success_criteria>

<output>
Create `.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md` when done. Record the measured tolerance value and its justification, and the exact sentinel comment strings, so plan 01-02 and the final strip plan can locate the region without re-deriving it.
</output>
