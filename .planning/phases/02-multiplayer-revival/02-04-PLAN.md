---
phase: 02-multiplayer-revival
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - 4/src/orchestrator.js
  - 4/src/ui/flow.js
  - 4/src/ui/util.js
autonomous: true
requirements: [FIX-03]
user_setup: []

must_haves:
  truths:
    - "A captain whose typed name contains angle brackets cannot break, blank or hijack another captain's action panel"
    - "Prompts still render their intended markup — coin images, ingredient chips, bolded captain names and no-break spans all survive unchanged on both host and guest"
    - "The host's prompt renderer and the guest's prompt renderer still produce identical markup for identical input — the two copies did not diverge"
    - statement: "Whether the reported fault reproduces was established by observing a guest's live DOM, not by reading the source"
      verification: backstop
  artifacts:
    - "Either a source-tier escaping fix covering both prompt renderers, or a recorded non-reproduction with the evidence that settled it"
  key_links:
    - "pname() at 4/src/ui/util.js:247-257 already routes typed names through escHtml, and pn() wraps it — so the player-authored value may already be escaped upstream of both renderers"
    - "escHtml at 4/src/ui/recipe.js:24 is the ONLY exported escaper in 4/; the three esc/escW helpers in util.js, flow.js and orchestrator.js are function-local and cover attribute values only"
    - "4/src/orchestrator.js:1262-1266 states the guest renderer is a genuine second copy of localAsk's markup — a change to one that skips the other reintroduces the bug on whichever side was forgotten"
  prohibitions:
    - "must not blanket-escape a prompt message, label or sub-line — these legitimately carry markup, and escaping them wholesale renders the game's own tags as visible text"
    - "must not fix one prompt renderer without the other; host and guest copies must stay byte-comparable in the markup they emit"
    - "must not change what 4/src/orchestrator.js puts on the wire — the remote prompt payload shape is untouched by this phase"
    - "must not change what 4/src/engine/index.js emits"
---

<objective>
Settle FIX-03's third named site: unescaped host-supplied HTML rendered into a guest's prompt. The
site is real code that has never executed, but the fault as written down may already be closed
upstream — and the naive fix would break every prompt in the game. This plan finds out which, by
measurement, and then does the smaller of the two right things.

Purpose: this is the only one of FIX-03's three sites that is not a straightforward crash guard, and
it is the one where a confident wrong fix does visible damage.

Output: either one escaping fix applied at the source and covering both prompt renderers, or a
recorded non-reproduction with the evidence — and in both cases, an entry for `02-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-multiplayer-revival/02-CONTEXT.md
@.planning/phases/02-multiplayer-revival/02-RESEARCH.md
@.planning/phases/02-multiplayer-revival/02-VALIDATION.md
@.planning/phases/02-multiplayer-revival/02-02-SUMMARY.md
@.claude/CLAUDE.md
@docs/HARD-WON-LESSONS.md
@docs/DRIVING-THE-GAME.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find out whether it reproduces — on a real guest's DOM, not in the source</name>
  <files>&lt;scratchpad&gt;/probe-fix03-esc.mjs (no production file — this task measures only)</files>

  <read_first>
    - `4/src/orchestrator.js` lines 1219-1285 — the whole of `watchPrompt`. Note the comment at
      1262-1266: this renderer is deliberately a **second copy** of the host's, and the two must match.
    - `4/src/ui/flow.js` lines 220-268 — `localAsk`, the first copy. Compare the two interpolations
      side by side and list every value each one drops into markup: the message, the labels, the
      sub-line, the reason attribute, the colours and classes.
    - `4/src/ui/util.js` lines 243-270 — `pname()` and `pn()`. **Read these before concluding
      anything.** `pname()` already routes a typed name through `escHtml`, and `pn()` wraps its result.
      If the player-authored value is escaped here, it is escaped in both renderers and in narration
      and in the captains box, and the fault as reported does not exist at the sink.
    - `4/src/ui/recipe.js` line 24 — `escHtml`, the only exported escaper in `4/`. CONTEXT.md and
      RESEARCH.md both say "`esc()` already exists in `4/src/ui/util.js`"; what is actually there at
      util.js:1639 is a **function-local** arrow inside `voyageAground()`, not an export. Treat the
      cited helper name as drift and use the real export.
    - `4/src/ui/lobby.js` lines 42-44 and around 350 — two comments that already state the escaping
      route as `pn() -> pname() -> escHtml`. That is the established pattern; a second escaping
      mechanism would be the thing to avoid.
    - `4/src/ui/panel.js` around line 1029 — `appendChatLine`, which does it the right way already:
      the seat name through `pn()`, the free text through `escHtml`.
    - `4/src/orchestrator.js` around line 1417 — where a joining captain's typed name is taken and
      length-clamped, so the probe injects at the real entry point rather than writing the node by hand.
    - `docs/HARD-WON-LESSONS.md` §2 — do not trust your own reasoning over a measurement, and verify
      against an independent path rather than against the suspect itself.
  </read_first>

  <action>
    Establish, by observation, whether any player-authored string reaches a guest's action panel as
    live markup. Do not settle this by reading the source: the source has two renderers, three
    function-local escapers with different coverage, and one exported escaper reached through two
    layers of indirection, and the last session to read it wrote down a helper that does not exist.

    Join a real game from plan 01's rig with a captain name that contains angle brackets and an
    attribute-shaped payload — something that would be structurally obvious in the DOM if it were
    parsed as markup, and harmlessly visible as literal text if it were not. Then drive the game to a
    prompt on the **other** browser that names that captain, and read the guest's `#actionPanel`
    subtree: the element count, the tag names present, and whether the payload appears as a text node
    or as parsed structure. A text node is the answer "already escaped"; a new element is the answer
    "reproduces".

    Do the same for the two other player-influenced values that reach the same markup: an option label
    and the sub-line. These travel a different route from the captain name and may not share its
    escaping.

    Record the finding either way, in the exact words of what you observed. **A non-reproduction is a
    real result and is not a failure of this task** — REQUIREMENTS.md's FIX-03 text and the ROADMAP's
    evidence block were both written from the intake report, which itself warns its citations have
    drifted, and RESEARCH.md's own assumptions log flags this site as the least verified of the three.

    Note also, for the findings document, what a naive fix here would have cost: prompt messages
    legitimately carry markup — the coin image, ingredient chips, the bolded captain name, the
    no-break span that keeps a name and its payout on one line — so escaping the message wholesale
    would render the game's own tags as visible angle brackets in every prompt on both sides. Say so,
    with an example of a real prompt string that would have broken.
  </action>

  <verify>
    <automated>node &lt;scratchpad&gt;/probe-fix03-esc.mjs</automated>
    The probe reports, for each of the three values, whether the injected payload appears in the
    guest's `#actionPanel` as a text node or as parsed element structure.
  </verify>

  <acceptance_criteria>
    - The probe is red-proofable: it demonstrates it can detect parsed markup at all, by injecting the same payload through a rendering path known to be unescaped and observing the element structure appear. A detector that reports "text node" for everything cannot distinguish escaped from broken.
    - For each of the three values — captain name, option label, sub-line — the summary states which of the two outcomes was observed, and quotes the DOM reading that settled it.
    - The reply names the exact escaping route each value travels, in the form used by the existing comments in `4/src/ui/lobby.js`, and says which function actually performs the escape.
    - The summary corrects the record: it states plainly that no exported `esc()` exists in `4/src/ui/util.js` and names `escHtml` in `4/src/ui/recipe.js` as the real export, so the next reader does not repeat the search.
    - No production file was modified by this task — `git diff --name-only` under `4/` is empty.
    - Zero `remote-debugging-port` and zero `http.server` processes remain, stated in the reply.
  </acceptance_criteria>

  <reversibility rating="reversible">
    An investigation with no production change.
  </reversibility>

  <done>
    It is known, from a guest's live DOM, whether a captain's typed name or a prompt's label or
    sub-line reaches another player's panel as markup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close it at the source, or close the record — whichever the measurement earned</name>
  <files>4/src/ui/util.js, 4/src/orchestrator.js, 4/src/ui/flow.js (fix branch only — the no-change branch touches none of them)</files>

  <read_first>
    - `.planning/phases/02-multiplayer-revival/02-04-SUMMARY.md` in progress, or task 1's recorded
      finding — this task's whole shape depends on it.
    - `scripts/host_guest_parity_check.js` — read what it asserts about prompt-class parity between the
      two renderers. It is hardcoded to root `src/` paths and **must not be ported** (that is Phase 3 /
      TEST-06), but its assertions name exactly the divergence a one-sided fix would create.
    - `4/src/ui/util.js` lines 243-270 again, now as a possible fix site rather than a fact to check.
  </read_first>

  <action>
    **If task 1 found the payload reaching a guest's DOM as parsed markup:** fix it once, at the source
    of the player-authored value, not at each sink. The source fix is the one that also covers
    narration, the captains box, the seat list and the decision log in the same change — fixing one of
    two prompt renderers leaves the other broken and creates exactly the host/guest divergence the
    comment above the guest renderer warns about. Reuse `escHtml`; do not add a fourth escaping helper
    to a file that already has three function-local ones with three different coverage sets. Then
    re-run task 1's probe and confirm the payload now reads as a text node on **both** sides, and
    confirm a normal prompt still renders its intended markup — the coin image, the ingredient chips
    and the bolded name must all still be elements, not visible angle brackets.

    **If task 1 found the payload already escaped:** change no code, and say so plainly. Wyatt's
    standing ruling covers this case without needing to be asked again: `/4` is `noindex, nofollow`
    with `Disallow: /4/`, he is the only player, and — in his words on 2026-08-19 — a protection whose
    only beneficiary is a hypothetical stranger is protection worth nothing. Adding defence-in-depth
    escaping to a path already escaped upstream would be the third gating scheme proposed in this
    phase and would be rejected for the same reason as the first two. Record the non-reproduction in
    the summary with the evidence, note that REQUIREMENTS.md's FIX-03 names three sites while
    CONTEXT.md's Integration Points lists four under a heading saying three, and hand both to
    `02-FINDINGS.md` for plan 07.

    In either branch, do not touch the remote prompt payload shape on the wire, and do not touch the
    engine. Phase 3 captures the determinism corpus immediately after this phase.
  </action>

  <verify>
    <automated>node &lt;scratchpad&gt;/probe-fix03-esc.mjs</automated>
    Re-run of task 1's probe. In the fix branch it must flip from parsed-structure to text-node, and a
    second assertion must confirm a normal prompt's intended elements are still elements. In the
    no-change branch the probe's result is unchanged and is recorded as the closing evidence.
  </verify>

  <acceptance_criteria>
    - Exactly one of the two branches was taken, and the summary names which and quotes the task 1 evidence that selected it.
    - **Fix branch only:** driving a prompt whose captain name carries an attribute-shaped payload leaves that payload as a text node in `#actionPanel` on the guest, and the same on the host.
    - **Fix branch only:** a normal prompt still renders its markup — the probe counts `img` and `b` elements inside `.apMsg` for a prompt known to contain a coin image and a bolded captain name, and the counts match the pre-fix counts exactly.
    - **Fix branch only:** the host and guest renderers emit comparable markup for the same input — the probe captures `#actionPanel` innerHTML on both sides for one identical prompt and the difference is limited to values that are legitimately per-seat.
    - **No-change branch only:** `git diff --name-only` under `4/` is empty for this plan, and the summary carries the evidence and the two-versus-three source inconsistency for `02-FINDINGS.md`.
    - `git diff 4/src/engine/index.js` is empty in both branches.
    - No file under `scripts/` was ported, copied or modified.
    - Zero `remote-debugging-port` and zero `http.server` processes remain, stated in the reply.
  </acceptance_criteria>

  <reversibility rating="costly">
    The fix branch touches a helper that every name-rendering surface in the game reads, so a wrong
    fix is visible everywhere at once rather than in one panel — which is exactly why it is applied at
    the source with the prompt-markup counts asserted, and why the no-change branch is a legitimate
    outcome rather than a shortfall.
  </reversibility>

  <done>
    FIX-03's third site is either closed in code or closed in the record, with the measurement that
    decided it written down.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a captain's typed name → every other captain's DOM | Free text entered at `#joinName` propagates through the roster into prompts, narration, the seat list and the log on every client |
| host browser → guest `#actionPanel` | The host composes prompt message, labels and sub-line; the guest renders them as markup by design, because they legitimately contain the game's own tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | `watchPrompt` / `localAsk` — player-authored text interpolated into panel markup | high | mitigate | Tasks 1-2 — establish reproduction against a live guest DOM, then fix at the source through `escHtml` so both renderers are covered by one change. If already escaped upstream, close in the record with the evidence. |
| T-02-12 | Tampering | Divergence between the two prompt renderers — a one-sided fix | medium | mitigate | Task 2 — the fix lands at the shared source, and the probe compares both sides' emitted markup for one identical prompt |
| T-02-13 | Denial of Service | A captain naming themself something enormous or structurally hostile blanks another player's panel | medium | mitigate | Covered by the same source fix; the existing 40-character clamp at the join entry point bounds the length half |
| T-02-02 | Information Disclosure | Every client can read every rival's recipe | medium | accept | Deferred to Phase 4 by CONTEXT.md; the pending todo was reviewed and deliberately not folded. Plan no work. |
| T-02-07 | Denial of Service | `remotePrompt` never resolves if a target guest neither answers nor disconnects cleanly | medium | accept | Wyatt's ruling, 2026-08-19: write it down, do not fix it. A countdown is ruled out by Phase 1's D-03 and presence detection is Phase 4's MP-13. No code task exists for it; plan 07 records it as the headline findings entry. |
| T-02-SC | Tampering | Supply chain — package installs | low | accept | This phase installs zero npm, pip or cargo packages; no package-manager surface, no legitimacy audit to run |
</threat_model>

<artifacts_this_phase_produces>
This plan creates no new element IDs or CSS classes. In the fix branch it may modify the existing
`pname()` / `pn()` escaping route in `4/src/ui/util.js` and the two prompt renderers in
`4/src/orchestrator.js` and `4/src/ui/flow.js`; in the no-change branch it produces only a findings
entry. It produces one scratchpad probe, `probe-fix03-esc.mjs`, throwaway, uncommitted, wired into no
test runner.

It also produces a **correction to the record** that later phases depend on: `escHtml` in
`4/src/ui/recipe.js:24` is the only exported escaper in `4/`; there is no exported `esc()` in
`4/src/ui/util.js`, despite CONTEXT.md and RESEARCH.md both citing one.

The full phase artifact list is enumerated in `02-01-PLAN.md`.
</artifacts_this_phase_produces>

<flagged_assumptions>
No SPEC.md exists, so the spec-less edge probe returned **0 resolved, 7 unresolved** — every
requirement `unclassified`. None is auto-backstopped or dropped; all seven are surfaced here. Real
coverage is `02-VALIDATION.md`'s Per-Task Verification Map.

| Requirement | Probe verdict | Status |
|---|---|---|
| MP-01 | unclassified — review manually | unresolved |
| MP-02 | unclassified — review manually | unresolved |
| MP-03 | unclassified — review manually | unresolved |
| MP-10 | unclassified — review manually | unresolved |
| MP-11 | unclassified — review manually | unresolved |
| MP-12 | unclassified — review manually | unresolved |
| FIX-03 | unclassified — review manually | unresolved |

Two further assumptions specific to this plan, both flagged rather than resolved during planning:
`02-RESEARCH.md`'s claim that `esc()` exists as a reusable helper in `4/src/ui/util.js` was checked
during planning and is **wrong** — the file's `esc` is function-local to `voyageAground()`. And
RESEARCH.md's Code Facts table itself flags this site as the one whose cited line range did not match
the actual interpolation, which is why task 1 measures rather than reads.
</flagged_assumptions>

<verification>
- The finding came from a guest's live DOM, not from reading the source — `docs/HARD-WON-LESSONS.md`
  §2, verify against an independent path, never against the suspect itself.
- The detector was shown able to report the parsed-markup outcome before its escaped-outcome reading
  was believed.
- No file under `scripts/` was ported. Formal parity gates for `4/` are Phase 3 / TEST-06.
- A green root `npm test` says nothing about `4/` and is not cited (`docs/HARD-WON-LESSONS.md` §3).
</verification>

<success_criteria>
FIX-03's third named site has a settled disposition backed by a measurement: either escaped at the
source with both renderers covered and every prompt still rendering its intended markup, or recorded
as not reproducing with the evidence and the source inconsistency handed to `02-FINDINGS.md`.
</success_criteria>

<output>
Create `.planning/phases/02-multiplayer-revival/02-04-SUMMARY.md` when done. Whichever branch was
taken, write the evidence in full — this is the single most likely thing in the phase for a later
session to re-litigate, and the summary is what stops it.
</output>
