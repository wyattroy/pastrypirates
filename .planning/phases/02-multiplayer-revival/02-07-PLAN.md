---
phase: 02-multiplayer-revival
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - .planning/phases/02-multiplayer-revival/02-FINDINGS.md
  - 4/src/ui/stage.js
autonomous: false
requirements: [MP-01, MP-02, MP-03, MP-10, MP-11, MP-12, FIX-03]
user_setup: []

must_haves:
  truths:
    - "02-FINDINGS.md records what actually broke during the shakeout, in the words of the error, not what the research predicted"
    - "The remotePrompt timeout gap is the headline findings entry, with the Phase-1 D-03 and Phase-4 MP-13 reasoning attached, and no code was written for it"
    - "Phases 4 and 5 can be scoped from the findings document without re-deriving anything"
    - "playpastrypirates.com/4 serves a build carrying a new PP4_STAMP, and Wyatt has been told which stamp to look for"
    - "The pushed diff touches only 4/ and .planning/ — no site-identity file moved and no root game file changed"
    - "D-09: Claude shook it out headless first so Wyatt is not the crash detector, but the pass is Wyatt playing a real voyage on his phone — nothing in this phase closes on headless evidence alone"
    - statement: "main and origin/main are zero ahead and zero behind in both directions after the push, re-run rather than remembered"
      verification: backstop
  artifacts:
    - ".planning/phases/02-multiplayer-revival/02-FINDINGS.md"
    - "A bumped PP4_STAMP in 4/src/ui/stage.js:32"
  key_links:
    - "4/ on main IS what playpastrypirates.com/4 serves — no build step, so a push is the deploy and the stamp is the only proof it arrived"
    - "The four named candidates the ROADMAP asks about — guest-side stage, the guest rim sweep, the greyed-button reasons, the remotePrompt gap — are what Phases 4 and 5 are scoped from"
  prohibitions:
    - "must not write code for the remotePrompt timeout — Wyatt ruled on 2026-08-19: write it down, do not fix it"
    - "must not let CNAME, robots.txt or sitemap.xml leave this repo, and must not hand-roll a deploy sync"
    - "must not bump PP4_STAMP more than once in this phase — Wyatt chose ONE drop and one test voyage"
    - "must not report git state from memory or from earlier in the session; re-run the command"
    - "must not close this phase on headless evidence, however green"
---

<objective>
Write the finding, then hand Wyatt one build to play.

The ROADMAP says this phase produces a finding, not just a feature — Phases 4 and 5 are scoped from
it. And Wyatt chose ONE drop: Firebase, the cards, the fault fixes and chat all land together and he
tests once. That makes this the only stamp bump in the phase and the only hand-off.

Purpose: everything before this proved the revival headlessly. Nothing in this phase closes on
headless evidence, so this is where it reaches a phone.

Output: `02-FINDINGS.md`, one build stamp, and a message telling Wyatt exactly what to look for.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multiplayer-revival/02-CONTEXT.md
@.planning/phases/02-multiplayer-revival/02-RESEARCH.md
@.planning/phases/02-multiplayer-revival/02-VALIDATION.md
@.planning/phases/02-multiplayer-revival/02-01-SUMMARY.md
@.planning/phases/02-multiplayer-revival/02-02-SUMMARY.md
@.planning/phases/02-multiplayer-revival/02-03-SUMMARY.md
@.planning/phases/02-multiplayer-revival/02-04-SUMMARY.md
@.planning/phases/02-multiplayer-revival/02-05-SUMMARY.md
@.planning/phases/02-multiplayer-revival/02-06-SUMMARY.md
@.claude/CLAUDE.md
@docs/GIT-AND-DEPLOY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write down what actually broke</name>
  <files>.planning/phases/02-multiplayer-revival/02-FINDINGS.md</files>

  <read_first>
    - All six preceding summaries in this phase. They are the source; this task synthesises rather
      than re-investigates.
    - `.planning/ROADMAP.md` → "Phase 2: Multiplayer Revival", the paragraph beginning "This phase
      produces a finding" — it names the four candidates the document owes an answer on.
    - `.planning/phases/02-multiplayer-revival/02-CONTEXT.md` § Claude's Discretion — the findings
      document's shape and location are Claude's call; the suggested path is the one in frontmatter.
    - `.planning/phases/01-before-the-engine-freezes/01-CONTEXT.md` D-03 — the shot clock is
      deliberately NOT how a dropped player is handled. This is half the reasoning the `remotePrompt`
      entry has to carry.
    - `.planning/REQUIREMENTS.md` around line 39 — MP-13, the presence-loss fallback, which is the
      other half and is Phase 4's.
    - `4/src/orchestrator.js` around line 1142 — `remotePrompt` itself, so the entry cites the real
      shape rather than a drifted line range.
    - `.claude/CLAUDE.md` §5 "Three conventions that keep the record honest" — point, don't restate;
      never hand-type a number that can be counted; no future tense in an append-only record.
  </read_first>

  <action>
    Write `02-FINDINGS.md`. It is a real deliverable with a real audience: the sessions that plan
    Phases 4 and 5, who will otherwise re-derive all of this from the code.

    Lead with the fourth latent fault, because it is the entry with the most reasoning attached and
    the one most likely to be re-litigated. `remotePrompt` waits on a guest's answer with nothing that
    ends the wait if the guest neither answers nor disconnects cleanly. **No code was written for it,
    by Wyatt's ruling of 2026-08-19**, and the entry must carry why so that Phase 4 does not
    rediscover it: a countdown is ruled out by Phase 1's D-03, which established that the shot clock
    is deliberately not the mechanism that handles a dropped player, and presence detection is already
    Phase 4's MP-13. Record the source inconsistency alongside it — REQUIREMENTS.md's FIX-03 names
    three sites while CONTEXT.md's Integration Points lists four under a heading saying three — and
    state plainly that FIX-03 in this phase meant the three named sites.

    Then answer the ROADMAP's three named candidates with what the shakeout actually observed:
    guest-side rendering of the new stage, which had never executed on a guest tier at all; the rim
    sweep on the guest and whether its documented accepted lag is what really happens; and the
    greyed-button reasons, the second copy of the host's markup where a divergence would show.

    Then everything else the shakeout found, in the words of what happened. Two categories that both
    matter: what broke, and **what worked when it was expected not to** — a research prediction that
    did not come true is as useful to Phase 4 as a crash. Carry forward plan 04's correction to the
    record about which escaping helper actually exists, because the next session to look will
    otherwise search for one that does not.

    Honour the three conventions. Point at the summaries rather than restating their verdicts — a
    pointer cannot go stale and a copy always can. Do not type a count that can be counted. Write it
    in the past tense throughout; this is a record of what happened, and a prediction left in a log
    rots into a lie with nobody editing it.

    Write it in plain English. Wyatt reads this, and he directs the work as a designer, not by reading
    the diff. Say what breaks for a player and then how you know.
  </action>

  <verify>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs validate health</automated>
    Run before the document is considered done, and surface what it finds in the reply. Treat the
    pass/fail verdict as close to meaningless — the value is in what it incidentally shows. A warning
    read and dismissed is fine; a warning nobody looked at is not.
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02-multiplayer-revival/02-FINDINGS.md` exists and its first substantive entry is the `remotePrompt` gap.
    - That entry names Phase 1's D-03 and Phase 4's MP-13 as the reasons no code was written, and states that no code was written.
    - It records the three-versus-four source inconsistency between REQUIREMENTS.md and CONTEXT.md, and states that FIX-03 in this phase meant the three named sites.
    - It answers all three ROADMAP-named candidates — guest-side stage, guest rim sweep, greyed-button reasons — each with an observation from the shakeout rather than a prediction.
    - It carries a section for predictions that did not come true, and that section is not empty unless the summaries genuinely contain none.
    - It carries plan 04's correction: `escHtml` in `4/src/ui/recipe.js` is the only exported escaper in `4/`, and no exported `esc()` exists in `4/src/ui/util.js`.
    - `git diff 4/` is empty for this task — writing the findings changes no game code.
    - The health check was run and its output is surfaced in the reply, with any warning either dismissed for a stated reason or acted on.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A document. Its cost if wrong is that Phase 4 plans from a wrong record, which is why it reports
    observations rather than predictions.
  </reversibility>

  <done>
    A session planning Phase 4 can read one document and know what is really broken in `4/`'s
    multiplayer, without re-running anything.
  </done>
</task>

<task type="auto">
  <name>Task 2: The one drop — bump the stamp, prove the diff, push, and tell Wyatt what to look for</name>
  <files>4/src/ui/stage.js</files>

  <precondition>
    `git fetch origin` has been run in this task, and `git rev-list --count origin/main..main` and
    `git rev-list --count main..origin/main` both report 0 before anything is committed. Both `main`
    and `origin/main` are local caches and neither is the remote; a stale ref once put a session 457
    commits behind and produced a confident, entirely wrong conclusion. Do not report this from memory
    or from earlier in the session — re-run the commands.
  </precondition>

  <read_first>
    - `docs/GIT-AND-DEPLOY.md` §5 — the full loop and the incident that earned it.
    - `.claude/CLAUDE.md` §6 — how the work reaches Wyatt's phone, and the tell that a session skipped
      it: he reports an old build stamp. It is never a cache; there is no build step.
    - `4/src/ui/stage.js` line 32 — `PP4_STAMP`, currently the Phase 1 build, and line 929 where it is
      rendered into the footer so he can read it.
    - `.planning/STATE.md` → Current Position, which records the Phase 1 stamp, so the new one is
      visibly a successor rather than a guess.
    - `scripts/deploy-preview.sh` — read its `EXCLUDES` list. Nothing in this phase should require
      touching it, and confirming that is the check.
  </read_first>

  <action>
    Wyatt chose ONE drop. Everything from this phase — the Firebase tags, the four welcome cards, the
    fault fixes and chat — reaches him together, in one build, which he tests once.

    Bump `PP4_STAMP` in `4/src/ui/stage.js` to a new value following the existing date-plus-letter
    convention, succeeding the Phase 1 stamp recorded in STATE.md. This is the phase's only stamp
    bump: intermediate plans changed code but a stamp with no drop behind it is the one thing the
    stamp exists not to do, and two stamps would mean two hand-offs where he asked for one.

    Then prove the diff before pushing, not after. List the changed paths and confirm every one falls
    under `4/` or `.planning/`. `CNAME`, `robots.txt` and `sitemap.xml` must not appear — `CNAME`
    contains the live domain and GitHub Pages reads it as a claim on that domain, so a copy in a second
    place takes the live game down for real players. Two sessions came within one command of doing
    exactly that, both by hand-rolling a sync. Do not hand-roll a sync here; nothing in this phase
    needs one.

    Also confirm no root game file changed. The root game and `4/` are different files serving
    different URLs, and this phase has no business touching the game real players are playing.

    Push, then pull, then re-run both counts and confirm zero in both directions. Then tell Wyatt, in
    plain English, in the reply he reads:

    - the exact stamp to look for in the footer, and where the footer is;
    - that Host a Crew and Join a Crew are back on the welcome screen, and that four cards now wrap
      two-by-two on a phone;
    - that chat is a 💬 button in the top ribbon beside ☰, opening a slide-up sheet, with a dot when
      something is unread and a brief flash under the ribbon when a message arrives;
    - that the bake-off should be switched off for the test voyage with `?bakeoff=0`, and what that
      URL looks like in full;
    - that he needs a second player or a second device, and the room code is how they join;
    - what to watch for, in his terms — does the guest's board keep up, does the recipe draft survive,
      does the chat flash arrive without hijacking the turn.

    Include the full URL with the query string in that same message. He should never have to scroll up
    or assemble it himself.

    Kill anything still running before replying.
  </action>

  <verify>
    <automated>git fetch origin &amp;&amp; git rev-list --count origin/main..main &amp;&amp; git rev-list --count main..origin/main</automated>
    Both must print 0 after the push and pull. Plus: `git diff --name-only HEAD~1 HEAD` lists only
    paths under `4/` and `.planning/`.
  </verify>

  <acceptance_criteria>
    - `4/src/ui/stage.js` line 32's `PP4_STAMP` differs from the Phase 1 value recorded in `.planning/STATE.md`, and the new value follows the same date-plus-letter convention.
    - `grep -c 'PP4_STAMP = ' 4/src/ui/stage.js` returns 1 — one definition, not two.
    - `git diff --name-only origin/main...main` lists no path outside `4/` and `.planning/`, and the executor pastes the list into the reply.
    - `CNAME`, `robots.txt` and `sitemap.xml` appear nowhere in that list, and no file was copied to any location outside this repository. <!-- planner-discipline-allow: CNAME, robots.txt, sitemap.xml — the three site-identity filenames must be named in the action (that IS the safety rule) and asserted absent from the diff; the grep runs over `git diff --name-only` output, never over this plan file -->
    - `scripts/deploy-preview.sh` was not invoked and no synchronisation to any other directory was performed — this phase publishes by pushing `main`, nothing else.
    - `git rev-list --count origin/main..main` and `git rev-list --count main..origin/main` both return 0, run after the push and pull, in this task, not recalled.
    - The reply states the exact build stamp string, gives the full `/4` URL including `?bakeoff=0`, and lists what to look at — in plain English, with no engineering jargon.
    - Zero `remote-debugging-port` and zero `http.server` processes remain, stated in the reply.
  </acceptance_criteria>

  <reversibility rating="costly">
    A push to `main` is live immediately — `/4` is served from `main` with no build step, so there is
    no staging step between the push and Wyatt's phone. It is revertible by another push, but the
    expensive part is not the code: he has one test voyage of attention to spend, and a crash spends
    it. That is what the whole headless shakeout in plan 06 exists to protect.
  </reversibility>

  <done>
    `playpastrypirates.com/4` is serving the revival, and Wyatt knows the stamp, the URL and what to
    look at.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wyatt plays a real voyage — this is the phase gate</name>
  <files>none — this is a human verification checkpoint</files>

  <what-built>
    Multiplayer is back at `playpastrypirates.com/4`. Host a Crew and Join a Crew are on the welcome
    screen again — four cards now, wrapping two-by-two on a phone. Three crashes that sat on
    never-run networking code are closed, including the one that killed every guest during the recipe
    draft. There is no skip button in a crew game. And chat has a home: a 💬 button in the top ribbon
    beside ☰ that opens a slide-up sheet, with a dot when something is unread and a brief flash under
    the ribbon when a message arrives while the sheet is shut.

    Everything headless is green, but headless evidence does not close this phase. Four faults sat on
    code that had never executed once, and the pass is you playing a real voyage.
  </what-built>

  <how-to-verify>
    Open the URL in the reply above — `playpastrypirates.com/4/?bakeoff=0` — and check the footer
    reads the new build stamp named in that message. If it reads the old one, stop and say so; that is
    never a cache, and it means the work did not arrive.

    1. **The welcome screen.** Four cards: Play Solo and Pass & Play on top, Host a Crew and Join a
       Crew beneath. Does the two-by-two grid look right on your phone, and is the order right?
    2. **Host and join.** Tap Host a Crew, get a code, and have a second player (or a second device)
       join with it. Do you both land in the same room with the right names?
    3. **Sail a voyage together.** The recipe draft is the moment that used to kill every guest —
       watch it especially. Then keep playing: does the other captain's board keep up with yours, does
       the narration read right, do prompts arrive when you expect them?
    4. **Chat.** Send a message each way. With the sheet shut, does the flash under the ribbon get
       noticed without hijacking your turn? Does the dot appear and clear? Hold a finger on the sea
       while a flash is up — it should step aside like every other floating box.
    5. **Drop out and come back.** Close the tab mid-voyage on the guest device, then reopen the page.
       You should be straight back in your seat without retyping the code.
    6. **Reload as host** mid-voyage. You should come back to the same day, not back to the start.

    Two things you should NOT expect to see, both deliberate: there is no ⏩ skip button in a crew game
    (skip is solo-only, your ruling), and nothing anywhere explains that a crew game ends differently —
    you chose to say nothing.
  </how-to-verify>

  <resume-signal>Type "approved", or tell me what went wrong and where in the voyage it happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → GitHub Pages → the live domain | A push to `main` is the deploy; there is no build step and no staging between it and real players |
| `4/` tree → repo root tree | Two trees with an identical internal layout; a mis-rooted edit applies cleanly to the wrong one and every safety signal still reports success |
| site-identity files → any second location | `CNAME` is a claim on `playpastrypirates.com`; a copy elsewhere does not fail safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-19 | Denial of Service | `CNAME` / `robots.txt` / `sitemap.xml` leaving this repo takes the live game down for real players | critical | mitigate | Task 2 — the changed-path list is pasted into the reply and asserted to contain no path outside `4/` and `.planning/`; no sync is hand-rolled, and `scripts/deploy-preview.sh` is the only sanctioned publisher |
| T-02-20 | Tampering | A mis-rooted edit lands in the root game instead of `4/` and every check still passes | high | mitigate | Task 2 — absolute paths throughout, and the changed-path list names the tree that must not appear and proves it did not |
| T-02-21 | Tampering | Restored multiplayer reaches the live `/4` URL where anyone could in principle join | low | accept | D-01, in Wyatt's own words: no one in the world is playing `/4` except him. `4/index.html:10` is `noindex, nofollow` and `robots.txt:8` is `Disallow: /4/`. Two gating schemes were proposed in this phase and both were rejected for this reason. |
| T-02-02 | Information Disclosure | Every client can read every rival's recipe | medium | accept | Deferred to Phase 4; reviewed here and deliberately not folded. Recorded in `02-FINDINGS.md`, no work planned. |
| T-02-07 | Denial of Service | `remotePrompt` never resolves | medium | accept | Wyatt's ruling — write it down, do not fix it. It is the headline entry of `02-FINDINGS.md` with the D-03 and MP-13 reasoning attached. |
| T-02-SC | Tampering | Supply chain — package installs | low | accept | This phase installs zero npm, pip or cargo packages; no package-manager surface, no legitimacy audit to run. The only restored dependency is two CDN `<script>` tags byte-identical to lines that have shipped in root `index.html` for the life of the project. |
</threat_model>

<artifacts_this_phase_produces>
This plan produces the phase's two remaining artifacts:

**New file path:** `.planning/phases/02-multiplayer-revival/02-FINDINGS.md` — the finding the ROADMAP
says this phase owes, and the document Phases 4 and 5 are scoped from.

**Modified constant:** `PP4_STAMP` at `4/src/ui/stage.js:32`, bumped exactly once in this phase.

The full phase artifact list — the restored `#choiceHost`, `#choiceJoin`, `#fbnote`, `#busynote`; the
new `#pp4Chat`, `#pp4ChatDot`, `#pp4ChatSheet`, `#pp4ChatFlash`; `body.pp4Chat`; the flash renderer and
unread-mark setter in `4/src/ui/panel.js`; the sheet toggle in `4/src/ui/stage.js`; and the ten
scratchpad probes, none committed and none wired into a test runner — is enumerated in
`02-01-PLAN.md`.
</artifacts_this_phase_produces>

<flagged_assumptions>
No SPEC.md exists, so the spec-less edge probe returned **0 resolved, 7 unresolved** — every
requirement `unclassified`. None is auto-backstopped or dropped; all seven are surfaced here. Real
coverage is `02-VALIDATION.md`'s Per-Task Verification Map and its Manual-Only Verifications table,
which is what this plan's checkpoint discharges.

| Requirement | Probe verdict | Status |
|---|---|---|
| MP-01 | unclassified — review manually | unresolved |
| MP-02 | unclassified — review manually | unresolved |
| MP-03 | unclassified — review manually | unresolved |
| MP-10 | unclassified — review manually | unresolved |
| MP-11 | unclassified — review manually | unresolved |
| MP-12 | unclassified — review manually | unresolved |
| FIX-03 | unclassified — review manually | unresolved |
</flagged_assumptions>

<verification>
- The health check was run and surfaced before the phase was reported closed, per CLAUDE.md §5.
- Git state was re-run, never recalled — both counts zero in both directions after the push.
- The findings document points at the summaries rather than copying their verdicts, holds no
  hand-typed count, and contains no future tense.
- Nothing closed on headless evidence: the phase's gate is the checkpoint above.
</verification>

<success_criteria>
The finding is written and Phases 4 and 5 can be scoped from it; `playpastrypirates.com/4` carries a
new build stamp Wyatt has been told to look for; and Wyatt has played a real networked voyage on his
phone and said it held together.
</success_criteria>

<output>
Create `.planning/phases/02-multiplayer-revival/02-07-SUMMARY.md` when done, and update
`.planning/STATE.md` with the new build stamp so the next session does not report an old one. Record
Wyatt's own words from the checkpoint verbatim — his phrasing is what later sessions cite as the
ruling, and paraphrasing it is how a decision gets quietly re-opened.
</output>
