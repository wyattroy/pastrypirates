---
phase: 07-foundation-determinism-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - scripts/lib/load_engine.js
  - scripts/determinism_baseline.js
  - scripts/real_game_test.js
  - scripts/dlog_replay_test.js
  - scripts/fixtures/determinism/
autonomous: true
requirements: [FOUND-01, FOUND-04]

must_haves:
  truths:
    - "`node scripts/determinism_baseline.js --verify` exits 0 against the committed 30-seed corpus, and exits 1 when any seed's freshly replayed event log no longer hashes to its committed `manifest.perSeed[].sha256` (FOUND-04, D-09)."
    - "`scripts/lib/load_engine.js` is the only place in the repo that string-slices `index.html` to obtain `Game`/`roundCfg`; all three Node harnesses obtain the engine through it (D-12)."
    - "Root `package.json` declares `\"type\": \"module\"` and `\"private\": true` and declares no dependency keys of any kind (FOUND-01, D-20)."
    - "`node scripts/dlog_replay_test.js` exits 0 and `node scripts/real_game_test.js 25` completes under ESM, in the same commit that introduces `package.json` (FOUND-01)."
    - "The committed corpus was captured from an `index.html` byte-identical to `main` — no production file is modified anywhere in this plan (D-21a)."
    - "Every seed file in the corpus has a non-empty event log and ends with a `__final__` state line (D-01, D-05)."
  artifacts:
    - package.json
    - scripts/lib/load_engine.js
    - scripts/determinism_baseline.js
    - scripts/fixtures/determinism/manifest.json
    - "scripts/fixtures/determinism/seed-12345.jsonl … seed-12374.jsonl (30 files)"
  key_links:
    - "The attribute-less eight-character script open tag ↔ the single engine script at `index.html:859` — a second attribute-less occurrence anywhere in the file silently redirects every extraction harness including `load_engine.js`."
    - "`html.indexOf(\"function escHtml\")` ↔ `index.html:1827` — the engine-region end sentinel."
    - "`new Game(cfg, seed, true)` third argument ↔ non-empty `g.events` (documented at `dlog_replay_test.js:103`); omit it and the corpus captures nothing."
    - "`manifest.perSeed[i].sha256` ↔ the freshly replayed event-log hash — never a whole-file diff of `manifest.json`."
    - "`package.json` `\"type\": \"module\"` ↔ every `.js` file under the repo root reclassified as ESM by Node's nearest-parent lookup."
  prohibitions:
    - statement: "`--verify` must not exit 0 when a seed's replayed event log differs from its committed hash."
      status: flagged-unverified
      verification: "Task 1's red-proof drill corrupts a committed `perSeed[].sha256` value, asserts exit code 1, restores it, and asserts exit code 0. An oracle whose failure path is never exercised certifies nothing."
    - statement: "`--verify` must not compare `manifest.json` byte-for-byte, and `capturedAt` must never participate in any pass/fail comparison."
      status: flagged-unverified
      verification: "Read the `--verify` implementation and confirm it loads the manifest as reference data only; confirm two consecutive `--capture` runs leave all 30 `.jsonl` files byte-identical while `capturedAt` legitimately changes."
    - statement: "`--capture` must not write a corpus in which any seed's event log is empty, or in which any required event type is absent."
      status: flagged-unverified
      verification: "The coverage assertion in `--capture` fails loudly and names the missing types; the per-seed event count is recorded in the manifest and asserted greater than zero."
    - statement: "`--verify` must not exit non-zero solely because `engineSourceHash` differs from the manifest value."
      status: flagged-unverified
      verification: "Phase 8 moves the engine source while keeping behavior byte-identical; a gate on the source hash would make Phase 8's own success condition unreachable. The source hash is reported as a diagnostic classification, never as a pass/fail input."
    - statement: "The corpus must not be captured from a tree whose `index.html` differs from `main`."
      status: flagged-unverified
      verification: "`git diff --stat main -- index.html` produces no output at capture time; no task in this plan lists `index.html` in its `<files>`."
    - statement: "`package.json` must not declare `dependencies` or `devDependencies`, and no package-manager install may run in this phase."
      status: flagged-unverified
      verification: "Acceptance criterion asserts both keys are absent from `package.json`; RESEARCH.md's Package Legitimacy Audit records zero external dependencies for this phase."
    - statement: "No commit may leave `node scripts/dlog_replay_test.js` failing."
      status: flagged-unverified
      verification: "`package.json` and the CommonJS-to-ESM conversion of both harnesses land in a single commit (Task 1); the harness exit codes are an acceptance criterion of that same task."
---

<objective>
Build the determinism regression oracle for the v1.1 milestone and land the Node-side module-loading contract, while `index.html` stays byte-identical to `main`.

This plan leads with a tracer: the thinnest end-to-end path that proves capture → verify works **and provably goes red**, wired through every layer the Node side of this phase touches — root `package.json`, the shared engine-extraction seam, the capture/verify tool, the on-disk fixture format, and the exit-code contract. The corpus is then widened to the locked 30-seed range and committed.

Purpose: every later phase in this milestone answers "did we break it?" by diffing against this corpus. Its quality is the ceiling on Phases 8–12's safety.
Output: `package.json`, `scripts/lib/load_engine.js`, `scripts/determinism_baseline.js`, both existing harnesses converted to ESM, and a committed 30-seed golden corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-foundation-determinism-baseline/07-CONTEXT.md
@.planning/phases/07-foundation-determinism-baseline/07-RESEARCH.md
@.planning/phases/07-foundation-determinism-baseline/07-PATTERNS.md
@.planning/phases/07-foundation-determinism-baseline/07-VALIDATION.md
</context>

<hard_sequencing_constraint>
**Read this before touching anything.**

1. **`index.html` is not modified anywhere in this plan.** The golden corpus is only a valid oracle if it is captured while `index.html` is byte-identical to `main`. A corpus captured after the file is edited encodes the change it exists to detect. The `index.html` edit belongs to Plan 07-02, which runs in Wave 2, after this plan's fixtures are committed.

   Note the precise scope: the invariant constrains `index.html` **only**. Adding `package.json` and rewriting files under `scripts/` do not affect engine behavior — the engine runs from a string inside a `vm` sandbox, indifferent to how the surrounding harness was loaded.

2. **`package.json` and the CommonJS-to-ESM conversion of both harnesses land in one commit (Task 1).** RESEARCH.md Pitfall 1 verified empirically that a root `package.json` with `"type": "module"` reclassifies every `.js` file in the repo by Node's nearest-parent lookup, and both existing harnesses use `require()`. Splitting these across commits leaves the repo in a state where the test suite is broken.
</hard_sequencing_constraint>

<assumptions>
## Flagged Assumptions — spec-less edge probe returned 5 unresolved rows

No SPEC.md exists for this phase, so the deterministic edge probe ran as a fallback. It returned **5 applicable / 0 resolved / 5 unresolved**: it classified FOUND-01…04 as `unclassified` and mis-binned FOUND-05 as `concurrency`. Per protocol `unclassified` rows stay unresolved and are never auto-backstopped and never dropped. They are surfaced here as explicit assumptions.

| Req | Probe result | Why the taxonomy does not fit | What this plan does instead |
|-----|--------------|-------------------------------|------------------------------|
| FOUND-01 | `unclassified` / unresolved | File-existence-and-module-semantics shaped; there is no input domain to find an edge in | Assumes the only real edge is CommonJS breakage under `"type": "module"`. Covered by Task 1's harness conversion and its exit-code acceptance criteria. |
| FOUND-02 | `unclassified` / unresolved | Browser page-load behavior, not a function with boundary values | Deferred to Plan 07-02 / 07-03. Assumes MIME and load-order are the only failure modes; both are measured, not assumed. |
| FOUND-03 | `unclassified` / unresolved | Script execution ordering is an HTML parser property | Deferred to Plan 07-02 / 07-03. Assumes MDN's deferral guarantee holds; the D-17 tripwire is the standing check. |
| FOUND-04 | `unclassified` / unresolved | The requirement is "an oracle exists and works", which has no numeric edge | Assumes the real edge is **the oracle silently passing when it should fail**. Covered by the red-proof drill (Task 1) and the coverage assertion (Task 2). |
| FOUND-05 | `concurrency` / unresolved — **mis-binned** | Documentation has no concurrency semantics whatsoever | Deferred to Plan 07-02 Task 3. Assumes doc accuracy is verifiable by content presence checks plus human review. |

**Additional flagged assumption (measured, contradicts a rationale in CONTEXT.md):** D-03's rationale states 30 seeds keeps the committed corpus "under ~1 MB". A direct measurement of the exact 30-seed range against the current engine produced **7,214 events totalling ~3,013,423 bytes (~2.87 MiB)** — roughly 3x the stated expectation. The *decision* in D-03 (30 seeds, `12345`–`12374`) is locked and is honored in full; only the size rationale was optimistic. Every event carries a full four-player `state` array and a `tokens` snapshot (`index.html:1257-1259`), which is inherent to D-01's "full event log, not aggregate statistics" and cannot be trimmed without changing the format. CONTEXT.md's Deferred Ideas already pre-authorize the response: reducing seed count later is safe, changing the format is not. Record the measured size in the SUMMARY; do not reduce the seed count in this phase.
</assumptions>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol Phases 8–12 may reference. Plans 07-01, 07-02, and 07-03 all carry this list.

**New files**
| Path | Owner plan |
|------|-----------|
| `package.json` | 07-01 |
| `scripts/lib/load_engine.js` | 07-01 |
| `scripts/determinism_baseline.js` | 07-01 |
| `scripts/fixtures/determinism/manifest.json` | 07-01 |
| `scripts/fixtures/determinism/seed-12345.jsonl` … `seed-12374.jsonl` (30 files) | 07-01 |
| `src/module-contract.js` | 07-02 |
| `src/main.js` | 07-02 |
| `docs/MODULES.md` | 07-02 |

**Modified files**
| Path | Change | Owner plan |
|------|--------|-----------|
| `scripts/real_game_test.js` | CommonJS → ESM; extraction block replaced by `loadEngine()` | 07-01 |
| `scripts/dlog_replay_test.js` | CommonJS → ESM; engine extraction replaced by `loadEngine()`; `replayShortfall` sentinel extraction untouched | 07-01 |
| `.gitignore` | `node_modules/` entry added defensively | 07-01 |
| `index.html` | exactly one line added — an attributed module script tag before the closing body tag | 07-02 |
| `README.md` | one pointer entry to `docs/MODULES.md` | 07-02 |

**Exported functions**
| Symbol | Module | Signature | Returns |
|--------|--------|-----------|---------|
| `loadEngine` | `scripts/lib/load_engine.js` | `async loadEngine()` | `{ Game, roundCfg, sourceHash }` |
| `MODULE_OK_FLAG` | `src/module-contract.js` | `const` | `"__pp_module_ok"` |
| `MODULE_CONTRACT_VERSION` | `src/module-contract.js` | `const` | `1` |

**CLI flags** — `scripts/determinism_baseline.js`
| Flag | Behavior | Exit codes |
|------|----------|-----------|
| `--capture` | Replays all seeds, writes the corpus and manifest, asserts coverage | 0 on success, 1 on coverage/empty-log failure |
| `--verify` | Replays all seeds, compares hashes against the committed manifest (default when no flag is given) | 0 all seeds match, 1 any mismatch |

**npm script names** — `package.json`
| Script | Command |
|--------|---------|
| `start` | `python3 -m http.server 8000` |
| `test` | `node scripts/determinism_baseline.js --verify && node scripts/dlog_replay_test.js` |
| `test:determinism` | `node scripts/determinism_baseline.js --verify` |

**Browser marker** — `window.__pp_module_ok` — set to `true` by `src/main.js` under a `typeof window !== "undefined"` guard. This is the machine-checkable browser-side proof of the module-loading contract (D-15) and the tripwire Phases 8–11 rely on to catch a load-order regression at page load.

**`manifest.json` field names**
`formatVersion`, `capturedAt`, `seedBase`, `seedCount`, `botStrategies`, `seatRotation`, `engineSourceHash`, `requiredEventTypes`, `coverage` (map of event type → count), `perSeed` (array of `{ seed, file, events, sha256 }`).
`capturedAt` is informational and never participates in a pass/fail comparison.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end determinism oracle on one seed — capture, verify, and prove it goes red</name>
  <reversibility rating="one-way">Phases 8–12 diff against the JSONL-plus-manifest fixture format; changing it mid-milestone invalidates the baseline and forces a re-capture from a tree that no longer matches `main`. The format is already chosen by locked decisions D-01/D-02/D-07, so no checkpoint is inserted — the rating is recorded for the phases that inherit it.</reversibility>
  <read_first>
    - `scripts/real_game_test.js` — whole file. Lines 16-49 are the extraction block `load_engine.js` consolidates; line 21 `SEED_BASE = 12345`; line 52 `BOT_STRATS`; lines 74-77 the seat-rotation and `new Game(cfg, seed, true)` call.
    - `scripts/dlog_replay_test.js` — whole file. Lines 20-43 are the duplicate engine extraction to delete; lines 45-64 are the `replayShortfall` sentinel extraction that stays local and unchanged; lines 66-72 the `check()` helper; lines 115-116 the exit-code contract; lines 103-104 the `record` third-argument gotcha.
    - `.planning/phases/07-foundation-determinism-baseline/07-PATTERNS.md` — verbatim excerpts of both extraction blocks, the ESM conversion recipe, the export shape for `loadEngine()`, and the event shape emitted by `Game.ev()`.
    - `.planning/phases/07-foundation-determinism-baseline/07-RESEARCH.md` — Pattern 1 (the `load_engine.js` body), Pattern 2 (ESM conversion), Pitfall 1 (the CommonJS breakage this task must not leave behind), Pitfall 3 (how `--verify` must compare).
  </read_first>
  <files>package.json, scripts/lib/load_engine.js, scripts/determinism_baseline.js, scripts/real_game_test.js, scripts/dlog_replay_test.js</files>
  <action>
Wire one seed all the way through every Node-side layer this phase touches, in a single commit, and prove the far end can fail.

**Root `package.json` (D-20, FOUND-01).** Declare `"name"`, `"private": true`, and `"type": "module"`. Add exactly three scripts: `start` runs `python3 -m http.server 8000` (D-19); `test:determinism` runs `node scripts/determinism_baseline.js --verify`; `test` runs `node scripts/determinism_baseline.js --verify && node scripts/dlog_replay_test.js`. Do not add a `dependencies` key and do not add a `devDependencies` key — their absence is a deliberate supply-chain property asserted below, not an oversight. Do not run any package-manager install.

**`scripts/lib/load_engine.js` (D-12).** Export a single `async function loadEngine()` returning `{ Game, roundCfg, sourceHash }`. Its body is a direct lift of `real_game_test.js:23-49` — same `index.html` read, same `html.indexOf` boundaries, same trailing `this.Game=Game;this.roundCfg=roundCfg;` export suffix, same `vm.createContext` sandbox shape including the writable `body.innerHTML` stub, same two guard conditions with their error messages preserved verbatim (the loud-failure-on-drift convention at PATTERNS.md "Shared Patterns"). Adapt only the packaging: `node:`-prefixed core imports, and `__dirname` derived from `path.dirname(fileURLToPath(import.meta.url))`, with the path resolving two levels up to the repo root rather than one. Add one behavior the existing harnesses lack — compute `sourceHash` as the hex SHA-256 of the sliced engine region using `node:crypto`, hashing the raw slice **before** the export suffix is concatenated, so the hash describes `index.html` and not this file's own scaffolding (D-11).

The function is `async` even though the extraction inside it is synchronous. Phase 8 replaces the body with a native dynamic import, which is inherently async; every caller must already be awaiting so that Phase 8 is a one-file diff (D-12).

**Convert both existing harnesses to ESM — same commit (RESEARCH.md Pitfall 1).** In `real_game_test.js` and `dlog_replay_test.js`, replace the `require` header with `node:`-prefixed imports, delete each file's own `Game`/`roundCfg` extraction block, and obtain the engine with a top-level `await loadEngine()`. `dlog_replay_test.js` keeps its own file read, `vm` import, and the entire `replayShortfall` sentinel extraction — that region is out of `load_engine.js`'s scope and must not be routed through it. Preserve every statistic, every `check()` case, every log line, and both files' exit behavior exactly as they are today: `dlog_replay_test.js` still ends on the failure-count exit, `real_game_test.js` still only prints. This conversion changes how the files load, never what they assert.

**`scripts/determinism_baseline.js` (D-09).** ESM. Positional flag handling in the existing no-library style (`real_game_test.js:20` is the precedent): `--capture` captures, `--verify` verifies, no flag defaults to `--verify`. Obtain the engine through `loadEngine()`. Reuse `SEED_BASE = 12345` and the `BOT_STRATS` roster verbatim, and build each game's seat list with the established rotation from `real_game_test.js:74-77`. Construct games as `new Game(roundCfg(strategies), seed, true)` — the third argument is required or `g.events` stays empty and the corpus captures nothing (`dlog_replay_test.js:103`).

Serialize one event per line as `JSON.stringify(event)` with no replacer and no indentation, newline-separated, into `scripts/fixtures/determinism/seed-<seed>.jsonl` (D-06, D-07). Key order comes from `Game.ev()`'s insertion order and is stable across runs of identical code. Hash each seed as the hex SHA-256 of that file's exact bytes and record it in `manifest.json` under `perSeed[].sha256`, alongside `formatVersion`, `capturedAt`, `seedBase`, `seedCount`, `botStrategies`, `seatRotation`, and `engineSourceHash` (D-02, D-11).

`--verify` performs three comparisons per seed and must never diff `manifest.json` as a file (RESEARCH.md Pitfall 3): first, hash the stored `.jsonl` bytes and compare to the manifest entry, which detects a corrupted or stale committed fixture; second, replay the seed fresh, serialize identically, hash, and compare to the same manifest entry, which is the behavior oracle; third, compare `loadEngine()`'s `sourceHash` to `manifest.engineSourceHash` and report the result as a diagnostic classification only. That third comparison must never on its own drive a non-zero exit — Phase 8 deliberately relocates the engine source while keeping behavior byte-identical, and a gate there would make Phase 8's own success condition unreachable. Print per-seed PASS/FAIL in the aligned `check()` style of `dlog_replay_test.js:67-72` and exit with the failure-count contract of `dlog_replay_test.js:116`. `capturedAt` is informational and must not participate in any comparison.

The tracer covers exactly one seed — `12345` — so the whole capture, verify, and failure path is proven before the corpus is widened. Task 2 widens this same code path to the locked 30-seed range of D-03; nothing about the file layout, manifest schema, or comparison logic changes when it does.

**Prove the oracle can go red.** Back up `manifest.json` outside the repo, overwrite `perSeed[0].sha256` with a value that cannot be a real digest, run `--verify` and confirm it exits 1 and names seed 12345, then restore the backup and confirm `--verify` exits 0 again. A verify path whose failure branch has never executed is worse than no verify — it converts every later phase's green run into a claim nobody has tested.
  </action>
  <acceptance_criteria>
    - `node -e 'const p=require("./package.json"); if(p.type!=="module") throw new Error("type"); if(p.private!==true) throw new Error("private"); if("dependencies" in p) throw new Error("dependencies key present"); if("devDependencies" in p) throw new Error("devDependencies key present"); console.log("package.json OK")'` prints `package.json OK` and exits 0.
    - `node -e 'const s=require("./package.json").scripts; for (const k of ["start","test","test:determinism"]) if(!s[k]) throw new Error("missing script "+k); console.log("scripts OK")'` prints `scripts OK`.
    - `grep -c 'require(' scripts/real_game_test.js` outputs `0` and `grep -c 'require(' scripts/dlog_replay_test.js` outputs `0`.
    - `grep -c 'loadEngine' scripts/real_game_test.js` and `grep -c 'loadEngine' scripts/dlog_replay_test.js` each output a value of at least 1.
    - `grep -c 'replayShortfall' scripts/dlog_replay_test.js` outputs a value of at least 4 — the sentinel extraction survived the conversion intact.
    - `grep -c 'indexOf' scripts/lib/load_engine.js` outputs a value of at least 2 — both engine-region boundaries are located in the seam.
    - `node scripts/dlog_replay_test.js` exits 0 and its final line reads `All cases passed.`
    - `node scripts/real_game_test.js 25` exits 0 and prints a line beginning `Total battles across all games:` with a value greater than 0.
    - `node scripts/determinism_baseline.js --capture` exits 0 and creates `scripts/fixtures/determinism/seed-12345.jsonl` and `scripts/fixtures/determinism/manifest.json`.
    - `wc -l < scripts/fixtures/determinism/seed-12345.jsonl` reports a value greater than 100 — the `record` third argument was passed and events actually accumulated.
    - `node scripts/determinism_baseline.js --verify` exits 0, and `node scripts/determinism_baseline.js` with no arguments also exits 0 (the default is verify).
    - Red-proof: after `cp scripts/fixtures/determinism/manifest.json /tmp/pp-manifest.bak`, then `node --input-type=module -e "import fs from 'node:fs'; const p='scripts/fixtures/determinism/manifest.json'; const m=JSON.parse(fs.readFileSync(p,'utf8')); m.perSeed[0].sha256='0'.repeat(64); fs.writeFileSync(p, JSON.stringify(m,null,2)+'\n');"`, the command `node scripts/determinism_baseline.js --verify; test $? -eq 1` succeeds and the output names seed `12345`.
    - Restore: after `cp /tmp/pp-manifest.bak scripts/fixtures/determinism/manifest.json`, `node scripts/determinism_baseline.js --verify` exits 0 again.
    - `git status --porcelain index.html` produces no output — `index.html` is untouched by this task.
    - `package.json`, both converted harnesses, and `scripts/lib/load_engine.js` are staged and committed together in one commit.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/determinism_baseline.js --capture && node scripts/determinism_baseline.js --verify && node scripts/dlog_replay_test.js && node scripts/real_game_test.js 25 && git status --porcelain index.html | wc -l | grep -qx '0' && echo TRACER_GREEN</automated>
  </verify>
  <done>One seed flows end to end — `loadEngine()` extracts the engine, a recorded game produces a non-empty event log, the log serializes to JSONL, hashes into the manifest, and `--verify` returns 0 against it and 1 against a corrupted manifest entry. Both existing harnesses run green under ESM in the same commit that introduces `package.json`. `index.html` is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the oracle to the locked 30-seed corpus with coverage assertion and divergence reporting</name>
  <read_first>
    - `scripts/determinism_baseline.js` — the tracer written in Task 1; this task widens it rather than rewriting it.
    - `.planning/phases/07-foundation-determinism-baseline/07-CONTEXT.md` — D-03 (seed range and rotation), D-04 (coverage assertion), D-05 (final state), D-07 (manifest contents), D-08 (commit the fixtures), D-10 (divergence report), D-11 (engine source hash).
    - `.planning/phases/07-foundation-determinism-baseline/07-PATTERNS.md` — the `Game.ev()` event shape at `index.html:1257-1259`, and the recommendation to carry the final-state snapshot as the last JSONL line so it participates in the same per-seed hash.
    - `scripts/dlog_replay_test.js` lines 66-72 and 115-116 — the PASS/FAIL alignment and exit-code shapes to mirror.
  </read_first>
  <files>scripts/determinism_baseline.js, .gitignore, scripts/fixtures/determinism/manifest.json, scripts/fixtures/determinism/</files>
  <action>
Widen the proven path to the full corpus. The file layout, manifest schema, and comparison logic from Task 1 do not change.

**Seed range (D-03).** Replace the tracer's single seed with 30 seeds, `12345` through `12374` inclusive, built from `SEED_BASE = 12345`. For seed index `i`, the four seats take `BOT_STRATS[(i + s) % BOT_STRATS.length]` for `s` in 0 through 3 — the rotation already established at `real_game_test.js:74-77`, reused verbatim so seed composition is reproducible from the convention rather than from a table.

**Final-state line (D-05).** Append one additional line as the last line of each seed file, an object whose `t` is `__final__`, carrying the winner, the final round number, and a per-player array of index, position, coins, ingredients, and done flag, with a fixed field order so serialization is stable. It is the last line of the same file, so it participates in that seed's single SHA-256 automatically rather than splitting the oracle across two files with two comparison mechanisms.

**Coverage assertion (D-04).** During `--capture`, tally every distinct event `t` value across all 30 seeds into a `coverage` map of type to count, and record it in the manifest so under-coverage is visible in a diff rather than invisible. Assert a required floor covering each mechanic D-04 names, mapped to the engine's actual event type strings: battle covers `battle` and `battleflee`; trade covers `trade`; dock covers `dock`; fish covers `fish`; storm and wind cover `windmove`, `tradewind`, and `shipwrecked`; run-aground covers `aground`; endgame covers `end`, `bakeoff`, and `finish`. Record that exact list in the manifest as `requiredEventTypes`. If any is absent, `--capture` must fail loudly and name the missing types.

All twelve were measured present across this exact seed range against the current engine, with counts ranging from 1,288 down to 1. Three are deliberately thin — `shipwrecked` occurs once, `aground` three times, and `anchor` three times — because those mechanics are rare, not because the corpus is weak. Do not remove an entry from the required list to make a capture pass; if one is genuinely absent, that is a finding to surface, not a list to edit. Note also that `anchorHold` is emitted nowhere in this seed range and is correctly not on the list.

Additionally assert that every seed's event count is greater than zero and that each file's line count equals its event count plus one for the final-state line. A corpus with a silently empty seed is a corpus that proves nothing about that seed.

**Divergence report (D-10).** When a seed's freshly replayed hash does not match its manifest entry, load the stored `.jsonl`, walk it line by line against the fresh serialization, and report the first index at which they differ, printing the stored line and the fresh line each truncated to a compact width. Print this detail for the first divergent seed only, while still counting and listing PASS/FAIL for every remaining seed, so a single run tells the reader both how widespread the break is and exactly where it starts. Continue to exit with the failure-count contract.

**Source-hash classification (D-11).** Report which of three situations holds: hashes match and the engine source hash matches, meaning nothing changed; hashes match but the engine source hash differs, meaning the source moved while behavior stayed identical — the expected and desired outcome for Phase 8; hashes differ, meaning behavior changed. Label them in the output in those terms. The source-hash comparison remains diagnostic and never drives the exit code on its own.

**Capture and commit (D-08).** Run `--capture` to write all 30 seed files and the manifest, then commit them. Fixtures that get regenerated are not an oracle; being immutable and reviewable in a diff is the entire value. Add a `node_modules/` entry to `.gitignore` defensively — no dependencies exist and none are planned, and the entry costs nothing.

Record the measured on-disk corpus size in the SUMMARY. A direct measurement puts it near 2.9 MiB rather than the roughly 1 MB anticipated in D-03's rationale, because every event carries a full four-player state array and a tokens snapshot. The seed count stays at 30 as locked; the size delta is a finding to report, not a reason to trim.
  </action>
  <acceptance_criteria>
    - `ls scripts/fixtures/determinism/seed-*.jsonl | wc -l` outputs `30`.
    - `ls scripts/fixtures/determinism/seed-12345.jsonl scripts/fixtures/determinism/seed-12374.jsonl` succeeds — the range endpoints both exist.
    - `node --input-type=module -e "import fs from 'node:fs'; const m=JSON.parse(fs.readFileSync('scripts/fixtures/determinism/manifest.json','utf8')); if(m.seedCount!==30) throw new Error('seedCount'); if(m.perSeed.length!==30) throw new Error('perSeed length'); if(m.perSeed.some(x=>!x.sha256||x.sha256.length!==64)) throw new Error('bad sha256'); if(m.perSeed.some(x=>!(x.events>0))) throw new Error('empty seed'); if(!m.engineSourceHash) throw new Error('engineSourceHash'); console.log('manifest OK')"` prints `manifest OK`.
    - `node --input-type=module -e "import fs from 'node:fs'; const m=JSON.parse(fs.readFileSync('scripts/fixtures/determinism/manifest.json','utf8')); const req=['battle','battleflee','trade','dock','fish','windmove','tradewind','shipwrecked','aground','end','bakeoff','finish']; const missing=req.filter(t=>!(m.coverage&&m.coverage[t]>0)); if(missing.length) throw new Error('missing types: '+missing.join(',')); console.log('coverage OK')"` prints `coverage OK`.
    - Every seed file's last line parses as JSON with a `t` value of `__final__`: `for f in scripts/fixtures/determinism/seed-*.jsonl; do tail -1 "$f" | node --input-type=module -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{if(JSON.parse(s).t!=='__final__')throw new Error('no final line');});"; done && echo FINAL_LINES_OK` prints `FINAL_LINES_OK`.
    - `node scripts/determinism_baseline.js --verify` exits 0 and reports 30 passing seeds.
    - Serialization idempotence: running `node scripts/determinism_baseline.js --capture` a second time leaves the seed files unchanged — `git diff --quiet -- 'scripts/fixtures/determinism/seed-*.jsonl'` exits 0 afterwards.
    - Divergence report still fires after widening: corrupt `perSeed[7].sha256` as in Task 1's drill, confirm `--verify` exits 1, confirm the output names the seed and prints a first-divergent-event index, then restore with `git checkout -- scripts/fixtures/determinism/manifest.json` and confirm exit 0.
    - `grep -c 'node_modules' .gitignore` outputs a value of at least 1.
    - `git status --porcelain index.html` produces no output — `index.html` is still untouched.
    - All 30 seed files and `manifest.json` are committed.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/determinism_baseline.js --capture && node scripts/determinism_baseline.js --verify && ls scripts/fixtures/determinism/seed-*.jsonl | wc -l | tr -d ' ' | grep -qx '30' && git status --porcelain index.html | wc -l | grep -qx '0' && echo CORPUS_GREEN</automated>
  </verify>
  <done>A committed 30-seed corpus covering seeds 12345 through 12374 exists under `scripts/fixtures/determinism/`, every seed file is non-empty and ends with a `__final__` line, the manifest records per-seed hashes plus a coverage map that satisfies the required-type floor, `--verify` exits 0 against it and 1 with a located first divergence when an entry is corrupted, and `index.html` is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-committed fixture corpus → Node dev tooling | The oracle is the trust anchor for every "did we break it?" answer in Phases 8–12. Its integrity is the phase's real security property. |
| `index.html` on disk → `node:vm` sandbox in `load_engine.js` | First-party repo source is read and executed by the harness. |
| `package.json` script surface → developer shell | A new executable surface lands in the repo where none existed. |

## STRIDE Threat Register

This phase's production surface is a `package.json` and files under `scripts/`. Nothing here is reachable by a remote user. The register is short because the surface is small, not because it was skipped.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `scripts/fixtures/determinism/*` | high | mitigate | `--verify` hashes the stored `.jsonl` bytes against the manifest on every run, so a corrupted or hand-edited committed fixture is detected rather than trusted (Task 2). |
| T-07-02 | Tampering | `package.json` supply-chain posture | high | mitigate | No `dependencies` key and no `devDependencies` key; `"private": true`. Asserted by an acceptance criterion in Task 1 so the zero-dependency property is enforced, not merely intended. |
| T-07-03 | Repudiation | `determinism_baseline.js --verify` | high | mitigate | An oracle whose failure branch has never executed silently certifies every later phase. The red-proof drill is a required acceptance criterion in Task 1 and re-run after widening in Task 2. |
| T-07-04 | Elevation of Privilege | `node:vm` sandbox in `load_engine.js` | low | accept | `vm` is not a security boundary and is not used as one — it executes first-party repo source in a dev-only harness, identical in posture to the two harnesses that already do this. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero package-manager installs occur in this phase. RESEARCH.md's Package Legitimacy Audit records the phase as introducing no external dependencies, so no `[ASSUMED]` or `[SUS]` package exists and no legitimacy checkpoint is required. The gate is the absence of both dependency keys in `package.json`. |
</threat_model>

<verification>
- `node scripts/determinism_baseline.js --verify` exits 0 against the committed corpus.
- `node scripts/determinism_baseline.js --verify` exits 1 when any `perSeed[].sha256` is corrupted, and names the seed.
- `node scripts/dlog_replay_test.js` exits 0.
- `node scripts/real_game_test.js 25` completes without error.
- `npm test` exits 0.
- `git log --oneline -- index.html | head -1` is unchanged from the pre-plan value — no commit in this plan touched `index.html`.
- `git diff --stat main -- index.html` produces no output.
</verification>

<success_criteria>
- Root `package.json` exists with `"type": "module"`, `"private": true`, three scripts, and no dependency keys (FOUND-01).
- Both existing harnesses run under ESM and obtain the engine through `scripts/lib/load_engine.js`, landing in the same commit as `package.json` (FOUND-01).
- A 30-seed golden corpus covering seeds 12345 through 12374 is captured from a byte-identical-to-`main` `index.html` and committed (FOUND-04).
- The corpus satisfies the required event-type floor and no seed is empty (FOUND-04).
- The `--verify` failure path is demonstrated, not assumed (FOUND-04).
</success_criteria>

<output>
Create `.planning/phases/07-foundation-determinism-baseline/07-01-SUMMARY.md` when done.

Record in the SUMMARY: the measured on-disk corpus size, the per-type coverage counts, the `engineSourceHash` value (Plan 07-02 compares against it), and the exact red-proof commands used.
</output>
