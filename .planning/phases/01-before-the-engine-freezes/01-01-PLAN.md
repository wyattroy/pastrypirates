---
phase: 01-before-the-engine-freezes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 4/src/ui/stage.js
  - 4/scripts/no_undef_check.js
  - scripts/no_undef_check.js
  - 4/scripts/stage_import_check.js
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "`node 4/scripts/stage_import_check.js` exits 0 — the first gate in this repo that loads `4/` runs green end to end."
    - "`node 4/scripts/no_undef_check.js` exits 0, and `node scripts/no_undef_check.js` still exits 0."
    - "`diff scripts/no_undef_check.js 4/scripts/no_undef_check.js` reports no difference — the two checkers stay byte-identical (CONTEXT.md Claude's Discretion, 'Keep the two checkers identical')."
    - "TEST-02 adjacency edge: the accessor heuristic skips a call-position identifier ONLY when the nearest preceding bare word is the accessor keyword; a bare module-scope browser-global call is still reported."
    - "The heuristic narrows false positives without weakening the check — the checker was observed failing on a deliberately planted bare browser-global call before its green was believed (CLAUDE.md §4)."
    - "The resize registration at `4/src/ui/stage.js:190` is guarded by a `typeof window` block and prefixed with `window.`, matching `4/src/main.js:32`."
    - statement: "The module-scope watchdog interval at `4/src/ui/stage.js:1449` is left exactly as it is — the import gate exits explicitly instead."
      verification: backstop
  artifacts:
    - "4/scripts/stage_import_check.js"
    - "4/scripts/no_undef_check.js (modified)"
    - "scripts/no_undef_check.js (modified)"
  key_links:
    - "`4/scripts/stage_import_check.js` -> dynamic import of `../src/ui/stage.js` -> stage.js's whole import graph -> explicit process exit. This is the path every later plan's gate reuses."
    - "`4/scripts/no_undef_check.js` -> the CALL_RE scan over `4/src/**/*.js` -> the accessor exclusion -> the exit code."
  prohibitions:
    - statement: "The accessor exclusion must not be broadened into a general loosening of the call-site scan. A gate that stops catching a real bare browser-global is not silent, it is reassuring (docs/HARD-WON-LESSONS.md §3)."
      status: unverified
      verification: judgment
    - statement: "The watchdog interval in stage.js must not be disabled, guarded away, or relocated in order to make the import gate terminate. The interval is deliberate browser behaviour; the gate exits, the game does not change."
      status: unverified
      verification: test

assumptions:
  - "Probe edge TEST-01/unclassified — the probe returned no category for this requirement. The requirement is binary (the module imports, or it throws) and the gate in Task 1 is its whole predicate. No further edge is applicable."
  - "Probe edge TEST-02/empty — no empty-input axis. The checker's input is the fixed `4/src/**/*.js` file set (25 files today); the heuristic adds no new input shape, and a zero-file scan is not a scenario this phase creates."
  - "Probe edge TEST-02/ordering — the failure report is ordered by file, then by match offset within the file. The heuristic only skips matches, never reorders them, so the ordering contract is unchanged."
---

<objective>
Make the first gate in this repository that loads `4/` run green, end to end.

Root `npm test` runs 21 gates and **not one of them loads `4/`** — the exact "gate scanning the
wrong tree" trap in `docs/HARD-WON-LESSONS.md` §3. Until a `4/` gate runs at all, nothing else in
this phase can be verified headlessly.

Purpose: TEST-01 and TEST-02 are this phase's spine. Every later plan's verification is a
`4/scripts/*.js` gate importing something under `4/src/`, and that shape does not work today.

Output: `4/src/ui/stage.js` importable under Node; `4/scripts/stage_import_check.js` (new); the
accessor heuristic applied to both byte-identical copies of `no_undef_check.js`.
</objective>

<tracer_judgment>
**The tracer-mode call, made explicitly (TRACER_MODE=true).**

Tracer-first fits this phase, and this plan is the tracer. The phase is six largely independent
repairs rather than one feature with a spine — but it does have a spine, and it is the verification
path, not the game. Every remaining requirement is proven by a new `4/scripts/*.js` gate importing a
module under `4/src/`, and that whole path is broken today: `4/src/ui/stage.js` throws at
module-evaluation time, and `4/scripts/no_undef_check.js` exits 1.

So the thinnest slice that touches every layer this phase modifies is: **a `4/scripts/` gate → a
`4/src/` module → its full import graph → a real exit code.** One path, wired end to end, with a
runnable verify, production-quality — the gate ships and stays.

Two payoffs land immediately in the next wave, which is how you tell this was a real tracer and not
a ceremony: plan 02's one-time-cleanup behaviour becomes unit-testable in Node *because*
`4/src/ui/stage.js` now imports, and plans 02 and 04 can edit `4/src/**` under a checker that
actually reports. Expansion tasks in later plans build out from this proven slice; no horizontal
layer decomposition appears anywhere in this phase.
</tracer_judgment>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-before-the-engine-freezes/01-CONTEXT.md
@.planning/phases/01-before-the-engine-freezes/01-RESEARCH.md
@.planning/phases/01-before-the-engine-freezes/01-PATTERNS.md
@.claude/CLAUDE.md

**The working tree is `/Users/wyattroy/Documents/Projects/pastrypirates`.** This repo contains
several trees with identical internal layout (`4/`, `v2/`, `v2bakeoff/`, root `src/`, root
`scripts/` vs `4/scripts/`). Every path below names its tree; use absolute paths (CLAUDE.md §3).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End to end — a 4/ gate imports 4/src/ui/stage.js and exits 0</name>
  <files>4/src/ui/stage.js, 4/scripts/stage_import_check.js</files>
  <read_first>
    - `4/src/ui/stage.js` lines 185-195 (the bare resize registration) and lines 1440-1455 (the
      watchdog interval that makes a bare promise chain hang — do not change it)
    - `4/src/main.js` lines 30-40 and 175-215 — the shipped `typeof window` guard this fix copies
    - `scripts/module_graph_check.js` lines 190-215 — the exit-on-resolve precedent
    - `4/scripts/seat_arg_check.js` lines 1-40 — the `4/scripts/` house shell (shebang, a header
      naming what is gated and why it earned a gate)
    - `.planning/phases/01-before-the-engine-freezes/01-PATTERNS.md`, section
      "`4/scripts/stage_import_check.js` (new, test, TEST-01)"
  </read_first>
  <action>
    Two edits, one path, proven end to end.

    (a) In `4/src/ui/stage.js`, wrap the module-scope resize registration at line 190 in an
    `if (typeof window !== "undefined") { ... }` block and prefix the call with `window.` inside
    that block. The callback body is unchanged: it resets `ribHAt` to negative-one-billion-ish and
    clears `lastVB`. Match the block-guard shape at `4/src/main.js:32`, not an inline ternary. Add a
    one-line comment naming why the guard exists: under Node there is no browser global to bind to,
    and this module is the largest in the new game at 1,545 lines.

    Do NOT add a guard to the watchdog interval further down the same file. That interval is
    deliberate browser behaviour with its own header comment; TEST-01 asks only that the import not
    throw. Guarding it would be scope creep and would change shipped behaviour.

    (b) Create `4/scripts/stage_import_check.js` following the `4/scripts/` house shell: shebang, a
    header naming what is gated (TEST-01: the 1,545-line stage layer must load headlessly) and why
    the explicit exit is not optional. Body: a dynamic import of `../src/ui/stage.js`; on resolve,
    print one PASS line and exit with status 0; on reject, print a FAIL line carrying the error and
    exit with status 1.

    The explicit exit call is load-bearing, not tidiness. The module-scope watchdog interval keeps
    the Node event loop alive forever after a successful import, so a script that merely awaits the
    promise hangs — shipping a gate that hangs CI instead of passing it. The precedent is
    `scripts/module_graph_check.js`, which already forces the exit after its own dynamic imports.

    Before believing the green: temporarily revert edit (a), run the gate, confirm it exits 1 and
    prints the `ReferenceError`, then restore the fix. A check nobody has seen fail is not yet a
    check (CLAUDE.md §4).
  </action>
  <verify>
    <automated>node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/stage_import_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/stage_import_check.js` exits 0
      and returns within 5 seconds — it does not hang.
    - `grep -c 'process.exit(0)' /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/stage_import_check.js`
      returns at least 1.
    - `grep -c 'window.addEventListener("resize"' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/stage.js`
      returns at least 1.
    - `grep -c 'typeof window' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/stage.js`
      returns at least 1, and the guard encloses the resize registration.
    - The watchdog region is untouched: `sed -n '1440,1465p' 4/src/ui/stage.js` still contains
      `setInterval(` and no `typeof` guard was added around it.
    - The failure path was demonstrated: the SUMMARY records the exit code and the first line of
      output observed with the guard temporarily removed.
  </acceptance_criteria>
  <done>A gate script in the `4/` tree imports the largest module in the new game under Node, exits 0, and has been observed exiting 1 when the fix is removed.</done>
</task>

<task type="auto">
  <name>Task 2: The accessor heuristic — both checkers, one commit, still byte-identical</name>
  <files>4/scripts/no_undef_check.js, scripts/no_undef_check.js</files>
  <read_first>
    - `4/scripts/no_undef_check.js` lines 1-40 (the file's own design philosophy: deliberately
      over-permissive, false positives are the worse failure mode) and lines 386-420 (`CALL_RE`,
      `checkFile`, and the existing property-call backward scan this fix mirrors)
    - `scripts/no_undef_check.js` — the byte-identical root copy
    - `4/src/ui/stage.js` line 1483 — the accessor pair inside the bridge object literal that
      produces the two duplicate false positives
    - `.planning/phases/01-before-the-engine-freezes/01-PATTERNS.md`, section
      "`4/scripts/no_undef_check.js` + `scripts/no_undef_check.js` (TEST-02 heuristic fix)"
  </read_first>
  <action>
    The checker reports three hits today. One was real and Task 1 fixed it. The other two are the
    same source line reported twice: the getter and the setter of an accessor pair, matched by
    `CALL_RE` as call-position identifiers because the regex has no concept of accessor syntax.

    In `checkFile`, immediately after the existing property-call exclusion, add a narrow accessor
    exclusion built from the same technique that exclusion already uses: from the position just
    before the matched identifier, walk backward past whitespace, then walk backward across a run of
    identifier characters to capture the preceding token, and skip the match when that token is
    exactly the bare accessor keyword — the three-letter reader keyword or the three-letter writer
    keyword. Word-boundary-check it: the character before that token must not itself be an
    identifier character, so a longer name merely ending in those three letters does not trigger the
    skip.

    Factor this into a module-internal helper named `precededByAccessorKeyword`, taking the masked
    source and the match index and returning a boolean, so the exclusion is greppable by name and
    provably identical in both copies.

    Do NOT loosen `CALL_RE` itself, and do NOT exclude those keywords anywhere else they appear. The
    exclusion is exactly "the nearest preceding bare word is an accessor keyword" and nothing wider.
    The file's own header explains why: the tool is deliberately over-permissive rather than
    under-permissive, and a broader fix would reintroduce the class of bug the checker exists to
    catch.

    Apply the identical change to `scripts/no_undef_check.js` in the same commit. The two files are
    byte-identical today and must stay so; the root copy exits 0 only because root `src/` happens to
    contain no accessor in this shape, so fixing one and not the other creates exactly the drift the
    intake report flagged.

    Before believing the green: create a scratch file under `4/src/` containing a bare module-scope
    call to a browser global, run the `4/` checker, confirm it exits 1 and names that file, then
    delete the scratch file. Then confirm both checkers exit 0.
  </action>
  <verify>
    <automated>node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/no_undef_check.js &amp;&amp; node /Users/wyattroy/Documents/Projects/pastrypirates/scripts/no_undef_check.js &amp;&amp; diff /Users/wyattroy/Documents/Projects/pastrypirates/scripts/no_undef_check.js /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/no_undef_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/no_undef_check.js` exits 0.
    - `node /Users/wyattroy/Documents/Projects/pastrypirates/scripts/no_undef_check.js` exits 0.
    - `diff /Users/wyattroy/Documents/Projects/pastrypirates/scripts/no_undef_check.js /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/no_undef_check.js`
      produces no output and exits 0.
    - Both files contain the helper name: `grep -c 'precededByAccessorKeyword'` returns at least 2
      in each of the two files (definition plus call site).
    - `CALL_RE`'s definition line is unchanged from its pre-task text.
    - The scratch-fixture demonstration is recorded in the SUMMARY: the exit code and the reported
      `file:line` observed while the planted bare browser-global call was present.
    - `cd /Users/wyattroy/Documents/Projects/pastrypirates && npm test` still passes all 21 root
      gates (the root checker is one of them).
  </acceptance_criteria>
  <done>Both copies of the checker exit 0, remain byte-identical, and were observed still catching a planted bare browser-global call.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol, file, and key this phase creates. The plan-review source-grounding pass reads this
list to exclude newly-created names from drift verification.

**New files**
- `4/scripts/stage_import_check.js` (plan 01)
- `4/scripts/pp4_timeroff_check.js` (plan 02)
- `4/scripts/pass_coin_test.js` (plan 04)
- `4/scripts/pass_narration_test.js` (plan 04)
- `4/scripts/planner_singleton_check.js` (plan 05)
- `.planning/phases/01-before-the-engine-freezes/01-BALANCE-BASELINE.md` (plan 03)
- `.planning/phases/01-before-the-engine-freezes/01-BALANCE-DELTA.md` (plan 06)

**New symbols**
- `precededByAccessorKeyword(masked, idx)` — module-internal helper in both copies of
  `no_undef_check.js` (plan 01)
- `cleanupLegacyTimerKey(store)` — new export from `4/src/ui/stage.js` (plan 02)
- `Game.prototype.doPass(p)` — new engine method in `4/src/engine/index.js` (plan 04)

**New storage keys**
- `pp4_timerOff` — the per-game turn-clock preference key (plan 02, D-01)
- `pp4_timerOffCleaned` — the one-time cleanup marker key (plan 02, D-02)

**New CLI surface**
- `scripts/bot_ladder4.js` gains a `--json` flag emitting a machine-readable run record for the
  before/after diff; the existing positional `[games] [seedMult]` arguments are unchanged (plan 03)

**New copy string**
- The pass narration tag `Recipe idea! (+1🌕)` appended by `EVENT_NARRATION.pass` (plan 04, D-06)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node process → repository source tree | The two new/modified gate scripts read and import source files from disk. No network, no user input, no privilege change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `4/scripts/no_undef_check.js` heuristic | low | mitigate | A broadened exclusion silently stops catching real undeclared browser globals — the "gate scanning the wrong tree" failure in a new shape. Mitigated by the narrow accessor-keyword-only rule and by the scratch-fixture demonstration in Task 2's acceptance criteria, which proves the check can still fail. |
| T-01-02 | Denial of Service | `4/scripts/stage_import_check.js` | low | mitigate | A gate that hangs (module-scope watchdog interval holding the event loop) blocks every later gate run. Mitigated by the explicit process exit on both branches and the 5-second bound in the acceptance criteria. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages. `package.json` declares no `dependencies` or `devDependencies` and this project has no build step (RESEARCH.md, Package Legitimacy Audit: not applicable). No supply-chain surface is introduced. |

**ASVS L1 assessment:** no authentication, session, access-control, cryptographic, or
externally-supplied-input surface is touched by this plan. No `high` threat is present, so the
configured `block_on: high` gate does not fire.
</threat_model>

<verification>
1. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/stage_import_check.js` exits 0
   within 5 seconds.
2. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/no_undef_check.js` exits 0.
3. `node /Users/wyattroy/Documents/Projects/pastrypirates/scripts/no_undef_check.js` exits 0.
4. `diff` of the two checkers is empty.
5. `cd /Users/wyattroy/Documents/Projects/pastrypirates && npm test` still passes.
6. Diff scope (CLAUDE.md §3): `git diff --name-only` lists only `4/src/ui/stage.js`,
   `4/scripts/no_undef_check.js`, `4/scripts/stage_import_check.js`, `scripts/no_undef_check.js`.
   Nothing under `v2/`, `v2bakeoff/`, `3/`, or root `src/` appears.
7. Both failure demonstrations (Task 1's reverted guard, Task 2's scratch fixture) are recorded in
   the SUMMARY with the observed exit codes.
</verification>

<success_criteria>
- TEST-01 satisfied: `4/src/ui/stage.js` imports under Node without throwing, proven by a committed
  gate that terminates.
- TEST-02 satisfied: `4/scripts/no_undef_check.js` exits 0, with the real bug fixed rather than
  suppressed and the false positives removed by a narrow, named heuristic.
- The two checkers remain byte-identical.
- The `4/scripts/` gate path is proven and available to every later plan in this phase.
</success_criteria>

<output>
Create `.planning/phases/01-before-the-engine-freezes/01-01-SUMMARY.md` when done.

State in the summary which surfaces were checked (CLAUDE.md §2 consistency): both copies of
`no_undef_check.js`, the resize registration, and the watchdog interval left deliberately alone.
</output>
