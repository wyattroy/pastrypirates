---
phase: 01-before-the-engine-freezes
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - 4/src/engine/index.js
  - 4/src/ui/flow.js
  - 4/src/ui/util.js
  - 4/scripts/pass_coin_test.js
  - 4/scripts/pass_narration_test.js
autonomous: true
requirements: [RULE-01, RULE-02]

must_haves:
  truths:
    - "A captain who passes receives exactly one dubloon, at every one of the three pass-emission sites: the human menu and the animated bot fallback in `4/src/ui/flow.js`, and the engine fallback in `4/src/engine/index.js` (RULE-01)."
    - "All three sites call one shared `Game.prototype.doPass(p)` rather than three inline increments — the elegant version deletes code (CLAUDE.md §2, CONTEXT.md Claude's Discretion)."
    - "Both bot paths pay. The flow-tier fallback is deliberately duplicated rather than inherited (`4/src/ui/flow.js:2130-2137` says why), and a fix applied only to the engine would fix the simulator and leave every real browser game exactly as broken."
    - "RULE-01 ordering edge, hard predicate: `p.coins` is incremented BEFORE `this.ev(...)` is called inside `doPass`, matching `doDock`'s existing ordering. `ev()` snapshots the purse at call time, so the reverse order records a purse one dubloon short at the exact tick the narration claims payment — a determinism-visible defect in the one phase where that is expensive."
    - "The coin mutation is never conditioned on the engine's record flag. `ev()` self-gates; the payment does not. (Resolved during planning: ordinary browser play already constructs the game with the record flag true, at `4/src/orchestrator.js:1526` and `4/src/ui/board.js:1969` — so `ev()` is not a silent no-op in real play. RESEARCH.md Open Question 1 is closed.)"
    - "The human-only sea-cursor advance stays outside `doPass` — it is per-device narration bookkeeping that bots never touch, not shared behaviour."
    - "The pass narration carries the tag `Recipe idea! (+1🌕)` as a subjectless fragment appended by the renderer in exactly ONE place, wrapped whole in a no-break span (D-06)."
    - "RULE-02 encoding edge: the tag survives the render pipeline intact. All 100 renderings — 50 sea creatures × the addressed and third-person persons — contain both the tag text and the coin character as a whole grapheme, asserted on the string `EVENT_NARRATION.pass` actually returns, not on the source literal."
    - "All 100 hand-written sea-creature strings are untouched (D-06). The fragment has no subject and no verb precisely so it works in both persons on all 50 lines."
    - "The coin goes through the existing emoji chokepoint rather than hand-rolled markup — a raw coin character in the builder body, swapped for the coin image later by `panel()`'s emojify call (D-50)."
    - "The engine gains no new source of nondeterminism: `4/src/engine/` still contains zero occurrences of the three wall-clock/random sources, exactly as it does today."
    - statement: "`doPass` changes the recorded purse value inside a pass event's state snapshot but does not change the event's shape — no key is added, removed or renamed in what the engine emits."
      verification: backstop
  artifacts:
    - "Game.prototype.doPass(p) in 4/src/engine/index.js"
    - "4/scripts/pass_coin_test.js"
    - "4/scripts/pass_narration_test.js"
  key_links:
    - "`4/src/engine/index.js` takeTurn fallback -> `this.doPass(p)`; `4/src/ui/flow.js` bot fallback -> `g.doPass(p)`; `4/src/ui/flow.js` human menu -> `appState.game.doPass(p)`. One method, three call sites."
    - "`doPass` -> `p.coins += 1` -> `this.ev({t:\"pass\", ...})` -> the event's own state snapshot. The arrow order is the predicate."
    - "`EVENT_NARRATION.pass` -> the appended no-break span -> `panel()`'s emojify chokepoint -> the coin image."
  prohibitions:
    - statement: "The narration must not claim a payment the engine did not make. The tag states an amount; if any of the three pass sites is missed, the copy lies to the player about their own purse. The two gates in this plan must both be green before the tag is considered shippable."
      status: unverified
      verification: test
    - statement: "The pass tag must not be conjugated, inflected, or agreed with any of the 50 hand-written sentences, and none of those 100 strings may be edited. The deleted seaSighting() conjugated, guessed articles and derived agreement, and got the plurals wrong; the seaLine contract at `4/src/ui/util.js:335-360` forbids all three (D-06)."
      status: unverified
      verification: test
    - statement: "The engine must not gain a wall-clock or random source. `4/src/engine/` is determinism-clean today and Phase 3 records a corpus against it; a single non-seeded call there makes lockstep replay meaningless and the corpus worthless."
      status: unverified
      verification: test

assumptions:
  - "Probe edge RULE-01/adjacency — no adjacency semantics. A pass pays exactly one dubloon per pass event; there is no equality, merge, or touching case, and two passes in the same turn are not reachable (the pass is the turn-ender)."
  - "Probe edge RULE-01/empty — no empty-input axis. `doPass(p)` is reached only from the turn loop with a live player object; there is no empty, single-element or null input shape to define."
  - "Probe edge RULE-02/adjacency — no adjacency semantics in a string append. The tag is concatenated after the sighting sentence with a single separating space; there is no merge or collide case."
  - "Probe edge RULE-02/ordering — the tag's position is fixed at the end of the narration text and is the same in both persons. There is no variable ordering to specify and no stability contract."
---

<objective>
Land the one new gameplay rule this milestone sanctions: passing pays a dubloon, and the narration
says so.

Purpose: this is the last change to the engine before its behaviour is frozen by a determinism
corpus in Phase 3. It has to be complete and correct now, not adjusted later — the same fix costs a
gated re-record if it lands after capture (`docs/DETERMINISM-RERECORD.md`). Passing is the
always-available turn-ender, so paying it changes an incentive at the centre of the game, which is
why plan 06 measures the result before the freeze.

Output: `Game.prototype.doPass(p)` called from all three pass-emission sites; the tag
`Recipe idea! (+1🌕)` appended by the renderer in one place; and two `4/` gates proving both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-before-the-engine-freezes/01-CONTEXT.md
@.planning/phases/01-before-the-engine-freezes/01-RESEARCH.md
@.planning/phases/01-before-the-engine-freezes/01-PATTERNS.md
@.planning/phases/01-before-the-engine-freezes/01-01-SUMMARY.md
@.claude/CLAUDE.md
@docs/DETERMINISM-RERECORD.md

**Bots and humans have identical rules and affordances** (`.planning/PROJECT.md` → Constraints;
CLAUDE.md §2). Bots pass, so bots are paid. This is not an open question and must not be raised as
one. The practical consequence is that all three code paths must agree, and two of the three are bot
paths.

**Show rendered copy, don't describe it** (CLAUDE.md §1, D-06). Wyatt picked this wording after
seeing real lines on screen and reversing himself twice. The narration gate in Task 2 prints a
sample of real rendered lines to stdout so the executor's SUMMARY can carry them, and plan 06 puts
them in front of him on his phone.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doPass — one shared method, three call sites, mutate before record</name>
  <files>4/src/engine/index.js, 4/src/ui/flow.js, 4/scripts/pass_coin_test.js</files>
  <read_first>
    - `4/src/engine/index.js` lines 901-915 — `doDock`, the sibling method whose naming and
      mutate-then-record ordering this copies exactly
    - `4/src/engine/index.js` lines 318-327 — `ev()`, which returns early on the record flag and
      snapshots every player's purse at call time. This is why the ordering is a predicate and not a
      style preference.
    - `4/src/engine/index.js` lines 2988-2996 — the engine fallback inside `takeTurn`, and line 2957
      where `takeTurn` emits its turn entry
    - `4/src/engine/index.js` line 956 — `nextSeaCreature`, which the pass event carries
    - `4/src/ui/flow.js` lines 1855-1866 — the human menu site, and the sea-cursor advance on the
      line immediately after it that must stay outside the shared method
    - `4/src/ui/flow.js` lines 2128-2143 — the animated bot fallback, and the comment block just
      above it explaining why this path is deliberately duplicated rather than inherited
    - `scripts/narration_test.js` lines 30-45 — the `check(name, actual, expected)` harness to copy
      verbatim
    - `scripts/narration_flow_test.js` lines 25-40 — the source-text setup this task's structural
      half copies for asserting a `flow.js` invariant without a DOM
    - `.planning/phases/01-before-the-engine-freezes/01-PATTERNS.md`, sections "`4/src/engine/index.js`
      — add `doPass(p)`", "A new RULE-01 test", and "RULE-01 structural check"
  </read_first>
  <behavior>
    - A game constructed with the record flag, driven to a state where a bot's turn resolves to the
      engine fallback, ends that turn with the acting captain's purse exactly one higher than before
      the turn, and with one new pass entry in the event log.
    - Calling `doPass(p)` directly raises `p.coins` by exactly 1 — not 0, not 2 — and appends exactly
      one pass entry.
    - The pass entry's own recorded state snapshot shows the acting captain's purse **after** the
      payment. Read the coins value out of the recorded snapshot for that seat and assert it equals
      the post-call purse. This is the ordering predicate; assert it directly rather than inferring
      it from source order.
    - The pass entry's key set is unchanged from a pre-change pass entry: the same keys, no key
      added, removed or renamed.
    - No other player's purse moves.
    - Structural, on `4/src/ui/flow.js` read as text: the human-menu region and the bot-fallback
      region each contain a call to the shared method, and neither contains a bare pass-event
      emission any more.
    - Structural, on `4/src/ui/flow.js` read as text: the sea-cursor advance still appears in the
      human-menu region and does NOT appear inside the engine's shared method.
  </behavior>
  <action>
    Add `doPass(p)` to the `Game` class in `4/src/engine/index.js`, placed as a sibling of `doDock`
    and following its shape exactly: increment `p.coins` by 1, **then** call `this.ev` with the pass
    event carrying the acting seat index and the next sea creature for that player. Mutate first,
    record second.

    That ordering is the requirement, not a preference. `ev()` builds its own state snapshot at the
    moment it is called, mapping every player's position, purse, ingredients, done flag and baking
    flag. Record before paying and the snapshot holds the pre-payment purse, so the replay scrubber
    shows a captain a dubloon short at the exact tick their narration claims they were paid. In the
    one phase where a determinism corpus is about to freeze this behaviour, that is expensive.

    Do not condition the coin increment on the engine's record flag. `ev()` already self-gates; the
    payment must not. (Planning resolved the question that made this worth stating: ordinary browser
    play does construct the game with the record flag true, at `4/src/orchestrator.js:1526` and
    `4/src/ui/board.js:1969`. The payment is unconditional regardless, which is what makes the
    question moot rather than merely answered.)

    Replace the bare pass-event emission at all three sites with a call to the shared method
    (RULE-01):
    - the engine fallback inside `takeTurn` becomes a call on `this`
    - the animated bot fallback in `4/src/ui/flow.js` becomes a call on the local game reference
    - the human menu site in `4/src/ui/flow.js` becomes a call on the game held in app state

    **Leave the sea-cursor advance on the line after the human-menu site exactly where it is.** It
    is per-device narration bookkeeping owned by one seat; bots walk their own derived offsets and
    never touch it. Folding it into the shared method would give it to bots.

    **Both bot paths must pay.** The flow-tier fallback is duplicated on purpose — the comment block
    above it says why — and fixing only the engine would fix the simulator and leave every real
    browser game exactly as broken.

    Add no wall-clock read and no random source anywhere under `4/src/engine/`. That directory is
    clean of all three today and Phase 3 records a corpus against it.

    Then create `4/scripts/pass_coin_test.js` following the `4/scripts/` house shell: shebang, a
    header naming what is gated and why the ordering matters, the local `check` harness copied
    verbatim, every assertion running before the exit, and an explicit process exit carrying the
    failure count. It has two halves — a headless engine half importing `../src/engine/index.js`
    directly and driving the behaviours in this task's `<behavior>` block, and a structural half
    reading `4/src/ui/flow.js` as raw text and asserting the call sites and the cursor placement.
    Locate the flow regions by searching for their surrounding code, not by line number.

    Before believing the green: temporarily swap the two statements inside the shared method so the
    event is recorded before the payment, run the gate, confirm the snapshot assertion fails and
    names itself, then restore. That assertion is the one that matters most in this file and it must
    be seen failing.
  </action>
  <verify>
    <automated>node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pass_coin_test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pass_coin_test.js` exits 0.
    - Its output contains a PASS line for each behaviour named in `<behavior>`, including the
      snapshot-ordering assertion and the unchanged-event-key-set assertion.
    - `grep -c 'doPass' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/engine/index.js`
      returns at least 2 — the definition and the engine call site.
    - `grep -c 'doPass' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/flow.js` returns
      exactly 2 — the human menu site and the bot fallback site.
    - `grep -c 'ev({t:"pass"' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/flow.js`
      returns 0 — no bare emission remains in the UI tier.
    - `grep -rc 'Math.random' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/engine/` sums to
      0, and the same holds for the two wall-clock sources. The engine is still determinism-clean.
    - `grep -c 'advanceSeaCursor' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/engine/index.js`
      returns 0 — the human-only cursor advance did not migrate into the engine.
    - The ordering failure was demonstrated: the SUMMARY records the failing assertion name and exit
      code observed with the two statements swapped.
    - `node 4/scripts/no_undef_check.js` and `node 4/scripts/stage_import_check.js` still exit 0.
  </acceptance_criteria>
  <done>All three pass sites pay one dubloon through one shared method that mutates before it records, proven by a gate that has been seen failing when the ordering is reversed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The pass narration says so — one appended fragment, 100 renderings</name>
  <files>4/src/ui/util.js, 4/scripts/pass_narration_test.js</files>
  <read_first>
    - `4/src/ui/util.js` lines 500-512 — the pass narration builder, and its existing comment about
      why the on-ship caption is generic
    - `4/src/ui/util.js` lines 335-360 — the seaLine contract. Both persons are read out verbatim;
      nothing is conjugated, no article is guessed, no agreement is derived. **This is what D-06 is
      built to honour.**
    - `4/src/ui/util.js` lines 560-570 and 670-705 — sibling narration builders already using the
      raw-coin-plus-no-break-span idiom
    - `4/src/ui/flow.js` lines 2226-2233 — the sailing-order precedent (G27/P7): a name and its
      amount are one readable unit, and the span wraps both
    - `4/src/ui/panel.js` line 434 — the emojify chokepoint that swaps the raw coin character for
      the coin image (D-50)
    - `4/src/shared/index.js` line 226 — the 50 hand-written entries. **Read to understand the
      shape; do not edit.**
    - `scripts/narration_test.js` lines 16-45 — the import style and harness this gate mirrors
    - `.planning/phases/01-before-the-engine-freezes/01-PATTERNS.md`, sections
      "`4/src/ui/util.js:502-509`" and "A new RULE-02 narration test"
  </read_first>
  <behavior>
    - For every one of the 50 sea-creature entries, rendered at the addressed person and at the
      third-person, the returned narration text ends with the tag and contains the coin character.
      That is 100 assertions.
    - The coin character survives as a whole grapheme in the returned string — assert on the string
      the builder actually returns, not on the source literal, so a mangled or split multi-code-point
      character is caught.
    - The tag appears exactly once in each rendering.
    - The tag is wrapped whole in a no-break span: the span's opening tag precedes the tag's first
      word and its closing tag follows the coin, with no other markup between.
    - The builder body contains no direct image-helper call — the coin is a raw character resolved
      later at the chokepoint.
    - The sighting sentence itself is unchanged: for each entry, the returned text still contains the
      entry's own rendered sentence verbatim ahead of the tag.
    - The on-ship caption and the wave pop are unchanged — the tag lands in the narration text only.
  </behavior>
  <action>
    In `4/src/ui/util.js`, extend the pass narration builder's text so it appends, after the sighting
    sentence and a single space, the exact fragment `Recipe idea! (+1🌕)` wrapped whole in
    `<span class="nobrk">` (D-06). Use a raw coin character, not an image-helper call: every other
    coin-amount narration line in this file uses the raw-character-plus-no-break-span form and relies
    on `panel()`'s emojify call to swap it for the coin image at the single chokepoint D-50 exists to
    guarantee. Hand-rolling markup here duplicates that chokepoint.

    Wrap the **whole** tag, not just the parenthetical — same reasoning as the sailing-order line
    (G27/P7): a unit and its amount are one readable thing and must not split across a line break.

    Change nothing else in the builder. The on-ship caption and the wave pop are cosmetic and
    separate from the narration text.

    **Do not touch the 50 hand-written entries in `4/src/shared/index.js`, in either person.** The
    tag is a subjectless fragment appended by the renderer in exactly one place, and that is the
    whole point: roughly 20 of the 50 lines end on the creature as the nearest grammatical subject,
    so any appended clause carrying a verb hands the pen to the shrimp. A fragment with no subject
    and no agreement works identically after all 50 sentences in both persons. The seaLine contract
    forbids conjugating, guessing articles, or deriving agreement from these strings — the deleted
    predecessor did all three and got the plurals wrong.

    Then create `4/scripts/pass_narration_test.js` following the `4/scripts/` house shell, importing
    the narration builder from `../src/ui/util.js` and the creature list from `../src/shared/index.js`,
    mirroring `scripts/narration_test.js`'s import style. Fan out across all 50 entries at both
    persons and assert the behaviours in this task's `<behavior>` block. Supply whatever minimal
    stand-in the builder's remaining parameters need; the builder is DOM-free.

    Have the gate also print three fully rendered sample lines to stdout — including the line that
    broke every earlier draft, the one ending on the donut shrimp — at both persons, so the SUMMARY
    can carry real rendered copy rather than a description of it. Wyatt reverses wording decisions
    against rendered lines, not against prose about them (CLAUDE.md §1).

    Before believing the green: temporarily remove the closing span from the appended tag, run the
    gate, confirm the wrapping assertion fails, then restore.
  </action>
  <verify>
    <automated>node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pass_narration_test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pass_narration_test.js` exits 0
      and reports at least 100 passing assertions across the two persons.
    - Its stdout contains three fully rendered sample lines at both persons, including the
      donut-shrimp line.
    - `grep -c 'Recipe idea!' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/util.js`
      returns exactly 1 — the tag is appended in one place only.
    - `grep -c 'nobrk' /Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/util.js` increased by
      exactly 1 relative to its pre-task value.
    - `git diff --name-only` does NOT list `4/src/shared/index.js`. All 100 hand-written strings are
      untouched.
    - `git diff --stat 4/src/ui/util.js` shows a single changed line in the narration builder.
    - The wrapping failure was demonstrated: the SUMMARY records the failing assertion name observed
      with the closing span temporarily removed.
    - `node 4/scripts/no_undef_check.js` still exits 0.
  </acceptance_criteria>
  <done>All 100 renderings carry the tag intact and unbroken, appended in one place, with the 50 hand-written sentences and the coin chokepoint both untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol, file, and key this phase creates. The plan-review source-grounding pass reads this
list to exclude newly-created names from drift verification.

**New files**
- `4/scripts/stage_import_check.js` (plan 01)
- `4/scripts/pp4_timeroff_check.js` (plan 02)
- `4/scripts/pass_coin_test.js` (plan 04)
- `4/scripts/pass_narration_test.js` (plan 04)
- `4/scripts/planner_singleton_check.js` (plan 05)
- `.planning/phases/01-before-the-engine-freezes/01-BALANCE-BASELINE.md` (plan 03)
- `.planning/phases/01-before-the-engine-freezes/01-BALANCE-DELTA.md` (plan 06)

**New symbols**
- `precededByAccessorKeyword(masked, idx)` — module-internal helper in both copies of
  `no_undef_check.js` (plan 01)
- `cleanupLegacyTimerKey(store)` — new export from `4/src/ui/stage.js` (plan 02)
- `Game.prototype.doPass(p)` — new engine method in `4/src/engine/index.js` (plan 04)

**New storage keys**
- `pp4_timerOff` — the per-game turn-clock preference key (plan 02, D-01)
- `pp4_timerOffCleaned` — the one-time cleanup marker key (plan 02, D-02)

**New CLI surface**
- `scripts/bot_ladder4.js` gains a `--json` flag emitting a machine-readable run record for the
  before/after diff; the existing positional `[games] [seedMult]` arguments are unchanged (plan 03)

**New copy string**
- The pass narration tag `Recipe idea! (+1🌕)` appended by `EVENT_NARRATION.pass` (plan 04, D-06)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine state → recorded event stream | `ev()` snapshots player state into the event log, which the replay scrubber and (from Phase 3) the determinism corpus read as truth. |
| Narration builder → rendered HTML in the panel | The builder returns an HTML string that `panel()` emojifies and inserts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | `doPass` mutate/record ordering | high | mitigate | Recording before paying writes a purse value into the event snapshot that contradicts the game state and the narration, and Phase 3 freezes that stream into a corpus. Mitigated by the mutate-first ordering copied from `doDock`, by the direct snapshot assertion in `4/scripts/pass_coin_test.js`, and by the requirement that the assertion be observed failing with the order reversed. |
| T-01-09 | Spoofing | the pass narration tag | medium | mitigate | The tag asserts a payment. If a pass site is missed, the interface tells the player something about their own purse that is false. Mitigated by requiring both gates green, and by the structural half of `pass_coin_test.js` asserting no bare pass emission survives in the UI tier. |
| T-01-10 | Tampering | new nondeterminism under `4/src/engine/` | high | mitigate | A single wall-clock or random call in the engine makes seeded lockstep replay meaningless and the Phase 3 corpus worthless. Mitigated by the explicit zero-occurrence acceptance criterion over `4/src/engine/` for all three sources, and by the shared method containing only an integer increment and an event call. |
| T-01-11 | Information Disclosure | the appended narration fragment | low | accept | The fragment is a fixed authored string with no interpolation of user input; the surrounding sighting sentence comes from a hand-written constant list. There is no injection surface to mitigate. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages. `package.json` declares no dependencies and this project has no build step. No supply-chain surface. |

**ASVS L1 assessment:** no authentication, session, access-control or cryptographic surface. The
only V5 touchpoint is an engine-controlled integer seat index flowing into the shared method; no
externally-supplied input is introduced. Two `high` threats are present and **both are dispositioned
`mitigate` with named, gated mitigations**, so the configured `block_on: high` gate is satisfied
rather than tripped.
</threat_model>

<verification>
1. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pass_coin_test.js` exits 0.
2. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pass_narration_test.js` exits 0.
3. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/no_undef_check.js` exits 0.
4. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/stage_import_check.js` exits 0.
5. `node /Users/wyattroy/Documents/Projects/pastrypirates/4/scripts/pp4_timeroff_check.js` exits 0.
6. `cd /Users/wyattroy/Documents/Projects/pastrypirates && npm test` still passes all 21 root gates.
7. The engine remains determinism-clean: zero occurrences of the three wall-clock/random sources
   under `4/src/engine/`.
8. Diff scope (CLAUDE.md §3): `git diff --name-only` lists only `4/src/engine/index.js`,
   `4/src/ui/flow.js`, `4/src/ui/util.js` and the two new `4/scripts/` gates. **`4/src/shared/index.js`
   does not appear**, and nothing under `v2/`, `v2bakeoff/`, `3/`, or root `src/` appears.
9. Both failure demonstrations (the reversed ordering, the removed closing span) are recorded with
   observed exit codes.
</verification>

<success_criteria>
- RULE-01 satisfied: a captain who passes receives one dubloon at every one of the three emission
  sites, through one shared method, with the purse mutated before the event is recorded.
- RULE-02 satisfied: the pass narration tells the captain they were paid, in both renderings, across
  all 50 sea-creature entries, with all 100 hand-written strings untouched.
- The engine gains no new source of nondeterminism, so Phase 3's corpus can be recorded once.
</success_criteria>

<output>
Create `.planning/phases/01-before-the-engine-freezes/01-04-SUMMARY.md` when done.

State in the summary which surfaces were checked (CLAUDE.md §2 consistency): name all three pass
sites individually, state that both bot paths pay, and state that the sea-cursor advance was
deliberately left outside the shared method. Paste the three rendered sample lines at both persons
verbatim — Wyatt judges copy against rendered lines, not descriptions of them.
</output>
