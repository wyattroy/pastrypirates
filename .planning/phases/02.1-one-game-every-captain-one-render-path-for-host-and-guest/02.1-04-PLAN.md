---
phase: 02.1-one-game-every-captain-one-render-path-for-host-and-guest
plan: 04
type: execute
wave: 4
depends_on: ["02.1-01", "02.1-02", "02.1-03"]
files_modified:
  - 4/src/ui/stage.js
autonomous: false
requirements: [PAR-07]
user_setup: []

must_haves:
  truths:
    - "playpastrypirates.com/4 serves a build carrying a new PP4_STAMP, bumped exactly once in this phase"
    - "The pushed diff touches only 4/ and .planning/ — no site-identity file moved and no root game file changed"
    - "Wyatt has hosted a real crew game in his own Safari, Claude has joined it as a driven Chrome guest, and the day counter, wind pill, camera framing, and prompt shape all matched between the two windows"
    - "Wyatt has played a stretch of the voyage himself, in Safari, and said it holds together — nothing in this phase closes on headless evidence alone"
    - statement: "main and origin/main are zero ahead and zero behind in both directions after the push, re-run rather than remembered"
      verification: backstop
  artifacts:
    - "A bumped PP4_STAMP in 4/src/ui/stage.js:32"
  key_links:
    - "4/ on main IS what playpastrypirates.com/4 serves — no build step, so a push is the deploy and the stamp is the only proof it arrived"
    - "The Phase 17 precedent (.planning/milestones/v1.2-phases/17-final-multiplayer-verification/) is the proven methodology this plan reuses: Wyatt hosts in real Safari, Claude drives the guest seat in Chrome — this is the first time anything in 4/ has been checked against real Safari at all"
  prohibitions:
    - "must not bump PP4_STAMP more than once in this phase — Wyatt's own convention from Phase 2 (one drop, one test)"
    - "must not let CNAME, robots.txt or sitemap.xml leave this repo, and must not hand-roll a deploy sync"
    - "must not report git state from memory or from earlier in the session; re-run the command"
    - "must not close this phase on headless evidence, however green — the human checkpoints in this plan are the gate, not a formality"
    - "must not attempt safaridriver or any headless Safari automation — ruled out by research (cannot run headless; a visible window would take over Wyatt's own machine)"
    - "must not use bakeoff-unsafe scenarios during the parity check — join with ?bakeoff=0, matching Phase 2's own scoping, since this phase does not touch bake-off code"
---

<objective>
Ship the one drop, then prove it the one way that actually proves it: Wyatt hosting in his own real
Safari, Claude driving the guest seat in Chrome, and the two windows compared directly — day counter,
wind pill, camera, prompt shape — followed by Wyatt playing a real stretch of the voyage himself.
Everything in Plans 01-03 was proven Chrome-to-Chrome, which cannot see a WebKit-specific failure;
this plan is the first time any of `4/` touches real Safari at all.

Purpose: this is the phase's own gate. CLAUDE.md rule 19 and this phase's own parent (Phase 2) both
exist because a green headless suite shipped seven visible bugs — nothing here closes without a human
who actually looked.

Output: one build stamp, a side-by-side Safari-host/Chrome-guest comparison with screenshots, and
Wyatt's own word that a real voyage held together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-one-game-every-captain-one-render-path-for-host-and-guest/02.1-RESEARCH.md
@.planning/phases/02.1-one-game-every-captain-one-render-path-for-host-and-guest/02.1-01-SUMMARY.md
@.planning/phases/02.1-one-game-every-captain-one-render-path-for-host-and-guest/02.1-02-SUMMARY.md
@.planning/phases/02.1-one-game-every-captain-one-render-path-for-host-and-guest/02.1-03-SUMMARY.md
@.claude/CLAUDE.md
@docs/GIT-AND-DEPLOY.md
@docs/DRIVING-THE-GAME.md
@.planning/milestones/v1.2-phases/17-final-multiplayer-verification/17-SAFARI-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The one drop — bump the stamp, prove the diff, push, and confirm sync</name>
  <files>4/src/ui/stage.js</files>

  <precondition>
    `git fetch origin` has been run in this task, and `git rev-list --count origin/main..main` and
    `git rev-list --count main..origin/main` both report 0 before anything is committed. Plans 01-03
    are committed and their own diffs are already scoped to `4/` and `.planning/`.
  </precondition>

  <read_first>
    - `docs/GIT-AND-DEPLOY.md` §5 — the full loop and the incident that earned it.
    - `.claude/CLAUDE.md` §6 — how the work reaches Wyatt's phone/machine, and the tell that a session
      skipped it (he reports an old build stamp — never a cache, since there is no build step).
    - `4/src/ui/stage.js` line 32 (`PP4_STAMP`) and line 985 (where it renders into the footer).
    - `.planning/STATE.md` → Current Position, which records the prior stamp (`2026-08-19b`), so the
      new one is visibly a successor rather than a guess.
    - `.planning/phases/02-multiplayer-revival/02-07-PLAN.md` Task 2 — the exact, already-proven
      version of this task; reuse its method rather than re-deriving it.
  </read_first>

  <action>
    Bump `PP4_STAMP` in `4/src/ui/stage.js:32` to a new value following the existing date-plus-letter
    convention, succeeding `2026-08-19b`. This is this phase's only stamp bump — three plans' worth of
    code changes reach Wyatt together, in one build, which he tests once.

    Prove the diff before pushing, not after: list every changed path across all four of this phase's
    plans and confirm every one falls under `4/` or `.planning/`. `CNAME`, `robots.txt`, and
    `sitemap.xml` must not appear. Confirm no root game file changed — the root game and `4/` are
    different files serving different URLs.

    Push, then pull, then re-run both `git rev-list --count` directions and confirm zero in both.

    Then tell Wyatt, in plain English, in the reply he reads:
    - the exact stamp to look for in the footer, and where the footer is;
    - that this build fixes the guest-side bugs he found reading Phase 2's fixes — the day counter,
      the wind pill, the camera not centring his own boat, and the flat-card prompt bug;
    - that the next step needs him to open Safari and host, and that Claude will join as the guest to
      compare the two screens directly — this is the FIRST time anything in `4/` has been checked
      against real Safari;
    - the exact URL to use, with `?bakeoff=0` in it (this phase does not touch the bake-off, and that
      flag keeps the test on the code this phase actually changed), and the Safari-cache trap: empty
      the cache first (⌥⌘E then ⌘R), because Safari caches ES modules per URL and a stale cache reads
      as a bug that is not there.

    Kill anything still running before replying.
  </action>

  <verify>
    <automated>git fetch origin &amp;&amp; git rev-list --count origin/main..main &amp;&amp; git rev-list --count main..origin/main</automated>
    Both must print 0 after the push and pull. Plus: the full changed-path list for this phase (all
    four plans) contains no path outside `4/` and `.planning/`.
  </verify>

  <acceptance_criteria>
    - `4/src/ui/stage.js:32`'s `PP4_STAMP` differs from `2026-08-19b` and follows the same
      date-plus-letter convention.
    - `grep -c 'PP4_STAMP = ' 4/src/ui/stage.js` returns 1.
    - The full changed-path list for this phase's four plans lists no path outside `4/` and
      `.planning/`, pasted into the reply.
    - `CNAME`, `robots.txt`, and `sitemap.xml` appear nowhere in that list.
    <!-- planner-discipline-allow: CNAME, robots.txt, sitemap.xml — the three site-identity filenames must be named here (that IS the safety rule) and asserted absent from the diff; the grep runs over the changed-path list, never over this plan file -->
    - `git rev-list --count origin/main..main` and `git rev-list --count main..origin/main` both
      return 0, run after the push and pull, in this task.
    - The reply states the exact stamp, the full URL with `?bakeoff=0`, the empty-cache instruction,
      and what changed, in plain English.
    - Zero `remote-debugging-port` and zero `http.server` processes remain, stated in the reply.
  </acceptance_criteria>

  <reversibility rating="costly">
    A push to `main` is live immediately. Revertible by another push, but the expensive part is not the
    code — Wyatt has one test session of attention to spend, and this is why three plans' worth of
    fixes were proven Chrome-to-Chrome before reaching this point.
  </reversibility>

  <done>
    `playpastrypirates.com/4` serves this phase's build, and Wyatt knows the stamp, the URL, and what
    changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wyatt hosts in real Safari</name>
  <files>none — this is a human verification checkpoint</files>

  <what-built>
    Three plans landed together: a guest's board state is now honest (day counter, wind pill, camera —
    all track the real game instead of freezing at spawn), a greyed button whose reason mentions a coin
    no longer breaks into a flat card, and the host and guest now render every prompt from one shared
    function instead of two hand-mirrored copies. All three were proven in driven Chrome-to-Chrome
    testing. None of it has ever touched real Safari — this is that check.
  </what-built>

  <how-to-verify>
    Open the URL from the previous message in **Safari** — first empty the cache (⌥⌘E then ⌘R) so you
    are not looking at a stale build. Confirm the footer reads the new stamp named in that message; if
    it reads the old one, stop and say so — that is never a cache, it means the work did not arrive.

    Tap **Host a Crew**, confirm your name, and you will land on the room screen with a code. Reply
    here with that code as soon as you have it — Claude will join as the guest in a driven Chrome
    window the moment you send it, and will report back what it sees compared side by side with what
    you're looking at in Safari.

    While you wait for that report, feel free to start sailing — the day counter, the wind pill in the
    top bar, and the camera following your own boat when you're asked to sail are the three things this
    phase fixed; you'll see them either working or not without needing to know what they used to do.
  </how-to-verify>

  <resume-signal>Reply with your room code once you're hosted in Safari.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Claude joins as a driven guest and compares both screens</name>
  <files>&lt;scratchpad&gt;/probe-02.1-04-safari-parity.mjs</files>

  <precondition>
    Task 2's checkpoint has been resumed with a room code from Wyatt's real Safari session, currently
    hosting live.
  </precondition>

  <read_first>
    - `.planning/milestones/v1.2-phases/17-final-multiplayer-verification/17-SAFARI-CHECKLIST.md` — the
      proven methodology this task reuses verbatim: Safari hosts, Chrome joins with the code, play
      through together.
    - `docs/DRIVING-THE-GAME.md` §1 (a fresh port, never reused this session), §5c (guest driving —
      distinct `--user-data-dir`, distinct `pp_id`), §8a (drive headless Chrome yourself over CDP).
    - `.planning/phases/02.1-one-game-every-captain-one-render-path-for-host-and-guest/02.1-01-SUMMARY.md`,
      `02.1-02-SUMMARY.md`, `02.1-03-SUMMARY.md` — the exact symptoms each plan fixed, and the exact
      checks each plan already ran Chrome-to-Chrome; this task repeats the SAME checks, now against
      real Safari on one side.
  </read_first>

  <action>
    Launch one headless-or-driven Chrome process (own `--user-data-dir`, a port not used earlier this
    session), navigate to `playpastrypirates.com/4/?bakeoff=0`, click **Join a Crew**, enter Wyatt's
    room code and a distinct name, and join.

    Run through the same checklist Plans 01-03 already proved Chrome-to-Chrome, now with Wyatt's window
    as the Safari side of the comparison:

    1. **Day counter** — read `#pp4Round`'s text on the guest (Chrome) window at a moment Wyatt confirms
       what day he's on in Safari; they must match.
    2. **Wind pill** — read `#pp4Pill`'s text on the guest window; ask Wyatt what his reads, or take a
       screenshot he can compare against his own screen; they must match.
    3. **Camera** — the next time the guest (Chrome) seat is asked to sail, screenshot the framing and
       confirm the guest's own boat is inside it, not off toward its spawn point.
    4. **Prompt shape** — at any ordinary prompt both windows receive, confirm the guest's renders as
       the same radial-or-flat shape the host's does (ask Wyatt to describe or screenshot his side,
       since Claude cannot see a real Safari window).

    Screenshot every check on the Chrome side. For anything only visible on Wyatt's screen (Safari
    itself), ask him directly and record his answer rather than assuming parity.

    Report the full comparison in plain English — what matched, what (if anything) did not — and tell
    Wyatt plainly that the next and final step is his own hands-on play, not this comparison.

    Kill the Chrome process before replying (leave the local nothing running — this task targets
    production, per docs/DRIVING-THE-GAME.md §9's "never verify against production" applying to
    developer-loop iteration, not to this phase-closing pass, which is deliberately against the real
    deploy).
  </action>

  <verify>
    <automated>node &lt;scratchpad&gt;/probe-02.1-04-safari-parity.mjs</automated>
    Records and prints the four checks above with their observed values from the Chrome side; the
    Safari-side values come from Wyatt's replies and are recorded alongside them, not asserted
    automatically.
  </verify>

  <acceptance_criteria>
    - A screenshot exists for the guest's day counter, wind pill, and sail-prompt camera framing on the
      Chrome side.
    - Wyatt's own reported values (day, wind, prompt shape) are recorded in the reply alongside the
      Chrome-side observations, and any mismatch is stated plainly rather than glossed over.
    - The reply is in plain English, naming what a player sees rather than internal state.
    - Zero `remote-debugging-port` processes remain, stated in the reply.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A verification pass against the live production deploy; no code changes in this task.
  </reversibility>

  <done>
    A real Safari host and a driven Chrome guest have been compared directly on every symptom this
    phase fixed, with the Safari-side evidence coming from Wyatt, not assumed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Wyatt plays a real voyage — this is the phase gate</name>
  <files>none — this is a human verification checkpoint</files>

  <what-built>
    Everything in this phase, compared side by side with Claude's driven guest in Task 3. Headless and
    driven-browser evidence is green, but that has been wrong before — Phase 2's own gate found seven
    faults on a build that had passed every automated check. The pass is you playing.
  </what-built>

  <how-to-verify>
    Keep playing the voyage you started in Task 2, in Safari, for a real stretch — sail a few turns,
    pass through at least one more day boundary, watch the wind pill as the forecast changes, and let a
    guest (the Chrome window Claude joined with, or a second real device if you'd rather) take a sail
    prompt so you can see whether the camera follows.

    If anything looks wrong — a stuck day counter, a blank wind pill, a camera that doesn't find the
    boat, a prompt that renders as a flat card when it should bloom, or anything else that reads as
    "the guest side is behaving differently than my side" — say so and where in the voyage it happened.
  </how-to-verify>

  <resume-signal>Type "approved", or describe what went wrong and where in the voyage it happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → GitHub Pages → the live domain | A push to `main` is the deploy; there is no build step and no staging between it and real players |
| `4/` tree → repo root tree | A mis-rooted edit applies cleanly to the wrong tree and every safety signal still reports success |
| site-identity files → any second location | `CNAME` is a claim on `playpastrypirates.com`; a copy elsewhere does not fail safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-08 | Denial of Service | `CNAME` / `robots.txt` / `sitemap.xml` leaving this repo takes the live game down for real players | critical | mitigate | Task 1 — the full changed-path list is pasted into the reply and asserted to contain no path outside `4/` and `.planning/`; no sync is hand-rolled |
| T-02.1-09 | Tampering | A mis-rooted edit lands in the root game instead of `4/` and every check still passes | high | mitigate | Task 1 — absolute paths throughout, changed-path list proves the tree that must not appear did not |
| T-02.1-10 | Denial of Service | Three plans' worth of changes ship together and a regression in any one is only caught by Wyatt's own play, since this is the first Safari pass | medium | mitigate | Every prior plan in this phase was proven Chrome-to-Chrome with red-proofed checks before reaching this plan; Task 3's driven comparison and Task 4's human gate are the last two independent checks before the phase closes |
| T-02.1-SC | Tampering | Supply chain — package installs | n/a | accept | Zero packages installed by this phase |
</threat_model>

<verification>
- Git state is re-run, never recalled — both counts zero in both directions after the push.
- A real Safari host and a driven Chrome guest were compared directly, with screenshots, per CLAUDE.md
  rule 19 — the first time anything in `4/` has been checked against real Safari.
- Nothing closed on headless evidence: the phase's actual gate is Task 4's checkpoint.
</verification>

<success_criteria>
`playpastrypirates.com/4` carries a new build stamp Wyatt has been told to look for; a real Safari host
and a driven Chrome guest were shown matching on every symptom this phase fixed; and Wyatt has played a
real voyage and said it holds together.
</success_criteria>

<output>
Create `.planning/phases/02.1-one-game-every-captain-one-render-path-for-host-and-guest/02.1-04-SUMMARY.md`
when done, and update `.planning/STATE.md` with the new build stamp and Wyatt's own words from the
Task 4 checkpoint, verbatim.
</output>
