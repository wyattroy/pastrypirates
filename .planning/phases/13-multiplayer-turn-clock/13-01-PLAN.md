---
phase: 13-multiplayer-turn-clock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/net/writers.js
  - src/net/watchers.js
  - src/net/index.js
  - scripts/net_contract_check.js
  - src/orchestrator.js
  - src/ui/util.js
  - src/main.js
  - src/ui/panel.js
autonomous: true
requirements: [CLOCK-02]

must_haves:
  truths:
    - "In a multiplayer game any player (host or guest) can trigger ▶/⏸ and the whole game freezes on every connected tab — the countdown stops and no bot captain acts until someone resumes (D-04/D-06)."
    - "Pausing writes rooms/{room}/paused via netSetPaused; every client's watchPause mirrors appState.shotClockPaused, and ONLY the host runs applyPauseState to mutate shotClockDeadline/shotClockPauseElapsed (D-06)."
    - "On resume the current player's countdown continues from the remaining time it had when paused, not a fresh 30s — reusing the existing shotClockPauseElapsed math (D-07)."
    - "The ⏱ timer on/off toggle remains available in multiplayer alongside the new ▶/⏸ pause — two distinct controls, both visible in a real 2+ human game (D-05)."
    - "applyPauseState(nowPaused) is idempotent per boolean value: repeated identical toggles coalesce (Firebase .set keeps the final value), so a double-click does not corrupt the paused state."
    - statement: "Two different players toggling pause within one Firebase round-trip is an accepted low-stakes race — only the host mutates shotClockDeadline/shotClockPauseElapsed; worst case is a net no-op the user resolves by clicking again (mirrors how timerOff handles the identical race today)."
      verification: backstop
    - "npm test passes, including the net_contract_check.js watcher-inventory gate now reading 19 watchers and 19 registry.attach() calls."
    - "node scripts/determinism_baseline.js --verify stays green 30/30 — no pause/timer change touches src/engine/ (D-10/VERIFY-02)."
  prohibitions:
    - "MUST NOT reimplement a second, parallel bot-freeze mechanism — route all freezing through the single existing appState.shotClockPaused / waitWhilePaused()."
    - "MUST NOT let any pause/timer change reach src/engine/ or alter lockstep replay."
    - "MUST NOT give guests authority to mutate shotClockDeadline/shotClockPauseElapsed — guests only write the network flag."
  artifacts:
    - "src/net/writers.js — netSetPaused"
    - "src/net/watchers.js — netWatchPaused"
    - "src/net/index.js — barrel re-export of netSetPaused/netWatchPaused"
    - "src/orchestrator.js — togglePause, watchPause"
    - "src/ui/util.js — applyPauseState (extracted); de-gated toggleShotClockPause"
    - "src/main.js — onTogglePause seam entry"
    - "src/ui/panel.js — de-gated ▶/⏸ visibility + guest-mirrored paused render"
    - "scripts/net_contract_check.js — inventory bumped to 19"
    - "Firebase node path rooms/{room}/paused"
  key_links:
    - "netWatchPaused added to scripts/net_contract_check.js WATCHER_INVENTORY AND the attachCount literal bumped 18→19 in the SAME commit that adds netWatchPaused to src/net/watchers.js — else npm test fails the watcher-inventory gate."
    - "#scPause.onclick rewired from toggleShotClockPause to togglePause so a guest (non-host) actually reaches the network path instead of hitting the isHost early-return."
    - "watchPause() wired in beginGame() alongside watchTimer() so every client tracks the shared paused flag."
    - "onTogglePause: orchestrator.togglePause added to main.js setNetHandlers so panel.js can reach it through the existing seam."
---

<objective>
Deliver CLOCK-02: a true play/pause control in multiplayer that any player can
trigger and that freezes the entire game (countdown + bot captains) on every
connected tab, resuming from where it left off. This is the phase's tracer — it
wires one entry point (togglePause) through every layer the phase touches:
net writer → Firebase node → net watcher → orchestrator → shared pause state →
UI render.

Purpose: prove the host-authoritative pause-sync architecture end-to-end on the
executor's best early-context tokens, since CLOCK-01 and CLOCK-03 build on the
same togglePause/onTogglePause seam this plan establishes.

Output: a networked pause flag (rooms/{room}/paused) mirroring the existing
timerOff pattern byte-for-byte, reusing the existing waitWhilePaused() freeze
with zero new freeze code, surfaced as a visible control for every player.

<assumption_delta_decision>
noun: the clock-control surface (the shot-clock panel's control buttons).
decision: add-alongside — per locked decision D-05 the existing ⏱ timer on/off
toggle is KEPT in multiplayer AND the ▶/⏸ pause is ADDED. The two controls are
NOT redundant: ⏱ removes countdown pressure (decisions never time out); ▶/⏸
freezes the whole game (bots included). Advisory record only, non-blocking.
</assumption_delta_decision>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multiplayer-turn-clock/13-CONTEXT.md
@.planning/phases/13-multiplayer-turn-clock/13-RESEARCH.md
@.planning/phases/13-multiplayer-turn-clock/13-VALIDATION.md
</context>

## Artifacts This Phase Produces (NEW symbols — for source-grounding drift exclusion)

Exact names are the implementer's discretion (RESEARCH Assumption A1 — the SHAPE
is load-bearing, not the names). This plan creates:

- `netSetPaused` (src/net/writers.js)
- `netWatchPaused` (src/net/watchers.js)
- `togglePause`, `watchPause` (src/orchestrator.js)
- `applyPauseState` (src/ui/util.js)
- `onTogglePause` net-handler seam entry (src/main.js `setNetHandlers({...})`)
- Firebase node path `rooms/{room}/paused`

(Later plans add: `SESSION_SCHEMA_V`/`SOLO_SCHEMA_V` + `v:` field on pp_sess/pp_solo
in 13-02; the `#shotClockNum` clickable-resume affordance in 13-03.)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end multiplayer pause sync — one path, host↔guest, wired through every layer</name>
  <files>src/net/writers.js, src/net/watchers.js, src/net/index.js, scripts/net_contract_check.js, src/orchestrator.js, src/ui/util.js, src/main.js</files>
  <read_first>
    - src/net/writers.js — analog: netSetTimerOff (~L57-59), the withReporter+db.ref(...).set() shape to clone.
    - src/net/watchers.js — analog: netWatchTimerOff (~L60-63), the registry.attach({scope:"room",event:"value",label:...}) shape to clone.
    - src/net/index.js — the barrel: netSetTimerOff/netWatchTimerOff appear in both the import list (~L21,L28) and the re-export list (~L46,L53); a new pair must be added to BOTH.
    - scripts/net_contract_check.js — the hardcoded WATCHER_INVENTORY array (~L264-271, 18 names), the attachCount !== 18 assertion (~L293), the error text at ~L295, and the PASS/FAIL log line at ~L319 ("all eighteen … exactly eighteen").
    - src/orchestrator.js — analogs: toggleTimer (~L147-151) and watchTimer (~L153-171) for the write/watch host-authoritative template; the net-import list (~L76-91) where netSetTimerOff/netWatchTimerOff are imported; beginGame's watchTimer() call site (~L972); wireLobby's `$("scPause").onclick=toggleShotClockPause;` (~L1011).
    - src/ui/util.js — toggleShotClockPause (~L612-628, the pause/resume state math and its `if(!appState.isHost||!soloBotGame())return;` gate at L613); waitWhilePaused (~L535-539); confirm shotClockPaused/shotClockPauseElapsed/shotClockDeadline/shotClockTimer already live on appState.
    - src/main.js — the setNetHandlers({...}) composition block (~L71-99) where onSetClockUI/onBroadcastClock are registered; the visibilitychange listener (~L147-151) which MUST be left untouched (see prohibitions / RESEARCH Pitfall 2).
    - src/net/registry.js — confirm registry.attach signature so netWatchPaused matches sibling watchers.
    - RESEARCH.md Architecture Patterns 1 & 2 hold the exact reference code for every symbol below.
  </read_first>
  <action>
Add a host-authoritative boolean pause node that mirrors the timerOff sync path exactly, and reuse the existing solo freeze for multiplayer — no new freeze code.

1. In src/net/writers.js add netSetPaused(db, room, val, onError) that writes rooms/{room}/paused via the same withReporter(db.ref(...).set(val), onError) shape as netSetTimerOff.
2. In src/net/watchers.js add netWatchPaused(db, room, handler) that attaches a room-scoped value watcher on rooms/{room}/paused via registry.attach with label "paused", mirroring netWatchTimerOff.
3. In src/net/index.js add netSetPaused and netWatchPaused to BOTH the import block and the re-export block alongside their timerOff siblings.
4. In scripts/net_contract_check.js add "netWatchPaused" to WATCHER_INVENTORY, change the attachCount comparison from 18 to 19, and update the failure-message and PASS/FAIL log-line text so the human-readable count reads 19/"nineteen" instead of 18/"eighteen". This MUST land in this same task (RESEARCH Pitfall 1 — the check hardcodes the count deliberately and will fail the instant netWatchPaused is added otherwise).
5. In src/orchestrator.js: import netSetPaused/netWatchPaused from ./net/index.js; add togglePause() that, when appState.db && appState.room, calls netSetPaused(appState.db, appState.room, !appState.shotClockPaused, netFail("pause")) and otherwise falls back to the local toggleShotClockPause() (solo/pass-and-play); add watchPause() that attaches netWatchPaused and, on each value change, sets the host branch to run applyPauseState(nextValue) and the guest branch to mirror appState.shotClockPaused = nextValue, then calls the setClockUI refresh — structurally identical to watchTimer(). Call watchPause() in beginGame() immediately after the existing watchTimer() call so every client tracks it. Rewire wireLobby's `$("scPause").onclick` from toggleShotClockPause to togglePause so a guest actually reaches the network path.
6. In src/ui/util.js: extract the pause/resume state-mutation body of toggleShotClockPause into a standalone exported applyPauseState(nowPaused) helper containing ONLY the shotClockDeadline/shotClockPauseElapsed/shotClockTimer math (no isHost/soloBotGame gate inside it). Rewrite toggleShotClockPause as a thin wrapper that keeps an `if(!appState.isHost)return;` guard, REMOVES the soloBotGame() half of the old gate (per D-05/D-06), calls applyPauseState(!appState.shotClockPaused), then the existing setClockUI refresh. The D-07 "resume where it left off" math must be unchanged — only relocated.
7. In src/main.js: add onTogglePause: orchestrator.togglePause to the setNetHandlers({...}) object. Do NOT modify the visibilitychange listener's ui.soloBotGame() guard (out of scope — extending auto-hide-pause to multiplayer is a flagged Open Question, not this phase).

Guests must NEVER call applyPauseState — only the host branch of watchPause does. Guests write the flag and mirror the boolean for rendering only.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node scripts/determinism_baseline.js --verify</automated>
    <human-check>
      In a 2-tab MCP multiplayer game (clear localStorage + set a UNIQUE pp_id per tab before each tab loads, sequentially — see 13-VALIDATION.md MP gotcha): from the HOST tab console invoke the wired togglePause() (or click #scPause on the host). Assert: (a) Firebase rooms/{room}/paused === true; (b) window.__pp_app_state_debug().shotClockPaused === true on BOTH host and guest tabs; (c) a bot captain's turn does not advance while paused (board/event count unchanged). Then from the GUEST tab invoke togglePause() and assert both tabs return to shotClockPaused === false and play resumes, with the host's countdown continuing from the remaining time (not reset to 30). Use a real .click()/call — NOT document.hidden spoofing (RESEARCH Pitfall 3).
    </human-check>
  </verify>
  <acceptance_criteria>
    - src/net/writers.js contains `export function netSetPaused` writing to `rooms/" + room + "/paused"`.
    - src/net/watchers.js contains `export function netWatchPaused` with a registry.attach call labeled "paused".
    - src/net/index.js re-exports both netSetPaused and netWatchPaused.
    - scripts/net_contract_check.js WATCHER_INVENTORY includes "netWatchPaused" and the attachCount comparison reads 19 (not 18).
    - src/orchestrator.js contains `export function togglePause` and `export function watchPause`, watchPause() is called inside beginGame(), and wireLobby wires `$("scPause").onclick=togglePause`.
    - src/ui/util.js contains `export function applyPauseState` and toggleShotClockPause no longer references soloBotGame() in its gate.
    - src/main.js setNetHandlers({...}) includes an onTogglePause entry mapping to orchestrator.togglePause; the visibilitychange listener at ~L147-151 is byte-for-byte unchanged.
    - `npm test` exits 0 (all 9 gates green, including watcher inventory completeness reading 19).
    - `node scripts/determinism_baseline.js --verify` prints 30/30 (or "All seeds passed.").
    - Behavior: pausing from either tab sets rooms/{room}/paused and window.__pp_app_state_debug().shotClockPaused===true on host AND guest; a bot turn does not advance while paused; resume continues the countdown from the remaining time.
  </acceptance_criteria>
  <done>Invoking pause from either the host or a guest in a 2-tab multiplayer game freezes the whole game (countdown + bots) on both tabs via rooms/{room}/paused, and resuming continues from the remaining time; npm test (incl. the 19-watcher inventory gate) and determinism 30/30 are green. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the ▶/⏸ pause control and paused state to every player in multiplayer</name>
  <files>src/ui/panel.js</files>
  <read_first>
    - src/ui/panel.js setClockUI() (~L51-133): the pause-button visibility line (~L64, `pauseEl.style.display=(appState.isHost&&soloBotGame()&&!appState.liveDone)?...`), the ⏱ toggle visibility line (~L70, keep as-is), and the TWO paused-render branches gated on `appState.isHost&&appState.shotClockPaused` (~L82 inside the `if(!state)` block, and ~L98 in the state-truthy block).
    - src/ui/panel.js imports (~L34-46): confirms appState, PAUSE_SYMBOL_IMG/PLAY_IMG, and netHandlers are already imported (netHandlers from ./handlers.js — the seam Task 1's onTogglePause entry populates).
    - RESEARCH.md "Code Examples → setClockUI() de-gating" for the exact before/after of L64 and both paused branches.
  </read_first>
  <action>
De-gate the shot-clock panel so the ▶/⏸ pause control and the paused visual appear in multiplayer for every player (not just the host, not just solo), leaving the ⏱ toggle logic untouched.

1. Change the pause-button (#scPause) visibility so it is shown for every player whenever the game is live — remove the `appState.isHost && soloBotGame()` condition and gate only on `!appState.liveDone`. This makes ▶/⏸ appear in both solo and multiplayer (D-09), and — combined with Task 1's #scPause.onclick=togglePause rewire — lets a guest pause (D-06).
2. Leave the ⏱ #scTimerToggle visibility line exactly as-is (still shown in multiplayer via its `!soloBotGame()` condition) — D-05 keeps it alongside the new pause.
3. In BOTH paused-render branches, drop the `appState.isHost&&` prefix so the condition becomes `appState.shotClockPaused` alone — every client now mirrors that flag via Task 1's watchPause, so guests render the "paused" state (big PAUSE_SYMBOL_IMG in #shotClockNum, "paused" label, "tap ▶ to resume" sub) identically to the host.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify</automated>
    <human-check>
      In a 2-tab MCP multiplayer game (unique pp_id per tab): confirm both the ▶/⏸ (#scPause) button AND the ⏱ (#scTimerToggle) button are visible on both tabs during play (D-05). Real .click() on #scPause from the GUEST tab → assert #shotClockPanel shows "paused" (wrap has class "paused") on BOTH tabs, a bot turn does not advance, and window.__pp_app_state_debug().shotClockPaused===true on both. Click again to resume and confirm the countdown continues from the remaining time. Then repeat in a SOLO game to confirm ▶/⏸ still works there (no regression).
    </human-check>
  </verify>
  <acceptance_criteria>
    - src/ui/panel.js #scPause visibility no longer references soloBotGame() or appState.isHost and gates on !appState.liveDone.
    - src/ui/panel.js #scTimerToggle visibility line is unchanged (still gated on !soloBotGame()).
    - Neither paused-render branch is gated on appState.isHost; both gate on appState.shotClockPaused alone.
    - `node scripts/determinism_baseline.js --verify` prints 30/30.
    - Behavior: a guest clicking #scPause shows "paused" on all tabs and freezes bots; both ▶/⏸ and ⏱ controls are visible in multiplayer.
  </acceptance_criteria>
  <done>Every player in a multiplayer game sees the ▶/⏸ pause button and the ⏱ toggle side by side, a guest's click freezes the whole table with the paused visual on all tabs, and solo pause is unregressed; determinism 30/30 green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any client (guest or host) → Firebase RTDB `rooms/{room}/paused` | any holder of the 4-char room code may write the pause flag (same as every existing room node) |
| Firebase `rooms/{room}/paused` → host game loop | only the host reacts authoritatively (applyPauseState mutates timing state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Denial of Service | `netSetPaused` write to `rooms/{room}/paused` | low | accept | Identical exposure already exists for `timerOff`/`narr`/`dlog` — any room-code holder can already write every room node; a guest pausing is not a NEW privilege (ASVS V4, accepted existing risk). Broader RTDB per-seat write ACLs are explicitly out of scope for this phase. |
| T-13-01b | Tampering | guest attempting to mutate `shotClockDeadline`/`shotClockPauseElapsed` | low | mitigate | Only the host branch of `watchPause` runs `applyPauseState`; guests write the boolean flag only and never touch deadline/pauseElapsed locally (RESEARCH Anti-Pattern) — prevents countdown desync. |

_No package-manager installs in this plan (zero new npm/pip/cargo deps), so no T-13-SC supply-chain checkpoint applies._
</threat_model>

<verification>
- `npm test` green (9 gates, incl. watcher inventory reading 19) after Task 1.
- `node scripts/determinism_baseline.js --verify` green 30/30 after each task (D-10/VERIFY-02).
- Per-wave: full `npm test` after both tasks merge.
- Manual/MCP (per 13-VALIDATION.md Manual-Only table): guest-initiated pause freezes countdown + bots on all tabs; resume continues from remaining time; both ▶/⏸ and ⏱ controls visible in multiplayer.
</verification>

<success_criteria>
Any player (host or guest) can pause and resume a multiplayer game; the freeze halts the countdown AND bot actions on every tab; resume picks up from the remaining time; the ⏱ toggle still coexists; npm test and determinism 30/30 stay green.
</success_criteria>

## Flagged Planner Assumptions (spec-less edge probe — surfaced, not dropped)

- CLOCK-02 **adjacency** edge: N/A — pause is a single boolean node, not a merge/collide/separate of adjacent values. No defensible acceptance criterion; surfaced rather than invented.
- CLOCK-02 **empty** edge: N/A — the node's only states are true/false/absent; absent (null) coerces to false, the safe default handled by watchPause. Surfaced.
- CLOCK-02 **ordering** edge: N/A — no ordering of equal elements; Firebase `.set()` keeps the last write. Surfaced.
- Guest paused-seconds cosmetic: in the state-truthy paused branch a guest has no locally-computed shotClockPauseElapsed, so the numeric seconds shown to a guest while paused is host-authoritative/approximate — the primary paused visual is the big symbol + "paused" label, which renders correctly for guests. Acceptable per RESEARCH's recommended de-gate; flagged so it is not mistaken for a defect.

<output>
Create `.planning/phases/13-multiplayer-turn-clock/13-01-SUMMARY.md` when done.
</output>
