---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 02
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - 4/src/orchestrator.js
  - 4/src/ui/flow.js
  - 4/src/ui/stage.js
  - 4/src/ui/util.js
autonomous: true
requirements: [PAR-09]
group: A
drop: "Group A — one director"
items: [17, 18, 19, 20, 21, 15]
architecture_change: true
engine_change: false

must_haves:
  truths:
    - "A two-tab crew game reproduces NONE of the eight divergences in Wyatt's side-by-side screenshots (D-26). That table is the acceptance list, not a summary of one."
    - "The host draws its screen from the same stream it publishes, through the same code a guest draws from — so a change to how anything is drawn reaches both screens because there is only one place it is drawn from (D-25)."
    - "Solo and pass-and-play still work, with no Firebase room at all — the host's own screen never waits on a network round trip."
    - "Wyatt gets an honest answer to item 18: the two parallel code paths he named are two ORCHESTRATIONS, and the host is now a listener like every other client (D-24)."
    - "Every stage that landed can be pointed at; every stage that did not is written down by name (D-04)."
    - "Wyatt is told a new PP4_STAMP and can see it live (D-02)."
  artifacts:
    - 4/src/orchestrator.js
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-DIRECTOR-MAP.md
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/a-*.png
  key_links:
    - "The fork at 4/src/orchestrator.js:1654-1655 — runLiveNet() on the host, nine watchers on a guest. This is the thing being restructured."
    - "watchChat() at 4/src/orchestrator.js:323, called one line BELOW the fork by every client including the host — the worked example of the shape being copied."
    - "ask() at 4/src/ui/util.js:1555 — seatLocal(seat) ? localAsk(...) : onRemotePrompt(...). The input fork, and the seam Stage 4 would have to move."
    - "remotePrompt (4/src/orchestrator.js:1144) returns a promise resolved by sendResponse (:1156) — an answer travels back over Firebase, which is why the prompt channel is NOT chat-shaped."
    - "netSetPrompt (4/src/net/writers.js:71) dereferences db with no null guard — solo and pass-and-play have no db, which is why a pure Firebase loopback cannot be the director."
---

<objective>
**Group A — ONE DIRECTOR.** The second of six drops (D-01), and the only architectural change in the
phase.

**The premise this plan was first written against was too small, and it has been replaced.** D-06
named two narration call sites; that was correctly traced and far too narrow. **D-24 is the root
cause: there are TWO DIRECTORS.** At `4/src/orchestrator.js:1654-1655` the host's screen is driven
by the game loop — `runLiveNet()` plays the voyage and calls the renderers directly as it goes —
while a guest's screen is driven by **nine independent Firebase listeners**, each deciding on its own
what to draw and when. **The renderers are shared, which is why 02.1's state-layer fix helped and why
this was missed. The orchestration is not.** Every renderer has two independent sets of callers that
can drift, and have.

**That is the "two parallel code paths" of item 18.** D-06 and D-07 are **superseded by D-25, not
withdrawn** — the narration divergence is one instance of the general fault, and it is still fixed
here.

**Wyatt's pick, verbatim, told plainly that it was bigger and riskier and touched the host path that
works for him today: "do it properly" (D-25).** The host becomes a listener to its own broadcast: it
computes the game and publishes, then draws from that same stream exactly as a guest does. **The
precedent is one line below the fork** — `watchChat()` is called by every client including the host,
and cannot drift. It is a worked example in the same file, not a theory.

**How closely the game display can follow `watchChat`, answered honestly** — this is the question the
plan owes, and the answer shapes the staging:

- **Display-only channels can follow it exactly.** Narration, board, camera, ribbon, battle stage,
  turn order: the host feeds them and draws from them, nothing travels back. This is chat's shape,
  one-way, and it carries **seven of Wyatt's eight divergences**.
- **Input-carrying channels cannot follow it as-is.** Chat is fire-and-forget; a prompt is not. The
  host's loop **awaits an answer** — `ask()` at `4/src/ui/util.js:1555` forks to `localAsk` for its
  own seat and to `remotePrompt` for a remote one, and `remotePrompt` returns a promise that
  `sendResponse` resolves over Firebase. Making the host answer its own prompt through that round
  trip is a real change with real risk, and it carries **one** divergence.
- **And the transport cannot be Firebase for the host's own screen.** `runLiveNet()` drives **solo
  and pass-and-play as well as a networked host** (the fork is `if(appState.isHost)`, which is true
  in solo, where there is no room). `netSetPrompt` dereferences `db` with no null guard
  (`4/src/net/writers.js:71`). **A host that drew only from its own Firebase echo would take solo and
  pass-and-play down.** Chat gets away with it because chat is switched off entirely in solo
  (`4/src/orchestrator.js:1645`). The game display cannot be.

**So "one director" here means one local dispatch that both tiers draw from, with Firebase as the
transport for remote clients only** — the host feeds the dispatch directly and mirrors to Firebase
when `db && room`, exactly as today's `isHost && db && room` guards already do; a guest feeds the
same dispatch from its listeners. One place anything is drawn from; no round trip on the host's own
screen; solo unaffected. That is faithful to D-25 and survives contact with solo.

Purpose: drift becomes structurally impossible instead of kept away by discipline.
Output: one draw path, seven divergences closed at the safe stop, and an honest list of what is left.

**D-04 is the stated exit condition, not a note.** If this balloons: **land what is safe, write down
exactly what is left, and move to Group B.** Wyatt's ruling. The staging below exists so that
stopping is always possible at a clean line, and **Task 5 runs whether or not Stage 4 was attempted.**

**D-05 still stands and still protects this work: do NOT delete the `isHost` branches.** They decide
who broadcasts and who creates the room, and that is the project's own sanctioned residue. **The fork
at `:1654` is being restructured, not deleted — the host still computes the game.**

**Determinism: no engine change.** Nothing in this plan alters what `4/src/engine/index.js` emits.
Plan 04 owns the one sanctioned engine change in this phase; **a second one is a re-scope, not a
detail.** If a stage appears to need one, stop that stage and write it down.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-RESEARCH.md
@.planning/todos/pending/narration-two-schedulers-unenforced.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<absolute_paths>
Every path is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`. The game under
development is `4/`. **Never touch a root-level `src/` path.**
</absolute_paths>

<the_eight_divergences>
## D-26 — Wyatt's acceptance list. Group A is done when a two-tab session reproduces NONE of these.

Read off his side-by-side screenshots. **Hang every verification on this table.**

| His shot | Host shows | Guest shows | Owning channel | Stage |
|---|---|---|---|---|
| 17a — Ahoy | the message once, in the centre card | **the same message twice** — a dark top strip *and* the centre card | narration + draft card | 1 |
| 17b — the wait | "⚓ Waiting for yer mateys…" | "⚓ Everyone's choosing their recipe…" | narration | 1 |
| 17c — drawing lots | the text once | **the text twice** — a bubble behind, plus the card | narration + draft card | 1 |
| 19 — waiting | **no narration at all**, board reads as dead | prompt still up | narration | 1 |
| 20 — the director | camera parked elsewhere | camera centred on the active boat | camera / active seat | 2 |
| 20 — sail window | **no sail squares** | sail squares highlighted | prompt | 4 |
| 20 — CAPTAINS panel | Wyargh's row first | his own row first — **different order** | turn order / rows | 3 |
| 21 — top-bar boats | updating with the turn | not updating | active seat / ribbon | 2 |

**Read the direction of these before assuming which side is wrong.** In 17a, 17c and 21 the guest is
worse. **In 19, 20-director and 20-sail the HOST is worse — the listener path is already drawing the
better picture.** That is evidence for D-25 rather than against it: converting the host toward the
listener path moves it toward behaviour that is already correct in three of the eight shots. It also
means "make the guest match the host" would have been the wrong fix in three places.

**The CAPTAINS row-order difference is OBSERVED, NOT EXPLAINED.** It may be deliberate — a "me first"
ordering so each captain finds their own row. **Measure before treating it as a bug** (CLAUDE.md rule
6). Stage 3 reproduces it and asks before changing anything.

**D-27 — the host's recipe picker in 17b is NOT a fault, and this is recorded so nobody reaches for
it later.** It appears blank beside the guest's populated one. Asked and answered — Wyatt: *"the
picker works, i just screenshotted it before the text finished animating in."* **Do not add it as an
item and do not fix it.** It looks exactly like a ninth divergence to anyone comparing that image.
</the_eight_divergences>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the channel map before moving a line — which of the nine draws what, and which carry an answer back</name>
  <files>.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-DIRECTOR-MAP.md</files>
  <read_first>
    **`.planning/todos/pending/narration-two-schedulers-unenforced.md`** — read before touching any
    narration timing. This area was scoped and deliberately deferred **twice** (Phase 15
    verification, then 2026-07-30), and its own recommendation is *"Do it on its own… and add the
    gate in the same commit as the unification."* Reading the graveyard before re-running a settled
    argument is CLAUDE.md rule 10. **The gate half cannot be honoured here:**
    `scripts/host_guest_parity_check.js` reads the ROOT game's `src/` and never loads `4/`. Closing
    that is Phase 3 / TEST-06, **not this phase** — record it, do not build it (CLAUDE.md rule 7).

    **`watchChat()` at `4/src/orchestrator.js:323`, and its call one line below the fork at `:1656`.**
    This is the shape being copied. Read both, and read what makes it undriftable: every client
    including the sender renders from the received message, and there is exactly one render call.
  </read_first>
  <action>
    **No code changes. This task produces the map the staging depends on, and it starts from a
    draft rather than from nothing** — confirm or correct what is below, do not re-derive it.

    Write `02.2-DIRECTOR-MAP.md` in the phase directory covering all nine watchers at
    `4/src/orchestrator.js:1655` — `watchEvents`, `watchPrompt`, `watchNarr`, `watchFlip`,
    `watchBattle`, `watchDraftPrompt`, `watchClock`, `watchTurnOrder`, `watchRecoveryState`. For
    each, three columns:

    1. **What it draws** — the renderers it calls.
    2. **Display-only, or does an answer travel back?** A channel is input-carrying if the host's
       loop blocks on a result. The known one is the prompt channel: `ask()`
       (`4/src/ui/util.js:1555`) forks `seatLocal(seat) ? localAsk(...) : onRemotePrompt(...)`, and
       `remotePrompt` (`4/src/orchestrator.js:1144`) returns a promise resolved by `sendResponse`
       (`:1156`). Check `watchFlip`, `watchDraftPrompt` and `watchBattle` the same way — the armed
       flippenator coin and the draft card both take a tap, so **do not assume they are display-only
       because they look like it.**
    3. **Where the host renders the same thing directly today** — the call site inside `runLiveNet()`
       or beneath it (e.g. `showAhoyIntro()` at `4/src/orchestrator.js:977`, `liveRender()`,
       `flash()`). **This is the line each stage removes**, and getting it wrong double-renders or
       blanks the host.

    Then confirm the **solo constraint** by reading rather than trusting this plan: `runLiveNet()` is
    reached for solo and pass-and-play as well as a networked host (the fork is `if(appState.isHost)`
    and solo has no room), and the net writers dereference `db` with no null guard
    (`4/src/net/writers.js:71`). **State in the map what each stage must do when `db` and `room` are
    absent.** If this turns out to be wrong, say so — it is the single assumption the whole staging
    rests on.

    Finish with the **stage order and the stop lines**, and record the indicative drift measure from
    D-24 so the summary can show movement: `showNarration` 2× in `ui/flow.js` / 7× in
    `orchestrator.js`; `localAsk` 7/13; `setActor` 13/7; `flash(` 14/9.
  </action>
  <verify>
    <automated>test -f /Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-DIRECTOR-MAP.md && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-DIRECTOR-MAP.md","utf8"); const w=["watchEvents","watchPrompt","watchNarr","watchFlip","watchBattle","watchDraftPrompt","watchClock","watchTurnOrder","watchRecoveryState"]; const missing=w.filter(x=>!s.includes(x)); if(missing.length) throw new Error("the map does not cover: "+missing.join(", ")); if(!/solo/i.test(s)) throw new Error("the map never states what each stage does with no Firebase room"); console.log("channel map complete OK")'</automated>
    <human-check>The map names, for each of the nine channels, what it draws, whether an answer travels back, and the exact host-side line a stage would remove.</human-check>
  </verify>
  <done>
    All nine channels are mapped. Every input-carrying channel is identified as such — not assumed
    display-only because it looks like it. The solo/pass-and-play constraint is confirmed by reading
    the code, or corrected. The stage order and its stop lines are written down.
  </done>
  <reversibility rating="reversible">A document. No code changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: STAGE 1 — one narration path (closes 17a, 17b, 17c, 19: four of the eight)</name>
  <files>4/src/orchestrator.js, 4/src/ui/flow.js, 4/src/ui/stage.js</files>
  <precondition>Task 1's map exists and names the exact host-side call sites this stage removes. Do not start from the line numbers in this plan alone — the map confirms them against the current tree.</precondition>
  <action>
    **The highest-value stage and the safest: display-only, chat-shaped, and it carries four of
    Wyatt's eight divergences.**

    Introduce the one dispatch for narration and make **both** tiers draw through it. The host feeds
    it directly and mirrors to Firebase only when `db && room` — never a round trip on its own
    screen, so **solo and pass-and-play are unaffected**. A guest feeds the same dispatch from
    `watchNarr`. After this stage there is exactly **one** place a narration line is drawn from.

    Two known duplications collapse as a consequence, and they are D-06's already-traced findings
    kept intact under D-25's larger fix:
    - **17a and 17c — the same text arriving twice on a guest.** `netIntroBarrier`
      (`4/src/ui/flow.js:2185`) sends one message down two independent channels: `onNetBroadcast(msg)`
      at `:2187` → `netBroadcast` (`4/src/orchestrator.js:307`) → the `narr` node → `watchNarr`
      (`:1381`) → a floating bubble; **and** `onRemoteDraftPrompt` at `:2212-2214` →
      `remoteDraftPrompt` (`:1169`) → the `draftPrompt` node → `watchDraftPrompt` (`:1186`) → the
      centre card. Both land on a guest with nothing coordinating them, and the bubble renders behind
      the overlay. **One dispatch means one of these renders, not both.** Before removing the
      broadcast, grep every consumer of the room's `narr` node and confirm nothing besides
      `showNarration` depends on it — research could not find one and left the call here as a code
      question.
    - **17b — the host and guest reading different sentences for the same beat.** The host renders
      "⚓ Waiting for yer mateys…" from `4/src/ui/flow.js:2211`; the guest is told "⚓ Everyone's
      choosing their recipe…" from `4/src/orchestrator.js:805`. **Whether these are one moment with
      two sentences or two adjacent moments, the condition is the same: one moment produces one
      sentence, from one place, on both sides.** Establish which in Task 5's session and record it.
      Do not leave both in place.

    **19 — the host showing no narration at all — is closed by the conversion itself**, because the
    host stops depending on `runLiveNet()` having remembered to call a renderer at that point and
    draws from the stream like everyone else. **Verify that specifically**; it is the divergence
    where the host is the worse screen and is the clearest proof the stage worked.

    **Item 19's other half — the wait box must clear on the event that ends the wait, not on a
    timer.** Wyatt's words: *"It shouldn't disappear by time, it should disappear when their
    teammates have played — otherwise the game can look stalled with no description as to why."*
    `stageFlash` (`4/src/ui/stage.js:526`) computes a hold at line 578 and registers a deadline
    (`S.bubDue`/`S.bubFinish`) that the tick loop tears down unconditionally at `:1506` and `:1531`,
    on elapsed time alone. **The mechanism to use already exists:** `stageFlash` retires the previous
    bubble the instant a new one is shown (`S.hurry()` at `:558`). A wait message that never
    registers a deadline sits there until the next real line replaces it — and the next real line is
    fired by the actual resolving event. Thread a wait-shaped flag that skips registering the
    deadline, at both wait sites.

    **Nothing is a constant (CLAUDE.md rule 9).** Do **not** solve this by raising the hold to a big
    number. **And list what reads the hold before changing how it is produced** — Plan 01 moved the
    ceiling at `:578` for item 10, and it must still read a 2550 floor and an 8775 ceiling afterwards.

    **THE ROLLBACK FOR THIS STAGE:** re-attach the host's direct narration render calls named in the
    map and detach the host from the narration dispatch. Nothing else in the plan depends on this
    stage having landed, so it reverts alone.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_narration_test.js && node 4/scripts/prompt_field_parity_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/stage.js","utf8"); if(!/Math\.max\(2550,\s*Math\.min\(8775,/.test(s)) throw new Error("Plan 01 D-10 hold clamp was lost or re-tuned; it must still read 2550/8775"); console.log("hold clamp intact OK")' && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/flow.js","utf8"); const i=s.indexOf("export async function netIntroBarrier"); const fn=s.slice(i, i+3000); const end=fn.indexOf("\n}"); const body=fn.slice(0,end).split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); if(/onNetBroadcast\(/.test(body) && /onRemoteDraftPrompt\(/.test(body)) throw new Error("netIntroBarrier still sends the same text down both channels"); console.log("one narration channel per moment OK")'</automated>
    <human-check>Two tabs, screenshot both sides at each of Wyatt's four narration beats. 17a: the Ahoy message appears ONCE on the guest, not as a dark top strip plus a centre card. 17c: the drawing-lots text appears once. 17b: host and guest read the SAME sentence. 19: the host has narration on screen where his screenshot showed a dead board, and the wait box is still there after ten seconds on both sides and is replaced — not faded to nothing — when the last captain picks. Solo game still plays start to finish with no Firebase.</human-check>
  </verify>
  <done>
    Four of Wyatt's eight divergences — 17a, 17b, 17c and 19 — do not reproduce in a two-tab session,
    with a both-sides screenshot for each. A narration line is drawn from exactly one place. Solo and
    pass-and-play still play. Plan 01's hold clamp is untouched.
  </done>
  <reversibility rating="costly">This changes live narration timing, which the graveyard document deferred twice for exactly that reason, and no automated gate in this codebase can see narration content parity. It reverts alone and cleanly, but only by eye on both sides will you know it needs to.</reversibility>
</task>

<task type="auto">
  <name>Task 3: STAGE 2 — one active seat, so the camera and the ribbon agree (closes 20-director and 21: six of eight)</name>
  <files>4/src/orchestrator.js, 4/src/ui/stage.js, 4/src/ui/util.js</files>
  <precondition>Stage 1 landed and its four divergences were verified closed on both sides, or Stage 1 was rolled back and that is written down. Do not stack Stage 2 on an unverified Stage 1 — an unattributable regression across two stages is exactly what the graveyard document warned about.</precondition>
  <action>
    **Display-only, chat-shaped, and it carries two more divergences — one on each side.**

    **21 — the guest's top-bar boats not updating.** `ribbonTick()` (`4/src/ui/stage.js:401-410`)
    picks the glowing boat from `S.activeSeat`, falling back to `appState.curSeat`. `curSeat` is
    written **only** by `setActor` (`4/src/ui/util.js:1740`) and `S.activeSeat` **only** by
    `window.__pp4.actor(seat)` (`4/src/ui/stage.js:1635`), and **every call site of either lives in
    the host's live simulation or a local prompt.** Not one of the guest's nine watchers calls
    either — which is D-24 in miniature: a shared renderer with two sets of callers, one of which
    does not exist on a guest.

    **20 — the director. Note the direction: here the HOST is the worse screen** (camera parked
    elsewhere; the guest is centred on the active boat). Wyatt's own words for item 20: *"They should
    be fixed once for both of them where the director follows the same exact rules as in the solo
    game — it shows active players."* That is this stage's requirement stated by him.

    Route the active seat through the one dispatch, so both tiers learn whose turn it is from the
    same place. **No engine change is needed and none is permitted here:** the engine's `ev()` never
    records an actor and there is no actor field in the event schema, but **every meaningful event
    already carries a `p: seat` field** (`turn`, `sail`, `dock`, `pass`, `attack`). Read it and set
    the active seat exactly the way `setActor` already does — mirroring the existing mechanism, not
    inventing a second one. Skip events that carry no seat (`newround`, `end`) rather than letting
    them blank the indicator, and **bound the seat to the known seat range before using it as an
    index.** This is the same technique `watchEvents` already uses to bake round, wind, storm and
    per-seat state onto `appState.game` — the fix that made a guest's day counter and sail camera
    correct in 02.1.

    **Items 15 and 20 wiring (D-08, D-11).** Read Plan 01's summary first. If Group Q reproduced and
    fixed the director fault, **verify it here rather than re-fixing it** — whatever Q landed is
    struck from Group A rather than done twice. If Q could not reproduce it, it lands here, which is
    where item 15 belongs anyway. **Do not aim a speculative fix at
    `4/src/ui/stage.js:1220`** — that block only runs when the current browser owns the prompt, so it
    cannot explain a spectating host, and the reproduction names the real site (CLAUDE.md rule 6).

    **THE ROLLBACK FOR THIS STAGE:** detach the host and guest from the active-seat dispatch and
    restore the direct `setActor` calls named in the map. Stage 1 is unaffected and stays.

    **THIS IS THE SAFE STOP LINE. Six of the eight divergences are closed here, the host's input path
    has not been touched, and the drop is shippable.** If anything about Stages 1-2 was harder than
    it looked, **go to Task 5 now** — that is D-04, and it is a success.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/planner_singleton_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/orchestrator.js","utf8"); const i=s.indexOf("export function watchEvents"); const j=s.indexOf("export function watchPrompt"); if(i<0||j<0) throw new Error("watchEvents/watchPrompt not found"); const fn=s.slice(i,j).split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); if(!/(setActor\(|__pp4\.actor\(|curSeat\s*=)/.test(fn)) throw new Error("the event path still never sets the active seat, so a guest ribbon cannot follow the turn"); console.log("active seat wiring OK")'</automated>
    <human-check>Two tabs, three consecutive turns by three different captains. 21: the guest's top-ribbon glow moves each turn, with no reload, and the SAME boat glows on host and guest at the same moment. 20: the host's camera is centred on the active boat, not parked elsewhere — this is the shot where the host was the worse screen. Screenshot both sides on each turn. Solo game still plays start to finish.</human-check>
  </verify>
  <done>
    Six of Wyatt's eight divergences do not reproduce. The camera and the ribbon agree with each other
    and across both screens, driven from one place. No engine change; nothing new in the event stream.
  </done>
  <reversibility rating="reversible">One read of an existing event field routed through the dispatch. The engine, its RNG and the decision log are untouched, so the corpus Phase 3 will capture is unaffected.</reversibility>
</task>

<task type="auto">
  <name>Task 4: STAGE 3 — the CAPTAINS row order: reproduce and ASK before treating it as a bug</name>
  <files>4/src/orchestrator.js, 4/src/ui/util.js</files>
  <precondition>Stage 2 landed and was verified, or was rolled back and written down. This stage is the smallest of the four and may correctly end with no code change at all.</precondition>
  <action>
    **This divergence is OBSERVED, NOT EXPLAINED, and that distinction is the whole task (D-26,
    CLAUDE.md rule 6).** His shot shows Wyargh's row first on the host and his own row first on the
    guest. **It may be deliberate** — a "me first" ordering so each captain finds their own row at a
    glance is a reasonable design, and it is the kind of thing that gets built on purpose and
    forgotten.

    **Do not fix it first.** Reproduce it in the two-tab session, screenshot both panels, then read
    the row-building path (`buildPlayerRows`, and `watchTurnOrder` for the guest) and establish
    **whether the order is derived per-viewer on purpose or diverges by accident.**

    - **If it is deliberate**, this is not one of the eight. Record it as answered, note it beside
      D-27 as a second thing that looks like a divergence and is not, and change nothing.
    - **If it is accidental**, route the row order through the one dispatch like Stages 1 and 2, so
      both tiers build the rows from the same input.
    - **If you cannot tell from the code**, that is a real outcome: put it to Wyatt with the
      screenshot and both readings, **using the question UI, with a recommendation marked** — he has
      asked for the question UI directly and repeatedly, and a prose list in a reply is a question he
      did not get asked. **In a remote session ask in `--text` mode**, because the menus do not render
      through the Claude app and an unanswerable question looks to him like the run stopped. **Do not
      block on his answer** — carry on to Task 5 and land the drop either way (D-04, D-14's standing
      principle that nothing in this phase waits on him).

    **THE ROLLBACK FOR THIS STAGE:** restore the previous row-order source. Stages 1 and 2 are
    unaffected.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/planner_singleton_check.js</automated>
    <human-check>A both-sides screenshot of the CAPTAINS panel, and a written verdict: deliberate (recorded, not changed), accidental (fixed, with an after screenshot), or unresolved (put to Wyatt with a recommendation, and the drop shipped anyway).</human-check>
  </verify>
  <done>
    The CAPTAINS row-order difference has a measured verdict rather than a fix applied to an
    unexplained observation. If it was deliberate, it is recorded beside D-27 so the next session
    comparing that image does not reach for it.
  </done>
  <reversibility rating="reversible">Either no change at all, or one row-order source moved behind the dispatch.</reversibility>
</task>

<task type="auto">
  <name>Task 5: STAGE 4 — the prompt channel, the one that is NOT chat-shaped (closes 20-sail: eight of eight). ATTEMPT ONLY IF STAGES 1–3 ARE GREEN.</name>
  <files>4/src/ui/util.js, 4/src/orchestrator.js, 4/src/ui/flow.js</files>
  <precondition>Stages 1, 2 and 3 have landed and been verified on both sides in a two-tab session, and there is budget left in this group. If any of them was rolled back, or if Stages 1-3 took longer than expected, SKIP this stage entirely and go to Task 6 — that is D-04 and it is the expected outcome, not a failure.</precondition>
  <action>
    **This is the risky one, and it is last for that reason. It carries exactly ONE of the eight
    divergences.** Read the cost before starting: **it buys one shot (20 — the host showing no sail
    squares) at the price of touching the path every player uses to take every turn.**

    **Why it is not chat-shaped.** Chat is fire-and-forget: send, and every client including the
    sender renders what comes back. A prompt is not — **the host's game loop blocks on the answer.**
    `ask()` (`4/src/ui/util.js:1555`) forks `seatLocal(seat) ? localAsk(...) : onRemotePrompt(...)`;
    `remotePrompt` (`4/src/orchestrator.js:1144`) publishes and returns a promise that `sendResponse`
    (`:1156`) resolves. Making the host render its own prompt through the same path means the host's
    own prompt must be published, drawn from the publication, and answered back into the loop.

    **Two things make this genuinely dangerous, and both must be handled or the stage abandoned:**
    1. **Solo and pass-and-play have no Firebase.** `runLiveNet()` drives them too, and
       `netSetPrompt` dereferences `db` with no null guard (`4/src/net/writers.js:71`). The host's
       own prompt must resolve through the **local dispatch**, never through a Firebase round trip —
       otherwise a solo game stops at its first prompt. **Verify a full solo game before believing
       this stage works at all.**
    2. **Pass-and-play has more than one human on one device.** `seatLocal(s)` is
       `s === appState.mySeat`, which does not describe pass-and-play, and `netIntroBarrier` already
       carries an explicit `appState.passAndPlay` branch for exactly this reason. **A conversion that
       reasons only about "my seat" will break the pass-the-device gate**, which is how a table of
       four plays on one phone.

    Also carry across what `watchPrompt` already does that `localAsk` historically did not — the
    flip-armed coin, the `apBack` escape hatch, the battle-prompt branch with `.btlBtn` rather than
    `.apBtn`. **These used to be host-only and a guest got a stray FLIP button and no back
    affordance**; the shared builder from 02.1-03 fixed the markup, and this stage must not
    re-open it from the other side.

    **THE ROLLBACK FOR THIS STAGE:** restore `ask()`'s `seatLocal` fork to call `localAsk` directly
    for the host's own seat. This is one conditional and it is the reason the stage is structured
    around that single seam rather than spread through the prompt renderers. **Stages 1-3 are
    unaffected and stay.**

    **STOP AND WRITE IT DOWN, rather than half-landing this.** A half-converted prompt path is worse
    than an unconverted one: the failure mode is a captain who cannot take their turn, which is the
    fault this whole phase is named after.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/planner_singleton_check.js</automated>
    <human-check>If attempted: a full SOLO game plays start to finish with no Firebase; a pass-and-play game still gates the device between two humans; and in two tabs the host's sail window shows the highlighted squares his screenshot was missing. Screenshot the host's sail window specifically — that is the divergence this stage buys. If skipped: Task 6's hand-off names it as left, with the one line that would revert it.</human-check>
  </verify>
  <done>
    Either the host draws its own prompt through the one path — proven by a solo game, a
    pass-and-play device gate, and the host's sail squares appearing — or the stage was skipped and
    named as left, with its single-seam rollback recorded. **Both are success.**
  </done>
  <reversibility rating="costly">This touches the path every player uses to take every turn, in solo, pass-and-play and networked play. A mistake does not look like a cosmetic fault — it looks like a captain who cannot take their turn. It reverts through one conditional at `ask()`'s `seatLocal` fork, which is why the stage is built around that seam.</reversibility>
</task>

<task type="auto">
  <name>Task 6: The Group A drop — the eight-divergence check, the written hand-off, stamp bumped once</name>
  <files>4/src/ui/stage.js</files>
  <read_first>
    `docs/DRIVING-THE-GAME.md` before touching a browser — §5c driving a guest while a human hosts,
    §5d arming a watcher for a sub-second window, §5e injecting state (**allowed in solo, FORBIDDEN
    in multiplayer**), §4a the flippenator coin `#flipCoinWrap` **is** the flip button and is not an
    `.apBtn` — that has stalled three separate attempts. `docs/GIT-AND-DEPLOY.md` §5 for the drop
    loop. `4/scripts/mp_rig.mjs` already is the two-window rig with `killAll()` baked in — use it,
    do not build another (CLAUDE.md rule 7).
  </read_first>
  <action>
    **This task runs whether Group A landed all four stages or one. It is D-04's exit condition made
    executable.**

    1. **Walk the eight-divergence table, in two tabs, both sides screenshotted** (D-03, D-26).
       Screenshots go to the phase directory's `shots/` with an `a-` prefix, **named for the shot
       they answer** (`a-17a-host.png`, `a-17a-guest.png`, and so on) so the pairs can be compared
       the way Wyatt compared his. **Look at the pictures, not at a state dump** — the checks that
       blessed the 02.1 build measured game state honestly, never looked at what was drawn, and he
       found seven bugs in twenty minutes. **This is also the method that found D-24 in the first
       place**: comparing his host and guest screenshots pixel by pixel took minutes and reframed the
       whole group.
    2. **Play a solo game start to finish, and a pass-and-play game far enough to see the device
       gate.** Neither has Firebase. **They are the two ways this conversion breaks silently**, and
       no amount of two-tab checking can see either.
    3. **Watch for Group E (D-21)** — a guest whose browser was *killed* mid-turn coming back to a
       battle prompt that draws nothing. One observation, three failed reproductions; only the battle
       path is implicated. **A watching brief, not a hunt.**
    4. **Write the hand-off, and write it even if everything landed.** Go through the eight
       divergences one at a time and say for each: closed and here is the screenshot pair, or still
       reproducing, or explained-and-not-a-fault. Then name every one of items 17, 18, 19, 20, 21 and
       15 as done or left. **Item 18 gets a plain-English answer to the question he actually asked**
       — that the two parallel paths were two orchestrations, that the host is now a listener like
       every other client, and how far that got.
    5. **Report the drift measure**, so the architectural claim is shown rather than asserted: the
       renderer call-site counts from D-24 (`showNarration` 2/7, `localAsk` 7/13, `setActor` 13/7,
       `flash(` 14/9) re-measured after the conversion. **Say plainly if they barely moved** — that
       would mean the stages closed symptoms without collapsing the two orchestrations, which is
       worth knowing and is not a failure of the drop.
    6. **Run the `4/` gates by name** (below). Root `npm test` scans only the older game and its
       result is not evidence about this phase.
    7. **Bump `PP4_STAMP`** at `4/src/ui/stage.js:32`, once (D-02).
    8. **Prove the diff touches only `4/` and `.planning/`** — never `CNAME`, `robots.txt` or
       `sitemap.xml`, which GitHub Pages reads as a claim on the live domain and which two separate
       sessions came one command from breaking.
    9. **Push, pull, verify zero both ways**, then tell Wyatt the stamp in plain English **with the
       size stated**: what he will experience differently, **how many of his eight divergences are
       closed**, and what is explicitly left. Include the URL in that same message.
    10. Kill every Chrome and every local server before replying.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/pass_narration_test.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/audio_map_check.js && node 4/scripts/pp4_timeroff_check.js && node 4/scripts/dlog_quantity_check.js && test "$(ls .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/a-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 8 && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^4/' | grep -v '^\.planning/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && test -z "$(grep -n 'PP4_STAMP = "2026-08-19c"' 4/src/ui/stage.js)" && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt opens playpastrypirates.com/4, sees the new stamp, and has a message telling him — against his own eight screenshots — which are closed and which are not.</human-check>
  </verify>
  <done>
    Group A is live under a fresh `PP4_STAMP`. Each of the eight divergences has a verdict with a
    both-sides screenshot pair behind it. A solo game and a pass-and-play game both still play. The
    named `4/` gates pass. The drift measure is re-reported honestly. Item 18 is answered in plain
    English. `main` and `origin/main` are zero apart both ways.
  </done>
  <reversibility rating="reversible">A push to `main` is how Wyatt playtests, not a release — the live root game is different files and is untouched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host's game loop -> the one dispatch -> the shared renderers | The new seam. Everything drawn on either tier passes through it |
| Host -> Firebase (`narr`, `ev`, `prompt`, `draftPrompt`) -> every guest | The transport for remote clients only; the host's own screen must not depend on it |
| Solo / pass-and-play -> the same dispatch with NO Firebase at all | `db` is null; the net writers dereference it without a guard |
| Repo `main` -> GitHub Pages -> real players on the live root game | A push publishes immediately; no build step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-05 | Denial of service | The host's own screen routed through a Firebase round trip | critical | mitigate | `runLiveNet()` drives solo and pass-and-play, which have no `db`, and `netSetPrompt` dereferences it unguarded (`4/src/net/writers.js:71`). The host feeds and draws from the **local** dispatch and mirrors to Firebase only when `db && room`. Every stage's human-check includes a full solo game; Stage 4's includes a pass-and-play device gate. **This is the failure mode most likely to be missed, because a two-tab check cannot see it.** |
| T-02.2-06 | Denial of service | A half-converted prompt path (Stage 4) | critical | mitigate | The failure mode is a captain who cannot take their turn — the exact fault this phase is named after. Stage 4 is last, is precondition-gated on Stages 1-3 being green, is built around the single `seatLocal` fork at `4/src/ui/util.js:1555` so it reverts through one conditional, and is explicitly abandonable under D-04. |
| T-02.2-07 | Tampering | Narration HTML crossing Firebase and rendered by `showNarration` | medium | mitigate | The conversion **reduces** the number of render sinks rather than adding one, so the HTML surface shrinks. Do not widen the dispatch's input to any player-authored string; free-text chat keeps its own separate channel. |
| T-02.2-08 | Spoofing | The dispatch trusting an event's `p` field to set the active seat | low | mitigate | The `ev` node is host-authoritative — only the host writes it — and this is the same trust already relied on for board positions, wind and the day counter. Bound the seat to the known seat range before using it as an index. |
| T-02.2-09 | Denial of service | A wait bubble that registers no dismissal deadline | medium | mitigate | Item 19's intent, but a bubble never replaced would stick forever. `S.hurry()` guarantees replacement by the next real line; verify the box does clear when the wait ends, and keep a replacement path for a resolving event that carries no narration. |
| T-02.2-10 | Repudiation | No automated gate can see narration or render parity in `4/` | high | accept | `scripts/host_guest_parity_check.js` reads the root game's `src/` and never loads `4/`; closing it is Phase 3 / TEST-06, out of scope. **Accepted with a compensating control that is stronger here than usual:** D-26's eight-divergence table is a concrete, screenshot-checkable acceptance list, walked in Task 6 with named before/after pairs. **Recorded, not silently worked around.** |
| T-02.2-11 | Repudiation | An architectural claim that closes symptoms without collapsing the two orchestrations | medium | mitigate | Task 6 re-measures D-24's renderer call-site counts after the conversion and reports them **even if they barely moved**. A green screenshot walk with unchanged drift numbers means the symptoms were patched and the fault was not, and Wyatt should be told that in those words. |
| T-02.2-12 | Denial of service | Headless Chrome and `http.server` left running | medium | mitigate | Bounded loops (`for (let i=0;i<N;i++)`, never `while(true)`), `killAll()` plus `pkill` inside the same task that starts one, asserted in Task 6's automated verify. |
| T-02.2-SC | Tampering | Package installs | n/a | accept | No package installs in this phase. |
</threat_model>

<verification>
- **The eight divergences of D-26 are the acceptance list.** Each gets a verdict and a both-sides
  screenshot pair named for the shot it answers.
- **A full solo game and a pass-and-play device gate are exercised at every stage.** They have no
  Firebase and are the two ways this conversion breaks silently.
- The named `4/` gates pass by name. **Root `npm test` is not evidence about this phase** — its 21
  gates scan only the older game.
- D-24's renderer call-site drift measure is re-reported after the conversion, honestly, even if it
  barely moved.
- No engine change: `4/src/engine/index.js` is untouched by this plan.
- Plan 01's narration hold clamp still reads a 2550 floor and an 8775 ceiling.
- `PP4_STAMP` bumped exactly once and confirmed serving.
- No Chrome and no local server survives this plan.
</verification>

<success_criteria>
Wyatt plays a crew game and the eight things he photographed side by side are not there — or the ones
still there are named, with a screenshot, and he was told which. The host draws its screen the way
every other client does, so the next change to how something is drawn cannot land on one screen and
miss the other.

**This plan does not satisfy the phase gate.** The gate is Wyatt playing a crew game and not seeing
items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or 22. It is his eyes, not a probe.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-02-SUMMARY.md` when done. It
must state: the new `PP4_STAMP`; **a row per divergence from D-26's table with its verdict and
screenshot pair**; which stages landed and which were rolled back or skipped, with the rollback used;
the re-measured drift numbers; whether the CAPTAINS row order turned out deliberate; that solo and
pass-and-play were both played; and the plain-English answer to item 18.
</output>
