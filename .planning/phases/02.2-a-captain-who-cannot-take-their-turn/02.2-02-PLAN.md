---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 02
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - 4/src/ui/flow.js
  - 4/src/orchestrator.js
  - 4/src/ui/stage.js
  - 4/src/ui/panel.js
autonomous: true
requirements: [PAR-09]
group: A
drop: "Group A — one narration path, one director rule"
items: [17, 18, 19, 20, 21, 15]

must_haves:
  truths:
    - "A guest no longer sees the same Ahoy sentence twice — once as a floating bubble and once on the dimmed ceremony card behind it (item 17)."
    - "One moment produces one sentence, from one place, on both sides of the table (D-07) — the host and the guest read the same words for the same beat."
    - "The waiting-for-yer-mateys box stays up until the mateys have actually played, and is replaced by the line that says so — never by a stopwatch (item 19, D-06)."
    - "A guest's top-bar boat glow moves to whichever captain's turn it is, all game, without a reload (item 21)."
    - "Wyatt gets an honest answer to item 18: the state layer was finished in 02.1 and holds; the parallel paths he named are narration call sites, not host/guest branches (D-05, D-06)."
    - "Wyatt is told a new PP4_STAMP and can see it live (D-02)."
  artifacts:
    - 4/src/ui/flow.js
    - 4/src/orchestrator.js
    - 4/src/ui/stage.js
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/a-*.png
  key_links:
    - "netIntroBarrier (4/src/ui/flow.js:2185) -> onNetBroadcast (orchestrator.js:307) -> Firebase narr node -> watchNarr (orchestrator.js:1381) -> showNarration: channel one."
    - "netIntroBarrier -> onRemoteDraftPrompt (orchestrator.js:1169) -> Firebase draftPrompt node -> watchDraftPrompt (orchestrator.js:1186) -> centre-stage card: channel two, carrying the SAME text."
    - "showNarration (4/src/ui/panel.js:1020) -> window.__pp4.narr -> stageFlash (4/src/ui/stage.js:526) -> the S.bubDue deadline read at :1506 and :1531."
    - "watchEvents (orchestrator.js:1219) -> each event's p field -> window.__pp4.actor / appState.curSeat -> ribbonTick (4/src/ui/stage.js:401-410)."
---

<objective>
**Group A — one narration path, and one rule for the director.** The second of six drops (D-01).

**Item 18 is Wyatt asking a question**, and this plan owes him an honest answer. He asked whether the
work to show hosts and guests with parity "but not from two parallel code paths" got done. **The
answer is half, but not the half the roadmap first named.** Phase 02.1 unified the **state layer**
and that holds. **Do NOT plan or execute this as "delete the host/guest render branches" — there are
none to delete (D-05).** All 45 `isHost`/`isGuest`/`amHost` hits in `4/src` were read one at a time:
they decide who writes to Firebase, who creates and joins the room, who runs the live sim, and who
owns the shot clock. That is the project's own sanctioned residue. Exactly one of them picks what to
draw. A change aimed at deleting them finds nothing to delete, or takes multiplayer down.

**The real fault is parallel narration call sites (D-06), and that is exactly what item 18 names.**
The condition that must become true is **D-07: one moment produces one sentence, from one place, on
both sides.** The roadmap's original criterion — "the render tier contains no host/guest branch that
changes what is drawn" — was already satisfied the day it was written and cannot gate this work.

Purpose: the guest and the host read the same game.
Output: one narration channel per moment, a wait box that clears on the event that ends the wait,
and a guest ribbon whose boat glow follows the turn.

**D-04 — the stated exit condition, and it is not a failure.** If this group balloons past what it
looks like: **land what is safe, write down exactly what is left, and move to Group B.** Wyatt's
ruling. Do not stall on A and do not leave a half-finished rewrite in the tree. **Task 5 is the
written hand-off and it runs either way.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-RESEARCH.md
@.planning/todos/pending/narration-two-schedulers-unenforced.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<absolute_paths>
Every path is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`. The game under
development is `4/`. **Never touch a root-level `src/` path.**
</absolute_paths>

<tasks>

<task type="auto">
  <name>Task 1: Item 17 — the Ahoy line arriving twice on a guest, through two Firebase channels</name>
  <files>4/src/ui/flow.js, 4/src/orchestrator.js</files>
  <read_first>
    **`.planning/todos/pending/narration-two-schedulers-unenforced.md` — read this before touching
    any narration timing.** This exact unification was scoped and deliberately deferred **twice**
    (Phase 15 verification, then 2026-07-30), and its own recommendation is quoted in the research:
    *"Do it on its own, after G17's strict fade has been human-verified in a browser, and add the
    gate in the same commit as the unification."* Reading the graveyard before re-running a settled
    argument is CLAUDE.md rule 10, and this is the single most load-bearing graveyard document in
    the phase. **Note the gate half cannot be honoured here**: `scripts/host_guest_parity_check.js`
    reads the ROOT game's `src/` and never looks at `4/` at all. Closing that gap is Phase 3
    (TEST-06), **not this phase** — record it, do not build it (CLAUDE.md rule 7).
  </read_first>
  <action>
    **CONFIRMED, traced end to end — this is not a candidate.** `netIntroBarrier(msg, btnLabel)` at
    `4/src/ui/flow.js:2185` sends the same `msg` text down **two independent Firebase channels for
    the same moment**:

    1. Line 2187, `netHandlers().onNetBroadcast(msg)` -> `netBroadcast` (`4/src/orchestrator.js:307`)
       -> the room's `narr` node -> every guest's `watchNarr()` (`orchestrator.js:1381`) ->
       `showNarration()` -> a normal floating bubble.
    2. Lines 2212-2214, `onRemoteDraftPrompt(...)` -> `remoteDraftPrompt` (`orchestrator.js:1169`)
       -> a **separate** `draftPrompt` node carrying the same text plus the `stage` flag ->
       `watchDraftPrompt()` (`orchestrator.js:1186`) -> a dimmed, centred ceremony card.

    On a guest both land at once with no coordination between the listeners, and the bubble renders
    *behind* the ceremony overlay. **That is Wyatt's item 17 exactly.** It is guest-only: the host
    never attaches `watchNarr()`, so a single-browser check cannot see it by construction — which is
    the whole reason D-03 exists.

    **The fix: send the text through exactly one channel per client.** The recommended shape is to
    stop calling `onNetBroadcast(msg)` at `flow.js:2187` for this function — it reads as vestigial,
    a leftover broadcast nothing needed once the draft-prompt channel existed, and the draft-prompt
    channel already carries `stage`, `waitMsg` and per-seat delivery. **Confirm before deleting:**
    grep every consumer of the room's `narr` node and of `watchNarr` and establish that nothing
    besides `showNarration` depends on these two ceremony broadcasts existing independently — a
    spectator view, a log, or a narration history would each be a reason to choose the other shape
    instead (make `watchNarr` skip rendering while a stage card is already showing the same text).
    Research could not find such a consumer by grep and explicitly left the final call here, as a
    code question rather than a design one. **Whichever shape you pick, say which and why in the
    summary** — how the two paths are unified is Claude's discretion, provided D-07 becomes true.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/prompt_field_parity_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/flow.js","utf8"); const body=s.slice(s.indexOf("export async function netIntroBarrier")); const end=body.indexOf("\n}"); const fn=body.slice(0,end).split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); const dual=/onNetBroadcast\(/.test(fn) && /onRemoteDraftPrompt\(/.test(fn); if(dual) throw new Error("netIntroBarrier still sends the same text down both channels"); console.log("one channel per moment OK")'</automated>
    <human-check>On the GUEST tab at the opening ceremony: the Ahoy line appears once, on the centred card, with no duplicate bubble behind the dim. Screenshot the guest and the host side by side.</human-check>
  </verify>
  <done>
    A guest sees the Ahoy sentence once, not twice. The host is unchanged. The summary says which of
    the two fix shapes was chosen and what grep established about who else reads the `narr` node.
  </done>
  <reversibility rating="reversible">One call removed (or one listener guarded) inside one function; no wire format changes and no engine emission changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Item 19 — the wait box clears when the mateys have played, not when a stopwatch says so</name>
  <files>4/src/ui/stage.js, 4/src/ui/panel.js, 4/src/ui/flow.js, 4/src/orchestrator.js</files>
  <action>
    **Wyatt's words, item 19:** *"It shouldn't disappear by time, it should disappear when their
    teammates have played — otherwise the game can look stalled with no description as to why or
    what's going on."*

    **CONFIRMED mechanism.** `showNarration()` (`4/src/ui/panel.js:1020`) routes through
    `window.__pp4.narr` -> `stageFlash()` (`4/src/ui/stage.js:526`), which computes a fixed hold at
    line 578 and registers a deadline (`S.bubDue`, `S.bubFinish`) that the tick loop tears down
    unconditionally at **two** sites, `4/src/ui/stage.js:1506` and `:1531` — purely on elapsed time,
    with zero connection to whether the wait is still real.

    **The fix is narrower than adding an event listener, and the mechanism already exists.**
    `stageFlash` already retires the previous bubble the instant a new one is shown (`S.hurry()` at
    line 558 — "one live bubble: retire the old one NOW"). So a wait message that never registers a
    `S.bubDue`/`S.bubFinish` deadline at all sits on screen exactly until the next real narration
    line replaces it — and the next real line is fired by the actual resolving event (the last matey
    finishing their pick), which already calls `showNarration`/`netNarrate` in the normal flow.
    Thread a distinct flag through `showNarration` -> `stageFlash` for wait-shaped messages that
    skips registering the deadline, and apply it at **both** wait sites: `4/src/ui/flow.js:2211`
    (inside `netIntroBarrier`) and `4/src/orchestrator.js:805` (inside `recipeDraftNet`).

    **Nothing is a constant (CLAUDE.md rule 9).** Do **not** solve this by raising the hold to a
    large number. A hardcoded "long enough" is a price list standing in for a quantity that moves; it
    fails silently and it is exactly the shape of fix the graveyard document warns about. And before
    changing how the hold is produced, **list what reads it, gates included** — Plan 01 already moved
    the ceiling at line 578 for item 10, and replacing a quantity with a calculation can leave a
    check unable to fail, which still reads as protection.

    **Item 18's other half, and D-07.** These two wait sites say different things — the host path
    renders "⚓ Waiting for yer mateys…" and the broadcast path says "⚓ Everyone's choosing their
    recipe…". D-06 reads them as one moment with two sentences; the research reads them as two
    separate moments (ahoy/turn-order dismissal versus recipe choosing) that share the same defect
    shape. **Establish which in the two-tab session of Task 4, and record it.** Either way the
    condition to satisfy is D-07: **each moment produces ONE sentence, from one place, on both
    sides.** If they are one moment, unify the sentence. If they are two, make each one say the same
    thing to the host and the guest. Do not paper over the difference by leaving both in place.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_narration_test.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/stage.js","utf8"); if(!/Math\.max\(2550,\s*Math\.min\(8775,/.test(s)) throw new Error("Plan 01 D-10 ceiling was lost or re-tuned; the hold clamp must still read 2550/8775"); console.log("hold clamp intact OK")'</automated>
    <human-check>In a two-tab crew game, hold both browsers at the recipe-choosing beat with one captain still deciding: the wait box is still on screen after ten seconds on BOTH sides, and it is replaced — not faded to nothing — the moment the last captain picks. Screenshot both sides at ten seconds and again after the pick.</human-check>
  </verify>
  <done>
    The waiting box stays up while the wait is real, on both sides, and goes away because the next
    real line arrives — not because a timer expired. Wyatt never sees a game that looks stalled with
    nothing on screen explaining why. Plan 01's narration ceiling is still 8775 over a 2550 floor.
  </done>
  <reversibility rating="costly">This touches live narration timing, which the graveyard document deferred twice for exactly that reason, and there is no automated gate in this codebase that can see narration content parity. Verify by eye on both sides before the drop.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Item 21 — a guest's top-bar boat glow follows the turn</name>
  <files>4/src/orchestrator.js</files>
  <action>
    **CONFIRMED — a real, unfixed gap, not "may already be fixed."**

    `ribbonTick()` (`4/src/ui/stage.js:401-410`) picks the glowing boat from `S.activeSeat`, falling
    back to `appState.curSeat`. `appState.curSeat` is written **only** by `setActor(s)`
    (`4/src/ui/util.js:1740`) and `S.activeSeat` **only** by `window.__pp4.actor(seat)`
    (`4/src/ui/stage.js:1635`). Every call site of either lives inside the **host's own live
    simulation** or a local prompt on whichever browser owns that seat. **Not one of the guest's
    `watch*` handlers calls either.** So a guest's boat glow is frozen at seat 0 for the whole game,
    for every seat's turn — item 21 exactly. `watchPrompt()` only reacts when the prompt is the
    guest's own, which is a minority of prompts in a three or four player game.

    **Take the UI-tier fix, not the engine one.** The engine's `ev()` never records an actor and
    there is no `actor`/`turn` field in the event schema, so option (a) would mean a new wire field
    baked at emission time — an **engine-emission change**, which this phase avoids on principle
    (the engine is seeded and lockstep replay depends on it; **prefer UI-tier fixes**). Option (b)
    needs neither: every meaningful event (`turn`, `sail`, `dock`, `pass`, `attack`) already carries
    a `p: seat` field. In `watchEvents()` (`4/src/orchestrator.js:1219`), read `e.p` when it is
    present and set the active seat exactly the way `setActor` already does on the host — mirroring
    the existing mechanism rather than inventing a second one. Skip the events that carry no seat
    (`newround`, `end`) rather than letting them blank the indicator.

    This is the same technique `watchEvents()` already uses to bake `round`, `wind`, `storm` and
    per-seat state onto `appState.game` — the fix that made a guest's day counter and sail camera
    correct in 02.1. **Wyatt's own framing of item 20 applies here too:** the director and the ribbon
    should follow the same rules on every screen, *"where the director follows the same exact rules
    as in the solo game — it shows active players."*
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/planner_singleton_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/orchestrator.js","utf8"); const i=s.indexOf("export function watchEvents"); const j=s.indexOf("export function watchPrompt"); const fn=s.slice(i,j).split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); if(!/(setActor\(|__pp4\.actor\(|curSeat\s*=)/.test(fn)) throw new Error("watchEvents still never sets the active seat, so a guest ribbon cannot follow the turn"); console.log("guest actor wiring OK")'</automated>
    <human-check>In a two-tab crew game, watch the guest's top ribbon across three consecutive turns by three different captains: the glow moves each time, with no reload. Screenshot the host and the guest at the same moment in each turn and confirm the same boat glows on both.</human-check>
  </verify>
  <done>
    A guest's top-bar boat glow moves to whichever captain's turn it is, all game, matching the
    host's ribbon at the same moment. No engine change; nothing new was added to the event stream.
  </done>
  <reversibility rating="reversible">One read of an existing event field inside one guest-side handler. The engine, its RNG and the decision log are untouched, so the determinism corpus Phase 3 will capture is unaffected.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Items 20 and 15 — only if Plan 01 handed them here</name>
  <files>4/src/ui/stage.js</files>
  <precondition>Plan 01's summary states whether items 20 and 15 were fixed in Group Q or handed to Group A unreproduced. Read that verdict before starting. If Plan 01 fixed them, this task is closed with a one-line note and no code change.</precondition>
  <action>
    **This task exists because of D-11 and D-08, and it may correctly do nothing.**

    **If Plan 01 already fixed items 20 and 15**, record that and skip. Whatever Group Q landed is
    struck from Group A rather than done twice.

    **If Plan 01 could not reproduce them**, reproduce them here, in the two-tab session Task 5 runs
    anyway — Group A is the right home for item 15 (D-08: every number 1-22 appeared in the roadmap's
    groups except 15; it is the same director fault as item 20). **Do not write a fix for an
    unreproduced fault** (CLAUDE.md rule 6). Specifically: **do not plan a one-line change to
    `4/src/ui/stage.js:1220` as though it were settled.** That line re-aims with
    `camToSeat(appState.mySeat ?? 0)`, but research established the block only runs when the current
    browser owns the prompt — so it does **not** explain a spectating host during a guest's ordinary
    turn. Let the reproduction name the real site.

    When it is named, the correct behaviour already exists in the same file and should be reused
    rather than re-invented: `camToSeat(subj)` at `4/src/ui/stage.js:574` frames the seat a moment is
    genuinely about, and `camFitSeats()` at `:109` frames every captain a question is about. Aim the
    director at the active captain, **by the same rule the solo game already uses** — that is item
    20 in Wyatt's own words, and item 21's fix in Task 3 puts the active seat within reach on a
    guest for the first time.

    **If it still does not reproduce after this session**, say so plainly, note it against items 20
    and 15, and do not change a line. An unmeasured claim never goes in a table beside measured ones.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/seat_arg_check.js</automated>
    <human-check>Either a before/after screenshot pair from both sides showing the director now framing the captain whose turn it is, or a written NOT-OBSERVED verdict naming what was tried.</human-check>
  </verify>
  <done>
    Items 20 and 15 are fixed with a screenshot pair proving it on both sides, or recorded as still
    unreproduced with the attempts written down. Neither outcome involves a speculative edit.
  </done>
</task>

<task type="auto">
  <name>Task 5: The Group A drop — two tabs, both sides, the written hand-off, stamp bumped once</name>
  <files>4/src/ui/stage.js</files>
  <read_first>
    `docs/DRIVING-THE-GAME.md` before touching a browser (§5c driving a guest while a human hosts;
    §5d arming a watcher for a sub-second window; §5e injecting state — **allowed in solo, FORBIDDEN
    in multiplayer**; §4a the flippenator coin `#flipCoinWrap` **is** the flip button and is not an
    `.apBtn`). `docs/GIT-AND-DEPLOY.md` §5 for the drop loop.
  </read_first>
  <action>
    **This task runs whether Group A landed everything or not. It is D-04's exit condition made
    executable.**

    1. **Play a two-tab crew game and screenshot BOTH sides** (D-03), using `4/scripts/mp_rig.mjs`
       against a local server on a fresh port. Screenshots go to the phase directory's `shots/` with
       an `a-` prefix. Look at the pictures. A green suite proves nothing about what Wyatt will see —
       the checks that blessed the 02.1 build measured game state honestly and never once looked at
       what was drawn, and he found seven bugs in twenty minutes.
    2. **Watch for Group E while you are in there (D-21).** A guest whose browser was *killed*
       mid-turn is reported to come back to a battle prompt that draws nothing — one observation,
       three failed reproductions, and only the battle path (`renderBattleFromSnap()` plus the armed
       coin) is implicated, since a sail window and an action menu both came back correctly after the
       same kill. **It is a watching brief, not a hunt.** Note what you see; mount no dedicated
       investigation.
    3. **Write the hand-off, and write it even if everything landed.** Name each of items 17, 18, 19,
       20, 21 and 15 with its state: done and how it was verified, or left and exactly what remains.
       Item 18 gets a plain-English answer to the question he actually asked. **This is D-04:** land
       what is safe, write down exactly what is left, and move to Group B — so he comes back to a
       visibly better game either way. Do not stall here and do not leave a half-finished rewrite in
       the tree.
    4. **Run the `4/` gates by name** (below). Root `npm test` scans only the older game; its result
       is not evidence about this phase.
    5. **Bump `PP4_STAMP`** at `4/src/ui/stage.js:32`, once (D-02).
    6. **Prove the diff touches only `4/` and `.planning/`** — never `CNAME`, `robots.txt` or
       `sitemap.xml`, which GitHub Pages reads as a claim on the live domain.
    7. **Push, pull, verify zero both ways.** Then tell Wyatt the stamp in plain English **with the
       size stated**: what he will experience differently, how much of his 22-item list this drop
       covers, and what it explicitly leaves undone. If you ask him to look at something, the URL
       goes in that same message.
    8. Kill every Chrome and every local server before replying.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/pass_narration_test.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/audio_map_check.js && test "$(ls .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/a-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 4 && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^4/' | grep -v '^\.planning/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt opens playpastrypirates.com/4, sees the new stamp, and in a crew game reads one sentence per moment on both screens with a wait box that outlasts the wait.</human-check>
  </verify>
  <done>
    Group A is live under a fresh `PP4_STAMP`; both sides of a real crew game are screenshotted; the
    named `4/` gates pass; the hand-off names every one of items 17, 18, 19, 20, 21 and 15 as done or
    left, with item 18 answered in plain English; and `main` and `origin/main` are zero apart both
    ways.
  </done>
  <reversibility rating="reversible">A push to `main` is how Wyatt playtests; the live root game is different files and untouched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host browser -> Firebase `narr` / `draftPrompt` nodes -> every guest browser | Narration HTML written by one client and rendered as HTML by every other |
| Host browser -> Firebase `ev` node -> every guest browser | Event stream carrying the seat field this plan newly reads |
| Repo `main` -> GitHub Pages -> real players on the live root game | A push publishes immediately; no build step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-05 | Tampering | Narration text crossing Firebase and rendered as HTML by `showNarration` | medium | mitigate | This plan **removes** a redundant broadcast rather than adding a new sink, so the HTML-rendering surface shrinks. No player-authored text is newly routed into the narration path; free-text chat keeps its own separate, already-escaped channel. Do not widen `showNarration`'s input to any user-supplied string as part of the wait-flag change. |
| T-02.2-06 | Spoofing | `watchEvents` newly trusting an event's `p` field to set the active seat | low | mitigate | The `ev` node is host-authoritative — only the host writes it (`isHost && db && room`), the same trust already relied on for board positions, wind and the day counter. Reading one more field from an already-trusted stream adds no new authority. Bound the seat to the known seat range before using it as an index. |
| T-02.2-07 | Denial of service | A wait bubble that never registers a dismissal deadline | medium | mitigate | The whole point of item 19, but a bubble that is never replaced would stick forever. `S.hurry()` guarantees replacement by the next real line; verify in Task 5's two-tab session that the box does clear when the wait ends, and keep a replacement path for the case where the resolving event arrives with no narration. |
| T-02.2-08 | Repudiation | No automated gate can see narration content parity in `4/` | medium | accept | `scripts/host_guest_parity_check.js` reads the root game's `src/` and never loads `4/`; closing that is Phase 3 / TEST-06, out of scope here. Accepted with compensating control: two-tab screenshots of both sides (D-03) and Wyatt's own play as the phase gate. **Recorded, not silently worked around.** |
| T-02.2-SC | Tampering | Package installs | n/a | accept | No package installs in this phase. |
</threat_model>

<verification>
- The named `4/` gates pass by name. Root `npm test` is not evidence about this phase.
- Both sides of a real two-tab crew game are screenshotted into the phase directory with an `a-` prefix.
- D-07 is demonstrated, not asserted: a screenshot pair for each unified moment showing the host and
  the guest reading the same sentence.
- The `narr`-node consumer grep for Task 1 is written down, with its result.
- `PP4_STAMP` bumped exactly once and confirmed serving.
- No Chrome and no local server survives this plan.
</verification>

<success_criteria>
A guest sees each moment's sentence once, in the same words the host sees, and the waiting box stays
up until the wait is actually over. A guest's top ribbon shows whose turn it is. Wyatt has a plain
answer to item 18. If anything in Group A ran long, it is written down and Group B started anyway.

**This plan does not satisfy the phase gate.** The gate is Wyatt playing a crew game and not seeing
items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or 22.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-02-SUMMARY.md` when done. It
must state: the new `PP4_STAMP`; which fix shape was chosen for item 17 and what the `narr`-consumer
grep found; whether the two wait sites turned out to be one moment or two; the state of every one of
items 17, 18, 19, 20, 21 and 15 (done, or left and what remains); and any Group E observation.
</output>
