---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 03
type: execute
wave: 3
depends_on: ["02.2-02"]
files_modified:
  - 4/src/ui/bakeoff.js
  - 4/src/ui/stage.js
  - 4/src/ui/util.js
  - 4/src/shared/index.js
  - 4/src/ui/flow.js
  - 4/index.html
autonomous: true
requirements: [PAR-10]
group: B
drop: "Group B — the faults visible on the first screen"
items: [2, 3, 5, 11, 22]

must_haves:
  truths:
    - "The Attack prompt whose reason mentions the powder cost renders as the same radial fan every other action uses, on both the host's screen and the guest's — or it is proven not to be a code fault at all and the real cause is named (item 2)."
    - "A live battle frames both fighting boats over the board, with the boats visible underneath, rather than pointing at a bystander's boat (item 3)."
    - "The ovens-be-roarin' block — box, Get bakin' button and helper text — is centred both ways, like every other ceremony card (item 5)."
    - "No popup appears until the director camera AND the ships have stopped moving (item 11, D-20)."
    - "On a laptop the game renders at the phone's aspect ratio — narrow, centred, square board — so Wyatt and the session debug the same picture (item 22, D-18)."
    - "Wyatt is told a new PP4_STAMP and can see it live (D-02)."
  artifacts:
    - 4/src/ui/bakeoff.js
    - 4/src/ui/stage.js
    - 4/src/ui/util.js
    - 4/index.html
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/b-*.png
  key_links:
    - "bakeoffIntroCard (4/src/ui/bakeoff.js:164) -> actionPanel's pp4Stage flag -> enterCenterStage (4/src/ui/stage.js:1099): the flag bakeoff.js:209 sets and bakeoffIntroCard never does."
    - "vwPx/vhPx (4/src/ui/util.js:1259-1260) -> every stage measurement including the camera aspect at 4/src/ui/stage.js:227-238: the single seam item 22's stopgap turns."
    - "camFitSeats (4/src/ui/stage.js:109) -> window.__pp4.battle (:1626) <- orchestrator.js:501 (host) and 4/src/ui/flow.js:2291 (guest): both sides already call the right function."
    - "emojify (4/src/shared/index.js) -> a button's data-why -> menuButtons's 16-character radial-eligibility check (4/src/ui/stage.js:1029): item 2's suspected path."
---

<objective>
**Group B — the faults visible on the first screen.** The third of six drops (D-01).

Four of these five items are **CANDIDATE**, which shapes this whole plan: **Task 1 reproduces before
anything is fixed, and the fix tasks are blocked on it** (CLAUDE.md rule 6). This project spent two
days on defects that turned out not to exist because a session put three unmeasured claims in a table
beside measured ones and four of five were false. Only item 5 is confirmed by reading the code, and
it goes in on its own.

Item 22 is a **stopgap only (D-18)**: desktop renders at the phone's aspect ratio so Wyatt and the
session debug the same picture. **The desktop redesign stays in Phase 8 and is not in this phase.**

Items 1 and 14 are also written into this group in the roadmap so the intent survives if Group Q left
either undone (D-13) — check Plan 01's summary first and do them here only if they are still open.

Purpose: the screen Wyatt sees first stops being wrong at a glance.
Output: a centred bake-off card, a battle framed on the fight, popups that wait for motion to stop,
a laptop that shows him the same picture as his phone — and an honest verdict on item 2.

**No UI-SPEC governs this phase — Wyatt's notes are the design contract (D-23).**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-RESEARCH.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-FINDINGS.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<absolute_paths>
Every path is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`. The game under
development is `4/`. **Never touch a root-level `src/` path.**
</absolute_paths>

<tasks>

<task type="auto">
  <name>Task 1: REPRODUCE FIRST — items 2, 3 and 11, in two tabs, with screenshots</name>
  <files>4/scripts/mp_rig.mjs (used, not modified), .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/</files>
  <read_first>
    `docs/DRIVING-THE-GAME.md` — **before touching a browser, not after getting stuck.** §3 solo,
    §4 the turn loop, §4a the flippenator coin `#flipCoinWrap` **is** the flip button and is not an
    `.apBtn` (this has stalled three separate attempts), §4d battle prompts use `.btlBtn` not
    `.apBtn`, §5b the autoplay driver, §5c driving a guest while a human hosts, §5d arming a watcher
    for a sub-second window, §5e **injecting state — allowed in solo, FORBIDDEN in multiplayer.**
    Use `4/scripts/mp_rig.mjs`; it already is the rig (CLAUDE.md rule 7 — do not build tooling when
    the ask is to fix the game).

    `docs/BOARD-RENDERING.md` — **mandatory before anything drawn on the board.** The board is drawn
    in five layers. **An HTML overlay mapped to board coordinates must be added to
    `CAM_HTML_LAYERS` or it detaches the moment the director zooms**, and **anything that animates
    continuously must be HTML, not SVG — Chrome cannot composite an SVG transform animation at all**
    (the same animation measured ~62 layouts/sec as SVG and zero as HTML).
  </read_first>
  <action>
    **Nothing is fixed in this task. It measures, and it blocks Task 3.** Record each finding as
    OBSERVED-with-screenshot or NOT-OBSERVED-with-screenshot. Screenshots go to the phase directory's
    `shots/` with a `b-` prefix. **Red-proof every instrument before believing it** — three separate
    checks in one night measured something other than what they named, including `offsetParent`,
    which is always null for a `position:fixed` element and therefore condemned the host's own
    working screen.

    **(a) Item 2 — the flat card, and a fix for it ALREADY SHIPPED.** Commit `2275d71` (2026-08-19)
    made `emojify()` tag-aware and **was in the build Wyatt played** (`PP4_STAMP` `2026-08-19c`,
    commit `4db2c00`, verified by ancestry). His screenshot still shows the attribute broken open.
    Force the exact scenario — a captain who cannot afford the powder, so the Attack circle is greyed
    and carries the why-sentence built at `4/src/ui/flow.js:1767` — and screenshot the prompt on
    **both** sides. **Three outcomes, and the plan allows all three:**
    - **(i) The double-emojify path.** `emojify()` guards tags by splitting on `<[^>]*>`, and its own
      documented limitation (`4/src/shared/index.js`, threat T-02.1-04) is that *a literal `>` inside
      an attribute value ends a tag span early* — asserting no string this codebase feeds through it
      contains one. **If the why-sentence is emojified before being placed into `data-why`,
      `iconImg()` injects an `<img … alt="">` that contains both a quote and a `>`, and the
      limitation fires on a string `emojify()` itself created.** This is the orchestrator's
      hypothesis and it is **CANDIDATE, NOT MEASURED**. Test it by instrumenting the actual
      `data-why` attribute value and the `textContent` that `menuButtons()`'s 16-character
      radial-eligibility check at `4/src/ui/stage.js:1029` measures.
    - **(ii) A second sink that never got the tag-aware fix.** Find it by following the same value.
    - **(iii) It does not reproduce at all** — in which case Wyatt's phone Safari served a **cached
      ES module** and this is a caching finding, not a code finding. **Safari's module caching is a
      known trap in this project** (a fresh server port, not a page `?cb=`, is the way round it). If
      this is the answer, say so and change no code.
    **Do not pre-commit to a fix. Identify which.**

    **(b) Item 3 — the battle camera.** Wyatt's words: *"the battle is centered over the game board,
    and you can see the battling boats underneath it."* Both sides **already call the correct
    function** — `camFitSeats()` via `window.__pp4.battle(a,d)` (`4/src/ui/stage.js:109`, `:1626`),
    from `4/src/orchestrator.js:501` on the host and `4/src/ui/flow.js:2291` on the guest — and the
    mechanism was photographed working in a solo forced battle at the 02.1 gate. **So the candidate
    root cause is a battle-ADJACENT sub-decision** (the call-the-winner / crow's-nest prompt, asked
    of someone who may not be a combatant) pulling the camera onto a bystander's own boat *before*
    `__pp4.battle()` claims the shot. Drive a **real multi-round networked battle with a nested
    spectator sub-decision** — the D7 solo test posed a single flip and cannot see this — and
    screenshot the moment the camera settles, on both sides.

    **(c) Item 11 — the popup that beats the motion (D-20).** The existing fix at
    `4/src/ui/stage.js:120-134` (playtest 21 item 2) gates only the **radial button fan's placement**
    on ship and camera settle, via `S.frameKey` and the `radKey` memo. It does not gate whether the
    **panel and message pill appear at all**. Capture frames across a prompt's arrival and establish
    whether the box and the board dim land before motion stops. **Sample real frames** — an idle
    headless page stops producing them, so an animation measures as free; drive frames and quote the
    fps beside any timing figure.

    **Bound everything and tear it down inside this task.** Every probe loop is
    `for (let i = 0; i < N; i++)`, never `while (true)`. Call the rig's `killAll()` then
    `pkill -f remote-debugging-port; pkill -f http.server` **before writing the reply**.
  </action>
  <verify>
    <automated>test "$(ls /Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/b-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 6 && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Six or more screenshots from both sides of a real crew game. Each of items 2, 3 and 11 has a written verdict: OBSERVED with the screenshot that shows it and the code path that produced it, or NOT-OBSERVED with what was tried. For item 2 the verdict names which of the three outcomes held.</human-check>
  </verify>
  <done>
    Items 2, 3 and 11 each have a measured verdict with a screenshot behind it. Item 2's verdict says
    whether this is the double-emojify path, a second unfixed sink, or a Safari module-cache finding
    with no code fault. No claim is stated as confirmed that was not seen. Every Chrome and server is
    dead before the reply.
  </done>
</task>

<task type="auto">
  <name>Task 2: Item 5 — the ovens-be-roarin' card is centred both ways</name>
  <files>4/src/ui/bakeoff.js</files>
  <action>
    **CONFIRMED by reading the file — this needs no reproduction.**

    `bakeoffIntroCard()` (`4/src/ui/bakeoff.js:164-185`) builds *"The ovens be roarin'! Yer
    ingredients be waitin'…"* with a plain `panel(html, true)` call and a single 11-character
    button, "Get bakin'!". Unlike every sibling ceremony screen — the ahoy and turn-order cards,
    which get the stage treatment via their option's `stage:true`, and the other `bakeoff.js` screens
    which set the panel's stage flag explicitly at `4/src/ui/bakeoff.js:209` — **`bakeoffIntroCard()`
    sets no stage flag anywhere in its body.** Its short button qualifies for the 16-character
    radial-eligibility check at `4/src/ui/stage.js:1029`, so the whole block falls into the radial
    bloom around the player's own ship instead of the centred ceremony treatment.

    Set the action panel's stage flag around the `panel(...)` call in `bakeoffIntroCard()`, mirroring
    the pattern already at `4/src/ui/bakeoff.js:209`, so it routes through `enterCenterStage()`
    (`4/src/ui/stage.js:1099-1101`) — the same code path every other centred ceremony card already
    uses. **Match the existing teardown too:** `4/src/ui/bakeoff.js:240`, `:242`, `:474`, `:497` and
    `:526` all clear the flag when their screen leaves. An intro card that sets the flag and never
    clears it strands the next prompt on the centre stage. Verify the flag is cleared when the
    player taps Get bakin'.

    **This is a consistency fix, so sweep it (CLAUDE.md rule 8):** name in the summary which ceremony
    surfaces you compared it against — the ahoy card, the turn-order card, and the other `bakeoff.js`
    stage screens — and confirm all of them now enter and leave the centre stage the same way.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/bakeoff.js","utf8"); const i=s.indexOf("function bakeoffIntroCard"); if(i<0) throw new Error("bakeoffIntroCard not found"); const fn=s.slice(i, s.indexOf("/* ================= the interaction", i)).split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); if(!/pp4Stage\s*=\s*"1"/.test(fn)) throw new Error("bakeoffIntroCard still sets no stage flag, so it will bloom round the ship instead of centring"); if(!/delete .*pp4Stage/.test(fn)) throw new Error("bakeoffIntroCard sets the stage flag and never clears it; the next prompt will be stranded on the centre stage"); console.log("bakeoff intro centring OK")'</automated>
    <human-check>Reach the bake-off in a solo game: the box, the Get bakin' button and the helper text sit centred both ways over a dimmed board, exactly like the opening ceremony card — and the next prompt after tapping it is back to normal. Screenshot before and after the tap.</human-check>
  </verify>
  <done>
    The ovens-be-roarin' block is centred horizontally and vertically over a dimmed board, matching
    every other ceremony card, and the stage clears when the player moves on. The summary names the
    ceremony surfaces compared.
  </done>
  <reversibility rating="reversible">Two lines in one render function, matching an existing in-file pattern.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Fix only what Task 1 observed — items 2, 3 and 11</name>
  <files>4/src/shared/index.js, 4/src/ui/flow.js, 4/src/ui/stage.js</files>
  <precondition>Task 1 has completed and each of items 2, 3 and 11 has a written OBSERVED or NOT-OBSERVED verdict with a screenshot. Do not start this task from a theory.</precondition>
  <action>
    **Fix what was seen. Change nothing for what was not.** For any item Task 1 marked NOT-OBSERVED,
    write the verdict against that item and move on — that is rule 6 working, not a failure.

    **Item 2, if outcome (i) or (ii) held:** fix at the site Task 1 named. If it is the
    double-emojify path, the shape of the answer is to stop a string that has already been through
    `emojify()` from going through it again on its way into an attribute — the attribute value and
    the visible label want different treatment. Whatever you change, `emojify()`'s documented
    limitation about a literal `>` inside an attribute value should end up **either honoured by
    construction or removed**, not left as an assertion that this codebase happens not to violate.
    Prove the fix by reading back the actual `data-why` value and the `textContent` that the
    16-character check at `4/src/ui/stage.js:1029` measures, on both sides. **If outcome (iii) held
    — it does not reproduce and Safari served a stale module — change no code**, write the caching
    finding into the summary, and tell Wyatt plainly what he saw and why.

    **Item 3, if observed:** stop the battle-adjacent sub-decision from claiming the camera before
    `window.__pp4.battle()` frames the fight. Reuse `camFitSeats()` (`4/src/ui/stage.js:109`), which
    already frames both combatants with the zoom derived from the gap between them — that is exactly
    what Wyatt asked for, and it is already wired on both sides. Do not add a second framing
    mechanism beside it.

    **Item 11, if observed (D-20):** *no popup appears until the director camera AND the ships have
    stopped moving.* The settle-detection technique already exists at `4/src/ui/stage.js:120-134` and
    needs applying one level up — at the point the panel first decides to show **any** content, not
    just where it blooms the buttons. **Read the shot-clock arming continuation at
    `4/src/ui/util.js:1511-1536` in full before touching panel timing**: the clock must still arm at
    the right moment even when the visible panel is deferred, and that continuation is threaded
    through `ask()`. This is a moderate change, not a one-liner.

    **Nothing is a constant (CLAUDE.md rule 9).** Any settle threshold is derived from what the game
    already computes — the camera tween's own completion and the ship glide's own completion — not
    from a typed millisecond figure. A hardcoded wait is a price list standing in for a quantity that
    moves, and it fails silently. **And before changing how any quantity is produced, list what reads
    it, gates included** — replacing a constant with a calculation can leave a check unable to fail,
    which still reads as protection.

    **Items 1 and 14 (D-13):** read Plan 01's summary. If Group Q landed the label parentheses and
    Pass's position, they are struck from here. If either is still open, do it here and sweep the
    same three surfaces — host prompt, guest prompt, draft channel — naming them in the summary.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/planner_singleton_check.js</automated>
    <human-check>For each item Task 1 observed, a before/after screenshot pair from both sides showing it fixed. For each item Task 1 did not observe, a written verdict and no diff.</human-check>
  </verify>
  <done>
    Every fault Task 1 saw is fixed and proven with a before/after screenshot pair on both sides.
    Every fault Task 1 did not see has a written verdict and no code change. Item 2's outcome is
    named explicitly — code fix, second sink, or Safari module cache.
  </done>
  <reversibility rating="costly">Item 11's change moves when the panel is built relative to the shot clock arming, which is threaded through `ask()`. A mistake here can arm the clock at the wrong moment, which a screenshot will not show — verify the clock behaviour explicitly in Task 5's session.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Item 22 — desktop renders at the phone's aspect ratio, as a reversible stopgap</name>
  <files>4/src/ui/util.js, 4/index.html</files>
  <read_first>
    `docs/BOARD-RENDERING.md` before changing anything the board is measured against.
  </read_first>
  <action>
    **STOPGAP ONLY (D-18).** Desktop renders at the phone's aspect ratio — narrow, centred, square
    board — **so Wyatt and the session debug the same picture.** **The desktop redesign stays in
    Phase 8 (DESK-01…DESK-08) and is explicitly not in this phase.** Do not improve the desktop
    layout here; make it match the phone.

    `4/index.html` has exactly two breakpoints today (`max-width:600px` and `max-width:480px`) and
    **no `min-width` query anywhere**. The stage's measurements funnel through **two helpers by
    explicit design** — `vwPx()` and `vhPx()` at `4/src/ui/util.js:1259-1260`, which read
    `document.documentElement.clientWidth/Height` with no cap — and `4/src/ui/stage.js`'s own header
    says every box on the stage is laid out from the layout viewport via those helpers, never from
    `window.innerWidth/innerHeight`. The camera's vertical extent is derived from that same measure
    at `4/src/ui/stage.js:227-238`. **That single seam is what makes a reversible stopgap possible.**

    Add one new `min-width` media rule that caps the outermost stage wrapper to a phone-shaped width
    and centres it, and make `vwPx()`/`vhPx()` measure **that container's** bounding rectangle when
    it exists, falling back to today's behaviour when it does not — a zero-risk default. Because the
    ribbon, camera, prompt placement and bubble placement all already read those two helpers, this
    one change point propagates everywhere with no renderer edits.

    **Confirm the outermost stage wrapper's real element id against the live DOM before writing the
    selector.** Research flagged this as its own lowest-confidence item: the recommendation is
    code-reading-derived and the exact id was never checked against a running page. Open the page,
    read the id, then write the rule.

    **State the revert in the summary**, because Phase 8 will do it: delete the new CSS rule and the
    container-measure branch. Both are additive, not destructive.

    **Sweep for consistency (CLAUDE.md rule 8):** the phone layout must be **visually unchanged** by
    this (DESK-08 says so for Phase 7, and it holds here too). Screenshot the phone width before and
    after and confirm they are identical.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node -e 'const h=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/index.html","utf8"); if(!/min-width\s*:/.test(h)) throw new Error("no min-width breakpoint was added, so desktop still renders at full viewport width"); const u=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/util.js","utf8").split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); if(!/getBoundingClientRect/.test(u.slice(u.indexOf("vwPx")-400, u.indexOf("vwPx")+800))) throw new Error("vwPx/vhPx still measure the document element only; the container branch is missing"); console.log("aspect stopgap OK")'</automated>
    <human-check>On a laptop the board is narrow, centred and square — the same picture Wyatt sees on his phone. At phone width the layout is pixel-identical to before the change; screenshot both widths, before and after.</human-check>
  </verify>
  <done>
    A laptop shows the game at the phone's aspect ratio, so Wyatt and the session are debugging the
    same picture. The phone layout is unchanged. The summary names the two lines to delete to revert
    it in Phase 8.
  </done>
  <reversibility rating="reversible">Both halves are additive — one CSS rule and one fallback branch. Deleting them restores today's behaviour exactly, which is the point of D-18.</reversibility>
</task>

<task type="auto">
  <name>Task 5: The Group B drop — two tabs, both sides, stamp bumped once</name>
  <files>4/src/ui/stage.js</files>
  <action>
    Same drop loop as every group (D-02, D-03), and `docs/GIT-AND-DEPLOY.md` §5 is the reference.

    1. **Play a two-tab crew game and screenshot BOTH sides** with `4/scripts/mp_rig.mjs` against a
       local server on a fresh port. **Screenshot the state you changed AND the state next to it** —
       the chat regression at the 02.1 gate was not in the chat sheet, it was a heading peeking out
       from behind the ribbon three hundred pixels away. **Look at the picture, not the DOM.**
    2. **Explicitly check the shot clock still arms at the right moment**, because Task 3's item 11
       change moves when the panel is built relative to it, and no screenshot shows that by itself.
    3. **Watch for Group E (D-21)** — a guest whose browser was killed mid-turn coming back to a
       battle prompt that draws nothing. Watching brief only; three dedicated hunts have failed.
    4. **Run the `4/` gates by name** (below). Root `npm test` scans only the older game.
    5. **Bump `PP4_STAMP`** at `4/src/ui/stage.js:32`, once.
    6. **Prove the diff touches only `4/` and `.planning/`** — never `CNAME`, `robots.txt` or
       `sitemap.xml`.
    7. **Push, pull, verify zero both ways**, then tell Wyatt the stamp in plain English **with the
       size stated**: what he will see differently, how much of his list this covers, what it leaves
       undone. Include any URL he needs in that same message.
    8. Kill every Chrome and server before replying.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/pass_narration_test.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/audio_map_check.js && node 4/scripts/pp4_timeroff_check.js && test "$(ls .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/b-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 8 && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^4/' | grep -v '^\.planning/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt opens playpastrypirates.com/4 on his laptop, sees the new stamp, and gets the narrow centred phone-shaped board — the same picture he sees on his phone.</human-check>
  </verify>
  <done>
    Group B is live under a fresh `PP4_STAMP`; both sides of a real crew game are screenshotted; the
    named `4/` gates pass; the shot clock still arms correctly; the diff touched only `4/` and
    `.planning/`; `main` and `origin/main` are zero apart both ways; and Wyatt has a plain-English
    message naming the stamp and the size of what landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A button's `why` text -> `emojify()` -> a `data-why` HTML attribute -> the DOM | Item 2's suspected path: a string is transformed then embedded in markup |
| Host browser -> Firebase `battle` node -> guest's `renderBattleFromSnap` | Battle snapshots rendered by every spectator |
| Repo `main` -> GitHub Pages -> real players on the live root game | A push publishes immediately; no build step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-09 | Tampering | `emojify()` splicing generated markup into an HTML attribute value (item 2's suspected mechanism) | high | mitigate | This is an HTML-attribute-injection shape, and today it is held off only by an in-code assertion that no string reaching it contains a literal `>`. Task 3 must end with that limitation **honoured by construction or removed**, not restated. All `why` and `label` text in this path is author-written, so there is no player-controlled input today — but the same sink would be an injection point if one were ever added, so fix the mechanism rather than the one string. |
| T-02.2-10 | Tampering | New `min-width` CSS rule and the `vwPx`/`vhPx` container branch | low | mitigate | Both additive with an explicit fallback to today's behaviour when the container is absent; the phone layout is screenshot-compared before and after. Reverting is deleting two things (D-18). |
| T-02.2-11 | Denial of service | Deferring the panel build for item 11 while the shot clock arming continuation runs through `ask()` | high | mitigate | A panel deferred past the clock's arming point could leave a captain unable to answer before their time expires — a turn-blocking fault, which is precisely the class this phase exists to end. Task 5 checks clock arming explicitly and by name, not by screenshot. |
| T-02.2-12 | Denial of service | Headless Chrome and `http.server` left running | medium | mitigate | Bounded loops; `killAll()` plus `pkill` inside the same task; asserted in Tasks 1 and 5's automated verify. |
| T-02.2-SC | Tampering | Package installs | n/a | accept | No package installs in this phase. |
</threat_model>

<verification>
- The named `4/` gates pass by name. Root `npm test` is not evidence about this phase.
- Every one of items 2, 3 and 11 has a measured OBSERVED/NOT-OBSERVED verdict with a screenshot
  before any line was changed for it.
- Item 2's verdict names which of the three outcomes held — including the Safari module-cache
  outcome, in which no code changes.
- The phone layout is screenshot-compared before and after item 22's change and is unchanged.
- The shot clock is verified to arm correctly after item 11's timing change.
- `PP4_STAMP` bumped exactly once and confirmed serving.
- No Chrome and no local server survives this plan.
</verification>

<success_criteria>
Wyatt opens the game on his laptop and gets the same narrow, centred, square-board picture he gets on
his phone. The ovens-be-roarin' card is centred. A battle frames the two boats fighting. A popup
waits for the boats to stop. And the flat-card Attack prompt is either fixed with a screenshot pair
proving it, or explained as something other than a code fault.

**This plan does not satisfy the phase gate.** The gate is Wyatt playing a crew game and not seeing
items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or 22.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-03-SUMMARY.md` when done. It
must state: the new `PP4_STAMP`; the OBSERVED/NOT-OBSERVED verdict and screenshot path for each of
items 2, 3 and 11; which of item 2's three outcomes held; the two things to delete to revert item 22
in Phase 8; the ceremony surfaces swept for item 5; whether items 1 and 14 were still open from Group
Q; and any Group E observation.
</output>
