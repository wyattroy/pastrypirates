---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 07
type: execute
wave: 6
depends_on: []
files_modified:
  - 4/src/ui/util.js
  - 4/src/ui/stage.js
  - 4/src/ui/panel.js
  - 4/src/ui/flow.js
  - 4/src/ui/recipe.js
  - 4/src/orchestrator.js
  - 4/src/engine/index.js
  - 4/index.html
  - 4/scripts/narration_timeline.mjs
  - docs/DISPLAY-RULES.md
autonomous: true
requirements: [PAR-02, PAR-08, PAR-10, PAR-14]
group: E
drop: "Group E — Wyatt's afternoon solo list (build t)"
items: [1, 2, 3, 4, 5, 6, 7, 8, 9]
plus: "his TWENTY-TWO's item 9 — the recipe card (D-35). Two lists, two number 9s: afternoon item 9 is the crate cue, twenty-two item 9 is this card. Neither is renumbered."

must_haves:
  truths:
    - "A narration box no longer pops up and vanishes in the same instant. In all four measured cases — the dock flip's TAILS bubble, the post-sail action menu, the crow's-nest 'battle's brewing' call, and morning item 3's pre-sail flash — no bubble is created and marked for removal inside the same 300ms (his items 2 and 8)."
    - "The flip-result bubble ('X flips TAILS') is actually visible for the hold it was given — not hidden behind the ceremony veil for ~47% of it — and the game can never stall waiting for a veil that does not leave."
    - "The Buy prompt's pill sits fully inside the board at 1400x900; it does not overhang the right edge into the dark margin."
    - "A storm reads as ONE summary. A captain whose push is stopped by another SHIP is folded into that summary instead of narrating inline, and a captain who never moved at all still appears in it (his item 3)."
    - "The black-market ceremony appears the first time any shelf empties, whoever empties it — bot or human (his item 7, rule 13). Measured today: a bot claims the first dry shelf in 76% of solo voyages, and today the ceremony is swallowed in every one of them."
    - "A medium narration line no longer sits at a flat 2550ms floor. Hold is overhead plus characters divided by a derived reading rate, so ~27 chars reads near 2.1s and ~75 chars near 4.5s, with the long-line ceiling Wyatt already approved unchanged (D-34)."
    - "The radial fan's prompt buttons carry the sail squares' own pulse — the same keyframes, not a new effect — so 'this is asking you to tap it' looks the same everywhere on the board (D-32, rule 8)."
    - "The Dock petal reads as the ingredient image, the word Dock, and a coin with no number (D-33) — on host, guest and draft alike, from one label."
    - "The wind pill is on screen from the first frame of a voyage, with a placeholder where the wind is not known yet, on BOTH tiers — the guest draws it too (his item 5, PAR-02)."
    - "The crate sound and the crate pop land on the Buy click, not after the narration has faded (his item 9). A bot's crate already lands at the right moment; the human's now matches it (rule 13)."
    - "The recipe modal is option C as Wyatt picked it (D-35): the image is no longer cramped against the italic description, and the lighter gradient at the top of the image reaches the title separator with no hard cut-off edge."
    - "Every number in this plan is measured before and after, on host AND guest, by an instrument that was shown to fail first (rule 6)."
    - "Wyatt is told a new PP4_STAMP and can see it live (D-02)."
  artifacts:
    - 4/scripts/narration_timeline.mjs
    - 4/src/ui/recipe.js
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-E-MEASUREMENTS.md
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/e-*.png
  key_links:
    - "ask()'s broadcast-mirror wait line (4/src/ui/util.js:1656) -> promptTick()'s wait-line retirement (4/src/ui/stage.js:1513, added by 3a80839) -> finish() (4/src/ui/stage.js:855). The whole pop-and-vanish race is these three lines, ~2ms apart."
    - "The radial fan's OWN existing dedup (4/src/ui/stage.js:1705, 'if a live bubble is just this pill's own words, retire it') -> the codebase has already ruled the mirror redundant with the pill. That ruling is why the fix belongs at the mirror, not at promptTick."
    - "netNarrate (4/src/orchestrator.js:308) picks the variant and drops it; watchNarr (4/src/orchestrator.js:1470) hands flash() the RAW payload and flash() picks for itself. Two tiers, two shapes, one drawn thing — this asymmetry is PAR-14's own seam and is where the wait-line predicate has to land."
    - "netSetNarr (4/src/net/writers.js:94) already carries `wait` across the wire as a UI-node field, with its own written precedent for why a display flag must cross. `variants` crosses too. Between them the predicate needs NO new wire field."
    - "#pp4Fx z-index 21 (4/index.html:1721) vs #pp4Veil z-index 44 (4/index.html:1733) -> the flip-result bubble's first ~1.2s is spent invisible."
    - "cerWatchResult()'s 1100ms reveal beat and 6000ms fallback (4/src/ui/stage.js:934, :937) -> the ONLY two numbers a visibility-deferred hold may derive its cap from. Nothing new gets typed."
    - "msgHoldMs() (4/src/ui/util.js:1256, HOLD_FLOOR/HOLD_MS_PER_CHAR/HOLD_PAUSE/HOLD_CEILING) -> stage.js:792's Math.max(2550, ...*1.5) with NARRATION_HOLD_CEILING_MS=3330. The 2550 floor is D-34's whole target; the ~5s ceiling is D-10 and stays."
    - "EVENT_NARRATION.blocked (4/src/ui/util.js:~631) still carries txt while windmove/blownOut/anchorHold no longer do -> the one storm outcome b8e9eea's own audit missed; runStormLive()'s inline narrateLastEvent() (4/src/ui/flow.js:~1200) is what fires it."
    - "noteStormOutcome()'s `if(!moved)return;` (4/src/engine/index.js) -> why a never-moved blocked captain is missing from the summary as well as narrating outside it. The one engine note change in this plan."
    - "narrateLastEvent()'s firstDry gate (4/src/ui/panel.js:1188) vs narrateCurrent()/botBeat() (4/src/ui/util.js:~1832) -> two narration functions, one of which never learned about the black market. Rule 23's worked example, live."
    - "humanDock()'s trailing liveRender() (4/src/ui/flow.js:~1353) sits AFTER `await narrateLastEvent()`, and liveRender() is where playForEvent() and spawnPops() fire -> the crate sound and the crate pop are both held behind a 2.5-5s narration hold. botBeat() already calls onLiveRender() BEFORE narrating."
    - "flow.js:1850's dock option (`label`/`short`) -> the one place the Dock petal's words are chosen; optionButtonsHTML (4/src/ui/util.js:1594) carries them to host, guest and draft."
    - "recipeModalHTML() (4/src/ui/recipe.js:357) draws the MODAL (.recipeModalThumb / h2 / .recipeModalDesc, styled 4/index.html:305-311); recipeCardHTML() (:345) draws the small draft-picker card (.recipeThumb / .recipeTitle / .recipeDesc, styled :1393-1398, desc hidden on the stage at :1923). TWO builders, two class families, NOT one shared component — D-35's rule-8 sweep has to say so rather than assume."
    - "The title separator is h2's own `border-bottom: 3px double #b48a52` (4/index.html:309) — there is no separate separator element, so 'the line separating the title' is h2's border-box bottom edge and that is what a getBoundingClientRect check must read."
    - "The sketch's .thumbWrap `margin: 14px -22px 4px` (notes/sketches/09-recipe-card/option-c-real-art.html:62) is BOTH of D-35's defects in one declaration: the 14px top margin is the band of plain card above the gradient (his hard edge), and the 4px bottom is the cramping against the italic description."
    - "@media print (4/index.html:330-334) reflows this same modal for Download PDF — a gradient that now reaches the separator must not break that path."
    - ".sailCell + @keyframes sailBounce (4/index.html:914, :927) and its prefers-reduced-motion opt-out at :930 -> the exact pulse D-32 names, and the accessibility rule that must travel with it."
---

> # READ THIS BOX BEFORE THE FIRST TOOL CALL — the three files this plan edits may already be dirty
>
> **The D-31 desktop-layout work touches `4/index.html`, `4/src/ui/stage.js` and `4/src/ui/util.js`
> — the same three files Tasks 2, 3 and 5 edit.** When this plan was written those edits were
> **uncommitted on disk** with `PP4_STAMP` already bumped to `2026-08-20u`, while the build Wyatt
> was playing was still `t`. Its own stash name is `d31-desktop-layout-wip-2026-08-21`.
>
> **First act of Task 1, before any measurement:** establish what is actually on disk and what is
> actually live.
>
> ```bash
> cd /Users/wyattroy/Documents/Projects/pastrypirates && git fetch origin && git pull origin main
> git status --short && git stash list
> grep -n 'PP4_STAMP =' 4/src/ui/stage.js          # what the tree says
> git show origin/main:4/src/ui/stage.js | grep -n 'PP4_STAMP ='   # what is LIVE
> ```
>
> ## THE SEQUENCING IS DECIDED. `u` SHIPS FIRST — DO NOT START THIS PLAN BEFORE IT HAS.
>
> **Wyatt's order, via the orchestrator, 2026-08-21: D-31 (`u`) lands first; Group E lands on top of
> it.** This is not a preference to weigh — it is the starting condition.
>
> **The gate, and it is checkable in one command.** Task 1 does not begin until BOTH are true:
>
> ```bash
> git fetch origin -q
> git show origin/main:4/src/ui/stage.js | grep -o '2026-08-20[a-z]'   # must read u OR LATER
> git status --porcelain -- 4/                                          # must print NOTHING
> ```
>
> A stamp earlier than `u`, or a dirty `4/`, means D-31 has not landed. **Stop and say so** — do not
> start measuring against a tree that is about to move under you, and do not finish D-31's work for
> it. Three of this plan's tasks edit the very files D-31 is holding, and Task 2's whole method is
> before-and-after numbers, which are worthless if the tree changes between the two.
>
> - **The stamp this plan bumps is the NEXT LETTER AFTER WHATEVER IS LIVE when Task 8 runs** — `v`
>   if `u` is live, `w` if D-31 needed a follow-up. Read it; never assume it.
> - **Line numbers in this plan are from the working tree as it stood on 2026-08-21 with D-31's
>   edits applied.** They will drift. **Every seam is also named by its function and its own
>   comment text — navigate by the name, verify by the line.**

<objective>
**Group E — Wyatt's afternoon solo list, played on build `t`.** Nine things he saw with his own eyes
in one sitting, triaged into seven tasks and **one drop**.

**Three of the nine are already his decisions and must not be re-asked** — D-32 (the petals get the
sail squares' pulse), D-33 (the Dock petal shows the ingredient, the word Dock, and a bare coin),
D-34 (narration holds at reading speed). **Four more already have a measured root cause** written
down in two debug documents, so this plan implements findings rather than hunting for them: items 2
and 8 plus the tails-vanish and morning item 3 all collapse to ONE race
(`.planning/debug/tails-narration-vanishes.md`), and items 3 and 7 are two instances of one class —
two code paths that must agree and were never converged
(`.planning/debug/storm-summary-and-black-market.md`, which cites rule 23 by name).

**The size, in his terms (rule 3):** this is **all nine** of the items on his afternoon list. After
it, a player stops seeing boxes flash and disappear, the storm speaks once, the black market shows
up, medium lines stop dragging, the buttons look tappable, the Dock petal tells the truth, the wind
pill stops popping in, and the crate lands when you buy it. **Plus one more he ruled on while this
was being planned:** the recipe card — item 9 of his TWENTY-TWO, not of the afternoon list — built
as option C with his two changes (D-35), which is Task 7. **What it leaves undone:** the desktop
layout (D-31), which is its own run and **ships before this plan starts**, not after it.

**Nothing here waits on Wyatt.** He is awake and will play the drop; every decision inside is
already his. There is no checkpoint in this plan.

Purpose: the game gets nine visible faults lighter in one build he can play tonight.
Output: one drop under a fresh `PP4_STAMP`, a before/after measurement table, and an honest account
of anything that did not reproduce.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF-2026-08-21-afternoon.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/debug/tails-narration-vanishes.md
@.planning/debug/storm-summary-and-black-market.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-MOUSE-QA-2026-08-21.md
@docs/DISPLAY-RULES.md
</context>

<absolute_paths>
Every path is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`. The game under
development is `4/`. **Never touch a root-level `src/` path** — the two trees share an identical
internal layout, so a mis-rooted edit opens a real file, applies cleanly, passes `node --check`, and
modifies the wrong copy with every safety signal reporting success. After each batch of edits:

```bash
git diff --name-only | grep -v '^4/' | grep -v '^\.planning/' | grep -v '^docs/'   # must print NOTHING
```
</absolute_paths>

<tasks>

<task type="auto">
  <name>Task 1: The instrument, and every BEFORE number this plan owes (rule 6)</name>
  <files>4/scripts/narration_timeline.mjs, .planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json</files>
  <precondition>A local static server can be started on a free port and a headless Chrome launched against it; `4/scripts/mouse_qa.mjs` runs to completion at 1400x900 on the current tree.</precondition>
  <read_first>
    `docs/DRIVING-THE-GAME.md` — **all of it, before you touch a browser**, not after you are stuck.
    §3 solo, §4 the turn loop and the two things that stall every naive driver, §5b the autoplay
    driver that actually plays, §5c driving a guest while a human hosts, §5e posing the state you
    want instead of playing your way to it. Sessions have re-derived every one of those lessons the
    hard way; do not add to the count. **§5e posing is allowed in solo and pass-and-play
    (`appState.room === null`) and FORBIDDEN in a real multiplayer room.**
    `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-MOUSE-QA-2026-08-21.md` — the
    rules the mouse rig paid for: always headless, always `--mute-audio`, launched from a background
    shell, never announce a window, **scope every `pkill` to your own port** (another agent's blanket
    `pkill -f remote-debugging-port` kills every probe on the machine).
    `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md` — the
    `MutationObserver` on `.pp4Bub` add/remove technique that measured 3305ms/3304ms host/guest and
    then 5304ms/5297ms after. Task 4 reuses it exactly; do not invent a second way to time a bubble.
  </read_first>
  <action>
    **Nothing in this plan may be reported as fixed against a number that was never taken first.**
    Two days were lost this month to five defects reported as confirmed with three of them
    unmeasured, four of which did not exist. This task exists so that cannot happen again.

    Build `4/scripts/narration_timeline.mjs` — a kept, repo-resident instrument in the same spirit
    as `4/scripts/mouse_qa.mjs`, taking `<outdir> <W> <H> <port> <dbgport>` and writing one JSON.
    Extend the mouse rig rather than starting over: real CDP mouse events, headless, muted, bounded
    loops (`for (let i=0;i<N;i++)`, never `while(true)`).

    Arm, in the page, before the first click:

    1. **A `MutationObserver` on `.pp4Bub`** recording, per bubble: text, `created_ms`,
       `out_ms` (when `out` joins its class list), `removed_ms`, and the **call stack captured at the
       moment `classList.add('out')` fires** — that stack is what named `promptTick` last time and it
       is what will prove the fix.
    2. **An occlusion sampler**: for each bubble, the ms during which `#pp4Veil` exists while the
       bubble is in the DOM. `visible_ms = removed_ms - created_ms - occluded_ms`. **This is the
       flip-result number**, and it is the one an ordinary hold measurement cannot see.
    3. **A `#pp4Prompt` / `#actionPanel` attribute timeline** — `display`, `pendingReveal`,
       `needsAction` — so the gap between the mirror vanishing and the real prompt arriving is a
       number, not an impression.
    4. **A rAF sampler of the board's own top edge** (`#boardwrap` or the board band, whichever the
       camera actually reads) across the first 4 seconds of a voyage, so item 5's claimed layout
       jump when the wind pill appears is measured rather than assumed.
    5. **A pill-geometry probe** at the Buy prompt: `.apMsg`'s rendered rect against `body`'s own
       rect, reporting `overRightPx`. The last run measured **63.39px** of overhang at 1400x900 on
       a clean build `t`.
    6. **An audio-cue timestamp**: monkey-patch nothing in the game — instead record the ms between
       the trusted mouse-down on the Buy petal and the `dock` event appearing in
       `appState.game.events`, and separately the ms until `liveRender()` has run (observable as
       `appState.evIdx` reaching that event). **That difference is item 9's whole complaint** and it
       must be a number before it is a fix.

    **RED-PROOF THE INSTRUMENT BEFORE BELIEVING IT.** Three checks in one night this month measured
    something other than what they named — `offsetParent` (always null for a `position:fixed`
    element, so it condemned a working screen), a poll for a prompt that a driver was answering
    every 700ms, and a check for buttons on a prompt kind that draws squares on the board. For each
    sampler above, **prove it can go red**: force a bubble to vanish early, force an occlusion,
    force an overhang. Record the red-proof in the output JSON as `redproof: {name: "seen red"}`.
    **When a check condemns something known to work, suspect the check first.**

    Then run it and capture BEFORE numbers for all four cases named in the trace:
    **A** the dock flip's TAILS bubble (pose the coin per §5e: `game.r = () => 0.9`, solo only,
    restored immediately after), **B** the crow's-nest "battle's brewing" call (the one case the
    trace derived from code and never drove — **drive it this time, or record it as still
    unmeasured; do not upgrade a code-derived finding to a measured one**), **C** the post-sail
    action menu, **D** morning item 3's pre-sail flash.

    Run at **1400x900** (his debug width) and — because every number in this plan must hold on both
    tiers — repeat cases A/C/D against a host+guest pair with `4/scripts/mp_rig.mjs` on fresh ports.
    Also capture the three narration-hold baselines D-34 needs: a **27-char** line, a **~75-char**
    line, and a long line at the ceiling.

    Write `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json`.

    **Kill every Chrome and every server before you finish this task**, scoped to your own ports.
    Not at the end of the plan — at the end of this task. Wyatt is at the keyboard on the machine
    the probes are heating; 53% CPU across 13 stray Chrome processes has already happened once.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && test -f 4/scripts/narration_timeline.mjs && node --check 4/scripts/narration_timeline.mjs && node -e 'const b=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json"); for(const k of ["cases","redproof","holds","pill","board_top","buy_to_render_ms"]) if(!(k in b)) throw new Error("e-before.json is missing "+k); const cs=Object.keys(b.cases); if(cs.length<3) throw new Error("expected at least 3 driven cases, got "+cs.length); const rp=Object.values(b.redproof); if(!rp.length||rp.some(v=>String(v).indexOf("red")<0)) throw new Error("no sampler was proven able to fail"); for(const h of ["c27","c75","long"]) if(typeof b.holds[h]!=="number") throw new Error("missing hold baseline "+h); console.log("BEFORE numbers captured:",JSON.stringify(b.holds),"overhang",b.pill.overRightPx)' && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
  </verify>
  <done>
    `4/scripts/narration_timeline.mjs` exists, runs, and every sampler in it has been seen red at
    least once. `e-before.json` holds the pop-and-vanish timings for at least three driven cases on
    both tiers, the flip bubble's occluded-versus-visible split, the three hold baselines, the pill
    overhang in px, the board-top sampler, and the Buy-click-to-render latency. Case B is either
    driven or explicitly recorded as still code-derived only. No browser and no server survives.
  </done>
  <reversibility rating="reversible">A measurement script and a JSON file. No game code is touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The wait line that retires itself — items 2 and 8, the tails vanish, and morning item 3, at ONE place</name>
  <files>4/src/ui/util.js, 4/src/ui/stage.js, 4/src/orchestrator.js, 4/index.html, docs/DISPLAY-RULES.md</files>
  <read_first>
    `.planning/debug/tails-narration-vanishes.md` in full — root cause, the millisecond timeline, and
    three proposed fix directions. `docs/DISPLAY-RULES.md` — this plan adds a rule to it.
    `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-03-SUMMARY.md` — **item 11's
    `pendingReveal` / `stageSettled()` gate was EXONERATED by the trace (0 violations, twice
    independently). Do not touch it.** The thing that flashes is a different element the gate never
    covered.
  </read_first>
  <action>
    **The mechanism, measured to the millisecond and not in dispute.** `ask()` posts a narration
    bubble carrying the same words as the prompt it is about to build; two milliseconds later, in the
    same synchronous turn, `panel()`'s trailing `syncPrompt()` runs `promptTick()`, which sees the
    action panel go empty to non-empty and retires whatever wait line is registered — including the
    one `ask()` itself just posted. The bubble pops in and is marked for its 300ms fade before a
    player can read a word of it, and the real prompt then stays behind its own (correct) settle gate
    for a further 0.7s to 1.2s. That is his "popped up and immediately disappeared", and it is the
    same event in all four cases.

    **THE FIX GOES AT THE MIRROR, NOT AT `promptTick`, and the codebase itself already decided
    this.** The radial fan carries a dedup whose own comment says the quiet part out loud: if a live
    bubble is only this pill's own words, retire it, because the pill already says it. **The mirror
    is redundant with the prompt by the project's own prior ruling.** Teaching `promptTick` to spare
    the line would therefore either leave a genuine duplicate standing on flat prompts, or be
    cancelled two lines later by that dedup — a fix that fights an existing fix. **And the
    retirement it would weaken is `3a80839`'s, which closed a real defect Wyatt reported — a
    "waiting for yer mateys" card outliving the prompt it announced. That fix must still work when
    you are finished, and Task 2's acceptance criteria measure it.**

    **The rule to implement, and it is a display rule, so write it into `docs/DISPLAY-RULES.md` in
    the same commit:** *a wait line addressed to the very captain who is about to be asked is not
    drawn on that captain's own screen — they are getting the question itself.* Everyone else still
    reads "…is deciding…", with no dismissal deadline, exactly as item 19 requires.

    **Derive the predicate from what the payload already carries — no new wire field, no new state
    (rule 9).** Two facts already cross to every client: the `wait` flag (whose own comment in
    `netSetNarr`/`netNarrate` records why a display flag MUST cross the wire, having caused a
    divergence once already) and the per-seat `variants` array. A wait line for which this browser's
    own seat has an addressed variant is, by construction, a wait line about a question coming to
    this browser. That is the whole predicate and it needs nothing invented.

    **IT MUST BE EVALUATED IN ONE PLACE THAT BOTH TIERS REACH (rule 23, PAR-14).** Note the
    asymmetry you are landing in the middle of: on the host, `netNarrate` picks the variant and then
    hands the render seam a bare string, discarding the payload; on a guest, `watchNarr` hands
    `flash()` the RAW payload and `flash()` picks for itself. **Two shapes for one drawn thing is
    exactly PAR-14's open seam.** Converge them — the host should hand the render seam the same
    thing a guest does — so the predicate is written once and cannot be aimed differently.
    **A branch on host-versus-guest anywhere in this diff is a defect, and the verify greps for
    one.** Host and guest decide who computes the game, never what is drawn.

    **Exit ramp, named rather than silent:** if converging those two call shapes reaches further
    than one clean change, implement the predicate at both seams from a single shared helper — one
    definition, two callers — and **write down in the summary that the two call shapes are still
    two, as a named seam**, the way `02.15-VERIFICATION.md` names the parked `ask()` channel. Do not
    quietly leave two copies of a predicate.

    **The second bug in the same moment: the ceremony veil hides the flip result.** The
    "X flips TAILS" bubble is drawn into a stacking context below the full-screen ceremony veil, so
    roughly 1.2 seconds of its 2550ms hold — about 47% — is spent invisible. **Do not raise the
    bubble above the veil.** Two reasons: the veil is Wyatt's own sanctioned exception to
    hold-the-sea and the ceremony is meant to own the screen; and the bubble's tail points at a boat
    the veil is covering, so it would point at nothing. **Fix the clock, not the z-order: a hold is
    a deadline for READING, and it must not run while the line cannot be seen.** Start the hold when
    the veil goes.

    **THE HOLD MUST STAY BOUNDED. This is the critical class in this project** — "the game just
    completely stalled" is a playtest-22 finding and every narration hold is awaited by the game
    loop. A deferral that can wait forever is a stall with a good excuse. Cap it, and **derive the
    cap from the ceremony's own two existing numbers** (its reveal beat after the face lands, and
    its own fallback teardown), never a new typed constant — nothing in this game is a constant.

    **The third bug in the same moment: the Buy pill overhangs the board's right edge** by 63.39px
    at 1400x900, measured on a clean build `t`. The leading hypothesis in the trace is a width read
    taken before the message's inline ingredient image has decoded — the same class of fault
    `panel.js` already documents and fixes for the panel box itself, by re-measuring when an image
    loads. **Mirror that existing pattern rather than inventing a second one.** Consider also that
    the placement memo key does not include the message's own text, so a content-only change can be
    short-circuited past the recompute — check both, fix what measures.

    **This one carries a trap worth stating plainly: the overhang does NOT reproduce with D-31's
    wider desktop layout applied** (the same probe measured it comfortably inside). That is almost
    certainly the wider column hiding the stale-width mechanism, not the mechanism being fixed.
    **Verify against a tree that reproduces it, and re-verify after D-31 lands.** If it genuinely
    cannot be reproduced on the tree you are shipping, say exactly that — observed on `t`, not
    reproducible on this tree, mechanism unfixed — rather than marking it done.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/prompt_field_parity_check.js && node -e 'const fs=require("fs"); const strip=p=>fs.readFileSync(p,"utf8").split("\n").filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join("\n"); const cp=require("child_process"); const base=p=>{try{return cp.execSync("git show origin/main:"+p,{encoding:"utf8"}).split("\n").filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join("\n");}catch(e){return null;}}; const n=(t,re)=>t==null?null:(t.match(re)||[]).length; const RE=/isHost|amHost|isGuest/g; for(const p of ["4/src/ui/util.js","4/src/ui/stage.js","4/src/orchestrator.js"]){const now=n(strip(p),RE), was=n(base(p),RE); if(was==null){console.log("no baseline for "+p+" — skipping");continue;} if(now>was) throw new Error(p+" gained "+(now-was)+" host/guest branch(es) ("+was+" -> "+now+"). The shot clock and room ownership are the sanctioned residue; rule 23 forbids ADDING one that decides what is DRAWN.");} const s=strip("4/src/ui/stage.js"); if(!/waitFinish/.test(s)) throw new Error("promptTick lost its wait-line retirement — 3a80839 fixed a real defect and must keep working"); console.log("narration seam: no NEW host/guest branch vs origin/main; 3a80839 retirement intact")' && node -e 'const b=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json"), a=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json"); let bad=[]; for(const k of Object.keys(a.cases)){const c=a.cases[k]; if(c.tier_host==null||c.tier_guest==null) continue; } for(const k of Object.keys(a.cases)){const c=a.cases[k]; if(c.selfretire_ms!=null && c.selfretire_ms<300) bad.push(k+" still self-retires in "+c.selfretire_ms+"ms");} if(bad.length) throw new Error(bad.join("; ")); const vb=b.cases.A&&b.cases.A.visible_ms, va=a.cases.A&&a.cases.A.visible_ms; if(vb!=null&&va!=null&&!(va>vb)) throw new Error("flip-result bubble is not visible longer than before: "+vb+" -> "+va); console.log("pop-and-vanish cleared; flip bubble visible "+vb+"ms -> "+va+"ms")' && grep -c 'wait line' docs/DISPLAY-RULES.md</automated>
    <human-check>Solo, at a dock, flip the coin and land TAILS. The flip result is readable, then the Buy prompt arrives — with nothing flashing in between and the pill fully on the board. Then sail: the action menu arrives once, after the boat has stopped. Screenshot both moments, and both sides of a crew game doing the same.</human-check>
  </verify>
  <done>
    In every driven case, no bubble is created and marked for removal within 300ms of each other, on
    host and on guest, and `e-after.json` proves it against `e-before.json`. The flip-result bubble's
    visible milliseconds went up. `promptTick` still retires a genuine stale wait line. No
    host-versus-guest branch decides what is drawn. `docs/DISPLAY-RULES.md` carries the new rule.
    The pill overhang is fixed, or its non-reproduction on the shipping tree is stated plainly.
  </done>
  <reversibility rating="costly">It changes the shape of the narration payload both tiers read. Reversible in code, but a half-reversion would re-fork the two paths — revert whole or not at all.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Narration holds at reading speed — his item 6 (D-34)</name>
  <files>4/src/ui/util.js, 4/src/ui/stage.js</files>
  <read_first>
    `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md` — item 10's whole
    story, including **the first attempt that shipped and changed nothing**: the outer clamp was
    raised while an inner ceiling was already binding every line before the clamp ever saw it. That
    is the shape of the mistake available here again.
  </read_first>
  <action>
    **His words, and the measurement behind them (D-34).** Today the hold is a floor plus a
    per-character term: `msgHoldMs()` returns base plus 20 per character plus 300 per pause, clamped
    into a narrow band, and then the stage multiplies by 1.5 and floors the result at 2550ms. So
    *"Blown into the trade winds!"* — 27 characters — computes to 1560ms and is then **floored to
    2550ms**, and so is every line up to about 85 characters. **The floor is the drag**, and it is
    what makes a short line feel as long as a medium one.

    Given the choice between lowering the floor and replacing the model, **Wyatt chose the
    reading-speed model (D-34)**: hold equals a small overhead plus characters divided by a reading
    rate. Targets he was shown and picked against: ~27 characters near **2.1s**, ~75 characters near
    **4.5s**, long lines unchanged.

    **DERIVE THE RATE ONCE, from something the game already computes** — a hardcoded
    characters-per-second is a price list standing in for a quantity that moves. The two anchor
    points above and the ceiling already approved under D-10 over-determine it; solve for the rate
    and let the two named holds fall out, rather than typing three numbers that must be kept in
    step. The elegant version of this change almost certainly **deletes** the floor rather than
    lowering it.

    **D-41 (Wyatt, 2026-08-21 evening): "assume people can read 15% faster."** Once the rate is
    derived from the two anchors, RAISE IT BY 15% — the ~27-char and ~75-char targets above become
    ~1.8s and ~3.9s. Change the rate, not the holds; the verify block's 27-char window (1500–2700ms)
    still admits the result, and the ceiling rule below is unchanged.

    **KEEP THE LONG-LINE CEILING. It is D-10, it is Wyatt's own approved result, and it measured
    5304ms on the host and 5297ms on the guest.** A reading-speed model that quietly re-opens the
    ceiling would undo a shipped decision he already made — that is a settled argument and the git
    log records what it cost.

    **BEFORE YOU CHANGE HOW THE HOLD IS PRODUCED, LIST WHAT READS IT.** This is the exact trap that
    produced a −21.2 ladder regression: replacing a constant with a calculation made every check
    that read it not merely wrong but *vacuous* — unable to fail, while still reading as protection.
    `msgHoldMs` has callers beyond the stage bubble, including the panel's own classic-path
    fallback and a chat-bubble alias that reproduces its curve deliberately. **Enumerate every
    caller and every gate that reads a hold, in the summary, before the change.** Decide for each
    whether it moves with the new model or is deliberately left on the old curve — and say which.

    Re-measure with the same `MutationObserver` technique on **both tiers**: 27 characters, ~75
    characters, and a long line, before and after, host and guest, into `e-after.json`.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/pass_narration_test.js && node -e 'const b=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json"), a=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json"); const B=b.holds,A=a.holds; if(!(A.c27<B.c27-300)) throw new Error("27-char line did not get shorter: "+B.c27+" -> "+A.c27); if(!(A.c27>1500&&A.c27<2700)) throw new Error("27-char hold "+A.c27+"ms is not near the ~2.1s D-34 names"); if(!(A.c75>A.c27)) throw new Error("a 75-char line must hold longer than a 27-char line; got "+A.c75+" vs "+A.c27); if(Math.abs(A.long-B.long)>400) throw new Error("the D-10 long-line ceiling moved: "+B.long+" -> "+A.long+" — it is Wyatt approved and must not"); if(a.holds_guest&&Math.abs(a.holds_guest.c27-A.c27)>150) throw new Error("host and guest disagree on the hold: "+A.c27+" vs "+a.holds_guest.c27); console.log("holds 27ch "+B.c27+"->"+A.c27+", 75ch "+B.c75+"->"+A.c75+", long "+B.long+"->"+A.long+" (ceiling held)")'</automated>
    <human-check>Play a voyage and read the short lines. They no longer sit there after you have finished reading them. The long ones are unchanged.</human-check>
  </verify>
  <done>
    A 27-character line holds near 2.1s and a 75-character line near 4.5s, measured on both tiers and
    within 150ms of each other. The long-line ceiling is where D-10 left it. The rate is derived
    once, not typed three times. The summary lists every caller of the hold and says which moved.
  </done>
  <reversibility rating="reversible">One formula, one call site's clamp. Revert restores the old curve exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 4: One narration path — the storm speaks once (item 3) and the black market shows up (item 7)</name>
  <files>4/src/ui/util.js, 4/src/ui/panel.js, 4/src/ui/flow.js, 4/src/engine/index.js</files>
  <read_first>
    `.planning/debug/storm-summary-and-black-market.md` in full — both root causes, the git
    archaeology, and the headless engine measurements (14.5% of storms; 76% of solo voyages).
    `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-04-SUMMARY.md` — the basis on
    which an engine change was permitted in this phase, and the disclosure that went with it.
  </read_first>
  <action>
    **These are one defect wearing two costumes, and the debug document says so by name: rule 23,
    ONE DISPLAY PATH. Two code paths that must agree were never converged, so a feature added to one
    silently never reached the other.** Fix them together, in that framing.

    **Item 3 — the storm.** A 2026-08-14 commit collapsed the per-ship storm chatter into a single
    summary line and its own header comment lists which outcomes it silenced: drift, blown-off-dock,
    and anchor-hold. **It never mentions the fourth — a push stopped by ANOTHER SHIP** — whose
    narration text still exists, and which fires inline from the per-square loop, a structurally
    separate call site the commit never touched. Measured: **14.5% of storms overall** carry at
    least one of these, **16.8% in rounds 1-3** (every captain starts one square from the others at
    Tortuga) falling to **10.2% by round 16** — which is exactly Wyatt's account: one-by-one early,
    one clean summary later, but not a hard rule.

    Strip that outcome's narration text the way its three silenced siblings already are, keeping the
    board feedback, and fold "held by a ship" into the summary's existing groups the way "held by
    land" already is.

    **The secondary defect matters as much as the first: a captain who never moved at all before
    being blocked is MISSING FROM THE SUMMARY ENTIRELY**, because the outcome recorder returns early
    when nothing moved. So today that captain both narrates outside the summary and is absent from
    inside it. **This is the one ENGINE change in this plan and it is disclosed, not slipped in:**
    the recorder must note a ship-block even when the ship never moved, so the summary can speak for
    them. **State it plainly in the commit message and in the summary.** The basis is the same one
    plan 02.2-04 recorded — the `4/` determinism corpus does not exist yet, so there is nothing to
    invalidate — **and that basis expires the moment a corpus is recorded.** Confirm before you
    write that it still has not been. Nothing else about the event stream changes.

    **Item 7 — the black market.** The once-per-voyage ceremony that teaches a player what a
    sold-out island means is gated inside the HUMAN narration path only. A bot's dock purchase
    narrates through a structurally separate, older function that has no knowledge of the flag at
    all — and the flag is a single voyage-wide latch that flips on the first shelf to empty
    **whoever empties it**. Measured across 500 seeded games: **a bot claims the first dry shelf in
    76.0% of solo voyages**, and in every one of those the ceremony is swallowed and can never
    appear again that voyage. Wyatt's report — that it simply was not there — is what a 76% failure
    rate looks like from a player's chair.

    **Two fixes are available and the plan chooses the wider one: CONVERGE.** Adding the same check
    to the bot path is smaller and it is the wrong shape — it leaves two narration functions that
    must be kept in step by discipline, which is the definition of a thing that will drift. The
    design-time question is *what makes these two agree?*, and "we remember to update both" is not
    an answer. **Rule 23 states the move exactly: when a SECOND consumer of the same thing appears,
    make the FIRST one go through the new path too.** The next feature gated on a once-per-voyage
    flag then gets this for free instead of paying for it again.

    **Rule 13 is the check that this landed right: a bot and a human must have identical rules and
    affordances.** After this task, "who emptied the shelf" cannot change whether the table is told.

    **Exit ramp, named:** if converging the two narration functions turns out to reach into the
    layering rules (the human path deliberately cannot import the flow module, and the bot path
    lives in a third file), then take the narrower fix — one shared, exported ceremony trigger both
    call — and **write in the summary that two narration functions still exist, as a named seam
    carried forward**, with the specific reason. Never leave it looking converged when it is not.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_narration_test.js && node 4/scripts/bot_bake_pass_check.js && node -e 'const fs=require("fs"); const strip=p=>fs.readFileSync(p,"utf8").split("\n").filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join("\n"); const u=strip("4/src/ui/util.js"); const bi=u.indexOf("blocked:(e,"); if(bi<0) throw new Error("could not locate the blocked storm narration entry"); const blk=u.slice(bi,bi+900); if(/return\s*\{\s*txt/.test(blk)) throw new Error("the ship-blocked storm outcome still RETURNS narration text — it must be silenced like its three siblings, keeping its board feedback"); const hooks=(u.match(/dryCeremony/g)||[]).length+(u.match(/firstDry/g)||[]).length+(u.match(/narrateLastEvent/g)||[]).length; if(hooks===0) throw new Error("util.js (narrateCurrent/botBeat, the BOT narration path) still knows nothing about the black-market ceremony — a bot emptying the first shelf still swallows it in ~76% of solo voyages"); console.log("storm blocked-line silenced; bot narration path now reaches the ceremony ("+hooks+" refs)")' && node -e 'const a=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json"); if(!a.storm||typeof a.storm.inline_lines!=="number") throw new Error("no measured storm result"); if(a.storm.inline_lines!==0) throw new Error("a storm still narrated "+a.storm.inline_lines+" line(s) outside its summary"); if(!a.storm.blocked_captains_in_summary) throw new Error("a ship-blocked captain is still missing from the summary"); if(!a.blackmarket||a.blackmarket.bot_first_ceremony_shown!==true) throw new Error("the ceremony did not appear when a BOT emptied the first shelf"); console.log("storm: one summary, blocked captain folded in; black market: ceremony shown on the bot path")'</automated>
    <human-check>Raise the storm chance and play several storms while the ships are still bunched at Tortuga. Each storm produces one summary sentence and every captain it touched is named in it. Then play an ordinary solo voyage and let a bot empty a shelf first — the black-market card appears, once.</human-check>
  </verify>
  <done>
    A storm that stops a ship against another ship reads inside the one summary, and a captain who
    never moved is still named there. The black-market ceremony appears the first time any shelf
    empties regardless of who emptied it, proven by driving the bot-first case. The engine note
    change is disclosed in the commit message and the summary, with the corpus basis re-confirmed.
    If the two narration functions are still two, the summary names that seam and why.
  </done>
  <reversibility rating="costly">It touches an engine outcome recorder. Reversible today only because no `4/` determinism corpus exists — confirm that is still true before committing, and say so.</reversibility>
</task>

<task type="auto">
  <name>Task 5: The three surfaces a player touches — the petal pulse (D-32), the Dock label (D-33), and the wind pill from frame one</name>
  <files>4/index.html, 4/src/ui/flow.js, 4/src/ui/stage.js</files>
  <read_first>
    `docs/BOARD-RENDERING.md` — mandatory before anything drawn (CLAUDE.md §4). **An HTML overlay
    mapped to board coordinates must be registered in `CAM_HTML_LAYERS` or it detaches the moment
    the director zooms**, and **anything that animates continuously must be HTML, not SVG** — Chrome
    cannot composite an SVG transform animation at all (~62 layouts/sec as SVG, zero as HTML).
    `docs/TRADE-SYSTEM.md` for what a dock actually sells before you re-word its button.
  </read_first>
  <action>
    Three of his items, all small, all visible at a glance, and all already decided.

    **(a) His item 1 — the prompt buttons are hard to notice. D-32: they get the SAIL SQUARES'
    pulse.** Not a new effect and not one of the four other glows in the file — he was shown
    `flipglow`, `pp4FocusPulse`, `pp4StagePulse`, `ahoyglow` and `goldShimmer`, and he chose the
    squares' own keyframes, applied through the same class. **The point is the vocabulary, not the
    animation: on this board, "pulsing" means "tap me", and it should mean that everywhere (rule
    8).**

    So this is a sweep, not a one-line edit. **Apply it to every prompt style that asks for a tap,
    and name in the summary which surfaces you checked and what each one got** — the radial fan's
    petals, the battle buttons, the flat button row, the centre-stage cards' own buttons, and the
    draft and recipe cards. Where a surface is deliberately left out, say why. **The FLIP coin keeps
    its own glow** unless converging it onto the shared class is genuinely trivial — if it is, do it
    and say so; if it is not, leave it and say that instead. An unexplained exception is how a
    consistency rule dies.

    **Carry the accessibility opt-out with the animation.** The sail squares already stop animating
    under a reduced-motion preference; a pulse copied without that rule is a regression wearing a
    feature's clothes. **Wherever the shared class lands, the reduced-motion rule lands with it.**

    Watch the cost, too: the squares' pulse is a transform-and-opacity animation on an HTML box,
    which is the cheap kind — keep it that way, and do not let it become a box-shadow animation on
    thirty elements at once. Quote frames-per-second beside any cost figure you report, and drive
    frames while you measure: an idle headless page stops producing them and animations then measure
    as free.

    **(b) His item 4 — the Dock petal. D-33: drop the anchor; show the ingredient image, the word
    Dock, and a bare coin with NO number.** His reason is exact and worth keeping: the flip decides
    whether it pays 1 or 3, so a number printed before the flip would be a lie. The dock sells a
    crate and the label shows the ingredient it sells — read `docs/TRADE-SYSTEM.md` before you touch
    the wording.

    The petal's words are chosen in one place, where the dock option is pushed with both a full
    label and its compact form, and they reach the host, a guest and the draft through the single
    shared button-row builder. **One edit; then prove it reached all three** — a guest and a draft
    card are exactly where a "one place" claim has been wrong before.

    Note the neighbouring ruling so you do not undo it: Attack states its powder and a pass pays a
    known amount **because those are known before you commit**; Dock is a coin flip and states no
    number. That is the consistency ruling already recorded in the file, and D-33 is its
    application, not an exception to it.

    **(c) His item 5 — the wind pill from frame one.** He wants `WIND NOW: ? · FORECAST: ?` on
    screen from the start, so nothing moves when the real wind arrives.

    **MEASURE BEFORE YOU BELIEVE THE REFLOW (rule 6).** The pill is a fixed, centre-translated
    element, so it may well cost the board nothing when it appears — Task 1's rAF sampler of the
    board's top edge over the first four seconds is what settles it. **Two honest outcomes, and
    both are fine: if the board moves, the placeholder fixes a real jump; if it does not, the
    placeholder is still what he asked for, and you report the shift as zero rather than claiming a
    fix you did not make.**

    **And there is a genuinely open requirement hiding in this item. PAR-02 was REOPENED at the 02.1
    gate: a guest holds the right wind data and DOES NOT DRAW THE PILL AT ALL** — the clock pill and
    chat bubble were missing from a guest's top bar too. **Check both tiers.** A placeholder that
    only the host ever sees would close his item 5 while leaving PAR-02 exactly as broken as it is
    now. Whatever you find, record it against PAR-02 by name.

    The placeholder text must be a placeholder, not a guess — the pill says the wind is not known
    yet, and never invents a direction.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node -e 'const fs=require("fs"); const h=fs.readFileSync("4/index.html","utf8"); const css=h.split("\n").filter(l=>!/^\s*(\/\*|\*)/.test(l)).join("\n"); const km=(css.match(/@keyframes\s+sailBounce/g)||[]).length; if(km!==1) throw new Error("D-32 says REUSE the sail squares keyframes, not clone them — found "+km+" definitions"); const rm=css.match(/prefers-reduced-motion[^}]*\{[^}]*\}/g)||[]; if(!rm.length) throw new Error("the reduced-motion opt-out vanished"); const uses=(css.match(/animation:\s*sailBounce/g)||[]).length; if(uses<2) throw new Error("the sail pulse is still applied to only "+uses+" surface — D-32 asks for the petals too"); console.log("D-32: one keyframe definition, "+uses+" surfaces, reduced-motion intact")' && node -e 'const fs=require("fs"); const f=fs.readFileSync("4/src/ui/flow.js","utf8").split("\n").filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join("\n"); const m=f.match(/canDock\)opts\.push\(\{[^}]*\}/); if(!m) throw new Error("could not find the dock option builder"); if(/⚓/.test(m[0])) throw new Error("D-33: the anchor is still on the Dock option"); if(!/short\s*:/.test(m[0])) throw new Error("the compact petal label is gone"); if(/\+\s*\$\{[^}]*dock(Heads|Tails)/.test(m[0])) throw new Error("D-33 says a bare coin with NO number — the flip has not happened yet"); console.log("D-33: anchor dropped, petal label carries no pre-flip number")' && node -e 'const a=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json"); if(!a.pill||typeof a.pill.board_shift_px_after!=="number") throw new Error("no measured board shift after the change"); if(a.pill.board_shift_px_after>1) throw new Error("the board still moves "+a.pill.board_shift_px_after+"px when the wind pill arrives"); if(a.pill.guest_draws_pill!==true) throw new Error("the guest still does not draw the wind pill — PAR-02 remains open, say so explicitly rather than failing silently"); console.log("wind pill: board shift "+a.pill.board_shift_px_before+"px -> "+a.pill.board_shift_px_after+"px; guest draws it")'</automated>
    <human-check>Open a voyage: the wind pill is there on the first frame with question marks, and nothing shifts when the real wind lands. The prompt petals pulse the same way the sail squares do. At a dock, the petal reads as the ingredient, the word Dock, and a coin — no anchor, no number. Screenshot all three, host and guest.</human-check>
  </verify>
  <done>
    The petals pulse with the sail squares' own keyframes, defined once, with the reduced-motion
    opt-out, and the summary names every surface swept and every deliberate exception. The Dock
    petal carries the ingredient, the word Dock and a bare coin on host, guest and draft. The wind
    pill is on screen from frame one on both tiers, with the board's measured shift reported either
    way, and PAR-02's guest question answered by name.
  </done>
  <reversibility rating="reversible">CSS, one label, and one placeholder string.</reversibility>
</task>

<task type="auto">
  <name>Task 6: The crate lands when you buy it — his item 9</name>
  <files>4/src/ui/flow.js</files>
  <read_first>
    `docs/AUDIO.md` — **mandatory before any sound change** (CLAUDE.md §4), and note what it already
    says about this exact stem: `store-ingredient` is the quietest file in the game at −31.9 LUFS,
    **15.6 dB** below the sword clash, and it is doing double duty as a placeholder elsewhere. Three
    defects are live in that document and one deleted line fixes two of them.
  </read_first>
  <action>
    **His complaint: the crate sound and the crate animation arrive after the summary has faded,
    instead of on the Buy click.** The cause is exact and it is one call in the wrong place.

    A human's dock emits the purchase event, then **awaits the full narration** — which under D-34
    is 2 to 5 seconds — and only then calls the render-and-sound moment. Both the sound cue and the
    crate pop hang off that one call, so both wait out the whole narration. **The bot's path already
    does it in the right order**: it renders (and therefore sounds) first, then narrates.

    **So this is a rule 13 fix as much as an audio fix: the bot already hears its crate at the right
    moment and the human does not.** Bring the human into line with the bot by moving the existing
    call to sit immediately after the purchase event is emitted — before the narration is awaited,
    not after.

    **THE EVENT-TO-SOUND MAP STAYS ONE MAP.** Do not add a second sound trigger, do not call the
    audio module from the prompt handler, and do not special-case the dock cue. **The map is not
    what is wrong — the moment is.** A second trigger would also fire the cue twice, which is a
    worse defect than a late one and much harder to notice in a summary.

    **The trap to check for, and it is the whole risk in this task: firing the same event's render
    twice would play the crate sound twice.** MOVE the call; do not add one. Then prove it: count
    the cue, do not eyeball it.

    Check what the reorder changes for what is drawn — the render reads the last event and the
    narration reads the last event, and two player-state writes currently sit after both. Confirm
    the panel still tells the truth about the purse (there is a deliberate silent event upstream
    whose entire job is that, and it must still be doing it), and that the narration still fires
    once, with the same words.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/audio_map_check.js && node 4/scripts/dlog_quantity_check.js && node -e 'const fs=require("fs"); const f=fs.readFileSync("4/src/ui/flow.js","utf8").split("\n").filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join("\n"); const i=f.indexOf("async function humanDock"); if(i<0) throw new Error("could not locate the humanDock definition"); const seg=f.slice(i, i+9000); const ev=seg.search(/ev\(\{\s*t\s*:\s*"dock"/); if(ev<0) throw new Error("could not locate the dock event emission"); const lr=seg.indexOf("liveRender()", ev); const nl=seg.indexOf("narrateLastEvent()", ev); if(lr<0||nl<0) throw new Error("could not locate the render call and the narration call after the dock event"); if(!(lr<nl)) throw new Error("the render-and-sound moment still runs AFTER the narration is awaited — the crate still lands late"); const n=(seg.slice(ev).match(/liveRender\(\)/g)||[]).length; if(n!==1) throw new Error("liveRender appears "+n+" times after the dock event — a moved call became an added one, and the crate sound will play twice"); console.log("dock: render-and-sound moment precedes the narration, exactly once")' && node -e 'const b=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json"), a=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json"); if(!(a.buy_to_render_ms < b.buy_to_render_ms - 1000)) throw new Error("Buy-click to crate moment did not improve by at least a second: "+b.buy_to_render_ms+"ms -> "+a.buy_to_render_ms+"ms"); if(a.buy_cue_count!=null && a.buy_cue_count!==1) throw new Error("the crate cue fired "+a.buy_cue_count+" times, not once"); console.log("crate lands "+b.buy_to_render_ms+"ms -> "+a.buy_to_render_ms+"ms after the Buy click, once")'</automated>
    <human-check>Unmuted, solo: dock, flip, buy a crate. The sound and the crate pop happen as you click, once. Then watch a bot buy one and confirm nothing changed for them.</human-check>
  </verify>
  <done>
    The crate sound and the crate pop fire on the Buy click, at least a second earlier than before
    and exactly once, measured. The event-to-sound map is unchanged and still has one entry per
    event. The bot's dock is untouched and the two paths now agree (rule 13).
  </done>
  <reversibility rating="reversible">One call moved within one function.</reversibility>
</task>

<task type="auto">
  <name>Task 7: The recipe card — build option C with his two changes (D-35)</name>
  <files>4/src/ui/recipe.js, 4/index.html</files>
  <read_first>
    **D-35 in `02.2-CONTEXT.md`, verbatim, before anything else** — it is short, it is his own words,
    and his notes are the design contract (D-23). Then
    `notes/sketches/09-recipe-card/option-c-real-art.html`, which is the thing he was looking at when
    he said them, and its `README.md`. Then `4/src/ui/recipe.js` — `recipeModalHTML()` at `:357`
    builds the modal, `recipeCardHTML()` at `:345` builds the small draft-picker card, and the CSS
    for both lives in `4/index.html` (modal at `:305-311`, small card at `:1393-1398`, with stage
    overrides at `:1921-1923`). `docs/BOARD-RENDERING.md` is NOT needed here — this card is a modal,
    not a board overlay.
  </read_first>
  <action>
    **His ruling (D-35), and it is a yes with two named changes — not a redesign.** *"yes, build C
    with two changes: 1. increase the padding around the bottom of the image (It's too cramped
    against the italicised description). 2. fix the strangely cut-off lighter gradient at the top of
    the images, so that the gradient extends all the way to the line separating the title and doesn't
    create the messy-looking edge."*

    Build option C into the **live** recipe modal. The sketch is the reference for the mechanism —
    a sticky title row carrying the two round print and email icons, the real art from the pastry
    assets, the text from the recipe book — not a file to copy wholesale.

    **BOTH OF HIS DEFECTS ARE THE SAME ONE DECLARATION IN THE SKETCH, and finding that first will
    save you guessing at either.** The sketch's image wrapper carries `margin: 14px -22px 4px` with
    the gradient painted on that wrapper's own box. So:

    - **The 14px TOP margin is his hard edge.** The gradient starts 14px below the title separator,
      leaving a band of plain card above it, and the radial gradient then terminates against its own
      box edge instead of fading out. That band and that termination together are the "messy-looking
      edge" he is describing.
    - **The 4px BOTTOM margin is the cramping.** Four pixels between the image and the italic
      description.

    **Change (a) — the padding below the image. DERIVE IT FROM THE CARD'S OWN TYPE SCALE, not a
    pixel constant (D-35 says so explicitly, and it is rule 9).** The description has its own font
    size and line height; express the gap in the card's own `em`/`lh` terms so it tracks the type if
    the type ever moves. **A pixel number here is a price list standing in for a quantity that
    moves** — and a hardcoded gap is exactly what would break the next time anyone touches the
    modal's font size.

    **Change (b) — the gradient reaches the separator.** Note carefully **which way**: the gradient
    is at the TOP OF THE IMAGE and the title sits ABOVE it, so the gradient extends **upward** until
    its top edge meets the separator's bottom edge. There must be no gap and no seam between them.

    **The separator is not its own element — it is the `h2`'s own `border-bottom` (a 3px double
    rule).** So "the line separating the title" means the `h2`'s border-box bottom edge, and that is
    what any measurement must read. Do not invent a separator `<div>` to make the check easier.

    **And "no hard cut-off edge" is a second requirement, not a restatement of the first.** Closing
    the gap is necessary and not sufficient: a gradient that fills the gap but still ends at a
    visible boundary is the same complaint moved. It must **fade to transparent before it reaches
    its own box edge** — the far stop is transparent, so there is nothing to see it end against.

    **Rule 8 sweep — and here the honest answer is that they do NOT share a component.** The
    orchestrator asked whether the draft picker's "Bake this!" card shares this one. **Read first,
    reported here:** `recipeModalHTML()` and `recipeCardHTML()` are two separate builders with two
    separate class families, and the small card's description is hidden outright on the stage.
    **Confirm that is still true, then state the consequence rather than skipping it silently:**
    D-35's two changes belong to the modal; the small card has no gradient today and no visible
    description to be cramped against. **If you find they DO share anything, sweep it and say so.**
    Name in the summary every place a recipe card is drawn and what each one got.

    **Do not break the print path.** This modal is printable — Download PDF is one of the two icons
    option C pins into the title row — and a print stylesheet already reflows it and hides the
    button row. A sticky header and a full-bleed gradient are both things that print badly if
    nobody checks. **Print it once and look at the result.**

    **The two icons are buttons a player taps, so they are inside rule 8's remit and inside D-32's.**
    If Task 5's shared pulse vocabulary applies to them, say whether it does and why — these are
    "act on this card" controls, not "answer the game" petals, and a defensible answer either way is
    fine. An undecided one is not.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node --check 4/src/ui/recipe.js && node -e 'const fs=require("fs"); const css=fs.readFileSync("4/index.html","utf8").split("\n").filter(l=>!/^\s*(\/\*|\*)/.test(l)).join("\n"); const i=css.indexOf("recipeModalThumb"); if(i<0) throw new Error("the modal thumb rule vanished"); const blk=css.slice(i-200,i+700); if(/margin:\s*[0-9.]+px\s+[^;]*\s[0-9.]+px\s*;/.test(blk)&&!/(em|lh|rem|ch)\b/.test(blk)) throw new Error("the image gap is still expressed only in px — D-35 says derive it from the card type scale"); if(!/(em|lh|rem)\b/.test(blk)) throw new Error("no type-relative unit found near the modal thumb — change (a) not applied"); const grad=blk.match(/(radial|linear)-gradient\([^;]*\)/); if(!grad) throw new Error("no gradient found on the modal image wrapper"); if(!/transparent|rgba\([^)]*,\s*0\s*\)/.test(grad[0])) throw new Error("the gradient does not fade to transparent — it will still end against a visible edge"); console.log("D-35: type-relative image gap, gradient fades out")' && node -e 'const a=require("./.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json"); const r=a.recipe_card; if(!r) throw new Error("no measured recipe-card geometry"); for(const w of ["w390","w1400"]){const m=r[w]; if(!m) throw new Error("no measurement at "+w); if(Math.abs(m.gradient_top - m.separator_bottom) > 1) throw new Error(w+": the gradient does not meet the title separator — gradient top "+m.gradient_top+" vs separator bottom "+m.separator_bottom+" ("+Math.abs(m.gradient_top-m.separator_bottom).toFixed(1)+"px apart)"); if(!(m.image_to_desc_gap_px >= m.desc_line_height_px)) throw new Error(w+": the image is still cramped — "+m.image_to_desc_gap_px+"px gap against a "+m.desc_line_height_px+"px line height");} console.log("D-35 measured at 390 and 1400: gradient meets the separator, image no longer cramped")'</automated>
    <human-check>Open a recipe card at phone width and at 1400. The lighter gradient runs right up to the double rule under the title with no band and no visible edge where it stops, and the image has room to breathe above the italic description. Then hit Download PDF and look at the printed page. Screenshot all three.</human-check>
  </verify>
  <done>
    The live recipe modal is option C. Measured at 390 and 1400 via `getBoundingClientRect`, the
    gradient's top edge meets the title separator's bottom edge within 1px, and the gap between the
    image and the italic description is at least one description line-height and is expressed in the
    card's own type-relative unit, not px. The gradient fades to transparent rather than ending at
    its box edge. The print path still works. The summary names every place a recipe card is drawn,
    says whether the two builders share anything, and gives a decided answer on the two icons.
  </done>
  <reversibility rating="reversible">One modal builder and its CSS. No engine, no board, no network.</reversibility>
</task>

<task type="auto">
  <name>Task 8: The Group E drop — one build, played in two tabs and solo, and handed to him with the size stated</name>
  <files>4/src/ui/stage.js</files>
  <read_first>
    `docs/GIT-AND-DEPLOY.md` §5 for the drop loop and the incident that earned it.
    `docs/DRIVING-THE-GAME.md` §5c for driving a guest while a human hosts.
    `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-MOUSE-QA-2026-08-21.md` for the
    mouse-QA invocation and the rules it paid for.
  </read_first>
  <action>
    **One drop for all nine items, and he will play it.** The same loop every group in this phase
    has run (D-02, D-03).

    1. **PLAY THE GAME YOURSELF FIRST, in two tabs, as a person (rule 19).** Host and guest, a real
       room, on fresh ports. Then a solo voyage, then a pass-and-play one. **The bar is whole
       voyages to the end card, not stretches of play** — that bar found a nine-day-old softlock on
       its first night. **Screenshot BOTH sides and compare them element by element**, because four
       of the seven faults this phase started with were host/guest divergences that a single browser
       cannot see by construction.
    2. **Screenshot the surfaces this plan changed, and the ones NEXT to them.** A regression this
       month was not in the sheet that changed; it was a heading peeking out from behind the ribbon
       three hundred pixels away. Save to
       `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/e-*.png`.
    3. **Run the real-mouse QA at 1400x900** — `node 4/scripts/mouse_qa.mjs <outdir> 1400 900 <port>
       <dbgport>` — headless, muted, launched from a background shell, on fresh ports, with every
       `pkill` scoped to your own port. A control that exists but cannot be clicked at its own
       centre is a finding, and that is the class of fault that put a Dock button off-screen past a
       green overnight suite.
    4. **Run the `4/` gates by name.** Root `npm test` scans only the older game and proves nothing
       about `4/`. Then `node scripts/host_guest_parity_check.js --tree=4 --strict`, which must show
       **exactly the three documented pre-existing reds — `PARITY-SAILRECT`, `PARITY-SWEEPARRIVE`,
       `PARITY-ORCH:localAsk`** — and no new ones. A fourth red means Task 2 or Task 4 re-forked a
       path. Run the planning health check too and **surface what it finds in the reply**; treat its
       pass/fail verdict as close to meaningless and read what it incidentally shows you.
    5. **Bump `PP4_STAMP` once**, to the next letter after whatever is live — read it, do not assume
       it (the box at the top of this plan).
    6. **Prove the diff touches only `4/`, `.planning/` and `docs/`** — never `CNAME`, `robots.txt`
       or `sitemap.xml`. Two sessions have come within one command of taking the live game down for
       real players by copying a file that claims the domain.
    7. **Push, pull, verify zero both ways.**
    8. **Write `02.2-E-MEASUREMENTS.md`** — one table, before and after, every number this plan
       produced, host and guest, with the instrument named beside each row and anything that did not
       reproduce marked as such. **Point to it; do not restate its verdicts elsewhere.**
    9. **Then tell Wyatt, in plain English, with the SIZE stated (rule 3).** Name the stamp. Say
       **what a player will experience differently**, **how much of his list it covers — all nine of
       the nine, or fewer and which**, and **what it explicitly leaves undone** (the recipe card
       waiting on his sketch pick, and D-31's desktop layout if it has not shipped). Use his own
       item numbers. Give him the URL in the same message. **If any item did not reproduce or was
       not fixed, say so in that message rather than in a document he has to go find.**
    10. **Correct the record in the open.** Two requirement rows are affected by what this plan
        measured and both should be updated with a dated note rather than silently: **PAR-10** was
        marked complete on a measurement that watched the prompt panel only, and the trace has since
        proven a different element breaks the same promise — "no popup appears until the camera and
        the ships have stopped moving" was not yet true when that row was ticked. **PAR-02** gets
        whatever Task 5 found about a guest drawing the wind pill. A requirement marked complete on
        a bad measurement rots exactly like one marked failed on a bad one — this phase has already
        learned that once.
    11. **Kill every headless Chrome and every local server before you reply.** Scoped to your own
        ports. He is at the keyboard, on the machine they are heating.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/pass_narration_test.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/audio_map_check.js && node 4/scripts/bot_bake_pass_check.js && node 4/scripts/dlog_quantity_check.js && node 4/scripts/pp4_timeroff_check.js && npm test --silent && node scripts/host_guest_parity_check.js --tree=4 --strict > /tmp/e-parity.txt 2>&1; node -e 'const t=require("fs").readFileSync("/tmp/e-parity.txt","utf8"); const known=["PARITY-SAILRECT","PARITY-SWEEPARRIVE","PARITY-ORCH:localAsk"]; const found=(t.match(/PARITY-[A-Za-z:]+/g)||[]); const extra=[...new Set(found)].filter(f=>!known.some(k=>f.indexOf(k.split(":")[0])===0)); if(extra.length) throw new Error("new parity failures beyond the three documented reds: "+extra.join(",")); console.log("parity: only the three documented reds")' && node ~/.claude/gsd-core/bin/gsd-tools.cjs validate health; cd /Users/wyattroy/Documents/Projects/pastrypirates && test -f .planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-E-MEASUREMENTS.md && test -f .planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-after.json && test "$(ls .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/e-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 6 && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^4/' | grep -v '^\.planning/' | grep -v '^docs/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && test -z "$(git diff --name-only origin/main..HEAD | grep -E '^(CNAME|robots\.txt|sitemap\.xml)$')" && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt opens playpastrypirates.com/4, sees the new stamp, and plays. Nothing pops up and vanishes. The storm speaks once. The black market shows up. Short lines stop dragging. The petals look tappable, the Dock petal reads as an ingredient plus a coin, the wind pill is there from the start, and the crate lands when he clicks Buy.</human-check>
  </verify>
  <done>
    Group E is live under a fresh `PP4_STAMP`. Whole voyages were played in two tabs, solo and
    pass-and-play, with both sides screenshotted and compared. Mouse QA ran at 1400x900. Every named
    `4/` gate passes, the parity gate shows only its three documented reds, and the health check was
    run with its findings surfaced in the reply. The diff touched only `4/`, `.planning/` and
    `docs/`, and no site-identity file. `main` and `origin/main` are zero apart both ways.
    `02.2-E-MEASUREMENTS.md` holds every before/after number with its instrument named. PAR-10 and
    PAR-02 carry dated corrections. Wyatt has a message that names the stamp, the size, and what is
    left. No browser and no server survives.
  </done>
  <reversibility rating="reversible">A stamp bump and documentation; the code changes it publishes are each individually revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host's narration payload -> Firebase `rooms/<room>/narr` -> every guest's render | Task 2 changes the shape of what crosses here; a guest renders whatever arrives |
| Repo `main` -> GitHub Pages -> real players on the live root game | A push publishes immediately; there is no build step |
| Engine event stream -> the (not yet recorded) `4/` determinism corpus | Task 4 makes the one engine note change in this plan |
| Local static server + headless Chrome on Wyatt's own machine | Every probe in this plan runs on the laptop he is working at |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-31 | Denial of service | A narration hold deferred until a full-screen veil leaves (Task 2) | critical | mitigate | Every narration hold is awaited by the game loop, and "the game just completely stalled" is this project's own critical class. The deferral is capped, and the cap is derived from the ceremony's existing reveal beat and fallback teardown — never a new typed constant, never unbounded. |
| T-02.2-32 | Tampering | The narration payload's shape, changed on the host and read by every guest (Task 2) | high | mitigate | No new wire field is permitted; the predicate is derived from `wait` and `variants`, both of which already cross. A guest running an older tab must degrade to today's behaviour, not to a blank panel. Verified by the two-tab pass and by the parity gate showing no fourth red. |
| T-02.2-33 | Repudiation | An engine outcome recorder change with no determinism corpus to protect it (Task 4) | high | mitigate | Disclosed in the commit message and the summary, on the same basis plan 02.2-04 recorded, and **that basis is re-confirmed before committing** — the moment a `4/` corpus exists this change needs a gated re-record instead. |
| T-02.2-34 | Denial of service | A duplicated render call firing the crate cue twice (Task 6) | medium | mitigate | The call is MOVED, never added; the verify counts occurrences in the function and fails on more than one, and the measured cue count must be exactly 1. |
| T-02.2-35 | Information disclosure | Site-identity files leaving this repo | critical | mitigate | `CNAME` is a *claim* on the live domain; a second copy takes the real game down for real players. The drop's verify greps the pushed diff for `CNAME`, `robots.txt` and `sitemap.xml` and fails on any of them. |
| T-02.2-36 | Denial of service | Headless Chrome and local servers left running on Wyatt's laptop | high | mitigate | Bounded loops only; kills scoped to this plan's own ports (a blanket kill has already destroyed another agent's probes); asserted at the end of Task 1 **and** Task 8, not only at the end of the plan. |
| T-02.2-37 | Tampering | Editing the wrong tree — the root game shares `4/`'s internal layout | high | mitigate | Absolute paths only; after each batch of edits, `git diff --name-only` is filtered to prove nothing outside `4/`, `.planning/` and `docs/` moved. A mis-rooted edit passes every ordinary safety signal. |
| T-02.2-38 | Spoofing | An instrument that reports a pass it cannot fail | high | mitigate | Rule 6. Every sampler in Task 1 must be seen red before it is believed, and the red-proof is recorded in `e-before.json` and gated by that task's verify. Three checks measured the wrong thing this month. |
| T-02.2-39 | Denial of service | The recipe modal's print/PDF path under a sticky header and a full-bleed gradient (Task 7) | medium | mitigate | Download PDF is one of the two icons option C pins into the title row, and a print stylesheet already reflows this modal and hides its button row. Sticky positioning and full-bleed backgrounds are both common print breakers. The task prints it once and looks at the page. |
| T-02.2-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan. Nothing is added to any manifest. |
</threat_model>

<verification>
- **Every claim in this plan is a before number and an after number, on host AND guest**, produced
  by an instrument that was shown to fail first. Nothing is reported as fixed on a single
  observation, and an unmeasured observation is never placed beside a measured one.
- **Case B (the crow's-nest "battle's brewing" call) was code-derived, not driven, in the original
  trace.** It is either driven here or it stays labelled code-derived. It does not get upgraded by
  being adjacent to three cases that were measured.
- The three documented pre-existing parity reds — `PARITY-SAILRECT`, `PARITY-SWEEPARRIVE`,
  `PARITY-ORCH:localAsk` — are the whole allowance. A fourth means a display path re-forked.
- `3a80839`'s wait-line retirement still works after Task 2. It fixed a defect Wyatt reported and
  trading one of his bugs for another is not a fix.
- Item 11's `pendingReveal` gate is untouched. It was exonerated twice at 0 violations.
- The D-10 long-line ceiling is unchanged by Task 3. It is his approved result.
- Rule 13 holds after Tasks 4 and 6: who empties a shelf cannot change whether the table is told,
  and a human's crate lands at the same moment a bot's does.
- Rule 23 holds after Tasks 2 and 4: nothing decides what is *drawn* on the strength of who is
  hosting. Where two paths remain two, the summary names the seam and the reason — never leaves it
  looking converged.
- D-35's two changes are both measured, not eyeballed: the gradient meets the title separator within
  1px at 390 and at 1400, and the image-to-description gap is at least one description line-height
  and expressed in the card's own type-relative unit. **The gradient reaches UPWARD to the
  separator** — it sits at the top of the image and the title is above it. A criterion written the
  other way round (gradient bottom meeting separator top) would put the gradient above the title
  and contradicts his own words; his notes are the design contract (D-23).
- The recipe card's rule-8 sweep states plainly whether the modal and the draft-picker card share a
  component. Read at planning: they do NOT — two builders, two class families. Confirm, then say so.
- No item is reduced to a placeholder, a "for now", or a simplified version. These are nine things
  Wyatt watched happen; each one either works or is reported as not reproduced.
- `PP4_STAMP` is bumped exactly once, to the letter after whatever is actually live.
- No Chrome and no local server survives this plan.

## Requirement mapping, stated honestly

This plan's items come from **a new list** — Wyatt's afternoon solo playtest of build `t` — and
`REQUIREMENTS.md` has no requirement minted for it. Rather than stretch an unrelated id, the four
claimed are the ones these items genuinely touch:

- **PAR-10** — *"no popup appears until the camera and the ships have stopped moving."* Ticked
  complete on plan 03's measurement of the prompt panel; the trace has since proven a **different
  element** breaks the same promise. Tasks 2 makes the row's own words true. Correct the row.
- **PAR-14** — *"there is one place that decides what the game draws… the honest answer is
  nothing."* Open and partial. Tasks 2 and 4 are both instances of it, and the storm/black-market
  debug document cites rule 23 by name.
- **PAR-08** — *"a long narration line holds long enough to read."* Task 3 (D-34) refines the same
  clause, keeping the ceiling that row already earned.
- **PAR-02** — *"a guest's wind pill… from the first event of the voyage onward."* **Reopened at the
  02.1 gate and carried into this phase**; Task 5 is exactly its words.

**Items 1, 4 and 9 (the petal pulse, the Dock label, the crate cue) map to no PAR requirement, and
neither does the recipe card (Task 7).** They are covered by D-32, D-33, D-35 and `docs/AUDIO.md`
instead. **The recipe card is the one item here that belongs to a requirement that exists but is
not this plan's** — PAR-13 (Group D) promises Wyatt *picked from sketches before either was built*,
and he has now picked; **PAR-13 stays with plan 02.2-06, which owns that row.** Task 8's hand-off
says the card shipped from Group E so the record is not confusing. **Recommend minting one new PAR for
Group E at verification** rather than back-filling them into a row written for something else.
</verification>

<success_criteria>
Wyatt plays tonight's build and the nine things he wrote down this afternoon are not there any more.
Boxes stop flashing and vanishing. The storm says one sentence. The black market turns up. Short
lines stop dragging. The buttons look like they want tapping, the Dock petal tells him what it
sells and does not promise a number the coin has not decided yet, the wind pill is there from the
first frame, and the crate lands when he clicks Buy. The recipe card is the one he picked, with the
gradient running clean up to the title rule and the image no longer squeezed against the words.

And every one of those claims has a number behind it, taken before and after, on both screens, by
an instrument that was made to fail first.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-07-SUMMARY.md` when done.

Also produced by this plan:
- `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-E-MEASUREMENTS.md` — the
  before/after table, every row naming its instrument.
- `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/e-before.json`, `e-after.json`
- `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/e-*.png`
- Recipe-card screenshots at 390 and 1400 plus the printed page, under the same `shots/e-*.png`.
- `4/scripts/narration_timeline.mjs` — kept in the repo, like `mouse_qa.mjs`, with its usage line in
  the summary so the next session does not rebuild it.
- Dated corrections on **PAR-10** and **PAR-02** in `.planning/REQUIREMENTS.md`.
- Status updates on `.planning/debug/tails-narration-vanishes.md` and
  `.planning/debug/storm-summary-and-black-market.md` — both move off `investigating`/`diagnosed`
  with `files_changed` filled in.
</output>
