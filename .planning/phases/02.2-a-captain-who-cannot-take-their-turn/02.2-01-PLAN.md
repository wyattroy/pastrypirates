---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 4/src/ui/audio.js
  - 4/src/ui/stage.js
  - 4/src/ui/flow.js
  - 4/src/ui/board.js
  - 4/scripts/audio_map_check.js
  - notes/sketches/08-end-of-voyage-card/
  - notes/sketches/09-recipe-card/
autonomous: true
requirements: [PAR-08, PAR-13]
group: Q
drop: "Group Q — the one-line wins"
items: [13, 10, 20, 15, 1, 14, 7, 8-sketch, 9-sketch]

must_haves:
  truths:
    - "Anchoring a ship plays the fishing sound, once, at a level that sits under the sword clash — not the 8-second storm bed."
    - "fishing.mp3, downloaded every game and never heard, is heard."
    - "The loudest sound in the game is the out-of-time sound and the quietest is the victory sound (docs/AUDIO.md's six numbers, per D-09)."
    - "A long narration line stays on screen about two seconds longer than it did — the ceiling only, per D-10."
    - "The Attack button reads a minus and a coin with no parentheses around them; the Pass button reads a plus and a coin with no parentheses around them — on the host's screen, the guest's screen, and the recipe-draft screen alike."
    - "The end-of-voyage stat row is labelled Bakeries and, in a game where three captains got home, reads three bakers home (D-22)."
    - "Wyatt can open two or three sketches each for the end-of-voyage card and the recipe card from a link, on his phone, and pick one whenever he has a minute (D-14)."
    - "Whether the director chases your own boat during another captain's turn has been OBSERVED or NOT-OBSERVED in a two-tab crew game, with a screenshot, before one line was changed for it (D-11, CLAUDE.md rule 6)."
    - "Wyatt is told a new PP4_STAMP and can see it live at playpastrypirates.com/4 (D-02)."
  artifacts:
    - 4/src/ui/audio.js
    - 4/src/ui/stage.js
    - 4/src/ui/flow.js
    - 4/src/ui/board.js
    - 4/scripts/audio_map_check.js
    - notes/sketches/08-end-of-voyage-card/*.html
    - notes/sketches/09-recipe-card/*.html
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/q-*.png
  key_links:
    - "audio.js's single SFX event map -> soundForEvent() -> the master bus: one key per event, and SFX_VOLUME is the one tuning point (docs/AUDIO.md)."
    - "flow.js's label template literals -> optionButtonsHTML() (4/src/ui/util.js:1481) -> host prompt, guest prompt AND draft channel: one edit reaches all three (02.1-03)."
    - "stage.js:578's hold clamp -> S.bubDue -> the tick loop at :1506/:1531: the ceiling is the only thing that moves."
    - "PP4_STAMP in 4/src/ui/stage.js:32 -> the stamp line the player reads at stage.js:985 -> main -> playpastrypirates.com/4."
---

<objective>
**Group Q — the one-line wins, and the tracer for this whole phase.**

This is the first of six drops (D-01: Q -> A -> B -> C -> C' -> D, Wyatt's order, unchanged
underneath Group Q). It carries items **13, 10, 1, 14, 7** and the sketches for **8** and **9**, and
it either lands items **20/15** or hands them to Group A with a written reason (D-11, D-08).

**It is the tracer, so it is production-quality and it goes all the way.** Real fixes, the `4/`
gates run by name, a real two-tab crew game with both sides screenshotted (D-03), a real `PP4_STAMP`
bump (D-02), and a real push that reaches Wyatt's phone. Nothing here is a scaffold or a spike. When
this plan is done Wyatt has a visibly and audibly better game in his hands, and the drop loop that
plans 02-06 all reuse has been proven once, end to end.

**Two CONFIRMED one-liners go FIRST** — item 13's deleted line and item 10's number — so the drop
has value banked before anything uncertain is touched.

Purpose: the fastest possible improvement a player notices, plus proof that the drop pipeline works.
Output: a live build with better sound, longer-held narration, cleaner button labels, an honest
end-of-voyage stat, and two sets of sketches waiting for Wyatt's pick.

**No UI-SPEC governs this phase — Wyatt's notes are the design contract (D-23).** His 22 numbered
items with screenshots are more specific than any generated spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-RESEARCH.md
@.claude/CLAUDE.md
</context>

<absolute_paths>
Every path in this plan is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`.
**The game under development is `4/`. The repo root is a DIFFERENT, older game that real players are
on.** More than one tree here shares an identical internal layout, so a relative path resolves in
both — a mis-rooted edit opens a real file, applies cleanly, passes `node --check`, and modifies the
wrong copy, with every safety signal reporting success. **Never touch a root-level `src/` path in
this phase.**
</absolute_paths>

<tasks>

<task type="tracer">
  <name>Task 1: The two confirmed one-liners — the anchor sound and the narration hold</name>
  <files>4/src/ui/audio.js, 4/src/ui/stage.js, 4/scripts/audio_map_check.js</files>
  <read_first>
    `docs/AUDIO.md` — **mandatory before touching `4/src/ui/audio.js`** (CLAUDE.md §4, D-09). It names
    the three live defects, confirms one deleted line fixes two of them, and carries the six measured
    volume numbers in its DEFECT-3 table. **Do not invent the numbers** (D-09, Claude's Discretion in
    CONTEXT explicitly says take them from the doc).
  </read_first>
  <action>
    **Item 13 (D-09).** `4/src/ui/audio.js` maps `anchorHold` twice inside the same object literal —
    at line 94 (paired with `fishing`) and again at line 119 (paired with `storm`). The second
    silently wins, which is why `fishing.mp3` is downloaded every game and never heard, and why one
    anchoring ship dumps the 8-second storm bed on the master bus at roughly three times the level
    the storm is mixed to sit. Delete the later of the two mappings so the earlier one stands. Line
    103 mentions the same key inside a comment and is documentation, not a mapping — leave it.

    Then set the six `SFX_VOLUME` entries at `4/src/ui/audio.js:39-46`, all still the untouched
    default of `1`, to the six measured values in `docs/AUDIO.md`'s DEFECT-3 table. Read them from
    the doc; do not retype them from memory. The result Wyatt should hear is that the loudest sound
    in the game becomes the out-of-time sound and the quietest becomes the victory sound, instead of
    a 15.6 dB spread with those two at the wrong ends.

    Then create `4/scripts/audio_map_check.js`, a small gate in the shape of the other `4/scripts/*`
    checks (plain Node, exit 0 or 1, no test library). `docs/AUDIO.md` asks for exactly this and says
    why: the existing check "never mentions `anchorHold` or `fishing` at all, and nothing anywhere
    checks the literal for duplicate keys. So the green tick is not evidence: this check cannot fail
    on this defect." It asserts two things: (a) no event key appears twice in the SFX event map, and
    (b) every stem named in `SFX_VOLUME` has a value other than the default `1`. **Write it red
    first** — confirm it exits 1 against the pre-fix file (`git stash` the audio change, run it, see
    it fail, restore) before believing it passing. A check that cannot fail is not protection.

    **Item 10 (D-10).** At `4/src/ui/stage.js:578` the narration hold is clamped by
    `Math.max(2550, Math.min(6750, ...))`. Wyatt asked for **+30% on the maximum only**: change
    `6750` to `8775`. **Do not touch the `2550` floor and do not touch the `* 1.5` multiplier.** Both
    are a deliberate, Wyatt-approved prior fix from playtest 11, and the comment two lines above says
    so in his own words — CLAUDE.md rule 10, and the graveyard section of the research confirms it.
  </action>
  <verify>
    <automated>node 4/scripts/audio_map_check.js && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/audio.js","utf8").split("\n").filter(l=>!/^\s*\/\//.test(l)).filter(l=>/anchorHold\s*:/.test(l)); if(s.length!==1) throw new Error("anchorHold is mapped "+s.length+" times outside comments; expected 1"); if(!/fishing/.test(s[0])) throw new Error("the surviving anchorHold mapping is not the fishing one"); console.log("audio map OK")' && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/stage.js","utf8"); if(!/Math\.max\(2550,\s*Math\.min\(8775,/.test(s)) throw new Error("the narration ceiling is not 8775 with the 2550 floor intact"); console.log("hold clamp OK")'</automated>
    <human-check>In a solo game: anchor a ship and hear the fishing sound rather than eight seconds of weather. Read a long narration line and notice it holds about two seconds longer than it did.</human-check>
  </verify>
  <done>
    The anchor sound plays once at a sensible level and the fishing sound is heard for the first
    time; one anchoring ship no longer fires the storm bed. A long narration line holds about two
    seconds longer. The floor and the multiplier are untouched. A new gate fails red against the old
    file and passes green against the new one.
  </done>
  <reversibility rating="reversible">Three small edits to two files plus one new gate script; each revertible in isolation.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The words a player reads — button labels (items 1, 14-parens) and the Bakeries stat (item 7)</name>
  <files>4/src/ui/flow.js, 4/src/ui/board.js</files>
  <action>
    **Items 1 and 14's parenthesis half (D-13).** Two label template literals in
    `4/src/ui/flow.js` wrap a coin amount in parentheses inside a `nobrk` span: the Attack label at
    line 1766 (`(−${...powder}🌕)`) and the Pass label at line 1832 (`(+${...passCoin}🌕)`). Drop the
    parentheses from both, keeping the `nobrk` span, the sign, the coin, and the conditional that
    hides the annotation entirely when the amount is zero. The sign characters differ deliberately —
    Attack uses U+2212 MINUS SIGN, Pass uses an ASCII plus, and the comment block above the Pass
    label explains why. **Preserve both characters exactly as they are.**

    **The consistency sweep is part of this task, not a note (CLAUDE.md rule 8).** Both labels flow
    through `optionButtonsHTML()` at `4/src/ui/util.js:1481`, which 02.1-03 established as the single
    shared builder for every prompt button row. Confirm by reading that one edit reaches all three
    surfaces — the **host prompt** (`localAsk`), the **guest prompt** (`watchPrompt`) and the **draft
    channel** (`watchDraftPrompt`) — and **name those three surfaces in the summary** as the ones
    checked. If any of the three turns out to build a label independently, that is a finding and it
    gets fixed here too.

    **Item 7 (D-22).** `4/src/ui/board.js:1935` prints the end-of-voyage stat row as `Bakery` with
    `finishOrder.length` behind it. Research settled that `finishOrder` is **not** miscounting — it
    correctly counts captains who *completed* a bake (`4/src/engine/index.js:2857-2862`), so
    "one baker home" was accurate about a different quantity than the word promised. **D-12's
    measure-before-fixing is discharged: this is not a scoring bug.** Wyatt's pick (D-22) is to count
    the captains who **reached Tortuga and started a bakery** — a new quantity the game does not
    track today. Compute it at render time from the per-player flags already carried in every event's
    state snapshot: a captain counts when they are baking or done. Label the row **Bakeries**, and
    make the value read **"3 bakers home"** for three, keeping the existing singular wording for one.
    This is UI-tier only and touches no engine field.
  </action>
  <verify>
    <automated>node -e 'const L=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/flow.js","utf8").split("\n").filter(l=>/label:`(⚔️ Attack|🌊 Pass)/.test(l)); if(L.length!==2) throw new Error("expected 2 coin-bearing label lines, found "+L.length); const bad=L.filter(l=>/\(\s*[−+]/.test(l)); if(bad.length) throw new Error("a coin amount is still parenthesized: "+bad.join(" | ")); if(!L.some(l=>l.includes("−"))||!L.some(l=>l.includes("+"))) throw new Error("a sign character was lost"); console.log("labels OK")' && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/board.js","utf8"); if(!/>Bakeries</.test(s)) throw new Error("the stat row is not labelled Bakeries"); if(/finishOrder\.length>1\?appState\.game\.finishOrder\.length\+" bakers, side by side"/.test(s)) throw new Error("the stat still reads finishOrder.length"); console.log("bakeries OK")' && node 4/scripts/no_undef_check.js && node 4/scripts/prompt_field_parity_check.js</automated>
    <human-check>Open a prompt with a greyed Attack and a Pass: both read their coin amount with no brackets round it. At the end of a voyage with three captains home, the stat row says Bakeries — 3 bakers home.</human-check>
  </verify>
  <done>
    The Attack and Pass buttons show their coin amount without parentheses, identically on the host's
    screen, the guest's screen and the draft channel — and the summary names those three surfaces as
    checked. The end-of-voyage row is labelled Bakeries and counts the captains who got home and
    fired their ovens, which in Wyatt's game is three.
  </done>
</task>

<task type="auto">
  <name>Task 3: REPRODUCE FIRST — the director chasing your own boat (items 20, 15) and where Pass sits in the fan (item 14)</name>
  <files>4/scripts/mp_rig.mjs (used, not modified), .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/</files>
  <read_first>
    `docs/DRIVING-THE-GAME.md` — **mandatory before touching a browser, not after getting stuck**
    (CLAUDE.md §4, rule 19). §3 solo, §4 the turn loop and the two things that stall every naive
    driver, §5b the autoplay driver, §5c driving a guest while a human hosts, §5d arming a watcher
    for a window too narrow to hand-drive, §5e injecting state — **allowed in solo, FORBIDDEN in
    multiplayer**. Note §4a: the flippenator coin `#flipCoinWrap` **is** the flip button and is not
    an `.apBtn`; that has stalled three separate attempts. `4/scripts/mp_rig.mjs` already exists and
    is the reusable two-window rig — a real host and a real guest, two independent Chromes, a real
    Firebase room, with the corrected visibility predicate and the fixed autoplay driver baked in,
    and a `killAll()` export. **Use it. Do not build a new rig** (CLAUDE.md rule 7).
  </read_first>
  <action>
    **Nothing is fixed in this task. It measures, and it blocks Task 4** (CLAUDE.md rule 6 — never
    report a defect as confirmed before you have measured it; this exact failure cost the project two
    days when four of five reported defects turned out not to exist).

    Run ONE bounded two-tab crew game with `4/scripts/mp_rig.mjs` and record three observations, each
    as OBSERVED-with-screenshot or NOT-OBSERVED-with-screenshot. Override the rig's `SHOTS` constant
    so screenshots land in
    `/Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/`
    with a `q-` prefix, so they survive the session and Wyatt can look at them.

    **(a) Items 20 and 15 — the director chasing your own boat (D-11, D-08).** CONTEXT's leading
    theory was `4/src/ui/stage.js:1220`, which re-aims with `camToSeat(appState.mySeat ?? 0)` — *my*
    ship — when the prompt's own anchor falls outside the safe band. **Research refined and partly
    demolished that theory:** that block only runs when the CURRENT browser owns the prompt, so it
    does **not** explain a host spectating an ordinary guest turn. **Do not go looking for it at
    :1220. Let the reproduction name the real site.** Watch the host's camera through a full guest
    turn and the guest's camera through a full host turn; screenshot both sides at the moment the
    camera settles. If the camera lands on the viewer's own boat rather than on the captain whose
    turn it is, that is the fault, and the next step is to record which code path aimed it — read
    what actually ran, do not assume.

    **(b) Item 14's ordering half — CANDIDATE.** Pass is already last in the `opts` array
    (`4/src/ui/flow.js:1832`, pushed after `moveInstead` at `:1812`), which is bottom-of-list for a
    flat card. A radial fan is not vertical, so "last in the array" does not necessarily mean
    "geometrically bottom-most". Screenshot a radial prompt that contains Pass and record where Pass
    actually sits.

    **(c) Free observation — Group E's watching brief (D-21).** A guest whose browser was *killed*
    mid-turn is reported to come back to a battle prompt that draws nothing. **One observation, three
    failed reproductions.** Do NOT mount a dedicated hunt — that has already failed three times. If a
    battle happens to occur in this session, note whether it drew. Nothing more.

    **Bound everything and tear it down inside this task** (CLAUDE.md §3). Every probe loop is
    `for (let i = 0; i < N; i++)`, never `while (true)`. Call the rig's `killAll()` and then
    `pkill -f remote-debugging-port; pkill -f http.server` **before writing the reply**, not when the
    plan ends. Wyatt once found two abandoned probes at 21% CPU each on the laptop he was reporting
    as overheating, and 53% CPU across 13 Chromes hours after that rule was written down.
  </action>
  <verify>
    <automated>test -d /Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots && test "$(ls /Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/q-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 4 && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Four or more screenshots exist, from both sides of a real crew game, and each of the three observations is written down as OBSERVED or NOT-OBSERVED with the screenshot that shows it.</human-check>
  </verify>
  <done>
    A two-tab crew game was played and both sides screenshotted. For each of items 20/15 and item
    14's ordering, the finding reads either "OBSERVED — here is the screenshot and here is the code
    path that aimed it" or "NOT OBSERVED in N attempts". No claim is stated as confirmed that was not
    seen. Every Chrome and every local server this task started is dead before the reply is written.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix only what Task 3 observed — and hand the rest to Group A in writing</name>
  <files>4/src/ui/stage.js, 4/src/ui/flow.js</files>
  <precondition>Task 3 has completed and its findings for items 20/15 and item 14's ordering are written down as OBSERVED or NOT-OBSERVED with screenshots. Do not start this task from a theory.</precondition>
  <action>
    **This task is conditional by design, and both outcomes are success (D-04, D-11).**

    **If Task 3 OBSERVED the director chasing the viewer's own boat:** fix it at the site Task 3
    named. The correct behaviour already exists in the same file — `4/src/ui/stage.js:574` uses
    `camToSeat(subj)` on the narration path, framing the seat a moment is genuinely *about*, and
    `camFitSeats()` at `:109` frames every captain a question is about when there is more than one.
    Aim the director at the captain whose turn it is, by the same rule the solo game already uses
    (item 20 in Wyatt's own words: *"the director follows the same exact rules as in the solo game —
    it shows active players"*). Prefer reusing the existing helper over adding a new one.

    **If Task 3 did NOT observe it:** do not change a line. Move items 20 and 15 into Plan 02 (Group
    A), where item 15 already belongs (D-08), write into the summary that Q could not reproduce them
    and exactly what was tried, and say so in the message Wyatt reads. **That is D-04 and rule 6
    working as intended, not a failure.** A requirement marked failed on a bad measurement rots
    exactly like one marked complete on a bad one.

    **If Task 3 OBSERVED Pass sitting somewhere other than the bottom of the radial fan:** move it,
    respecting the narration box's top-to-bottom DOM reveal order (CLAUDE.md rule 11:
    `.apBack` -> `.apMsg` -> `.apBtns` -> `.apSub`, built by `localAsk()`), and sweep the same three
    surfaces named in Task 2. **If Pass is already at the bottom, item 14's ordering half is already
    satisfied** — record that and change nothing.

    **Nothing is a constant (CLAUDE.md rule 9).** If this fix wants a threshold — a settle time, a
    margin, a band — derive it from something the game already computes rather than typing a number.
    And before changing how any existing quantity is produced, list what reads it, gates included:
    replacing a constant with a calculation can leave a check unable to fail, which still reads as
    protection.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/prompt_field_parity_check.js</automated>
    <human-check>If the camera fault was observed and fixed: in a two-tab game the director now frames the captain whose turn it is, on both sides, and the screenshot pair shows it. If it was not observed: the summary says so plainly and names Plan 02 as its new home.</human-check>
  </verify>
  <done>
    Either the director now follows the active captain on both sides and a before/after screenshot
    pair proves it, or items 20 and 15 are handed to Plan 02 with the reproduction attempts written
    down. Item 14's ordering half is either fixed or recorded as already satisfied. No line was
    changed for a fault nobody saw.
  </done>
  <reversibility rating="reversible">A camera-aim change in one render-tier call; the engine and its RNG are never touched (stage.js's own header contract).</reversibility>
</task>

<task type="auto">
  <name>Task 5: Sketch items 8 and 9 — two or three throwaway options each, early, so Wyatt can pick whenever</name>
  <files>notes/sketches/08-end-of-voyage-card/, notes/sketches/09-recipe-card/</files>
  <read_first>
    `docs/BOARD-RENDERING.md` — the board is drawn in five layers. **An HTML overlay mapped to board
    coordinates must be added to `CAM_HTML_LAYERS` or it detaches the moment the director zooms, and
    anything that animates continuously must be HTML, not SVG — Chrome cannot composite an SVG
    transform animation at all** (the same animation measured ~62 layouts/sec as SVG and zero as
    HTML). These sketches are throwaway standalone pages and do not live on the board, so neither
    trap bites here — but whichever sketch Wyatt picks gets built into the real screens in Plan 06,
    and it will.
  </read_first>
  <action>
    **D-14, Wyatt's pick: sketch early, build in Group D.** These ship with THIS drop so he can
    choose whenever he has a minute, and Group D is built against a choice already made. Nothing in
    the phase waits on him.

    Write **2-3 throwaway standalone HTML files each** into `notes/sketches/08-end-of-voyage-card/`
    and `notes/sketches/09-recipe-card/`. Standalone means one self-contained file per option, no
    imports from `4/src`, no build step — he opens it and looks at it. Put them under `notes/` rather
    than a dot-directory because he browses in Finder, where dot-folders are hidden, and because
    `notes/` is served from the repo root so **each sketch has a real URL he can open on his phone**.

    **Item 8 — the end-of-voyage card.** His constraint: the card must be **collapsible (or
    hold-the-sea)** so the final board can be seen behind it. His words: *"Be a UX designer and
    suggest a solution."* Take that seriously — these are three different answers to the problem, not
    three colourways of one answer. Hold-the-sea is the game's existing gesture for exactly this
    (press and hold fades every floating box), so one option should use it; the others should be
    genuinely different mechanisms. Whichever options use an existing gesture must behave the way
    that gesture behaves everywhere else (CLAUDE.md rule 8, consistency).

    **Item 9 — the recipe card.** His constraints: the gradient must **reach the box edges** and
    **darken, not lighten**; the **Download PDF and Email buttons must clearly sit above** the
    scrolling recipe. His words: *"You may redesign the buttons, place them elsewhere, and lay out
    the recipe itself differently if you want."* So the button treatment and the recipe layout are
    open; the gradient direction and the button position are not.

    Add one short `README.md` in each folder — plain English, no jargon (CLAUDE.md rule 3) — saying
    what each option does differently and what it costs, one line each, so he can choose from the
    text without opening all six.

    **QA your own sketches before handing them over** (CLAUDE.md rule 19): open each one in a browser
    and screenshot it. Every fault Wyatt found in twenty minutes at the 02.1 gate was visible in the
    first screenshot nobody took. Kill the browser and any server before replying.
  </action>
  <verify>
    <automated>test "$(ls /Users/wyattroy/Documents/Projects/pastrypirates/notes/sketches/08-end-of-voyage-card/*.html 2>/dev/null | wc -l | tr -d ' ')" -ge 2 && test "$(ls /Users/wyattroy/Documents/Projects/pastrypirates/notes/sketches/09-recipe-card/*.html 2>/dev/null | wc -l | tr -d ' ')" -ge 2 && test -f /Users/wyattroy/Documents/Projects/pastrypirates/notes/sketches/08-end-of-voyage-card/README.md && test -f /Users/wyattroy/Documents/Projects/pastrypirates/notes/sketches/09-recipe-card/README.md && for f in /Users/wyattroy/Documents/Projects/pastrypirates/notes/sketches/*/*.html; do grep -q "</html>" "$f" || exit 1; done && test -z "$(pgrep -f remote-debugging-port)"</automated>
    <human-check>Each sketch opens standalone in a browser and looks like a finished idea, not a wireframe. Item 9's gradient reaches the edges and darkens. Item 8's card can be got out of the way to see the board behind it.</human-check>
  </verify>
  <done>
    Two or three options each for the end-of-voyage card and the recipe card exist as standalone HTML
    files under `notes/sketches/`, each screenshotted, each with a one-line plain-English description
    of what it does differently — and each reachable from a URL on Wyatt's phone.
  </done>
</task>

<task type="auto">
  <name>Task 6: The Group Q drop — two tabs, both sides, stamp bumped once, live</name>
  <files>4/src/ui/stage.js</files>
  <read_first>
    `docs/GIT-AND-DEPLOY.md` §5 — the drop loop. `docs/DRIVING-THE-GAME.md` §9: never verify against
    production; §1: use a port never loaded before in this session, because Chrome caches ES modules
    per URL.
  </read_first>
  <action>
    **D-02: one build drop per group, and the stamp is bumped exactly once per drop.** D-03: a
    two-tab crew game with BOTH sides screenshotted before any group is handed over.

    1. **Play the game** (CLAUDE.md rule 19). Run one more two-tab crew game with
       `4/scripts/mp_rig.mjs` against a **local** server on a fresh port, and screenshot **both**
       sides. Four of the seven faults at the 02.1 gate were host/guest divergences, which a
       single-browser probe cannot see by construction. Look at the pictures, not at a state dump —
       the checks that blessed that build measured game state honestly and never once looked at what
       was drawn. Confirm by eye: no brackets round the coin amounts, the anchor sound behaves, a
       long line holds longer.
    2. **Run the `4/` gates by name.** A green root `npm test` proves nothing about this phase — its
       21 gates scan only the older game and not one of them loads `4/`. The commands are in the
       verify block below.
    3. **Bump `PP4_STAMP`** at `4/src/ui/stage.js:32` from `2026-08-19c` to a fresh date stamp. Once.
    4. **Prove the diff touches only what it should**: `4/`, `.planning/`, and `notes/sketches/`.
       Nothing else, and above all **never `CNAME`, `robots.txt` or `sitemap.xml`** — GitHub Pages
       reads `CNAME` as a *claim* on the live domain, and a second copy takes the real game down for
       real players. Two separate sessions came within one command of doing this.
    5. **Push, pull, verify zero both ways.** Note at planning time local `main` was **4 commits
       ahead** of `origin/main` (the 02.2 planning docs) — `git fetch origin` first and expect to
       carry them.
    6. **Tell Wyatt the stamp**, in plain English, in the message he reads (CLAUDE.md rule 3), with
       **the size stated**: what a player experiences differently, how much of his 22-item list this
       covers, and what it leaves undone. **Include the sketch URLs in that same message** — if you
       ask him to look at something, the link goes in the same answer, never scrolled-up-for.
       The tell that a session skipped this step is that he reports an old build stamp. **It is never
       a cache — there is no build step.** If he cannot see it, it is not on `main`.
    7. Kill every Chrome and server before replying.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/pass_narration_test.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/audio_map_check.js && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^4/' | grep -v '^\.planning/' | grep -v '^notes/sketches/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && test -z "$(grep -n 'PP4_STAMP = "2026-08-19c"' 4/src/ui/stage.js)" && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt opens playpastrypirates.com/4 and the build stamp in the corner reads the new date. He has the sketch links in the same message.</human-check>
  </verify>
  <done>
    Group Q is live at `playpastrypirates.com/4` under a fresh `PP4_STAMP`; the eight named `4/` gates
    pass; both sides of a real crew game are screenshotted; the diff touched only `4/`, `.planning/`
    and `notes/sketches/`; `main` and `origin/main` are zero apart in both directions; and Wyatt has
    a plain-English message naming the stamp, the size of what landed, and the sketch links.
  </done>
  <reversibility rating="reversible">A push to `main` is how Wyatt playtests, not a release — the live root game is different files and is untouched. Revertible by a normal revert commit and a re-push.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest browser -> Firebase room -> every other browser | Narration HTML, prompt labels and seat names cross here; every client renders what it receives |
| Repo `main` -> GitHub Pages -> real players on the live root game | A push publishes immediately; there is no build step and no staging |
| `notes/sketches/*.html` -> public web | Anything under the repo root is served publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-01 | Tampering | Site-identity files (`CNAME`, `robots.txt`, `sitemap.xml`) | critical | mitigate | Task 6's diff gate rejects any changed path outside `4/`, `.planning/` and `notes/sketches/`. Never hand-roll a sync; `scripts/deploy-preview.sh` only. Two prior sessions came one command from taking the live domain down. |
| T-02.2-02 | Information disclosure | `notes/sketches/*.html` served publicly from the repo root | low | accept | Throwaway design mockups containing no secrets, no keys and no player data. Public exposure is the point — Wyatt opens them from his phone. |
| T-02.2-03 | Tampering | Prompt label template literals -> `optionButtonsHTML()` -> innerHTML | low | mitigate | Labels are static author-written template literals interpolating only numeric config values (`cfg.powder`, `cfg.passCoin`). No user-supplied text enters this path in this plan. Task 2 changes punctuation only and keeps the existing `nobrk` span structure. |
| T-02.2-04 | Denial of service | Headless Chrome and `http.server` processes left running | medium | mitigate | Every probe loop bounded; `killAll()` plus `pkill` inside the same task that starts one; asserted in Tasks 3, 5 and 6's automated verify. Wyatt has twice found abandoned probes heating the laptop he was debugging a performance problem on. |
| T-02.2-SC | Tampering | Package installs | n/a | accept | **No package installs in this phase.** It is a bug-fix phase over the existing vanilla-JS `4/` tree with no build step and no new dependencies (RESEARCH: Package Legitimacy Audit — Not applicable). No legitimacy checkpoint is required. |
</threat_model>

<verification>
- The eight named `4/` gates pass by name — `no_undef_check`, `stage_import_check`, `pass_coin_test`,
  `pass_narration_test`, `planner_singleton_check`, `prompt_field_parity_check`, `seat_arg_check`,
  and the new `audio_map_check`. **Root `npm test` is not a check on this phase** and its result is
  not evidence here.
- The new `audio_map_check.js` was proven to fail red against the pre-fix file.
- Both sides of a real two-tab crew game are screenshotted into the phase directory.
- Every reproduction claim is written as OBSERVED or NOT-OBSERVED, never as confirmed-without-measurement.
- `PP4_STAMP` bumped exactly once and confirmed serving from `playpastrypirates.com/4`.
- No Chrome and no local server survives this plan.
</verification>

<success_criteria>
Wyatt opens `playpastrypirates.com/4`, sees the new build stamp, hears the fishing sound when a ship
anchors instead of eight seconds of weather, reads narration that holds long enough to read, sees no
brackets round the coin on the Attack and Pass buttons, and — at the end of a voyage where three
captains got home — reads "Bakeries: 3 bakers home". He has links to two or three sketch options each
for the end-of-voyage card and the recipe card, and can pick whenever he has a minute. Items 20 and
15 are either fixed with a screenshot pair proving it, or handed to Plan 02 with the reproduction
attempts written down.

**This plan does not satisfy the phase gate.** The gate is Wyatt playing a crew game and not seeing
items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or 22. It is his eyes, not a probe.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md` when done.
It must state: the new `PP4_STAMP`; the three surfaces checked for the label sweep; the OBSERVED /
NOT-OBSERVED verdict for items 20/15 and 14's ordering with the screenshot paths; whether 20/15 moved
to Plan 02; and the sketch URLs.
</output>
