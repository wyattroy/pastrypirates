---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 05
type: execute
wave: 5
depends_on: ["02.2-04"]
files_modified:
  - scripts/economy_table.js
  - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-ECONOMY-TABLE.md
autonomous: true
requirements: [PAR-12]
group: "C-prime"
drop: "Group C' — the economy, measured before touched. NO GAME CODE CHANGES."
items: [12]
changes_game_code: false
bumps_stamp: false

must_haves:
  truths:
    - "Wyatt has a table showing purses over time, battle counts and game length at each of the settings he named, from a few hundred games each (D-17)."
    - "The table says, in plain English, whether each setting gets closer to his target: every player feels able to do what they want at least once per game, and no player holds more than about 10 coins."
    - "No economy number in the shipping game has changed. He asked for the numbers, not for the change (D-17)."
    - "No PP4_STAMP was bumped, because nothing a player can see changed."
  artifacts:
    - scripts/economy_table.js
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-ECONOMY-TABLE.md
  key_links:
    - "roundCfg's crateBase field (4/src/engine/index.js:3040, today 6) -> price(left) = max(1, crateBase - left) (4/src/engine/index.js:812-813) -> the 2-5 dubloon dock range Wyatt wants to see as 1-3."
    - "roundCfg's powder field (4/src/engine/index.js:3028, today 2) -> the battle cost Wyatt wants to see at 4."
    - "Game's already-tracked battles/attWins counters and each player's coins -> readable off the finished game object with no new instrumentation and no event-stream change."
---

<objective>
**Group C' — the economy, measured before touched.** The fifth of six drops (D-01), and the one that
is not a build.

**THIS PLAN CHANGES NO GAME CODE. IT PRODUCES A TABLE.** D-17, Wyatt's ruling: *"Change no number
until he picks. He asked for the numbers, not for the change."* His words on the item itself were
just *"Run the numbers."*

Since Pass began paying a dubloon, purses have run away and money means nothing. **His target,
quoted, and the table must be read against it:** *every player feels able to do what they want at
least once per game, and no player holds more than about 10 coins.* His candidate levers are dock
output **1-3** instead of today's 2-5, and battle cost **4** instead of today's 2.

Purpose: he decides with numbers in front of him instead of arguing about it.
Output: one report he can read on his phone, and a headless script that produced it.

**There is no `PP4_STAMP` bump in this plan and that is deliberate.** D-02 asks for one build drop
per group; this group produces no build, because nothing a player can see changed. Bumping a stamp
for an unchanged game would tell Wyatt something false. **The deliverable is the table.**

**The plan ENDS with the table in his hands. There is no task that applies a setting.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-RESEARCH.md
@.claude/CLAUDE.md
</context>

<absolute_paths>
Every path is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`. **The only files this
plan may write are `scripts/economy_table.js` and the report in the phase directory.** Any diff
touching `4/src/` is a failure of this plan's own contract.
</absolute_paths>

<tasks>

<task type="auto">
  <name>Task 1: Extend the offline simulator to report what Wyatt asked for — purses, battles, length</name>
  <files>scripts/economy_table.js</files>
  <read_first>
    `scripts/bot_ladder4.js` — **read its header in full.** It is the existing offline balance
    simulator: it builds a game per run from `roundCfg(STRATS)` with the bake-off on, and reports
    pass rate, voyage length in rounds, and per-seat turn and pass counts **from the already-recorded
    event stream**. The dev seed family is `×7919`; **`×104729` is held out for close calls and must
    stay held out.** Also `docs/BOT-DESIGN-PRINCIPLES.md` principle 10 and `docs/TRADE-SYSTEM.md` —
    mandatory before touching anything that prices a trade (CLAUDE.md §4).

    **Read the graveyard before you touch a defended number** (CLAUDE.md rule 10):
    `git log --all --format="%H %s" -S "crateBase"` and the same for `powder`. The tell that you are
    about to re-run a settled argument is catching yourself reasoning that one number going up is
    acceptable because a different number stayed flat.
  </read_first>
  <action>
    **The existing simulator does not report the three things D-17 asks for.** It reports pass rate,
    voyage length, and turn/pass counts. It does **not** report purses, battle counts, or purses over
    time.

    Write `scripts/economy_table.js` beside it — a headless dev script, no risk to the shipping game
    — that runs the same construction and additionally tallies:

    - **purses over time** — each captain's coins sampled per round, not only at the end, because
      "no player holds more than about 10 coins" is a claim about the *whole voyage*, not the final
      score. Report the distribution: median and worst-case peak purse across the run.
    - **battle counts** — the game object already tracks `battles` and `attWins` (initialised at
      `4/src/engine/index.js:299`). These are plain instance fields readable off the finished game.
    - **game length** — rounds to completion, which the ladder already computes.
    - **"could a captain do what they wanted at least once?"** — the closest measurable proxy for
      his actual target. Suggested: how often a captain was priced out of an action they had a
      reason to take (an unaffordable Attack, an unaffordable crate). **Say in the report which proxy
      you chose and why**, because it is a judgement and he should be able to disagree with it.

    **No new instrumentation and no engine-emission change.** Everything above is already recorded or
    already tracked — the same "already recorded, just not read" pattern the ladder's own header
    describes for turns and passes. **If something genuinely is not readable without touching the
    engine, stop and report that rather than adding an emission**: Group C just spent the cheap
    moment for engine changes and Phase 3's corpus is next.

    Accept `--crateBase` and `--powder` as command-line overrides, spread over `roundCfg()` at
    construction. **Do not edit `roundCfg()` itself** — the overrides live in the script, so the
    shipping config is never touched.

    **Bound it.** No `while (true)`. A few hundred games per setting, as D-17 asks, at the dev seed
    family.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node scripts/economy_table.js 20 7919 --json > /dev/null && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/scripts/economy_table.js","utf8"); if(/while\s*\(\s*true\s*\)/.test(s)) throw new Error("unbounded loop"); if(!/crateBase/.test(s)||!/powder/.test(s)) throw new Error("the two levers are not wired as overrides"); if(/104729/.test(s)) throw new Error("the held-out seed family must stay held out"); console.log("simulator shape OK")' && test -z "$(git diff --name-only -- 4/src)"</automated>
    <human-check>A short run prints purses, battle counts and voyage length, and accepts both levers on the command line.</human-check>
  </verify>
  <done>
    A headless script reports purses over time, battle counts and game length, and takes the dock
    price and battle cost as command-line overrides. Nothing under `4/src/` changed.
  </done>
  <reversibility rating="reversible">A new standalone dev script. It is not loaded by the game and not wired into any gate.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the matrix — a few hundred games at each of his settings</name>
  <files>.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-ECONOMY-TABLE.md</files>
  <precondition>Task 1's script runs and reports purses, battles and length. Do not run a matrix against a script whose numbers have not been sanity-checked against today's known behaviour first.</precondition>
  <action>
    **The two levers, with their exact fields confirmed by reading the engine:**

    - **Dock output** is the crate purchase price, computed as `price(left) = max(1, crateBase - left)`
      at `4/src/engine/index.js:812-813`. `crateBase` lives in `roundCfg()` at
      `4/src/engine/index.js:3040` and is **6** today. With the typical island stock in a three or
      four player game that yields roughly a **2-5** dubloon range as an island depletes — which is
      exactly the "2-5" Wyatt named. Lowering `crateBase` toward **4** produces roughly his **1-3**.
    - **Battle cost** is `powder`, `roundCfg()` at `4/src/engine/index.js:3028`, **2** today. His
      candidate is **4**.

    **Red-proof the instrument before believing it (CLAUDE.md rule 6).** Run today's shipping
    settings first and check the output against something already known — the ladder's own reported
    voyage length and pass rate. If the baseline does not match what the existing simulator says
    about the same games, the new script is wrong and its whole matrix is worthless. **When a check
    condemns something known to work, suspect the check first.**

    Then run the matrix at a few hundred games per cell, dev seed family `×7919`:
    today's dock price and today's battle cost (the baseline); his dock price with today's battle
    cost; today's dock price with his battle cost; and both of his together. Add intermediate values
    only if a cell lands close to his target and the difference matters — **`×104729` stays held out
    for exactly those close calls.**

    Write `02.2-ECONOMY-TABLE.md` in the phase directory. **Written for Wyatt to read, in plain
    English** (CLAUDE.md rule 3) — dubloons, battles, days, captains. Not fixtures, not distributions,
    not confidence intervals. Structure it as:
    1. **The headline in two sentences** — which setting comes closest to his target, and what it
       costs.
    2. **The table** — one row per setting, columns for typical purse, worst purse anyone held,
       battles per game, and days per voyage.
    3. **Against his target, per setting** — does every captain get to do what they want at least
       once, and does anyone end up over about ten coins? Say which proxy answered the first half.
    4. **What the numbers do not tell him** — the honest boundary. This is bot play, not human play;
       say so.

    **Never hand-type a number that can be counted** — the table is generated from the run and
    written down once, and the report says which command produced it so he can re-run it.
  </action>
  <verify>
    <automated>test -f /Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-ECONOMY-TABLE.md && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-ECONOMY-TABLE.md","utf8"); for (const w of ["purse","battle","day"]) if(!new RegExp(w,"i").test(s)) throw new Error("the report never mentions "+w); if(!/node scripts\/economy_table\.js/.test(s)) throw new Error("the report does not name the command that produced it"); if((s.match(/\n\|/g)||[]).length < 5) throw new Error("no table rows found"); console.log("report shape OK")' && cd /Users/wyattroy/Documents/Projects/pastrypirates && test -z "$(git diff --name-only -- 4/src)"</automated>
    <human-check>Wyatt reads the report and can say which setting he wants without asking a follow-up question. The baseline row matches what he already experiences: runaway purses.</human-check>
  </verify>
  <done>
    A table exists covering today's settings and each of Wyatt's candidates, at a few hundred games
    each, reporting purses over time, battles per game and voyage length, read against his own stated
    target and written in his language. The baseline was checked against known behaviour before the
    matrix was believed. Nothing under `4/src/` changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Put the table in his hands — and stop</name>
  <files>.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-ECONOMY-TABLE.md</files>
  <action>
    **This is where the plan ends. There is no task after this one, and there must not be.**

    Commit the script and the report, push, pull, verify zero both ways. **Do not bump `PP4_STAMP`**
    — no game code changed, and a new stamp would tell him something untrue.

    Then give him the table in a message he can read on a phone: the headline first, the
    recommendation marked, and the trade-off stated. **Ask him to pick using the question UI, not as
    prose in a reply** — he has asked for that directly and repeatedly, and it is a standing
    instruction that does not expire when he is at his laptop. Mark the recommendation as the first
    option and leave room for "Other", because his write-in is usually better than anything offered.
    **If the session is running remotely, ask in `--text` mode** — the menus do not render through
    the Claude app, so a question asked the normal way is one he physically cannot answer, and to him
    it looks like the run simply stopped.

    **Include the link to the report in that same message.** If you ask him to look at something, the
    URL goes in the answer he is reading; he should never have to scroll up or guess.

    **Then stop.** Do not apply a setting. Do not "prepare" a branch with the change ready. Do not
    write a follow-up task that lands his pick — **that is a separate piece of work, after he picks,
    and it changes what the engine emits, which is not free after Phase 3.** He asked for the
    numbers, not for the change (D-17).
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^scripts/' | grep -v '^\.planning/')" && test -z "$(git diff --name-only origin/main..HEAD | grep '^4/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/engine/index.js","utf8"); if(!/crateBase\s*:\s*6/.test(s)) throw new Error("crateBase changed in the shipping config — D-17 says change no number until he picks"); if(!/powder\s*:\s*2/.test(s)) throw new Error("powder changed in the shipping config — D-17 says change no number until he picks"); console.log("shipping economy untouched OK")' && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt has the table, has been asked to pick with the question UI, and the shipping game plays exactly as it did before this plan ran.</human-check>
  </verify>
  <done>
    The table is committed, pushed and in Wyatt's hands with a marked recommendation and a question
    he can answer from his phone. `4/src/engine/index.js` still ships a dock price of 6 and a battle
    cost of 2. No stamp was bumped. **No setting was applied.**
  </done>
  <reversibility rating="reversible">Nothing in the shipping game changed, so there is nothing to reverse.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/economy_table.js` -> `roundCfg()` overrides -> a throwaway in-memory game | The only place a changed economy number exists in this plan is inside a headless run |
| Repo `main` -> GitHub Pages -> real players | A push publishes immediately; this plan's push carries no game code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-17 | Tampering | An economy override leaking from the script into the shipping `roundCfg()` | high | mitigate | The overrides are spread at construction inside `scripts/economy_table.js` and `roundCfg()` itself is never edited. Every task's automated verify asserts `crateBase` is still 6 and `powder` is still 2 in `4/src/engine/index.js`, and that the diff contains no `4/` path at all. This is D-17's whole point made mechanical. |
| T-02.2-18 | Repudiation | A measurement instrument that reports something other than what it names | high | mitigate | Task 2 red-proofs the baseline against the existing ladder's known figures before the matrix is believed. Three separate checks in one night on this project measured something other than what they named; when a check condemns something known to work, suspect the check first. |
| T-02.2-19 | Information disclosure | The held-out seed family being consumed by routine runs | medium | mitigate | `×104729` is reserved for close calls per the ladder's own header; asserted absent from the script by Task 1's verify and used only if a cell lands close to Wyatt's target. |
| T-02.2-20 | Denial of service | Long unbounded simulator runs | low | mitigate | Bounded game counts, no `while (true)`, asserted by Task 1's verify. This is a Node script with no browser, so no headless Chrome is started. |
| T-02.2-SC | Tampering | Package installs | n/a | accept | No package installs in this phase. |
</threat_model>

<verification>
- `git diff` for this plan contains **no `4/` path whatsoever**.
- `4/src/engine/index.js` still ships `crateBase: 6` and `powder: 2`.
- The baseline row was checked against the existing ladder's known figures before the matrix was
  believed.
- No `PP4_STAMP` bump, stated as deliberate.
- The report names the command that produced it, so Wyatt can re-run it.
- No local server survives this plan.
</verification>

<success_criteria>
Wyatt reads one page and knows what each of his two ideas would actually do to purses, battles and
voyage length — and can pick, or say neither, without asking a follow-up question. The game he plays
tonight is the game he played this morning, because he asked for the numbers, not for the change.

**This plan does not satisfy the phase gate**, and it is not meant to. The gate is Wyatt playing a
crew game and not seeing items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or 22 — item 12 is not among
them, because it is a measurement, not a fault.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-05-SUMMARY.md` when done. It
must state: that **no game code changed and no stamp was bumped**, and why; which proxy was chosen
for "could a captain do what they wanted at least once"; the baseline red-proof result; and that
Wyatt has been asked to pick, with no setting applied.
</output>
