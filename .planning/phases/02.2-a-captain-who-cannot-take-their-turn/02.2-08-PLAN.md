---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 08
type: execute
wave: 7
depends_on: ["02.2-07", "02.2-06"]
files_modified:
  - 4/src/ui/stage.js
  - 4/src/ui/flow.js
  - 4/src/ui/board.js
  - 4/src/ui/util.js
  - 4/src/orchestrator.js
  - 4/index.html
autonomous: true
requirements: [PAR-13]
group: F
drop: "Group F — the fan, and Wyatt's own five (build 2026-08-21j)"
decisions: [D-44, D-46, D-47, D-48, D-49, D-50, D-51, D-52]

must_haves:
  truths:
    - "On a phone, no two prompt circles overlap and none is piled on another, in any fan direction, including a cornered boat with eight buttons (D-44)."
    - "The italic helper line never sits on a button, and the peek hint never sits on the helper line (the two seams measured in the 260821-qwv summary)."
    - "The wind pill never collides with anything in the header row — not the ribbon's gradient, not the fast-forward button — at phone, tablet/stacked and side-by-side widths (D-47 + D-52, one structural fix)."
    - "The captains card is not flush against the bottom of the window in the stacked branch, and the layout gate samples a stacked/tablet size it has never covered (D-52)."
    - "Pass is the lowest button on screen in every prompt that offers it, in every fan direction (D-48)."
    - "Every coin flip — the dock flip and both battle flips — takes 1.5s from the frame the coin starts spinning to the frame the result is painted, measured, not asserted (D-49)."
    - "On desktop the menu's items sit in the column below the captains box; the phone keeps its hamburger. ONE list of items, two mounts (D-50, rule 23)."
    - "The turn-clock row is gone from the menu; the header chip is the only control for it (D-51)."
    - "The five small phone faults named in 260821-qwv-SUMMARY.md are fixed (D-46)."
  artifacts:
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/f-*.png
  key_links:
    - "#pp4Pill z-index:19 vs #pp4Ribbon z-index:20 + its linear-gradient (4/index.html) -> D-47 in one number; #pp4Fx is 21 and #pp4Cap 22, so the sweep is those two plus the peek fade and the ceremony veil."
    - "flow.js's opts.push for Pass -> already last in the ARRAY; stage.js's spots[i] assignment in the radial fan is what reorders it. D-48 belongs at spot assignment."
    - "humanFlip() (4/src/ui/flow.js): onBroadcastFlip('spin') -> await sleep(340) -> flip() -> result; .coin.spin is `animation:coinspin .34s linear infinite`; the coin now starts spinning on the TAP inside setFlipActive's callback. Visible flip = scheduling latency + 340ms. orchestrator.js's two broadcastFlip('spin') sites are the battle path and must land on the same clock."
    - "NEED = D + 6 in the fan's separation pass (4/src/ui/stage.js) -> 66px circles, 6px gaps; four fit in a row at 390px even at 16px."
---

# Plan 02.2-08 (Group F): the fan, and Wyatt's own five

**Everything here is his, decided at the question UI or typed to the session on 2026-08-21 evening.
The diagnoses for D-47/48/49 are already done and written into `02.2-CONTEXT.md` — read them before
touching those three; do not re-derive them.**

## Tasks

<task type="auto">
  <name>Task 1: The fan stops piling, and the helper line stops landing on buttons (D-44)</name>
  <files>4/src/ui/stage.js</files>
  <action>
    **His ruling (D-44), asked with the measurement:** circles stay full size, the gap becomes a
    real one, and **when the corner is too tight the cluster drifts away from the boat rather than
    piling.** D-38's proximity preference yields here — he had already said a control you cannot hit
    is the one unacceptable outcome.

    Today `NEED = D + 6` — 66px circles, 6px apart — and the separation pass gives up before it is
    satisfied, so at 390x664 the smoke gate hard-fails `Trade/Pass +1` stacked, and the judge reads
    the cluster as *"packed with almost no gap."* **Derive the gap** (rule 9) rather than typing 16:
    it is a fraction of the circle the game already computes, so it tracks D at every screen size.

    **The drift is the new part and it is what must be got right:** when no layout at the required
    gap fits within the boat's neighbourhood, widen the search outward from the boat — in rings —
    until one does, instead of clamping everything into a band that cannot hold it. The existing
    `formationOK` is the predicate to keep asking; what changes is that failing it moves the whole
    cluster out rather than squeezing it. **A layout that still cannot satisfy the gap must be
    reported by the gate, never silently accepted** — if you end up with a fallback that piles, log
    it and say so in the summary.

    **The two helper-line seams, measured in `260821-qwv-SUMMARY.md` so nobody re-measures them:**
    (a) `peekHintTick()` runs early in the tick, before the pill and helper line are re-placed, so
    the hint is always one tick behind where `.apSub` ends up — on `solo-phone-021` they overlap by
    1.75px. **Order the hint last in the tick.** (b) `.apSub` is not in the fan's `obstacles` list
    (only the pill is) and nothing lifts it the way `liftAskClearOfFan` lifts the pill, so on
    `passplay-phone-021` the helper line sits on the Pass circle. **Push the sub's rect into
    `obstacles`.** Two one-line seams; expect knock-on effects on the fan search and re-run the fast
    gate at all five sizes because of them.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_layout_check.mjs --out=/tmp/f1 && echo LAYOUT-OK</automated>
    Red-proof first: the smoke gate ALREADY fails `Trade/Pass +1` at 390x664 on today's build —
    capture that failure before the change, and the same command passing after. Then pose a cornered
    boat with eight buttons (DRIVING-THE-GAME §5e, solo) at 390x664 and screenshot it.
  </verify>
  <done>Zero piled/overlapping controls at all five sizes; a posed eight-button cornered fan
  screenshotted and READ; the gap is derived from D, not typed.</done>
</task>

<task type="auto">
  <name>Task 2: His three — the wind pill, Pass lowest, every flip 1.5s (D-47, D-48, D-49)</name>
  <files>4/index.html, 4/src/ui/stage.js, 4/src/ui/flow.js, 4/src/ui/board.js, 4/src/orchestrator.js</files>
  <action>
    **Read `02.2-CONTEXT.md`'s "THE THREE DIAGNOSES" section first — the cause of each is already
    established.**

    **D-47 + D-52 ARE ONE FAULT — read D-52 in CONTEXT before you start.** His screenshot shows the
    ⏩ button drawn over the pill in the stacked/tablet branch; the gradient washing over the pill is
    the same omission at a different width. The pill is `position:fixed; left:50%` — drawn in the
    header row, not part of it — so the ribbon's flex children collide with it and, at `z-index:19`
    under the ribbon's 20, win. **Fix it structurally: on desktop/tablet the pill becomes a real
    child of the ribbon's flex row** (spacing and paint order then come free, and the whole collision
    class goes with it). **The phone keeps its own fixed pill below the ribbon** — D-18/D-31, one
    decision read in one place, never a second layout. A z-index bump alone is the wrong fix: it
    cures the gradient and leaves the ⏩ overlapping.

    **ALSO IN HIS SCREENSHOT, unflagged (rule 22):** the captains card sits flush against the bottom
    of the window in the stacked branch — give it the breathing room the side-by-side column has,
    derived from the gap already in use. **AND THE GATE HAS NEVER SEEN THIS SHAPE:**
    `stage_layout_check.mjs` samples 390/960/1400/1890/1920, every one of which lands in the phone or
    the side-by-side branch. **Add a stacked/tablet size** (a tall window narrow enough to miss
    side-by-side — derive it from the same `iw >= boardSideFull + capGap + SIDE_CAP_MIN` test the
    code branches on, so it cannot drift out of the branch it is meant to cover) and keep it. Adding
    a SIZE is coverage, not a per-bug assertion — D-37 permits it.

    **D-47 — the wind pill in front of the gradient.** `#pp4Pill` is `z-index:19` under
    `#pp4Ribbon`'s 20 and its `linear-gradient`. It was right until his design note 2 moved the pill
    INTO the header row. Raise the pill above the ribbon. **Sweep (rule 8):** `#pp4Fx` 21, `#pp4Cap`
    22, the peek fade `body.pp4Foot #pp4Pill{opacity:0}`, and the ceremony veil at 44 must all still
    behave — screenshot the pill against the ribbon on phone AND desktop, and during a ceremony.

    **D-48 — Pass is always the lowest button.** *"so it's seen as the last option of what to do."*
    **This is item 14's other half**, which `02.2-01` left open as "never reproduced" — it is now an
    instruction, not a bug report, so it gets built regardless. Pass is already last in `opts`; fix
    it where `spots[i]` is assigned in the radial fan, by giving the pass option the spot with the
    greatest `y`. Then it holds whichever way the fan points, and the card fallback (already correct
    by DOM order) is unaffected. **Verify in at least two fan directions** — a boat with open water
    below it and a boat cornered so the fan points up.

    **D-49 — every flip takes 1.5s.** *"figure out why this is"* — the cause is in the diagnoses
    section: an INFINITE spin animation whose length is decided by when the result arrives, and the
    coin now starts spinning on the tap while the 340ms sleep only begins once the promise chain
    resumes. So the visible flip is scheduling latency + 340ms and no two are alike. **Stamp the
    moment the spin is painted** (in `setFlipCoin("spin")`, the one spelling of a spinning coin) and
    wait the REMAINDER of 1500ms, so the length is the same however slow the chain was. **Both battle
    flip sites in `orchestrator.js` go on the same clock** — D-49 says every flip, and two flips in
    one game taking different times is the fault, not just the dock one.
    **MEASURE IT, do not assert it** (rule 6): time paint-to-result over CDP for a dock flip and a
    battle flip, three each, before and after. Before will vary; after must be 1.5s ±100ms.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_layout_check.mjs --out=/tmp/f2 && echo OK</automated>
    Flip timings before/after in a JSON beside the summary. Screenshots: the wind pill on phone and
    desktop; a fan pointing down and a fan pointing up, Pass lowest in both.
  </verify>
  <done>Pill legible over the ribbon; Pass lowest in both fan directions, screenshotted; six measured
  flips at 1.5s ±100ms across both code paths, with the varying "before" numbers beside them.</done>
</task>

<task type="auto">
  <name>Task 3: The desktop menu moves below the captains box; the turn-clock row goes (D-50, D-51)</name>
  <files>4/index.html, 4/src/ui/stage.js, 4/src/ui/util.js</files>
  <action>
    **His words:** *"in desktop, move the menu options out of the hamburger menu and put them neatly
    below the captain's box"* and *"remove the 'Turn clock: OFF – no rush' button in the menu, it's
    already visible always in the header row."*

    **Desktop only.** The phone keeps its ☰ — D-18/D-31 promise the phone stays as it is, and he did
    not ask for a phone change. The target is the column D-31 built beside the board *"with room for
    chat/log later"*; the menu is what lands in it first, below the captains card.

    **ONE LIST, TWO MOUNTS (rule 23, and this is the whole design of the task).** Do not build a
    desktop copy of the menu. There is one function that produces the menu's items; the ☰ sheet
    mounts it on phone and the column mounts it on desktop, chosen by the same `pp4Side` class
    `computeStageGeometry()` already sets — **one decision, read twice, never two decisions kept in
    step.** If you find yourself writing a second list of items, stop: that is the fault this
    project's rule 23 exists to prevent, and it is how the two-directors bug was born.

    **D-51 travels with it:** the turn-clock row leaves the item list entirely, so it is gone from
    both mounts. The header chip keeps the state. Check nothing else reads that row.

    **Sizing:** the captains card hugs its content (build `w`) and the column has a derived width —
    the menu block sits under it and must not push either off-window at 960, 1400, 1890 or 1920.
    Derive its spacing from the gap the column already uses.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_layout_check.mjs --out=/tmp/f3 && echo OK</automated>
    Screenshot the desktop column at 1400x900 and 1920x1080 with the menu block under the captains
    card, and the phone at 390x664 showing the ☰ unchanged. Open the ☰ on phone and confirm the
    turn-clock row is gone and everything else is still there.
  </verify>
  <done>One item list, two mounts, named in the summary; desktop column screenshotted at two widths;
  phone ☰ unchanged but for the removed row; no clipping at any of the five sizes.</done>
</task>

<task type="auto">
  <name>Task 4: The five small phone faults (D-46)</name>
  <files>4/src/ui/stage.js, 4/src/ui/panel.js, 4/src/ui/flow.js, 4/index.html</files>
  <action>
    Each is named with its screen in
    `.planning/quick/260821-qwv-phone-layout-pass-right-edge-clipping-hi/260821-qwv-SUMMARY.md`:
    1. **A duplicated "Docking at …" line, one copy hidden behind the flip coin** (`solo-phone-017`,
       `-018`). This is the only one that is a real bug rather than a fit problem — **find why the
       line is emitted twice before moving either copy.** Two narration call sites for one moment is
       rule 23's own signature (Group A's whole finding), so look there first.
    2. **A bottom toolbar clipped by the screen edge over the End-of-Voyage card** (`solo-phone-024`,
       `solo-phone-eov`) — newly visible at the honest 664 height.
    3. **A captain row sliced by the bottom edge behind the recipe sheet** (`passplay-phone-008`).
    4. **Stray faint "hov." text on a board tile** (`solo-phone-011`) — looks like a truncated label;
       find what renders it.
    5. **The two recipe cards' ingredient rows misalign when one title wraps to three lines**
       (`passplay-phone-004`) — cosmetic, only visible now the ghosting is gone.
    **If any of these turns out not to reproduce, say so and measure before claiming it** (rule 6) —
    do not "fix" a screenshot artifact.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_layout_check.mjs --out=/tmp/f4 && echo OK</automated>
    A posed screenshot per fault at 390x664, before and after, read by you.
  </verify>
  <done>Each of the five is fixed or reported not-reproducing with the measurement that says so.</done>
</task>

---

## ⚡ RESCOPE — Wyatt, 2026-08-22 early. Applies to EVERY task above.

He asked why the night was slow, was told the cost is in before/after measurement rather than in the
fixes, and made two calls.

### 1. POSE THE STATE — do not play your way to it

His words: *"can you intelligently trigger the events that you need, eg storms, by non destructively
modifying code eg storm likelihood = 1?"* **Yes — and `docs/DRIVING-THE-GAME.md` §5e is the method,
already written.** Use it aggressively for every rare moment.

- **Mutate the LIVE object at runtime, never the source.**
  `const st = (await import('/4/src/state/index.js')).appState;` then set whatever the feature keys
  on. Storms: `st.game.cfg.storm = 1` — rolls at a ROUND boundary, and `rollStorm` refuses a third
  consecutive storm, so expect storms, not every round. End of voyage: `me.ing = [...me.recipe]` and
  let the driver steer home; **do not also set `pos`** (tried and rejected — the sail prompt's cells
  come from the engine's own position and the ship sails back out). Coins gate every
  can-I-afford-it branch. Black market: drain a shelf.
- **A LIVE MUTATION CANNOT SHIP; A SOURCE EDIT CAN.** Editing `roundCfg` in `4/src/engine/index.js`
  and reverting is the OLD method and is **forbidden** — Phase 14 carries a verification row whose
  whole job is proving that scaffolding never shipped. This is the precise reason his phrasing
  ("non destructively modifying code") resolves to a runtime mutation and not a file edit.
- **RED-PROOF EVERY INJECTION.** Force the known-negative first, confirm it, then inject. §5e records
  the storm check that passed while proving nothing, because a storm was already running.
- **Solo only.** Forbidden in multiplayer (the host is the sole authority — injecting desyncs it) and
  while capturing determinism.

### 2. MEASURE ONLY WHAT HAS A NUMBER IN IT

His call, to land more of his list by morning.

- **Still measured, before and after, with an instrument shown to FAIL first — anything he gave a
  number for.** In this plan that is **D-49's 1.5s flip** (all three flip sites) and any hold or
  spacing figure a task states. Numbers are exactly where this project has been burned: D-10 shipped
  a narration change that measured as doing literally nothing, and the fan's 6px gap was a number
  nobody had looked at on a real phone.
- **No longer required: before/after instrumentation for purely visual fixes** — the wind pill's
  collision, Pass sitting lowest, the desktop menu's new home, the five small phone faults, the
  captains card's breathing room. **A screenshot you have opened and READ is the proof.** Say what
  you saw in the picture.
- **Unchanged regardless:** the structural gates still run, the fix is looked at in the rendered
  image, and anything that could differ between host and guest is checked on both.
