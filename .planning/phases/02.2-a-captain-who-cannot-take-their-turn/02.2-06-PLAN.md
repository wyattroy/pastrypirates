---
phase: 02.2-a-captain-who-cannot-take-their-turn
plan: 06
type: execute
wave: 6
depends_on: ["02.2-05"]
files_modified:
  - 4/src/ui/bakeoff.js
  - 4/src/orchestrator.js
  - 4/index.html
  - 4/src/ui/board.js
  - 4/src/ui/stage.js
autonomous: true
requirements: [PAR-13]
group: D
drop: "Group D — design and copy"
items: [6, 16, 8, 9]

must_haves:
  truths:
    - "Once a captain has attempted their bake, the Bake-Off card leaves the screen and does not come back, so simultaneous bake-offs are visible (item 6, D-16)."
    - "A joining captain keeps the name they typed. A name another HUMAN holds is refused with a warning under the Yer Captain Name box on JOIN VOYAGE — not a blocking popup (item 16, D-19)."
    - "A name a BOT holds is granted to the human, and the bots swap names to accommodate (item 16, D-19)."
    - "If Wyatt has picked a sketch, the end-of-voyage card can be got out of the way to see the final board, and the recipe card's gradient reaches the edges and darkens with the Download PDF and Email buttons above the scrolling recipe (items 8, 9)."
    - "If Wyatt has NOT picked, items 6 and 16 ship anyway and 8 and 9 wait for him. This plan never stalls at a checkpoint (D-14)."
    - "Wyatt is told a new PP4_STAMP and can see it live (D-02)."
  artifacts:
    - 4/src/ui/bakeoff.js
    - 4/src/orchestrator.js
    - 4/index.html
    - .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/d-*.png
  key_links:
    - "bakeoffIntroCard (4/src/ui/bakeoff.js:164) and its sibling teardowns at :240, :242, :474, :497, :526 -> the actionPanel stage flag: item 6's card lifecycle lives here."
    - "joinRoom's fresh-claim netClaimSeat write (4/src/orchestrator.js:1568-1573) -> a seat's name, written verbatim with no collision check today; renameMySeat (:1494) has the identical gap."
    - "unusedDefaultName(withoutSeat(s,i), i) -> the collision check that ALREADY exists, but only on the blank-name fallback path."
    - "#joinName on JOIN VOYAGE (4/index.html:1935) -> where the inline warning must appear; MAX_NAME_LEN=18 (4/src/ui/util.js:2017) caps what any warning can promise."
---

<objective>
**Group D — design and copy.** The sixth and last drop (D-01).

**This plan must never stall waiting for Wyatt, and that is a locked decision (D-14).** The sketches
for items 8 and 9 shipped early with Group Q precisely so he could pick whenever he had a minute.
**If he has picked, build his pick. If he has not, build items 6 and 16, leave 8 and 9, say so
plainly, and finish.** There is no checkpoint in this plan. Nothing in the phase waits on him.

Purpose: the last four things on his list, and none of them holding up the drop.
Output: a Bake-Off card that knows when it is finished, a join screen that keeps the name you typed,
and — if he has chosen — the two redesigned cards.

**No UI-SPEC governs this phase — his notes are the design contract (D-23).**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-CONTEXT.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-RESEARCH.md
@.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<absolute_paths>
Every path is relative to `/Users/wyattroy/Documents/Projects/pastrypirates`. The game under
development is `4/`. **Never touch a root-level `src/` path.**
</absolute_paths>

<tasks>

<task type="auto">
  <name>Task 1: Item 6 — the Bake-Off card leaves once you have attempted your bake, and does not come back</name>
  <files>4/src/ui/bakeoff.js</files>
  <read_first>
    `docs/BOARD-RENDERING.md` — mandatory before anything drawn (CLAUDE.md §4). **An HTML overlay
    mapped to board coordinates must be added to `CAM_HTML_LAYERS` or it detaches the moment the
    director zooms**, and **anything that animates continuously must be HTML, not SVG — Chrome cannot
    composite an SVG transform animation at all.**
  </read_first>
  <action>
    **Wyatt's ruling (D-16): the card does NOT come back.** The attempt is locked in and the card has
    nothing left to offer. The reason this matters to a player: while the card is up, they cannot see
    the other captains' simultaneous bake-offs.

    The card's lifecycle lives in `4/src/ui/bakeoff.js`. Its builder is `bakeoffIntroCard()` at
    `:164`, which Plan 03 gave the centre-stage treatment, and the file's existing teardown calls
    sit at `:240`, `:242`, `:474`, `:497` and `:526`. Trace the path a captain takes from "Get
    bakin'!" through a completed guess, find where the card is currently re-shown or left standing
    after the attempt resolves, and make it leave once and stay gone for that captain's bake — while
    the rest of the table's bake-offs stay visible behind it.

    **Do not solve this with a flag that only the UI knows about if a player field already answers
    the question.** The engine already tracks whether a captain is baking and whether they are done
    (`p.baking`, `p.done` — the same pair Plan 01 used for the Bakeries stat). Ask what exists **by
    behaviour** before adding state: a day once went into building something a design doc says in one
    sentence already shipped, in the same file being edited, because grep cannot surface a capability
    that exists under a different name.

    **Sweep for consistency (CLAUDE.md rule 8):** if the card's dismissal uses an existing gesture,
    it must behave the way that gesture behaves everywhere else. Hold-the-sea already fades every
    floating box — prompts of all styles, narration bubbles, the stay-put confirm — but deliberately
    **not** the centre-stage intros or the flip-ceremony veil, and that exception is Wyatt's own
    pick. Name in the summary which surfaces you checked.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/planner_singleton_check.js && node -e 'const s=require("fs").readFileSync("/Users/wyattroy/Documents/Projects/pastrypirates/4/src/ui/bakeoff.js","utf8").split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n"); const sets=(s.match(/pp4Stage\s*=\s*"1"/g)||[]).length; const clears=(s.match(/delete .*pp4Stage/g)||[]).length; if(clears < sets) throw new Error("bakeoff.js sets the centre stage "+sets+" times and clears it only "+clears+"; a card can be left standing"); console.log("bakeoff card lifecycle OK")'</automated>
    <human-check>In a game with more than one captain baking: attempt your bake, and the card leaves. It does not return. You can watch the other captains' bake-offs. Screenshot the moment after the attempt resolves and again two beats later.</human-check>
  </verify>
  <done>
    Once a captain has attempted their bake, the card is gone and stays gone, and the simultaneous
    bake-offs behind it are visible. The summary names the surfaces swept for gesture consistency.
  </done>
  <reversibility rating="reversible">A render-tier lifecycle change in one UI module; no engine field is added.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Item 16 — a joining captain keeps the name they typed</name>
  <files>4/src/orchestrator.js, 4/index.html</files>
  <action>
    **CONFIRMED — no collision check exists today for a typed name.** In `joinRoom()`'s fresh-claim
    path (`4/src/orchestrator.js:1568-1573`) the typed name is written to the seat **verbatim, with
    zero check against other seats**. Only the fallback — `unusedDefaultName(withoutSeat(s, i), i)`,
    used when the player accepted the auto-suggestion unchanged — already checks for collisions.
    `renameMySeat()` (`4/src/orchestrator.js:1494`) has the identical gap and must be fixed the same
    way, or the rule holds on joining and fails on renaming.

    **D-19, Wyatt's ruling, two different answers depending on who holds the name:**

    - **Another HUMAN holds it** (the seat has an id and is not a bot): **refuse the claim.** Do not
      write to Firebase. Surface a warning **under the Yer Captain Name box on JOIN VOYAGE** —
      `#joinName` at `4/index.html:1935` — as an **inline DOM message**.
    - **A BOT holds it**: **grant it to the human anyway, and the bots swap names to accommodate.**
      Reassign the bot via the collision-checking helper that already exists,
      `unusedDefaultName()`, excluding the newly-claimed name. **Reuse it rather than writing a
      second name-picker.**

    **The warning must NOT be an `alert()`, and this is not a style preference.** A blocking `alert()`
    already caused one incident in this project — it silently froze a page mid-probe and the tab
    looked hung. Elsewhere in this same file `alert()` is the existing pattern; **do not follow it
    here.**

    **Copy register.** This message is spoken inside the game world, so it is pirate speak — the
    credits and About page are the only places that are not, and that boundary is diegetic and
    deliberate. Match the register of the neighbouring join-screen copy.

    **The 18-character ceiling is context the warning must respect.** `MAX_NAME_LEN` is 18
    (`4/src/ui/util.js:2017`) and Firebase enforces the same cap server-side, independently of client
    code. That cap is **not** a bug and is already correct — do not "fix" it, but do not write a
    warning that promises something the cap will refuse.

    **This is a Firebase write path, so mind the race.** Two captains can type the same name at the
    same instant. The check belongs inside the same transactional claim that writes the seat, not
    before it — otherwise the check passes for both and the last write wins, which is the bug wearing
    a fix's clothes.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/planner_singleton_check.js && node -e 'const fs=require("fs"); const R="/Users/wyattroy/Documents/Projects/pastrypirates/"; const s=fs.readFileSync(R+"4/src/orchestrator.js","utf8"); const seg=(from,to)=>{const i=s.indexOf(from); const j=to?s.indexOf(to,i):s.length; return s.slice(i,j).split("\n").filter(l=>!/^\s*\/\//.test(l)).join("\n");}; const join=seg("export async function joinRoom"); const jb=join.slice(0, join.indexOf("\n}\n")>0?join.indexOf("\n}\n"):3000); if(!/\bbot\b/.test(jb)) throw new Error("joinRoom never distinguishes a bot-held name from a human-held one"); const ren=seg("export async function renameMySeat","export async function joinRoom"); if(!/\bbot\b/.test(ren)) throw new Error("renameMySeat still has the identical collision gap"); const h=fs.readFileSync(R+"4/index.html","utf8"); if(!/joinName/.test(h)) throw new Error("the join name box is missing"); console.log("name collision wiring OK")'</automated>
    <human-check>Two browsers: the host is called Bess. A guest types Bess on JOIN VOYAGE and gets a warning under the name box — no popup, no frozen tab — and can change it and get in. Then a guest types a name one of the BOTS is using and is let in under that name, with the bot renamed. Screenshot both cases.</human-check>
  </verify>
  <done>
    A joining captain keeps the name they typed unless another human already holds it, in which case
    they are told so under the box they typed it in. A bot's name is given up to a human and the bot
    takes another. Renaming mid-lobby follows the same rule. No blocking popup anywhere in the path.
  </done>
  <reversibility rating="costly">This changes a Firebase seat-claim write path, which is how every player gets into a room. A mistake locks people out of games rather than showing a cosmetic fault, and it is not visible in a screenshot of a working join — test the refusal path and the grant path explicitly, in two browsers.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Items 8 and 9 — build Wyatt's pick, or leave them cleanly</name>
  <files>4/src/ui/board.js, 4/src/ui/stage.js</files>
  <precondition>Check whether Wyatt has picked a sketch option for item 8 and/or item 9 (the sketches shipped with Group Q into notes/sketches/, per D-14). His pick may be in a message, in the phase record, or absent. If absent for an item, that item is skipped — this task does not wait and does not ask.</precondition>
  <action>
    **D-14 is explicit: nothing in the phase waits on him and Group D must never stall at a
    checkpoint.** So this task has two legitimate endings and both are success.

    **If he has picked, build the pick.** Not an interpretation of it — the option he chose, with its
    constraints intact:
    - **Item 8 — the end-of-voyage card.** It must be **collapsible (or hold-the-sea)** so the final
      board can be seen. If the chosen option uses hold-the-sea, it must behave exactly as
      hold-the-sea behaves everywhere else in the game (CLAUDE.md rule 8), and note the sanctioned
      exception: hold-the-sea deliberately does **not** fade the centre-stage intros or the
      flip-ceremony veil, which is Wyatt's own pick and must not be "made consistent".
    - **Item 9 — the recipe card.** The gradient **reaches the box edges** and **darkens, not
      lightens**. The **Download PDF and Email buttons sit clearly above** the scrolling recipe. The
      button treatment and the recipe layout itself are open — his words: *"You may redesign the
      buttons, place them elsewhere, and lay out the recipe itself differently if you want."*

    **Read `docs/BOARD-RENDERING.md` before building either** (CLAUDE.md §4). Both traps apply
    directly here: **an HTML overlay mapped to board coordinates must be added to `CAM_HTML_LAYERS`
    or it detaches the moment the director zooms** — item 8's card sits over the final board, which
    is exactly that situation — and **anything that animates continuously must be HTML, not SVG**,
    because Chrome cannot composite an SVG transform animation at all (~62 layouts/sec as SVG,
    **zero** as HTML).

    **Respect the narration box's reveal order (CLAUDE.md rule 11):** whatever sits highest is
    revealed first, following the DOM order `localAsk()` builds — back button, message text, action
    buttons, italic helper text. A new element's reveal order follows its visual position and does
    not need re-deciding. Two separate playtest findings traced back to violating this.

    **If he has NOT picked, skip cleanly.** Build nothing for that item. Do **not** build a "v1", a
    simplified version, a placeholder, or a static stand-in — a reduced version of his design is
    worse than an honest gap, because it looks answered. Write into the summary and into his drop
    message: which of 8 and 9 are still waiting, that the sketches are at their URLs, and that they
    will be built whenever he picks. **Then finish the plan.**

    **QA your own work before handing it over (CLAUDE.md rule 19):** screenshot whatever you built,
    both sides in multiplayer, and look at the picture — not the DOM, not computed styles. Everything
    that got through at the 02.1 gate was obvious in a screenshot and invisible to an assertion.
  </action>
  <verify>
    <automated>node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js</automated>
    <human-check>If built: the end-of-voyage card can be got out of the way and the final board is visible behind it; the recipe card's gradient runs edge to edge and gets darker, with the two buttons above the scrolling recipe. Screenshot each, both sides. If not built: the summary and Wyatt's message both say which items are waiting and where the sketches are.</human-check>
  </verify>
  <done>
    Either Wyatt's chosen designs are built and screenshotted, or items 8 and 9 are explicitly left
    with their sketch links restated — and in neither case did the plan stop and wait for him.
  </done>
</task>

<task type="auto">
  <name>Task 4: The Group D drop — two tabs, both sides, stamp bumped once, and the phase handed over</name>
  <files>4/src/ui/stage.js</files>
  <read_first>
    `docs/DRIVING-THE-GAME.md` before touching a browser — §5c driving a guest while a human hosts is
    the relevant section for Task 2's join-collision check, and §5e's state injection is **allowed in
    solo, FORBIDDEN in multiplayer**. `docs/GIT-AND-DEPLOY.md` §5 for the drop loop.
  </read_first>
  <action>
    Same drop loop as every group (D-02, D-03), plus the phase hand-off.

    1. **Play a two-tab crew game and screenshot BOTH sides**, with `4/scripts/mp_rig.mjs` against a
       local server on a fresh port. Include the two join paths from Task 2 — a refused human-name
       collision and a granted bot-name collision — because those are the changes a screenshot of a
       normal join cannot see.
    2. **Watch for Group E one last time (D-21)** — a guest whose browser was killed mid-turn coming
       back to a battle prompt that draws nothing. Watching brief; no dedicated hunt.
    3. **Run the `4/` gates by name.** Root `npm test` scans only the older game.
    4. **Bump `PP4_STAMP`** at `4/src/ui/stage.js:32`, once.
    5. **Prove the diff touches only `4/` and `.planning/`** — never `CNAME`, `robots.txt` or
       `sitemap.xml`.
    6. **Push, pull, verify zero both ways.**
    7. **Hand the phase over, not just the drop.** Wyatt's message names the stamp, states the size,
       and then says plainly **which of his twenty-two items are done, which are left, and why** —
       including anything Group A wrote off under D-04, anything a reproduction never found, and
       items 8 and 9 if he has not picked. His numbering is authoritative; use his numbers.
    8. **Then tell him the gate is his.** The phase gate is **him playing a crew game and not seeing
       items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or 22.** It is his eyes, not a probe, and **no
       plan in this phase may claim to have satisfied it.** Give him the URL in the same message.
    9. Kill every Chrome and server before replying.
  </action>
  <verify>
    <automated>cd /Users/wyattroy/Documents/Projects/pastrypirates && node 4/scripts/no_undef_check.js && node 4/scripts/stage_import_check.js && node 4/scripts/pass_coin_test.js && node 4/scripts/pass_narration_test.js && node 4/scripts/planner_singleton_check.js && node 4/scripts/prompt_field_parity_check.js && node 4/scripts/seat_arg_check.js && node 4/scripts/audio_map_check.js && node 4/scripts/bot_bake_pass_check.js && node 4/scripts/dlog_quantity_check.js && node 4/scripts/pp4_timeroff_check.js && node ~/.claude/gsd-core/bin/gsd-tools.cjs validate health && test "$(ls .planning/phases/02.2-a-captain-who-cannot-take-their-turn/shots/d-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 4 && test -z "$(git diff --name-only origin/main..HEAD | grep -v '^4/' | grep -v '^\.planning/')" && test "$(git rev-list --count origin/main..main)" = "0" && test "$(git rev-list --count main..origin/main)" = "0" && test -z "$(pgrep -f remote-debugging-port)" && test -z "$(pgrep -f http.server)"</automated>
    <human-check>Wyatt opens playpastrypirates.com/4, sees the new stamp, and has a message telling him — in his own numbering — what is done, what is left, and that the last word is his.</human-check>
  </verify>
  <done>
    Group D is live under a fresh `PP4_STAMP`; both sides of a real crew game including both join
    paths are screenshotted; the named `4/` gates pass and the health check was run and its findings
    surfaced in the reply; the diff touched only `4/` and `.planning/`; `main` and `origin/main` are
    zero apart both ways; and Wyatt has an item-by-item hand-off in his own numbers, with the gate
    named as his.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player-typed captain name -> `joinRoom`'s seat claim -> Firebase -> every other client's lobby and ribbon | The only genuinely player-authored string in this phase, and it is rendered on every screen |
| Firebase RTDB security rule -> `/seats/*/name` | An 18-character cap enforced server-side, independently of client code |
| Repo `main` -> GitHub Pages -> real players on the live root game | A push publishes immediately; no build step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-21 | Spoofing | A joining human claiming a name another human already holds | high | mitigate | This is item 16 and it is literally an impersonation surface: two captains under one name at one table. Task 2 refuses the human-held case inside the same transactional claim that writes the seat, so a simultaneous double-claim cannot slip between check and write. |
| T-02.2-22 | Tampering | A player-typed name echoed back into the inline warning and into every client's lobby and ribbon | high | mitigate | The warning under `#joinName` echoes attacker-controlled text; escape it on the way into the DOM and prefer text content over markup. Confirm the existing lobby and ribbon renderers escape a seat name too — this is the one path in the phase where a player controls a string every other player renders. |
| T-02.2-23 | Denial of service | A blocking `alert()` in the join path | medium | mitigate | Explicitly forbidden by Task 2. A blocking `alert()` already froze a page mid-probe in this project and the tab looked hung. The warning is inline DOM only. |
| T-02.2-24 | Tampering | A card overlay mapped to board coordinates detaching from the board when the director zooms | medium | mitigate | `docs/BOARD-RENDERING.md`: any HTML overlay on board coordinates must be registered in `CAM_HTML_LAYERS`. Item 8's end-of-voyage card sits over the final board and is exactly this case; a prior commit already paid for this trap once. |
| T-02.2-25 | Denial of service | Headless Chrome and `http.server` left running | medium | mitigate | Bounded loops; `killAll()` plus `pkill` inside the same task; asserted in Task 4's automated verify. |
| T-02.2-SC | Tampering | Package installs | n/a | accept | No package installs in this phase. |
</threat_model>

<verification>
- The named `4/` gates pass by name, plus the health check, with its findings surfaced in the reply
  rather than read and dropped.
- Both join paths — human collision refused, bot collision granted — are exercised in two browsers
  and screenshotted, because neither is visible in a screenshot of a normal join.
- The join warning is inline DOM, never `alert()`, and the echoed name is escaped.
- `renameMySeat` got the same rule as `joinRoom`.
- Items 8 and 9 are either built to Wyatt's pick or explicitly left — never reduced to a placeholder.
- `PP4_STAMP` bumped exactly once and confirmed serving.
- No Chrome and no local server survives this plan.
</verification>

<success_criteria>
The Bake-Off card knows when it is finished. A captain keeps the name they typed unless a real person
already has it, and is told so where they typed it. If Wyatt picked a sketch, that design is live.
And he has an item-by-item account of his own twenty-two, in his own numbers.

**This plan does not satisfy the phase gate, and neither does any other plan in this phase.** The
gate is **Wyatt playing a crew game and not seeing items 1, 2, 3, 5, 11, 14, 15, 17, 19, 20, 21 or
22.** It is his eyes, not a probe.
</success_criteria>

<output>
Create `.planning/phases/02.2-a-captain-who-cannot-take-their-turn/02.2-06-SUMMARY.md` when done. It
must state: the new `PP4_STAMP`; whether items 8 and 9 were built or left waiting on Wyatt's pick;
the surfaces swept for item 6's gesture consistency; that both join collision paths were exercised in
two browsers; the health check's findings; and the item-by-item state of all twenty-two in Wyatt's
numbering, so the phase gate can be handed to him with an honest list.
</output>
