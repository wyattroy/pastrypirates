---
quick_id: 260824-nh8
description: "Item 23 re-opened: one attention vocabulary — stage buttons glow, board prompt buttons grow, Start buttons glow in every mode"
date: 2026-08-24
---

# Quick task 260824-nh8 — PLAN

**Execution note:** executed directly in the orchestrating context, same as 260824-5qz and for the
same reason — this session holds the live measurements (the radial fan's animation is RUNNING but
imperceptible: 66→72px, ring at 28% alpha over the sea) and Wyatt's three rulings from the question
UI, verbatim. A spawned planner would re-derive both.

## His rulings (2026-08-24, question UI — locked)

1. Board prompt buttons: **strong grow, no ring** — the golden ring is not theirs.
2. **"ALL MODES LOOK and behave THE SAME... every clickable button that is on a STAGE [gets] the
   orange glow, and every action prompt button that is over the board [gets] a scale pulse."**
   One display function, changed in one place. Stop piecemeal patches. Start buttons pulse in every
   mode. He also demands an explanation of why the code drifted.
3. **Don't touch the sail squares.**

## The diagnosis being fixed

The orange glow exists THREE times (`flipglow`, `ahoyglow`, the ring half of `pp4StagePulse`), and
button attention is assigned by four scattered rules. The radial fan's pulse runs (measured,
computed animationName + 66→72px swing) but is imperceptible — scale 1.09 with a 28%-alpha ring
over the open sea — which is why the same dock reads "pulsing" over sand and "dead" over water.
Start buttons have never been covered by any rule.

## Tasks

1. **The vocabulary block** (`4/index.html`): one clearly-marked block, at the end of the
   stylesheet, that is the ONLY place attention animations are assigned. Two keyframes:
   `pp4Glow` (the coin's orange ring — coin, .ahoyGlow, centre-stage .apBtn, .btlBtn, the three
   Start buttons) and `pp4Grow` (scale only — non-centre #actionPanel .apBtn; radial petals get a
   bigger swing than card rows via one custom property set in the same block). Delete flipglow,
   ahoyglow and pp4StagePulse plus their scattered assignment rules. Sail squares and plankglow
   untouched. Hover-pause and reduced-motion travel with the block (reduced motion kills the grow —
   it moves — and keeps the glow, which is a fading ring, the coin's only affordance cue).
2. **QA + ship**: probe the radial fan (grow computed + painted swing), a centre-stage card, a
   battle card and the pass-and-play Start (glow computed); screenshot fan + stage; gates
   (stage_layout_check at its two known failures, npm test); bump PP4_STAMP → 2026-08-24d; ship loop.
