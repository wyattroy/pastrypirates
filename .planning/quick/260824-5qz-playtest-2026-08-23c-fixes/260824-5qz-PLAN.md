---
quick_id: 260824-5qz
description: Execute HANDOFF-2026-08-23-evening.md — Wyatt's approved fix order for his 2026-08-23c playtest
date: 2026-08-24
status: in-progress
---

# Quick task 260824-5qz — the 2026-08-23c playtest fixes

**The plan IS the handoff:** `.planning/HANDOFF-2026-08-23-evening.md` carries the approved order,
his verbatim notes, the evidence index, and what was already answered from the code. This file does
not restate it (point, don't restate) — it only tracks execution shape.

## Tasks, in his approved order

1. **Tier 1 — the three blockers**
   1. The "⚓ Reconnecting to yer voyage…" hang: reproduce in the two-window rig (crew game, host
      closes tab), fix, AND give that screen an escape hatch. (`4/src/orchestrator.js` boot/resume.)
   2. A bake-off trigger verified working in CREW games, so his checklist items 43–46 become
      testable. Solo `?ovens=1` verified along the way.
   3. The blank-space lag: play, measure, bisect builds if needed. Nothing measured yet.
2. **Tier 2 — the visible-bug batch (~13 fixes)** — the list in the handoff, verbatim from his notes.
   Each fix: reproduce or locate precisely, fix, screenshot, atomic commit.
3. **Tier 3 — desktop menu redesign (item 21)**: prepare 2–4 concrete options with screenshots,
   ask via the question UI with a recommendation. Do not guess.

## Execution notes

- Executing directly in the orchestrating session (not a spawned executor): the session holds the
  read evidence (three screenshots + 22 video frames, read pixel-by-pixel), the three governing
  docs, and the handoff's answered-already list. Handing that to a fresh subagent would re-pay the
  cost the handoff exists to avoid.
- Atomic commits per fix, on branch `claude/handoff-2026-08-23-evening-lunbr1`.
- Ship loop when a batch is playable: bump `PP4_STAMP`, prove diff touches only `4/` (+ docs/
  planning), push.
- QA bar: rendered screenshots, both sides for anything multiplayer (rule 19).
