# PREDICTION — `T-087`, remove "Your rulings, in hand" from the Glass

Written 2026-09-03T07:15Z by watch c1, **before any measurement or edit**, with its falsifiers.
His words, on the Glass 2026-09-02T13:18:28.755Z: *"Remove the 'Your rulings in hand' box from the
Glass"*. No solution beyond that sentence — the sentence IS the solution, so it goes in first.

## What I expect, and why

1. **The card is rendered in exactly one place and its data feeds nothing else.**
   `scripts/wyclau/glass.mjs:1262-1268` builds it; `ruled` is parsed once at `:480-485` from the
   Chart's `## RULED` table. I expect grep to find no other consumer of `ruled` in the generator.
   **Falsifier:** any other read of `ruled` — a count in the header, a badge, the state block, the
   client-side script. If one exists, removing the card is not a deletion but a re-wiring, and the
   size of this item is wrong.

2. **The `twoCol` grid will be left holding one child, and that is a visible defect, not a
   no-op.** `:1071-1075` makes `.twoCol` two equal columns above the phone breakpoint. Deleting
   one of two children leaves "Shipped today" occupying the left half of the page with dead space
   beside it. **So the correct removal also unwraps the grid**, which is a second edit, not an
   afterthought.
   **Falsifier:** if `.twoCol` turns out to be `auto-fit`/`auto-fill` rather than `1fr 1fr`, a
   single child would stretch on its own and the unwrap is unnecessary.

3. **`scripts/qa/rulings_triage_check.mjs` will go RED, in two of its five cases, and it is
   RIGHT to.** Cases 2 and 5 assert things about the rendered card (`<table id="ruled">`);
   case 5's own comment says over-hiding *"is the failure that would replace the one he
   reported."* Cases 1, 3 and 4 judge the RECORD (`## RULED` → checklist → `## SETTLED RULINGS`)
   and should be untouched.
   **Falsifier:** if the gate stays green after the card is gone, then case 2's and case 5's
   regexes never actually located the card and the gate has been proving nothing — a worse finding
   than the one I am predicting.

4. **His safety is not lost by removing the card, because the lifecycle already routes an
   outstanding ruling onto the Tasks card** (`- [ ] Your ruling: …` in the STEP 1 CHECKLIST,
   enforced by the same gate's case 4). The card he asked me to remove shows rulings *before*
   anyone has triaged them.
   **Falsifier — and this is the one that would stop the whole item:** if there is a state in
   which a ruling is on NEITHER surface (harvested into `## RULED`, no checklist row yet), then
   removing the card genuinely hides something from him, and the honest answer is to remove the
   card AND make the untriaged ruling surface somewhere he can see — or to put the trade to him.

## What would make me report a partial

If falsifier 4 fires, I do the removal he asked for and say plainly, in the reply and on his page,
what window of invisibility it opens and what closes it. I do not quietly keep the box because I
thought better of his instruction.
