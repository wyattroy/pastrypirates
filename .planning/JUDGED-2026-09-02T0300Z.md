# The 0137Z queue, judged — the first release trial whose evidence describes the build in the tree

*Watch 2026-09-02T03:00Z, Wy-Blade. His standing ruling INBOX-20260902T0050Z — "Judge the
screenshots first", before staging, before release — applied to the second queue.*

---

## WHY THIS QUEUE IS DIFFERENT FROM EVERY OTHER ONE

**The trial sailed build `2026.09.01.8`, and `2026.09.01.8` is the stamp in the tree right now.**
Every previous trial in this project was retired by a fix that shipped after it sailed — four times
in one day. `passplay-desktop-003-settled.png` carries the proof in its own bottom-right corner:
*Build 2026.09.01.8*, photographed by the run itself.

**So this is the first visual evidence this project has ever had about the build it would actually
stage.** That is the whole reason this was the watch's one item, and the reason no `src/` file was
touched to get it: a stamp bump would have retired it for the fifth time.

## AND THE PICTURES SURVIVED — ALL OF THEM, WHICH HAS NEVER HAPPENED BEFORE

The 1914Z queue lost **252 of its 343 pictures** to an overlapping trial, because every run writes
the same filenames and nothing stamps a screenshot with the run that took it (Chart row, still open,
still the real fix). The 02:19Z watch could only save 221, and the hole it left had the worst
possible shape: the **low-numbered screens — the opening of the game** — in solo and pass-and-play,
the two modes a Reddit visitor opens first.

Nothing was sailing when this watch woke, so the snapshot was taken **before the claim was even
written**:

```
node scripts/qa/judge_the_queue.mjs --snapshot=judge-0137Z-shots --before=2030-01-01T00:00:00Z
  -> took 820 file(s); 0 png already rewritten by a later run and left behind
node scripts/qa/judge_the_queue.mjs --judge=judge-0137Z-shots
  -> 315 queued · 0 already judged · 0 lost to a later trial
```

**315 of 315 queued screens present. Zero lost.** Every leg has its `001`–`015` — the ceremony, the
recipe draft, the drawing of lots, the first days — for the first time in the record.

## THE STRUCTURAL HALF, WHICH STANDS ON ITS OWN

| | |
|---|---|
| voyages sailed | **10 of 10**, 78 minutes, FULL gear |
| voyages that did NOT run | **none** |
| `npm test` | **PASS** |
| modes / sizes / engines | three / three / both (Chromium + WebKit) |
| why the headline still reads FAILED | settle timing (screens photographed while still moving, longest 2.7–3.0s) · the 315 deferred screens this pass clears · a handful of coverage gaps (`deny`, `walk away`, `vanilla beans` offered but not exercised on some legs) |

**None of those is a player-facing defect.** The report's FAILED headline is about the instrument,
not about the game — with the one genuine exception below.

---

# WHAT THE PASS FOUND

## 1. THE LAST SCREEN OF THE GAME, AGAIN — a second mode, the current build, and now SCOPED

**`solo-phone-023-settled.png`.** The automatic judge found this on its own, unprompted:
*"award cards' text … clipped/overlapped by the fixed 'Play again!' button sitting on top of them."*

Opened by eye, it is the identical fault the 02:19Z watch found on `passplay-phone-039` — down to
the detail. *The Silver-Tongued Ledger*'s winner, **Davy Scones in pink**, is sliced horizontally:
only the tops of the letters clear the button's frosted backing. *Crustbeard* on the right card
survives because it sits a few pixels higher.

**What is new, and it matters twice over:**

- **It is not one screen in one mode.** That was pass-and-play; this is **solo**. It is what the End
  of Voyage does on a phone.
- **IT IS PHONE-ONLY, AND THE TABLET IS A WORKING REFERENCE.**
  `solo-tablet-022-settled.png` — in this run's queue, written 02:03Z, so same run and same build —
  shows **four** award cards in one row with **every winner's name fully legible** (Davy Scones,
  Crustbeard, Dough Hook, Flaky Jack), the whole stats table readable, and *Play again!* sitting
  clear **below** the content. Nothing is covered.

**So the posed pair rule 26 asks for already has its third picture.** Whoever fixes this poses
390×664 before and after and checks the result against the tablet, which is what the screen is
supposed to look like. Chart row updated rather than duplicated.

### ⚠ THE CITATION ABOVE WAS WRONG FIRST TIME, AND THE CAUSE IS WORTH MORE THAN THE FIX

This first cited `solo-tablet-031-settled.png` as *"same run, same build"*. **It is neither.**
Its mtime is **2026-09-01T14:52Z** — hours before even the 1914Z run — and `grep` finds it **not in
the 0137Z queue at all**. It was caught by reading the build stamp printed in its own corner:
`2026.09.01.7`, not `.8`.

**THE CAUSE IS THE STOPGAP ITSELF, WHICH TURNS OUT TO HAVE THIS ROW'S OWN DISEASE.**
`judge_the_queue.mjs --snapshot` copies every PNG in `sea-trial-shots/` older than its cutoff. Run
with a far-future `--before`, as this watch ran it, **it takes 820 files when only 315 belong to
this run.** The other ~505 are leftovers from earlier runs that this trial never overwrote — sitting
in the snapshot under ordinary-looking names, with nothing on them to say which run they came from.
**A session reading the snapshot folder by filename gets an older build's picture and no warning.**
That is *"a queue judged after a later run describes a mixture of runs, silently"* — the open Chart
row — reproduced **inside the tool built to prevent it**.

**WHAT IS AND IS NOT AT RISK, checked rather than assumed:**
- **The 315 JUDGED screens are safe.** `judge_the_queue.mjs` reads only `judge-queue.json`, which is
  this run's own manifest, and every cited queued file's mtime falls inside the run's 01:37–02:55Z
  window (`solo-phone-003` 01:39Z, `solo-tablet-003` 01:52Z, `passplay-phone-003` 01:54Z,
  `passplay-desktop-003` 02:03Z, `passplay-phone-039` 02:11Z, `crew-phone-guest-003` 02:21Z,
  `solo-tablet-wk-003` 02:42Z, `solo-phone-023` 01:51Z).
- **BY-EYE reading of the folder is what is unsafe**, and one of this watch's own citations fell
  into it. Every by-eye claim on this page has now had its mtime and queue membership checked.

**The durable fix belongs to the existing row: make the snapshot take only what the queue names.**

## 2. THE "BAKE THIS!" PILL COVERS THE PASTRY YOU ARE ABOUT TO CHOOSE — AND IT IS DELIBERATE

**This is filed as a design question for Wyatt, NOT as a defect, and the correction is the point.**

**The prediction, written before anything was opened:** the openings have never been judged, the
three faults already found are all the same shape (something on top covering text or art), so expect
at least one more of that shape — most likely on a phone. **What would prove it wrong:** the phone
legs' 001–015 come back clean.

**What was seen.** On the recipe picker, the highlighted card's orange *Bake this!* pill sits on top
of that recipe's pastry art and covers nearly all of it, while the unselected card beside it shows
its pastry in full. Verified across **six legs, all three modes, three sizes, both engines** —
`solo-phone-003`, `passplay-phone-003`, `solo-tablet-003`, `solo-tablet-wk-003`,
`crew-phone-guest-003`, `passplay-desktop-003`. The starkest is `crew-phone-guest-003`: *Mexican
Chocolate Torte* is a blank cream sliver, *Molten Chocolate Lava Cake* beside it is fully drawn.

**THE PREDICTION WAS WRONG, AND THE GRAVEYARD IS WHY.** Commit `4d1bc6cd` (2026-08-13) says it in
plain words: *"A 'Bake this!' pill now appears **over the pastry art** of the tapped card."* It was
built to solve a different problem — *"nothing said how to commit a recipe; the confirming second tap
was undiscoverable."* And it has not drifted: `.pp4Bake { top:35px }` is unchanged since that commit
(`git log -S`), and the stage's `.recipeThumb { height:54px }` since `511c4270`, 2026-08-11 — two
days **before** the pill existed. **The pill has always sat exactly where it sits.**

`docs/INTENDED-BEHAVIOUR.md:113` covers the neighbouring false positive (*"one card highlighted and
one not is the two-tap selection state"*) but not this. Checking it was rule 28 working, and reading
the commit log was rule 10 working — without both, this would have been filed as a bug and would
have been the fourth false positive in that family.

**Why it still goes to Wyatt.** He asked tonight (INBOX-20260902T0048Z) *"what is the maximum size
they are displayed at?"* about this very art, while deciding whether to compress it. The honest
answer has a part nobody had said: **on the one screen where a player compares two pastries to
choose between them, the selected one is not really displayed at all.** That is a taste call, it is
his, and it is now in BLOCKED ON WYATT with the trade stated (moving the pill may put back the
discoverability problem it was built for) and a recommendation marked.

## 3. ONE JUDGE FINDING ASSESSED AND NOT PASSED ON AS A DEFECT

**`solo-tablet-003-settled.png`** — the judge wrote *"Recipe-selection modal overlaps the captains
panel below it, clipping the 'Davy Scones' row label down to just 'Dav'."* Opened by eye: that is a
modal drawn over a panel, which is what a modal does, and the same occlusion appears on
`solo-tablet-wk-003`. **Recorded rather than dropped silently**, because a finding quietly deleted
and a finding examined are different things — but it is not being carried forward as a defect.

---

## THE BLIND SAMPLE — CEO 86's finding 2, answered with a measurement instead of a caveat

CEO Review 86 said the previous pass's *"218 PASS"* carried **no measured trust level**, and that
the one known false PASS was found because a human had already flagged that screen — not by
sampling. So this pass took a sample properly.

**The protocol, and the order is the whole point:** four screens were named in writing — one per leg
family — **before any verdict was read**, then opened and judged by eye, then and only then compared
against the recorded JSON.

| screen | my verdict, by eye | judge's verdict |
|---|---|---|
| `crew-desktop-host-015-settled.png` | PASS — prompt, three action circles, captains panel, menu all legible | — |
| `solo-desktop-012-settled.png` | PASS — the seven-circle trade fan overlaps boats, but every label reads, and that overlap is his settled ruling *"don't touch the trade fan, it's fine"* | **PASS** |
| `passplay-phone-020-settled.png` | PASS, one uncertainty named: the *Trade* circle is translucent where *Dock* and *Muse* are opaque | **PASS** |
| `solo-phone-wk-010-settled.png` | PASS — dock prompt, both circles legible on WebKit | — |

**AND THE ONE UNCERTAINTY IS NAMED RATHER THAN SMOOTHED AWAY.** The translucent *Trade* circle on
`passplay-phone-020` is most likely the run's own known settle miss — that leg reported **16 screens
that never stopped moving before being checked** — not a defect. **It is not being reported as one,
because a single still cannot tell a mid-fade from a bug** (rule 6), and the way to settle it is a
posed board, not another trial.

**A dividend from `solo-phone-wk-010`, on the open 🌕 question:** on that WebKit leg the coins render
perfectly — because they are `coin-emoji.png` **images**. That is consistent with the existing Chart
row rather than new: what came back blank was the **typed** U+1F315 in the black-market string, not
the image. It narrows the open question without answering it, and real Safari is still unknown.

## THE CAVEAT THAT BELONGS BESIDE EVERY NUMBER ON THIS PAGE

**The automatic judge's clean count is a FLOOR, not a ceiling** (Chart row, 02:19Z watch): it
cleared `crew-desktop-guest-012` with plainly clipped text on it, and of five PASS screens a human
has examined, one was wrong. It is not wrong constantly — it found finding 1 here entirely on its
own — but a PASS count is *"nothing was noticed"*, never *"the screens are clean"*. Every
release sentence quoting these numbers has to carry that clause.

## WHAT IS NOT CLEARED

Nothing. **315 of 315 queued screens had a surviving picture and every one was put in front of a
judge or an eye.** `judge_the_queue.mjs:79-81` drops a screen whose picture did not survive rather
than judging whatever now sits at its old path — that is structural, not a promise — and on this run
it dropped none, because none was lost.

*Results: `judge-0137Z-shots/judge-results.json`. Pictures: `judge-0137Z-shots/` (gitignored,
~800 files, on Wy-Blade).*
