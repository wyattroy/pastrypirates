# PREDICTION — written 2026-09-03T07:57Z, BEFORE any measurement

**Watch c2, Wy-Blade. Item: his ruling `INBOX-20260902T225032Z` — delete `about.html`'s
"How it plays" section. Chart row `T-114`.**

His words, verbatim: *"Agree with your rec -- delete "how it plays"*, confirmed by him in the
question UI at 6:52 PM ET — *"That's the whole instruction."*

This note exists so the result cannot be retrofitted. Rule 6's working form.

---

## WHAT I EXPECT, AND WHY

**P1. The section is ONE self-contained `<div class="abtCard">`, lines 167–203.** I have read
those lines: `<div class="abtCard">` at 167, `<h2>How it plays</h2>` at 168, `</div>` at 203, and
the next card (`What the captains are saying`) opens at 205. So the deletion is a clean excision
and does not need any sentence rewritten.

**P2. All four false statements `T-114` names die with the section, and no fifth survives it.**
Located while sizing: fish at `:187`, "flip a coin for a crate" at `:184`, "first baker home wins"
at `:175-176`, "wind sets your sailing budget — cheap with it, dear against it" at `:181-182`.
All four sit between 167 and 203.

**P3. Nothing links INTO the section.** It carries no `id=`, so no anchor can target it. I expect
no nav item, no `href="about.html#..."` anywhere in the repo, and no JS reading `.abtRules`.

**P4. Three images become orphaned** — `about-recipes.jpg`, `about-flippenator.jpg`,
`about-home-island.jpg` — used only here. I will NOT delete them (not my item, and rule: nobody
shrinks or removes an asset without measuring first). I will report them.

**P5. This makes the public site teach LESS but teach nothing FALSE.** The in-game
"How to play" modal still exists and is unaffected, so a person who opens the game still gets
rules. `/rules.html` does not exist yet — his ruling 1-of-4 orders it built — so between this
change and that one, a stranger arriving from Google gets no rules on a public page.

---

## WHAT WOULD PROVE ME WRONG — the falsifiers, named before the result exists

- **F1 kills P1/P2:** any of the four false statements appears OUTSIDE 167–203. Then deleting the
  card is not sufficient and I must say so rather than claim the row is closed.
- **F2 kills P3:** any `#`-anchor, nav entry, sitemap entry or script selector reaches into this
  card. Then this is not a clean excision and I have broken a link.
- **F3 kills P4:** any of the three images is used elsewhere. Then they are not orphaned and my
  report would be wrong.
- **F4 kills P5, and it is the one I most expect to be argued with:** if `about.html` turns out to
  be the ONLY place a person can read the rules without launching the game — e.g. the in-game
  modal is gone, or the About page is what `sitemap.xml` and the game's own links advertise AS the
  rules — then deleting this section makes the site WORSE for a player until `/rules.html` lands,
  and the honest report is "his ruling done, and here is the gap it opens", not "fixed".

**If F4 fires I do the work anyway** — it is his explicit, twice-confirmed instruction — but the
report says the gap out loud and the rules page becomes the next row, not a footnote.

---

## SEQUENCING CONCERN, STATED BEFORE THE WORK, NOT AFTER

He answered 1-of-4 (*build a new `/rules.html`*) and 2-of-4 (*About drops "How it plays"*) thirty
seconds apart. Done in his order, the rules page exists before About loses its copy. Done in mine,
there is a window with neither. I am doing 2 first because it is the watch-sized half and because
what it deletes is false today; I am not treating that as a free choice, and it is in the report.
