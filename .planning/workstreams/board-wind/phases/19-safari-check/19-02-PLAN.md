---
phase: 19-safari-check
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/wind_dot_contract_check.js
  - package.json
autonomous: true
requirements: [WIND-00]

must_haves:
  truths:
    - "A future edit cannot silently reintroduce a BUG-01-class property into the wind-dot region of src/ui/board.js without npm test going red"
    - "A future edit cannot silently draw wind-dot randomness from the game's own RNG stream without npm test going red"
    - "The guard cannot silently pass by scanning nothing: if any windDot symbol exists in src/ui/board.js, the region markers and the required exports must exist too"
    - "Every assertion in the guard has been demonstrated to FAIL against its own synthetic violation before being trusted"
    - "npm test is green on the branch with the new guard wired in and zero determinism fixture changes"
  prohibitions:
    - "The gate must never be answered with the pre-baked tiling-sheet technique the storm rain already uses — sheets are already proven Safari-safe, so a pass earned that way would look like a verdict while teaching nothing about the genuine unknown (D-01)."
  artifacts:
    - path: "scripts/wind_dot_contract_check.js"
      provides: "Standing mechanical guard on the compositor-only contract, the determinism contract, workstream file ownership, and the pure-half math contract"
      contains: "--drill"
    - path: "package.json"
      provides: "The guard wired into npm test"
      contains: "wind_dot_contract_check.js"
  key_links:
    - from: "scripts/wind_dot_contract_check.js"
      to: "src/ui/board.js"
      via: "locates the WIND DOT PROTOTYPE region by its literal begin/end markers and scans only between them"
      pattern: "WIND DOT PROTOTYPE"
    - from: "package.json"
      to: "scripts/wind_dot_contract_check.js"
      via: "npm test script chain, immediately after ui_contract_check.js"
      pattern: "wind_dot_contract_check"
---

<objective>
Build the Wave 0 mechanical guard this phase's own validation strategy commits to, and wire it into
`npm test` — before the wind-dot code it guards exists.

Purpose: `19-VALIDATION.md` Wave 0 Requirements names "a cheap automated no-forbidden-properties
self-check" as a commitment, and `19-RESEARCH.md`'s Validation Architecture explains why: the single
hardest constraint in this phase is the compositor-only house rule learned expensively through
BUG-01, and there is no browser test framework to catch a regression. Writing the guard first means
every later task in the phase lands against a live gate rather than a promise, and the guard's own
`--drill` mode proves each assertion actually bites before anyone trusts it.

Output: `scripts/wind_dot_contract_check.js`, wired into `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/board-wind/phases/19-safari-check/19-CONTEXT.md
@.planning/workstreams/board-wind/phases/19-safari-check/19-PATTERNS.md
@.planning/workstreams/board-wind/phases/19-safari-check/19-VALIDATION.md
@scripts/ui_contract_check.js
@scripts/module_graph_check.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/wind_dot_contract_check.js with six assertions and a --drill mode</name>
  <files>scripts/wind_dot_contract_check.js</files>
  <read_first>
    - scripts/ui_contract_check.js (the exact shape to mirror: shebang, a header naming what is gated and why, one PASS/FAIL line per assertion, every assertion run before exit so one run reports every problem, named failures with file:line, self-exclusion of scripts/, and the `--drill` red-proof harness that fixtures a synthetic violation into a temp tree)
    - scripts/module_graph_check.js:41-46 (the established convention for an assertion that must tolerate a not-yet-existing target by construction)
    - index.html:97-116 (the written BUG-01 post-mortem — it names, verbatim, the properties that dragged Safari to ~2fps, and is the source of the forbidden list)
    - src/ui/board.js:285-356 (stormLayerSpecs / buildStormLayers — what a compliant seeded decoration looks like, and the DEMO_RAIN_SEED fallback)
    - .planning/workstreams/board-wind/phases/19-safari-check/19-PATTERNS.md (§Shared Patterns and §Small standalone self-check script)
  </read_first>
  <action>
    Create `scripts/wind_dot_contract_check.js` following `scripts/ui_contract_check.js`'s structure
    exactly. Its header must state what is gated and why, naming BUG-01, D-01, D-12 and D-14, and
    must record that it was written BEFORE the code it guards so its assertions are deliberately
    absence-tolerant until the region appears.

    Define the two region markers as module constants, spelled ASCII-only and byte-exactly as:
    `/* ===== WIND DOT PROTOTYPE (Phase 19 / WIND-00) BEGIN ===== */` and
    `/* ===== WIND DOT PROTOTYPE (Phase 19 / WIND-00) END ===== */`.

    Region extraction, in this order and no other: locate both markers on the RAW file text (they
    are themselves block comments, so they must be found before any comment handling), take the
    text strictly between them, then strip `//` line comments and `/* */` block comments from that
    slice before any substring scan. Say in the header why stripping is required here even though
    `ui_contract_check.js` deliberately does not strip: the region's own explanatory comments will
    legitimately reference the properties BUG-01 was caused by, and an unstripped scan would flag
    the documentation that exists to prevent the mistake. Report failures with a line number
    computed against the original file, not the stripped slice.

    Six assertions, all run before exit, one PASS/FAIL line each:

    1. **Region integrity (the anti-silent-pass assertion).** If `src/ui/board.js` contains the
       substring `windDot` anywhere, then: both markers appear exactly once each, BEGIN before END,
       and the file exports both `windDotSpecs` and `windDotFrame`. If `windDot` does not appear at
       all, PASS with an explicit `(region not present yet)` note printed on the PASS line so a
       trivially-passing run is never mistaken for a real one. This assertion is what makes
       assertions 2, 3, 5 and 6 mandatory the moment the prototype lands.
    2. **Compositor-only contract (BUG-01).** Within the stripped region, none of these substrings
       appear: `mask`, `blur(`, `gradient`, `filter:`, `box-shadow`, `backdrop`. Failure text must
       name the offending substring and quote the source line.
    3. **Determinism contract (D-12).** Within the stripped region, neither `.r()` nor `Math.random`
       appears, and `mulberry32(` appears at least once. Failure text must cite D-12 and point at
       `src/ui/board.js:299-302`.
    4. **Workstream file ownership (D-14).** No `.js` file under `src/` other than `src/ui/board.js`
       contains `windDot`, and `index.html` contains none of `windDot`, `windHud`, `wdot`. Exclude
       `scripts/` from the scan (this file necessarily contains those tokens). Failure text must
       cite D-14 and the concurrent Phase 18 edits to `index.html`.
    5. **Off by default (D-08 / D-10).** `src/ui/board.js` matches the regex
       `WIND_PROTOTYPE_ENABLED_DEFAULT\s*=\s*false` — the source default is off, so a normal build
       shows a player nothing.
    6. **Pure-half math contract.** Dynamically `import()` `src/ui/board.js` (Node can already load
       it — no DOM is touched at module top level) and assert, when assertion 1 reported the region
       present:
       - `windDotSpecs(12345,10)` deep-equals a second call with the same arguments (reproducible);
       - `windDotSpecs(12345,10)` does NOT deep-equal `windDotSpecs(999,10)` (the seed matters);
       - `windDotSpecs(1,-1).length === 0` and `windDotSpecs(1,0).length === 0`;
       - `windDotSpecs(1,100).length === 100` and `windDotSpecs(1,101).length === 100`;
       - `windDotSpecs(null,10).length === 10` and does not throw (the seedless demo-board fallback);
       - for 200 sampled times across at least two full cycles, `windDotFrame(spec,t,600,600)`
         returns `opacity` within `[0,1]` inclusive and `x`,`y` both finite.
       Skip with an explicit `(exports not present yet)` note when assertion 1 reported absence.

    Add a `--drill` mode mirroring `ui_contract_check.js`'s: fixture a synthetic violation of each
    assertion into a temp tree, confirm each assertion fails against its own violation, plus one
    negative control per assertion confirming a compliant fixture passes. Print
    `ALL 6 ASSERTIONS RED-PROOF DRILLED OK` and exit 0 only when every drill behaved. Include a
    drill case proving the comment-stripping works both ways: a forbidden substring inside a region
    comment must NOT fail assertion 2, and the same substring in region code MUST fail it.

    No flags other than `--drill`. Exit 0 on pass, 1 on any failure, printing a named reason with
    file:line per failure.
  </action>
  <acceptance_criteria>
    - `node scripts/wind_dot_contract_check.js` exits 0 against the current tree and prints exactly six PASS lines.
    - Assertion 1's PASS line contains the literal `(region not present yet)` while `src/ui/board.js` has no `windDot` symbol.
    - `node scripts/wind_dot_contract_check.js --drill` exits 0 and prints `ALL 6 ASSERTIONS RED-PROOF DRILLED OK`.
    - `grep -c 'BEGIN =====' scripts/wind_dot_contract_check.js` is at least 1 and the marker constant is byte-identical to the string quoted in the action.
    - `node -e "process.exit(/WIND DOT PROTOTYPE \(Phase 19 \/ WIND-00\) (BEGIN|END) =====/.test(require('fs').readFileSync('scripts/wind_dot_contract_check.js','utf8'))?0:1)"` exits 0.
    - The script's header names BUG-01, D-01, D-12 and D-14 by identifier.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/wind_dot_contract_check.js &amp;&amp; node scripts/wind_dot_contract_check.js --drill</automated>
  </verify>
  <done>Six assertions pass against the current tree, each has been demonstrated to fail against its own synthetic violation, and a trivially-passing run announces itself as such.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into npm test</name>
  <files>package.json</files>
  <precondition>`npm test` is green on this branch before the new script is added, so any new red is unambiguously attributable to this change.</precondition>
  <read_first>
    - package.json (the `test` script chain — note the ordering convention and that `ui_contract_check.js` sits immediately after `module_graph_check.js`)
    - scripts/ui_contract_check.js:13-25 (the header's own account of WHY it was only wired into `npm test` once its assertions were expected to pass — the same reasoning applies here and the opposite conclusion holds: this guard's assertions pass from the moment it is written)
  </read_first>
  <action>
    Add `node scripts/wind_dot_contract_check.js` to the `test` script in `package.json`,
    immediately after `node scripts/ui_contract_check.js` and before `node scripts/no_undef_check.js`,
    matching the existing `&amp;&amp;`-chained style with no other reordering of the chain.

    Run the full suite and confirm it is green with zero changes to any file under `scripts/fixtures`
    — the 31-seed determinism corpus must be untouched, which is this phase's standing proof that
    D-11 and D-12 held.
  </action>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `git status --porcelain scripts/fixtures` produces no output (zero fixture changes).
    - `node -e "const s=require('./package.json').scripts.test.split('&amp;&amp;').map(x=>x.trim()); const i=s.findIndex(x=>x.includes('ui_contract_check')), j=s.findIndex(x=>x.includes('wind_dot_contract_check')); process.exit(j===i+1?0:1)"` exits 0.
    - `git diff --name-only` lists exactly `package.json` and `scripts/wind_dot_contract_check.js` for this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm test &amp;&amp; test -z "$(git status --porcelain scripts/fixtures)"</automated>
  </verify>
  <done>The guard runs on every `npm test`, positioned immediately after `ui_contract_check.js`, and the determinism corpus is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits → the wind-dot region of `src/ui/board.js` | The only boundary this plan defends; everything crossing it is checked mechanically rather than by memory |
| shared game RNG stream → decoration | Assertion 3 is the mechanical half of this phase's determinism defence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Tampering (of shared multiplayer state) | wind-dot randomness source in `src/ui/board.js` | high | mitigate | Assertion 3 fails `npm test` on any RNG accessor or unseeded random inside the region; the 31-seed determinism suite is the independent backstop. |
| T-19-03 | Denial of service (self-inflicted) | the always-on decorative layer over the board | high | mitigate | Assertion 2 mechanically forbids the exact property class that caused BUG-01's ~2fps Safari collapse; assertion 5 keeps the whole prototype off by default so a regression cannot reach a player. |
| T-19-04 | Tampering | concurrent Phase 18 edits to `index.html` | medium | mitigate | Assertion 4 fails if any wind-dot symbol appears in `index.html`, enforcing D-14's all-inline-styling route and removing the cross-workstream collision entirely. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no packages are installed in this phase (`19-RESEARCH.md` §Package Legitimacy Audit). `package.json` gains one `node scripts/...` invocation and no dependency. |
</threat_model>

<verification>
- `node scripts/wind_dot_contract_check.js` exits 0 with six PASS lines.
- `node scripts/wind_dot_contract_check.js --drill` exits 0 — every assertion demonstrably bites.
- `npm test` exits 0 with zero changes under `scripts/fixtures`.
</verification>

<success_criteria>
The compositor-only contract, the determinism contract, the workstream ownership rule and the
off-by-default rule are all machine-checked on every `npm test`, and the guard announces loudly when
it is scanning nothing yet.
</success_criteria>

## Artifacts this phase produces

Created by **this plan**:

| Artifact | Kind |
|---|---|
| `scripts/wind_dot_contract_check.js` | new file |
| `WIND_DOT_BEGIN_MARKER` / `WIND_DOT_END_MARKER` (module constants in that script) | new constants |
| `node scripts/wind_dot_contract_check.js` entry in `package.json` `scripts.test` | new test-chain entry |

Referenced but created by later plans (excluded from drift verification — they do not exist yet):
the `WIND DOT PROTOTYPE` region markers, `WIND_PROTOTYPE_ENABLED_DEFAULT`, `windDotSpecs`,
`windDotFrame` and every other symbol listed in 19-01's "Artifacts this phase produces" table.

<output>
Create `.planning/workstreams/board-wind/phases/19-safari-check/19-02-SUMMARY.md` when done
</output>
