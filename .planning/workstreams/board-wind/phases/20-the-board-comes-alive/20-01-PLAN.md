---
phase: 20-the-board-comes-alive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/board.js
  - src/ui/panel.js
  - scripts/wind_dot_contract_check.js
autonomous: true
requirements: [WIND-01]
must_haves:
  truths:
    - "A player who opens the game with no URL flag and no localStorage key sees drifting wind dots on an ordinary non-storm turn (D-06)."
    - "When a storm arrives the dots fade out over roughly a second and fade back when it passes; the board never carries both effects at full strength (D-01)."
    - "When the wind changes direction the dots fade out as the new round line types, the layer re-aims while nothing is visible, and a fresh field fades in once the line lands — the whole field never visibly swivels in place (D-02)."
    - "The measuring rig is gone: no bottom-right panel, no 0-100 dial, no smoothness readout, no will-change toggle, no end-of-voyage frame summary (D-06)."
    - "`prefers-reduced-motion` players still see dots, held still, on screen — never nothing (D-23)."
    - "`npm test` is green in the same commit as every source change that trips a guard rule (D-06)."
  artifacts:
    - "src/ui/board.js — the shipped wind layer between renamed BEGIN/END markers, with the two-factor opacity envelope and the deferred re-aim"
    - "src/ui/panel.js — one import and one line attaching the reveal-completion hook"
    - "scripts/wind_dot_contract_check.js — markers re-pointed at the shipped region, the off-by-default assertion retired, a new assertion 7 covering the fade/defer contract"
  key_links:
    - "render() -> windDotsTick(angle, storming): the wind direction and storm state are already in hand at exactly the moment the round changes (src/ui/board.js:1341-1350)"
    - "panel() -> revealDone.then(windLayerApplyPendingDirection): the one seam every narration line on every client passes through, host and guest alike"
    - "scripts/wind_dot_contract_check.js BEGIN_MARKER/END_MARKER <-> the in-source marker comments: byte-exact, must move together"
  prohibitions:
    - statement: "Nothing in this plan touches src/engine/index.js or changes what it emits (D-21)."
      status: open
    - statement: "All decoration randomness comes from a private mulberry32 seeded from the game seed, never the shared game RNG (D-22)."
      status: open
    - statement: "Animation is compositor-only — transform and opacity. No masks, no filters, no live gradients, no blur over the board (D-24)."
      status: open
    - statement: "Animated layers are HTML over the board, never SVG children (D-25)."
      status: open
    - statement: "Every new animation carries a prefers-reduced-motion branch (D-23)."
      status: open
    - statement: "No wind-dot-namespaced symbol may appear in any src/ file other than src/ui/board.js — assertion 4 of the guard enforces this and must not be weakened to accommodate the D-02 hook."
      status: open
---

<objective>
Ship the wind dots always-on and prove Phase 20's one genuinely new cross-file coupling end to end: a
wind direction change fades the dot field out while the round line types, re-aims it unseen, and
fades it back in once the line lands (D-02) — with the storm fade (D-01) sharing the same single
opacity authority, the Phase 19 measuring rig deleted (D-06), and the mechanical guard re-pointed at
the shipped code in the same commit so `npm test` never goes red for a reason unrelated to a defect.

Purpose: this is the phase's tracer. Every other plan extends code whose shape is already proven
(seeded specs, pure frame functions, shared host/guest builders). Only this one invents a handshake
between `src/ui/board.js` and `src/ui/panel.js` that has no precedent in this codebase, and only this
one has to retire a guard assertion that a locked decision permanently invalidates. Proving both on
the first commit means an architectural dead-end costs one commit, not ten.

Output: an always-on, storm-aware, direction-change-aware wind layer; a retired off-by-default
assertion; a new assertion 7 covering the fade and defer contract; `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-CONTEXT.md
@.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-PATTERNS.md
@.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-VALIDATION.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One direction change, end to end — dots ship always-on, fade out as the line types, re-aim unseen, fade back in</name>
  <files>src/ui/board.js, src/ui/panel.js, scripts/wind_dot_contract_check.js</files>

  <read_first>
    - `src/ui/board.js:429-470` — the prototype header, `WIND_PROTOTYPE_ENABLED_DEFAULT`, `windPrototypeEnabled()`, and the constant block. This is what "always-on" removes.
    - `src/ui/board.js:497-564` — `windDotSpecs` and `windDotFrame`, including the quarter-cosine fade envelope at lines 548-557 that Task 1 extracts into a shared helper.
    - `src/ui/board.js:633-696` — the module-state `let` block and `windEnsureLayer()`; the `.wlayer` element is the one thing that carries the rotation and will now also carry the layer opacity.
    - `src/ui/board.js:887-925` — `windDotLoop`, `startWindDots`, `stopWindDots`. The loop is where the opacity envelope is stepped each frame.
    - `src/ui/board.js:1063-1080` — `windDotsTick(angle)` as it exists today, including its own comment stating that a direction change re-aims every dot with no restart. That behaviour is the bug D-02 corrects.
    - `src/ui/board.js:1337-1351` — the `render()` wind block. `storming` is already computed at line 1341 and `angle` at the `windDotsTick` call on line 1350; both are already in hand.
    - `src/ui/panel.js:548-566` — `panel()`'s reveal plumbing. Line 560 builds `revealDone`, line 561 assigns it to `msgEl._revealDone`. This is the one seam every narration line passes through on every client.
    - `src/ui/panel.js:34-46` — the existing import block; `panel.js` already imports four names from `./board.js`.
    - `scripts/wind_dot_contract_check.js:78-90` — `BOARD_REL`, `BEGIN_MARKER`, `END_MARKER`, `TRIGGER`.
    - `scripts/wind_dot_contract_check.js:265-310` — assertion 4 (file ownership, which scans every `src/**/*.js` for the ownership token and is the reason the new export must be named outside the wind-dot namespace) and assertion 5 (off-by-default, which D-06 permanently invalidates).
    - `scripts/wind_dot_contract_check.js:400-432` — `runAll`, where each assertion's PASS/FAIL line is printed and where assertion 7 is added.
    - `20-CONTEXT.md` D-01, D-02, D-06, D-21 through D-26.
  </read_first>

  <behavior>
    - `windFadeEnvelope(0.5, 0.2)` returns 1; `windFadeEnvelope(0, 0.2)` returns 0; `windFadeEnvelope(1, 0.2)` returns 0; every sampled value across u in [0,1) is within [0,1] and finite.
    - `windFadeStep(0, 1, 100, 1000)` returns a value strictly between 0 and 1 and strictly greater than 0; repeated application with dtMs summing past fadeMs reaches exactly 1 and never overshoots.
    - `windFadeStep(1, 0, 100, 1000)` decreases; repeated application floors at exactly 0 and never goes negative.
    - `windLayerOpacity(1, 1)` is 1; `windLayerOpacity(0, 1)` is 0; `windLayerOpacity(1, 0)` is 0; `windLayerOpacity(0.5, 0.5)` is 0.25; every result is within [0,1].
    - The module exports `windLayerApplyPendingDirection` and `windLayerState` as functions.
    - The module no longer exports `WIND_PROTOTYPE_ENABLED_DEFAULT` or `windPrototypeEnabled`.
    - `windDotFrame` still satisfies the existing assertion 6 bounds after the envelope is extracted.
  </behavior>

  <action>
Make the dots ship always-on and replace the instant re-aim with a fade-out / re-aim / fade-in envelope driven from the narration reveal, per D-01, D-02 and D-06. Do all of the following in ONE commit — the guard edits are coupled to the source changes that trip them, and splitting them turns `npm test` red for a non-defect.

1. **Always-on (D-06).** Delete the exported enable constant and the `windPrototypeEnabled()` memoized reader, and remove every call to it (there is exactly one, at the top of `windDotsTick`). Delete the `?wind=1` query read and the `pp_wind_proto` localStorage read together with it. Rewrite the block comment at `src/ui/board.js:429-437` and the file-header note at `src/ui/board.js:34-41` so neither still describes an off-by-default prototype — leave a short note recording that Phase 19's gate passed (see `19-VERDICT.md`) and that this is now shipped, always-on decoration.

2. **Rename the region markers.** Change both in-source marker comments to read `WIND LAYER (Phase 20 / WIND-01..03)` in place of `WIND DOT PROTOTYPE (Phase 19 / WIND-00)`, keeping the surrounding `/* ===== ... BEGIN ===== */` and `/* ===== ... END ===== */` framing byte-for-byte identical in shape. Update `BEGIN_MARKER` and `END_MARKER` in `scripts/wind_dot_contract_check.js` to the new strings in the same commit — the guard matches exact text and will report a missing region otherwise. Widen nothing else: the region keeps its current start and end positions in the file, so later plans add their code inside it.

3. **Extract the fade envelope.** Lift the quarter-cosine fade at `src/ui/board.js:548-557` into a new exported pure function `windFadeEnvelope(u, frac)` returning a multiplier in `[0,1]` for a cycle position `u` and a rise/fall fraction `frac`, and have `windDotFrame` call it in place of its inline branch. Multiply by the existing `PLATEAU` constant at the `windDotFrame` call site, not inside the helper, so the helper stays a pure envelope. Plan 20-06's channel specks reuse this same helper, which is the concrete mechanism by which D-28's "the specks inherit the numbers Wyatt approves" is satisfied.

4. **One opacity authority (D-01 + D-02).** Add module-scope `windStormFactor` and `windDirFactor`, both starting at 1, and their targets. Add exported pure helpers `windFadeStep(current, target, dtMs, fadeMs)` — one linearly-interpolated, clamped step toward the target — and `windLayerOpacity(stormFactor, dirFactor)` — the clamped product. In `windDotLoop`, step both factors by the real frame delta and write `layer.style.opacity` from `windLayerOpacity(...)` exactly once per frame. This is the only place the layer's opacity is ever written; per-dot opacity keeps being written on each dot element and multiplies naturally. Add constants `WIND_STORM_FADE_MS` (1000, D-01's "roughly a second each way") and `WIND_DIR_FADE_MS` (600 as a starting value — this is one of the numbers the D-07 tuning page in plan 20-07 puts under Wyatt's sliders, so declare it beside the other tunables with a comment saying so).

5. **Defer the re-aim (D-02).** Change the signature to `windDotsTick(angle, storming)` and update the single call site in `render()` at `src/ui/board.js:1350` to pass the `storming` boolean already computed at line 1341. Initialise `windAngle` to `null` so the very first tick applies its rotation immediately with no fade (round 1 has no previous heading to change from). On a later tick where `angle` differs from the applied `windAngle`: store it in `windPendingAngle`, record `windPendingSinceMs`, set the direction-factor target to 0, and do NOT write the rotate transform. Set the storm-factor target from `storming` on every tick, independently. Export `windLayerApplyPendingDirection()`, which returns immediately when nothing is pending and otherwise writes the `rotate(pendingAngle + 180deg)` transform on the `.wlayer` element (the same `angle + 180` convention `--slant` uses), promotes the pending angle to `windAngle`, clears the pending state, and sets the direction-factor target back to 1. Export `windLayerState()` returning a plain object snapshot of `{angle, pendingAngle, stormFactor, dirFactor}` so the contract is inspectable headlessly.

6. **Name the export outside the ownership namespace.** Assertion 4 of `scripts/wind_dot_contract_check.js` fails on any occurrence of its ownership token in any `src/**/*.js` file other than `src/ui/board.js`, and `src/ui/panel.js` has to import this hook. `windLayerApplyPendingDirection` and `windLayerState` are chosen deliberately so they fall outside that token — do not rename them into the dot namespace, and do not weaken assertion 4 to accommodate a different name. Add one line to the guard's header comment recording that `src/ui/panel.js` legitimately imports the wind LAYER hook, that this is not an ownership violation, and why the naming carries the rule.

7. **The safety net.** If the pending direction is never applied — the failure mode `narration-two-schedulers-unenforced.md` predicts, where a future change routes a round line through a path that never resolves a reveal — the dots would stay invisible forever with no error. In `windDotLoop`, if `windPendingSinceMs` is set and more than `WIND_PENDING_MAX_MS` (6000) has elapsed, apply the pending direction from the loop itself. This must be a real code path, not a comment.

8. **Wire the narration seam (`src/ui/panel.js`).** Add `windLayerApplyPendingDirection` to the existing `./board.js` import block. In `panel()`, immediately after line 561 assigns `msgEl._revealDone`, and only inside the branch where `msgEl` exists, attach a `.then(windLayerApplyPendingDirection)` continuation to the same `revealDone` promise. Do NOT add a parameter to `flash()`, do NOT wire this in `narrateLastEvent()`, and do NOT touch `src/orchestrator.js`. Record why in a comment: the `newround` narration does not travel through `narrateLastEvent()` at all — it is flashed directly from `src/orchestrator.js:861` and `:885`, and reaches the screen through the broadcast handler calling `showNarration()`, which calls `panel()`. Hooking `panel()` is the one seam that covers the host path, the guest mirror path and both narration schedulers with a single line, and the hook is a no-op on every line where nothing is pending.

9. **Retire assertion 5 and add assertion 7.** Remove `checkOffByDefault`, its regular expression, its `runAll` entry and its `--drill` fixtures from `scripts/wind_dot_contract_check.js`, replacing the header text that explains it with a dated note stating that D-06 reversed the requirement it encoded and that the assertion was retired deliberately rather than left passing over deleted code. In its place add assertion 7, `checkFadeContract`, in the same idiom as `checkPureMath`: dynamically import the module and assert the behaviours listed in this task's behavior block, including that the two removed rig symbols are absent from the module namespace. Give it its own PASS/FAIL line in `runAll` and its own `--drill` fixtures proving it can fail. Do not touch assertions 2, 3 or 4 in this plan — assertion 2's narrowing belongs with the styling change that trips it, in plan 20-04.
  </action>

  <verify>
    <automated>node scripts/wind_dot_contract_check.js; test $? -eq 0</automated>
    <automated>node scripts/wind_dot_contract_check.js --drill; test $? -eq 0</automated>
    <automated>npm test; test $? -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/wind_dot_contract_check.js` exits 0 and prints no line containing `off by default`.
    - `node scripts/wind_dot_contract_check.js` prints a PASS line for the new fade contract assertion, and the region-integrity line does NOT carry the note `(region not present yet)`.
    - `node scripts/wind_dot_contract_check.js --drill` exits 0, proving the new assertion can fail against a synthetic violation.
    - `node -e "import('./src/ui/board.js').then(m=>{const gone=['WIND_PROTOTYPE_ENABLED_DEFAULT','windPrototypeEnabled','windDotsApplyPendingDirection'].filter(k=>k in m);const need=['windLayerApplyPendingDirection','windLayerState','windFadeEnvelope','windFadeStep','windLayerOpacity'].filter(k=>typeof m[k]!=='function');if(gone.length||need.length){console.error({gone,need});process.exit(1)}})"` exits 0.
    - `node -e "import('./src/ui/board.js').then(m=>{const eq=(a,b)=>Math.abs(a-b)<1e-9;if(!eq(m.windLayerOpacity(1,1),1)||!eq(m.windLayerOpacity(0,1),0)||!eq(m.windLayerOpacity(1,0),0)||!eq(m.windLayerOpacity(0.5,0.5),0.25))process.exit(1);if(!eq(m.windFadeEnvelope(0.5,0.2),1)||!eq(m.windFadeEnvelope(0,0.2),0))process.exit(1);let v=0;for(let i=0;i<40;i++)v=m.windFadeStep(v,1,100,1000);if(!eq(v,1))process.exit(1);let w=1;for(let i=0;i<40;i++)w=m.windFadeStep(w,0,100,1000);if(!eq(w,0))process.exit(1)})"` exits 0.
    - `npm test` exits 0 with every chained script reporting PASS.
    - `src/ui/panel.js` contains exactly one call to `windLayerApplyPendingDirection`, and `src/orchestrator.js` is unchanged by this task (`git diff --name-only HEAD` lists only `src/ui/board.js`, `src/ui/panel.js` and `scripts/wind_dot_contract_check.js`).
    - `git diff --stat HEAD -- src/engine/index.js` reports no change (D-21).
    - `windDotsTick` has arity 2 and is called with two arguments from `render()`: `grep -c "windDotsTick(angle, *storming)" src/ui/board.js` returns 1.
  </acceptance_criteria>

  <reversibility rating="reversible">The fade is a layer-level opacity envelope over the existing rotation logic; reverting restores the instant re-aim without touching any other subsystem. D-02 rates this reversible itself.</reversibility>

  <done>A player with no URL flag sees drifting dots. A storm fades them out and back. A wind direction change fades them out while the round line types, re-aims the layer unseen, and fades a fresh field in once the line lands — the field never swivels in place. The guard's off-by-default assertion is gone by decision, a fade-contract assertion stands in its place, and `npm test` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the measuring rig — the panel, the dial, the readout, the meter, the will-change toggle and the end-of-voyage summary</name>
  <files>src/ui/board.js</files>

  <read_first>
    - `src/ui/board.js:471-495` — the `WIND_METER_*` constants and the preallocated histogram plus its counters.
    - `src/ui/board.js:698-800` — `windHistMedian`, `windMeterSample`, `windMeterReset`, `windMeterSummary`, `windFormatElapsed`.
    - `src/ui/board.js:820-840` — the end-of-voyage summary block that calls `windMeterSummary()` and `windFormatElapsed()`.
    - `src/ui/board.js:887-925` — `windDotLoop`, whose sampling half and readout-throttle half both go, and whose transform half and reduced-motion behaviour both stay.
    - `src/ui/board.js:927-1061` — `buildWindHud` and `windSetDotCount`, both deleted whole.
    - `src/ui/board.js:566-631` — `buildWindDots`. Its `requestAnimationFrame` initial-frame paint at lines 617-630 exists because of the Phase 19 reduced-motion bug and must survive this task intact; its `windWillChangeOn` reads are the only part that changes.
    - `19-VERDICT.md` — what the rig measured and why nothing further is owed to it.
    - `20-CONTEXT.md` D-06 and D-23.
  </read_first>

  <action>
Remove Phase 19's measuring instrumentation now that its verdict is recorded, per D-06. Delete, in `src/ui/board.js` only:

- `buildWindHud()` in full, its call from `windDotsTick`, and every element it created — the fixed bottom-right panel, the on/off switch, the range input, the plus/minus/reset step buttons, the numeric readout beside the slider, the live smoothness readout and the will-change toggle button.
- `windSetDotCount()` in full, and the module-scope `windDotsOn`, `windDotCount` (replaced by the fixed shipped density) and `windWillChangeOn` state. Where `buildWindDots` currently inherits the will-change hint onto newly created dots, drop the hint entirely rather than hard-coding it on — Phase 19 shipped it defaulting off and never measured a benefit.
- The whole frame-timing meter: `windHistMedian`, `windMeterSample`, `windMeterReset`, `windMeterSummary`, `windFormatElapsed`, the preallocated histogram and every counter that feeds them, the `WIND_METER_*` constant block, the `WIND_READOUT_MS` throttle and `windLastReadoutMs`, and `windDocVisible()` — which exists solely so the meter could discard backgrounded frames and has no other consumer.
- The end-of-voyage frame summary block that renders the meter's plain-English lines into the stats panel.
- The `windDotLoop` branches that fed the meter and the readout. The loop keeps its frame delta (the fade envelope from Task 1 needs it), its transform-writing branch, its reduced-motion skip and its unconditional re-arm.

Replace the dial's live count with a single module constant at D-02's target density. Name it `WIND_DOT_COUNT` and set it to 8, sitting mid-range in Wyatt's stated 5-10 — it is one of the values plan 20-07's tuning page puts under his sliders, so declare it beside the other tunables with a comment saying so. `WIND_DOT_MAX` stays: assertion 6 of the guard asserts the clamp at 100 and Phase 19's verdict says the whole range is in reserve.

Two things D-06 names explicitly that must NOT be removed: the `prefers-reduced-motion` branch, and `buildWindDots`'s `requestAnimationFrame` initial-frame paint. The paint is the fix for the Phase 19 bug where reduced-motion players saw no dots at all rather than dots holding still, because a dot whose transform is never written sits outside the clipped layer. Leave its explanatory comment in place; it is the only record of why that call exists.

Update the region's header comment so it describes what the region now contains rather than a tracer with an on-screen panel.
  </action>

  <verify>
    <automated>node scripts/wind_dot_contract_check.js; test $? -eq 0</automated>
    <automated>npm test; test $? -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "import('./src/ui/board.js').then(m=>{const gone=['buildWindHud','windSetDotCount','windMeterSample','windMeterReset','windMeterSummary'].filter(k=>k in m);if(gone.length){console.error(gone);process.exit(1)}})"` exits 0.
    - `node -e "import('./src/ui/board.js').then(m=>{if(typeof m.buildWindDots!=='function'||typeof m.windDotLoop!=='function')process.exit(1)})"` exits 0 — the dot pool builder and the shared loop survive.
    - `src/ui/board.js` still contains the reduced-motion media query read: `grep -c 'prefers-reduced-motion' src/ui/board.js` returns at least 1.
    - `src/ui/board.js` still contains `buildWindDots`'s initial-frame paint: `grep -c 'requestAnimationFrame' src/ui/board.js` returns at least 2 (the initial-frame paint and the loop's re-arm).
    - No element id from the deleted rig survives anywhere under `src/`: `grep -rc 'windHud\|windDial\|windReadout\|windSwitch\|windWillChange' src/ | grep -v ':0$'` produces no output.
    - `node scripts/wind_dot_contract_check.js` exits 0 and its region-integrity line does not carry `(region not present yet)`.
    - `npm test` exits 0.
    - `git diff --stat HEAD -- src/engine/index.js index.html` reports no change.
  </acceptance_criteria>

  <done>The bottom-right panel, the dial, the readout, the frame meter and the end-of-voyage summary are gone from the source, not merely hidden. The dots run at a fixed shipped density with no player-facing control. Reduced-motion players still see dots holding still. `npm test` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser -> guest browser | Presentation state is rendered locally on each client from the same seeded inputs; no new payload crosses the wire in this plan |
| game RNG stream <-> decoration RNG stream | The determinism fixtures depend on the game stream being untouched by decoration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | `src/ui/board.js` wind layer | high | mitigate | Assertion 3 of `scripts/wind_dot_contract_check.js` mechanically bans the shared game RNG and `Math.random` inside the region and requires a private `mulberry32` — an accidental draw would invalidate all 31 determinism fixtures (D-21/D-22) |
| T-20-02 | Spoofing | host/guest presentation | medium | mitigate | The `panel()` reveal seam fires on every client including guests, so a guest's dot layer re-aims on the same signal the host's does rather than drifting into a permanently faded state |
| T-20-03 | Denial of Service | board compositing in Safari | medium | mitigate | Compositor-only `transform`/`opacity` writes (D-24), HTML over the board rather than SVG children (D-25), and Phase 19's measured verdict of smooth at 100 dots (`19-VERDICT.md`) |
| T-20-04 | Repudiation | `scripts/wind_dot_contract_check.js` | low | mitigate | The retired assertion is replaced by a dated header note recording the decision, not silently deleted, and a new assertion takes its slot so the guard cannot pass over absent code |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No packages are installed by this plan or this phase; `20-RESEARCH.md` records the audit as not applicable |
</threat_model>

<verification>
- `npm test` exits 0 after each task's commit.
- `node scripts/wind_dot_contract_check.js` reports a PASS for every assertion with no `(region not present yet)` note.
- `node scripts/wind_dot_contract_check.js --drill` exits 0.
- `git diff --stat` against the phase's base commit lists only `src/ui/board.js`, `src/ui/panel.js` and `scripts/wind_dot_contract_check.js`.
</verification>

<success_criteria>
- The dots render for a player who passes no URL flag and sets no localStorage key.
- A storm arrival fades the dot layer toward zero and its departure fades it back, over roughly a second each way.
- A wind direction change defers the layer rotation until the round line has finished typing, and the layer opacity is at or near zero for the whole of that window.
- A pending direction that is never signalled is applied by the loop within six seconds rather than leaving the layer blank.
- The Phase 19 measuring rig has no surviving symbol, element id or call site.
- The guard's off-by-default assertion is retired by decision and a fade-contract assertion stands in its place.
</success_criteria>

<output>
Create `.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Symbols and files created or renamed by THIS plan (the source-grounding pass must treat these as new, not as drift):

**New exports in `src/ui/board.js`:** `windFadeEnvelope`, `windFadeStep`, `windLayerOpacity`, `windLayerApplyPendingDirection`, `windLayerState`

**Changed signatures:** `windDotsTick(angle, storming)`

**New constants in `src/ui/board.js`:** `WIND_STORM_FADE_MS`, `WIND_DIR_FADE_MS`, `WIND_PENDING_MAX_MS`, `WIND_DOT_COUNT`

**New module state in `src/ui/board.js`:** `windStormFactor`, `windDirFactor`, `windPendingAngle`, `windPendingSinceMs`

**Renamed source markers:** `/* ===== WIND LAYER (Phase 20 / WIND-01..03) BEGIN ===== */` and its matching END marker, replacing the Phase 19 prototype markers

**New in `scripts/wind_dot_contract_check.js`:** `checkFadeContract` (assertion 7) and its drill fixtures; `BEGIN_MARKER` / `END_MARKER` re-pointed at the new marker text

**Deleted from `src/ui/board.js`:** `WIND_PROTOTYPE_ENABLED_DEFAULT`, `windPrototypeEnabled`, `buildWindHud`, `windSetDotCount`, `windMeterSample`, `windMeterReset`, `windMeterSummary`, `windHistMedian`, `windFormatElapsed`, `windDocVisible`, the `WIND_METER_*` constants, `WIND_READOUT_MS`

**Deleted from `scripts/wind_dot_contract_check.js`:** `checkOffByDefault` and `ENABLED_DEFAULT_RE`

**Note on a name deliberately NOT used:** `20-RESEARCH.md` proposes `windDotsApplyPendingDirection` for the D-02 hook. That name carries assertion 4's ownership token, and `src/ui/panel.js` must import the hook — so it would fail `npm test` the moment the import lands. `windLayerApplyPendingDirection` is used instead.
