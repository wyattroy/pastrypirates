---
phase: 20-the-board-comes-alive
plan: 07
type: execute
wave: 5
depends_on: ["20-06"]
files_modified:
  - src/ui/board.js
  - scripts/wind_dot_contract_check.js
  - tune-wind.html
autonomous: false
requirements: [WIND-01, WIND-02, WIND-03]
must_haves:
  truths:
    - "Wyatt can move a slider and watch the real shipped code change on screen — the page imports the actual functions from src/ui/board.js, not a reimplementation (D-07)."
    - "One sitting approves the whole moving board: dot look and fade timings, channel speck density, and whirlpool rotation speed (D-29)."
    - "The numbers he lands on become the shipped defaults, transferred from the page's own paste-ready readout rather than retyped (D-07)."
    - "He was sent the URL with its port in the same message that asked him to look at it."
    - "Nothing in WIND-01 was declared finished before he signed off, and WIND-02, WIND-03, WIND-04 and WIND-05 were never blocked waiting for him (D-28)."
    - "Every headless browser and local server started for this plan was killed before the plan closed."
  artifacts:
    - "src/ui/board.js — WIND_TUNING, the one live object every tunable is read from, and windReadTuning's paste-ready snapshot"
    - "tune-wind.html — the throwaway harness with three slider groups, importing the real modules and rendering against a real round board"
    - "scripts/wind_dot_contract_check.js — the layer assertion extended to cover the tuning object's shape and bounds"
  key_links:
    - "tune-wind.html -> ./src/ui/board.js — the real windDotSpecs, windDotFrame, rimSpeckSpecs, rimSpeckFrame, rimArcWaypoints and whirlDurationSec, so approval lands on what ships"
    - "tune-wind.html -> ./src/engine/index.js — a real Game built from roundCfg gives real rimCellInfo and rimHead, so the speck sliders act on real arc geometry rather than an invented ring"
    - "WIND_TUNING <-> every reading site in the wind region — one object, so a number Wyatt lands on cannot be applied in one place and missed in another"
  prohibitions:
    - statement: "The page must import the real modules from src/. lab.html is the wrong analog — it is a 121KB pre-refactor standalone that does not load src/ui/board.js at all, and using it would have Wyatt approving code that is not what ships (D-07)."
      status: open
    - statement: "The URL, with its port, goes in the same message that asks Wyatt to look at the page. He should never have to scroll back or guess."
      status: open
    - statement: "Every headless Chrome and every local server started here is killed in the same session it is started. This has heated his machine twice, and 'at the end of the session' has already proven too late once."
      status: open
    - statement: "The tuning gate covers the dots, the speck density and the whirlpool speed. It does not gate WIND-02, WIND-03, WIND-04 or WIND-05, all of which ship before it (D-28)."
      status: open
    - statement: "index.html is never touched, and nothing that identifies the live site is added to or copied out of this repo."
      status: open
    - statement: "Nothing in this plan touches src/engine/index.js or changes what it emits; the page constructs a Game and reads it (D-21)."
      status: open
---

<objective>
Put the whole moving board under Wyatt's hands in one sitting: a throwaway page driving the real
shipped functions, with three groups of sliders — the dots and their fade timings, the channel speck
density, and the whirlpool rotation speed — then take the numbers he lands on and make them the
shipped defaults.

Purpose: two of these numbers do not exist anywhere yet. D-08 never states a speck density and D-09
locks a range rather than a value, so somebody has to choose them, and the only person who can is
the one who will look at the board. D-07 makes his approval a hard gate on WIND-01, and D-29 widens
it to all three groups so this is one approval round instead of three. Everything else in the phase
has already shipped by the time he is asked, which is exactly what D-28 asked for — he is the
bottleneck for the dots' final numbers and for nothing else.

Output: `WIND_TUNING`, `windReadTuning`, `tune-wind.html`, Wyatt's approved numbers as the shipped
defaults, and no processes left running.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-CONTEXT.md
@.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-PATTERNS.md
@.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-06-SUMMARY.md
@docs/DRIVING-THE-GAME.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One live tuning object, and a page that drives the real code with it</name>
  <files>src/ui/board.js, scripts/wind_dot_contract_check.js, tune-wind.html</files>

  <precondition>`python3` is on PATH and can serve the repo root — `python3 -m http.server` is the mechanism `docs/DRIVING-THE-GAME.md` uses throughout, and the page loads ES modules so it cannot be opened from the filesystem directly.</precondition>

  <read_first>
    - `src/ui/board.js` — every constant introduced across plans 20-01 through 20-06 that carries a comment saying it goes under Wyatt's sliders. Collect them before writing anything; the list is the tuning object's shape.
    - `src/ui/board.js:1560-1572` — `seedIdleGameState`, which builds a real round-board `Game` in three lines. The page copies this exactly rather than inventing a board.
    - `src/engine/index.js:52-58` — the `Game` constructor's parameters, and `src/engine/index.js:813-823` — `roundCfg`, which takes the strategy array and returns the 15-by-15 round-board config. Read-only under D-21.
    - `src/ui/board.js` — the exported pure functions the page must import: the dot spec and frame pair, the speck waypoint, spec and frame trio, the whirlpool duration function, and the shared fade envelope.
    - `scripts/wind_dot_contract_check.js` — the layer and rim-flow assertions, which gain the tuning object's shape and bounds checks.
    - `20-CONTEXT.md` D-07, D-28, D-29, and the Claude's Discretion note on whether the page is deleted or left uncommitted.
    - `20-RESEARCH.md` §Open Questions item 1 — why the live-setter pattern is recommended over preview-and-hand-copy.
    - `docs/DRIVING-THE-GAME.md` — required reading before serving or driving anything in a browser.
  </read_first>

  <behavior>
    - `src/ui/board.js` exports a `WIND_TUNING` object whose keys cover all three groups: the dot count, the dot size range, the sway period range, the fade fraction, the storm and direction fade durations, the speck count, the speck size range and vanish fraction, and the whirlpool duration range.
    - Every wind-region reading site takes its value from that object; no tunable is read from a second place.
    - `windReadTuning()` returns a plain snapshot object with the same keys and the current values.
    - Mutating a value on `WIND_TUNING` changes what the pure frame functions return on the next call, with no rebuild and no reload.
    - The defaults in the object reproduce, exactly, the behaviour shipped at the end of plan 20-06 — this task changes where numbers live, never what they are.
  </behavior>

  <action>
**Gather the tunables (`src/ui/board.js`).** Plans 20-01 through 20-06 each declared their tunable constants with a comment saying they go under Wyatt's sliders. Collect all of them into a single exported object, `WIND_TUNING`, whose initial values are exactly the values those constants hold today — this task moves numbers, it does not change any. Update every reading site in the wind region to read from the object. Add an exported `windReadTuning()` returning a plain snapshot, and write a comment above the object stating that it is deliberately mutable because a throwaway tuning page drives it live, that the object literal below is the shipped default set, and that this mirrors the live-settable dial Phase 19 already proved works for Wyatt's approval workflow.

One object rather than a set of setters, deliberately: a number Wyatt lands on then cannot be applied in one place and missed in another, and the object literal is itself the paste-ready form the approval produces.

Extend the layer and rim-flow assertions in `scripts/wind_dot_contract_check.js` to assert the object exists, that every expected key is present and numeric, that each range key's minimum is below its maximum, and that the whirlpool range stays inside D-09's 8-to-12 seconds. That last one matters: the slider must not be able to hand back a value the decision does not allow.

**Build `tune-wind.html`** at the repo root. A single standalone page with one `script type="module"` block importing, by relative path, the real functions from `./src/ui/board.js` and `Game` and `roundCfg` from `./src/engine/index.js`. Do not copy any of their logic into the page and do not start from `lab.html` — it is a pre-refactor standalone that does not load `src/ui/board.js` at all, so anything approved through it would be approved on code that is not what ships. That trap is recorded twice, in D-08 of `19-CONTEXT.md` and again in D-07's warning here.

The page needs:

- **A real board.** Build a `Game` the same way `seedIdleGameState` does — the four-strategy array, `roundCfg`, a fixed seed so the page is reproducible between sittings — and take `rimCellInfo`, `rimHead` and the grid size from it. The speck sliders then act on real, unequal arc geometry rather than an invented even ring, which is the whole difference between tuning a density that looks right and one that looks right on a board the engine cannot generate.
- **A board-sized stage** with the same proportions the real board has, carrying a dot layer and a rim-flow layer built the same way the real ones are, and the board art behind them so the specks are judged against the arrows they travel through.
- **Three labelled slider groups**, in this order: the dots, the channel specks, the whirlpools. Every slider writes straight into `WIND_TUNING` and shows its current numeric value beside it. Group one carries the dot count, the size range, the sway period range and — per D-29 — the storm fade and direction fade durations, since those are D-01 and D-02's timings and the Discretion note already put them under this gate. Group two carries the speck count, size range and vanish fraction. Group three carries the whirlpool duration range, bounded to 8 through 12 so a slider cannot produce a value D-09 forbids.
- **A direction-change button and a storm toggle**, so the two fade timings can actually be judged — a fade duration slider is meaningless without something to trigger the fade.
- **A paste-ready readout** that renders `windReadTuning()` as the exact object literal source, updating live, with a copy button. This is what makes Task 3 mechanical rather than a retyping exercise, and retyping a list by hand is how this project lost four of Wyatt's approved copy rewrites once already.
- **A robots meta tag set to noindex**, because the repo root is published: `lab.html` sets the precedent that a dev page lives here and ships, and a dev page in the search index does not.

Do not add anything to `index.html`, do not touch the CNAME file, the robots file or the sitemap, and do not copy this repo anywhere.
  </action>

  <verify>
    <automated>node scripts/wind_dot_contract_check.js; test $? -eq 0</automated>
    <automated>npm test; test $? -eq 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('tune-wind.html','utf8');const need=['./src/ui/board.js','./src/engine/index.js','windDotSpecs','windDotFrame','rimSpeckSpecs','rimSpeckFrame','rimArcWaypoints','whirlDurationSec','WIND_TUNING','noindex'];const miss=need.filter(t=>!s.includes(t));if(miss.length){console.error(miss);process.exit(1)}"</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "import('./src/ui/board.js').then(m=>{const t=m.WIND_TUNING;if(!t||typeof t!=='object')process.exit(1);const keys=Object.keys(t);if(keys.length<12)process.exit(1);if(keys.some(k=>typeof t[k]!=='number'))process.exit(1);if(typeof m.windReadTuning!=='function')process.exit(1)})"` exits 0.
    - The whirlpool range stays inside D-09's bounds: `node -e "import('./src/ui/board.js').then(m=>{const t=m.windReadTuning();const mn=t.whirlMinSec,mx=t.whirlMaxSec;if(!(mn>=8&&mx<=12&&mn<mx))process.exit(1)})"` exits 0.
    - The tuning object actually reaches the frame math: `node -e "import('./src/ui/board.js').then(m=>{const s=m.windDotSpecs(7,1)[0];const a=m.windDotFrame(s,900,600,600);m.WIND_TUNING.swayPeriodMinMs=200;m.WIND_TUNING.swayPeriodMaxMs=260;const b=m.windDotFrame(s,900,600,600);if(Math.abs(a.x-b.x)<1e-9)process.exit(1)})"` exits 0.
    - `tune-wind.html` imports the real modules and carries the noindex meta — asserted by the third automated check above.
    - `tune-wind.html` builds a real round board rather than an invented ring: `grep -c "roundCfg" tune-wind.html` returns at least 1.
    - All three slider groups are present: `grep -c 'type="range"' tune-wind.html` returns at least 10.
    - The page carries a direction-change trigger and a storm toggle, so the two fade timings can be judged.
    - `node scripts/wind_dot_contract_check.js` exits 0 with every assertion PASS, and `node scripts/wind_dot_contract_check.js --drill` exits 0.
    - `npm test` exits 0.
    - `git diff --stat HEAD -- index.html CNAME robots.txt sitemap.xml src/engine/index.js` reports no change.
  </acceptance_criteria>

  <reversibility rating="reversible">Gathering constants into one object is a mechanical move with identical defaults, and the page is throwaway by design. Reverting restores the individual constants.</reversibility>

  <done>Every tunable in the phase lives in one live object with its shipped defaults unchanged, and a page at the repo root drives the real shipped functions against a real round board with three groups of sliders and a paste-ready readout of the current numbers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Wyatt tunes the whole moving board in one sitting — the hard gate on WIND-01</name>

  <precondition>Task 1 is committed, `npm test` is green, and `python3 -m http.server` can bind port 8020 on this machine.</precondition>

  <read_first>
    - `docs/DRIVING-THE-GAME.md` — required before serving or driving anything in a browser. Read §8a's distinction between measuring layout and measuring cost, and note that this task measures neither: it is Wyatt looking at a board.
    - `.claude/CLAUDE.md` — the standing rules on including the link with its port, and on killing every server and headless browser in the same session it is started.
    - `20-CONTEXT.md` D-07, D-28, D-29.
  </read_first>

  <what-built>
    Everything in the phase except the final numbers. The dots ship always-on, fade for storms and
    for direction changes, and each one has its own sway rhythm, size and soft edge. Four whirlpools
    turn clockwise at seeded speeds. Specks ride the trade-wind channel and are swallowed at each
    whirlpool. The round line carries a pastry scent on every direction change. Rim squares in the
    move highlights read as a warning and the landing square carries a ghost of the player's own boat.

    `tune-wind.html` drives the real shipped functions — the same `windDotSpecs`, `windDotFrame`,
    `rimSpeckSpecs`, `rimSpeckFrame` and `whirlDurationSec` the game imports, against a real
    round board built from the real engine config — with three groups of sliders and a paste-ready
    readout of the current numbers.

    Two of these numbers have never existed anywhere: D-08 never stated a speck density, and D-09
    locked a range rather than a value for the whirlpool speed. Both are on the page.
  </what-built>

  <how-to-verify>
    Serve the repo root on port 8020 and give Wyatt the URL in the same message that asks him to look:

        python3 -m http.server 8020

    The page is at **http://localhost:8020/tune-wind.html** — put that exact line, with the port,
    in the message. He should never have to scroll back or guess.

    Ask him to land three groups of numbers in one sitting:

    1. **The dots.** Count, size range, sway rhythm range, and the two fade timings. Use the page's
       direction-change button and storm toggle to judge the fades — the question is whether the
       field dies away and returns without popping, not whether a duration reads well as a number.
       His reference for the whole layer is his own phrase: blowing dust particles, not all rushing
       one way at once.
    2. **The channel speck density.** How many specks make the rim read as flowing without becoming
       a second weather system competing with the dots.
    3. **The whirlpool rotation speed**, inside 8 to 12 seconds per revolution. Slow enough to read
       as a deep ocean drain rather than a spinning icon.

    Three things to put in front of him that are not sliders, because they are taste calls this phase
    made on his behalf and he has not seen them yet:

    - **Dots drift across chat speech bubbles.** The dot layer and the bubble layer declare the same
      stacking and the dots win on document order. That follows from D-05 as written — dots over
      everything — but nobody has looked at it on a real board.
    - **The rim warning square** is a deep current blue with a white stroke and no bounce, deliberately
      the opposite of the friendly amber sail-here square. Colour is his.
    - **The ghost boat's fadedness** on the landing square.

    Then ask whether the tuning page should be kept in the repo or removed. D-07 calls it throwaway
    and explicitly leaves that open; `lab.html` is the precedent for a dev page living at the repo
    root, and the watercolor restyle in a later milestone keeps the same size and shape, so the page
    would still be usable if it stays.

    **Kill the server the moment he has signed off** — `pkill -f http.server` — and confirm nothing
    was left running. Do not leave it up across a reply. He is at the keyboard, on the machine it
    runs on, and this project has twice left processes heating it.
  </how-to-verify>

  <resume-signal>
    Paste the numbers from the page's readout, plus answers on the three taste calls and on whether
    the page stays. Or describe what still feels wrong.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: His numbers become the shipped defaults, and nothing is left running</name>
  <files>src/ui/board.js, tune-wind.html</files>

  <precondition>Wyatt has signed off in Task 2 and the approved numbers are recorded in that checkpoint's response.</precondition>

  <read_first>
    - The checkpoint response from Task 2 — the approved numbers, in the page's own paste-ready form.
    - `src/ui/board.js` — the tuning object introduced in Task 1, whose literal is what changes.
    - `scripts/wind_dot_contract_check.js` — the range assertions, which must still pass against the new values.
    - `20-CONTEXT.md` D-07, D-29, and the Discretion note on the page's fate.
  </read_first>

  <action>
Replace the values in the tuning object with the numbers Wyatt landed on, taken from the page's paste-ready readout rather than retyped from a message. Retyping a list by hand is exactly how four of his approved copy rewrites shipped missing once already, and the readout exists so that cannot happen here.

Change values only. Do not add keys, do not remove keys, and do not restructure the object — anything beyond the numbers is a change he did not approve.

Apply his answers on the three taste calls the checkpoint put in front of him:

- If he wants chat bubbles above the dots, raise the bubble container's stacking rather than lowering the dot layer's — D-05 puts the dots over the board, and a speech bubble is interface rather than board. If he is happy as it is, record that he looked and leave it.
- If he named a different rim warning colour or stroke, change the named constants in `src/ui/flow.js`. Each is a one-line change by construction.
- Same for the ghost boat's opacity.

Act on his answer about the page's fate: if it stays, leave it committed with its noindex meta; if it goes, delete it and say so in the summary. Either is correct under D-07 — what is not correct is leaving the question unanswered.

Then confirm nothing this plan started is still running: kill any lingering local server and any headless browser, and check. Two abandoned probes at 21% CPU each, and 53% CPU across thirteen Chrome processes, are both on this project's record — the second one hours after the rule was written down.

Record in the summary that WIND-01 is now complete, with the note that WIND-02, WIND-03, WIND-04 and WIND-05 all shipped before this gate rather than behind it, which is what D-28 asked for.
  </action>

  <verify>
    <automated>node scripts/wind_dot_contract_check.js; test $? -eq 0</automated>
    <automated>npm test; test $? -eq 0</automated>
    <automated>pgrep -f "http.server" >/dev/null && exit 1; pgrep -f "remote-debugging-port" >/dev/null && exit 1; exit 0</automated>
    <human-check>A full solo voyage, played through, looks the way Wyatt approved on the tuning page.</human-check>
  </verify>

  <acceptance_criteria>
    - The tuning object's keys are unchanged and only its values moved: `git diff HEAD~1 -- src/ui/board.js | grep -c '^[-+].*:' ` is greater than 0, and `node -e "import('./src/ui/board.js').then(m=>{if(Object.keys(m.windReadTuning()).length<12)process.exit(1)})"` exits 0.
    - The approved whirlpool range still satisfies D-09: `node -e "import('./src/ui/board.js').then(m=>{const t=m.windReadTuning();if(!(t.whirlMinSec>=8&&t.whirlMaxSec<=12&&t.whirlMinSec<t.whirlMaxSec))process.exit(1)})"` exits 0.
    - The approved dot count sits in D-02's stated 5-to-10 density band, or the summary records Wyatt's own reason for going outside it.
    - `node scripts/wind_dot_contract_check.js` exits 0 with every assertion PASS and `--drill` exits 0.
    - `npm test` exits 0.
    - No local server and no headless browser is running: `pgrep -f "http.server"` and `pgrep -f "remote-debugging-port"` both return nothing.
    - The page's fate is decided: either `tune-wind.html` exists and contains the noindex meta, or it does not exist and the summary says it was removed at Wyatt's request.
    - `git diff --stat HEAD~2 -- src/engine/index.js CNAME robots.txt sitemap.xml` reports no change.
  </acceptance_criteria>

  <reversibility rating="reversible">A values-only edit to one object literal, plus at most three one-line taste constants. Reverting restores the pre-approval defaults.</reversibility>

  <done>The numbers Wyatt landed on are the shipped defaults, transferred from the page's own readout. His answers on the chat-bubble overlap, the rim warning colour and the ghost boat's fadedness are applied or recorded. The tuning page's fate is decided rather than left open. No server and no browser this plan started is still running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root -> published site | The repo root is published by GitHub Pages, so a page added here is reachable by the public |
| this repo -> any other deploy target | Files that assert this deployment is the live game must never leave |
| local server -> Wyatt's machine | A process started for a checkpoint outlives the tool call that started it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-28 | Spoofing | search indexing of the published site | medium | mitigate | `tune-wind.html` carries a noindex meta tag, so a dev harness at the published root is not offered to search engines as content — the same class of mistake the first preview deploy made with a robots file, caught only by reading the diff |
| T-20-29 | Tampering | the live domain | high | mitigate | Nothing is copied out of this repo by this plan; the CNAME, robots and sitemap files are asserted unchanged. Two sessions have come within one command of taking the live game down by copying a CNAME |
| T-20-30 | Denial of Service | Wyatt's own machine | high | mitigate | The local server is bound for the duration of one checkpoint and killed the moment he signs off, and both `pgrep` checks are acceptance criteria rather than a note. Abandoned probes have heated this machine twice |
| T-20-31 | Repudiation | the approval record | medium | mitigate | The numbers are transferred from the page's own paste-ready readout rather than retyped, and the checkpoint's response is the record of what he approved |
| T-20-32 | Tampering | approved values drifting out of range | medium | mitigate | The guard asserts the whirlpool range stays inside D-09's 8 to 12 seconds and that every range key's minimum is below its maximum, so a slider cannot hand back a value the decision forbids |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No packages are installed by this plan or this phase |
</threat_model>

<verification>
- `node scripts/wind_dot_contract_check.js` exits 0 with every assertion PASS; `--drill` exits 0.
- `npm test` exits 0.
- `pgrep -f "http.server"` and `pgrep -f "remote-debugging-port"` both return nothing.
- `git diff --stat` against the phase base lists only `src/ui/board.js`, `scripts/wind_dot_contract_check.js`, `tune-wind.html` and — only if Wyatt asked for a colour change — `src/ui/flow.js`.
</verification>

<success_criteria>
- Wyatt approved the dots, the speck density and the whirlpool speed in one sitting, against the real shipped code.
- He received the URL with its port in the same message that asked him to look.
- The numbers he landed on are the shipped defaults, transferred rather than retyped.
- The whirlpool range still satisfies D-09.
- The tuning page's fate is decided.
- Nothing this plan started is still running on his machine.
</success_criteria>

<output>
Create `.planning/workstreams/board-wind/phases/20-the-board-comes-alive/20-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New exports in `src/ui/board.js`:** `WIND_TUNING` (a deliberately mutable object holding every tunable in the phase), `windReadTuning()`

**Retired from `src/ui/board.js`:** the individual tunable constants declared across plans 20-01 through 20-06, whose values become `WIND_TUNING`'s defaults. The non-tunable constants — the dot maximum, the speck maximum, the layer stacking, the seed salts, the pending-direction timeout — stay as constants.

**New file:** `tune-wind.html` at the repo root — a throwaway harness importing the real modules, carrying three slider groups, a direction-change trigger, a storm toggle, a paste-ready readout and a noindex meta. Its fate is decided by Wyatt at the checkpoint.

**Changed in `scripts/wind_dot_contract_check.js`:** the layer and rim-flow assertions extended with the tuning object's key presence, numeric types, range ordering and the whirlpool bound.

**Possibly changed in `src/ui/flow.js`:** `RIM_HL_FILL`, `RIM_HL_STROKE` or `RIM_GHOST_OPACITY`, only if Wyatt names different values at the checkpoint.