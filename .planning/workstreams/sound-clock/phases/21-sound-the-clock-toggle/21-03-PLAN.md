---
phase: 21-sound-the-clock-toggle
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/ui/util.js
  - src/orchestrator.js
  - src/ui/panel.js
autonomous: true
requirements: [FIX-02/N-03]

must_haves:
  truths:
    - "The BUG-02 re-arm logic exists in exactly one place — applyTimerOff() in src/ui/util.js — and both the Firebase listener and the local branch reach it through that one function (D-18)"
    - "toggleTimer() no longer depends on a Firebase connection to do anything: with no db and no room it applies the change locally (FIX-02/N-03)"
    - "The pp_timerOff preference is written on every toggle in every mode, not only when Firebase is connected (D-19)"
    - "appState.timerOff is seeded from pp_timerOff at the start of a solo or pass-and-play game, not only a multiplayer one (D-19)"
    - "The #scTimerToggle visibility expression depends only on appState.liveDone — no mode hides it and no mode greys it (D-20)"
    - "src/ui/util.js reaches setClockUI through netHandlers().onSetClockUI(), never by importing src/ui/panel.js — no import cycle, proven by scripts/module_graph_check.js"
    - "src/engine/index.js is byte-identical before and after this plan"
    - statement: "In EACH of solo, pass-and-play and multiplayer: play a full turn, switching the timer OFF mid-turn and then back ON mid-turn, and the turn still completes — the clock stops immediately when switched off and re-arms for the player whose turn is in progress when switched back on (D-17 + D-18, both directions, one full turn)"
      verification: backstop
    - statement: "A player who switches the timer off, closes the game and starts a new solo game still has it off (D-19)"
      verification: backstop
    - statement: "No mode shows a greyed-out or inert turn-clock toggle (D-20)"
      verification: backstop
  artifacts:
    - "applyTimerOff(off) exported from src/ui/util.js, beside applyPauseState"
    - "toggleTimer() in src/orchestrator.js with the db/room branch togglePause() already uses"
    - "watchTimer() reduced to Firebase wiring only"
  key_links:
    - "src/orchestrator.js toggleTimer() -> applyTimerOff() — the local, non-Firebase path (the whole of FIX-02/N-03)"
    - "src/orchestrator.js watchTimer() -> applyTimerOff() — the networked path, now sharing one body with the local one (D-18 parity)"
    - "src/ui/util.js applyTimerOff() -> netHandlers().onSetClockUI() — the ui-tier render seam, mirroring toggleShotClockPause()"
    - "src/orchestrator.js beginGame() -> pp_timerOff -> appState.timerOff — the per-browser seed, in every mode (D-19)"
---

<objective>
Make the turn-timer on/off toggle actually work in solo and pass-and-play, through one local
non-Firebase code path, with full behavioural parity to multiplayer in both directions.

Purpose: FIX-02/N-03. `toggleTimer()` currently returns early when there is no Firebase connection,
so the control silently does nothing in two of three modes, and `#scTimerToggle` is hidden in solo
entirely. `.planning/todos/pending/2026-07-31-solo-mode-needs-disabled-turn-clock-button.md` records
that a greyed placeholder is the OPPOSITE of what Wyatt asked for.

Output: `applyTimerOff(off)` extracted beside `applyPauseState`, a `toggleTimer()` with the same
db/room branch `togglePause()` already proved, the per-browser seed extended to every mode, and the
toggle visible everywhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/sound-clock/ROADMAP.md
@.planning/workstreams/sound-clock/REQUIREMENTS.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-CONTEXT.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-RESEARCH.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-PATTERNS.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-VALIDATION.md
@.planning/todos/pending/2026-07-31-solo-mode-needs-disabled-turn-clock-button.md
@./.claude/CLAUDE.md
@docs/DRIVING-THE-GAME.md
</context>

<assumptions>
- **Behavioural parity, not structural symmetry, is the constraint** (CONTEXT § Claude's
  Discretion). The mechanism chosen — extracting the existing body rather than writing a fresh local
  one — is chosen precisely because D-18's re-arm already lives in that body and re-deriving it is
  how it gets re-lost.
- **`appState.isHost` is `true` in solo and pass-and-play** with `room = null`
  (`src/ui/flow.js:1714` and `:1725` set it explicitly at game start), so the existing
  `isHost`-gated shot-clock arm/disarm machinery already works with no Firebase connection. This is
  already true for `togglePause()`; it was simply never extended to `toggleTimer()`.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Extract applyTimerOff() beside applyPauseState()</name>
  <files>src/ui/util.js, src/orchestrator.js</files>
  <read_first>
    - `src/orchestrator.js` lines 201-219 (`watchTimer()` — read the callback body in full; the BUG-02 comment and the `shotClockSeat==null` double-arm guard are the parts that must survive the move unchanged)
    - `src/ui/util.js` lines 1258-1291 (`applyPauseState()` — the sibling this new function structurally matches, including its stated no-gate discipline; and `toggleShotClockPause()` directly below it, which shows the `netHandlers().onSetClockUI()` render seam a ui-tier file must use)
    - `src/ui/util.js` lines 1216-1255 (`startShotClock`, `stopShotClock`, `rearmShotClock`) and line 1311 (`currentTurnSeat`) — all three already live in this same file, so the move adds no import edge
    - `src/ui/handlers.js` (the `netHandlers()` accessor and why it exists)
    - `21-PATTERNS.md` § `src/orchestrator.js` `toggleTimer()`/`watchTimer()` and § `src/ui/util.js` `applyTimerOff(off)`
  </read_first>
  <action>
Add `applyTimerOff(off)` to `src/ui/util.js`, placed directly beside `applyPauseState()`.

Its body is `watchTimer()`'s existing Firebase-listener body, moved VERBATIM — not rewritten,
not re-derived, not tidied. Everything from the `was` capture through the trailing render call comes
across intact, including the whole BUG-02 comment block and the `shotClockSeat==null` guard that
prevents double-arming. The only edit is the source of the new value: where the body read the
snapshot's value it now reads the `off` parameter, coerced the same way. D-18 exists because
omitting the re-arm caused the *"I paused the timer and then the game wouldn't continue"* freeze;
re-deriving that branch from scratch is exactly how it gets re-lost.

Keep every `appState.isHost` gate exactly where it already sits inside the body. Do not add one and
do not remove one. This mirrors `applyPauseState()`'s stated discipline — no host or mode gate lives
in the function, the caller decides who may call it — and here the gates were already internal, so
they stay internal.

Change the trailing render call: the body currently calls `setClockUI()` directly because it lives
in `src/orchestrator.js`, a different tier with a different import surface. Once it lands in
`src/ui/util.js` it must call `netHandlers().onSetClockUI()` instead, exactly as
`toggleShotClockPause()` does one function below it. `src/ui/util.js` is imported by
`src/ui/panel.js`, so importing `panel.js` back would close an import cycle
`scripts/module_graph_check.js` forbids outright.

`stopShotClock`, `rearmShotClock` and `currentTurnSeat` are all already declared in
`src/ui/util.js`, so this move adds zero new cross-module import edges in either file — it relocates
one function body and nothing else.

Then reduce `watchTimer()` in `src/orchestrator.js` to just its Firebase wiring: attach the watcher
and have its callback call `applyTimerOff()` with the coerced snapshot value. Import `applyTimerOff`
into `src/orchestrator.js`. Afterwards, check whether `stopShotClock`, `rearmShotClock` or
`currentTurnSeat` still have any other caller in `src/orchestrator.js`; drop from that file's import
list only the ones that no longer do, and leave the rest.
  </action>
  <verify>
    <automated>node scripts/module_graph_check.js</automated>
    <automated>node scripts/no_undef_check.js</automated>
    <automated>node scripts/ui_contract_check.js</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/ui/util.js','utf8');if(!/export function applyTimerOff\(/.test(s)){console.log('FAIL applyTimerOff not exported from src/ui/util.js');process.exit(1);}if(!/rearmShotClock\(/.test(s.slice(s.indexOf('export function applyTimerOff(')))){console.log('FAIL the re-arm branch did not come across with the body');process.exit(1);}console.log('PASS applyTimerOff extracted with its re-arm branch');"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `applyTimerOff` is exported from `src/ui/util.js` and its body contains the `rearmShotClock` call and the `shotClockSeat == null` guard
    - `grep -c 'applyTimerOff' src/orchestrator.js` reports at least 2 — the import and the `watchTimer()` call
    - `node scripts/module_graph_check.js` exits 0 — no import cycle, no ui→main edge
    - `node scripts/no_undef_check.js` and `node scripts/ui_contract_check.js` exit 0
    - `npm test` exits 0; `git diff --stat HEAD~1 -- src/engine/index.js` prints nothing
    - Multiplayer behaviour is unchanged: in a two-window host + guest game, a guest flipping the timer off still stops the host's countdown and flipping it back on still re-arms the host's current turn
  </acceptance_criteria>
  <reversibility rating="costly">D-18's re-arm. Losing it re-creates a game-freezing regression in solo and pass-and-play that is only observable by playing a full turn with the toggle flipped both ways — which is exactly why the body is moved rather than rewritten.</reversibility>
  <done>One function owns the timer-off state transition, including the re-arm, and both the Firebase listener and (next task) the local path call it.</done>
</task>

<task type="auto">
  <name>Task 2: The local non-Firebase path, and the per-browser seed in every mode</name>
  <files>src/orchestrator.js</files>
  <read_first>
    - `src/orchestrator.js` lines 162-182 (`toggleTimer()` — the early return at the top is the bug; `togglePause()` directly below is the proven db/room branch to copy)
    - `src/orchestrator.js` lines 1252-1277 (`beginGame()` — note the `pp_timerOff` read at `:1274` is inside an `isHost && db && room && !replaying` branch, so it never runs in solo or pass-and-play)
    - `src/ui/flow.js` lines 1709-1731 (`startSinglePlayer` and `startPassAndPlay` — both set `appState.isHost = true` with `appState.room = null`, then reach `beginGame` through `netHandlers().onBeginGame`)
    - `src/state/index.js` line 94 (`timerOff: false` — the default a never-seeded solo game keeps today)
    - `.planning/todos/pending/2026-07-31-solo-mode-needs-disabled-turn-clock-button.md`
  </read_first>
  <action>
Rewrite `toggleTimer()` in `src/orchestrator.js` to the exact shape `togglePause()` already uses
immediately below it. Compute the next value from `appState.timerOff`. Persist it to `pp_timerOff`
in the existing try/catch **first**, before any branch — today that write sits after the early
return, so in solo and pass-and-play the preference was never written at all. Then branch: when
there is both a db and a room, write it to Firebase through the existing `netSetTimerOff` call
unchanged, so every client's `watchTimer()` mirrors it and the whole table stays in sync; otherwise
call `applyTimerOff` with the next value directly.

Delete the guard at the top of the function that returns when there is no db or no room. That single
line is the bug this requirement exists to fix — it makes the control a silent no-op in solo and
pass-and-play, which is the "silently doing nothing" outcome D-20 names explicitly. Leave the
identical-looking guard inside `broadcastClock()` alone; that one is correct and unrelated.

Because `applyTimerOff` carries the re-arm branch verbatim from Task 1, D-17 and D-18 now hold
identically across all three modes through one shared body: switching off stops the countdown
immediately and un-times the player whose turn is in progress, and switching back on mid-turn
re-arms the clock for that same player right away. Do not add a local-only variation of either
direction.

Then close the D-19 gap. `beginGame()` reads `pp_timerOff` only inside its
`isHost && db && room && !replaying` branch, which never runs in solo or pass-and-play, so
`appState.timerOff` keeps its `false` default there and a player who switched the timer off last
game gets it back on. Add an unconditional read of `pp_timerOff` into `appState.timerOff` in
`beginGame()`, positioned before that existing branch, wrapped in the same try/catch idiom and
guarded by `!appState.replaying` so a reload-replay keeps whatever the live game actually had. Leave
the existing Firebase seed write exactly as it is — it still needs to publish the host's preference
to the room. After this, all three switches — timer, mute and the existing pause — behave
consistently: "on by default" governs a player who has never touched it, not a reset every game.
  </action>
  <verify>
    <automated>node scripts/no_undef_check.js</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/orchestrator.js','utf8');const m=s.match(/export function toggleTimer\(\)\{[\s\S]*?\n\}/);if(!m){console.log('FAIL toggleTimer not found');process.exit(1);}const b=m[0];const need=[['applyTimerOff','the local non-Firebase branch'],['netSetTimerOff','the multiplayer branch'],['pp_timerOff','the per-browser write']];const miss=need.filter(([t])=>!b.includes(t));if(miss.length){console.log('FAIL toggleTimer missing: '+miss.map(x=>x[1]).join(', '));process.exit(1);}console.log('PASS toggleTimer has both branches and the local write');"</automated>
    <automated>npm test</automated>
    <human-check>
Serve on a fresh port. In a SOLO game: on your own turn with the countdown running, click the ⏱
toggle. The countdown stops at once and the clock reads as off. Still on the same turn, click it
again. The clock re-arms for you immediately and counts down again — then finish that turn normally
and confirm the game continues into the next one. Repeat the identical off-then-on-then-finish-the-turn
sequence in a PASS-AND-PLAY game, and again in a two-window MULTIPLAYER game. Then leave the timer
off, reload the page, start a fresh solo game and confirm it is still off.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `toggleTimer()`'s body contains the local `applyTimerOff` branch, the `netSetTimerOff` branch, and the `pp_timerOff` write
    - `npm test` exits 0; `git diff --stat HEAD~1 -- src/engine/index.js` prints nothing
    - **D-18, the full-turn both-ways check, run once in EACH of solo, pass-and-play and multiplayer:** during a single turn, switch the timer OFF (the countdown stops immediately and the current player is un-timed) and then back ON (the clock re-arms for that same in-progress turn), and then play that turn through to completion — the game continues into the next turn in all three modes. A toggle that flips the icon but leaves the turn unable to end is a FAIL, and this is the only check that catches it
    - After switching the timer off and reloading, a new solo game still starts with the timer off
    - In multiplayer, a guest flipping the timer still syncs to the whole table exactly as before
  </acceptance_criteria>
  <reversibility rating="costly">D-18. The failure mode is a game-freezing regression in two modes, observable only by playing a full turn with the toggle flipped both ways — which is why that exact sequence is written into the acceptance criteria rather than "the toggle works".</reversibility>
  <done>The toggle works locally in solo and pass-and-play with full parity to multiplayer in both directions, and the preference is remembered per browser in every mode.</done>
</task>

<task type="auto">
  <name>Task 3: The toggle is visible in every mode (D-20)</name>
  <files>src/ui/panel.js</files>
  <read_first>
    - `src/ui/panel.js` lines 82-97 (the `#scTimerToggle` render block; note line 86's expression is `soloBotGame`'s only functional use in this file — the reference at line 77 is a comment about the ▶/⏸ pause and is unrelated) and line 42 (the import list `soloBotGame` arrives on)
    - `src/ui/util.js` line 1257 (`soloBotGame()` — still used by `src/main.js`'s auto-pause listener, so the function itself stays)
    - `.planning/todos/pending/d41-two-greyed-states-never-eyeballed.md`
    - `21-RESEARCH.md` § Code Examples, "Making the toggle button visible in every mode (D-20)"
  </read_first>
  <action>
In `setClockUI()`, change the `#scTimerToggle` display expression so visibility depends only on
`appState.liveDone` — hidden at end of voyage, shown otherwise, in every mode. The mode half of the
condition goes away entirely.

`src/ui/panel.js` line 86 is the only functional caller of `soloBotGame()` in this file; the mention
at line 77 is a comment about the ▶/⏸ pause and must stay exactly as it is. After the change, if
nothing else in `src/ui/panel.js` calls `soloBotGame()`, drop it from the import list at line 42.
The function itself stays in `src/ui/util.js` — `src/main.js`'s auto-pause listener still calls it.

Leave the `innerHTML` and `title` lines at 87-89 alone; they already reflect state correctly and the
tooltip copy is unchanged by this plan.

Do not add a disabled attribute, a greyed style, or a "why it's unavailable" caption. The D-41
greyed-with-a-reason pattern is a fallback for genuine dead-ends only, and D-20 states plainly that
after this phase there should be no mode where the toggle is greyed out — a control that silently
does nothing is exactly what this phase exists to remove. Task 2 has already given every mode a
working code path behind it.
  </action>
  <verify>
    <automated>node scripts/no_undef_check.js</automated>
    <automated>node scripts/ui_contract_check.js</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/ui/panel.js','utf8');const line=s.split('\n').find(l=>l.includes('toggleEl.style.display'));if(!line){console.log('FAIL toggleEl display line not found');process.exit(1);}if(!line.includes('liveDone')){console.log('FAIL display no longer keys on liveDone');process.exit(1);}if(/soloBotGame/.test(line)){console.log('FAIL the mode gate is still on the display line: '+line.trim());process.exit(1);}console.log('PASS toggle visibility depends only on liveDone');"</automated>
    <automated>npm test</automated>
    <human-check>
In a solo game, a pass-and-play game and a two-window multiplayer game, confirm the ⏱ toggle is
visible on the clock panel in all three, is not greyed out in any of them, and that clicking it in
each actually changes the clock. Play to an end of voyage and confirm it disappears with the panel.
Check a narrow browser window too — the existing `@media` block already resizes `#scPause` and
`#scTimerToggle`, and solo now shows both at once for the first time; confirm they do not overlap.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The `toggleEl.style.display` assignment line references `appState.liveDone` and carries no mode gate — proven by the line-scoped check above, which inspects that one assignment line rather than the whole file
    - `node scripts/no_undef_check.js` and `node scripts/ui_contract_check.js` exit 0
    - `npm test` exits 0
    - The ⏱ toggle is visible and functional in solo, pass-and-play and multiplayer, and greyed in none of them
    - The toggle disappears at end of voyage with the clock panel
    - At a narrow viewport width, `#scPause` and `#scTimerToggle` do not visually overlap in solo
  </acceptance_criteria>
  <done>Every mode shows a working turn-clock toggle, and no mode shows a greyed placeholder.</done>
</task>

</tasks>

## Artifacts this plan produces

- `applyTimerOff(off)` — exported from `src/ui/util.js`, beside `applyPauseState`
- `toggleTimer()` in `src/orchestrator.js` rewritten with the `db && room` / local branch
- `watchTimer()` in `src/orchestrator.js` reduced to Firebase wiring
- `beginGame()` seeds `appState.timerOff` from `pp_timerOff` in every mode

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → page JS | `pp_timerOff` is user-writable; now read in solo and pass-and-play too, where it never was before |
| Firebase `timerOff` node → page JS | unchanged shape, unchanged write path — this plan adds no new node and no new writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-08 | Tampering | `pp_timerOff` now read on the solo/pass-and-play path | low | mitigate | Read inside the existing try/catch and compared with `=== "1"`; any other stored value yields `false`. The value only ever becomes a boolean on `appState.timerOff` — never a key, index, path or rendered string. Worst case is a wrong preference, not a crash. |
| T-21-09 | Elevation of Privilege | `applyTimerOff()` callable from a ui-tier module with no host gate at the call site | low | accept | The `appState.isHost` gates live inside the body and come across unchanged, mirroring the shipped `applyPauseState()` discipline. A guest calling it mirrors a boolean for rendering and mutates no clock state, which is the pre-existing multiplayer behaviour. |
| T-21-10 | Denial of Service | the timer toggle leaving a turn with no armed clock (the BUG-02 freeze) | high | mitigate | The re-arm branch is moved verbatim rather than rewritten, so one body serves both the networked and the local path. The full-turn both-ways manual check in all three modes is a mandatory acceptance criterion, not optional polish (D-18, rated costly). |
| T-21-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages. |
</threat_model>

<verification>
- `node scripts/module_graph_check.js` — no import cycle from the extraction
- `node scripts/no_undef_check.js` and `node scripts/ui_contract_check.js`
- `npm test` — full suite including the determinism gate
- The D-18 full-turn both-ways manual check in solo, pass-and-play AND multiplayer — this is the
  accepted headless gap `21-VALIDATION.md` records, and it is mandatory
</verification>

<success_criteria>
- The timer toggle works in solo and pass-and-play through one local non-Firebase path (FIX-02/N-03)
- Switching off stops the countdown immediately (D-17); switching back on re-arms the current
  player's clock (D-18) — identically in all three modes
- The setting is remembered per browser in every mode (D-19)
- No mode shows a greyed-out or inert toggle (D-20)
</success_criteria>

<output>
Create `.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-03-SUMMARY.md` when done
</output>
