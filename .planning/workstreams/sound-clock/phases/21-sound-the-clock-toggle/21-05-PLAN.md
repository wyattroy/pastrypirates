---
phase: 21-sound-the-clock-toggle
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
files_modified:
  - assets/icons/speaker.png
  - src/shared/index.js
  - src/ui/panel.js
  - index.html
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-03, FIX-02/N-03]

must_haves:
  truths:
    - "assets/icons/speaker.png exists at non-zero size and is referenced by exactly one constant, SPEAKER_IMG in src/shared/index.js"
    - "SPEAKER_IMG is exported from src/shared/index.js and is NOT added to EMOJI_IMG — emojify() must not begin swapping anything for it"
    - "The muted state renders blocked-slash.png over the speaker rather than instead of it (D-14)"
    - "src/engine/index.js is byte-identical across the whole phase — the v1.3 determinism fence held from 21-01 through 21-05"
    - statement: "The speaker icon is drawn in the game's style and Wyatt has signed it off (D-14)"
      verification: backstop
    - statement: "The full phase matrix passes: every audible behaviour, the mute button in all three modes, the timer toggle both ways through a full turn in all three modes, and the credits copy — in BOTH Chrome and Safari"
      verification: backstop
    - statement: "A storm with sound playing over the storm overlay causes no jank, stall or crash in Safari — the browser and the moment that produced this project's worst bug (BUG-01)"
      verification: backstop
  artifacts:
    - "assets/icons/speaker.png"
    - "SPEAKER_IMG in src/shared/index.js"
    - "The image-based mute icon render in setClockUI(), with the slash composited over the speaker"
  key_links:
    - "src/shared/index.js SPEAKER_IMG -> src/ui/panel.js setClockUI() -> iconImg(SPEAKER_IMG) + iconImg(BLOCKED_SLASH_IMG) — the same on/off idiom #scTimerToggle already uses, composited rather than swapped"
---

<objective>
Drop in the real speaker icon, and run the phase's full human verification matrix in both browsers.

Purpose: D-14's art is the phase's one genuine external dependency, deliberately isolated here so no
engineering was ever blocked on it. And `21-VALIDATION.md` is explicit that most of this phase's
behaviour is manual-only — this is where that gets done, systematically, rather than in fragments.

Output: `assets/icons/speaker.png`, `SPEAKER_IMG`, the image-based icon render, and a completed
verification matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/sound-clock/ROADMAP.md
@.planning/workstreams/sound-clock/REQUIREMENTS.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-CONTEXT.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-VALIDATION.md
@.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-PATTERNS.md
@./.claude/CLAUDE.md
@docs/DRIVING-THE-GAME.md
</context>

<assumptions>
**The art runbook exists — research was wrong about this.** `notes/art-generation-process.md` is
cited by `21-CONTEXT.md` and by Wyatt's own global memory. `21-RESEARCH.md` Pitfall 2 concluded it
does not exist because `git log` found nothing; the real reason is that `notes/` is in `.gitignore`,
so the file is present on disk in Wyatt's main project folder and simply invisible from any worktree
or clone. Ask him for it rather than concluding it is missing. If it cannot be produced in-session,
the abbreviated runbook in his global memory is sufficient to attempt the icon — download button plus
the Chrome Location setting, near-black background rather than green. Either way, do not block: this
is one PNG, treated exactly like every other icon in `assets/icons/`.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The real speaker icon (D-14)</name>
  <files>assets/icons/speaker.png, src/shared/index.js, src/ui/panel.js, index.html</files>
  <precondition>`notes/art-generation-process.md` (or Wyatt's confirmation of the abbreviated runbook) is available in-session; if not, halt this task and report — do not substitute a borrowed icon, and do not block plan 21-05's Task 2, which can run against the emoji scaffold 21-04 shipped.</precondition>
  <read_first>
    - `src/shared/index.js` lines 22 and 50-52 (the `ASSET_BASE` prefix and the `*_IMG` const block `SPEAKER_IMG` joins, beside `STOPWATCH_IMG` and `BLOCKED_SLASH_IMG`) and line 213 (the trailing named `export { ... }` list) and lines 77-94 (`EMOJI_IMG` — read it to confirm `SPEAKER_IMG` must NOT go in here)
    - `src/ui/panel.js` lines 34-46 (the `../shared/index.js` import list) and lines 82-97 (`#scTimerToggle`'s `iconImg(BLOCKED_SLASH_IMG)` / `iconImg(STOPWATCH_IMG)` swap, and the `#btnMute` block 21-04 added directly above it)
    - `index.html` lines 398-410 (the `#scPause img` / `#scTimerToggle img` sizing rules) and the `#btnMute` rules 21-04 added
    - `assets/icons/blocked-slash.png` (the established switched-off overlay)
    - `21-CONTEXT.md` D-14 (including the explicit rejection of reusing `horn.png`, and why)
  </read_first>
  <action>
Produce `assets/icons/speaker.png` — a speaker drawn in the game's established icon style, matching
the visual weight of `stopwatch.png` and `pause-symbol.png` so it sits naturally beside them. Follow
the project's asset runbook (see this plan's assumptions block for where it actually lives). Do not
reuse `horn.png` or any other existing icon: D-14 rejects `horn.png` specifically because it already
renders the 📯 in narration text and would carry two meanings.

Add `SPEAKER_IMG` to `src/shared/index.js`'s `*_IMG` const block at lines 51-52, alongside
`STOPWATCH_IMG` and `PAUSE_SYMBOL_IMG`, built from `ASSET_BASE` the same way, and add `SPEAKER_IMG`
to the trailing named export list at line 213. Do NOT add it to `EMOJI_IMG` — it stands for no emoji,
and adding it would make `emojify()` start swapping a character it has no business touching.

In `src/ui/panel.js`, add `SPEAKER_IMG` to the existing `../shared/index.js` import list at line 36
and change the `#btnMute` block's `innerHTML` from the emoji scaffold 21-04 shipped to the image
form. Unmuted renders `iconImg(SPEAKER_IMG)` alone. Muted renders `iconImg(SPEAKER_IMG)` with
`iconImg(BLOCKED_SLASH_IMG)` on top of it — note this differs from `#scTimerToggle` at line 87, which
*swaps* one icon for the other; D-14 asks for the slash shown OVER the speaker, so both images render
and the slash is positioned on top. Add the small positioning rule for that overlay beside the
existing `#btnMute` rules in `index.html`, reusing the `width:60%; height:60%; object-fit:contain`
proportions the other clock icons already use so the slash lands centred on the speaker at every
viewport size. Leave `style.display` and `title` exactly as 21-04 set them.
  </action>
  <verify>
    <automated>node -e "const {statSync}=require('fs');const s=statSync('assets/icons/speaker.png');if(!s.size){console.log('FAIL speaker.png is empty');process.exit(1);}console.log('PASS speaker.png present, '+s.size+' bytes');"</automated>
    <automated>node -e "import('./src/shared/index.js').then(m=>{if(!m.SPEAKER_IMG){console.log('FAIL SPEAKER_IMG not exported');process.exit(1);}if(Object.values(m.EMOJI_IMG).includes(m.SPEAKER_IMG)){console.log('FAIL SPEAKER_IMG was added to EMOJI_IMG');process.exit(1);}console.log('PASS SPEAKER_IMG exported and kept out of EMOJI_IMG');}).catch(e=>{console.log('FAIL '+e.message);process.exit(1);})"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/ui/panel.js','utf8');const b=s.slice(s.indexOf('btnMute'),s.indexOf('btnMute')+700);if(!b.includes('SPEAKER_IMG')||!b.includes('BLOCKED_SLASH_IMG')){console.log('FAIL the mute render does not composite the speaker with the slash');process.exit(1);}console.log('PASS muted state renders the slash over the speaker');"</automated>
    <automated>npm test</automated>
    <human-check>
Open a running game and look at the mute button beside the turn clock. The speaker reads as a speaker
at normal and at narrow viewport widths, sits at the same visual weight as the pause and stopwatch
icons next to it, and the muted state clearly shows the slash across it. Wyatt signs off on the icon.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `assets/icons/speaker.png` exists at non-zero size
    - `SPEAKER_IMG` is exported from `src/shared/index.js` and is not a value in `EMOJI_IMG`
    - The `#btnMute` render in `setClockUI()` references both `SPEAKER_IMG` and `BLOCKED_SLASH_IMG`, with the slash composited over the speaker rather than replacing it
    - `npm test` exits 0
    - The icon renders correctly at normal and narrow viewport widths in both Chrome and Safari, and Wyatt has approved it
  </acceptance_criteria>
  <reversibility rating="reversible">D-14's icon is one asset path constant; a redraw replaces the PNG with no code change.</reversibility>
  <done>The mute button shows a purpose-drawn speaker, slashed when muted, approved by Wyatt.</done>
</task>

<task type="auto">
  <name>Task 2: The phase verification matrix — Chrome and Safari</name>
  <files>(no source changes — verification only)</files>
  <precondition>A local server is running via `npm start` on a port not previously loaded in either browser, and both Chrome and Safari are available. Safari caches ES modules, so Safari must be pointed at a FRESH port — a `?cb=` query string on the page does not bust a module cache.</precondition>
  <read_first>
    - `21-VALIDATION.md` § Per-Task Verification Map (every row marked manual-only is a row of this matrix) and § Manual-Only Verifications
    - `docs/DRIVING-THE-GAME.md` — required reading before any browser driving. §1 (fresh port), §4a (the `#flipCoinWrap` coin IS the flip button, not an `.apBtn` — this has stalled three separate attempts), §5b (the armed autoplay driver), §5c (driving a guest seat while a human hosts), §5d (arm a watcher for sub-second windows, do not try to click)
    - `21-01-SUMMARY.md`, `21-02-SUMMARY.md`, `21-03-SUMMARY.md`, `21-04-SUMMARY.md`
  </read_first>
  <action>
Run the phase's full manual verification matrix and record the result of every row in the plan
summary — pass, fail, or not-reached, with the browser and mode it was checked in. This task changes
no source; its output is evidence. Any failure it surfaces is filed and fixed before the phase closes,
not carried.

Rename any browser tab being driven to indicate it is under automation, and rename it back the moment
driving stops.

Run every row below in Chrome, then repeat rows 1, 2, 5, 6 and 9 in Safari on a fresh server port.

Row 1, sound at the right moments (AUDIO-01, D-01 through D-04, D-07): in a solo game confirm
sailing, docking, fishing, trading and battles each make their mapped sound, and that bot captains'
actions are audible too, not only your own.

Row 2, flips (D-02, D-10): every flip makes a noise, battle rounds and routine docking flips alike;
during a fast multi-round battle successive flips layer into a flurry rather than cutting each other
off, and no flip lands silently.

Row 3, the storm (D-08, D-09): force a storm by temporarily setting the storm probability to 1 in
`roundCfg` and REVERT that edit before finishing. Confirm the storm sound is heard exactly once for
the round, not once per affected captain; that it sits underneath the short sounds rather than
drowning them; and that it fades out as the storm moment resolves — never a hard cut, never droning
into the next round.

Row 4, the win screen (D-05): play to an end of voyage and confirm it carries a sound.

Row 5, mute (AUDIO-02, D-13, D-15, D-16): the button sits to the right of the turn clock from the
first turn; clicking it silences the game and flips the icon; a mid-game reload comes back still
muted; at end of voyage the button disappears with the clock panel and a muted player stays silent
through the celebration.

Row 6, the background tab (D-12): switch away mid-game — sound goes quiet; switch back — it resumes.

Row 7, the timer toggle (FIX-02/N-03, D-17, D-18, D-19, D-20): in EACH of solo, pass-and-play and
multiplayer, play one full turn while switching the timer OFF mid-turn and then back ON mid-turn, and
confirm the turn still completes and the game continues into the next one. This exact sequence is the
only thing that catches D-18's failure mode. Confirm the toggle is visible and not greyed in all
three modes, and that leaving it off and starting a fresh solo game keeps it off.

Row 8, multiplayer (D-07): with two windows per §5c, confirm the guest hears the host's and the bots'
actions; that muting one window leaves the other audible; and that the timer toggle still syncs
across the table.

Row 9, Safari under a storm (the project's own worst-bug territory): force a storm in Safari with
sound on and watch for jank, a stall or a crash while the storm overlay animates. BUG-01 was a Safari
storm-overlay near-crash, and a storm sound layered over that overlay has never been tried.

Row 10, credits and copy (AUDIO-03, milestone constraint 3): open the Credits modal and confirm Luis
appears once, credited for both mechanics and sound, with one working link. Wyatt reads and approves
the credit clause and both mute tooltips (hover the mute button in each state). Record his
disposition against `.planning/todos/pending/copy-shipped-vs-approved-gate.md`.

Finally, record in the summary that two sounds ship as explicit placeholders and are on the shopping
list for Luis — `store-ingredient` at the win screen (D-05) and `battle-swords` on the shot clock
expiring (D-22) — naming the two constants that swap when real files arrive. Also note whether any of
the six files sounded jarring next to the others and needs loudness normalising, which is the one
Claude's-discretion item only a listening pass can settle; the per-stem volume table is the single
place to adjust it.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node scripts/audio_mapping_test.js</automated>
    <automated>node scripts/module_graph_check.js</automated>
    <automated>git diff --stat $(git merge-base HEAD origin/main) -- src/engine/index.js</automated>
    <human-check>
All ten rows above, run in Chrome; rows 1, 2, 5, 6 and 9 repeated in Safari on a fresh server port.
Every row's outcome recorded in `21-05-SUMMARY.md` with its browser and mode.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and `node scripts/audio_mapping_test.js` exits 0
    - `git diff --stat` against the merge base for `src/engine/index.js` prints nothing — the v1.3 engine fence held across the entire phase
    - `node scripts/module_graph_check.js` exits 0 — the tier fence held, including for the new `src/shared/audio.js`
    - All ten matrix rows are recorded in `21-05-SUMMARY.md` with an explicit pass, fail or not-reached, naming the browser and mode
    - Rows 1, 2, 5, 6 and 9 are recorded for Safari as well as Chrome
    - Row 7's full-turn both-ways check is recorded separately for each of solo, pass-and-play and multiplayer
    - The summary names both placeholder sounds and the constants that swap them
    - Any temporary storm-probability edit used to force a storm is reverted, confirmed by a clean `git status` for `src/`
  </acceptance_criteria>
  <done>Every manual-only row in `21-VALIDATION.md` has been run and recorded, in both browsers, with the two hard fences proven automatically.</done>
</task>

</tasks>

## Artifacts this plan produces

- `assets/icons/speaker.png` — new asset
- `SPEAKER_IMG` — exported constant in `src/shared/index.js`
- The image-based `#btnMute` icon render in `setClockUI()`, slash composited over speaker
- A completed, recorded verification matrix in `21-05-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| same-origin static assets → DOM | `assets/icons/speaker.png` loaded by `iconImg()`, identical to every other icon in the game |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-14 | Tampering | `SPEAKER_IMG` asset path | low | mitigate | Built from the module-local `ASSET_BASE` constant and a literal filename, exactly like the other 80-odd `*_IMG` constants beside it. No runtime string reaches the path, and it is not added to `EMOJI_IMG`, so `emojify()` cannot route arbitrary text through it. |
| T-21-15 | Spoofing | a borrowed or wrong icon shipping as the mute control | low | mitigate | The task halts rather than substituting a borrowed icon, and Wyatt's sign-off is an acceptance criterion. |
| T-21-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages. |
</threat_model>

<verification>
- `npm test` — full suite, including the determinism gate
- `node scripts/audio_mapping_test.js` and `node scripts/module_graph_check.js`
- `git diff --stat` against the merge base for `src/engine/index.js` — the phase-wide engine fence
- The ten-row human matrix in Chrome and Safari
</verification>

<success_criteria>
- The mute button shows a purpose-drawn speaker, slashed when muted, approved by Wyatt (D-14)
- Every manual-only row in `21-VALIDATION.md` has been run and recorded in both browsers
- Both hard fences are proven automatically: `src/engine/index.js` untouched, tier layering intact
- Both placeholder sounds are named in the summary as placeholders, not shipped silently
</success_criteria>

<output>
Create `.planning/workstreams/sound-clock/phases/21-sound-the-clock-toggle/21-05-SUMMARY.md` when done
</output>
