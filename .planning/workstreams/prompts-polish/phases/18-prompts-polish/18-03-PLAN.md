---
phase: 18-prompts-polish
workstream: prompts-polish
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/ui/util.js
  - src/ui/flow.js
  - index.html
  - scripts/narration_test.js
  - scripts/bot_storm_narration_test.js
  - scripts/extract_narration_lines.js
  - scripts/ui_contract_check.js
autonomous: true
requirements: [FIX-04, FIX-21]
tags: [narration, copy, line-breaks, storm]

must_haves:
  truths:
    - "No player ever sees the storm-drift line — neither the addressed variant nor the third-person one; both are removed together (FIX-04)."
    - "The storm-drift capsule still appears beside the captain's name in the Captains box — only the narration line is gone (FIX-04)."
    - "Every trailing signed-coin parenthetical in narration wraps as one unbreakable block (FIX-21)."
    - "An end-of-voyage award stat keeps its quantity and unit on the same line (FIX-21)."
    - "A new anchored gate in scripts/ui_contract_check.js REFUSES (fails) rather than skips when it cannot find its subject, so a future site cannot silently drop the wrap."
    - "npm test stays green, including determinism_baseline.js --verify — no engine event shape changes."
  artifacts:
    - "src/ui/util.js: windmove EVENT_NARRATION entry reduced to its capsule only"
    - "src/ui/util.js: nobrk-wrapped parentheticals at the aground and sidebet sites"
    - "src/ui/flow.js: nobrk-wrapped parenthetical in the turn-order draw consolation line"
    - "index.html CSS: .awardStat b nowrap rule"
    - "scripts/ui_contract_check.js: checkCoinParentheticalNobrk + the coin-parenthetical-nobrk gate"
  key_links:
    - "EVENT_NARRATION.windmove -> describeFor() returns null when there is no txt -> syncLogLines pushes null -> every logLines consumer must be null-safe"
    - "EVENT_NARRATION.windmove -> captions() reads .caps independently of .txt, so the capsule survives"
    - ".nobrk (index.html:294 white-space:nowrap) -> every narration site that builds a trailing signed-coin chunk"
---

<objective>
Remove the storm-drift narration line for good (FIX-04), and stop narration orphaning a trailing
chunk on a line wrap — both the `(±N🌕)` parentheticals and the end-of-voyage award quantity+unit
(FIX-21).

Purpose: FIX-21 is a THIRD recurrence. `.nobrk` already exists and already works at 2 of 6 narration
sites; it was never applied everywhere because each site hand-builds its own string. This plan sweeps
every site RESEARCH enumerated AND adds an anchored gate so a seventh site cannot ship unwrapped.

Output: one narration entry reduced to its capsule, six wrapped parentheticals, one CSS nowrap rule,
and a permanent refuses-rather-than-skips gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/prompts-polish/ROADMAP.md
@.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-RESEARCH.md
@.planning/todos/pending/copy-shipped-vs-approved-gate.md
@src/ui/util.js
</context>

<hard_constraints>
1. **Nothing in this plan touches `src/engine/index.js`.** `npm test` chains
   `determinism_baseline.js --verify` and MUST stay green.
2. **Both viewer variants of the storm-drift line are removed together** (ROADMAP criterion 9,
   D-07/NARR-05). Removing one and leaving the other is the defect, not a partial fix.
3. **The award quantity+unit fix is made in the `index.html` CSS block, not `src/ui/board.js`.**
   `src/ui/board.js` belongs to the concurrent `board-wind` workstream; the award card already
   wraps its value+unit in a `<b>` inside `.awardStat`, and a `white-space` rule on that `<b>`
   delivers the same outcome inside a file this workstream owns. RESEARCH proposed a `board.js`
   edit here; the CSS route is chosen deliberately to avoid a second cross-workstream touch.
4. **The `coinDelta(n)` shared helper RESEARCH recommends is deliberately NOT built in this phase** —
   it would live in `src/shared/index.js`, which is outside this workstream's ownership list. The
   durability half of FIX-21 is delivered instead by the anchored gate in Task 3, which is inside
   `scripts/`. Record this in the SUMMARY so the helper is not silently forgotten.
5. **Never re-match an approved copy row to source by line number** (copy-gate rule, learned twice
   the hard way). Locate every site by its surrounding literal text.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the storm-drift narration line, keep the capsule, re-point every assertion</name>
  <files>src/ui/util.js, scripts/narration_test.js, scripts/bot_storm_narration_test.js, scripts/extract_narration_lines.js</files>
  <read_first>
    - `src/ui/util.js` line 327 — the `windmove` entry of `EVENT_NARRATION`. Both viewer variants sit
      on this one line, together with the `caps` capsule.
    - `src/ui/util.js` lines 711-721 — `describeFor()`. It returns `null` when the builder produces no
      `txt`. This is the mechanism that silences the line, and it is also the trap: whatever consumes
      the result must be null-safe.
    - `src/ui/util.js` `syncLogLines()` — it pushes `describeFor(...)` straight into
      `appState.logLines`, so a silent event now pushes `null`. Trace EVERY consumer of
      `appState.logLines` (the log panel render, the scrubber, `narrateCurrent()`) and confirm each
      already guards, or add the guard.
    - `src/ui/util.js` lines 801-804 — `captions()`. It reads `r.caps` independently of `r.txt`, which
      is why the Captains-box capsule survives.
    - `scripts/narration_test.js` lines ~62-80 (the `FAB` fabricated-event map, including
      `windmove`), and lines ~486-495 — `COVERED_SINGLE_SUBJECT` (asserted to have exactly 16 keys)
      and `SILENT_KEYS` (currently `turn`, `end`).
    - `scripts/extract_narration_lines.js` line ~150 — `TABLE_GROUPS.windmove`, and how
      `scripts/narration_audit_check.js` consumes the extractor's output.
  </read_first>
  <behavior>
    - `EVENT_NARRATION.windmove(fabricatedEvent, at)` returns an object with a `caps` array and no
      text key.
    - `describeFor({t:"windmove",p:0}, NEUTRAL_VIEWER)` returns `null`.
    - `describeFor({t:"windmove",p:0}, 0)` also returns `null` — the addressed variant is gone too.
    - `captions({t:"windmove",p:0})` still returns a one-entry array carrying the drift capsule.
    - No consumer of `appState.logLines` throws when the array contains a `null` entry.
  </behavior>
  <action>
    Reduce the `windmove` entry of `EVENT_NARRATION` in `src/ui/util.js` so the builder returns only
    the capsule: the entry becomes
    `windmove:(e,at,cellPx,viewerSeat)=>({caps:[[e.p,"🌬️ drifts"]]}),`. Both viewer variants leave in
    the same edit. Keep the `🌬️ drifts` capsule string byte-identical.

    Confirm — do not assume — that `describeFor()` returning `null` is harmless on every path. Walk
    each `appState.logLines` consumer and each `describeFor`/`describe` call site and add a guard
    only where one is genuinely missing. Do not add defensive guards where one already exists.

    In `scripts/narration_test.js`: move `windmove` out of `COVERED_SINGLE_SUBJECT` and into
    `SILENT_KEYS`, and lower the pinned `COVERED_SINGLE_SUBJECT` count assertion from 16 to 15,
    updating the assertion's own label text so it does not claim a stale number. Keep the `FAB`
    entry for `windmove` — the builder must still be callable without throwing. Add a positive
    assertion that the builder returns a `caps` array with one entry and produces no narration text,
    and a second that `describeFor` yields `null` for both the neutral and the addressed viewer.

    In `scripts/bot_storm_narration_test.js`: re-point (do not delete) any assertion that pinned the
    storm-drift narration string, so it now asserts the absence of a narration line and the presence
    of the capsule. The two BUG-2 assertions at lines ~381-382 exercise event ORDERING, not text —
    leave them untouched.

    In `scripts/extract_narration_lines.js`: run it and inspect the delta. If the entry now
    contributes no extractable line, keep its `TABLE_GROUPS` mapping (it costs nothing and documents
    the key's group) and update any pinned total the extractor or `narration_audit_check.js` asserts.
    If `narration_audit_check.js` fails after the removal, fix it by re-pointing the affected row —
    match by label and by literal text, never by line number.
  </action>
  <verify>
    <automated>node scripts/narration_test.js &amp;&amp; node scripts/bot_storm_narration_test.js &amp;&amp; node scripts/extract_narration_lines.js &amp;&amp; node scripts/narration_audit_check.js &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - The `windmove` entry in `src/ui/util.js` is exactly
      `windmove:(e,at,cellPx,viewerSeat)=>({caps:[[e.p,"🌬️ drifts"]]}),` (whitespace-normalised).
    - `grep -c 'blown by the storm' src/ui/util.js` returns `0`.
    - `grep -rc 'blown by the storm' src/ scripts/ index.html` reports `0` in every file.
    - `node scripts/narration_test.js` exits 0 and its output contains a PASS line asserting the
      windmove builder yields a capsule and no narration text.
    - `node scripts/bot_storm_narration_test.js` exits 0.
    - `node scripts/narration_audit_check.js` exits 0 with no assertion reported as skipped.
    - `npm test` exits 0, including `determinism_baseline.js --verify`.
    - `grep -c 'drifts' src/ui/util.js` returns at least `1` — the capsule survived.
  </acceptance_criteria>
  <done>The storm-drift line is gone in both viewer variants, the Captains-box capsule still marks the drift, every logLines consumer is null-safe, and the whole suite is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap every unwrapped trailing chunk — six narration sites plus the award stat</name>
  <files>src/ui/util.js, src/ui/flow.js, index.html</files>
  <read_first>
    - `index.html` line 294 — `.nobrk { white-space: nowrap; }`, the mechanism that already exists.
    - `src/ui/util.js` lines 645-655 — the battleflee and fishing sites, which are ALREADY correct.
      Copy their exact span shape; do not invent a second convention.
    - `src/ui/util.js` line 452 — the aground `lossTag` local.
    - `src/ui/util.js` lines 528-534 — the sidebet-won and sidebet-lost branches. Note there are FOUR
      separate `(+…)` chunks across lines 530-531 (a `you` variant and a third-person variant on each
      line) and TWO `(−…)` chunks across lines 533-534. Wrap every one of them.
    - `src/ui/flow.js` lines 1494-1502 — the turn-order draw consolation line and the comment above it
      explaining why the coin is carried at all.
    - `index.html` lines 649-659 — `.awardCard` and the `.awardStat` / `.awardStat b` rules.
    - `src/ui/board.js` around line 785 (READ ONLY, do not edit) — confirm the award value and unit
      are already inside one `<b>` element and that `def.unit` values carry a LEADING space
      (` sq`, ` rounds`, ` tails`, ` heads`), which is exactly why they break apart today.
  </read_first>
  <action>
    In `src/ui/util.js`, wrap the aground `lossTag` parenthetical in a `nobrk`-classed span, matching
    the exact span shape already used at the battleflee and fishing sites. Keep the U+2212 minus
    character and the coin glyph byte-identical — this is approved copy, only the wrapper is new.

    In the same file, wrap all four signed-coin parentheticals in the sidebet-won branch and both in
    the sidebet-lost branch the same way. Each wrapper encloses the opening parenthesis, the sign,
    the number, the coin glyph and the closing parenthesis — nothing more, nothing less.

    In `src/ui/flow.js`, wrap the per-captain parenthetical inside the turn-order draw consolation
    map callback the same way.

    In the `index.html` CSS block, extend the existing `.awardStat b` rule with a nowrap declaration
    so the award's value and its leading-space unit stay on one line. Do not add a new selector and
    do not touch `.awardStat` itself — the stat label outside the `<b>` must keep wrapping normally.

    Change no copy text anywhere in this task. Every character inside every string stays as it is;
    only wrapper markup and one CSS declaration are added. Record all seven touched sites in the
    SUMMARY for Task 3's gate to anchor against.
  </action>
  <verify>
    <automated>node scripts/narration_test.js &amp;&amp; node scripts/narration_flow_test.js &amp;&amp; node scripts/narration_audit_check.js &amp;&amp; npm test</automated>
    <human-check>Drive Chrome to an end of voyage per docs/DRIVING-THE-GAME.md §5b, narrow the viewport to 320px, and confirm no award card splits a number from its unit and no narration line leaves a lone parenthetical on its own line.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nobrk' src/ui/util.js` returns at least `13` (the 7 pre-existing spans at the
      battleflee/fishing sites plus 7 new ones: 1 aground, 4 sidebet-won, 2 sidebet-lost) — record the
      exact before and after counts in the SUMMARY.
    - `grep -c 'nobrk' src/ui/flow.js` returns at least `1`.
    - `node scripts/narration_audit_check.js` exits 0 — no approved copy string was altered.
    - `git diff -U0 src/ui/util.js src/ui/flow.js | grep -cE '^[+-].*(bribe|aground|sugarfish|candycrab)' `
      shows only balanced pairs where the sole difference is added span markup (inspect and record).
    - The `.awardStat b` rule in `index.html` contains a `white-space` declaration whose value is
      `nowrap`, and `grep -c 'awardStat' index.html` is unchanged from before this task.
    - `git diff --stat src/ui/board.js` shows no change from this plan.
    - `npm test` exits 0.
    - In a driven 320px-wide end-of-voyage session, every `.awardStat b` element's
      `getClientRects().length` is `1` (its content occupies a single line box).
  </acceptance_criteria>
  <done>All six narration parentheticals and the award quantity+unit are unbreakable, no copy string changed, and board.js was not touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: An anchored gate so a seventh unwrapped site cannot ship</name>
  <files>scripts/ui_contract_check.js</files>
  <read_first>
    - `scripts/ui_contract_check.js` line 715 onward and `checkStormRainSeeded` at line ~834 — the
      "anchored, and REFUSES rather than skips when it cannot find its subject" pattern. This is the
      working example to copy, named as such by the copy-gate todo.
    - `scripts/ui_contract_check.js` lines ~874-918 — the `results.push({ name: ... })` registration
      block, to see how a new gate is added to the run.
    - `scripts/ui_contract_check.js` lines ~1240-1290 — the self-test cases for
      `checkStormRainSeeded`, showing how a gate proves it fails on a synthetic bad tree.
    - `src/ui/flow.js` — note the coin parentheticals that are legitimately NOT narration and must NOT
      be caught by this gate: BUTTON LABELS (the anchor-safely option, the flip prompt, the buy/take
      option pair, the attack option, the fish option) and prose in code comments. A blanket
      "no coin-close-paren outside a nobrk span" sweep over these files fails on all of them — the
      gate must be anchored to the specific narration sites, not to the whole file.
  </read_first>
  <action>
    Add `checkCoinParentheticalNobrk(root)` to `scripts/ui_contract_check.js`, registered in the
    results block under the gate name `coin-parenthetical-nobrk`.

    Anchor it to the seven narration sites Task 2 wrapped plus the two that were already correct, by
    matching each site's distinctive surrounding literal text (for example the aground repairs
    clause, each sidebet outcome clause, the turn-order consolation clause, the battleflee clause and
    the fishing clauses) — never by line number, per the copy-gate's twice-learned rule. For each
    anchor, assert the coin parenthetical that follows it is enclosed in a `nobrk`-classed span.

    Make it REFUSE rather than skip: if any anchor's surrounding literal cannot be located in the
    tree, the gate FAILS with a message naming the missing anchor. A gate that silently vanishes when
    its input moves is the exact failure mode `narration_audit_check.js`'s assertion 8 already has,
    and the copy-gate todo makes refusing non-negotiable.

    Add self-test cases in the same style as `checkStormRainSeeded`'s: one synthetic tree with an
    unwrapped parenthetical that the gate must FAIL, and one with every site wrapped that it must
    PASS. Scope every match to narration-building code only — button-label and comment occurrences
    must not be reachable by the gate's matchers.
  </action>
  <verify>
    <automated>node scripts/ui_contract_check.js &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/ui_contract_check.js` output contains a line beginning `PASS` and naming
      `coin-parenthetical-nobrk`.
    - `grep -c 'checkCoinParentheticalNobrk' scripts/ui_contract_check.js` returns at least `3`
      (definition, registration, self-test).
    - Temporarily reverting any ONE of the seven wraps from Task 2 makes
      `node scripts/ui_contract_check.js` exit non-zero and print a FAIL naming
      `coin-parenthetical-nobrk`; restore the wrap afterwards and record the experiment in the SUMMARY.
    - Temporarily renaming one anchored literal makes the gate FAIL with a message naming the missing
      anchor (it refuses; it does not silently pass); restore afterwards and record it.
    - `node scripts/ui_contract_check.js` exits 0 in the restored tree.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>A permanent, anchored, refuses-rather-than-skips gate proves every narration coin parenthetical is unbreakable, demonstrated by two recorded negative experiments.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
| Symbol / artifact | File | Kind |
|---|---|---|
| `EVENT_NARRATION.windmove` (capsule-only form) | src/ui/util.js | changed entry — no longer produces narration text |
| 7 new `nobrk`-classed spans | src/ui/util.js, src/ui/flow.js | changed narration markup |
| `white-space: nowrap` on `.awardStat b` | index.html (CSS block) | changed CSS rule |
| `checkCoinParentheticalNobrk(root)` | scripts/ui_contract_check.js | new gate function |
| `coin-parenthetical-nobrk` | scripts/ui_contract_check.js | new gate name in the results block |
| `windmove` moved to `SILENT_KEYS`; `COVERED_SINGLE_SUBJECT` count 16 -> 15 | scripts/narration_test.js | changed assertions |
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game state -> narration `innerHTML` | narration strings are built per call site and injected as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | Tampering | added `nobrk` span markup | low | accept | The added spans carry a static class name and enclose already-existing, already-escaped interpolations. No new user-controlled interpolation point. |
| T-18-07 | Information Disclosure | `describeFor()` returning null | medium | mitigate | Every `appState.logLines` consumer is walked and confirmed null-safe in Task 1; a missed consumer would throw and take down the log panel, not leak — acceptance criteria pin the full suite green. |
| T-18-08 | Repudiation | copy removed without inventory record | medium | mitigate | The storm-drift removal is recorded against `copy-shipped-vs-approved-gate.md` in plan 18-07 Task 1; `narration_audit_check.js` must stay green here and is an acceptance criterion. |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A this phase — zero packages installed. |
</threat_model>

<verification>
- `npm test` green after each task.
- Two recorded negative experiments proving the new gate actually fails when it should.
- One driven 320px end-of-voyage pass confirming no award stat splits.
</verification>

<success_criteria>
- FIX-04 complete: both viewer variants gone, capsule intact, all narration scripts re-pointed
  rather than deleted.
- FIX-21 complete: all enumerated sites wrapped, award stat held together in CSS, board.js untouched,
  and a permanent gate preventing the fourth recurrence.
</success_criteria>

<output>
Create `.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-03-SUMMARY.md` when done.
Record: before/after `nobrk` counts per file, the seven anchored sites by their literal text, both
negative-experiment results, and the deferred `coinDelta(n)` helper with its ownership reason.
</output>
