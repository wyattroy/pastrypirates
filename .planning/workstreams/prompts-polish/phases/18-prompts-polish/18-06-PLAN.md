---
phase: 18-prompts-polish
workstream: prompts-polish
plan: 06
type: execute
wave: 5
depends_on: ["18-01", "18-03", "18-05"]
files_modified:
  - index.html
  - src/ui/util.js
  - src/ui/lobby.js
autonomous: true
requirements: [FIX-06, FIX-09, FIX-17]
tags: [css, visual, mobile, buttons]

must_haves:
  truths:
    - "Every solid-orange primary button renders with the outline + faded-fill treatment, matching the footer buttons (FIX-06)."
    - "No primary button reverts to a solid orange fill on hover (FIX-06)."
    - "The coloured circle beside a captain's name is gone from the player rows AND from the lobby seat list, and each row closes the gap it left (FIX-17)."
    - "On a 320-390px wide screen the ingredient chips stay readable rather than collapsing into one vertical column (FIX-09)."
    - "Both FIX-09 candidate treatments exist in the tree and both are rendered at 320, 375 and 390 for a by-eye choice (D-03) — neither is picked blind."
    - { statement: "Which FIX-09 treatment reads better on a real narrow phone.", verification: backstop }
    - { statement: "The restyled primary buttons and the dot-less rows look right by eye, including the .ahoyGlow ring against the new pale fill.", verification: backstop }
  artifacts:
    - "index.html CSS: button.primary and button.primary:hover using the footer outline + faded-fill recipe"
    - "index.html CSS: .apBtn.primary:hover matching the same recipe"
    - "index.html CSS: .prowTop with the dot column and dot area removed, in BOTH the base rule and the narrow-screen override"
    - "index.html CSS: the two FIX-09 candidate treatments, one live by default and one selectable by a body class"
    - "src/ui/util.js and src/ui/lobby.js: the seat-colour swatch spans removed"
    - "Renders of both FIX-09 treatments at 320, 375 and 390"
  key_links:
    - "FIX-17 removes the dot COLUMN from .prowTop's grid — FIX-09's narrow-screen areas must be authored against the post-FIX-17 three-column grid, not the current four-column one"
    - "button.primary -> the 9 static class=\"primary\" sites in index.html AND the dynamic .apBtn.primary path built in src/ui/flow.js"
    - "HEXCOL[i] still drives .player-row's --rowcol border and the .pname colour — only the swatch span goes"
---

<objective>
Three small visual fixes, sequenced deliberately because two of them touch the same CSS grid:
restyle the solid-orange primary buttons to the project's outline + faded-fill pattern (FIX-06),
remove the coloured circle beside every captain name (FIX-17), and stop the ingredient chips
collapsing into a vertical column on a narrow phone (FIX-09).

Purpose: FIX-17 removes a column from `.prowTop`'s grid; FIX-09 re-authors that same grid's
narrow-screen areas. Doing FIX-09 first would author it against a grid that is about to change.

Output: one restyled button rule set, a dot-free player row and lobby seat, and TWO complete FIX-09
candidate treatments plus renders at 320/375/390 for Wyatt to choose between by eye (D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/prompts-polish/ROADMAP.md
@.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-RESEARCH.md
@docs/DRIVING-THE-GAME.md
</context>

<binding_decisions>
- **D-03** — FIX-09 is decided by eye. Build BOTH candidate treatments (shrink the chips vs. give
  them their own full-width row at narrow widths) and produce a render at 320/375/390 for Wyatt to
  choose. Do NOT pick one blind.
- **ROADMAP criterion 10** rules that the circle goes from EVERYWHERE it appears, player rows and the
  lobby seat list alike. RESEARCH logged assumption A2 wondering whether the lobby dot should survive;
  that question is already answered by Wyatt's own 2026-08-01 "everywhere" instruction. Do not
  re-open it — implement both sites.
</binding_decisions>

<hard_constraints>
1. **FIX-09 is PURE CSS.** `.prowTop` grid, the `@media (max-width: 480px)` override, and `.chip`
   sizing, all in the `index.html` CSS block. It does NOT need a `src/ui/board.js` edit — the board
   reference in the FIX-09 todo is where chip CONTENTS are rendered, not their layout.
2. **`src/ui/board.js` is not touched by this plan at all** (it belongs to the concurrent `board-wind`
   workstream).
3. **Order matters: FIX-17 before FIX-09.** FIX-17 removes the dot column from `.prowTop`; FIX-09's
   narrow-screen `grid-template-areas` must be authored against the resulting three-column grid.
4. **Chips are confirmed non-interactive** (no click handler anywhere in `src/ui/board.js` or
   `src/ui/util.js`), so shrinking them costs no tap target. Do not add one.
5. **Nothing touches `src/engine/index.js`.** `npm test` must stay green.
6. **No dead CSS ships.** Both FIX-09 treatments exist only until Wyatt chooses; plan 18-07 deletes
   the loser. This codebase's own D-33/D-34/D-40 rules forbid leaving unreachable code in the tree.
7. **Line numbers in RESEARCH.md have drifted.** Locate every rule by its selector text.
</hard_constraints>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle the primary buttons to the outline + faded-fill pattern</name>
  <files>index.html</files>
  <read_first>
    - `index.html` lines 123-126 — the base `button` rule and the two `button.primary` rules that drive
      every primary button in the game.
    - `index.html` lines 140-152 — the footer button family: `.footerBtn`, `.footerHow`/`.footerCredits`/
      `.footerLog`, `.footerFeedback`, and the KOFI-01 comment block above `.footerKofi`, whose
      border/background/colour trio is the exact target recipe and whose comment explains WHY the light
      tint is the house pattern.
    - `index.html` line 158 — `.footerLeave`, the red destructive-action recipe.
    - `index.html` lines 331-347 — `.apBtn`, `.apBtn.primary:hover`, `.apBtn.apDisabled`, `.apBack` and
      `.ahoyGlow`. The `.apBtn.primary:hover` rule reintroduces a solid orange fill on hover and must
      move with the base rule or the fix is only half applied.
    - `index.html` lines 1010-1016 — the comment recording the destructive-action design intent above
      `#btnConfirmLeave`, and the button itself.
    - `src/ui/flow.js` around line 1444 (READ ONLY) — `cls:"primary ahoyGlow"`, the ONE dynamic site
      that puts `primary` on an `.apBtn`. It inherits the base rule, so it needs no JS change.
  </read_first>
  <action>
    Change the `button.primary` rule so its declarations are the footer recipe: a 1.5px solid border in
    the Ko-Fi orange already used by `.footerKofi`, that rule's pale background, and its deep-amber
    text colour. Change `button.primary:hover` from the brightness filter to the same darker pale
    background `.footerKofi:hover` uses. Take the three colour values verbatim from the existing
    `.footerKofi` rules — do not introduce a fourth orange.

    Change `.apBtn.primary:hover` to match the same hover background, dropping the accent fill and the
    brightness filter, so an in-panel primary button does not flash solid orange on hover.

    Enumerate every primary button site and record the count in the SUMMARY: the static
    `class="primary"` occurrences in `index.html` and the one dynamic site in `src/ui/flow.js`. The
    ROADMAP says twelve; a direct count of the current tree finds nine static plus one dynamic. Report
    the real number rather than asserting the ROADMAP's — the fix is one rule either way, but the
    discrepancy must be recorded, not silently absorbed.

    For `#btnConfirmLeave`, apply the red `.footerLeave` recipe rather than the orange one, per the
    destructive-action design intent documented in the comment directly above it. Add the class to that
    one button in the markup; do not change its label text.

    Do not change `--accent` itself and do not touch any other rule that uses it — the accent colour is
    load-bearing elsewhere (the active-turn row border, the range input, `.seat.me`).
  </action>
  <verify>
    <automated>node scripts/ui_contract_check.js &amp;&amp; node scripts/narration_audit_check.js &amp;&amp; npm test</automated>
    <human-check>Drive Chrome per docs/DRIVING-THE-GAME.md §§1-3 and screenshot the front door, the lobby, the in-panel prompt with an `.ahoyGlow` primary button, and the leave-game confirm dialog.</human-check>
  </verify>
  <acceptance_criteria>
    - The `button.primary` rule's declaration block contains the same border colour, background and
      text colour as the `.footerKofi` rule, character for character on each value.
    - The `button.primary:hover` declaration block contains the same background value as
      `.footerKofi:hover`.
    - `grep -n 'button.primary' index.html` shows no declaration containing `var(--accent)`.
    - `grep -n '.apBtn.primary:hover' index.html` shows no declaration containing `var(--accent)` and
      no `filter` declaration.
    - `grep -c 'footerLeave' index.html` increases by exactly 1 from its pre-task value (record both).
    - `grep -c 'class="[^"]*primary' index.html` is unchanged from its pre-task value — no primary
      button was added or removed.
    - `npm test` exits 0.
    - Screenshots captured for all four surfaces, with the `.ahoyGlow` ring legible against the new
      pale fill (judged in 18-07's review).
  </acceptance_criteria>
  <done>Every primary button, static and dynamic, renders outline + faded fill with no solid-orange hover; the leave-game button uses the red destructive recipe; the true button count is recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the captain colour swatch everywhere, and close the gap it leaves</name>
  <files>src/ui/util.js, src/ui/lobby.js, index.html</files>
  <read_first>
    - `src/ui/util.js` lines 105-116 — `buildPlayerRows()`'s row template. Note the swatch span sits
      first inside `.prowTop`, and that `HEXCOL[i]` is ALSO used for the row background tint, the
      `--rowcol` custom property and the `.pname` colour — those three stay.
    - `src/ui/lobby.js` line 180 — `renderSeatList()`'s seat template. The lobby swatch is generated
      here in JS, NOT in static markup (the FIX-17 todo's `index.html:576` claim is wrong).
    - `index.html` lines 163-180 — the `.prowTop` base rule with its four-column
      `grid-template-columns` and two-row `grid-template-areas`, the `@media (max-width: 480px)`
      override with its own areas, and the four `.prowTop .{area}` assignment rules. The comment above
      explains why the recipe row is a second grid row inside the same grid; keep that property.
    - `index.html` line 188 — the swatch sizing rule inside `.player-row`.
    - `index.html` lines 757-760 — `.seat` (a flex row with a 10px gap, so removing a child closes the
      gap automatically — no column bookkeeping needed there) and its own swatch sizing rule.
  </read_first>
  <action>
    Delete the seat-colour swatch span from `buildPlayerRows()` in `src/ui/util.js` and from
    `renderSeatList()` in `src/ui/lobby.js`. Leave every other use of `HEXCOL[i]` in both templates
    untouched — the row tint, the `--rowcol` border colour and the name colour are how a captain stays
    identifiable once the circle is gone.

    In the `index.html` CSS block, drop the first (14px) column from `.prowTop`'s
    `grid-template-columns` and remove the swatch's area name from BOTH the base rule's
    `grid-template-areas` and the `@media (max-width: 480px)` override's, keeping each row string's
    remaining area order and the two-row structure exactly as it is. Delete the now-dead area
    assignment rule for the swatch, the swatch sizing rule inside `.player-row`, and the swatch sizing
    rule inside `.seat`.

    In the narrow-screen override, the leading empty cell that currently sits under the swatch column
    on the recipe row must be re-derived against the new three-column grid so the recipe still tucks
    under the captain's NAME and not under the row's left edge — that alignment is the documented
    intent of the existing rule and must survive.

    Do not remove `HEXCOL` itself or any other consumer of it.
  </action>
  <verify>
    <automated>node scripts/no_undef_check.js &amp;&amp; node scripts/ui_contract_check.js &amp;&amp; node scripts/module_graph_check.js &amp;&amp; npm test</automated>
    <human-check>Drive Chrome per docs/DRIVING-THE-GAME.md §§1-3, screenshot the lobby seat list before starting and the player rows during play, at 390px and at desktop width.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n 'class="dot"' src/ui/util.js src/ui/lobby.js | grep -vE '^[^:]+:[0-9]+: *//'` returns
      nothing (no emitting occurrence remains; comment lines are excluded from the count).
    - `grep -cE '^\s*\.(prowTop|player-row|seat)\s+\.dot' index.html` returns `0`.
    - `.prowTop`'s `grid-template-columns` declaration has exactly three column values.
    - Both `.prowTop` `grid-template-areas` declarations (the base rule and the narrow-screen override)
      contain exactly three area tokens per row string.
    - `grep -c 'HEXCOL' src/ui/util.js` decreases by exactly 1 from its pre-task value and
      `grep -c 'HEXCOL' src/ui/lobby.js` decreases by exactly 1 (record both before/after) — one use
      removed per file, the rest intact.
    - `npm test` exits 0.
    - In the driven session at 390px, `document.querySelector('.prowTop .pname').getBoundingClientRect().left`
      equals `document.querySelector('.prowRecipe').getBoundingClientRect().left` within 1px — the
      recipe still tucks under the name.
    - In the driven session, `document.querySelectorAll('.player-row .dot, .seat .dot').length` is `0`.
  </acceptance_criteria>
  <done>No colour circle renders beside any captain name in the player rows or the lobby, the rows close the gap, the recipe still aligns under the name on narrow screens, and every other use of the seat colour survives.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build BOTH narrow-screen chip treatments and render each at 320/375/390</name>
  <files>index.html</files>
  <precondition>Task 2 has landed — .prowTop is now a three-column grid with no swatch column. Authoring these areas against the old four-column grid produces a silently broken layout.</precondition>
  <read_first>
    - `index.html` — `.prowTop`'s base rule and `@media (max-width: 480px)` override AS AMENDED BY
      TASK 2, plus the comment above them explaining the recipe-row placement intent.
    - `index.html` line 191 — `.chip` (currently 34px square, with a 1px padding and a 95%-sized inner
      image), `.chips` (a wrapping flex row with a 3px gap), and the `.chip.have` / `.chip.extra`
      state variants which must keep working at any size.
    - `18-RESEARCH.md` "FIX-09" under Code Examples — the two candidate treatments, and the note that
      option B matches the precedent the recipe row's own narrow-screen treatment already set.
    - `docs/DRIVING-THE-GAME.md` §§1-3 for reaching a live board, and §6 for reading state.
  </read_first>
  <action>
    Author both candidates inside the existing `@media (max-width: 480px)` block, against the
    three-column grid Task 2 produced.

    Treatment A (the live default): keep the chips in their existing grid cell and shrink `.chip` to a
    smaller square so a full hold fits on one line instead of wrapping into a column. Choose the
    largest size that fits the widest realistic hold at 320px without wrapping, and record the number
    and the reasoning in the SUMMARY.

    Treatment B: give the chips their own full-width row by adding a third row to the narrow-screen
    `grid-template-areas`, matching the precedent the recipe row already set in this same rule, and
    leave `.chip` at its full size.

    Make B selectable rather than commented out: scope B's rules under a body-level class (for example
    `body.chipsOwnRow .prowTop { ... }`) so both treatments are live CSS that can be toggled at runtime
    by adding or removing that class, and neither is dead code. Add a short comment above the pair
    naming D-03, stating that exactly one of the two survives plan 18-07 and the other is deleted.

    Produce six renders — treatments A and B at each of 320, 375 and 390 — from a driven Chrome
    session on a live board with a captain holding a realistic mixed hold (some `.chip.have`, some
    plain, at least one `.chip.extra`), toggling the body class between captures. Save them under the
    phase directory with names that state the treatment and the width, and list the six paths in the
    SUMMARY so 18-07's checkpoint can present them.

    Do not change `.chips`'s flex behaviour, the gap, or the `.chip.have`/`.chip.extra` colours.
  </action>
  <verify>
    <automated>node scripts/ui_contract_check.js &amp;&amp; npm test</automated>
    <human-check>Drive Chrome per docs/DRIVING-THE-GAME.md §§1-3 to a live board, set the viewport to each of 320, 375 and 390 in turn, toggle `document.body.classList.toggle('chipsOwnRow')` between captures, and screenshot the players panel each time.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'chipsOwnRow' index.html` returns at least `1`, and the rule sits inside the
      `@media (max-width: 480px)` block.
    - `grep -c 'D-03' index.html` returns at least `1` — the comment naming the pending decision and
      the deletion obligation is present.
    - Six render files exist under
      `.planning/workstreams/prompts-polish/phases/18-prompts-polish/`, one per
      (treatment, width) combination, and all six paths are listed in the SUMMARY.
    - In each of the six driven captures, `document.querySelectorAll('.chips')[0].getClientRects().length`
      is `1` — the hold occupies a single line box in BOTH treatments at every width (this is the actual
      requirement; which one reads better is Wyatt's call).
    - At each width in both treatments, every `.chip` element's rendered width is at least 20px, so the
      icons stay legible.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Both treatments are live, toggleable CSS authored against the post-FIX-17 grid, each keeps the hold on one line at 320/375/390, and six renders exist for Wyatt to choose from.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
| Symbol / artifact | File | Kind |
|---|---|---|
| `button.primary` / `button.primary:hover` | index.html (CSS block) | changed rules — outline + faded fill |
| `.apBtn.primary:hover` | index.html (CSS block) | changed rule — no solid-orange hover |
| `footerLeave` class on `#btnConfirmLeave` | index.html (markup) | changed — destructive-action recipe |
| swatch span removed | src/ui/util.js, src/ui/lobby.js | deleted markup |
| `.prowTop` three-column grid + two-token-shorter areas | index.html (CSS block) | changed rules |
| `.prowTop .dot`, `.player-row .dot`, `.seat .dot` | index.html (CSS block) | deleted rules |
| FIX-09 treatment A (`.chip` narrow-screen size) | index.html (CSS block) | new rule — live default |
| FIX-09 treatment B (`body.chipsOwnRow .prowTop`) | index.html (CSS block) | new rule — toggleable candidate |
| Six renders, treatment x {320, 375, 390} | phase directory | new artifacts for the D-03 choice |
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan is presentation-only: CSS rules plus the removal of two static spans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-16 | Information Disclosure | seat identity after the swatch is removed | medium | mitigate | `HEXCOL[i]` still drives the row tint, the `--rowcol` active-turn border and the `.pname` colour; acceptance criteria pin that exactly one `HEXCOL` use is removed per file, and 18-07's review looks at the lobby before names lock in. |
| T-18-17 | Denial of Service | dead CSS shipping | low | mitigate | Both FIX-09 treatments are live and toggleable rather than commented out, and 18-07 deletes the loser; a comment naming D-03 and the deletion obligation is an acceptance criterion. |
| T-18-18 | Tampering | grid areas authored against a stale column count | medium | mitigate | Task 3 carries an explicit precondition that Task 2 has landed, and the acceptance criteria count area tokens per row string. |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A this phase — zero packages installed. |
</threat_model>

<verification>
- `npm test` green after each task.
- Driven Chrome captures at 320/375/390 for both FIX-09 treatments, plus lobby and player-row captures
  for FIX-17 and four button-surface captures for FIX-06.
- The by-eye judgements (FIX-09 choice, button look, `.ahoyGlow` legibility, lobby readability) are
  backstops resolved in 18-07, not in this plan.
</verification>

<success_criteria>
- FIX-06 and FIX-17 complete and green.
- FIX-09 has BOTH treatments live and six renders produced — D-03 satisfied, nothing picked blind.
- The real primary-button count is recorded against the ROADMAP's stated twelve.
</success_criteria>

<output>
Create `.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-06-SUMMARY.md` when done.
Record: the enumerated primary-button sites and the true count, before/after `HEXCOL` counts per file,
the chosen treatment-A chip size with its reasoning, and the six render paths.
</output>
