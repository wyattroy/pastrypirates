---
phase: 18-prompts-polish
workstream: prompts-polish
plan: 07
type: execute
wave: 6
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06"]
files_modified:
  - .planning/workstreams/prompts-polish/phases/18-prompts-polish/18-COPY-CHANGES.md
  - .planning/todos/pending/copy-shipped-vs-approved-gate.md
  - index.html
autonomous: false
requirements: [FIX-03, FIX-04, FIX-06, FIX-07, FIX-08, FIX-09, FIX-10, FIX-16, FIX-17, FIX-21]
tags: [phase-gate, copy-inventory, safari, visual-review]

must_haves:
  truths:
    - "Every copy change made in this phase is recorded against .planning/todos/pending/copy-shipped-vs-approved-gate.md, matched by label and literal text and never by line number (milestone constraint 3, ROADMAP criterion 13)."
    - "Exactly one FIX-09 treatment survives in the tree; the other is deleted, so no unreachable CSS ships (D-03 resolved)."
    - "npm test is green at the end of the phase, including determinism_baseline.js --verify (ROADMAP criterion 12)."
    - "No file in this phase's diff touches src/engine/index.js (ROADMAP criterion 12)."
    - { statement: "On a narrow window (320 / 375 / 390) and across an orientation change, the action button is never clipped — verified by Wyatt on real Safari.", verification: backstop }
    - { statement: "A narration line fades exactly where it sat with no sideways jump, and the box does not slice a still-fading line — judged by eye.", verification: backstop }
    - { statement: "The restyled primary buttons read correctly, including the .ahoyGlow ring against the new pale fill.", verification: backstop }
    - { statement: "The lobby seat list is still readable with the colour circle gone, before names lock in.", verification: backstop }
  artifacts:
    - "18-COPY-CHANGES.md: the phase's full copy-change ledger"
    - ".planning/todos/pending/copy-shipped-vs-approved-gate.md: an appended Phase 18 entry pointing at the ledger"
    - "index.html: exactly one FIX-09 narrow-screen chip treatment"
  key_links:
    - "each SUMMARY's recorded copy strings -> 18-COPY-CHANGES.md -> the copy-shipped-vs-approved gate"
    - "18-06's six renders -> the human checkpoint -> the surviving FIX-09 treatment"
---

<objective>
Close the phase: record every copy change against the copy-inventory gate, get Wyatt's by-eye
judgements on the four things an agent genuinely cannot confirm, act on his FIX-09 choice, and prove
the whole phase is green.

Purpose: this project has already lost approved copy once, by exactly the mechanism this plan
supplies — a human retyping a list with nothing comparing shipped source against approved
dispositions. And criterion 1 (narrow-window clipping) is explicitly a Safari check that no agent
can close; the original near-crash happened on Safari and that is where it must be re-verified.

Output: a copy ledger, a resolved D-03, one surviving treatment, and a green phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/prompts-polish/ROADMAP.md
@.planning/todos/pending/copy-shipped-vs-approved-gate.md
@.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-03-SUMMARY.md
@.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-04-SUMMARY.md
@.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-06-SUMMARY.md
</context>

<hard_constraints>
1. **Never re-match an approved copy row to source by line number.** The line-number keying drifted
   twice already. Match by label and by the literal text.
2. **D-16 is absolute:** Wyatt's approval notes are words only — the absence of an icon from a note is
   never an instruction to remove an icon.
3. **A gate must refuse rather than skip when its input is missing.** Do not add anything to this
   phase's verification that silently vanishes when a `.planning/` file is archived or stripped.
4. **Do not build the full copy comparison tool here.** Tasks 5/6/7 of the copy-gate todo (the
   per-row byte comparison, the applier, the permanent scope rule) are a separate, accepted residual
   and are explicitly out of this phase's scope. This plan records THIS phase's changes so the ledger
   does not fall further behind — it does not close the gap.
5. **No dead CSS ships.** The losing FIX-09 treatment is deleted, not commented out.
</hard_constraints>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the phase copy-change ledger and register it against the gate</name>
  <files>.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-COPY-CHANGES.md, .planning/todos/pending/copy-shipped-vs-approved-gate.md</files>
  <read_first>
    - `.planning/todos/pending/copy-shipped-vs-approved-gate.md` in full — especially "The chain that
      must not break" (`@copy` marker -> extractor id -> card id -> alias map -> the 209 dispositions),
      the two matching rules learned the hard way, and "The trap in the input" describing how an
      assertion whose input goes missing vanishes instead of failing.
    - `18-03-SUMMARY.md` — the storm-drift line removed (both viewer variants) and the confirmation
      that the seven `nobrk` wraps changed markup only, not text.
    - `18-04-SUMMARY.md` — the two new ruled strings recorded verbatim, plus the loser-addressed
      composite variant.
    - `18-02-SUMMARY.md` — the win-banner article change, which alters what
      `@copy adhoc.voyageend.victory` renders for eight recipes without renaming any recipe.
    - `18-05-SUMMARY.md` and `18-06-SUMMARY.md` — confirm which of their changes were copy and which
      were pure presentation, so the ledger's "no copy change" rows are asserted rather than assumed.
    - `scripts/extract_narration_lines.js` and `scripts/narration_audit_check.js` — how the `@copy`
      markers are harvested, so each ledger row can name the marker it belongs to.
  </read_first>
  <action>
    Create `18-COPY-CHANGES.md` in the phase directory as a table with one row per copy-affecting
    change, keyed by its `@copy` marker (or by the `EVENT_NARRATION` key where no marker exists) and
    by the literal text — never by file line number. Each row records: the requirement ID, the marker
    or key, the BEFORE literal, the AFTER literal, and the disposition (added, removed, rendering
    changed, or unchanged-markup-only).

    Cover at minimum: the two storm-drift variants removed together (FIX-04); the two new give-up
    strings and the loser-addressed composite variant added (FIX-07); the win-banner article change,
    recorded as a RENDERING change to its marker with no recipe renamed (FIX-08); the seven `nobrk`
    wraps recorded explicitly as markup-only with the enclosed text byte-identical (FIX-21); and the
    `setClockUI` pending branch recorded as reusing existing strings with no new copy (D-02). Add a
    row for any other change the SUMMARYs reveal — read them rather than working from this list alone.

    Include a short header stating which parts of the copy-shipped-vs-approved gap this ledger does
    and does NOT close: it records this phase's own changes so the ledger does not fall further behind;
    it does not implement the per-row byte comparison, the applier, or the permanent scope rule, which
    remain the accepted residual.

    Append a dated entry to `.planning/todos/pending/copy-shipped-vs-approved-gate.md` pointing at the
    ledger by relative path, listing the requirement IDs whose copy it covers, and noting that the
    residual is unchanged. Append — do not rewrite the file or alter any of its existing numbers.
  </action>
  <verify>
    <automated>node scripts/extract_narration_lines.js &amp;&amp; node scripts/narration_audit_check.js &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `18-COPY-CHANGES.md` exists in the phase directory and contains a row for every one of FIX-04,
      FIX-07, FIX-08 and FIX-21, plus an explicit no-new-copy row for the D-02 clock display.
    - Every row names a `@copy` marker or an `EVENT_NARRATION` key; `grep -c ':[0-9]' 18-COPY-CHANGES.md`
      returns `0` — no row is keyed by a file line number.
    - For each ADDED or REMOVED row, the recorded BEFORE/AFTER literal appears (or provably no longer
      appears) in the current source when grepped by that literal.
    - `.planning/todos/pending/copy-shipped-vs-approved-gate.md` gained exactly one appended dated
      entry: `git diff -U0 .planning/todos/pending/copy-shipped-vs-approved-gate.md | grep -c '^-'`
      returns `0` (nothing was removed or rewritten).
    - `node scripts/narration_audit_check.js` exits 0 with no assertion reported as skipped.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Every copy change in the phase is recorded by label and literal against the gate, the gate file is appended rather than rewritten, and the ledger states plainly what it does not close.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wyatt's Safari check, the by-eye judgements, and the FIX-09 choice</name>
  <what-built>
    Ten fixes across the narration panel, prompts, buttons and copy — everything in Phase 18. The
    agent-verifiable parts are already proven: `npm test` green including the determinism check,
    driven-Chrome measurements of button-reveal timing, ghost position, panel height floor, button
    containment at 320/375/390, clock arming, and the frozen clock display.

    Four things remain that no agent can confirm, and one choice only Wyatt can make.
  </what-built>
  <how-to-verify>
    Start the local server (`npm start`, which serves on port 8000) and open the game in **Safari** —
    the browser matters here, not just the width. Per this project's own history, the original
    near-crash was Safari-specific and Safari is where the panel work must be re-verified.

    1. **Criterion 1 — narrow-window clipping (Safari, required).** Narrow the window to roughly 320,
       then 375, then 390 points wide. At each width, play until an action prompt with buttons
       appears. Confirm the button row is fully visible and not cut off at the bottom of the blue/yellow
       box. Then rotate the device (or resize the window between portrait-ish and landscape-ish) while
       a prompt is on screen and confirm the button is still fully visible afterwards.

    2. **Criterion 2 — the fade (by eye).** Watch several narration lines replace each other. Confirm
       the outgoing line fades exactly where it was sitting — no jump sideways — and that the box does
       not shrink and slice through a line that is still fading.

    3. **FIX-06 — the buttons.** Look at the "Create the game", "Join the voyage", "Start the voyage",
       "At the helm!", "Send feedback", "Everyone's aboard?" and "Start a fresh voyage" buttons, and at
       an in-panel primary button with the pulsing ring. They should now be outlined with a pale fill
       like the footer buttons rather than solid orange. Confirm the pulsing ring is still legible
       against the paler fill, and that "Aye, leave the game" reads as the red destructive button.

    4. **FIX-17 — the lobby.** Open a lobby before anyone has set a name. With the colour circles gone,
       confirm the seats are still easy to tell apart.

    5. **FIX-09 — your choice (D-03).** Six renders are attached from plan 18-06: treatment A (smaller
       chips, chips stay on the captain's row) and treatment B (chips get their own full-width row),
       each at 320, 375 and 390. Pick one. The other is deleted.
  </how-to-verify>
  <resume-signal>
    Reply with: (a) "safari ok" or a description of what clipped and at which width; (b) "fade ok" or
    what you saw; (c) "buttons ok" or what to change; (d) "lobby ok" or what to change; and (e)
    "chips: A" or "chips: B".
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply the FIX-09 choice, delete the loser, and prove the phase green</name>
  <files>index.html</files>
  <precondition>Task 2's checkpoint returned a chips choice of A or B. Do not guess if the reply is ambiguous — ask again rather than delete the wrong treatment.</precondition>
  <read_first>
    - `index.html`'s `@media (max-width: 480px)` block as written by 18-06 Task 3 — both treatments and
      the comment naming D-03 and the deletion obligation.
    - `18-06-SUMMARY.md` — the treatment-A chip size and its reasoning, and the six render paths.
    - `.planning/workstreams/prompts-polish/ROADMAP.md` Phase 18 success criteria 1-13, to walk the
      final gate against the list rather than from memory.
  </read_first>
  <action>
    Delete the losing treatment's rules entirely — including the body-class scoping selector if
    treatment B lost, and the D-03 comment naming the pending decision, which is spent once the choice
    is made. Replace that comment with a one-line note recording WHICH treatment Wyatt chose and when,
    so the next reader does not re-litigate it. Leave the winner's rules exactly as authored; do not
    re-tune them in the same edit.

    Then walk the ROADMAP's thirteen success criteria one at a time and record, per criterion, the
    concrete evidence that closes it: the test command and its exit status, the driven-session
    measurement, or Wyatt's checkpoint reply. Any criterion whose only evidence is a human reply is
    recorded as closed BY that reply, quoted — not as an agent-asserted pass.

    Run the full suite one final time and confirm the phase's whole diff touches no forbidden file.
  </action>
  <verify>
    <automated>npm test &amp;&amp; git diff --stat main -- src/engine/index.js</automated>
    <human-check>One final driven-Chrome pass per docs/DRIVING-THE-GAME.md §5b: play a solo game through to an end of voyage at 390px, confirming no console errors and that the win banner, award cards, player rows and prompts all render.</human-check>
  </verify>
  <acceptance_criteria>
    - Exactly one narrow-screen chip treatment remains: `grep -c 'chipsOwnRow' index.html` returns `1`
      if B won and `0` if A won, and the corresponding other treatment's rules are absent.
    - `grep -c 'D-03' index.html` returns `1` and the surviving line records the chosen treatment and
      the date, not a pending decision.
    - `npm test` exits 0, including `determinism_baseline.js --verify`.
    - `git diff --stat main -- src/engine/index.js` produces no output — the engine is untouched across
      the entire phase.
    - `git diff --name-only main` lists no file outside this workstream's ownership except
      `src/ui/board.js`, and that file's diff is exactly the two coordinated FIX-08 lines (or zero
      lines, if 18-02's checkpoint chose the handoff option).
    - The final driven end-of-voyage session at 390px completes with zero uncaught console errors.
    - All thirteen ROADMAP success criteria are recorded in the SUMMARY with named evidence, and every
      criterion closed only by a human reply quotes that reply.
  </acceptance_criteria>
  <done>One treatment ships, the phase's thirteen criteria each carry named evidence, the engine is provably untouched, and the full suite is green.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
| Symbol / artifact | File | Kind |
|---|---|---|
| Phase 18 copy ledger | 18-COPY-CHANGES.md | new file |
| Phase 18 gate entry | .planning/todos/pending/copy-shipped-vs-approved-gate.md | appended entry |
| surviving FIX-09 treatment + decision record comment | index.html (CSS block) | changed — loser deleted |
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved copy -> shipped source | the ledger is the only artifact connecting the two for this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-19 | Repudiation | unrecorded copy divergence | high | mitigate | The ledger records every change by marker and literal, never by line number; `narration_audit_check.js` must exit 0 with no skipped assertion, and the gate file is appended rather than rewritten (pinned by a zero-deletions diff criterion). |
| T-18-20 | Denial of Service | Safari-specific panel regression shipping unseen | critical | mitigate | Criterion 1 is a blocking human checkpoint on real Safari — it cannot be closed by an agent, is declared `verification: backstop` in `must_haves`, and Task 3 refuses to record it as passed without a quoted human reply. |
| T-18-21 | Tampering | deleting the wrong FIX-09 treatment | medium | mitigate | Task 3 carries a precondition forbidding a guess on an ambiguous reply, and the surviving-count criterion is asserted per treatment. |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A this phase — zero packages installed across all seven plans. |
</threat_model>

<verification>
- `npm test` green.
- `git diff --stat main -- src/engine/index.js` empty.
- Thirteen ROADMAP criteria each recorded with named evidence; four of them closed only by Wyatt's
  quoted checkpoint reply.
</verification>

<success_criteria>
- Criterion 13 (every copy change recorded) closed by the ledger.
- Criterion 12 (no engine touch) closed by an empty engine diff across the phase.
- Criteria 1, 2, 8 and 10's by-eye halves closed by Wyatt's reply, quoted — never agent-asserted.
- D-03 resolved and exactly one treatment in the tree.
</success_criteria>

<output>
Create `.planning/workstreams/prompts-polish/phases/18-prompts-polish/18-07-SUMMARY.md` when done.
It must contain the thirteen-criterion evidence table and Wyatt's checkpoint reply quoted verbatim.
</output>
