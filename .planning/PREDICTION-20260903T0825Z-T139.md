# PREDICTION — `T-139`, written 2026-09-03T08:25Z, BEFORE any measurement

**The item.** `.planning/CHART.md:94` — *"Your ruling: do you want `SCHEDULED` to stop hiding your
ideas? **He answered; not yet built.**"* His ruling (`.claude/memory/DECISIONS.md:695`): three
states — **OPEN shows, SCHEDULED shows and says so, PARKED shows dimmed with its reason, and only
genuinely-finished words hide.**

**What I have read so far, and it is READING, not measuring.** `scripts/wyclau/glass.mjs:606-645`
carries a comment block headed *"⚑ THREE STATES, NOT TWO — WYATT'S RULING, question UI,
2026-09-02"*, and below it a filter that drops only `state !== "finished"` and tags the survivors
`SCHEDULED · …` / `PARKED · …`. **A comment is not a measurement (rule 6) and neither is a filter I
have not run.**

## WHAT I EXPECT, AND WHY

**P1 — HIS RULING IS ALREADY BUILT, and the row is stale.** The filter at `glass.mjs:640-645` is
his three states exactly. I expect running the page's own logic over the live `CHART.md` to show
**zero ideas hidden by `SCHEDULED` alone**, against the 9 that were hidden when the question was
put to him.
*Falsifier F1:* if any idea block in the live Chart whose declared fate is `SCHEDULED` is absent
from the rendered Tasks list, it is NOT built and I am wrong.

**P2 — the row got its "not yet built" from a MOVE, not from a check.** The row's own note says it
was *"lifted out of `## RULED`"* by watch c1 at 07:1xZ because the card carrying it is being
deleted. I expect the git history of `CHART.md` to show the row's text carried across unchanged
from the `## RULED` table, where it had been sitting since he answered — i.e. nobody re-checked the
claim, they preserved it.
*Falsifier F2:* if the phrase "not yet built" was written fresh at 07:1xZ against a measurement, or
if it predates his 12:28:02Z answer, my account of how it got stale is wrong.

**P3 — there is ONE copy of the rule, not two.** `glass.mjs:622-637` says it stopped owning a copy
and now imports `stateOf` from `lib/chart_model.mjs` after the two diverged by eleven rows. I
expect exactly one definition of the three-state `stateOf` in the repo, imported by both the page
and the Chartkeeper.
*Falsifier F3:* a second `stateOf`/fate-word list living anywhere else means rule 23 is still open
here and the item is not finished, whatever the page currently renders.

**P4 — the deliverable of this watch is a CLOSE, not a build.** If P1–P3 hold, the honest work is:
a check that can go RED on the old behaviour and GREEN on the live one, then `close_item.mjs`.
*Falsifier F4:* if the check I write cannot be made to fail against the pre-ruling behaviour, it is
not a measurement and I must not close on it.

## WHAT HAPPENED IMMEDIATELY BEFORE (rule: widen the time horizon)

The row did not appear because anything broke. It appeared because **a surface was being retired** —
Wyatt asked for the *"Your rulings in hand"* box removed, and c1 checked what only lived there.
Four rulings had empty `now` cells and would have dropped off his page silently. So the trigger for
this row is a **card deletion**, not a defect, and that is exactly the condition under which a
stale claim gets copied forward without being re-tested.

## THE ONE THING THAT WOULD MAKE ME WRONG ABOUT THE WHOLE ITEM

If the three-state filter is live in `glass.mjs` but the **published page** he actually reads was
rendered before it landed, then it is built and he still cannot see it — and the item is open for a
reason nothing in the source can show me. **I cannot read the live artifact from this session (no
Artifact tool), so I must say that boundary out loud rather than close over it.**
