# PREDICTION — `T-102`, written 2026-09-03T06:55Z, BEFORE any measurement

Watch b2, Wy-Blade. Rule 6's working form: what I expect, why, and what would prove me wrong —
written before the result exists so it cannot be retrofitted.

**His ask, verbatim (2026-09-02, 3:07 PM ET, on the Glass):** *"Consider whether this file should be
generated by a script from the actual pages rather than hand-maintained — a hand-kept sitemap goes
stale silently the next time a page is added. **Recommend, don't just build; flag it as a small job
and let me decide.**"*

**WHAT HAPPENED IMMEDIATELY BEFORE (step 0).** A watch built `scripts/qa/sitemap_write.mjs` under
`T-098` before this note was ever fated, then `T-098` closed. CEO 142's finding 3 says the choice
was never put to him. So the risk here is not that nothing was done — it is that **something
adjacent was done and the row now looks handled.**

## What I expect to find

1. **The page LIST in `sitemap.xml` is correct TODAY and his risk is still entirely unaddressed.**
   `sitemap_write.mjs:12-16` says in its own comment that it reads the URL list *out of the existing
   file* and deliberately does not discover pages. So it derives the DATES from git — which is the
   half he asked for in his *first* instruction — and leaves the LIST hand-kept, which is the half
   this sentence is about. **The generator that exists is not the generator this ask names.**

2. **Nothing in the 100+ gate chain goes red when a public page is missing from the sitemap.**
   `sitemap_lastmod_check.mjs` walks *sitemap → files* (`locToFile`), so it can only see an entry
   pointing at a page that does not exist. **The opposite direction — a page that exists with no
   entry — is invisible to it by construction.** That is the same shape as the parity gate aimed at
   the wrong tree (`CLAUDE.md` §2): green forever, on a question it never asks.

3. **The risk is about to become real, not hypothetical.** `T-100` builds `/rules.html`, and it is
   already speced and waiting only on his question 1. The next page added to this site is a page
   already on the Chart.

## What would prove me WRONG

- If `sitemap_lastmod_check.mjs` (or any other gate) enumerates public pages from the directory or
  from `robots.txt` and asserts the sitemap covers them, **his risk is already guarded** and the
  honest recommendation is "nothing to do — tell him it is handled."
- If `sitemap.xml` is missing a page that exists and is public TODAY, then this is not a
  future-tense risk at all and the recommendation is a fix, not a question.
- If more than two genuinely public pages exist at the root, prediction 1 is wrong about the list
  being correct today.

## What I will NOT do

He wrote *"Recommend, don't just build."* So the deliverable is a measurement, a recommendation and
a question put to him — **not a page-discovering generator.** If I find myself writing one, I have
substituted my judgement for his, which is the fault this note exists to catch.
