---
phase: 13-multiplayer-turn-clock
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/ui/panel.js
autonomous: true
requirements: [CLOCK-03]

must_haves:
  truths:
    - "When paused, clicking the large pause symbol (#shotClockNum, rendering PAUSE_SYMBOL_IMG) resumes the clock — in BOTH solo and multiplayer (D-08/D-09)."
    - "The #shotClockNum onclick handler and its pointer cursor are cleared in every non-paused setClockUI() branch, so the standing 500ms re-render never leaves a stale click-to-resume handler live on a non-paused clock (RESEARCH Anti-Pattern 4)."
    - "The clickable symbol reaches togglePause via the netHandlers().onTogglePause seam (panel.js is a ui-tier module and cannot import src/orchestrator.js directly — the module-graph check forbids it)."
    - "The existing small #scPause corner button continues to resume exactly as before — the big symbol is an ADDED second resume affordance, not a replacement (D-09), and no new large overlay/artwork is introduced (D-08)."
    - "node scripts/determinism_baseline.js --verify stays green 30/30 — the change is DOM-render-only, never touches src/engine/ (D-10/VERIFY-02)."
  prohibitions:
    - "MUST NOT add a new large overlay, modal, or artwork — only the existing large pause symbol becomes clickable (D-08)."
    - "MUST NOT make the whole clock panel clickable — only #shotClockNum (D-08)."
    - "MUST NOT let any change reach src/engine/ or alter lockstep replay."
  artifacts:
    - "src/ui/panel.js — #shotClockNum made clickable (cursor + onclick → onTogglePause) in the paused branches; onclick/cursor reset in every non-paused branch"
  key_links:
    - "Depends on 13-01: togglePause() and the onTogglePause seam entry in main.js's setNetHandlers must already exist; panel.js already imports netHandlers from ./handlers.js."
    - "A single top-of-function reset (numEl.onclick=null; numEl.style.cursor='') right after numEl is resolved guarantees the no-stale-handler invariant across every non-paused branch, with the handler set only in the paused branches."
---

<objective>
Deliver CLOCK-03: make the large "PAUSED" symbol in the center of the shot clock
a clickable button that resumes the game, in both solo and multiplayer (D-08/D-09).
This is a small UI expansion on top of the pause-sync seam established by 13-01 —
the same togglePause path, now reachable by clicking the big symbol as well as the
small corner button.

Purpose: give players an obvious, large resume target where their eye already is
(the paused symbol) instead of hunting for the small corner control.
Output: #shotClockNum gains a pointer cursor + onclick in the paused branches and
has its handler defensively cleared everywhere else.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multiplayer-turn-clock/13-CONTEXT.md
@.planning/phases/13-multiplayer-turn-clock/13-RESEARCH.md
@.planning/phases/13-multiplayer-turn-clock/13-VALIDATION.md
</context>

## Artifacts This Phase Produces (NEW behavior — for source-grounding drift exclusion)

- `#shotClockNum` clickable-to-resume affordance in src/ui/panel.js `setClockUI()`
  (paired with a defensive onclick/cursor reset covering every non-paused branch).

(Sibling plan 13-01 produces the pause-sync symbols; 13-02 the boot version-guard.)

<tasks>

<task type="auto">
  <name>Task 1: Make the large paused symbol (#shotClockNum) clickable-to-resume, solo and multiplayer</name>
  <files>src/ui/panel.js</files>
  <read_first>
    - src/ui/panel.js setClockUI() (~L51-133): where numEl (#shotClockNum) is resolved (~L63); the TWO paused branches that render PAUSE_SYMBOL_IMG / the paused countdown (~L82-85 and ~L98-106 — after 13-01 these are gated on appState.shotClockPaused alone); and EVERY other branch that writes numEl and returns without the pause symbol: timer-off (~L77-79), waiting/turn-clock (~L94-95), and the active/waiting countdown branches (~L125, ~L132).
    - src/ui/panel.js imports (~L46): netHandlers is already imported from ./handlers.js — call netHandlers().onTogglePause() (the seam entry 13-01 registered in main.js). Do NOT import togglePause from orchestrator directly (module-graph forbids ui→orchestrator).
    - RESEARCH.md "Code Examples → setClockUI() de-gating and clickable-symbol wiring" for the exact onclick/cursor pairing and the "clear in every other branch" warning (Anti-Pattern 4).
  </read_first>
  <action>
Make #shotClockNum a resume button only while paused, and guarantee it is inert otherwise. Immediately after numEl is resolved near the top of setClockUI(), reset it defensively each tick: clear its onclick to null and clear its pointer cursor — this single reset covers every non-paused branch at once (the function re-runs on the 500ms interval, so a handler set in a prior paused tick must not survive into a later non-paused tick). Then, inside BOTH paused-render branches, set numEl's cursor to pointer and set numEl.onclick to a handler that calls netHandlers().onTogglePause() — routing through the seam so the same togglePause path the corner #scPause button uses is invoked, which resolves correctly in both solo (local toggleShotClockPause) and multiplayer (networked netSetPaused). Leave the small #scPause corner button and its wiring untouched; the big symbol is an additional affordance. Add no overlay, modal, or new artwork, and do not make any element other than #shotClockNum clickable.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify</automated>
    <automated>npm test</automated>
    <human-check>
      SOLO (MCP/manual): start a solo game, click the corner ▶/⏸ to pause; while #shotClockPanel's wrap has class "paused", do document.getElementById('shotClockNum').click() → assert window.__pp_app_state_debug().shotClockPaused flips to false (resumed). MULTIPLAYER (2-tab, unique pp_id per tab): pause from any tab, then .click() #shotClockNum on a tab → assert shotClockPaused becomes false on BOTH tabs and play resumes from the remaining time. Regression: while NOT paused (active countdown), confirm clicking #shotClockNum does nothing (onclick was reset) — no accidental pause.
    </human-check>
  </verify>
  <acceptance_criteria>
    - src/ui/panel.js resets numEl.onclick (to null) and clears numEl's cursor once per setClockUI() invocation before the branch logic, so non-paused branches never carry a live handler.
    - Both paused-render branches set numEl.style.cursor to a pointer and set numEl.onclick to a handler invoking netHandlers().onTogglePause().
    - panel.js does NOT import togglePause from src/orchestrator.js (routes via the netHandlers seam); `npm test` module-graph gate stays green.
    - No new overlay/modal/artwork is added; only #shotClockNum is made clickable; #scPause wiring is unchanged.
    - `npm test` exits 0 and `node scripts/determinism_baseline.js --verify` prints 30/30.
    - Behavior: clicking the big paused symbol resumes in both solo and multiplayer; clicking #shotClockNum while not paused does nothing.
  </acceptance_criteria>
  <done>Clicking the large paused symbol resumes the game in both solo and multiplayer via the togglePause seam, the handler is cleared on every non-paused tick (no stale-click bug), the corner button is unchanged, and npm test + determinism 30/30 are green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click on `#shotClockNum` → `netHandlers().onTogglePause()` → togglePause | same-origin DOM interaction; introduces no NEW external surface — routes to the pause path 13-01 already established |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | (no new trust boundary) | clickable `#shotClockNum` resume affordance | low | accept | CLOCK-03 only makes an already-rendered element clickable, routing to the same `togglePause` path introduced in 13-01; it inherits 13-01's accepted pause-spam disposition (T-13-01) and adds no new boundary or privilege. The only functional risk (a stale onclick surviving a re-render) is mitigated by the per-tick reset, not a security control. |

_No package-manager installs in this plan, so no T-13-SC supply-chain checkpoint applies._
</threat_model>

<verification>
- `node scripts/determinism_baseline.js --verify` green 30/30 (D-10/VERIFY-02).
- `npm test` green (per-wave full suite; module-graph gate confirms no ui→orchestrator import).
- Manual/MCP (per 13-VALIDATION.md): big-symbol click resumes in solo and multiplayer (all tabs); clicking while not paused is inert.
</verification>

<success_criteria>
The large "PAUSED" image resumes the clock when clicked, consistently in solo and multiplayer, without a stale-handler bug and without any new overlay; the corner button still works; determinism 30/30 stays green.
</success_criteria>

## Flagged Planner Assumptions (spec-less edge probe — surfaced, not dropped)

- CLOCK-03's edge (the "unclassified" probe row) is RESOLVED into the must_haves above as the "clear onclick/cursor in every non-paused branch" truth (RESEARCH Anti-Pattern 4). No unresolved CLOCK-03 edge remains.
