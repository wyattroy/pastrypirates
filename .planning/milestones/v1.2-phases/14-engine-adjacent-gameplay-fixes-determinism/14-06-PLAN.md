---
phase: 14-engine-adjacent-gameplay-fixes-determinism
plan: 06
type: execute
wave: 5
depends_on: ["14-05"]
files_modified:
  - src/ui/util.js
  - package.json
  - src/engine/index.js
  - .planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md
autonomous: false
requirements: [STORM-01, AI-01, VERIFY-02]

must_haves:
  truths:
    - "Wyatt sees the complete storm-copy list — the nine existing lines reused as-is, the three new `moored` variants, and the refused-hail clause — and edits or approves it before the phase closes (D-14, D-27)."
    - "No storm or outcome copy ships without passing through that approval; auto-generated copy never substitutes for it (D-14, project precedent from NARR-06)."
    - "The three new test scripts are wired into `npm test` so they are standing gates rather than scripts that rot."
    - "`npm test` passes every gate, including the three new ones, against the re-recorded baseline (VERIFY-02)."
    - "A human confirms in Safari AND in Chrome that the boat steps square by square through a full storm push, that each bot outcome gets its own narration line, and that a bot which hails takes no other action that turn."
    - "The forced-storm test hook (`cfg.storm=1`) is reverted before the phase closes — no test scaffolding ships."
    - "`14-VALIDATION.md` carries a completed Per-Task Verification Map and is marked `wave_0_complete: true` and `nyquist_compliant: true` only after every row is green or explicitly accounted for."
  artifacts:
    - path: "package.json"
      provides: "The new test scripts wired into the standing gate list"
      contains: "bot_storm_narration_test.js"
    - path: ".planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md"
      provides: "Completed validation sign-off for the phase"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "package.json test script"
      to: "scripts/hail_ranking_test.js, scripts/storm_moored_reason_test.js, scripts/bot_storm_narration_test.js"
      via: "each new script is appended to the && chain so it runs on every npm test"
      pattern: "hail_ranking_test\\.js"
  prohibitions:
    - statement: "MUST NOT auto-generate or silently finalise storm copy in place of Wyatt's approval — by project precedent (PROJECT.md Key Decisions, and how NARR-06 shipped) this copy is authored by him, and presenting a fait accompli defeats the gate."
      requirement_id: STORM-01
      category: values
    - statement: "MUST NOT leave the forced-storm test hook in the shipped source — a temporary `cfg.storm=1` used to reach a storm during UAT is scaffolding, and shipping it would make every round a storm."
      requirement_id: STORM-01
      category: safety
---

<objective>
Close the phase: put the storm copy in front of Wyatt for his edit, make the new tests standing gates,
and confirm in both target browsers that the storm and the hail actually read the way the phase set
out to make them read.

Purpose: D-14 is an in-phase approval gate with real project precedent — storm copy is authored by
Wyatt, not auto-generated, which is how NARR-06 shipped in v1.0. D-27 reframed what he is being asked:
most of the lines already exist and just needed surfacing, so the ask is "keep, punch up, or rewrite"
rather than a blank page. And the visible behaviours this phase fixed are, by their nature, the ones a
harness cannot see.

Output: approved copy in the shipping table, three new gates in `npm test`, and a signed-off
validation record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-CONTEXT.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-RESEARCH.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-02-SUMMARY.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-05-SUMMARY.md

<interfaces>
<!-- The nine existing storm/outcome lines D-27 established already read correctly for a bot subject.
     These are presented for "keep / punch up / rewrite", not written from scratch. -->
windmove     src/ui/util.js:248   `${pn(e.p)} is carried by the storm`
blownOut     src/ui/util.js:249   `⛵ A gale blows ${pn(e.p)} off the dock!`
dodge        src/ui/util.js:251   `${pn(e.p)} pays 1🌕 to anchor safely`
anchor       src/ui/util.js:252   `${pn(e.p)} flips ⚪HEADS — dodges the rocks!`
moored       src/ui/util.js:253   (now three variants — the genuinely new copy)
blocked      src/ui/util.js:254   `Spotting ${pn(e.other)} dead ahead, ${pn(e.p)} strikes sail and holds fast.`
anchorHold   src/ui/util.js:255   `${pn(e.p)}'s anchor already down — it holds fast, no need to pay twice in one storm ⚓`
aground      src/ui/util.js:258   `${pn(e.p)} flips ⚫TAILS — runs aground! …`
shipwrecked  src/ui/util.js:261   `${pn(e.p)} is shipwrecked, and spends their turn making repairs.`
parley       src/ui/util.js:257   (now carries the refused-hail action-cost clause — also new)

<!-- package.json — the gate list is a single && chain in the "test" script string -->
"test": "node scripts/determinism_baseline.js --verify && node scripts/engine_contract_check.js && node scripts/dlog_replay_test.js && node scripts/net_registry_test.js && node scripts/net_contract_check.js && node scripts/state_contract_check.js && node scripts/module_graph_check.js && node scripts/ui_contract_check.js && node scripts/no_undef_check.js"

<!-- Tuning knobs Wyatt may want moved during UAT (all in src/ui/util.js, from 14-05) -->
BOT_MSG_HOLD_MULTIPLIER, STORM_STEP_MS, BOT_STORM_STEP_MS, and (existing) MSG_HOLD_MULTIPLIER
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan adds no new symbols. It finalises `EVENT_NARRATION`'s new/changed lines in `src/ui/util.js`,
appends three gates to `package.json`'s `test` script, and closes out `14-VALIDATION.md`.

Full phase inventory, for the record: `scripts/determinism_diff.js` (+ `diffAllSeeds`,
`--assert-clean`, `--ignore-keys`, `--json`), `docs/DETERMINISM-RERECORD.md`, `rankHailTargets`,
`priceHailOffer`, `hailWorthIt`, `HAIL_BASE_PRICE`, `HAIL_RESERVE`, `kind:"hail"` on the `parley`
event, `Game.mooredReason`, the `moored` event `reason` values (`justDocked` / `dock` / `home`),
`wind2` now present on every simulator event, `botWindLeg`, `botMsgHoldMs`,
`BOT_MSG_HOLD_MULTIPLIER`, `STORM_STEP_MS`, `BOT_STORM_STEP_MS`, `flash`'s `holdMs` parameter, the
three-way `EVENT_NARRATION.moored` branch, and the test scripts `scripts/hail_ranking_test.js`,
`scripts/storm_moored_reason_test.js`, `scripts/bot_storm_narration_test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Storm copy — Wyatt's edit pass (D-14, D-27)</name>
  <decision>Approve, punch up, or rewrite the storm and hail narration lines this phase surfaces and adds.</decision>
  <context>
By project precedent this copy is authored by Wyatt, not auto-generated (PROJECT.md Key Decisions:
"Storm-text audit produces a list back to Wyatt for rewrite"; that is how NARR-06 shipped in v1.0).
D-27 narrowed the ask: research confirmed that all nine storm event types already have narration lines
written in shared third person that read correctly whether the subject is a human or a bot. Until this
phase, bots simply never got them shown. So most of this list is "these already exist and will now
appear during bot pushes — keep them, or punch them up?", and only a few lines are genuinely new.

Present two clearly separated groups, with the exact current strings:

**Group A — existing lines, now surfaced per-square for bots (keep / punch up / rewrite).** The nine
entries listed in `<interfaces>` above. Note for each that it will now appear once per outcome during
a bot's storm push, at the shorter bot pacing, where previously at most one outcome per push was ever
narrated.

**Group B — genuinely new copy (edit before it ships).** Read these verbatim out of
`14-05-SUMMARY.md` and `14-02-SUMMARY.md`:
1. `moored` / `justDocked` — you docked here deliberately last turn, so the storm cannot drag you into land.
2. `moored` / `dock` — the storm blew you onto a dock and you steadied against it. This is the lucky
   save D-20 deliberately kept; Wyatt's own wording for it was *"you're able to steady your boat
   against the dock to not be blown aground"*.
3. `moored` / `home` — moored at Tortuga; the harbour holds you.
4. The refused-hail closing clause on `parley`, which makes the bot visibly paying its action cost
   read as a rule rather than a glitch (D-24).

Also flag, briefly, that the pacing is tunable at UAT via four named constants
(`MSG_HOLD_MULTIPLIER`, `BOT_MSG_HOLD_MULTIPLIER`, `STORM_STEP_MS`, `BOT_STORM_STEP_MS`) — he does
not have to decide now, but he should know the knob exists before Task 3's playtest.
  </context>
  <options>
    <option id="approve-as-drafted">
      <name>Ship the drafted lines as written</name>
      <pros>Nothing else to do; the copy already reads in-voice and is sized for the new pacing.</pros>
      <cons>Copy is Wyatt's craft surface, and a first draft rarely survives contact with the actual on-screen rhythm.</cons>
    </option>
    <option id="edit-group-b">
      <name>Edit the four new lines, keep the nine existing ones</name>
      <pros>Focuses his attention where the copy is genuinely new; the nine existing lines already shipped and read correctly.</pros>
      <cons>Those nine will now appear far more often than before, so a line that was fine once per push may grate at four.</cons>
    </option>
    <option id="edit-everything">
      <name>Rewrite across both groups</name>
      <pros>Treats the whole storm sequence as one piece of writing, which is what a player actually experiences.</pros>
      <cons>Larger edit pass; overlaps Phase 15's narration audit (NARR-01), which is the natural home for a full sweep.</cons>
    </option>
  </options>
  <resume-signal>Reply with the approved text for each edited line, or "approve-as-drafted". Name any pacing constant you want changed and its new value.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the approved copy, make the new tests standing gates, sign off validation</name>
  <files>src/ui/util.js, package.json, .planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md</files>
  <precondition>Task 1's checkpoint was answered and the approved text for every edited line is on the record.</precondition>
  <read_first>
    - Wyatt's answer from Task 1
    - `src/ui/util.js` lines 225-334 (`EVENT_NARRATION` — the entries being edited)
    - `package.json` (the nine-gate `test` script string)
    - `.planning/phases/14-.../14-VALIDATION.md` (the Per-Task Verification Map and Sign-Off block)
  </read_first>
  <action>
Apply Wyatt's approved text to `EVENT_NARRATION` in `src/ui/util.js`, exactly as given. Do not
"improve" it in passing — his wording is the deliverable. If he named new values for any pacing
constant, apply those too. If he approved as drafted, change nothing and say so in the SUMMARY.

Append the three new scripts to `package.json`'s `test` chain, after `no_undef_check.js`, in the same
`&& node scripts/<name>` form the existing nine use: `hail_ranking_test.js`,
`storm_moored_reason_test.js`, `bot_storm_narration_test.js`. Also add a convenience script
`"test:determinism-diff": "node scripts/determinism_diff.js"` alongside the existing
`test:determinism`, so the divergence tool is discoverable next to the oracle it complements. Do not
reorder or remove any existing gate — the determinism oracle stays first.

Update `.planning/phases/14-.../14-VALIDATION.md`: fill the Per-Task Verification Map with the real
per-task status now that every plan has run, tick the Wave 0 Requirements checkboxes (the full
per-seed diff, the first-storm-round assertion, the pure hail logic, and the `moored` reason
assertion are all delivered), tick the Validation Sign-Off boxes, and set `wave_0_complete: true`,
`nyquist_compliant: true` and `status: validated` in the frontmatter. Record the D-26 finding from
14-04 in the sign-off notes: the literal pre-storm assertion was implemented, run, and its result
carried to Wyatt's decision rather than being softened.

Then run the full suite and confirm every gate is green — twelve now, not nine.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e 'const p=require("./package.json");for(const s of ["hail_ranking_test.js","storm_moored_reason_test.js","bot_storm_narration_test.js"])if(!p.scripts.test.includes(s)){console.error("gate not wired: "+s);process.exit(1)}if(!p.scripts.test.startsWith("node scripts/determinism_baseline.js --verify")){console.error("the determinism oracle is no longer the first gate");process.exit(1)}if(!p.scripts["test:determinism-diff"]){console.error("test:determinism-diff convenience script missing");process.exit(1)}console.log("gate list OK: "+p.scripts.test.split("&&").length+" gates")'</automated>
    <automated>node -e 'const s=require("fs").readFileSync(".planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md","utf8");for(const k of ["wave_0_complete: true","nyquist_compliant: true","status: validated"])if(!s.includes(k)){console.error("validation not signed off: missing "+k);process.exit(1)}if(s.includes("*TBD by planner*")){console.error("the Per-Task Verification Map is still a placeholder row");process.exit(1)}console.log("validation signed off")'</automated>
  </verify>
  <acceptance_criteria>
    - Every line Wyatt edited appears in `src/ui/util.js` verbatim as he wrote it; nothing he did not ask for was reworded.
    - `package.json`'s `test` script runs twelve gates, with `determinism_baseline.js --verify` still first and the three new scripts appended.
    - `test:determinism-diff` exists as a convenience script.
    - `npm test` exits 0 with all twelve gates passing.
    - `14-VALIDATION.md` has no placeholder rows, has every Wave 0 and Sign-Off box ticked, carries `wave_0_complete: true`, `nyquist_compliant: true` and `status: validated`, and records the D-26 finding.
  </acceptance_criteria>
  <done>The approved copy is in the shipping table, the phase's three new tests run on every `npm test`, and the validation record is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Safari + Chrome playtest — the things a harness cannot see</name>
  <files>src/engine/index.js (temporary forced-storm hook, reverted before the task completes), src/ui/util.js (only if Wyatt names a pacing constant to change)</files>
  <precondition>`npm test` passes all twelve gates and a local static server can be started (`npm start` serves the repo over HTTP; `file://` is unsupported for ES modules).</precondition>
  <read_first>
    - `.planning/phases/14-.../14-VALIDATION.md` §"Manual-Only Verifications" (the seven rows this task discharges)
    - `docs/VERIFICATION-CHECKLIST.md` (the project's existing manual-verification shape)
    - `.planning/STATE.md` §Blockers/Concerns — the Safari storm precedent and the two-tab `pp_id` gotcha
  </read_first>
  <action>
Prepare the playtest, run it in both browsers, then remove the scaffolding.

**Set up.** Start the static server on a FRESH port — Safari caches ES modules aggressively and a
`?cb=` query string does not defeat it, so a new port is the reliable way to get new code. Force
storms by temporarily setting `cfg.storm=1` in `roundCfg` (`src/engine/index.js`); this is scaffolding
and MUST be reverted before the phase closes. Note the revert explicitly in the SUMMARY.

**What to watch for**, discharging the Manual-Only Verifications table in `14-VALIDATION.md`:

1. *Per-square stepping (STORM-01, D-09)* — during a bot's push the boat advances one square at a
   time across the full four-square dir1+dir2 sequence. It must not jump.
2. *Every outcome narrated (STORM-01, D-11)* — through a multi-leg bot push, each outcome that occurs
   produces its own line: dodge, anchor, aground, moored, anchor-hold, blocked. Not just the last one.
3. *No false dock message (STORM-01 criterion 2, D-22)* — when a moored line appears, the boat is
   already drawn where the message says it is. The board is never a square behind the words.
4. *Three moored lines (D-21)* — reaching a berth, being blown onto a dock, and having docked last
   turn each read differently and each read true.
5. *Pacing (D-10)* — a four-player storm round does not drag, and nothing scrolls past unread. If it
   is off, the fix is a constant: `BOT_MSG_HOLD_MULTIPLIER` or `BOT_STORM_STEP_MS` in `src/ui/util.js`.
6. *Hail costs the action (AI-01, D-24)* — in pass-and-play or a two-window game, decline a bot's
   hail and confirm its turn ENDS: the refused-hail line appears and no fish/dock/attack follows.
7. *Hail targeting and pricing (D-06, D-07)* — with two human seats, note which seat gets hailed and
   at what price, and confirm no bot ends the voyage broke.
8. *Tortuga's wind shadow (D-18)* — park downwind of Tortuga and confirm the sail budget drops the way
   it does downwind of any other island.
9. *No console errors* in either browser across a full voyage.

Run 1-5 and 8-9 in Safari (the project's storm-crash precedent browser) and repeat at least 1, 3, 6
and 9 in Chrome.

**Tear down.** Revert `cfg.storm=1`. Re-run `npm test` and confirm twelve green gates against the
unmodified source. Record every observation, and any pacing constant Wyatt asked to change, in the
SUMMARY. Anything that fails is a finding for `/gsd-verify-work`, not something to paper over here.
  </action>
  <verify>
    <automated>node -e 'const s=require("fs").readFileSync("src/engine/index.js","utf8");const m=s.match(/storm:\s*([0-9.]+)/);if(!m){console.error("could not read the storm probability from roundCfg");process.exit(1)}if(parseFloat(m[1])>=1){console.error("the forced-storm test hook is still in the source: storm="+m[1]);process.exit(1)}console.log("forced-storm hook reverted (storm="+m[1]+")")'</automated>
    <automated>npm test</automated>
    <human-check>
Serve the game on a fresh port and play a full voyage in Safari, then repeat the key checks in Chrome.
Confirm, in order:
1. A bot's storm push moves the boat ONE SQUARE AT A TIME across the whole four-square push — no jump.
2. Each outcome during that push gets its own narration line, not just the last one.
3. When a moored/"steadies" line appears, the boat is ALREADY drawn at the square the message
   describes — the board is never a square behind the words.
4. The three moored situations (docked last turn / blown onto a dock / at a Tortuga berth) read
   differently and each reads true.
5. A four-player storm round feels snappy but nothing scrolls past unread.
6. Decline a bot's hail: the refused-hail line appears and the bot's turn ENDS — no fishing, docking
   or attacking follows.
7. With two human seats, the bot hails a sensible target at a sensible price, and no bot ends the
   voyage broke.
8. A ship parked downwind of Tortuga has a reduced sail budget, as it would downwind of any island.
9. No console errors in either browser.
Reply "approved" if all nine hold, or describe exactly what you saw for any that did not.
    </human-check>
  </verify>
  <acceptance_criteria>
    - All nine observations above were checked in Safari (1-5, 8-9) and Chrome (1, 3, 6, 9), and the result of each is recorded in the SUMMARY as observed, not inferred.
    - The forced-storm hook is reverted — the storm probability in `roundCfg` is back below 1, asserted by the probe above.
    - `npm test` exits 0 with twelve gates against the unmodified source.
    - Any pacing change Wyatt asked for during the playtest is applied to the named constant and re-verified.
    - Any observation that failed is written up as a finding rather than adjusted away.
  </acceptance_criteria>
  <done>Both target browsers confirm the storm reads correctly and the hail costs the bot its action, the scaffolding is gone, and the full gate suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scaffolding → shipped source | A temporary forced-storm setting lives in the deterministic engine file; leaving it in would change every game |
| approved copy → shipped strings | Wyatt's wording is the deliverable; anything that rewrites it in passing defeats the approval gate |

No new attack surface: narration strings, a `package.json` script chain, and a manual playtest. No
auth, no session, no network calls added, no persisted user data, no new dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-18 | Tampering | forced-storm hook shipped by accident | medium | mitigate | Reverting it is an acceptance criterion with an automated probe that reads the storm probability back out of `roundCfg`. |
| T-14-19 | Tampering | approved copy silently reworded when applied | low | mitigate | The action forbids in-passing edits and the acceptance criterion requires verbatim application. |
| T-14-20 | Repudiation | manual observations recorded as inferred rather than seen | low | mitigate | Each of the nine checks is enumerated with its exact observable, and the SUMMARY must record what was actually seen per browser. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; RESEARCH.md `## Package Legitimacy Audit` records "N/A — this phase introduces no new external packages". Wiring existing local scripts into `npm test` installs nothing. |
</threat_model>

<verification>
- Wyatt's approved copy is applied verbatim.
- `npm test` runs twelve gates and passes.
- `14-VALIDATION.md` is signed off with no placeholder rows.
- The forced-storm hook is reverted, asserted automatically.
- The nine-point playtest is confirmed in Safari and Chrome.
</verification>

<success_criteria>
- STORM-01, AI-01 and VERIFY-02 are all satisfied and observed, not just asserted.
- The phase's three new tests are permanent gates.
- No scaffolding, no unapproved copy, no open validation rows.
</success_criteria>

<output>
Create `.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-06-SUMMARY.md` when done.
Record Wyatt's copy decisions, any pacing constants changed, the per-browser playtest observations,
and confirmation that the forced-storm hook was reverted.
</output>
