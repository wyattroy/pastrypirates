---
phase: 14-engine-adjacent-gameplay-fixes-determinism
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - scripts/fixtures/determinism/manifest.json
  - scripts/fixtures/determinism
  - docs/DETERMINISM-RERECORD.md
autonomous: false
requirements: [VERIFY-02]

must_haves:
  truths:
    - "Every divergent event across all 30 seeds is enumerated and attributed to one of D-15, D-18 or D-21 before any fixture byte is rewritten (D-26)."
    - "The re-record runs exactly once, after the divergence report and after a blocking human decision — never to clear a red gate (D-15, D-16)."
    - "`docs/DETERMINISM-RERECORD.md` records what changed, why, and the measured evidence, and is committed in the same commit as the new fixtures (D-16's surviving requirement)."
    - "After `--capture`, `npm test` passes all nine gates and `node scripts/determinism_baseline.js --verify` reports 30/30 against the NEW baseline (VERIFY-02)."
    - "`node scripts/determinism_diff.js --assert-clean` exits 0 against the newly captured corpus — the tool that gated the door also confirms the door closed cleanly."
    - "The re-recorded manifest covers every entry in `REQUIRED_EVENT_TYPES` — a corpus that silently under-covers a mechanic is caught by `capture()`'s own assertion at `scripts/determinism_baseline.js:127-131`."
    - statement: "No divergence in a battle, trade, dock or fish event, nor in a `__final__` state line, is left unattributed to D-15, D-18 or D-21 in the divergence report."
      verification: backstop
  artifacts:
    - path: "docs/DETERMINISM-RERECORD.md"
      provides: "The completed D-16 record: what changed, why, the full attributed divergence, and the human decision that authorised the capture"
      min_lines: 60
    - path: "scripts/fixtures/determinism/manifest.json"
      provides: "The new 30-seed behavioural baseline"
      contains: "engineSourceHash"
  key_links:
    - from: "docs/DETERMINISM-RERECORD.md"
      to: "scripts/fixtures/determinism/manifest.json"
      via: "the record names the new capturedAt/engineSourceHash it authorises"
      pattern: "engineSourceHash"
  prohibitions:
    - statement: "MUST NOT run `--capture` before the full divergence report exists and the blocking decision checkpoint has been answered — the enumeration is the authorisation, and a re-record without it silently buries any unrelated regression as the new truth."
      requirement_id: VERIFY-02
      category: transparency
    - statement: "MUST NOT use `scripts/rebase_source_hash.js` here — it exists only for a pure code relocation with zero behaviour change and refuses to touch a manifest whose behaviour moved. Reaching for it to avoid a real re-record would produce a false green."
      requirement_id: VERIFY-02
      category: transparency
---

<objective>
Walk the one-way door deliberately: enumerate every divergence the phase's three engine changes
produced, attribute each one, put the evidence in front of Wyatt, and only then re-record the 30
golden fixtures — once.

Purpose: `--capture` has no dry-run. It always writes, and it permanently destroys the previous
byte-for-byte oracle, so any unrelated regression riding along becomes the new definition of correct.
D-16's rule is "re-record deliberately, never reflexively"; D-26 replaces its now-unachievable
"storm-only differences" test with an explainability test. This plan is that test, executed.

Output: an attributed divergence report, a human decision on the record, a re-captured corpus, and
`npm test` green again on all nine gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-CONTEXT.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-RESEARCH.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-VALIDATION.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-01-SUMMARY.md
@.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-03-SUMMARY.md
@docs/DETERMINISM-RERECORD.md

<interfaces>
<!-- scripts/determinism_baseline.js — the only tool that may write fixtures -->
node scripts/determinism_baseline.js --capture   // rewrites manifest.json AND all 30 seed-*.jsonl
node scripts/determinism_baseline.js --verify    // default; the behaviour oracle
// capture() asserts the corpus covers REQUIRED_EVENT_TYPES before the write counts as a pass (:127-131)

<!-- scripts/determinism_diff.js — created in 14-01 -->
node scripts/determinism_diff.js                 // human-readable full report, exit 0
node scripts/determinism_diff.js --json          // { seeds:[...], summary:{ divergentSeeds, byEventType, byKey } }
node scripts/determinism_diff.js --ignore-keys=wind2,reason
node scripts/determinism_diff.js --assert-clean  // exit 1 if any seed diverges

<!-- The nine npm test gates, in order -->
determinism_baseline --verify, engine_contract_check, dlog_replay_test, net_registry_test,
net_contract_check, state_contract_check, module_graph_check, ui_contract_check, no_undef_check
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan produces no new symbols. It rewrites `scripts/fixtures/determinism/manifest.json` and all
30 `scripts/fixtures/determinism/seed-*.jsonl` files, and completes `docs/DETERMINISM-RERECORD.md`.
</artifacts_this_phase_produces>

<flagged_assumptions>
Surfaced by the spec-less probe fallback and carried here rather than resolved silently.

1. **VERIFY-02 / unclassified — UNRESOLVED, deliberately.** The edge probe could not classify
   VERIFY-02 and this plan does not auto-resolve it. The honest statement: the determinism criterion
   itself is being redefined by this phase (D-26 replaces D-16's narrow test with an explainability
   test), which is exactly the kind of change a human confirms rather than a probe. It is presented
   at Task 2's checkpoint and must not be closed by any automated step.

2. **D-26's literal pre-storm assertion is expected to FAIL, for a measured reason.** Every player
   spawns on a Tortuga berth (`src/engine/index.js:209`), and `leeward()` is a WIND effect that
   applies on every round, not a storm effect — so from round 1 at least one player sits downwind of
   home in all 30 seeds (measured during planning). D-15 compounds this: `ev()` writes `wind2` onto
   every event, so every seed's line 0 changes. The assertion is still implemented and still run;
   its failure is evidence to present, not a criterion to quietly soften. Task 1 must attribute the
   pre-storm divergences rather than dismiss them.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and attribute every divergence (D-26)</name>
  <files>docs/DETERMINISM-RERECORD.md</files>
  <precondition>All three fixture-perturbing changes are in the working tree: `leeward()` tests the upwind square against home (14-01), `play()` rolls `windNow2` and `takeTurn` applies two gusts (14-03 Task 1), and `windPush` emits `moored` with a `reason` (14-03 Task 2). `node scripts/determinism_baseline.js --verify` is expected to be RED at this point.</precondition>
  <read_first>
    - `docs/DETERMINISM-RERECORD.md` (seeded by 14-01 Task 2 — you are completing it)
    - `.planning/phases/14-.../14-01-SUMMARY.md` and `14-03-SUMMARY.md`
    - `scripts/determinism_diff.js` (the tool you are about to rely on)
    - `scripts/determinism_baseline.js` lines 103-148 (`capture()` and its coverage assertion)
    - `.planning/phases/14-.../14-CONTEXT.md` — D-15, D-16, D-18, D-21, D-26
  </read_first>
  <action>
Produce the complete divergence evidence. Do not write any fixture byte in this task.

Run four passes and keep every output:

1. `node scripts/determinism_diff.js` — the full human-readable report over all 30 seeds.
2. `node scripts/determinism_diff.js --json` — the machine report, kept for the numbers.
3. `node scripts/determinism_diff.js --ignore-keys=wind2` — isolates D-15's additive serialization
   delta so the remainder is behavioural.
4. `node scripts/determinism_diff.js --ignore-keys=wind2,reason` — isolates D-21's added field too,
   so what remains is the genuine routing/outcome change, which is D-18's blast radius plus D-15's
   second gust.

Now attribute. Every divergent line must map to one of exactly three causes, and the mapping must be
stated with the numbers behind it:

- **D-15 (both gusts + `windNow2`)** — the added `wind2` key on every event; storm rounds where a ship
  now travels up to 4 squares instead of 2; the extra `this.r()` call per round shifting the RNG
  stream from that round forward.
- **D-18 (Tortuga's wind shadow)** — `sailBudget` dropping 9 → 7 for ships downwind of home, changing
  `stepToward`'s Dijkstra result and therefore where bots end up, which cascades into `dock`, `trade`,
  `fish` and `battle` events. This cascade is legitimate and is precisely why D-26 replaced D-16's
  narrow "storm-only" test.
- **D-21 (`moored` reason)** — the added `reason` key on `moored` events, plus any `moored` event now
  fired where the old separate home early-return used to fire one (the fold is behaviour-neutral, so
  the count should match).

Then look for what does NOT map. Walk the `summary.byEventType` histogram and ask of each entry
whether the three causes above explain it. The warning signs RESEARCH.md named: a divergence in
`battle`, `trade`, `dock` or `fish` that cannot be traced back to a changed position, or a `__final__`
line whose `pos`/`coins`/`ing` delta has no routing explanation. **If anything is unattributable,
STOP and report it as a finding — an unexplained fingerprint change is a real bug, not paperwork.**

Complete `docs/DETERMINISM-RERECORD.md`: fill in the full attributed report (real numbers from the
four passes, not estimates), the per-seed `preStormStructuralDivergence` verdict with its measured
explanation, an explicit "unattributed divergences: none" (or the list), and leave the verdict line
reading `AWAITING DECISION — see 14-04 Task 2`.

Do NOT run `node scripts/determinism_baseline.js --capture` in this task. Do NOT run
`scripts/rebase_source_hash.js` — it is for a pure code relocation with zero behaviour change and
would refuse this manifest anyway; reaching for it here would produce a false green.
  </action>
  <verify>
    <automated>node scripts/determinism_diff.js --json > /tmp/pp14-final-diff.json &amp;&amp; node -e 'const r=require("/tmp/pp14-final-diff.json");if(r.seeds.length!==30){console.error("report does not cover 30 seeds");process.exit(1)}if(!r.summary.divergentSeeds){console.error("no divergence found — the engine changes are not in the tree");process.exit(1)}console.log("divergentSeeds="+r.summary.divergentSeeds+" byEventType="+JSON.stringify(r.summary.byEventType))'</automated>
    <automated>node -e 'const s=require("fs").readFileSync("docs/DETERMINISM-RERECORD.md","utf8");const need=["D-15","D-18","D-21","D-26","preStormStructuralDivergence","unattributed","AWAITING DECISION"];const miss=need.filter(k=>!s.includes(k));if(miss.length){console.error("record is missing: "+miss.join(", "));process.exit(1)}if(s.split("\n").length<60){console.error("record is too thin for a one-way door");process.exit(1)}console.log("re-record document complete and pending decision")'</automated>
    <automated>git diff --name-only | grep -q 'scripts/fixtures/determinism' &amp;&amp; { echo "FAIL: fixtures were modified before the decision checkpoint"; exit 1; } || echo "fixtures untouched"</automated>
  </verify>
  <acceptance_criteria>
    - All four diff passes were run and their real outputs are recorded in `docs/DETERMINISM-RERECORD.md`.
    - Every divergent event type in `summary.byEventType` is attributed in the record to D-15, D-18 or D-21, with the reasoning stated.
    - The record contains an explicit `unattributed divergences:` line — either `none`, or a list, in which case the plan halts and the finding is reported rather than proceeding to Task 2.
    - The per-seed `preStormStructuralDivergence` verdict is recorded with its measured explanation (berth spawn at `src/engine/index.js:209`; `leeward()` is wind-driven; `wind2` lands on every event).
    - `docs/DETERMINISM-RERECORD.md` is at least 60 lines and its verdict line reads `AWAITING DECISION — see 14-04 Task 2`.
    - No file under `scripts/fixtures/determinism/` has been modified.
  </acceptance_criteria>
  <done>Every divergence across all 30 seeds is enumerated and traced to a named decision, with any unexplained change surfaced rather than absorbed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: One-way door — authorise the single fixture re-record</name>
  <decision>Re-record the 30 golden determinism fixtures now, consuming the previous byte-for-byte oracle.</decision>
  <context>
`node scripts/determinism_baseline.js --capture` rewrites `manifest.json` and all 30
`seed-*.jsonl` files. After it runs, the previous baseline no longer exists, so "nothing else
changed across this boundary" can never again be proven. Undoing it means reverting D-15, D-18 and
D-21 in `src/engine/index.js` AND restoring the fixtures from git history — a coordinated revert, not
a refactor. CONTEXT.md rates this one-way (D-15, widened by D-18); this is the single gate for all
three changes, which is why the individual engine tasks did not each stop to re-ask.

Present to Wyatt, from `docs/DETERMINISM-RERECORD.md` — real numbers, no summarising away the
awkward parts:

1. The three named causes and one line each on what they change.
2. The divergent-seed count, the divergent-line histogram by event type, and the histogram by
   differing key.
3. The attribution: which event types trace to which decision, and the explicit
   `unattributed divergences:` line.
4. **The honest D-26 finding.** D-26 asked for an assertion that no seed diverges before its first
   storm round. That assertion is implemented and was run, and it is expected to FAIL — because
   `leeward()` is a WIND effect that applies on every round rather than a storm effect, every player
   spawns on a Tortuga berth (`src/engine/index.js:209`), and D-15's `wind2` field lands on every
   event from line 0. The criterion was written before those two mechanisms were understood. The
   evidence actually relied on is the per-key attribution that separates an additive serialization
   delta from a real behavioural change. Ask Wyatt to confirm that substitution explicitly — this is
   the phase redefining its own determinism criterion, which is his call, not the tooling's.
5. The unresolved VERIFY-02 probe row, stated as unresolved.
  </context>
  <options>
    <option id="capture-now">
      <name>Re-record now</name>
      <pros>All three fixture-perturbing changes have landed and every divergence is attributed; `npm test` goes green again on the new baseline and the rest of the phase runs against a working gate.</pros>
      <cons>The previous oracle is gone permanently; any regression hidden inside the attributed set becomes the new definition of correct.</cons>
    </option>
    <option id="investigate-first">
      <name>Investigate a specific divergence before capturing</name>
      <pros>Any line Wyatt cannot place gets chased down while the old oracle still exists to compare against — the only moment that comparison is ever possible again.</pros>
      <cons>The determinism gate stays red and the rest of the phase runs without it.</cons>
    </option>
    <option id="revert-a-change">
      <name>Revert one of the three engine changes and re-measure</name>
      <pros>Shrinks the blast radius if one decision turns out to cost more than it is worth; D-18 alone accounts for the routing cascade.</pros>
      <cons>Contradicts a confirmed decision (D-15, D-18 and D-21 are all locked in CONTEXT.md) and re-opens the phase's scope.</cons>
    </option>
  </options>
  <resume-signal>Select: capture-now, investigate-first, or revert-a-change. If capture-now, also confirm explicitly that the D-26 pre-storm assertion is superseded by the per-key attribution evidence.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Capture once, verify green, commit the record with the fixtures (D-15, D-16)</name>
  <reversibility rating="one-way">This task consumes the previous byte-for-byte oracle. Undo requires
  reverting D-15, D-18 and D-21 in `src/engine/index.js` and restoring all 31 fixture files from git
  history. Authorised by Task 2's checkpoint.</reversibility>
  <files>scripts/fixtures/determinism/manifest.json, scripts/fixtures/determinism/seed-*.jsonl, docs/DETERMINISM-RERECORD.md</files>
  <precondition>Task 2's checkpoint was answered `capture-now`, and `docs/DETERMINISM-RERECORD.md` records `unattributed divergences: none`.</precondition>
  <read_first>
    - `docs/DETERMINISM-RERECORD.md` (the authorised record, including Wyatt's answer)
    - `scripts/determinism_baseline.js` lines 103-148 (`capture()` — what it writes and what it asserts)
    - `package.json` (the nine-gate `test` script string)
  </read_first>
  <action>
Run `node scripts/determinism_baseline.js --capture` exactly once. It rewrites
`scripts/fixtures/determinism/manifest.json` and all 30 `seed-*.jsonl` files, and its own coverage
assertion (`:127-131`) fails the run if the new corpus stops covering any of `REQUIRED_EVENT_TYPES` —
do not weaken that list to make a capture pass; a genuinely absent mechanic is a finding.

Then confirm the door closed cleanly:
- `node scripts/determinism_baseline.js --verify` reports 30/30 against the new manifest.
- `node scripts/determinism_diff.js --assert-clean` exits 0 — the tool that gated the door now
  reports nothing left to explain.
- `npm test` passes all nine gates, including the determinism gate that has been red since 14-01.

Finish `docs/DETERMINISM-RERECORD.md`: replace the `AWAITING DECISION` verdict with the decision
actually given, the date, the new manifest's `capturedAt` and `engineSourceHash`, and a short recovery
note naming the revert path (revert the three engine changes AND restore the fixtures from git
history). Record Wyatt's answer on the D-26 criterion substitution verbatim.

Commit the new fixtures and the completed record TOGETHER in one commit — D-16's surviving
requirement is that the explanation travels with the fixtures, not in a separate commit someone has to
go looking for.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify</automated>
    <automated>node scripts/determinism_diff.js --assert-clean</automated>
    <automated>npm test</automated>
    <automated>node -e 'const m=require("./scripts/fixtures/determinism/manifest.json");if(m.perSeed.length!==30){console.error("manifest does not carry 30 seeds");process.exit(1)}const miss=m.requiredEventTypes.filter(t=>!(m.coverage[t]>0));if(miss.length){console.error("new corpus under-covers: "+miss.join(", "));process.exit(1)}const d=require("fs").readFileSync("docs/DETERMINISM-RERECORD.md","utf8");if(d.includes("AWAITING DECISION")||d.includes("PENDING")){console.error("the record still reads pending after the capture");process.exit(1)}if(!d.includes(m.engineSourceHash)){console.error("the record does not name the new engineSourceHash it authorised");process.exit(1)}console.log("re-record complete, coverage intact, record closed")'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/determinism_baseline.js --capture` was run exactly once, after the checkpoint, and its coverage assertion passed without `REQUIRED_EVENT_TYPES` being edited.
    - `node scripts/determinism_baseline.js --verify` reports all 30 seeds passing against the new manifest.
    - `node scripts/determinism_diff.js --assert-clean` exits 0.
    - `npm test` passes all nine gates.
    - `docs/DETERMINISM-RERECORD.md` contains no `PENDING` or `AWAITING DECISION` marker, names the new `engineSourceHash` verbatim, records the decision and date, records Wyatt's answer on the D-26 substitution, and states the revert path.
    - The new fixtures and the completed record are in a single commit.
    - `scripts/rebase_source_hash.js` was not run.
  </acceptance_criteria>
  <done>The corpus is re-recorded once, all nine gates are green against the new baseline, and the reason it changed is committed alongside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → the oracle | The corpus defines "correct" for every later phase; rewriting it transfers authority from the old baseline to the new one |
| human decision → irreversible write | The blocking checkpoint is the only thing standing between a red gate and a permanent redefinition of correct behaviour |

No new attack surface: this plan runs offline Node scripts over local files and writes a doc. No auth,
no session, no network, no persisted user data, no new dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Tampering | `--capture` burying an unrelated regression as the new baseline | high | mitigate | Full per-seed enumeration + per-key attribution + an explicit `unattributed divergences:` line, all produced BEFORE the blocking `checkpoint:decision`. The plan halts on any unattributed divergence. |
| T-14-12 | Repudiation | no record of why the oracle changed | medium | mitigate | `docs/DETERMINISM-RERECORD.md` names the causes, the numbers, the decision, the date, the new `engineSourceHash`, and the revert path — committed in the same commit as the fixtures. |
| T-14-13 | Tampering | weakening `REQUIRED_EVENT_TYPES` to force a capture to pass | medium | mitigate | The verify probe re-reads `manifest.requiredEventTypes` against `manifest.coverage`, and the action forbids editing that list. |
| T-14-14 | Spoofing | `rebase_source_hash.js` used to fake a green without a real re-record | medium | mitigate | Restated as a prohibition and an acceptance criterion; the tool gates on every seed's fresh hash still matching the frozen one and would refuse this manifest anyway. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; RESEARCH.md `## Package Legitimacy Audit` records "N/A". |

`security_block_on` is `high`. T-14-11 is the one high-severity item in this phase and it is
`mitigate`, gated by a blocking human checkpoint — nothing here blocks.
</threat_model>

<verification>
- The divergence report covers 30 seeds and every event type in it is attributed.
- The blocking decision was answered before any fixture byte changed.
- `npm test` passes all nine gates and `--assert-clean` exits 0 against the new corpus.
- `docs/DETERMINISM-RERECORD.md` is closed out and committed with the fixtures.
</verification>

<success_criteria>
- The corpus was re-recorded exactly once, deliberately, with the evidence on the record.
- VERIFY-02 is green (30/30) against the new baseline.
- The determinism gate is usable again for the rest of the phase and the rest of the milestone.
</success_criteria>

<output>
Create `.planning/phases/14-engine-adjacent-gameplay-fixes-determinism/14-04-SUMMARY.md` when done.
Record the decision given, the new `engineSourceHash`, and the answer on the D-26 criterion
substitution.
</output>
