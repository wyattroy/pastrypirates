---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/ui/util.js
  - src/ui/index.js
  - src/main.js
  - index.html
autonomous: true
requirements: [SPLIT-03]
user_setup: []

must_haves:
  truths:
    - "The net-free, DOM-free pure helper cluster (formatting/name/geometry/session/awards/badges/shot-clock helpers/narration strings) lives verbatim in src/ui/util.js, re-exported through src/ui/index.js"
    - "src/ui/util.js contains no `from \"../net` import (ui↛net) and `node scripts/module_graph_check.js` exits 0"
    - "byte-diff of the moved helper bodies (index.html pre-move slices vs src/ui/util.js) shows no reformatting"
    - "`node scripts/determinism_baseline.js --verify` reports 30/30"
    - "`git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1 (never --capture, D-10)"
    - "The still-classic UI/orchestration functions keep resolving the moved helpers as bare identifiers because src/main.js republishes them via `...ui`"
  artifacts:
    - src/ui/util.js
  key_links:
    - "src/ui/util.js re-exported by src/ui/index.js so `...ui` in the PP bridge keeps classic callers of these helpers resolving"
---

<objective>
Extract the pure leaf helpers — the largest, lowest-risk slice of the 95 helper/logic-tier functions that touch neither the DOM nor `src/net/` — into `src/ui/util.js`, moving bottom-up so every callee is either already-moved (importable) or a shared/engine/state symbol.

Purpose: clear the leaf layer first so the board/panel/flow waves that depend on these helpers can `import` them instead of reading bridge globals.
Output: `src/ui/util.js`, an expanded barrel, a shrunk classic region, game still runnable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the pure formatting / name / geometry / session helpers into src/ui/util.js</name>
  <files>src/ui/util.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (tier `helper/logic`, dom:false, net:[] — the authoritative move list with line ranges and calls/calledBy; move bottom-up)
    - index.html lines for each target function (from analysis.json): escHtml already moved in 11-01; this task takes seatDisplayOrder (366-371), dockOrient (415-419), islandArtPlacement (423-442), tracePolygonLoops (444-467), roundedPathFromLoop (469-482), shipXY (637-643), pname (648-651), rawName (655-658), pn (659), poss (662), fl (662), fmtItem (663), windHoldPhrase (673-676), describe (777-786), syncLogLines (794-796), pulseEl (801-805), islandXY (971-974), boatXY (1698-1702), captions (1000-1004), computeAwards (1012-1047), assignBadges (1073-1090), pastelize (1776-1780), apBtnStyle (1781), resolveOpt (1786-1790), ask (1791-1811), armClock (1815-1818), msgHoldMs (1645-1653)
    - src/engine/index.js (the verbatim-motion + import-rewiring precedent + header comment shape)
    - src/ui/recipe.js (the 11-01 sibling — the src/ui header/import convention already established this phase)
    - src/ui/index.js + src/main.js (the barrel to extend and the `...ui` bridge that must keep republishing)
  </read_first>
  <action>
    Move the pure formatting/name/geometry/awards/narration-string helper functions listed above (all `helper/logic` tier with dom:false and net:[]) verbatim into a new `src/ui/util.js`. Use analysis.json's `calls` data to order the move bottom-up: a helper whose only callees are other util helpers, shared symbols, engine symbols, or `appState` moves cleanly. Replace each function's former bare reads of shared/engine/state symbols with explicit `import { ... } from "../shared/index.js"` / `"../engine/index.js"` / `"../state/index.js"`; util-to-util calls stay direct same-file calls. If a helper is genuinely pure (no DOM, no appState, no engine) it MAY instead go to `src/shared/` at your discretion (D-06) — keep that a separate, clearly noted subset. Export every moved function; add `export * from "./util.js"` to src/ui/index.js. Delete the moved bodies from the classic region. The `...ui` spread in src/main.js already republishes them — do not touch the bridge deletion.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/util.js` contains `export function computeAwards(` and `export function describe(` and at least one `import ... from "../shared/index.js"`
    - `src/ui/util.js` contains NO `from "../net` / `from "./net` import (ui↛net)
    - byte-diff of the moved bodies (index.html pre-move slices vs util.js) shows no reformatting
    - `grep -c 'function computeAwards\|function describe\|function assignBadges' index.html` returns 0 (moved out)
    - `node scripts/module_graph_check.js` exits 0
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>The pure leaf helpers live in src/ui/util.js with explicit imports, the classic region no longer defines them, graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the session / solo-state / shot-clock helper functions into src/ui/util.js</name>
  <files>src/ui/util.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (remaining `helper/logic` net-free/DOM-free functions and their calledBy — confirm each callee is already moved or a shared/engine/state symbol)
    - index.html lines: getMyId (3086-3090), genCode (3091), saveSession (3092), clearSession (3093), encodeDec (3098), decodeDec (3099), saveSoloState (3118-3121), clearSoloState (3122), resumeSoloGame (3123-3133), replayShortfall (3144-3150), fixEv (3276-3280), startShotClock (1261-1271), stopShotClock (1272-1281), rearmShotClock (1288-1300), toggleShotClockPause (1306-1322), shotClockTick (1361-1367), applyShotClockPenalty (1368-1376), currentTurnSeat (1424-1432), soloBotGame (1302), waitWhilePaused (1197-1201), stepDelay (1205), botBeat (1211), narrateCurrent (1214-1221), setActor (1222), seatLocal (1223), decisionIsLocal (1227), seatStrat (3054), spawnPops (991-998), updateRecipeBanner (3606-3610), preloadAssets (3737-3745), withShotClock (1826-1842)
    - src/ui/util.js (the file being extended — task 1 established its imports/header)
    - src/state/index.js (session/timer/shot-clock names now on appState — resumeSoloGame/shot-clock helpers read appState.game, appState.timer etc.)
  </read_first>
  <action>
    Move the session-persistence, solo-state, shot-clock-control, and small game-flow helper functions listed above (all net-free, DOM-free) verbatim into `src/ui/util.js` (extending task 1's file), again bottom-up per analysis.json `calls`. Rewire their bare reads of appState / shared / engine into imports. Note `withShotClock`, `startShotClock`, `rearmShotClock`, `toggleShotClockPause` read the `appState.shotClockTimer`/`appState.timer` handles — import `appState` and preserve the exact property access (Phase 10 already migrated these to `appState.NAME`). Export everything; barrel already re-exports util.js. Delete moved bodies from the classic region. Any helper that turns out to call a still-classic DOM function (e.g. a render helper) is NOT in this net-free/DOM-free set — defer it to the board/panel wave rather than forcing a `globalThis` reference.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/util.js` contains `export function withShotClock(` and `export function resumeSoloGame(`
    - `grep -c 'function withShotClock\|function resumeSoloGame\|function startShotClock' index.html` returns 0
    - `src/ui/util.js` still contains NO `from "../net` import
    - byte-diff of the moved bodies shows no reformatting
    - `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30
  </acceptance_criteria>
  <done>Session/solo/shot-clock helpers moved into src/ui/util.js with import-based state access; classic region shrunk; graph + determinism green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console/global scope → app internals | The `...ui` bridge still republishes moved helpers; surface unchanged this wave (shrinks in 11-07) |
| localStorage → session helpers | saveSession/saveSoloState read/write pp_sess/pp_solo — existing behavior, pure code motion, no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Tampering | moved shot-clock / session helpers | medium | mitigate | verbatim byte-diff + `--verify` 30/30 + module_graph_check prove no behavior/order change |
| T-11-04 | Information disclosure | localStorage session helpers | low | accept | no secret material — session token + solo game state only, unchanged from pre-move |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained |
</threat_model>

<verification>
- `node scripts/module_graph_check.js` exits 0 (no cycle, ui↛net holds)
- `node scripts/determinism_baseline.js --verify` 30/30 after each task
- byte-diff pure-motion audit on every moved function
- classic region function count dropped by the moved set
</verification>

<success_criteria>
The ~55 net-free/DOM-free helper functions are in src/ui/util.js with explicit imports; the classic region is materially smaller; graph acyclic; determinism 30/30; bridge still keeps the remaining classic functions runnable.
</success_criteria>

## Artifacts this phase produces

- `src/ui/util.js` (this plan). Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/main.js` (expanded orchestrator), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, `window.revealMyRecipe` (retained global, 11-07), surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug` (NOT part of the deleted bridge).

## Flagged Assumptions

- **95-helper split (RESEARCH Q1a #2):** helpers land in `src/ui/util.js`; a truly-pure subset MAY go to `src/shared/` at executor discretion (D-06 / Claude's Discretion). analysis.json `calledBy` supports either grouping.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-02-SUMMARY.md` when done
</output>
