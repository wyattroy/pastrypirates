---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/ui/board.js
  - src/ui/index.js
  - src/main.js
  - index.html
autonomous: true
requirements: [SPLIT-03]
user_setup: []

must_haves:
  truths:
    - "The board + storm render cluster (drawBoard, render, buildStormLayers, renderLog, renderDecorativeBoard, popEmoji, spawnPops, syncBoardSizing, el, iconAt) lives verbatim in src/ui/board.js, re-exported through src/ui/index.js"
    - "src/ui/board.js imports its helpers from ../shared, ../engine, ../state, and ./util.js — never from ../net (`node scripts/module_graph_check.js` exits 0)"
    - "buildStormLayers still builds the storm overlay lazily once and render() is invoked the same number of times (storm surface preserved for the Safari re-verification in 11-08)"
    - "byte-diff of the moved render bodies (index.html pre-move slices vs src/ui/board.js) shows no reformatting"
    - "`node scripts/determinism_baseline.js --verify` reports 30/30"
    - "`git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1 (never --capture, D-10)"
  artifacts:
    - src/ui/board.js
  key_links:
    - "src/ui/board.js → ./util.js (geometry/format helpers moved in 11-02) — the acyclic ui-internal edge module_graph_check.js verifies"
    - "buildStormLayers → drawBoard/render storm overlay path — the v1.0 BUG-01 Safari perf-risk surface that 11-08 re-verifies"
---

<objective>
Extract the board + storm rendering cluster — the DOM-heaviest, Safari-sensitive slice — into `src/ui/board.js`, moving verbatim so the v1.0 BUG-01 storm-crash fix (pre-baked PNG tile, snap-not-animate) is carried byte-identical.

Purpose: relocate the storm render surface into its module ahead of the panel/flow waves, keeping the render cadence and lazy `buildStormLayers` construction structurally identical so Safari does not regress.
Output: `src/ui/board.js`, expanded barrel, shrunk classic region, storm surface intact for 11-08's Safari pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-PATTERNS.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract drawBoard / render / storm layers into src/ui/board.js</name>
  <files>src/ui/board.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (tier `ui (DOM)`, the calls/calledBy for drawBoard/render — confirms their callees are now in src/ui/util.js, src/ui/recipe.js, shared, engine, or state)
    - index.html lines: el (402-403), iconAt (406-410), drawBoard (484-636, 153L), buildStormLayers (811-832), render (833-945, 113L), renderLog (947-962), popEmoji (975-990), spawnPops (991-998 — already candidate in util; keep wherever its callers are), renderDecorativeBoard (3415-3429), syncBoardSizing (3701-3730)
    - src/ui/util.js (the 11-02 sibling providing shipXY/boatXY/islandXY/islandArtPlacement/tracePolygonLoops/roundedPathFromLoop/dockOrient — import these)
    - src/engine/index.js (Game types drawBoard/render read: appState.game state shape) and src/shared/index.js (image maps, DIRS, man)
    - .planning/phases/07-foundation-determinism-baseline/07-03-SUMMARY.md (the forced-storm procedure and the render-only-guest caveat — the storm surface being preserved here is what 11-08 re-verifies)
  </read_first>
  <action>
    Create `src/ui/board.js` with the src/ui header convention (phase, ui↛net constraint, DOM+state-read purity). Move el, iconAt, drawBoard, buildStormLayers, render, renderLog, popEmoji, renderDecorativeBoard, syncBoardSizing verbatim from the classic region. Rewire their bare reads: import geometry/format helpers from `./util.js` and `./recipe.js`, game/image/direction symbols from `../engine/index.js` and `../shared/index.js`, and `appState` from `../state/index.js`. Preserve the storm path exactly — `buildStormLayers`'s lazy one-time DOM construction and the number/order of `render()` calls must not change (this is the Safari BUG-01 surface; algorithmic identity is the whole point). Export all; add `export * from "./board.js"` to src/ui/index.js. Delete moved bodies from the classic region. The `...ui` bridge continues to republish these so any still-classic caller (panel/flow, not yet moved) resolves render/drawBoard as bare identifiers.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/board.js` contains `export function drawBoard(` and `export function render(` and `import ... from "./util.js"`
    - `src/ui/board.js` contains NO `from "../net` import (ui↛net)
    - byte-diff of drawBoard/render/buildStormLayers bodies (index.html pre-move vs board.js) shows no reformatting or logic change
    - `grep -c 'function drawBoard\|function buildStormLayers\|function render' index.html` returns 0 for these definitions (moved out)
    - `node scripts/module_graph_check.js` exits 0 (ui→util/recipe/engine/shared edges valid, no cycle)
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
  </acceptance_criteria>
  <done>Board + storm rendering is in src/ui/board.js verbatim with import-based deps; storm construction/render cadence unchanged; graph + determinism green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the remaining board-adjacent DOM helpers (log/emoji/decorative/sizing/flip-coin)</name>
  <files>src/ui/board.js, src/ui/index.js, index.html</files>
  <read_first>
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-analysis.json (`ui (DOM)` tier remaining board-adjacent: renderLog, popEmoji, setFlipCoin (1233-1240), setFlipActive (1241-1249), celebrateHomeDocks (1093-1109), victoryConfetti (1113-1125 — helper/logic but DOM-adjacent), showStats (1126-1154))
    - index.html lines listed above
    - src/ui/board.js (task 1's file — these helpers are called by render/drawBoard; co-locate or import)
    - src/ui/util.js (captions/computeAwards/assignBadges moved in 11-02 — showStats/celebrate import from here)
  </read_first>
  <action>
    Move the remaining board/celebration DOM helpers (renderLog if not already in task 1, popEmoji, setFlipCoin, setFlipActive, celebrateHomeDocks, victoryConfetti, showStats) verbatim into `src/ui/board.js` (or a sibling `src/ui/celebrate.js` at your discretion — keep the barrel re-exporting either way). Rewire bare reads into imports from ./util.js, ../engine, ../shared, ../state. These read game/award state and build DOM only — confirm none call `src/net/`. Delete moved bodies from the classic region. Keep the barrel export current.
  </action>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify && node scripts/module_graph_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/board.js` (or celebrate.js) contains `export function showStats(` and `export function celebrateHomeDocks(`
    - `grep -c 'function showStats\|function celebrateHomeDocks\|function victoryConfetti' index.html` returns 0
    - the moved file contains NO `from "../net` import
    - byte-diff shows no reformatting
    - `node scripts/module_graph_check.js` exits 0; `node scripts/determinism_baseline.js --verify` reports 30/30
  </acceptance_criteria>
  <done>Board-adjacent DOM/celebration helpers moved with import-based deps; classic region shrunk; graph + determinism green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser compositor → storm overlay DOM | The v1.0 BUG-01 Safari CPU-compositing surface; preserved verbatim, re-verified by a human in 11-08 |
| console/global scope → app internals | `...ui` bridge still republishes render/drawBoard; surface unchanged this wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering | storm render path (drawBoard/buildStormLayers/render) | high | mitigate | verbatim byte-diff (no algorithmic change) + preserved lazy-once storm construction + render cadence + the D-12 human Safari forced-storm pass in 11-08 — a structural render-timing regression here is the milestone's known Safari risk |
| T-11-02 | Information disclosure | bridge-published render globals | low | accept | game state only, no secrets |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained |
</threat_model>

<verification>
- `node scripts/module_graph_check.js` exits 0
- `node scripts/determinism_baseline.js --verify` 30/30 after each task
- byte-diff pure-motion audit on drawBoard/render/buildStormLayers especially
- storm construction lazy-once + render cadence structurally unchanged (visual proof deferred to 11-08 Safari gate)
</verification>

<success_criteria>
Board + storm + celebration rendering lives in src/ui/board.js verbatim with explicit imports; the storm surface is structurally identical for the Safari re-verification; graph acyclic; determinism 30/30.
</success_criteria>

## Artifacts this phase produces

- `src/ui/board.js` (this plan). Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/main.js` (expanded orchestrator), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, `window.revealMyRecipe` (retained global, 11-07), surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug`.

## Flagged Assumptions

- **Storm re-verification is deferred, not skipped:** algorithmic identity is asserted here by byte-diff, but the visual/perf proof on Safari is a `backstop` human step in 11-08 (D-12) — the corpus is UI-blind and cannot see a render regression.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-03-SUMMARY.md` when done
</output>
