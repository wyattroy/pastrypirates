---
phase: 11-ui-extraction-orchestration-bridge-removal
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-08-SUMMARY.md
autonomous: false
requirements: [SPLIT-03, SPLIT-05, SPLIT-06]
user_setup: []

must_haves:
  truths:
    - "The full phase definition-of-done (D-11) is met in one run: `npm test` green (determinism + engine/net/state/ui contract checks + module-graph check) exits 0, plus the 11-07 Chrome solo + two-tab clean-console proof and the `window.PP` bridge provably gone"
    - "`node scripts/ui_contract_check.js` 4/4 PASS, `node scripts/module_graph_check.js` acyclic (ui↛net), `grep -rc 'PP-BRIDGE' src/` == 0, `grep -c '^<script>$' index.html` == 0 — all phase criteria mechanically confirmed together"
    - "`node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` stays 1 (corpus never captured, D-10)"
    - statement: "Storm rendering re-verifies cleanly on Safari after UI extraction — the storm overlay renders at an acceptable frame rate with no perf/compat regression (the v1.0 BUG-01 surface)"
      verification: backstop
  artifacts:
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-08-SUMMARY.md
  key_links:
    - "D-12 Safari storm pass — the one genuine human step of the milestone; no automation can drive Safari (criterion 5)"
---

<objective>
Close Phase 11 with the two verifications automation cannot fully self-confirm: a single consolidated run of the full automated phase gate, and the human Safari storm re-verification (D-12 / criterion 5) — the one genuine human step, since no automation can drive Safari and the storm render surface is the v1.0 BUG-01 near-crash risk.

Purpose: confirm all three requirements (SPLIT-03/05/06) hold together mechanically, and that the storm still renders cleanly on Safari at the UI-extraction boundary.
Output: a phase-close verification record; Wyatt's Safari storm sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-RESEARCH.md
@.planning/phases/07-foundation-determinism-baseline/07-03-SUMMARY.md
</context>

<!-- planner-discipline-allow: PP-BRIDGE -->
<!-- planner-discipline-allow: Object.assign(globalThis -->
<!-- planner-discipline-allow: ^<script>$ -->

<tasks>

<task type="auto">
  <name>Task 1: Consolidated automated phase gate — all SPLIT-03/05/06 checks in one run</name>
  <files>.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-08-SUMMARY.md</files>
  <read_first>
    - package.json (the `test` chain now including module_graph_check.js + ui_contract_check.js)
    - scripts/ui_contract_check.js + scripts/module_graph_check.js (the two new standing gates)
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VALIDATION.md (the Requirements → Test Map to confirm every row is now green)
  </read_first>
  <action>
    Run the full phase acceptance gate in one pass and record each result in the SUMMARY: `npm test`; `node scripts/module_graph_check.js`; `node scripts/ui_contract_check.js`; `grep -rc 'PP-BRIDGE' src/`; `grep -rc 'Object.assign(globalThis' src/`; `grep -c '^<script>$' index.html`; `node scripts/determinism_baseline.js --verify`; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l`. Map each result to the SPLIT-03 (ui↛net), SPLIT-05 (main orchestrates + markup-only index.html), SPLIT-06 (acyclic) rows of the VALIDATION Requirements → Test Map. No code changes expected — this is the consolidated confirmation that 11-01…11-07 hold together. If any check is red, STOP and file it as a gap rather than proceeding to the Safari step.
  </action>
  <verify>
    <automated>npm test && node scripts/module_graph_check.js && node scripts/ui_contract_check.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (determinism + engine/net/state/ui contract checks + module-graph check)
    - `node scripts/ui_contract_check.js` 4/4 PASS; `node scripts/module_graph_check.js` exits 0
    - `grep -rc 'PP-BRIDGE' src/` == 0; `grep -rc 'Object.assign(globalThis' src/` == 0; `grep -c '^<script>$' index.html` == 0
    - `node scripts/determinism_baseline.js --verify` reports 30/30; `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1
    - every SPLIT-03/05/06 row of the VALIDATION Requirements → Test Map is confirmed green in the SUMMARY
  </acceptance_criteria>
  <done>All three requirements' automated checks pass together in one recorded run; the phase is mechanically complete pending the human Safari pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Safari storm re-verification (D-12) — Wyatt's manual pass</name>
  <what-built>The fully-extracted build (bridge gone, UI in modules, storm render surface relocated verbatim into src/ui/board.js): drawBoard / render / buildStormLayers now module-scoped but algorithmically identical to the v1.0 storm-crash fix.</what-built>
  <how-to-verify>
    This is Wyatt's step — no automation can drive Safari (D-12 / criterion 5). Serve over the local HTTP test server (never file://). Open the game in SAFARI. Play a solo game and reach a STORM (storms fire during normal play; play a few rounds until the storm overlay appears — the pre-de-globalization console forced-storm hack from 07-03-SUMMARY.md is no longer reachable because `appState`/`rollStorm` are no longer console globals after the bridge deletion; see the flagged note below). When the storm overlay renders, watch the frame rate: it must stay smooth with no jank, no runaway CPU, no near-crash — the v1.0 BUG-01 symptom must NOT reappear. Confirm the pre-baked storm PNG tile resolves (no missing-asset overlay) and narration height snaps rather than animates. Also confirm the board and panels render normally around the storm.
  </how-to-verify>
  <resume-signal>Type "approved" (storm renders cleanly on Safari, no perf regression) or describe the jank / crash / missing-asset behavior observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safari compositor → storm overlay DOM | The v1.0 BUG-01 CPU-compositing surface; the only boundary automation cannot self-verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering | storm render path on Safari | high | mitigate | verbatim motion (11-03) preserved algorithm; the human Safari forced/natural storm pass is the final confirmation that render timing did not regress — the milestone's known Safari risk |
| T-11-SC | Tampering | package installs | low | accept | no installs — zero-dep maintained |
</threat_model>

<verification>
- Consolidated automated gate: `npm test` + module_graph_check + ui_contract_check + the three greps + `--verify` 30/30, all recorded together
- Blocking human Safari checkpoint: storm renders cleanly, no perf/compat regression (D-12)
</verification>

<success_criteria>
All SPLIT-03/05/06 automated checks green in one consolidated run; Wyatt confirms the storm re-verifies cleanly on Safari with no perf/compat regression. Phase 11 complete.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/11-.../11-08-SUMMARY.md` (this plan, the phase-close record). Full phase artifact list: `src/ui/{index,recipe,util,board,panel,lobby,flow,handlers}.js`, `src/orchestrator.js`, `src/main.js` (expanded orchestrator, bridge deleted), `scripts/{module_graph_check,ui_contract_check,analyze_classic}.js|mjs`, `window.revealMyRecipe` (single retained global), surviving debug hooks `window.__pp_module_ok|__pp_boot_count|__pp_net_debug|__pp_app_state_debug`.

## Flagged Assumptions

- **D-12 forced-storm procedure adapted:** 07-03-SUMMARY.md's console forced-storm (`appState.game.cfg.storm=1; rollStorm = …`) assumed global `appState`/`rollStorm`; after Phase-10 de-globalization + this phase's bridge deletion those are no longer console-reachable (and `__pp_app_state_debug` returns a read-only copy by design). The Safari pass therefore triggers the storm via normal gameplay rather than the console hack. This is a deliberate procedure adaptation, not a coverage gap — the storm render surface is unchanged and the frame-rate observation is identical. Do NOT add a new console-writable global to restore the hack (it would fail ui_contract_check's retained-globals allowlist).
- **SPLIT-03/05/06 edge-probe unresolved:** finally represented by the consolidated concrete checks in Task 1 (ui_contract_check, module_graph_check, the bridge-gone + `<script>` greps, `--verify`). Not silently dropped.

<output>
Create `.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-08-SUMMARY.md` when done
</output>
