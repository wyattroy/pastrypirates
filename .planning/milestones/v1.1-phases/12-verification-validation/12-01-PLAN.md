---
phase: 12-verification-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/VERIFICATION-CHECKLIST.md
autonomous: false
requirements: [VERIFY-01]
user_setup: []

must_haves:
  truths:
    - "`npm test` runs green post-refactor (30/30 determinism + engine/net/state/ui/module-graph/no-undef gates) — satisfies VERIFY-01's automated baseline (D-04)"
    - "A committed docs/VERIFICATION-CHECKLIST.md exists that a human or the orchestrator can re-run on demand (D-01) — no browser-test-framework dependency introduced"
    - "The refactor branch boots cleanly in Chrome from the local server: window.__pp_module_ok === true, window.__pp_boot_count === 1, zero console errors"
  artifacts:
    - docs/VERIFICATION-CHECKLIST.md
  key_links:
    - "Local server serves THIS worktree bound to 127.0.0.1 — never the public playpastrypirates.com (still v1.0), per D-01"
    - "Frozen determinism corpus stays at exactly 1 commit — never --capture (D-04 / D-10)"
---

<objective>
Stand up the Phase 12 verification apparatus end-to-end on the thinnest possible path, and pin Criterion 1 (VERIFY-01) — the headless determinism/regression harness running green post-refactor. This is the lead (tracer) plan: it proves the whole verification spine (local-serve → Chrome-MCP → debug-hook assertion → committed checklist → npm-test evidence) works before the later plans expand it into full solo and multiplayer scenario coverage.

Per D-01, the lasting deliverable is a committed markdown checklist a human or the orchestrator re-runs on demand — NOT a Playwright / Puppeteer / auto-running browser-test framework (explicitly out of scope; it would violate the project's no-build, minimal-dependency ethos). Per D-04, Criterion 1 is satisfied by running the existing 30-seed harness green; no new determinism fixtures are added this milestone.

Purpose: Establish the repeatable verification procedure and lock in the automated regression gate as VERIFY-01 evidence.
Output: docs/VERIFICATION-CHECKLIST.md (new, committed) with the Criterion-1 automated section filled and a boot-smoke line recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-verification-validation/12-CONTEXT.md
@.planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VALIDATION.md
@package.json
@src/main.js
@src/module-contract.js
</context>

<artifacts_this_phase_produces>
This plan creates:
- `docs/VERIFICATION-CHECKLIST.md` — the committed, repeatable verification checklist (four sections: Criterion 1 automated; Criterion 2 solo E2E; Criterion 3 multiplayer + recovery; Criterion 4 manual Safari). Later plans (12-02, 12-03, 12-04) fill the remaining sections. Criterion 1 is filled here.

Later plans in this phase also produce `.planning/phases/12-verification-validation/12-VALIDATION.md` (the Requirements→Test map closeout, written in 12-04).
No new source symbols, endpoints, or runtime code are introduced this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Verification spine tracer — npm-test-green + committed checklist + one Chrome boot smoke</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - package.json (the existing `test` script chain — the Criterion-1 baseline)
    - scripts/determinism_baseline.js (the `--verify` 30/30 determinism oracle)
    - src/main.js (the debug hooks: window.__pp_module_ok, window.__pp_boot_count, window.__pp_net_debug, window.__pp_app_state_debug)
    - src/module-contract.js (MODULE_OK_FLAG === "__pp_module_ok")
    - .planning/phases/11-ui-extraction-orchestration-bridge-removal/11-VALIDATION.md (the format to mirror)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-01, D-04, the local-serve and debug-hook gotchas)
  </read_first>
  <action>
    Create docs/VERIFICATION-CHECKLIST.md as a re-runnable procedure with a "How to re-run" preamble and four checkbox sections. The preamble MUST state: serve the refactor branch locally with `python3 -m http.server` bound to 127.0.0.1 from this worktree's root; confirm the server's working directory is this worktree before trusting any browser check (stale-server-port gotcha); NEVER verify against playpastrypirates.com, which is still v1.0 (D-01). The four sections are headed "Criterion 1 — Automated determinism/regression harness (VERIFY-01)", "Criterion 2 — Solo gameplay-loop E2E (VERIFY-02)", "Criterion 3 — Two-tab multiplayer + pause/refresh recovery (VERIFY-03)", "Criterion 4 — Manual desktop-Safari playthrough (VERIFY-04)"; leave sections 2-4 as empty checkboxes for later plans.
    Run the full `npm test` chain. Record into the Criterion-1 section: the exact 9-script chain that ran, `determinism_baseline.js --verify` showing 30/30 with `SOURCE: unchanged`, and an overall PASS. This is the VERIFY-01 automated evidence (satisfied by the existing 30-seed baseline per D-04 — do not add fixtures).
    Then serve the branch locally and drive Chrome via the browser-MCP to the local URL. Assert the clean-boot signal: `window.__pp_module_ok === true`, `window.__pp_boot_count === 1`, and `read_console_messages` with onlyErrors returns empty. Start a solo game and take exactly ONE live action (a single coin-flip or one sail step) to prove the game is interactive end-to-end. Record a "boot smoke: PASS" line with the observed values into the checklist. Do not embed code fences.
  </action>
  <verify>
    <automated>npm test</automated>
    <human-check>Chrome-MCP navigation to the local 127.0.0.1 server shows window.__pp_module_ok === true, window.__pp_boot_count === 1, zero console errors (read_console_messages onlyErrors empty), and one solo action (coin-flip or sail) succeeds; the boot-smoke line is recorded in docs/VERIFICATION-CHECKLIST.md.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (30/30 determinism + all 9 gates green)
    - docs/VERIFICATION-CHECKLIST.md exists with the four-section skeleton and the "How to re-run" preamble naming the 127.0.0.1 local-serve rule and the playpastrypirates.com prohibition
    - Criterion-1 section records the npm-test PASS with 30/30 and SOURCE: unchanged
    - Chrome boot shows window.__pp_module_ok === true, window.__pp_boot_count === 1, zero console errors; one solo action completes; the boot-smoke PASS line is recorded
  </acceptance_criteria>
  <done>The verification apparatus is proven end-to-end on one thin path and the checklist artifact is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Criterion 1 as the repeatable determinism/regression gate (VERIFY-01, D-04)</name>
  <files>docs/VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - package.json (the `test` chain and the absence of any dependencies/devDependencies)
    - scripts/determinism_baseline.js (the frozen 30-seed corpus and the --verify vs --capture distinction)
    - .planning/phases/12-verification-validation/12-CONTEXT.md (D-04 keep-30-seed-baseline; D-01 minimal-dependency ethos)
  </read_first>
  <action>
    In the Criterion-1 section of docs/VERIFICATION-CHECKLIST.md, document the exact re-run procedure so any operator can reproduce it: run `npm test`; the frozen-corpus invariant is that `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` must equal 1 (the corpus is never re-captured — never pass `--capture`), per D-04 and D-10 (keep the 30-seed baseline; targeted storm/battle/recovery fixtures are deferred, not a v1.1 blocker). Record the currently-observed frozen-corpus count and the npm-test PASS as the standing VERIFY-01 evidence. Also assert and record the D-01 minimal-dependency guarantee: package.json declares zero dependencies and zero devDependencies (no browser-test-framework was added this phase). Do not embed code fences.
  </action>
  <verify>
    <automated>npm test && [ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" = "1" ] && node -e "const p=require('./package.json');process.exit((Object.keys(p.dependencies||{}).length+Object.keys(p.devDependencies||{}).length)===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Criterion-1 section records the `npm test` PASS, the frozen-corpus count of 1, and the "never --capture" rule (D-04)
    - The section records that package.json has zero dependencies and zero devDependencies (no browser-test-framework introduced, D-01)
    - VERIFY-01 evidence is captured and repeatable from the checklist alone
  </acceptance_criteria>
  <done>VERIFY-01's automated baseline is pinned and reproducible from the committed checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → local HTTP test server | The `python3 -m http.server` process exposes the worktree over a socket; only loopback should be reachable |
| browser tab → local server origin | Verification integrity depends on hitting THIS worktree, not the public v1.0 origin |

Phase 12 introduces no new runtime code, endpoint, or untrusted-input handler — the honest attack surface is test-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information disclosure | local HTTP test server | low | mitigate | Bind `python3 -m http.server` to 127.0.0.1 (loopback only), never 0.0.0.0; the server is ephemeral and stopped after testing |
| T-12-02 | Spoofing (verification integrity) | browser origin under test | medium | mitigate | Preamble mandates confirming the server cwd equals this worktree and asserting the local 127.0.0.1 URL; never verify against playpastrypirates.com (still v1.0) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase; browser-test-framework (Playwright/Puppeteer) is explicitly out of scope (D-01). Automated check asserts package.json has zero dependencies and zero devDependencies. |
</threat_model>

<verification>
- `npm test` exits 0 (30/30 determinism + engine/net/state/ui/module-graph/no-undef gates).
- Frozen corpus commit count is exactly 1 (`git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l`).
- package.json declares zero dependencies and zero devDependencies.
- docs/VERIFICATION-CHECKLIST.md exists with the four-section skeleton and a filled Criterion-1 section (npm-test PASS + boot smoke).
</verification>

<success_criteria>
Criterion 1 (VERIFY-01) is proven green post-refactor and captured as a repeatable procedure; the verification apparatus (local-serve → Chrome-MCP → debug hooks → committed checklist) is proven end-to-end on one thin path; the checklist artifact exists for later plans to expand.
</success_criteria>

<output>
Create `.planning/phases/12-verification-validation/12-01-SUMMARY.md` when done.
</output>
