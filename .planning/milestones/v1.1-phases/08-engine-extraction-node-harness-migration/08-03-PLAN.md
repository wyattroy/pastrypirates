---
phase: 08-engine-extraction-node-harness-migration
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - index.html
  - src/engine/index.js
  - scripts/lib/load_engine.js
  - scripts/real_game_test.js
  - scripts/dlog_replay_test.js
autonomous: true
requirements: [SPLIT-01, ENGINE-01, ENGINE-02, ENGINE-03, ENGINE-04]

must_haves:
  truths:
    - "The Game class, roundCfg, the bot-weight tables and rollStorm live in src/engine/index.js with zero DOM, window, Firebase, localStorage, wall-clock or unseeded-random access."
    - "scripts/lib/load_engine.js obtains the engine by native import — no vm, no string slice, no dependency on any index.html text marker — and still returns { Game, roundCfg, sourceHash } to unchanged callers."
    - "The engine extraction and the harness migration are the same commit; no commit in this phase leaves the test suite broken."
    - "sourceHash derives from the concatenated, path-prefixed, lexicographically-sorted source of src/engine/**/*.js plus src/shared/**/*.js."
    - "node scripts/determinism_baseline.js --verify exits 0 across all 30 seeds with the engine fully outside index.html."
    - "dlog_replay_test.js's second, separate replayShortfall sentinel extraction still resolves against the shrunken index.html."
  prohibitions:
    - "scripts/determinism_baseline.js --capture is never run — not to refresh, not to fix a mismatch, not 'just for the source hash'. A red --verify means the code is wrong, not the fixture (D-01)."
    - "No per-seed sha256 in scripts/fixtures/determinism/manifest.json is edited, and no seed-*.jsonl file is modified, regenerated, deleted, or re-sorted (D-02). manifest.json is not touched at all in this plan."
    - "verify() in scripts/determinism_baseline.js is not weakened — no seed skipped, no comparison removed, no early return added, no exit code softened, no tolerance introduced."
    - "loadEngine()'s drift guards are not deleted or softened into warnings — a harness that silently passes because it is testing the wrong thing is worse than no harness."
    - "The [3,2,1] spacing literal inside the island-placement loop is not reordered; it governs how many this.r() calls are consumed before an island position is finalised."
    - "No moved line is renamed, reformatted, re-indented, re-ordered, minified, or 'tidied' while moving (REQUIREMENTS.md Out of Scope). Harness edits are limited to load_engine.js's body plus factually-stale header prose."
    - "No tombstone or placeholder comment is left at a deletion site in index.html."
  artifacts:
    - src/engine/index.js (complete engine tier)
    - scripts/lib/load_engine.js (native import; vm and string slice retired)
  key_links:
    - "scripts/lib/load_engine.js -> src/engine/index.js via native import — the single seam Phase 7 built for this moment (D-11)."
    - "src/engine/index.js -> src/shared/index.js: one-directional; src/shared must never import from src/engine."
    - "index.html classic live turn loop -> Game instance methods and rollStorm, via the PP-BRIDGE globals."
---

<objective>
Move `class Game`, `roundCfg`, the bot-weight tables and the remaining engine code out of `index.html` into `src/engine/index.js`, and in the same commit flip `scripts/lib/load_engine.js` from `vm`/string-slice extraction to a native `import`. This is the critical path of the milestone.

D-12 and ROADMAP criterion 3 require these to land together: a commit where the engine has moved but the harnesses still slice `index.html` is a commit with a broken test suite. Phase 7 built `load_engine.js` as a single seam for exactly this moment — if this turns into a three-file edit, that seam failed and it is worth saying so out loud (D-11).

Purpose: SPLIT-01 and ENGINE-02, gated by ENGINE-03.
Output: a pure engine module, a retired `vm` extraction, and a green 30-seed corpus proving nothing changed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-CONTEXT.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-RESEARCH.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-02-SUMMARY.md
@.planning/phases/07-foundation-determinism-baseline/07-01-SUMMARY.md
@docs/MODULES.md
@scripts/lib/load_engine.js
@src/shared/index.js
@src/engine/index.js
</context>

<artifacts_this_phase_produces>
Full phase register is in `08-01-PLAN.md`. This plan produces:

- **`src/engine/index.js`** completed: `rollStorm` (from 08-01), `PERSONALITY`, `AW`, `TW`, `DW`, `FISH_BASE`, `Game`, `roundCfg`. Imports named symbols from `../shared/index.js`; nothing in `src/shared/` imports from here.
- **`scripts/lib/load_engine.js`** rewritten body: native `import` of `../../src/engine/index.js`, `sourceHash` recomputed from module sources, `node:vm` dependency removed, `index.html` no longer read. Signature and `{ Game, roundCfg, sourceHash }` return shape unchanged (D-13).
- **`scripts/real_game_test.js` / `scripts/dlog_replay_test.js`** — header comment prose only, corrected where it describes the retired extraction mechanism (assumption A-02 in `08-01-PLAN.md`). No logic changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the engine and retire the vm/string-slice harness in one commit</name>
  <files>src/engine/index.js, index.html, scripts/lib/load_engine.js, scripts/real_game_test.js, scripts/dlog_replay_test.js</files>
  <read_first>
    - `index.html` from the `// Bot decision weights` comment block through the closing brace of `class Game` — read the whole class body in one pass. It is ~730 lines; extract everything you need on that pass rather than re-reading ranges.
    - `index.html` around `for(const spacing of [3,2,1]){` — the Tier-1 order-load-bearing literal inside island placement
    - `index.html` around `/* ================= UI ================= */`, `const $=`, `let game=null,...`, the `// Fixed "Round World" ruleset` comment and `function roundCfg(strategies)`, and `function escHtml` — the non-contiguous boundary
    - `scripts/lib/load_engine.js` in full — the hybrid from 08-01, both drift guards, the `sourceHash` computation
    - `scripts/real_game_test.js:1-25` and `scripts/dlog_replay_test.js:1-55` — the header prose describing the retired mechanism, and `dlog_replay_test.js`'s second, separate `replayShortfall` sentinel extraction
    - `index.html:4978-4987` — the `replayShortfall` sentinel region, which sits far below the engine region and must survive the shrink untouched
    - `src/shared/index.js` — the export list the engine module will import from
    - `.planning/phases/08-engine-extraction-node-harness-migration/08-RESEARCH.md` §Q3 and §Q4
  </read_first>
  <reversibility rating="costly">Retires the `vm`/string-slice seam the harnesses have used since v1.0. Git-revertible, but the corpus gate must be green in this same commit — a red `--verify` discovered later becomes a bisect across the largest diff in the milestone.</reversibility>
  <action>
**A. Append the engine tier to `src/engine/index.js`, verbatim, in original source order.**

After the existing `rollStorm` block and before the trailing `export` statement, append in source order:
1. The `// Bot decision weights` comment block and `const PERSONALITY={...};`.
2. The `grudgeBonus`/`windAdv`/`rematchEscalate` comment block and `const AW=`, `const TW=`, `const DW=`, `const FISH_BASE=`.
3. The entire `class Game{ ... }` body, unchanged.
4. The `// Fixed "Round World" ruleset` comment and `function roundCfg(strategies){ ... }`.

Extend the trailing `export { ... }` block to name `rollStorm`, `PERSONALITY`, `AW`, `TW`, `DW`, `FISH_BASE`, `Game`, `roundCfg`. Keep the trailing-export style so every moved line stays byte-identical — 08-04 audits exactly that.

**B. Add the import from the shared tier.** Add one `import { ... } from "../shared/index.js";` near the top of `src/engine/index.js`, naming every shared symbol the engine code references. This is the one directed edge D-03's two-tier layout permits — `src/engine/` may import from `src/shared/`, never the reverse, which is what acceptance criterion 17 asserts. Derive that list mechanically from the moved code rather than from memory; expect at least `mulberry32`, `ING_ALL`, `DIRS`, `TET`, `man`, `windStepCost`, `SAIL_BUDGET`, `SAIL_BUDGET_LEEWARD`, `OPPOSITE`, `NAMES`, `dockPlace`, `iname`, and the ingredient/label helpers the class narrates with. A missing entry surfaces immediately as a `ReferenceError` on the first `--verify` run — that is the intended fast feedback, not a reason to guess broadly and import unused names.

**C. Annotate the last order-load-bearing construct (ENGINE-04, and D-10's "any other object literal whose iteration order reaches `this.r()`" clause — the island-spacing literal is the one such construct in the engine tier).** Add a comment line immediately above `for(const spacing of [3,2,1]){` starting with the exact token `// ORDER IS LOAD-BEARING`, followed by the reason: each iteration calls `shapeFor()`, which consumes two to four `this.r()` calls, so reordering the literal changes how many draws are consumed and in what sequence before an island position is finalised.

This is the **only** annotation in `src/engine/index.js` — the other six live in `src/shared/index.js` from 08-02 (D-09's five direction tables plus `TET`). The token must appear exactly once in this file and nowhere in its header comment or prose, bringing the phase total to seven across the two module files, matching RESEARCH's Tier-1 + Tier-2 inventory.

**D. Delete the moved lines from `index.html`.** Two non-contiguous regions: the weights-through-`class Game` block, and the `roundCfg` comment plus function. Between them, the `/* ================= UI ================= */` banner, `const $=id=>document.getElementById(id);` and `let game=null,evIdx=0,timer=null,logLines=[];` stay exactly where they are (D-07; A-01 in `08-01-PLAN.md`). `function escHtml` and everything below it stays. No tombstones.

**E. Rewrite `scripts/lib/load_engine.js`'s body.** Replace the `vm` + string-slice mechanism with a native `import` of `../../src/engine/index.js`. Remove the `node:vm` import, the monolith file read, the `<script>`/`escHtml` boundary search, and the DOM sandbox stub — all of it is now dead weight, and the end marker it keyed off has no other consumer in the repo (verified: `load_engine.js` was its only reference). This is D-11's single-seam moment and D-13's contract: the body flips, the signature does not.

**Rewrite the file's header comment too, not just the body.** The existing header describes the retired extraction mechanism and names the monolith by filename several times. Acceptance criteria 13 and 14 assert that neither the `vm` module specifier nor that filename appears anywhere in this file — including in comments — because a stale comment describing a mechanism the file no longer uses is exactly the drift this project's own loud-failure convention exists to prevent. Describe the new mechanism (native import of the engine barrel, `sourceHash` computed over the module sources) in prose that names neither.

<!-- planner-discipline-allow: node:vm -->
<!-- planner-discipline-allow: index.html -->


Recompute `sourceHash` per RESEARCH Q3: enumerate every `.js` file under `src/engine/` and `src/shared/`, sort by relative path lexicographically (not `fs.readdir` order, which is unspecified on some platforms), and for each file feed `relative/path.js` + a newline + the raw UTF-8 content + a newline into one `sha256`. The path prefix prevents a false negative where two different file-boundary splits concatenate to the same byte stream. Include `src/shared/` as well as `src/engine/`, because the order-load-bearing constants live in shared and a reorder there is exactly the kind of source change this hash exists to flag.

Keep both drift guards in spirit — they must still throw loudly, with messages updated to describe the new failure mode (the engine module failing to export `Game`/`roundCfg` as functions). Do not soften a guard into a warning.

Keep `loadEngine()`'s async signature and its `{ Game, roundCfg, sourceHash }` return shape (D-13). No caller changes. If this edit spills past `load_engine.js` into `real_game_test.js`, `dlog_replay_test.js` or `determinism_baseline.js` logic, stop and say so in the SUMMARY — that means Phase 7's seam did not do its job (D-11).

**F. Correct the now-false harness header prose.** `scripts/real_game_test.js`'s header describes extracting a region "verbatim and run in a Node `vm` context" and references the `================= UI =================` marker; `scripts/dlog_replay_test.js`'s header describes "two separate extractions". Update only the prose that is now factually wrong, so it describes the native import for the engine while preserving `dlog_replay_test.js`'s accurate description of its second, still-`vm`-based `replayShortfall` extraction. Change no logic in either file.

**G. Confirm the `replayShortfall` sentinel survived.** `dlog_replay_test.js`'s second extraction keys off the sentinel comment pair at `index.html:4978` and `:4987`, far below the engine region. The shrink shifts their line numbers but not their text. `npm test` proves it, but state explicitly in the SUMMARY that this second extraction was deliberately left in place — it is outside D-12's scope, which covers engine loading only, and 07-VERIFICATION.md records the same scoping.

Commit A through G as one commit (D-12).
  </action>
  <acceptance_criteria>
Run from the repo root. `wc -l` piped through `tr -d ' '` for BSD `wc`. Criterion 1 is D-16's primary gate and must be green in this same commit — the largest diff of the milestone is the worst possible place to discover a red `--verify` three commits late.

1. `node scripts/determinism_baseline.js --verify; echo "exit=$?"` → prints `exit=0` and `All seeds passed.`
2. `node scripts/determinism_baseline.js --verify | grep -c 'PASS seed'` → `30`
3. `node scripts/determinism_baseline.js --verify | grep -q 'SOURCE: moved, behavior identical'` → exit 0
4. `npm test; echo "exit=$?"` → prints `exit=0`
5. `node scripts/real_game_test.js 25; echo "exit=$?"` → prints `exit=0`
6. `node scripts/dlog_replay_test.js; echo "exit=$?"` → prints `exit=0` (proves the `replayShortfall` sentinel extraction still resolves)
7. `grep -q '^class Game{' index.html; echo "exit=$?"` → prints `exit=1`
8. `grep -q '^function roundCfg' index.html; echo "exit=$?"` → prints `exit=1`
9. `grep -q '^const PERSONALITY=' index.html; echo "exit=$?"` → prints `exit=1`
10. `[ "$(grep -c '^class Game{' src/engine/index.js)" -eq 1 ]` → exit 0
11. `[ "$(grep -c 'ORDER IS LOAD-BEARING' src/engine/index.js)" -eq 1 ]` → exit 0 (the island-spacing literal is the engine tier's only annotated construct); and the combined phase total across both module files is `7`: `[ "$(cat src/shared/index.js src/engine/index.js | grep -c 'ORDER IS LOAD-BEARING')" -eq 7 ]` → exit 0
12. `grep -q 'for(const spacing of \[3,2,1\]){' src/engine/index.js; echo "exit=$?"` → prints `exit=0` (literal order byte-preserved)
13. `grep -q 'node:vm' scripts/lib/load_engine.js; echo "exit=$?"` → prints `exit=1`
14. `grep -q 'index.html' scripts/lib/load_engine.js; echo "exit=$?"` → prints `exit=1` (the harness no longer reads or references the HTML at all)
15. `[ "$(grep -c 'from "../../src/engine/index.js"' scripts/lib/load_engine.js)" -eq 1 ]` → exit 0
16. `sed 's://.*::' src/engine/index.js src/shared/index.js | grep -nE 'document\.[A-Za-z]+|window\.[A-Za-z]+|\bfirebase\b|localStorage|Date\.now|Math\.random|\bglobalThis\b|new Function'; echo "exit=$?"` → prints `exit=1`
17. `grep -rn 'from ".*engine' src/shared/; echo "exit=$?"` → prints `exit=1` (DAG direction: shared never imports engine)
18. `node --input-type=module -e "import('./src/engine/index.js').then(m=>{if(typeof m.Game!=='function'||typeof m.roundCfg!=='function'||typeof m.rollStorm!=='function')process.exit(1)})"; echo "exit=$?"` → prints `exit=0` — imports under plain Node with no DOM, proving ENGINE-01 behaviourally
19. `node --input-type=module -e "import('./scripts/lib/load_engine.js').then(async m=>{const r=await m.loadEngine();const ok=typeof r.Game==='function'&&typeof r.roundCfg==='function'&&/^[0-9a-f]{64}\$/.test(r.sourceHash);process.exit(ok?0:1)})"; echo "exit=$?"` → prints `exit=0` (D-13 return shape preserved)
20. `[ "$(grep -c '<script>' index.html)" -eq 1 ]` → exit 0
21. `[ "$(grep -c 'const \$=id=>document.getElementById(id);' index.html)" -eq 1 ]` and `[ "$(grep -c 'let game=null,evIdx=0,timer=null,logLines=\[\];' index.html)" -eq 1 ]` → both exit 0
22. `[ "$(grep -c 'replayShortfall' index.html)" -ge 5 ]` → exit 0 (sentinel region intact)
23. `git log --oneline -1 --name-only | grep -q 'src/engine/index.js'` and the same commit's file list includes `scripts/lib/load_engine.js` → the extraction and the harness migration are one commit (D-12)
24. `[ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" -eq 1 ]` → exit 0
25. `[ "$(git log --oneline -- scripts/fixtures/determinism/manifest.json | wc -l | tr -d ' ')" -eq 1 ]` → exit 0
26. `git status --porcelain scripts/fixtures/determinism/` → empty output
  </acceptance_criteria>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify &amp;&amp; npm test &amp;&amp; node scripts/real_game_test.js 25 &amp;&amp; [ "$(cat src/shared/index.js src/engine/index.js | grep -c 'ORDER IS LOAD-BEARING')" -eq 7 ] &amp;&amp; { sed 's://.*::' src/engine/index.js src/shared/index.js | grep -qE 'document\.[A-Za-z]+|window\.[A-Za-z]+|\bfirebase\b|localStorage|Date\.now|Math\.random|\bglobalThis\b|new Function' &amp;&amp; exit 1 || true; } &amp;&amp; [ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" -eq 1 ]</automated>
  </verify>
  <done>`class Game`, `roundCfg` and the bot-weight tables live in `src/engine/index.js` and import from `../shared/index.js`; `scripts/lib/load_engine.js` obtains them by native import with no `vm` and no reference to `index.html`; both landed in one commit; all 30 seeds verify green and the corpus is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Post-shrink audit of index.html — nothing left behind, nothing dragged along, no orphaned boundary markers</name>
  <files>none — read-only audit; any finding is fixed by amending Task 1's commit</files>
  <read_first>
    - `index.html` around the classic `<script>` opening and the first ~30 lines that now follow it — confirm what the region looks like after the shrink
    - `index.html` around `/* ================= UI ================= */` through `function escHtml` — the new junction
    - `scripts/lib/load_engine.js` as rewritten in Task 1
    - `docs/MODULES.md` §"extraction hazard" — the bare-`<script>`-tag rule Phase 7 established
  </read_first>
  <reversibility rating="reversible">Read-only audit; any finding is fixed by amending Task 1's commit.</reversibility>
  <action>
Confirm the shrink is clean rather than assuming it. Three questions, each answered by a command, not by reading:

**1. Did anything get dragged along that should have stayed?** The three items with an explicit "stays behind" ruling are `const $` (D-07), `let game=null,evIdx=0,timer=null,logLines=[]` (assumption A-01), and `"use strict";` (assumption A-03). Confirm each is still present exactly once in `index.html` and absent from both module files.

**2. Did anything get left behind that should have moved?** Scan the remaining classic script for any declaration of a symbol that is now also exported by either module — a duplicate declaration would shadow the bridge silently and produce a stale copy that never sees module-side changes. Compare the two barrels' export lists against the classic script's remaining top-level `const`/`let`/`function`/`class` declarations and confirm zero overlap.

**3. Are the retired boundary markers orphaned or still load-bearing?** `load_engine.js` was the only consumer of the end marker and of the `<script>` start offset; both references are now gone. `escHtml` itself is still a live function used by the UI and must stay — only its role as a slice boundary retired. Confirm no file in `scripts/` still searches for either marker, and that the bare `<script>` tag count is still exactly 1 (Phase 7's contract, `docs/MODULES.md`).

<!-- planner-discipline-allow: function escHtml -->


Record the measured `index.html` line count before and after the phase in the SUMMARY — this is the first phase in the milestone where the monolith shrinks, and the number is the headline evidence for it.
  </action>
  <acceptance_criteria>
1. `[ "$(grep -c 'const \$=id=>document.getElementById(id);' index.html)" -eq 1 ]` → exit 0
2. `grep -q 'getElementById' src/shared/index.js src/engine/index.js; echo "exit=$?"` → prints `exit=1`
3. `[ "$(grep -c 'let game=null,evIdx=0,timer=null,logLines=\[\];' index.html)" -eq 1 ]` → exit 0
4. `[ "$(grep -c '"use strict";' index.html)" -eq 1 ]` → exit 0
5. Duplicate-declaration scan returns zero overlap: for every name exported by `src/shared/index.js` or `src/engine/index.js`, `grep -cE "^(const|let|var|function|class|async function) ?<name>\b" index.html` is `0`. Run this as a loop over the full export list and print `ok` only if no name matched.
6. `grep -rn 'function escHtml' scripts/; echo "exit=$?"` → prints `exit=1`
7. `grep -rn 'indexOf("<script>")' scripts/; echo "exit=$?"` → prints `exit=1`
8. `[ "$(grep -c 'function escHtml' index.html)" -eq 1 ]` → exit 0 (still a live UI helper)
9. `[ "$(grep -c '<script>' index.html)" -eq 1 ]` → exit 0
10. `wc -l index.html | tr -d ' '` → a value at least 900 lines lower than the pre-phase `5640`; the exact before/after numbers are recorded in the SUMMARY.
11. `node scripts/determinism_baseline.js --verify; echo "exit=$?"` → prints `exit=0`
12. `npm test; echo "exit=$?"` → prints `exit=0`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify &amp;&amp; npm test &amp;&amp; [ "$(grep -c '<script>' index.html)" -eq 1 ] &amp;&amp; [ "$(grep -c 'const \$=id=>document.getElementById(id);' index.html)" -eq 1 ] &amp;&amp; [ "$(wc -l < index.html | tr -d ' ')" -lt 4740 ]</automated>
  </verify>
  <done>`index.html` shrank by at least 900 lines with no symbol duplicated between the classic script and the modules, the three "stays behind" items intact, the retired slice markers confirmed orphaned in `scripts/`, and the suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 7 golden corpus → moved engine | The frozen per-seed hashes are the only evidence that moving ~800 lines of game logic changed nothing. |
| `loadEngine()` → its callers | Three harnesses trust this one function to hand them the real engine. If it silently hands back something else, every downstream green is meaningless. |
| Module realm → classic-script realm | The live turn loop reaches `Game` instance methods and `rollStorm` through the bridge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `--capture` vs. `scripts/fixtures/determinism/` | critical | mitigate | Prohibited (D-01). Criteria 24–26 fail if the corpus gains a commit or the tree dirties. |
| T-08-02 | Tampering | `manifest.perSeed[].sha256` | critical | mitigate | Frozen (D-02); `manifest.json` is not touched at all in this plan and criterion 25 asserts it still has one commit. |
| T-08-09 | Spoofing | `loadEngine()`'s drift guards | high | mitigate | The vm-era guards existed because a slice that silently moved would let the harness test the wrong text. The native-import era has the same failure mode in a new shape: a module that loads but exports the wrong thing. Guards must be preserved as throws, not softened to warnings; criterion 19 asserts the return shape end-to-end. |
| T-08-10 | Tampering | `[3,2,1]` island-spacing literal | high | mitigate | Tier-1 order-load-bearing: each iteration consumes two to four `this.r()` calls, so a reorder shifts the RNG stream for every seed. Annotated (criterion 11) and byte-order grepped (criterion 12) on top of the corpus gate. |
| T-08-11 | Tampering | Duplicate declaration shadowing the bridge | high | mitigate | A symbol left declared in the classic script while also exported by a module would shadow the bridge with a stale copy that never sees module-side changes — and would look correct until the two diverge. Task 2 criterion 5 scans the full export list for exactly this. |
| T-08-03 | Tampering | `verify()` in `determinism_baseline.js` | high | mitigate | Prohibited; criterion 2 fails if any seed stops being compared. |
| T-08-04 | Repudiation | Commit granularity | medium | mitigate | D-12: extraction and harness migration are one commit, asserted by criterion 23. `manifest.json` stays out of it entirely. |
| T-08-12 | Denial of service | `replayShortfall` sentinel region | medium | mitigate | The shrink moves the sentinel comments' line numbers. If either comment were reworded or removed, `dlog_replay_test.js` throws. Criteria 6 and 22 cover it. |
| T-08-06 | Information disclosure | `window.PP` surface | low | accept | Game constants and pure functions only; the page already ran entirely as global classic-script code. Bridge is deleted in Phase 11. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages introduced; `package.json` gains no `dependencies`/`devDependencies` key. An install task appearing during execution is out of scope and must halt the plan. |
</threat_model>

<verification>
- 30/30 seeds green with the engine fully outside `index.html`, corpus untouched and still one commit deep.
- `npm test`, `real_game_test.js 25` and `dlog_replay_test.js` all exit 0.
- `load_engine.js` contains no `node:vm` and no reference to `index.html`; `loadEngine()` still returns `{ Game, roundCfg, sourceHash }` with a 64-hex-char hash.
- Comment-stripped purity scan over both module files returns no matches; `src/shared/` imports nothing from `src/engine/`.
- Seven `ORDER IS LOAD-BEARING` annotations across the two module files.
- Extraction and harness migration in one commit.

**Coverage gap this task does not close, deliberately:** a green corpus exercises `Game.play()`, which never sets `windNow2` and never calls `rollStorm` from outside the class. The classic live turn loop is different code. 08-05 covers it in a browser.
</verification>

<success_criteria>
SPLIT-01, ENGINE-01 and ENGINE-02 are satisfied — a pure, DOM-free engine module that the Node harnesses import natively — with ENGINE-03 proven green in the same commit and ENGINE-04's annotation set complete at seven.
</success_criteria>

<output>
Create `.planning/phases/08-engine-extraction-node-harness-migration/08-03-SUMMARY.md` when done. Record the `index.html` line count before and after, the new `sourceHash` value, the exact shared-import list the engine module needed, and whether the `load_engine.js` edit stayed contained to that one file (D-11's stated test of Phase 7's seam).
</output>
