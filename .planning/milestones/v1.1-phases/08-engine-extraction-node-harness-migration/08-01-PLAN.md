---
phase: 08-engine-extraction-node-harness-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/main.js
  - src/shared/index.js
  - src/engine/index.js
  - scripts/lib/load_engine.js
autonomous: true
requirements: [SPLIT-01, SPLIT-02, ENGINE-02, ENGINE-03]

must_haves:
  truths:
    - "A symbol declared only inside an ES module and published via Object.assign(globalThis, PP) resolves as a BARE identifier inside a classic-script function body that was parsed before the module ran — proven in a real browser on this exact page, not by spec reasoning (RESEARCH Assumption A1)."
    - "The same mechanism works for a symbol the headless corpus never exercises: rollStorm is called from the classic live turn loop (index.html:4762, :4785) as well as from Game.play() (:1774)."
    - "Startup is inverted: index.html no longer self-invokes boot(); src/main.js calls window.boot() after the bridge is populated, and the page still reaches its lobby."
    - "node scripts/determinism_baseline.js --verify exits 0 across all 30 seeds with two real symbols already living outside index.html."
    - "scripts/lib/load_engine.js sources the two moved symbols from the real modules (not from the HTML slice), so the harness tests the same code the browser runs."
  prohibitions:
    - "scripts/determinism_baseline.js --capture is never run — not to refresh, not to fix a mismatch, not 'just for the source hash'. A red --verify means the code is wrong, not the fixture (D-01)."
    - "No per-seed sha256 in scripts/fixtures/determinism/manifest.json is edited, and no seed-*.jsonl file is modified, regenerated, deleted, or re-sorted (D-02)."
    - "verify() in scripts/determinism_baseline.js is not weakened — no seed skipped, no comparison removed, no early return added, no exit code softened, no tolerance introduced."
    - "No moved line is renamed, reformatted, re-indented, re-ordered, minified, or 'tidied' while moving (REQUIREMENTS.md Out of Scope)."
    - "No tombstone or placeholder comment is left at a deletion site in index.html — the diff must read as pure removal."
    - "The bridge does not grow beyond the minimum surface this phase actually needs (D-15). The 40+ implicit globals (game, myId, room, db, …) are Phase 10's scope under GLOBAL-01; publishing them through the bridge here would blur the phase boundary and inflate this phase's blast radius. Only engine symbols the classic region provably references may be added."
  artifacts:
    - src/shared/index.js
    - src/engine/index.js
    - src/main.js (bridge population + inversion of control)
    - scripts/lib/load_engine.js (hybrid: module-sourced symbols injected into the vm sandbox)
  key_links:
    - "src/main.js -> globalThis: Object.assign(globalThis, PP) publishes every module export as a global-object property."
    - "index.html class Game constructor (:1080) -> mulberry32: bare identifier, resolved through the bridge."
    - "index.html runLiveNet (:4762, :4785) -> rollStorm: bare identifier, resolved through the bridge — the corpus-blind path."
    - "src/main.js -> window.boot(): the classic script no longer self-starts."
    - "scripts/lib/load_engine.js -> src/shared/index.js + src/engine/index.js: vm sandbox seeded from the real modules."
---

<objective>
Prove the phase's single load-bearing architectural bet — that `Object.assign(globalThis, PP)` from a deferred module makes bare identifiers resolve correctly inside classic-script code that was parsed first — by moving two real symbols end-to-end through every layer this phase will touch, before the expensive extraction is built on top of it.

RESEARCH.md rates this mechanism MEDIUM-HIGH, not HIGH (Assumption A1): it rests on ECMAScript spec reasoning plus this project's own `firebase`-global precedent, but was never executed in a real browser. If A1 is wrong, plans 08-02 through 08-05 are all built on sand. This tracer costs one commit and removes the doubt.

Purpose: de-risk before the ~950-line verbatim move.
Output: `src/shared/index.js` and `src/engine/index.js` seeded with one real symbol each, a populated and named `window.PP` bridge, inverted startup, and a hybrid `load_engine.js` — all with the 30-seed corpus green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-CONTEXT.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-RESEARCH.md
@.planning/phases/07-foundation-determinism-baseline/07-01-SUMMARY.md
@.planning/phases/07-foundation-determinism-baseline/07-03-SUMMARY.md
@docs/MODULES.md
@src/main.js
@scripts/lib/load_engine.js
</context>

<artifacts_this_phase_produces>
Complete register for Phase 8. Plans 08-02…08-05 add to it; nothing else is created.

**New module files**
| Path | Contents | Introduced |
|------|----------|-----------|
| `src/shared/index.js` | Leaf tier. Exports: `mulberry32`, `ING_ALL`, `ING_EMOJI`, `ASSET_BASE`, `ING_IMG`, `ING_HOLE_IMG`, `BOARD_IMG`, `DOCK_IMG`, `WIND_ARROW_IMG`, `TRADE_SWIRL_IMG`, `PLAY_IMG`, `PAUSE_IMG`, `FLIP_HEADS_IMG`, `FLIP_TAILS_IMG`, `CLOCK_IMG`, `FLIP_SOCKET_IMG`, `COMPASS_DIAL_IMG`, `COMPASS_NEEDLE_IMG`, every other `*_IMG` constant declared in `index.html:882`–`:918`, `EMOJI_IMG`, `EMOJIFY_RE`, `emojify`, `BOAT_IMG`, `ISLAND_SHAPE_IMG`, `TET`, `ING_NAME`, `ING_PLAIN`, `DOCK_PLACE`, `DOCK_FLAVOR`, `dockPlace`, `dockFlavor`, `iname`, `ilabel`, `ingImg`, `ilabelImg`, `iconImg`, `DIRS`, `DIRNAME`, `PERP`, `STORM_DIAG`, `OPPOSITE`, `SAIL_BUDGET`, `SAIL_BUDGET_LEEWARD`, `windStepCost`, `NAMES`, `DEFAULT_NAMES`, `unusedDefaultName`, `COLORS`, `HEXCOL`, `man` | 08-01 (`mulberry32` only), completed 08-02 |
| `src/engine/index.js` | Engine tier. Exports: `rollStorm`, `PERSONALITY`, `AW`, `TW`, `DW`, `FISH_BASE`, `Game`, `roundCfg`. Imports from `../shared/index.js`; never the reverse. | 08-01 (`rollStorm` only), completed 08-03 |

**The bridge**
- **Name:** `window.PP` — an object built as `{ ...sharedNamespace, ...engineNamespace }` in `src/main.js`.
- **Surface:** every export of both barrels above. Not a curated subset — RESEARCH Pattern 3 shows the classic live turn loop calls `game.r()`, `rollStorm(game)`, `PERP[...]`, `windStepCost`, and a dozen `Game` instance methods directly from outside the class.
- **Second mechanism:** `Object.assign(globalThis, PP)` — publishes the same keys as global-object properties so ~150+ pre-existing bare-identifier call sites in the classic region need zero edits (D-15).
- **Grep marker:** both lines carry the literal token `PP-BRIDGE` so Phase 11's removal sweep has one thing to search for.
- **Lifetime:** temporary by design. Phase 10 migrates consumers off it; Phase 11 deletes it (ROADMAP Phase 11 criterion 3).

**New functions in the classic script (`index.html`)** — introduced 08-02
- `applyEngineBootstrapEffects()` — carries the three relocated D-06 impurities (`index.html:920`, `:922`, `:1002`) plus the `:996`–`:1001` comment block that explains `:1002`.
- `attachPastryArt()` — wraps the `RECIPE_BOOK.forEach(...)` statement at `index.html:2095`, which references the moving `ASSET_BASE` at true classic top level and therefore cannot be fixed by bridge timing alone (RESEARCH Pattern 2).

**New tooling**
| Path | Flags | Purpose | Introduced |
|------|-------|---------|-----------|
| `scripts/engine_contract_check.js` | none (exit 0/1) | Four assertions: engine/shared purity (comment-stripped), `ORDER IS LOAD-BEARING` annotation coverage, shared→engine DAG direction, moved-symbol export completeness. Wired into `npm test`. | 08-04 |
| `scripts/rebase_source_hash.js` | none (exit 0/1) | Rewrites **only** `manifest.engineSourceHash`, gated on a fresh-replay comparison already matching every frozen per-seed hash. Never touches `perSeed[]`, `capturedAt`, `coverage`. Exists because `--capture` is currently the only write path to `manifest.json` and it rewrites the frozen hashes (D-01/D-02). | 08-05 |

**Modified, not created:** `index.html` (shrinks by ~950 lines), `src/main.js`, `scripts/lib/load_engine.js`, `scripts/real_game_test.js` + `scripts/dlog_replay_test.js` (header prose only), `package.json` (`test` script), `scripts/fixtures/determinism/manifest.json` (`engineSourceHash` line only), `docs/MODULES.md`.
</artifacts_this_phase_produces>

<assumptions>
Surfaced rather than dropped — no SPEC.md exists for this phase.

- **A-01 — `index.html:1813` (`let game=null,evIdx=0,timer=null,logLines=[];`) must stay in the classic script.** D-07 names only `const $` (`:1812`) as the "sits inside the extraction region but is not engine" case. Line `:1813` is the same class of item and is not named anywhere in CONTEXT.md or RESEARCH.md. It is app state the UI mutates (`beginGame` assigns `game=new Game(...)` at `:5414`), and 07-03-SUMMARY.md records that `let game` deliberately lives in classic script scope and never lands on `window`. Treated as: stays put, exactly like `$`. Flagged because it is an unstated edge of D-07.
- **A-02 — The header prose in `scripts/real_game_test.js` and `scripts/dlog_replay_test.js` describing the `vm`/string-slice mechanism becomes factually false once `load_engine.js` imports natively.** D-11 says "nothing else about the harnesses changes"; that is read as *no logic changes*. A comment that lies about how the harness works violates the project's own loud-failure-on-drift convention. Treated as: correcting that prose in 08-03 is in scope and is the only permitted harness edit beyond `load_engine.js`.
- **A-03 — `"use strict";` at `index.html:860` stays in the classic script.** It governs the whole remaining classic block. Modules are strict by default, so it is not needed in the new module files.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end bridge slice — two real symbols out of index.html, through the bridge, into the harness, corpus green</name>
  <files>src/shared/index.js, src/engine/index.js, src/main.js, index.html, scripts/lib/load_engine.js</files>
  <read_first>
    - `index.html:855-870` — the classic `<script>` opening, `"use strict";`, the `/* RNG */` banner, and `function mulberry32` at `:862`
    - `index.html:1016-1023` — the two-line comment block and `function rollStorm(g)`
    - `index.html:1078-1082` — the `Game` constructor's `this.rng=mulberry32(seed)` call site
    - `index.html:1770-1778` — `Game.play()`'s `rollStorm(this)` call site
    - `index.html:4755-4790` — `runLiveNet`'s two `rollStorm(game)` call sites and the `windNow2`/`PERP` second-gust rolls
    - `index.html:5591-5640` — `boot()`, the `boot();` self-invocation at `:5636`, `</script>` at `:5637`, the module tag at `:5638`
    - `src/main.js` (all 34 lines) — the existing `typeof window` guard and the `__pp_module_ok` marker
    - `scripts/lib/load_engine.js` (all 56 lines) — the vm sandbox and the `sourceHash` computation
    - `docs/MODULES.md` — the classic-before-module ordering rule this must not violate
  </read_first>
  <reversibility rating="reversible">Two small symbol moves plus one startup rewiring; a single `git revert` restores the pre-tracer tree with no external state involved.</reversibility>
  <action>
Move exactly two symbols out of `index.html` and wire the full bridge path around them. Every line that moves must arrive byte-identical.

**1. Create `src/shared/index.js`.** Open with a short header comment naming it the Phase 8 shared leaf tier and stating that it holds no DOM, `window`, Firebase, wall-clock, or unseeded-random access. Then move `index.html:861-862` verbatim (the `/* ================= RNG ================= */` banner travels with the function it labels). Close the file with a single trailing `export { mulberry32 };` statement. Use a trailing export block rather than prefixing `export ` onto the declaration — that keeps every moved line byte-identical to its source, which is what makes the pure-motion audit in 08-04 mechanically checkable.

**2. Create `src/engine/index.js`.** Header comment naming it the Phase 8 engine tier, same purity statement, plus a line recording that it imports from `../shared/index.js` and must never be imported *by* `src/shared/`. Move `index.html:1016-1023` verbatim (the two `notes/edits #1a` comment lines and the storm-roll function declaration at `:1018`). Close with `export { rollStorm };`.

Both new files follow `src/` layout D-03 (`src/engine/` for engine tier, `src/shared/` for leaves with no engine dependency) and the leaves-first dependency-depth split of D-04. Both must satisfy the D-08 purity rule from the first line they contain: zero `document`, `window`, `firebase`, `localStorage`, `Date.now`, `Math.random`.

**3. Delete the moved lines from `index.html`** — `:861-862` and `:1016-1023`. Leave nothing behind at either site: no tombstone, no "moved to src/…" note, no blank-line padding beyond what the surrounding code already had. The diff for these hunks must read as pure removal. `"use strict";` at `:860` stays (A-03).

**4. Extend `src/main.js`** inside its existing `if (typeof window !== "undefined")` block, after the `firebase` tripwire. Import both barrels as namespace objects. Build `const PP = { ...shared, ...engine };`. Assign `window.PP = PP;` and `Object.assign(globalThis, PP);`. Put the literal token `PP-BRIDGE` in a comment on each of those two lines, together with the note that this is temporary and removed in Phase 11 (D-14's "temporary and named" constraint, and the grep target ROADMAP Phase 11 criterion 3 will use). Then call `window.boot();` as the last statement in the block. Use only named exports in both barrels so no `default` key lands on `globalThis`.

**5. Invert startup.** Delete the classic script's self-invocation of `boot` at `index.html:5636` (the whole line, leaving nothing behind). `boot` is a classic-script `function` declaration, so it is already an own property of `window` with no bridge entry needed — `window.boot()` works as-is. This inversion is required *now*, not later: `boot()` reaches `resumeSoloGame()` at `:5610`, which constructs `new Game(...)`, which calls bare `mulberry32` — so a saved solo game in `localStorage` would throw a `ReferenceError` at parse time if the classic script still self-invoked ahead of the module.

**6. Make `scripts/lib/load_engine.js` a hybrid.** Keep the existing string-slice and `vm` execution of the (now two-symbol-lighter) engine region, but import both barrels at the top of the file and spread their exports into the `sandbox` object before `vm.createContext`, so the sliced region resolves `mulberry32` and `rollStorm` against the real module code rather than against text that is no longer in the HTML. Leave `loadEngine()`'s signature and its `{ Game, roundCfg, sourceHash }` return shape untouched (D-13). Leave both drift guards and their exact error messages untouched. Add one comment noting the hybrid is transitional and that 08-03 replaces the whole body with a native import.

The `sourceHash` will change because the sliced region shrank. That is expected and is diagnostic-only — `determinism_baseline.js:219-232` never lets `SOURCE` drive the exit code. The gate is the per-seed hash comparison: D-16 makes `--verify` green across all 30 seeds, achieved without re-capture, this phase's primary gate, and it applies to this tracer commit exactly as it applies to the large moves that follow.
  </action>
  <acceptance_criteria>
Every command below runs from the repo root. `wc -l` output is piped through `tr -d ' '` because BSD `wc` on macOS right-pads its count (recorded in 07-01-SUMMARY.md).

1. `node scripts/determinism_baseline.js --verify; echo "exit=$?"` → prints `exit=0` and the line `All seeds passed.`
2. `node scripts/determinism_baseline.js --verify | grep -c 'PASS seed'` → `30`
3. `node scripts/determinism_baseline.js --verify | grep -q 'SOURCE: moved, behavior identical'` → exit 0
4. `npm test; echo "exit=$?"` → prints `exit=0`
5. `node scripts/real_game_test.js 25; echo "exit=$?"` → prints `exit=0`
6. `grep -q 'function mulberry32' index.html; echo "exit=$?"` → prints `exit=1`
7. `grep -q 'function rollStorm' index.html; echo "exit=$?"` → prints `exit=1`
8. `[ "$(grep -c 'export { mulberry32 }' src/shared/index.js)" -eq 1 ]` → exit 0
9. `[ "$(grep -c 'export { rollStorm }' src/engine/index.js)" -eq 1 ]` → exit 0
10. `grep -qx 'boot();' index.html; echo "exit=$?"` → prints `exit=1`
11. `[ "$(grep -c 'window.boot()' src/main.js)" -eq 1 ]` → exit 0
12. `[ "$(grep -c 'Object.assign(globalThis' src/main.js)" -eq 1 ]` → exit 0
13. `[ "$(grep -c 'PP-BRIDGE' src/main.js)" -eq 2 ]` → exit 0
14. `[ "$(grep -c '<script>' index.html)" -eq 1 ]` → exit 0 (the attribute-less count Phase 7's contract fixed at 1)
15. `[ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" -eq 1 ]` → exit 0. This is the anti-`--capture` tripwire: the 30 corpus files have exactly one commit in their entire history and must still have exactly one at the end of Phase 8.
16. `[ "$(git log --oneline -- scripts/fixtures/determinism/manifest.json | wc -l | tr -d ' ')" -eq 1 ]` → exit 0. Unchanged until 08-05's dedicated rebase commit.
17. `git status --porcelain scripts/fixtures/determinism/` → empty output
18. `node --input-type=module -e "import('./src/shared/index.js').then(m=>{if(typeof m.mulberry32!=='function')process.exit(1)})"; echo "exit=$?"` → prints `exit=0`
19. `node --input-type=module -e "import('./src/engine/index.js').then(m=>{if(typeof m.rollStorm!=='function')process.exit(1)})"; echo "exit=$?"` → prints `exit=0`
20. `git diff -- index.html | grep -c '^+[^+]'` → `0` (this task only removes lines from `index.html`; it adds none)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify &amp;&amp; npm test &amp;&amp; node scripts/real_game_test.js 25 &amp;&amp; [ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" -eq 1 ]</automated>
  </verify>
  <done>`mulberry32` and `rollStorm` live in `src/shared/index.js` and `src/engine/index.js` respectively, `index.html` no longer declares them and no longer self-invokes `boot()`, `src/main.js` publishes the `PP-BRIDGE` and calls `window.boot()`, `load_engine.js` seeds its sandbox from the real modules, all 30 seeds verify green, and the corpus fixtures have not been touched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Assumption A1 in a real browser — bare-identifier resolution through the bridge, on this exact page</name>
  <files>none — verification only; this task modifies no files</files>
  <precondition>A static HTTP server is serving THIS worktree (`/Users/wyattroy/Documents/Projects/pastrypirates/.claude/worktrees/new-session-d6e9d7`). 07-03-SUMMARY.md records that stale `python3 -m http.server` processes from sibling worktrees hold ports 8000/8001 and return a healthy 200 from the wrong tree — confirm the serving process's cwd matches this worktree before trusting any result, and use port 8777 if 8000/8001 are held.</precondition>
  <read_first>
    - `.planning/phases/07-foundation-determinism-baseline/07-03-SUMMARY.md` — the browser-check method, the `window.__pp_module_ok` tripwire, the storm-forcing snippet, and the stale-dev-server hazard
    - `src/main.js` as written by Task 1 — the bridge population order
    - `index.html:5591-5638` — `boot()` and the module tag, post-inversion
  </read_first>
  <reversibility rating="reversible">Verification only; modifies no files.</reversibility>
  <action>
Drive Chrome via Chrome MCP against the local server and answer one question: does a bare identifier that now exists only inside a module resolve correctly inside classic-script code that was parsed before the module ran?

Confirm the server's working directory matches this worktree first (`curl` the page and check that `src/main.js` is fetchable and that the page's script tag count matches this tree) — a green result served from a sibling worktree proves nothing.

Load the page and collect, in the console: `window.__pp_module_ok`, `typeof firebase`, `typeof window.PP`, `typeof mulberry32`, `typeof rollStorm`, and `mulberry32 === window.PP.mulberry32`. The last one is the actual A1 proof — a bare identifier resolving to the same function object the module exported. Record the full console output, including any errors or warnings.

Then prove the *deferred call-site* half, which matters more than the typeof checks: start a solo game from the lobby so the classic script's `beginGame()` runs `new Game(cfg, seed, true)`, whose constructor calls bare `mulberry32`. A board that renders with ships placed is proof that a classic-script class body resolved a module-only symbol at call time.

Then prove the corpus-blind half. `rollStorm` is called from `Game.play()` (which the corpus covers) *and* from the classic live turn loop at `index.html:4762` and `:4785` (which the corpus cannot cover — `determinism_baseline.js` calls `g.play()`, which never sets `windNow2`). Force a storm using the method 07-03-SUMMARY.md documented, adapted for the bridge: set `game.cfg.storm = 1` using the bare `game` identifier (NOT `window.game` — `let game` at `index.html:1813` lives in classic script scope and never lands on `window`, and a `<div id="game">` exists that `window.game` resolves to instead), then override `rollStorm` with a replacement that still consumes exactly one `g.r()` call before returning true, sets `g.stormStreak = 1`, and returns true. The `g.r()` call is mandatory — omitting it shifts every subsequent draw. Because `rollStorm` is now a global-object property published by `Object.assign`, plain reassignment still works.

Play through at least two rounds with the storm active and confirm the second-gust narration fires (the `DIRNAME[game.windNow2]` line at `index.html:3941`), ships get pushed twice, and the console stays free of `ReferenceError`. That is the `rollStorm is not defined` / `PERP is not defined` failure class RESEARCH Pitfall 1 warns about, caught at the cheapest possible moment.

If any check fails, do not patch around it — stop and report. A failed A1 invalidates the design that plans 08-02 through 08-05 are built on, and the correct response is to replan the bridge mechanism, not to work around a symptom.

Safari is deliberately not exercised here (D-18). ROADMAP schedules Safari re-verification at Phase 11 (UI extraction, where storm rendering could regress) and Phase 12.
  </action>
  <acceptance_criteria>
1. The serving process's working directory is confirmed to be this worktree, and the port used is recorded in the SUMMARY.
2. `window.__pp_module_ok` evaluates to `true` (strict boolean, not truthy-coerced).
3. `typeof firebase` evaluates to `"object"`.
4. `typeof window.PP` evaluates to `"object"`.
5. `typeof mulberry32` evaluates to `"function"` when entered as a bare identifier.
6. `typeof rollStorm` evaluates to `"function"` when entered as a bare identifier.
7. `mulberry32 === window.PP.mulberry32` evaluates to `true`.
8. `rollStorm === window.PP.rollStorm` evaluates to `true`.
9. A solo game starts, the board renders, and four ships are visible — proving the `Game` constructor's bare `mulberry32` call resolved at runtime.
10. With the storm forced, at least two rounds are played and the second-gust narration referencing `game.windNow2` appears at least once.
11. The browser console contains zero entries matching `ReferenceError`, zero matching `is not defined`, and zero matching `Failed to load module`, from initial page load through the end of the storm rounds.
12. The console output for checks 2–8 is pasted verbatim into the plan SUMMARY as evidence, not paraphrased.
13. `git status --porcelain` shows no file modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(git status --porcelain | wc -l | tr -d ' ')" -eq 0 ] &amp;&amp; node scripts/determinism_baseline.js --verify</automated>
    <human-check>Chrome MCP console transcript showing `mulberry32 === window.PP.mulberry32` is `true`, a rendered solo board, and a storm round with second-gust narration and a clean console.</human-check>
  </verify>
  <done>Assumption A1 is proven empirically on this page: a module-only symbol resolves as a bare identifier inside classic-script code, at both an early call site (`Game` constructor) and a corpus-blind one (the live turn loop's `rollStorm`), with a clean console throughout. Plans 08-02 through 08-05 may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 7 golden corpus → Phase 8 code | The 30 committed `seed-*.jsonl` files and their frozen `manifest.perSeed[].sha256` values are the only independent evidence that moved code still behaves identically. Everything downstream trusts them. |
| Module realm → classic-script realm | `Object.assign(globalThis, PP)` publishes module-private values onto the global object, crossing from module scope into the shared page global namespace. |
| Repository → developer intent | Commit messages and diffs are the only record of whether a manifest change was a legitimate re-base or an oracle reset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `scripts/determinism_baseline.js --capture` vs. `scripts/fixtures/determinism/` | critical | mitigate | `--capture` is prohibited for the whole phase (D-01). Enforced mechanically by acceptance criteria 15–17: the 30 `.jsonl` files must still have exactly one commit in their entire git history, and `git status --porcelain scripts/fixtures/determinism/` must be empty. A `--capture` run makes both fail immediately. |
| T-08-02 | Tampering | `manifest.perSeed[].sha256` | critical | mitigate | Frozen (D-02). `manifest.json` must have exactly one commit in its history until 08-05's dedicated re-base, and that re-base commit's diff must be exactly one changed line — a `perSeed[]` edit shows up as a multi-line diff. |
| T-08-03 | Tampering | `verify()` in `scripts/determinism_baseline.js` | high | mitigate | The oracle can be neutralized by weakening the checker rather than the fixtures: dropping comparison 1 or 2, skipping a seed, adding an early return, or softening the exit code. Prohibited in `must_haves.prohibitions`; acceptance criterion 2 (`grep -c 'PASS seed'` equals 30) fails if any seed stops being compared. |
| T-08-04 | Repudiation | Commit granularity | medium | mitigate | Folding the manifest change into a code-motion commit makes an oracle reset read as noise. D-02/D-12: extraction + harness migration is one commit; the `engineSourceHash` re-base is its own clearly-messaged commit in 08-05. |
| T-08-05 | Tampering | `Object.assign(globalThis, PP)` | medium | mitigate | A bridged name colliding with an existing global would silently overwrite it. Verified before planning: all 47 candidate symbol names have exactly one declaration in `index.html`, and none matches any of the 97 element `id` attributes in the document. Re-asserted by 08-04's contract check. |
| T-08-06 | Information disclosure | `window.PP` global surface | low | accept | The bridge makes engine internals reachable from the browser console. Accepted: the entire game already executed as global-scope classic-script code before this phase, so the trust boundary is unchanged. No secrets, tokens, or credentials are exposed — the values are game constants, image paths, and pure functions. Phase 10/11 remove the bridge anyway. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable and asserted as such: this phase introduces zero packages. `package.json` has neither a `dependencies` nor a `devDependencies` key, and none is added. RESEARCH.md's Package Legitimacy Audit records the same finding. Any install task appearing during execution is out of scope and must halt the plan. |
</threat_model>

<verification>
- `node scripts/determinism_baseline.js --verify` exits 0 with 30/30 PASS and the `SOURCE: moved, behavior identical` classification.
- `npm test` exits 0.
- Chrome console proves bare-identifier resolution for both a shared-tier and an engine-tier symbol, with a rendered solo game and a storm round.
- The corpus fixtures are untouched: one commit in history, clean working tree.
</verification>

<success_criteria>
RESEARCH Assumption A1 moves from MEDIUM-HIGH to proven, on this page, in a real browser, for both a corpus-covered symbol and a corpus-blind one — at a cost of one commit and two moved functions, before any large-scale code motion depends on it.
</success_criteria>

<output>
Create `.planning/phases/08-engine-extraction-node-harness-migration/08-01-SUMMARY.md` when done. Include the verbatim Chrome console transcript, the port used, and the confirmed server cwd.
</output>
