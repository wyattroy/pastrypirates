---
phase: 08-engine-extraction-node-harness-migration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - index.html
  - src/shared/index.js
  - src/main.js
  - scripts/lib/load_engine.js
autonomous: true
requirements: [SPLIT-02, ENGINE-01, ENGINE-03, ENGINE-04]

must_haves:
  truths:
    - "Every shared constant and pure helper named by ROADMAP criterion 1 — ING_ALL, DIRS, man, shuffle-class helpers, mulberry32, and the image maps — lives in src/shared/index.js and is importable by Node with no DOM present."
    - "The three impurities at index.html:920, :922, :1002 no longer sit in engine-tier code; they live in applyEngineBootstrapEffects() in the classic script, together with the :996-:1001 comment block that explains :1002."
    - "index.html:2095's RECIPE_BOOK.forEach — a true classic-top-level statement referencing the moving ASSET_BASE — is wrapped in attachPastryArt() and invoked from src/main.js after the bridge exists."
    - "DIRS, DIRNAME, PERP, STORM_DIAG, OPPOSITE and TET each carry an ORDER IS LOAD-BEARING annotation and preserve their original key/element order exactly."
    - "node scripts/determinism_baseline.js --verify exits 0 across all 30 seeds after the shared tier has left index.html."
    - "The page still loads: window.__pp_module_ok is true, the lobby renders, and boot() runs exactly once despite document.body.innerHTML being rewritten from the module."
  prohibitions:
    - "scripts/determinism_baseline.js --capture is never run — not to refresh, not to fix a mismatch, not 'just for the source hash'. A red --verify means the code is wrong, not the fixture (D-01)."
    - "No per-seed sha256 in scripts/fixtures/determinism/manifest.json is edited, and no seed-*.jsonl file is modified, regenerated, deleted, or re-sorted (D-02)."
    - "verify() in scripts/determinism_baseline.js is not weakened — no seed skipped, no comparison removed, no early return added, no exit code softened, no tolerance introduced."
    - "No key in DIRS, DIRNAME, PERP, STORM_DIAG or OPPOSITE is re-ordered, and no element in TET is re-ordered — object literal order feeds this.r() and a reorder desyncs every seed from the constructor onward."
    - "index.html:1812 (const $) and index.html:1813 (let game=null,evIdx=0,timer=null,logLines=[]) do not move — they are UI/app state, not engine (D-07, A-01)."
    - "No moved line is renamed, reformatted, re-indented, re-ordered, minified, or 'tidied' while moving (REQUIREMENTS.md Out of Scope)."
    - "No tombstone or placeholder comment is left at a deletion site in index.html."
  artifacts:
    - src/shared/index.js (complete shared tier)
    - "index.html: applyEngineBootstrapEffects()"
    - "index.html: attachPastryArt()"
  key_links:
    - "src/main.js -> applyEngineBootstrapEffects() -> attachPastryArt() -> window.boot(), in that exact order."
    - "src/shared/index.js DIRS -> index.html Game constructor Object.values(DIRS) dock placement (RNG-indexed)."
    - "scripts/lib/load_engine.js -> src/shared/index.js: the vm sandbox is seeded with the whole shared tier."
---

<objective>
Move the entire shared leaf tier out of `index.html` into `src/shared/index.js`, verbatim, and fix the two parse-time hazards that the move creates. This satisfies SPLIT-02 and relocates the three ENGINE-01 impurities.

This is one atomic task, not two, because the pieces are inseparable: the moment `ASSET_BASE` leaves `index.html`, the top-level statement at `:2095` throws at parse time; the moment `CLOCK_IMG`/`FLIP_SOCKET_IMG`/`emojify` leave, the three impurities at `:920`/`:922`/`:1002` throw. A commit that does the move without the fixes is a commit with a dead page.

Purpose: get the leaves out first (D-04, dependency depth), so 08-03's `class Game` move has somewhere to import from.
Output: a complete `src/shared/index.js`, two new bootstrap functions in the classic script, and a green 30-seed corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-CONTEXT.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-RESEARCH.md
@.planning/phases/08-engine-extraction-node-harness-migration/08-01-SUMMARY.md
@docs/MODULES.md
@src/main.js
@src/shared/index.js
@scripts/lib/load_engine.js
</context>

<artifacts_this_phase_produces>
Full phase register is in `08-01-PLAN.md`. This plan produces:

- **`src/shared/index.js`** completed to its full export surface: `mulberry32` (from 08-01), `ING_ALL`, `ING_EMOJI`, `ASSET_BASE`, `ING_IMG`, `ING_HOLE_IMG`, `BOARD_IMG`, `DOCK_IMG`, `WIND_ARROW_IMG`, `TRADE_SWIRL_IMG`, `PLAY_IMG`, `PAUSE_IMG`, `FLIP_HEADS_IMG`, `FLIP_TAILS_IMG`, `CROWN_IMG`, `CRATE_OVERBOARD_IMG`, `COIN_IMG`, `FLAME_IMG`, `ANCHOR_IMG`, `BATTLE_IMG`, `HOURGLASS_IMG`, `ALARM_IMG`, `DAGGER_IMG`, `SKULL_IMG`, `SCROLL_IMG`, `DOOR_IMG`, `CLOCK_IMG`, `FLIP_SOCKET_IMG`, `COMPASS_DIAL_IMG`, `COMPASS_NEEDLE_IMG` and every other `*_IMG` constant declared between the `CROWN_IMG` line and the `PIRATE_FLAG_IMG` line, `EMOJI_IMG`, `EMOJIFY_RE`, `emojify`, `BOAT_IMG`, `ISLAND_SHAPE_IMG`, `TET`, `ING_NAME`, `ING_PLAIN`, `DOCK_PLACE`, `DOCK_FLAVOR`, `dockPlace`, `dockFlavor`, `iname`, `ilabel`, `ingImg`, `ilabelImg`, `iconImg`, `DIRS`, `DIRNAME`, `PERP`, `STORM_DIAG`, `OPPOSITE`, `SAIL_BUDGET`, `SAIL_BUDGET_LEEWARD`, `windStepCost`, `NAMES`, `DEFAULT_NAMES`, `unusedDefaultName`, `COLORS`, `HEXCOL`, `man`.
- **`index.html`: `applyEngineBootstrapEffects()`** — a classic-script `function` declaration (so `window.applyEngineBootstrapEffects` exists automatically) holding the three relocated D-06 impurities.
- **`index.html`: `attachPastryArt()`** — a classic-script `function` declaration wrapping the `RECIPE_BOOK.forEach(...)` statement.
- **`window.__pp_boot_count`** — a new standing browser tripwire (same convention as Phase 7's `window.__pp_module_ok`), incremented in `src/main.js` immediately before `window.boot()`. Exists because moving the `document.body.innerHTML` rewrite out of parse time is the one genuinely behavior-adjacent change in this plan; a double-boot would otherwise be silent.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move the shared leaf tier out verbatim, relocate the three impurities, defuse the ASSET_BASE parse hazard</name>
  <files>src/shared/index.js, index.html, src/main.js, scripts/lib/load_engine.js</files>
  <read_first>
    - `index.html` from the `const ING_ALL=` line through the `const man=` line — the full shared region. Read it in one pass; note that lines originally numbered `:861-862` and `:1016-1023` were already removed by 08-01, so every number below has shifted upward and you must locate by symbol, not by number.
    - `index.html` around the `document.documentElement.style.setProperty("--clock-img"` line, the `--flip-socket-img` line, and the six-line comment block ending in `document.body.innerHTML = emojify(document.body.innerHTML);` — the three D-06 impurities and the prose that explains the third
    - `index.html` around `RECIPE_BOOK.forEach((r,i)=>r.img=` and the `for(const r of RECIPE_BOOK)RECIPE_LOOKUP[...]` line directly below it
    - `index.html` around `/* ================= UI ================= */`, `const $=id=>document.getElementById(id);`, and `let game=null,evIdx=0,timer=null,logLines=[];` — the boundary that must NOT move
    - `src/shared/index.js` and `src/main.js` as written by 08-01
    - `scripts/lib/load_engine.js` as written by 08-01 — the hybrid sandbox seeding
    - `.planning/phases/08-engine-extraction-node-harness-migration/08-RESEARCH.md` §"Order-Load-Bearing Constants — Full Inventory" and §"Pattern 2"
  </read_first>
  <reversibility rating="costly">The largest single verbatim code motion of the milestone. Git-revertible, but redoing it costs a full re-execution; hence the corpus gate on the same commit rather than at phase end.</reversibility>
  <action>
**A. Append the shared tier to `src/shared/index.js`, verbatim, in original source order.**

Append after the existing `mulberry32` block and before the trailing `export` statement, preserving the source's own top-to-bottom order (RESEARCH Q4 confirms the monolith's order already satisfies leaves-first dependency depth, so no statement needs reordering — this is D-04's leaves-first split landing with zero statement reordering).

The image-path maps travel with this tier per D-05: they are pure string data with no DOM access, so the engine may safely import them. What is *not* safe is *applying* them to the DOM — that is §C's job.

<!-- planner-discipline-allow: const ING_ALL= -->
<!-- planner-discipline-allow: const DIRS= -->
<!-- planner-discipline-allow: ^RECIPE_BOOK\.forEach -->


1. The `/* ================= Engine (port of cocoa_pirates_sim.py) ================= */` banner and everything from `const ING_ALL=` through the `PIRATE_FLAG_IMG=...;` line and `const CLOCK_IMG=...;`.
2. Skip the `--clock-img` `setProperty` line — it goes to §B.
3. `const FLIP_SOCKET_IMG=...;`.
4. Skip the `--flip-socket-img` `setProperty` line — §B.
5. Everything from `const COMPASS_DIAL_IMG=` through `const iconImg=src=>...;` — this includes the `EMOJI_IMG` table, `EMOJIFY_RE`, `emojify`, `BOAT_IMG`, `ISLAND_SHAPE_IMG`, `TET`, the `ING_NAME`/`ING_PLAIN`/`DOCK_PLACE`/`DOCK_FLAVOR` tables and the small label helpers, with all their interleaved comments. `EMOJIFY_RE` executes `Object.keys(EMOJI_IMG).sort(...)` at module top level, so it must stay below `EMOJI_IMG` — keeping original order handles this by construction.
6. Skip the six-line comment block beginning `// One-time sweep of the static page markup` through `document.body.innerHTML = emojify(document.body.innerHTML);` — comment and statement both go to §B as a unit.
7. Everything from `const DIRS=` through `const windStepCost=...;`.
8. Everything from `const NAMES=` through `const man=...;` — `NAMES`, `DEFAULT_NAMES`, `unusedDefaultName`, `COLORS`, `HEXCOL`, `man` and their comments. Stop before the `// Bot decision weights` comment block; that and everything below it is engine tier and moves in 08-03.

Extend the trailing `export { ... }` block to name every symbol listed in this plan's artifacts section. Keep using a trailing export block rather than `export ` prefixes so every moved line stays byte-identical to its source — 08-04 audits exactly that.

**B. Add `ORDER IS LOAD-BEARING` annotations (D-09 for the five direction tables, D-10 for `TET` — the additional literal RESEARCH's inventory identified as order-reaching).** Insert a comment line immediately above each of `DIRS`, `DIRNAME`, `PERP`, `STORM_DIAG`, `OPPOSITE`, and `TET`. Every annotation starts with the exact token `// ORDER IS LOAD-BEARING` so a count grep works, followed by an em-dash and a construct-specific reason.

The count gate in acceptance criterion 5 is exact, so the token must appear **only** on those six annotation lines — do not repeat it in the file's header comment, in prose, or in the export block, or the count reads 7 and the gate fails on documentation rather than on a real defect. Per-construct reasons:
- `DIRS` — its `Object.values` iteration order builds the candidate dock-cell array that the constructor then indexes with an `this.r()`-derived index; a reorder changes every dock position for an identical RNG draw, and also flips seat-spawn assignment and Dijkstra tie-breaks.
- `DIRNAME`, `OPPOSITE` — parallel tables keyed to `DIRS`; they must stay in lockstep with it.
- `PERP`, `STORM_DIAG` — consumed only by the classic live turn loop, where `PERP[windNow][Math.floor(game.r()*2)]` indexes directly by RNG draw. The headless corpus cannot catch a reorder here; note that in the annotation.
- `TET` — indexed by `Math.floor(this.r()*TET.length)` for shape selection, and separately by `ISLAND_SHAPE_IMG` for art placement, so a reorder breaks both RNG parity and rendering.

**C. Relocate the three D-06 impurities into `index.html`.** These are exactly the three CONTEXT.md enumerates — the two `documentElement.style.setProperty` calls and the body-innerHTML emoji sweep — and relocating them is what makes the D-08 purity grep in acceptance criterion 10 return zero hits rather than three. Create a `function applyEngineBootstrapEffects(){ ... }` declaration in the classic script, placed just above `function boot(){`. Move into it, verbatim and in original order: the `--clock-img` `setProperty` line, the `--flip-socket-img` `setProperty` line, the six-line `// One-time sweep of the static page markup ...` comment block, and the `document.body.innerHTML = emojify(...)` statement. The comment block travels with the statement it explains — leaving it behind in `src/shared/index.js` would both orphan the prose and plant a false positive in the purity scan (RESEARCH Q5 documents this exact hit). Declare it as a `function`, not a `const` arrow, so it lands on `window` automatically with no bridge entry.

**D. Defuse the `ASSET_BASE` parse hazard.** Wrap the `RECIPE_BOOK.forEach((r,i)=>r.img=...)` statement in `function attachPastryArt(){ ... }`, declared in place. Leave the `for(const r of RECIPE_BOOK)RECIPE_LOOKUP[...]` line directly below it at top level — it references no moving symbol. Deferring the `.img` assignment is safe: `RECIPE_BOOK[i].img` is read only inside `recipeCardHTML` and `recipeModalHTML`, both of which run on demand (RESEARCH Assumption A2).

**E. Delete the moved lines from `index.html`.** No tombstones, no "moved to src/shared" notes, no extra blank lines. The `/* ================= UI ================= */` banner, `const $=id=>document.getElementById(id);`, and `let game=null,evIdx=0,timer=null,logLines=[];` all stay exactly where they are — the first two per D-07, the third per this phase's assumption A-01 (it is app state the UI assigns into at `beginGame`, and 07-03-SUMMARY.md records that it deliberately lives in classic script scope, off `window`).

**F. Wire the call order in `src/main.js`.** Inside the existing browser guard, after the bridge is populated, call in this exact sequence: `window.applyEngineBootstrapEffects()`, then `window.attachPastryArt()`, then increment `window.__pp_boot_count` (initialising it to 0 if unset), then `window.boot()`.

The ordering is load-bearing and the original code says why: the relocated comment states the body rewrite must happen "before any element-lookup/event-wiring below runs". `boot()` is where that wiring happens (`wireWelcome`, `wireLobby`, `wireRecipeModal`), so the rewrite must precede it. Add a comment at the call site recording that this ordering preserves that invariant.

Note the one genuinely behavior-adjacent consequence and state it in the SUMMARY: the body rewrite previously executed mid-parse, when only the markup above the classic `<script>` had been parsed; it now executes after the full document is parsed, so it re-serialises and re-parses the whole body including the `<script>` elements. Script elements inserted via `innerHTML` are marked non-executable by the HTML parser and do not re-run, so `src/main.js` must not execute twice — `window.__pp_boot_count` exists to prove that rather than assume it.

**G. Update `scripts/lib/load_engine.js`.** The hybrid sandbox seeding from 08-01 already spreads the shared namespace; confirm it now covers the whole shared tier and that the sliced region still resolves. Do not change `loadEngine()`'s signature or return shape.
  </action>
  <acceptance_criteria>
Run from the repo root. `wc -l` is piped through `tr -d ' '` for BSD `wc` (07-01-SUMMARY.md). Criterion 1 is D-16's primary gate and must be green in this same commit — not deferred to phase end.

1. `node scripts/determinism_baseline.js --verify; echo "exit=$?"` → prints `exit=0` and `All seeds passed.`
2. `node scripts/determinism_baseline.js --verify | grep -c 'PASS seed'` → `30`
3. `npm test; echo "exit=$?"` → prints `exit=0`
4. `node scripts/real_game_test.js 25; echo "exit=$?"` → prints `exit=0`
5. `[ "$(grep -c 'ORDER IS LOAD-BEARING' src/shared/index.js)" -eq 6 ]` → exit 0
6. `for s in DIRS DIRNAME PERP STORM_DIAG OPPOSITE TET; do grep -B1 "^const $s=\|^const $s\b" src/shared/index.js | grep -q 'ORDER IS LOAD-BEARING' || { echo "MISSING $s"; exit 1; }; done; echo ok` → prints `ok`
7. `grep -q 'N:\[0,-1\],S:\[0,1\],E:\[1,0\],W:\[-1,0\]' src/shared/index.js; echo "exit=$?"` → prints `exit=0` (DIRS key order byte-preserved)
8. `grep -q 'N:"north",S:"south",E:"east",W:"west"' src/shared/index.js; echo "exit=$?"` → prints `exit=0`
9. `grep -q 'N:"S",S:"N",E:"W",W:"E"' src/shared/index.js; echo "exit=$?"` → prints `exit=0`
10. `sed 's://.*::' src/shared/index.js | grep -nE 'document\.[A-Za-z]+|window\.[A-Za-z]+|\bfirebase\b|localStorage|Date\.now|Math\.random|\bglobalThis\b|new Function'; echo "exit=$?"` → prints `exit=1` (no matches after comment stripping)
11. `[ "$(grep -c 'function applyEngineBootstrapEffects' index.html)" -eq 1 ]` → exit 0
12. `[ "$(grep -c 'function attachPastryArt' index.html)" -eq 1 ]` → exit 0
13. `[ "$(grep -c 'setProperty("--clock-img"' index.html)" -eq 1 ]` and `[ "$(grep -c 'setProperty("--flip-socket-img"' index.html)" -eq 1 ]` and `[ "$(grep -c 'document.body.innerHTML = emojify' index.html)" -eq 1 ]` → all exit 0
14. `[ "$(grep -c 'const \$=id=>document.getElementById(id);' index.html)" -eq 1 ]` → exit 0 (D-07: `$` did not move)
15. `[ "$(grep -c 'let game=null,evIdx=0,timer=null,logLines=\[\];' index.html)" -eq 1 ]` → exit 0 (A-01: app state did not move)
16. `grep -q 'const ING_ALL=' index.html; echo "exit=$?"` → prints `exit=1`
17. `grep -q 'const DIRS=' index.html; echo "exit=$?"` → prints `exit=1`
18. `grep -q 'const ASSET_BASE=' index.html; echo "exit=$?"` → prints `exit=1`
19. `grep -qE '^RECIPE_BOOK\.forEach' index.html; echo "exit=$?"` → prints `exit=1` (the statement is no longer at top level)
20. `[ "$(grep -c '<script>' index.html)" -eq 1 ]` → exit 0
21. `node --input-type=module -e "import('./src/shared/index.js').then(m=>{const need=['ING_ALL','DIRS','DIRNAME','PERP','STORM_DIAG','OPPOSITE','TET','man','mulberry32','ASSET_BASE','ING_IMG','EMOJI_IMG','emojify','windStepCost','HEXCOL','NAMES','unusedDefaultName','iconImg','ilabelImg','ingImg','iname','ilabel','dockPlace','dockFlavor','BOAT_IMG','ISLAND_SHAPE_IMG','SAIL_BUDGET','SAIL_BUDGET_LEEWARD'];const miss=need.filter(k=>!(k in m));if(miss.length){console.error('MISSING',miss);process.exit(1)}})"; echo "exit=$?"` → prints `exit=0`. This runs under plain Node with no DOM, so it also proves ENGINE-01 purity behaviourally, not just by grep.
22. `[ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" -eq 1 ]` → exit 0
23. `[ "$(git log --oneline -- scripts/fixtures/determinism/manifest.json | wc -l | tr -d ' ')" -eq 1 ]` → exit 0
24. `git status --porcelain scripts/fixtures/determinism/` → empty output
  </acceptance_criteria>
  <verify>
    <automated>node scripts/determinism_baseline.js --verify &amp;&amp; npm test &amp;&amp; [ "$(grep -c 'ORDER IS LOAD-BEARING' src/shared/index.js)" -eq 6 ] &amp;&amp; { sed 's://.*::' src/shared/index.js | grep -qE 'document\.[A-Za-z]+|window\.[A-Za-z]+|\bfirebase\b|localStorage|Date\.now|Math\.random|\bglobalThis\b|new Function' &amp;&amp; exit 1 || true; } &amp;&amp; [ "$(git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l | tr -d ' ')" -eq 1 ]</automated>
  </verify>
  <done>`src/shared/index.js` holds the complete shared leaf tier verbatim with six order-load-bearing annotations, `index.html` declares `applyEngineBootstrapEffects()` and `attachPastryArt()` and no longer declares any shared constant, `src/main.js` calls the three bootstrap steps in the documented order, and all 30 seeds verify green with the corpus untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Browser load smoke check — the page still boots, exactly once, after the body rewrite moved out of parse time</name>
  <files>none — verification only; this task modifies no files</files>
  <precondition>A static HTTP server is serving THIS worktree; confirm the serving process's cwd matches before trusting any result (07-03-SUMMARY.md records stale sibling-worktree servers returning healthy 200s from the wrong tree).</precondition>
  <read_first>
    - `.planning/phases/08-engine-extraction-node-harness-migration/08-01-SUMMARY.md` — the port and server-cwd confirmation method that worked
    - `src/main.js` as written by Task 1 — the bootstrap call order
    - `index.html` `applyEngineBootstrapEffects()` and `boot()` as written by Task 1
  </read_first>
  <reversibility rating="reversible">Verification only; modifies no files.</reversibility>
  <action>
This is the check the 30-seed corpus structurally cannot perform. The corpus runs `Game.play()` in Node; it would stay green even if the page never rendered a pixel. Task 1 moved the `document.body.innerHTML` rewrite from parse time to module time, which is the one change in this plan that could plausibly break page load while leaving the corpus green.

Drive Chrome via Chrome MCP. Confirm the server's cwd first. Load the page and collect: `window.__pp_module_ok`, `typeof firebase`, `window.__pp_boot_count`, `typeof window.PP`, `Object.keys(window.PP).length`, and `document.querySelectorAll('script[src="src/main.js"]').length`.

Then confirm the visual and interactive surface the body rewrite could have destroyed: the lobby renders, the emoji-to-image swap actually happened (the static markup shows custom art, not raw emoji — that is `applyEngineBootstrapEffects()` doing its job at the new time), the CSS custom properties `--clock-img` and `--flip-socket-img` are set on `document.documentElement`, a lobby button responds to exactly one click (double-wiring from a double boot would fire it twice), and the recipe modal opens with pastry art present (proving `attachPastryArt()` ran).

Do not fix forward if something fails. Report the failure with the console transcript — a broken bootstrap ordering is a design issue to correct in Task 1's commit, not to patch around in a later plan.

Safari is not exercised (D-18); ROADMAP schedules it at Phases 11 and 12.
  </action>
  <acceptance_criteria>
1. The serving process's cwd is confirmed to be this worktree and the port is recorded in the SUMMARY.
2. `window.__pp_module_ok` evaluates to `true`.
3. `typeof firebase` evaluates to `"object"`.
4. `window.__pp_boot_count` evaluates to `1` — not `2`, not `undefined`.
5. `document.querySelectorAll('script[src="src/main.js"]').length` evaluates to `1`.
6. `typeof window.PP` evaluates to `"object"` and `Object.keys(window.PP).length` is at least `50`; the exact count is recorded in the SUMMARY as the baseline 08-03 will grow.
7. `getComputedStyle(document.documentElement).getPropertyValue('--clock-img')` is a non-empty string containing `clock.png`, and the same check for `--flip-socket-img` contains `flip-socket.png`.
8. The lobby is visible and the static markup shows custom `<img>` art rather than raw emoji characters, confirmed by a screenshot plus a DOM query showing at least one `img.narrIcon` or equivalent swapped element in static markup.
9. Opening the recipe modal shows pastry artwork, and at least one `RECIPE_BOOK` entry evaluated in the console has a non-empty `.img` string.
10. A single click on a lobby control produces exactly one response (no duplicated modal, no doubled handler effect).
11. The browser console contains zero entries matching `ReferenceError`, zero matching `is not defined`, and zero matching `Failed to load module`, from page load through the recipe-modal check.
12. The console output for checks 2–7 is pasted verbatim into the SUMMARY.
13. `git status --porcelain` shows no file modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(git status --porcelain | wc -l | tr -d ' ')" -eq 0 ] &amp;&amp; node scripts/determinism_baseline.js --verify</automated>
    <human-check>Chrome MCP transcript showing `__pp_boot_count === 1`, both CSS custom properties set, emoji art swapped in static markup, recipe modal art present, and a clean console.</human-check>
  </verify>
  <done>The page loads and boots exactly once with the shared tier living outside `index.html`, the relocated bootstrap effects observably applied, and a clean console.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 7 golden corpus → moved shared tier | The frozen per-seed hashes are the only evidence that moving ~180 lines of constants changed nothing. |
| Module realm → classic-script realm | `Object.assign(globalThis, PP)` now carries ~50 more names across the boundary. |
| Parse-time DOM → module-time DOM | `document.body.innerHTML` rewriting moved from mid-parse to post-parse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `--capture` vs. `scripts/fixtures/determinism/` | critical | mitigate | Prohibited (D-01). Acceptance criteria 22–24 fail immediately if the corpus files gain a commit or the working tree dirties. |
| T-08-02 | Tampering | `manifest.perSeed[].sha256` | critical | mitigate | Frozen (D-02); criterion 23 asserts `manifest.json` still has exactly one commit in its history at this point in the phase. |
| T-08-07 | Tampering | `DIRS`/`DIRNAME`/`PERP`/`STORM_DIAG`/`OPPOSITE`/`TET` key order | critical | mitigate | The single highest-impact desync site in the milestone: `Object.values(DIRS)` builds the candidate dock array that the constructor indexes by `this.r()`, so a reorder desyncs all 30 seeds from the first event. Mitigated three ways — verbatim move discipline, `ORDER IS LOAD-BEARING` annotations (criteria 5–6), and literal byte-order greps on the three tables whose order is not otherwise self-evident (criteria 7–9) — on top of the corpus gate. |
| T-08-08 | Denial of service | `document.body.innerHTML` rewrite timing | high | mitigate | Moving the rewrite past full-document parse could double-execute `src/main.js` (double boot, double-wired handlers) or destroy already-wired listeners. Mitigated by the `window.__pp_boot_count` tripwire, the `script[src="src/main.js"]` count, and the single-click check (Task 2 criteria 4, 5, 10). |
| T-08-03 | Tampering | `verify()` in `determinism_baseline.js` | high | mitigate | Prohibited in `must_haves.prohibitions`; criterion 2 (`grep -c 'PASS seed'` equals 30) fails if any seed stops being compared. |
| T-08-05 | Tampering | `Object.assign(globalThis, PP)` name collisions | medium | mitigate | Verified before planning: each of the 47 candidate symbols has exactly one declaration in `index.html`, and none collides with any of the document's 97 element `id` attributes. Re-asserted mechanically by 08-04's contract check. |
| T-08-06 | Information disclosure | `window.PP` surface | low | accept | Game constants and pure functions only; the page already ran entirely as global classic-script code. No secrets or credentials. Bridge is deleted in Phase 11. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages introduced; `package.json` has neither a `dependencies` nor a `devDependencies` key and none is added. An install task appearing during execution is out of scope and must halt the plan. |
</threat_model>

<verification>
- 30/30 seeds green on `node scripts/determinism_baseline.js --verify`, with the corpus files untouched and still one commit deep.
- Comment-stripped purity scan over `src/shared/index.js` returns no matches.
- Six `ORDER IS LOAD-BEARING` annotations, each attached to the right construct, with three literal key-order greps as a second line of defence.
- Chrome: `__pp_module_ok === true`, `__pp_boot_count === 1`, both CSS custom properties set, emoji art swapped, recipe art attached, clean console.
</verification>

<success_criteria>
SPLIT-02 is satisfied — the shared constants and pure helpers ROADMAP criterion 1 names live in a leaf module importable by Node with no DOM — and the three ENGINE-01 impurities plus the one parse-time `ASSET_BASE` hazard are relocated, with byte-for-byte seeded output preserved.
</success_criteria>

<output>
Create `.planning/phases/08-engine-extraction-node-harness-migration/08-02-SUMMARY.md` when done. Record the `Object.keys(window.PP).length` baseline, the port used, and an explicit note on the body-rewrite timing change and how `__pp_boot_count` proved it safe.
</output>
