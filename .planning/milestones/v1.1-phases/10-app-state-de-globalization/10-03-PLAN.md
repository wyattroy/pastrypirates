---
phase: 10-app-state-de-globalization
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - index.html
autonomous: true
requirements: [GLOBAL-01]
user_setup: []

must_haves:
  truths:
    - "The 9 net-consumed identity/session names (db, myId, mySeat, isHost, roster, turnOrder, numSeats, passAndPlay, soloMeta) are migrated to `state.NAME` at every read AND write site in index.html:859-4667 (D-07)."
    - "Every `src/net/` call site (the ~30 netWatch*/netWrite*/netRead* invocations) now passes the CURRENT value at call time (`netWatchFlip(state.db, state.room, ...)`), so transport reads live state, not a stale snapshot (D-07)."
    - "`src/net/*.js` remains free of app-state references - net_contract_check.js still passes (net receives plain arguments only)."
    - "The determinism corpus stays 30/30 on `--verify` (D-08)."
  prohibitions:
    - "MIGRATION-CORRUPTION: no string-literal or comment body changes (byte-safety diff empty); Firebase path strings like `rooms/{room}/...` live in src/net and are NOT touched here."
    - "MISSED-WRITE-SITE: zero bare (non-`state.`) occurrences of the 9 names remain in the region; a missed `isHost`/`mySeat` write desyncs seat/host authority (isHost has 10 write sites)."
    - "STALE-ARG: no net call site may keep reading a bare `db`/`myId` that no longer receives writes."
    - "NEVER-CAPTURE: no `--capture`."
  artifacts: []
  key_links:
    - "index.html net call sites -> src/net/ transport functions via plain arguments read at call time; `state.db`/`state.mySeat`/`state.isHost` must resolve current values."
---

<objective>
Migrate the identity/session state that `src/net/` consumes: `db, myId, mySeat, isHost, roster, turnOrder, numSeats, passAndPlay, soloMeta`. The critical property is call-site freshness - Phase 9 deliberately passes these into `netWatchX(db, room, ...)` as plain arguments read at call time, and that pattern stays correct only if every call site is migrated to read `state.db`/`state.room`/etc. at the moment of call.

Purpose: D-07 - these five (db/myId/room/mySeat/isHost) are consumed pervasively by the extracted networking module's call sites. `room` was migrated in the tracer; this plan finishes the set and proves the ~30 net call sites resolve current values, not stale bare bindings that no longer get reassigned.

Output: the 9 names migrated, all net call sites reading live state, net_contract_check.js still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-app-state-de-globalization/10-RESEARCH.md
@scripts/net_contract_check.js
@docs/MODULES.md
@.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the net-consumed identity/session names and prove call-site freshness</name>
  <files>index.html</files>
  <read_first>
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` "Net call-site freshness (Q3)" (the enumerated ~30 call sites: index.html 2116, 2202, 2278, 2545, 3068, 3881, 3928, 3934, 3940, 4071, 4092, 4094, 4114, 4116, 4120, 4142, 4154, 4209, 4218, 4357, 4379, 4383, 4388, 4391, 4420, 4460, 4471, 4543, 4546, 4652 - re-confirm by grep), and inventory rows 5-6, 8-12, 17, 44 (write sites: db 3940; myId 4623; mySeat 3983/4255/4266/4279/4318/4330/4342/4364/4376/4651; isHost 3983/4255/4266/4342/4346/4364/4376/4381/4651/4655; roster 3986/4256/4267/4278/4385/4392/4532; turnOrder 3790/4439/4462; passAndPlay 3987/4266; soloMeta 3981/3986/4258/4269).
    - `scripts/net_contract_check.js` assertion 3 (the app-state denylist inside src/net/) - confirm this plan does not add any app-state reference INSIDE src/net/ (we only touch index.html call-site arguments).
    - `docs/MODULES.md` "What deliberately did not move into src/net/" (the `db` handle stays a classic-script global; this plan de-globalizes it into `state.db`).
  </read_first>
  <action>
    Run `node scripts/migrate_app_state.js --migrate db,myId,mySeat,isHost,roster,turnOrder,numSeats,passAndPlay,soloMeta`. The tool rewrites only identifier-position occurrences in the classic region - including the net call-site arguments, which become `netWatchFlip(state.db, state.room, ...)` (with `state.room` already migrated by the tracer).
    Review the diff. Confirm every one of the ~30 enumerated net call sites now reads `state.db`/`state.mySeat`/`state.isHost`/etc. as arguments, so transport reads the current value at call time. Confirm the isHost write cluster (10 sites) and the mySeat write cluster (10 sites) are all `state.`-qualified - a single missed write here silently desyncs host authority or seat identity.
    Do NOT edit any file under src/net/. The Firebase path strings (`"rooms/"+room+...`) live in src/net and receive `room` as an argument; they are untouched by this migration because they are string content inside src/net, not classic-script identifiers.
    NEVER run `--capture`.
  </action>
  <verify>
    <automated>node scripts/migrate_app_state.js --check-names db,myId,mySeat,isHost,roster,turnOrder,numSeats,passAndPlay,soloMeta && diff <(node scripts/migrate_app_state.js --extract-strings HEAD:index.html) <(node scripts/migrate_app_state.js --extract-strings index.html) && node scripts/net_contract_check.js && node scripts/determinism_baseline.js --verify</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/migrate_app_state.js --check-names db,myId,mySeat,isHost,roster,turnOrder,numSeats,passAndPlay,soloMeta` exits 0.
    - The `--extract-strings` diff (HEAD vs working tree) is EMPTY.
    - `node scripts/net_contract_check.js` exits 0 (5/5 PASS - src/net/ still has zero app-state references; unchanged by this plan).
    - `node scripts/determinism_baseline.js --verify` exits 0 (30/30).
    - `grep -cE "netWatch[A-Za-z]+\(db\b|netWrite[A-Za-z]+\(db\b|netRead[A-Za-z]+\(db\b" index.html` returns 0 (no net call site passes a bare `db` argument anymore).
    - `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1.
  </acceptance_criteria>
  <done>The identity/session names are `state.`-qualified everywhere; all net call sites pass live `state.*` values; net_contract_check and corpus both green; strings/comments byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html call site -> src/net/ | Identity/session values cross into transport as plain arguments; a stale bare binding would send the wrong room/seat/host to Firebase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | a missed isHost/mySeat write desyncing host authority or seat identity | high | mitigate | `--check-names` exit 0 for all 9 names; diff review of the 10-site isHost and 10-site mySeat write clusters. |
| T-10-05 | Tampering | a net call site left reading a bare (now-orphaned) db/myId | high | mitigate | Grep asserts zero `netWatch*(db,...)` bare-argument call sites; diff review of all ~30 enumerated sites. |
| T-10-01 | Tampering | a Firebase path string or comment corrupted | high | mitigate | Tokenizer skips strings/comments; src/net/ untouched; `--extract-strings` diff empty; net_contract_check green. |
</threat_model>

<verification>
- `--check-names` for the 9 names exits 0.
- `--extract-strings` diff empty.
- `node scripts/net_contract_check.js` -> 5/5 PASS.
- `node scripts/determinism_baseline.js --verify` -> 30/30.
- No bare-`db` net call-site arguments remain.
</verification>

<success_criteria>
Identity/session state is de-globalized with net call-site freshness proven, net_contract_check and corpus green, and no string/comment content changed.
</success_criteria>

<output>
Create `.planning/phases/10-app-state-de-globalization/10-03-SUMMARY.md` when done.
</output>
