---
phase: 10-app-state-de-globalization
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - index.html
autonomous: true
requirements: [GLOBAL-01]
user_setup: []

must_haves:
  truths:
    - "The 7 replay/resume control-flow names (replaying, dlog, dlogIdx, dlogN, evIdx, resumeEvLen, resumeReadFailed) are migrated to `state.NAME` at every read AND write site in index.html:859-4667 (D-06)."
    - "The migration preserves read/write ORDERING and timing of the replay fast-forward - postfix `++` on a `state.` property behaves identically to postfix `++` on a bare variable (D-06)."
    - "The determinism corpus stays 30/30 on `--verify` AND `dlog_replay_test.js` still passes after the migration (D-06/D-08)."
  prohibitions:
    - "NO-REORDER: no read/write of the replay counters may be reordered, deferred, batched, or wrapped in an accessor - the fast-forward reads and mutates these in a precise order (D-06)."
    - "MIGRATION-CORRUPTION: no string-literal or comment body changes (byte-safety diff must be empty)."
    - "MISSED-WRITE-SITE: zero bare (non-`state.`) occurrences of the 7 names remain in the region after this task."
    - "NEVER-CAPTURE: no `--capture`; corpus stays 1 commit deep (D-08)."
  artifacts: []
  key_links:
    - "The `replaying` guard gates render/broadcast during fast-forward; `state.replaying` must be read at exactly the same points."
---

<objective>
Migrate the highest-determinism-risk subset first: the replay/resume control-flow state. These names are read and mutated in a precise order during the recorded-log fast-forward after a reload; the migration mechanism (plain property access) is order-preserving by the JS spec, but this subset gets its own plan so the corpus AND the native replay test both gate it in isolation.

Purpose: D-06 flags these as replay-load-bearing - the same class of risk as Phase 8's iteration-order constants, but for control flow. Migrating them alone, verified against both the corpus and the dlog replay harness, isolates any ordering regression before it is buried under 39 more names.

Output: `replaying, dlog, dlogIdx, dlogN, evIdx, resumeEvLen, resumeReadFailed` migrated to `state.`, corpus + replay both green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-app-state-de-globalization/10-RESEARCH.md
@.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the replay/resume control-flow names</name>
  <files>index.html</files>
  <read_first>
    - `.planning/phases/10-app-state-de-globalization/10-RESEARCH.md` Pattern 1 (the postfix-`++` before/after example: `state.dlogN++`, `state.dlog[state.dlogIdx++]`), Common Pitfalls 3 (no accessors/Proxy), inventory rows 2, 38-43 (write sites: replaying 3990/4015/4550; dlog 3968/3989/4258/4269/4438/4544; dlogIdx 2654/2866/3090/3742/3989/4438/4545; dlogN 2654/2866/3090/3742/3962/3989/4438/4545; evIdx 2382/4144/4283/4436; resumeEvLen 4547; resumeReadFailed 4542/4543/4546 - re-confirm by grep, line numbers drift).
    - `.planning/phases/10-app-state-de-globalization/10-01-SUMMARY.md` - the migration-tool usage and byte-safety diff procedure established by the tracer.
  </read_first>
  <action>
    Run `node scripts/migrate_app_state.js --migrate replaying,dlog,dlogIdx,dlogN,evIdx,resumeEvLen,resumeReadFailed`. The tool (built in 10-01) rewrites only identifier-position occurrences inside the classic region, never inside strings/comments, never a name already prefixed by `state.`.
    Review the diff line-by-line with specific attention to the fast-forward decision resolvers (around the dlog-replay sites) - confirm each postfix `++` migrated verbatim to a `state.`-qualified property increment and that no read/write was reordered, split, or moved relative to its neighbours. The ONLY permitted change per line is inserting the state qualifier before the identifier; assert no operator, guard condition, or statement order changed.
    Do not touch any of the other 39 names, and do not touch the 7 render handles. NEVER run `--capture`.
  </action>
  <verify>
    <automated>node scripts/migrate_app_state.js --check-names replaying,dlog,dlogIdx,dlogN,evIdx,resumeEvLen,resumeReadFailed && diff <(node scripts/migrate_app_state.js --extract-strings HEAD:index.html) <(node scripts/migrate_app_state.js --extract-strings index.html) && node scripts/determinism_baseline.js --verify && node scripts/dlog_replay_test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/migrate_app_state.js --check-names replaying,dlog,dlogIdx,dlogN,evIdx,resumeEvLen,resumeReadFailed` exits 0 (zero bare occurrences remain in the region).
    - The `--extract-strings` diff between `HEAD:index.html` and the working tree is EMPTY.
    - `node scripts/determinism_baseline.js --verify` exits 0 (corpus 30/30).
    - `node scripts/dlog_replay_test.js` exits 0.
    - `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1.
    - The diff touches ONLY lines that reference these 7 names, and each changed line differs from HEAD by inserted `state.` qualifiers only (no reordered statements): reviewed and recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The replay/resume control-flow names are fully `state.`-qualified with ordering preserved; corpus and native replay test both green; strings/comments byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokenizer -> index.html | Rewriting the replay fast-forward path; a reordered read/write silently corrupts a replay rebuild. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | replay counters (dlogIdx/dlogN/evIdx) reordered or wrapped in an accessor | high | mitigate | Identifier-only rewrite; line-by-line diff review asserting no reorder; corpus `--verify` + `dlog_replay_test.js` both gate it. |
| T-10-01 | Tampering | a bare replay word inside a string/comment rewritten | high | mitigate | Tokenizer skips string/comment regions; `--extract-strings` diff must be empty. |
| T-10-02 | Tampering | a missed reassignment site desyncing replay state | high | mitigate | `--check-names` reports any remaining bare usage; exit 0 required. |
</threat_model>

<verification>
- `--check-names` for the 7 names exits 0.
- `--extract-strings` diff empty (byte-safety).
- `node scripts/determinism_baseline.js --verify` -> 30/30.
- `node scripts/dlog_replay_test.js` -> pass.
- Corpus still 1 commit deep.
</verification>

<success_criteria>
The replay/resume control-flow state is migrated with ordering provably preserved, corpus and replay harness both green, and no string/comment content changed.
</success_criteria>

<output>
Create `.planning/phases/10-app-state-de-globalization/10-02-SUMMARY.md` when done.
</output>
