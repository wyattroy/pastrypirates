---
phase: 10-app-state-de-globalization
plan: 07
type: execute
wave: 7
depends_on: ["10-06"]
files_modified: []
autonomous: false
requirements: [GLOBAL-02]
user_setup: []

must_haves:
  truths:
    - "The one inline `onclick=\"revealMyRecipe()\"` attribute fires with no ReferenceError (GLOBAL-02 criterion 2)."
    - "A representative set of the 40 `.onclick=` closures (createRoom, joinRoom, startGame, sendResponse, leaveGame, toggleTimer) click through with no new no-undef/ReferenceError (GLOBAL-02/D-10)."
    - "A full solo game plays to end-of-voyage with a clean console (ROADMAP criterion 4)."
    - "A two-tab multiplayer game (host + guest) still syncs, read from the rendered CAPTAINS panel per the render-only-guest methodology (ROADMAP criterion 4/D-10)."
    - "A host-refresh replay fast-forward reconstructs state cleanly after de-globalizing the replay control-flow names (D-06 behavioural leg)."
  prohibitions:
    - "NO-CAPTURE: verification is observational only - never run `--capture`."
    - "NO-CODE-EDIT: this is a verification plan; if a defect is found, it routes to a gap-closure plan, not an ad-hoc edit here."
  artifacts: []
  key_links:
    - "Guests are render-only under host authority; their local `state.game` is intentionally stale - read the rendered CAPTAINS panel, not `state.game.players[].pos` (the Phase 9 finding)."
---

<objective>
Prove GLOBAL-02 and ROADMAP criterion 4 in a real browser: the one inline handler fires, a representative closure surface clicks through cleanly, a full solo game and a two-tab multiplayer game both remain playable with no new no-undef/ReferenceError, and a host-refresh replay reconstructs state cleanly. The corpus is structurally blind to app-state/UI control flow (D-08), so this browser leg is the necessary, non-optional complement to the green `--verify`.

Purpose: The headless suite cannot exercise DOM handlers or multiplayer control flow. D-10 reduces GLOBAL-02 to exactly this observable surface; this plan runs it, reusing the Phase 9 two-tab harness and the render-only-guest note. Per D-12, Safari is not required at this boundary (the roadmap schedules Safari re-verification at Phases 11 and 12) - Chrome is sufficient here.

Output: a recorded Chrome click-through transcript (solo + two-tab + host-refresh replay) confirming a clean console.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-app-state-de-globalization/10-VALIDATION.md
@.planning/phases/09-networking-layer-watcher-cleanup/09-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Chrome click-through - inline attr, closures, solo, two-tab MP, host-refresh replay</name>
  <what-built>
    All 46 app-state globals are de-globalized behind `state.*`, published by reference through the PP bridge; `revealMyRecipe` stays a reachable `function`; the read-only `window.__pp_app_state_debug` hook is documented; `npm test` (including the new state contract check) is green. This gate confirms the game still plays end-to-end in Chrome, which the corpus cannot verify.
  </what-built>
  <how-to-verify>
    Preconditions: `npm test` is green and `git log --oneline -- 'scripts/fixtures/determinism/*.jsonl' | wc -l` returns 1. Start `python3 -m http.server 8000` (or `npm start`). Drive clicks via `dispatchEvent(new MouseEvent(...))` on real handler elements (avoids the pixel-misclick lesson from Phase 9), keeping the console open throughout.

    1. Inline attribute (GLOBAL-02 criterion 2): reach a state where the "Check my recipe" button renders and fire its inline `onclick="revealMyRecipe()"`. Confirm the recipe reveals and the console shows no ReferenceError.
    2. Closure surface: click through createRoom, joinRoom, startGame, sendResponse, leaveGame, and toggleTimer on real elements. Confirm each acts and the console stays clean (no new no-undef/ReferenceError).
    3. Full solo game (criterion 4): play a solo game to end-of-voyage. Console clean throughout; end-of-voyage renders.
    4. Two-tab multiplayer (criterion 4): open two Chrome tabs with distinct `pp_id` set sequentially (the Phase 9 shared-localStorage procedure). Host creates a room, guest joins, host starts and plays at least one full turn including a move that triggers narration. Confirm the guest observes it live by reading the rendered CAPTAINS panel (NOT `state.game.players[].pos` - the guest's local game is intentionally stale under host authority). Confirm `window.__pp_app_state_debug` returns a snapshot object on both tabs and the console is clean.
    5. Host-refresh replay (D-06 behavioural leg): in a game with recorded decisions, refresh the host tab and confirm the replay fast-forward reconstructs state cleanly (`state.replaying` gates render/broadcast; no divergence, no console error).
    Record the transcript verbatim in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" if all five legs pass with a clean console, or describe the failure (which leg, which handler, the exact console error) so it can route to a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click -> handler -> app state | Handlers must resolve `state.*` and named functions after de-globalization; a broken resolution surfaces only in a live DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Denial of Service (game unplayable) | a missed migration surfacing as a runtime ReferenceError only in the browser | high | mitigate | The five-leg click-through (inline attr, closures, solo, two-tab, host-refresh replay) is the standing behavioural gate the corpus cannot provide; a clean console is required to pass. |
| T-10-03 | Information Disclosure | the debug hook mutating live state during the MCP session | low | mitigate | `__pp_app_state_debug` returns a snapshot copy (verified in 10-06); reading it during this session cannot alter authoritative state. |
</threat_model>

<verification>
- Inline `revealMyRecipe()` attribute fires, no ReferenceError.
- createRoom/joinRoom/startGame/sendResponse/leaveGame/toggleTimer click through clean.
- Full solo game to end-of-voyage, clean console.
- Two-tab multiplayer syncs (read the rendered CAPTAINS panel).
- Host-refresh replay reconstructs state cleanly.
- Precondition holds: `npm test` green; corpus still 1 commit deep.
</verification>

<success_criteria>
GLOBAL-02 and ROADMAP criterion 4 are proven in Chrome: every handler surface works, solo + two-tab multiplayer + host-refresh replay all play with a clean console, and the transcript is recorded.
</success_criteria>

<output>
Create `.planning/phases/10-app-state-de-globalization/10-07-SUMMARY.md` when done.
</output>
